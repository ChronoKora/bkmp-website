# BKMP — Projekt-Leitfaden für Claude Code

Statische Website (kein Build-Schritt, kein package.json) + Supabase-Backend + Vercel-Deploy. Öffentliche Investoren-/Marktplatz-Seite UND das Idle-Dorf-Spiel leben in derselben Codebasis. Der Besitzer ist nicht-technisch und lässt Claude-Code-Sessions den gesamten Code schreiben — Commits/Deploys übernimmt er selbst.

## ⚠️ Laufendes Redesign — Phasen-Status

Am 17.07.2026 wurde ein **vollständiges professionelles Redesign** freigegeben (Plan: `C:\Users\David\.claude\plans\delightful-floating-frog.md`). Rollout ist **phasenweise mit Zwischen-Deployments** — jede Phase wird fertig, geprüft, getaggt (`git tag phase-N-complete`), bevor die nächste beginnt.

**Aktueller Stand:** Phase 1 bis 5.5 komplett fertig und ausführlich verifiziert. **Phase 5.6 (modularer Kampfrenderer mit transparenten Drachen) wurde am 19.07.2026 wieder vollständig zurückgebaut** (Nutzer-Entscheidung "Wir rudern hier zurück... baue wieder die alten ein!") — die alte Seite-an-Seite-Kampfdarstellung (#idleBattlefield, Dorf links/Drache rechts) ist wieder die einzige Darstellung für ALLE Drachen inkl. Feuer/Wasser/Wind/Yaksha-Boss. Entfernt: `js/prototype/bkmp-proto-battlefield.js` (gelöscht), der zugehörige HTML-Block + `<script>`-Tag in index.html, der CSS-Abschnitt in style.css, die Hook-Aufrufe in js/ui/bkmp-hud.js. Die konvertierten Greenscreen-WebMs liegen weiterhin unangetastet in `assets/prototype-battle/` (nicht mehr referenziert, bewusst nicht gelöscht - siehe Chat, falls der Nutzer sie endgültig entfernen will). `sql/20260719-restrict-normal-dragon-rotation.sql` ist dadurch hinfällig (wurde ohnehin nie ausgeführt) - Grund für die Rotationsbeschränkung ist entfallen. Phase 5.4 (einheitliche Tab-UI) ist teilweise offen (Erfolge/Dorf-Skins/Dungeon/Turm/Arena/Gilde/Gilden-Tech/Gildenboss/Bestenliste/Drachenzucht/Kampfbereich-Feinschliff noch nicht überarbeitet, siehe Tasks). **Phase 6.0 (finale QA/Stabilisierung) am 19.07.2026 abgeschlossen** — voller Bericht als Artefakt, kein Release-Blocker gefunden, lokaler Release-Kandidaten-Commit `1ccfa38` erstellt (nicht gepusht, nicht deployed, keine SQL ausgeführt) - der Kampfrenderer-Rückbau kam DANACH und ist noch nicht in einem RC-Commit. Kein Kollisions-Hinweis aktiv.

⚠️ **Phase 5.0 hat einen kritischen, nie deployten Bug gefunden+gefixt** (siehe Abschnitt "App-Modus-Promotion" unten) — `html.bkmp-app-mode` wurde global, aber mehrere Regeln waren noch mit der Annahme "nur /app" geschrieben und haben dadurch Header/Footer/Nav/alle 10 Marketing-Sektionen sowie einen Vollbild-Splash dauerhaft über die normale Website gelegt. Vor jeder weiteren Arbeit an `html.bkmp-app-mode`-Selektoren in style.css: prüfen, ob der Selektor eine Klasse trifft, die AUCH auf der normalen Website vorkommt (z.B. `.joke-card`, `header`, `footer`, bare Tag-Namen) - falls ja, gehört er entweder auf `.zone-game` (Stimmungs-Chrome) oder `html.bkmp-app-mode.zone-game` (echtes /app-Vollbild-Verhalten wie Splash/Header-Ausblenden/Basis-Schriftgröße), nicht auf `html.bkmp-app-mode` allein.

| Phase | Inhalt | Status |
|---|---|---|
| 1 | Inline-Scripts extrahiert, `defer`, Lazy-Loading, Cache-Header, Bild-Pipeline, Tokens (inert), `/sql/`-Ordner | ✅ fertig |
| 2 | idledorf.js in `/js/systems/*.js` zerlegen (siehe Modul-Karte unten) | ✅ fertig (2a+2b+2c) |
| 3 | design-tokens.css tatsächlich verdrahten, geteilte UI-Komponenten (`js/ui/bkmp-ui-components.js`) | ✅ fertig |
| 4 | Marketing-/Investoren-/Marktplatz-Redesign (professionelle Stimmung) | ✅ fertig |
| 5.0 | App-Modus-Ideen einzeln übernehmen/verwerfen (Touch-Targets, Bottom-Nav, Kampf-Juice, Popup-Chrome, HUD-Portrait) | ✅ fertig |
| 5.1 | UI-Fixes, Performance (Pause-bei-Unsichtbarkeit, Effektmodus), Prestige-Zeremonie | ✅ fertig |
| 5.2 | Kompaktes HUD/Nav-Overlay, Erfolge-/Bestenlisten-250er-Deckel entfernt | ✅ fertig |
| 5.3 | 5 gemeldete Spieler-Bugs behoben (Runen-Doppelausrüstung, Ei-Dungeon-Regen, Feedback-Button, Klick-Erkennung, Liber-Fixstufe) + Runen überleben Prestige | ✅ fertig |
| 5.4 | Einheitliche UI für alle Idle-Dorf-Tabs | 🟡 teilweise (Shell/Upgrades/Skilltree/Prestige/Runen fertig; Erfolge/Dorf-Skins/Dungeon/Turm/Arena/Gilde/Gilden-Tech/Gildenboss/Bestenliste/Drachenzucht/Kampfbereich-Feinschliff offen, siehe Tasks) |
| 5.5 | Gemeinsames Reward-Presentation-System an alle Belohnungsmomente angeschlossen | ✅ fertig |
| 5.6 | Modularer Kampf-Ebenensystem mit transparenten Drachen | ❌ zurückgebaut (19.07., Nutzer-Entscheidung) — alte Seite-an-Seite-Darstellung wieder Standard für alle Drachen |
| 6.0 | Finale QA, Stabilisierung, Release-Kandidat | ✅ fertig (19.07.2026, siehe QA-Artefakt in der Session) — RC-Commit `1ccfa38`, nicht gepusht |
| 6 (Admin) | Admin & idle-stream-mini.html ans neue System anpassen | offen |
| 7.0 | Mobile-First-Redesign Idle-Dorf-Kampfbereich (Auftrag: 42-Abschnitte-Brief) | 🟡 erste Abnahmestufe fertig (20.07.2026), wartet auf visuelle Freigabe — siehe Abschnitt weiter unten |
| 7.1 | Kompaktes Overlay/Höhen-Fix Idle-Dorf (Auftrag: 30-Abschnitte-Brief) | 🟡 erste Abnahmestufe fertig (21.07.2026), wartet auf visuelle Freigabe — siehe Abschnitt weiter unten |
| 7.2 | Automatisierte Playwright-E2E-Testsuite (Auftrag: 43-Abschnitte-Brief) | ✅ Stufe 1 komplett fertig (21.07.2026): alle 14 Auftrags-Punkte gebaut, 67 Tests (60 grün + 6 korrekt uebersprungen auf Desktop-Projekt + 1 bewusst als bekannter Bug verfolgter `test.fail()`), 2 echte App-Bugs gefunden+sofort gefixt, 2 echte App-Bugs gefunden+bewusst offen gelassen (Doku), nichts gepusht/deployed — siehe Abschnitt weiter unten. Reste des 43-Abschnitte-Auftrags (Abschnitte 21-36) noch offen, brauchen neue Freigabe |
| 7.3 | Kritischer Repair "Navigations-Buttons verschwinden" (Auftrag: 28-Abschnitte-Brief, Video-Beweis) | ✅ fertig (23.07.2026) — echter Root-Cause-Bug gefunden+gefixt (nicht der vermutete), neue Playwright-Regressionssuite (24 Tests, rot vor/grün nach dem Fix), volle Regression sauber, nichts gepusht/deployed — siehe Abschnitt weiter unten |
| 7 | Barrierefreiheit, totes CSS löschen (u.a. doppelte `.idle-dorf-tab`-Basisregel), restliche Assets auf WebP | offen |

**⚠️ Parallele Sessions:** Dieses Repo hat keine Worktree-Isolation — mehrere Claude-Code-Sessions können gleichzeitig hier arbeiten. **Vor jeder größeren Änderung an `idledorf.js`, `style.css` oder den Dateien unter `/js/` diesen Abschnitt auf einen Hinweis wie „⚠️ Phase 2 läuft, nicht parallel an idledorf.js arbeiten" prüfen** (wird hier eingetragen, sobald eine Phase mit hohem Kollisionsrisiko startet, und nach Abschluss wieder entfernt).

## Phase 7.0 — Mobile-First-Redesign Kampfbereich (20.07.2026, erste Abnahmestufe)

Nutzer-Auftrag: vollständiges Mobile-First-Redesign des Idle-Dorf-Kampfbereichs (42-Abschnitte-Brief, nicht in CLAUDE.md dupliziert). Ausdrücklich nur die **erste Abnahmestufe** implementiert (Mobile Shell/Top-HUD/Schlachtfeld/Dorf/Drache/HP-Leisten/Schadenszahlen/Kampf-Log-Sheet/Bottom-Nav/Mehr-Sheet/Safe-Area/Portrait/Querformat) — alle anderen Tabs (Upgrades/Drachenzucht/Dungeon/Runen/Skilltree/Prestige/Erfolge/Turm/Arena/Gilde/Gilden-Tech/Gildenboss/Bestenliste/Dorf-Skins) folgen erst nach visueller Freigabe durch den Nutzer. **Nichts gepusht/deployed, keine Spielwerte/Formeln geändert.**

**Root Cause (vor Implementierung dokumentiert):** Es gab **drei parallele, nie vereinheitlichte Mobil-Navigationssysteme**: (A) `js/prototype/bkmp-proto-compact-hud.js` (lief unconditional an ALLEN Breiten, versteckte die alte HUD/Tabs/Stufenleiste), (B) `js/core/bkmp-app-mode-bootstrap.js`s `bkmpIdleSetupMobileTabOverflow()` (verschob echte Tab-Buttons in `#idleAppMoreSheet`, lief nur bei `BKMP_APP_MODE` oder ≤760px), (C) die statische Desktop-Gruppierung `.idle-dorf-tab-group` (nur `min-width:761px`). System A hatte zudem **kein `position:fixed`** (schwebte im Dokumentfluss statt am unteren Rand zu kleben) und **keine safe-area-Behandlung**.

**Umbau:** System B liefert jetzt das echte, gruppierte, safe-area-bewusste Bottom-Sheet (`#idleAppMoreSheet`, Gruppen an den Nutzer-Auftrag angeglichen: Fortschritt/Kampf & Rang/Gilde/Sammlung); System A wurde auf 4 Haupt-Tabs reduziert (Kampf/Upgrades/Drachen/Dungeon, statt vorher 5 andere) und sein "Mehr"-Button proxied jetzt einfach auf den echten `#idleAppMoreBtn` von System B (eigenes Dropdown bleibt nur als Desktop-Fallback, falls System B nie initialisiert wurde, z.B. bei Erstladung im breiten Querformat). System C (Desktop-Gruppierung) unverändert. Neu: `.idle-combat-log-toggle`/`#idleCombatLogSheet` (Kampf-Log als Bottom-Sheet, Portal-Pattern wie beim Mehr-Menü, Log-Logik selbst unangetastet).

**Nachtrag 21.07.2026 (Spieler-Report Crocodilandy927, öffentliches Feedback: "nervt auf dem PC, wenn man immer auf Mehr drücken muss"):** System A (`bkmpProtoChudInit()`) lief bis dahin tatsächlich **unconditional auf JEDER Breite inkl. vollem Desktop** und versteckte dabei die alte, vollständige Tableiste (`#idleDorfTabs`) - System C's Desktop-Gruppierung (siehe oben, eigentlich für genau diesen Fall gebaut) kam dadurch nie zum Zug. Fix: `bkmpProtoChudInit()` bricht jetzt ganz am Anfang ab, wenn weder `BKMP_APP_MODE` noch `matchMedia('(max-width:760px)')` zutrifft (identische Bedingung wie `bkmpIdleSetupMobileTabOverflow()` in System B) - auf breitem Desktop bleibt die alte Anzeige seitdem einfach unangetastet sichtbar, das komplette Prototyp-Nav (inkl. `bkmpIdleCombatLogInit()`-Aufruf am Ende der Funktion, dessen Toggle-Button ohnehin nur per CSS in der mobilen Media Query sichtbar ist) wird auf Desktop gar nicht mehr aktiviert. Per `getComputedStyle` bei 1280px (alte Tableiste sichtbar, neues Nav `display:none`) und 390px (unverändert wie vorher: neues Nav sichtbar, 6 Buttons, alte Tableiste versteckt) verifiziert.

**Drei reale CSS-Spezifitäts-/Stacking-Bugs beim eigenen Testen gefunden+gefixt** (nicht nur behauptet — via `getComputedStyle`/CSSOM-Rule-Matching verifiziert, siehe Kommentare an den jeweiligen Stellen in style.css/js/ui/bkmp-hud.js):
1. `html.bkmp-app-mode #idlePanelKampf .idle-battlefield{min-height:0!important}` (Phase-13-Fix, Flex-Wachsen-Mechanik) schlug ein naives `height:clamp(...)` — Fix: gleiche Selektor-Spezifität + `!important`, nur als Unter-/Obergrenze um das bestehende Flex-Wachsen herum, kein fester `height`.
2. `.idle-dorf-overlay .idle-dorf-card > *{position:relative;z-index:1}` fängt jedes direkte Karten-Kind in einem eigenen Stacking-Context — `#bkmpProtoCompactNav`s `position:fixed` griff dadurch nicht; Fix: ID-Selektor (`#bkmpProtoCompactNav.bkmp-proto-nav`) statt reiner Klasse.
3. Gleiche Ursache verhinderte, dass `#idleCombatLogSheet` (verschachtelt in `#idlePanelKampf`, selbst kein direktes Karten-Kind) über `.joke-buttons` malen konnte — Fix: Portal-Pattern (`bkmpIdleCombatLogEscapeToOverlay`, gleiches Muster wie `bkmpProtoChudEscapeToOverlay`), hängt das Sheet beim Öffnen zu `#idleDorfOverlay` um.
4. Reine `max-width`-Media-Queries erreichen breite Querformat-Telefone nicht (oft 700–950px breit) — betroffene Regeln auf `@media (max-width:Npx), (max-height:500px) and (orientation:landscape)` erweitert (Komma = ODER).

**Verifiziert** (Browser-Vorschau, kein echter Spieler-Login möglich — siehe unten): 360×800, 390×844, 430×932, 844×390 (Querformat), 1920×1080 (Desktop-Regression) — jeweils per `getComputedStyle`/`getBoundingClientRect` UND Screenshot geprüft: Battlefield wächst korrekt auf ~54dvh (min 340px/max 620px), Bottom-Nav korrekt `position:fixed`, Mehr-Sheet öffnet gruppiert + schließt bei Tab-Klick, Kampf-Log-Sheet öffnet/schließt sauber ohne Überlappung, Desktop bei 1920px unverändert (Nav bleibt `position:relative`, Battlefield behält alte Flex-Wachsen-Höhe).

**Nicht geprüft:** echter Kampf mit realer Spieler-Session — Login/Account-Erstellung ist für Claude-Code-Sessions untersagt (Sicherheitsregel), daher nur strukturelle/CSS-Verifikation über eine manuell sichtbar geschaltete leere Kampf-Szene möglich, keine echten Sprites/HP-Werte/Tick-Loop getestet. Vor dem Deploy nochmal am echten Gerät gegenprüfen.

**Bekannte offene Kleinigkeiten (kein Blocker für diese Stufe):** die zwei sich überschneidenden unconditional `html.bkmp-app-mode .idle-battlefield`-Blöcke (~Zeile 9033/9467) wurden bewusst NICHT konsolidiert (Risiko bei blindem Zusammenführen ohne visuelle Kontrolle) — bleibt Phase-7-Aufräumarbeit. `bkmpIdleSetupMobileTabOverflow()`s eigener Breiten-Trigger (`BKMP_APP_MODE || matchMedia(max-width:760px)`) prüft nur bei Seitenladung, nicht bei Rotation — lädt jemand die Seite bereits im breiten Querformat, bekommt das "Mehr"-Menü den einfacheren Desktop-Dropdown-Fallback statt des gruppierten Sheets (funktioniert, nur weniger poliert).

### Weitere Live-Bugfixes in derselben Nacht (20.07.2026, während auf Freigabe gewartet wurde)

Der Nutzer meldete parallel fünf unabhängige, echte Spieler-Bugs (nicht Teil von Phase 7.0), alle gefixt und committed, noch nicht gepusht:

1. **Offline-Fortschritt zeigte nur "1 Min." nach echtem Laptop-Zuklappen:** ein noch offener 4s-Debounce-Speicher-Timer (`bkmpIdleQueueSync`) wird vom OS beim Einschlafen nur eingefroren, nicht abgebrochen — er feuert beim Aufwachen sofort und überschreibt `last_seen_at` mit "jetzt", BEVOR die Offline-Abfrage den alten Wert lesen kann. Fix: `bkmpIdleCancelPendingSyncTimer()` (idledorf.js), verwirft einen noch offenen Timer vor jedem Offline-Claim (in `bkmpIdleOpenModal`/`bkmpIdleCatchUpAfterHidden`), ohne Daten zu verlieren.
2. **Balken/Griff-Reste mitten in Skilltree/Gilde/anderen Tabs auf normaler Desktop-Breite (von mehreren Spielern gemeldet):** `#idleAppMoreSheet` ist ein direktes Kind von `.idle-dorf-card`, die dauerhaft eine (visuell unsichtbare, aber technisch aktive) `transform` traegt (`.joke-overlay.visible .joke-card{transform:translateY(0) scale(1)}`) - macht die Karte zum Bezugsrahmen für alle `position:fixed`-Kinder. Der bestehende `transform:none!important`-Fix griff nur bei schmaler Breite/echtem App-Modus, nicht auf normaler Desktop-Breite. Fix: `#idleAppMoreSheet` wird jetzt unconditional beim Laden zu einem echten Geschwister von `.idle-dorf-card` auf `#idleDorfOverlay`-Ebene umgehängt (`bkmp-app-mode-bootstrap.js`, gleiches Portal-Muster wie beim Kampf-Log-Sheet).
3. **"Akt X erreicht!"-Popup (Phase 5.5 "Aktfreischaltung", 19.07.) auf Nutzerwunsch komplett entfernt** — der Trigger-Block in `bkmpIdleHandleDragonDefeated` (idledorf.js, rief `bkmpRewardPresent` beim Überschreiten einer 10-Stufen-Akt-Grenze auf) wurde ersatzlos gelöscht. Die zugrundeliegende Akt-Gruppierung (Stufenwahl-Popup, `bkmpIdleFormatStage`) bleibt unangetastet, nur die Popup-Benachrichtigung ist weg.
4. **Ausgerüstete Runen verschwanden nach kurzem Raus-/Reintappen (Live-Meldung im Admin-Feedback, Spieler "Bärli", zweimal am 20.07.: "alle Runen gelöscht" / "diesmal alles Schildrunen"):** anders als Gold/Prestige/frisch gedroppte Runen (alle drei werden bei `visibilitychange`(hidden)/`beforeunload` erzwungen nachgeholt) lief `bkmpRuneToggleEquip`s DB-Schreibvorgang (`updatePlayerRuneEquipped`) bisher als reines fire-and-forget-Promise ohne jede Nachverfolgung — brach der Browser die PATCH-Anfrage beim Verstecken/Verwerfen der Seite (auf Mobilgeräten häufig, fühlt sich für den Spieler wie normales Wechseln an) mitten im Flug ab, kam die Ausrüstung nie in der DB an; der nächste echte Neuladevorgang zeigte dann wieder den alten (unausgerüsteten) Stand. Fix: `bkmpRunePendingEquipWrites` (neue Map, `js/core/bkmp-idle-state.js`) verfolgt jede unbestätigte Ausrüsten-Schreibanfrage; `bkmpRunePersistEquip()`/`bkmpRuneFlushPendingEquipsNow()` (`js/systems/bkmp-runes.js`) schicken sie bei jedem Verstecken/Schließen erneut ab (idempotentes PATCH, unschädlich bei Doppel-Zustellung) — gleiche Stelle wie die drei anderen Flush-Aufrufe in idledorf.js. Auch `bkmpRuneNormalizeDuplicateEquips()` nutzt jetzt denselben Pfad. Verifiziert per injiziertem Mock-Test im Browser (blockierte erste Anfrage → Flush löst erneuten Versuch aus → Map bleibt leer nach echtem Erfolg), da ohne echten Spieler-Login kein realer Netzwerk-Race nachstellbar ist.
5. **AFK-Belohnung wieder 0 nach 90 Min. UND nach nur 15 Min. (ChronoKora, 20.07., trat trotz des bereits gefixten "1 Min."-Bugs erneut auf):** der bestehende Fix (`bkmpIdleCancelPendingSyncTimer`, siehe Punkt 1) hilft nur, wenn der Abbruch VOR dem Feuern des eingefrorenen 4s-Timers laeuft - arbeitet der Browser beim Aufwachen den ueberfaelligen Timer in einer FRUEHEREN/gleichzeitigen Aufgabe ab als den `visibilitychange`-Listener selbst (Reihenfolge nicht browseruebergreifend garantiert), schreibt `bkmpIdleFlushSync()` `last_seen_at=JETZT` bereits BEVOR der Abbruch ueberhaupt zum Zug kommt - unabhaengig davon, wie lange der Spieler wirklich weg war (erklaert, warum sowohl 90 als auch nur 15 Minuten betroffen waren, derselbe Rennen-Ausgang, nicht die Dauer, entscheidet). Fix: `bkmpIdleLastSeenSyncBlockedUntil` (Zeitstempel statt Timer-Wettlauf, `js/core/bkmp-idle-state.js`) wird beim Verstecken des Tabs gesetzt (also VOR jedem moeglichen Einschlafen, garantiert vor dem Freeze) und sperrt `bkmpIdleFlushSync()`s `last_seen_at`-Schreibvorgang fuer bis zu 15s, laeuft aber von selbst aus (kein "fuer immer blockiert"-Risiko) und wird sofort nach einem abgeschlossenen Offline-Claim (`bkmpIdleCatchUpAfterHidden`/`bkmpIdleOpenModal`) wieder freigegeben. Verifiziert per injiziertem Mock-Test (Schreibvorgang waehrend der Sperre bleibt aus, direkt nach Freigabe klappt er wieder normal).
6. **AFK-Belohnung IMMER NOCH 0 nach dem Punkt-5-Fix (ChronoKora, 20.07., diesmal mit Screenshot belegt):** der Screenshot zeigte die Karte diesmal korrekt mit "46 Min." (Punkt 5 wirkt also) - aber alle Belohnungen 0 UND ein "Niederlage gegen Yaksha der Drachenboss"-Popup. Ursache: `api/claim-idle-offline-progress.js` (server-seitige, vereinfachte Kampf-Simulation) kannte bisher nur Angriff/Verteidigung/Krit, nicht aber die drei Magie-Skilltree-Effekte, die im Live-Tick tatsaechlich mitkaempfen (Brand `elem_fire`, Blitzschlag `elem_lightning`, passive Dorf-HP-Regeneration aus `shield_regen`/`repair_speed_pct`/`heal_pct`, siehe `bkmpIdleTick` in idledorf.js) - bei starker Magie-Zweig-Investition kann das den Unterschied zwischen "gewinnt live problemlos" und "verliert hier in der Simulation" ausmachen. Verschaerft durch: ein verlorener Kampf fiel bisher IMMER nur genau 1 Stufe zurueck - bei einem breiten unmodellierten Kraftunterschied konnte das die GESAMTE Offline-Zeit in einer langen Rueckzugs-Schleife verbrennen, ohne je einen gewinnbaren Kampf zu finden. Fix (zwei Teile, beide in derselben Datei): (a) `idle_skill_nodes` wird jetzt zusaetzlich geladen (gleiches `sbFetch`-Muster wie `idle_dragons`/`idle_game_config`, keine neue Migration noetig - `skill_allocations` ist bereits Teil der Zeile) und speist Brand/Blitzschlag als erwarteten Bonusschaden pro Treffer sowie die Dorf-Regeneration als Netto-Abzug vom Gegenschlag-Schaden ein; (b) exponentiell wachsender Rueckzug bei mehreren Niederlagen in Folge (1, 2, 4, 8, ...) statt immer nur 1 Stufe - findet einen gewinnbaren Kampf in ~log2(Stufe) statt bis zu Stufe-vielen Einzelschritten. Verifiziert per eigenstaendigem Node-Testskript mit gemocktem `fetch` (drei Szenarien: normaler Sieg unveraendert / Kraftwand ohne Magie-Boni entkommt jetzt schnell statt 0 Belohnung / dieselbe Wand MIT vollen Magie-Boni gewinnt sogar hoehere Stufen) - kein neuer DB-Zugriff, keine Migration, sofort wirksam nach Deploy.
7. **"Keine Gems und Essenzen mehr offline" (Spieler Crocodilandy927, 20.07., ueber das neue oeffentliche Feedback-Board entdeckt) - dritter, eigenstaendiger Bug in derselben Offline-Simulation:** Standarddrachen haben laut `idle_dragons` `crystal_reward_base=0`/`essence_reward_base=0` - Kristalle/Essenz kommen normalerweise nur von Boss/Miniboss ODER von den "seltenen" Drachen (Schattendrache/Wuffdrache, live 8% Wuerfelchance PRO Kill, `idle_game_config.rare_spawn.chancePct`). Die Offline-Simulation kannte diese Chance nicht (wurde beim Bau des Simulators bewusst als "nur Rauschen" auf `nie` gesetzt) und hat den `rare_spawn`-Konfigwert nicht einmal geladen - bei kurzen/mittleren Abwesenheiten mit vielen simulierten Standard-Kills ist das aber kein Rauschen mehr, sondern ein spuerbar fehlender Einnahmepfad. Fix: `rare_spawn` wird jetzt mitgeladen, Erwartungswert des "rare"-Pools (Durchschnitt aller Belohnungsfelder × Chance) wird pro simuliertem Nicht-Boss/Miniboss-Kill als ROHWERT aufsummiert und ERST NACH der ganzen Schleife einmal gerundet (nicht pro Kill - bei ~0,08 erwarteten Kristallen pro Kill wuerde `Math.round()` pro Kill sonst konsequent auf 0 abrunden und die gesamte Ausbeute verschwinden lassen, obwohl die Summe ueber viele Kills laengst spuerbar ist). Verifiziert per Node-Testskript: 176 simulierte Kills -> 14 Kristalle/14 Essenz statt vorher 0.
8. **"Nervt auf dem PC, wenn man immer auf Mehr druecken muss" (Crocodilandy927, ueber das Feedback-Board) - siehe Phase-7.0-Abschnitt oben ("Nachtrag 21.07.2026") fuer die volle Erklaerung:** System A lief unconditional auf jeder Breite inkl. Desktop und versteckte dabei System C's eigentlich fuer genau diesen Fall gebaute volle Desktop-Tableiste. Auf Nutzerentscheidung ("kompaktes Nav nur noch auf schmalen Bildschirmen") gefixt.
9. **"Keine Gems und Essenzen mehr, obwohl Gebaeude eigentlich unabhaengig produzieren sollten" (Nutzer-Nachfrage 21.07., nachdem Bug 7 oben die Kampf-Offline-Simulation schon gefixt hatte) - eigenstaendiger, ZWEITER Rundungsfehler, diesmal nicht offline-spezifisch:** `bkmpIdleAccrueProductionBuildings()` (idledorf.js) lief nicht nur beim Zurueckkommen, sondern bei JEDEM Drachen-Kill waehrend aktivem Spielen (ueber `bkmpIdleRefreshLiveTabs()`, 300ms-Drosselung, sobald der Upgrades-Tab offen ist) - bei einem einzelnen ~300ms-Aufruf ist der Ertrag einer niedrigstufigen Kristallmine/Manaquelle (Basisrate nur 3-4/Std.) weit unter 1, wurde per `Math.floor()` verworfen, ABER die Zeitmarke sprang trotzdem sofort auf "jetzt" - der Bruchteil war fuer immer weg, bei JEDEM Kill erneut. Bei durchgehend geoeffnetem Upgrades-Tab waehrend des Kampfes produzierten diese Gebaeude dadurch faktisch NIE etwas, unabhaengig vom Offline-Status. Fix: Zeitmarke wird nur noch um die Zeit vorgerueckt, die tatsaechlich in eine ganze Einheit umgewandelt wurde - der Rest bleibt fuer den naechsten Aufruf stehen. Der 72h-Offline-Deckel bleibt unangetastet (bei echter Kappung springt die Marke weiter hart auf "jetzt", ueberschuessige Zeit verfaellt bewusst). Per eigenstaendigem Node-Testskript verifiziert (4 Szenarien: 20min aktives Spielen im 300ms-Takt lieferte vorher 0, jetzt korrekt 1 Kristall; normale Offline-Rueckkehr/72h-Deckel/gemischtes Szenario unveraendert korrekt).
10. **Arena-Gewinnchance ergaenzt (Spieler-Wunsch, Minecraft-Server-Chat 21.07., Lilith57 an Kora: "ich mag nicht so gern raetselraten, ob ich wen schlagen kann") - keine Bug, neues Feature:** `bkmpArenaGetOpponents()` (supabase.js) laedt jetzt zusaetzlich `attack/defense/hp` aus `idle_player_state` mit (dieselben Spalten, die `bkmpIdleRecomputeEffectiveStats()` ohnehin schon bei jeder Aenderung inkl. Runen/Skilltree/Prestige/Upgrades dorthin spiegelt - keine Migration noetig). Jede Gegner-Karte in der Arena (`js/systems/bkmp-arena.js`) zeigt jetzt `🎲 ~NN%` geschaetzte Gewinnchance, berechnet mit EXAKT derselben Formel wie `arena_attack()` serverseitig (`supabase-idle-arena.sql`: `Angriff*2+Verteidigung+HP*0.3`, dann `eigene_Staerke/(eigene+gegnerische_Staerke)`) - die Anzeige kann also nie von der echten Chance abweichen, bleibt aber ausdruecklich ein Erwartungswert (der Kampf selbst ist weiter ein `random()`-Wurf, keine Garantie, per Tooltip erklaert). Farbcodierung (gruen/gelb/rot ab 60%/40%) ist rein zusaetzlich, der Prozentwert traegt die eigentliche Information. Formel per Node-Testskript gegen drei Szenarien verifiziert (gleich stark ~50%, deutlich staerker/schwaecher jeweils weit ueber/unter 50%).
11. **"Lvl 2018 -> nach F5 Lvl 2001, keine Moeglichkeit mehr in irgendein Menu zu gehen" (Baerli, 21.07., ueber Admin-Feedback gemeldet) - kritischer Datenverlust-Bug, nicht nur Kosmetik:** `bkmpIdleMergeRemoteSpendableFields()` (idledorf.js, urspruenglich nur gegen Mehrfach-Tab-Konflikte auf der Twitch-Seite gedacht, laeuft seit dem Gildenboss-Belohnungs-Fix vom 16.07. aber UNGATED auch auf der normalen Hauptseite nach jedem Gildenboss-Sieg, siehe bkmp-guild.js) erkannte einen Prestige-Reset auf einer anderen Session bisher rein daran, dass der frisch vom Server geladene Stand niedriger war als der eigene lokale Baseline-Snapshot (`remote.level < baseline.level`). Das ist aber KEIN verlaessliches Reset-Signal - genau derselbe Zustand entsteht auch voellig normal, wenn eine zweite Session (zweiter Tab/Geraet, oder die eigenen Saves liefen laut dem "Speichern fehlgeschlagen"-Toast vom 17.07. eine Weile ins Leere) einen aelteren, noch nicht nachgezogenen Snapshot ueberschrieben hat - KEIN Prestige. Das bisherige bedingungslose Ueberschreiben hat in diesem Fall echten, laengst erspielten Fortschritt (Level/XP/Ressourcen/Skillpunkte/Stufe) geloescht - nicht auf einen runden Reset-Wert, sondern auf genau den Stand des letzten stalen Snapshots (erklaert den unrunden Sprung 2018 -> 2001 statt auf Stufe 1). Fix: verlangt jetzt zusaetzlich eine Bestaetigung ueber den tatsaechlichen, monoton wachsenden `prestige_level`-Zaehler (idle_prestige_state) - nur ein ECHTER Prestige-Aufstieg erhoeht ihn; ein blosser Ruecksprung ohne erhoehten Zaehler ueberschreibt jetzt nichts mehr, der naechste eigene Autosave schreibt den bereits korrekten, hoeheren lokalen Stand einfach fort. Die vermutete Ursache fuer das gemeldete Menu-Einfrieren direkt danach (nicht mit Stack-Trace bestaetigt, aber plausibel): die abrupte Zustandsmutation loeste `bkmpIdleSpawnDragon()`/Re-Renders auf einer durch den falschen Rollback inkonsistenten `current_dragon_index`/`highest_dragon_index`-Kombination aus. Per eigenstaendigem Node-Testskript verifiziert (3 Szenarien: staler Snapshot ohne Prestige-Erhoehung ueberschreibt jetzt korrekt NICHTS mehr; echter Prestige-Reset mit erhoehtem Zaehler wird weiterhin korrekt uebernommen; normaler eingeholter Stand loest gar keine Pruefung aus).

12. **Kampfbereich bei 1920x1080 gequetscht (ChronoKora, 21.07., mit Video belegt und per `ffmpeg`-Frame-Extraktion sowie eigener `getComputedStyle`-Nachstellung bestaetigt) - drei zusammenwirkende Desktop-Regressionen, keine davon war je auf schmalen Mobil-Breiten sichtbar:**
    - **(a) `#idleCombatLogSheet` ohne Basis-Regel:** das mobile Kampf-Log-Bottom-Sheet (Phase 7.0) hatte nur fuer seine Kindelemente (`-handle`/`-header`) eine `display:none`-Basisregel, nicht fuer sich selbst - rendete dadurch auf JEDER Breite als normaler 171px hoher Block mitten im Kampf-Tab statt unsichtbar zu bleiben. Fix: `.idle-combat-log-sheet` in dieselbe Basisregel aufgenommen (style.css).
    - **(b) Flex-Wachsen-Trick lief auf Breiten, wo er nicht funktionieren kann:** `html.bkmp-app-mode #idlePanelKampf .idle-battlefield{flex:1 1 auto!important;min-height:0!important}` (Phase 13) laesst das Schlachtfeld nur dann sinnvoll wachsen, wenn `.idle-dorf-card` selbst eine ECHTE, von aussen vorgegebene Hoehe hat (nur in zwei Faellen der Fall: echter Vollbild-App-Modus `html.bkmp-app-mode.zone-game`, oder die mobile Kartenregel `@media(max-width:640px)` - beide setzen explizit `height:100vh/100dvh`). Auf normaler Desktop-Website-Breite hat die Karte dagegen nur `max-height:95vh` (Inhalts-Hoehe mit Deckel, kein echter "Wachstumsraum") - "min-height:0!important" hat dort nur die alte, verlaessliche 220px-Untergrenze der Basisregel ausgehebelt, ohne dass echtes Wachstum moeglich war. Ergebnis bei 1920x1080: Schlachtfeld fiel auf ~153px zurueck (verifiziert per `getComputedStyle`). Fix: die Phase-13-Regeln auf `.zone-game` verengt + eigene Kopie fuer die mobile Kartenbreite ergaenzt - auf normaler Desktop-Breite greift jetzt wieder die alte 220px-Untergrenze, kein Quetschen mehr (nach Fix (a)+(b) zusammen: 415px statt 153px).
    - **(c) Nutzer-Vorschlag direkt umgesetzt** ("Mach doch einfach oben alle Button viel schmaler von der Hoehe?"): eigene, dichtere Fassung der 4 Tab-Gruppen-Buttons (BASIS/SAMMLUNG/HERAUSFORDERUNGEN/GILDE) ab `min-width:1000px` (bewusst enger als die bestehende 761px-Grenze, um Tablet-Touch-Ziele nicht anzutasten) - `min-height` 44px->32px, kleineres Padding/Schriftgroesse, engere Gruppen-Abstaende. `min-height:44px` war fuer Touch-Ziele gedacht, auf echtem Desktop mit Maus nicht noetig. Reduziert die Gesamthoehe der Tab-Leiste spuerbar, ohne mobile/Tablet-Groessen zu beruehren.
    - Alle drei Fixes zusammen per `getComputedStyle`/`getBoundingClientRect` bei exakt 1920x1080 verifiziert: Schlachtfeld 415px (vorher 153px), Footer-Buttons ("Als App installieren"/"Für Streamer"/"Schließen") enden jetzt sauber innerhalb der Karte statt das Schlachtfeld zu ueberlappen (vorher im Video sichtbar abgeschnitten). Der urspruengliche Report kam als 27-Sek.-Bildschirmaufnahme (`ffmpeg`-Standbilder daraus gezogen, da kein direktes Video-Tool verfuegbar ist - funktioniert als Notloesung fuer kuenftige Video-Reports).
    - **Noch offen (Nutzer-Idee, bewusst NICHT in diesem Hotfix umgesetzt):** hierarchische Haupt-Kategorien (z.B. "Gilde" anklicken -> erst dann Gildenboss/Gilden-Tech als Unterpunkte zeigen, statt alle Tabs flach nebeneinander) - eine groessere Informationsarchitektur-Aenderung, die alle Spieler-Navigationsgewohnheiten betrifft und eigene visuelle Iteration braucht, siehe offene Aufgabe "Phase 5.4: Kampfbereich final angleichen".

13. **Direkter Folge-Fund derselben Nacht (Screenshot: "Jetzt fehlen die Button komplett?") - eigenstaendiger, von Fix 12 unabhaengiger Bug, reproduziert per gezieltem Breiten-Wechsel-Test:** `bkmpIdleSetupMobileTabOverflow()` (bkmp-app-mode-bootstrap.js) wird bisher nur EINMAL bei Seitenladung geprueft (`window.BKMP_APP_MODE || matchMedia(max-width:760px)`) - der Kommentar dort begruendete das mit "das Fenster wird waehrend eines laufenden Groessenwechsels i.d.R. nicht offen sein", trifft aber nicht den eigentlich haeufigen Fall: viele Browserfenster starten schmal (<=760px) und werden ERST DANACH maximiert, oft laengst bevor der Spieler das Idle-Dorf ueberhaupt zum ersten Mal oeffnet. Die Funktion entfernt beim ersten (schmalen) Lauf unwiderruflich die echten `.idle-dorf-tab-group`-Container aus dem DOM (`.remove()`) und verschiebt die einzelnen Tab-Buttons in die mobile Flach-Struktur (6 Haupt-Tabs direkt in die Leiste, restliche 9 ins "Mehr"-Sheet) - eine SPAETERE Verbreiterung des Fensters kann diese zerstoerten Container nicht zurueckholen: die Desktop-CSS-Gruppierung findet nichts mehr zum Greifen, "Kampf" haengt einzeln mit 0x0px in der Leiste, die restlichen 9 Tabs stecken im fuer Desktop nie vorgesehenen "Mehr"-Sheet fest. Reproduziert per `getComputedStyle`: Seite bei 700px laden -> ohne Neuladen auf 1339px verbreitern -> `.idle-dorf-tab-group`-Anzahl faellt von 4 auf 0. Sauberes Zurueckbauen waere aufwaendig (Original-Gruppierung muesste komplett neu rekonstruiert werden, ohne die von idledorf.js gesetzten Klick-Listener zu verlieren) - stattdessen ein einfacher, robuster Fix: ein debounced `resize`-Listener (400ms) loest ein `location.reload()` aus, sobald die Seite tatsaechlich (wieder) Desktop-Breite hat, NACHDEM die zerstoerende Umbaumassnahme schon gelaufen ist. Echter App-Modus ausgenommen - dort ist die kompakte Nav die absichtlich dauerhafte Struktur, unabhaengig von der Breite. Per Browser-Test verifiziert (schmal laden -> `resize`-Event ausloesen -> Seite laedt automatisch neu -> alle 4 Gruppen + Kampf-Button mit echter Groesse (78x31px) sind zurueck).
    - **Nachbesserung, selbe Nacht (Spieler-Test: "im Localhost sehe ich sie, aber nicht auf der normalen Website" - Fix per Screenshot-Beweis auf localhost bestaetigt funktionierend, auf bkinvestment.de trotz identischem, bereits gepushtem Code weiterhin fehlend):** die erste Fassung nutzte ein DAUERHAFTES `sessionStorage`-Flag ("nur einmal pro Tab-Sitzung neu laden"). Der Testtab lief die ganze Nacht ueber fuer wiederholte Tests - die Sperre war beim erneuten Auftreten bereits "verbraucht", der echte, neue Fall wurde faelschlich uebersprungen (identisches Symptom wie der urspruengliche Bug, diesmal durch die eigene Absicherung verursacht). Fix: zeitbasiertes Abklingen (10s, per `Date.now()`-Zeitstempel statt Boolean) statt Dauersperre - verhindert weiterhin eine Neulade-Schleife waehrend eines einzelnen Zieh-Vorgangs, heilt sich aber bei jedem SPAETEREN echten Auftreten in derselben Tab-Sitzung erneut selbst. Per Browser-Test verifiziert: 20s alter (simuliert "abgestandener") Zeitstempel + der alte Boolean-Schluessel gesetzt -> Fix greift trotzdem, laedt neu, alle 4 Gruppen zurueck.

## Phase 7.1 — Kompaktes Overlay/Höhen-Fix Idle-Dorf (21.07.2026, erste Abnahmestufe)

Nutzer-Auftrag: 30-Abschnitte-Brief (nicht in CLAUDE.md dupliziert) - Idle-Dorf-Overlay wirkte auf Desktop/Laptop wie eine lange gescrollte Webseite statt einem kompakten Idle-Game, Kampf war erst nach viel Chrome (HUD/Nav/Stufenleiste/eigener Effektmodus-Block) sichtbar. Ausdrücklich nur **erste Abnahmestufe** (Overlay-Höhenlogik, Header/Nav/Stufensteuerung kompakter, Effektmodus ins HUD integriert, Kampffeld-Höhenformel, Footer verkleinert, Height-Breakpoints) - restliche Tabs/hierarchische Navigations-Idee folgen erst nach visueller Freigabe. **Nichts gepusht/deployed, keine Spielwerte/Formeln geändert** (git-Status vor Beginn geprüft: sauber, `main`, deckungsgleich mit `origin/main`, Commit `3dde403`).

**Root-Cause-Befund (vor Umsetzung dokumentiert, per echter `getBoundingClientRect`-Messung mit injiziertem Mock-Zustand, da kein echter Login möglich - siehe unten):**
1. `#idleFxModeBtn` lebt als eigenständiges Geschwister-Element zwischen `.idle-stage-bar` und `.idle-battlefield` - obwohl der Button selbst klein ist (CSS bereits `width:fit-content` ab 481px), belegt er dadurch eine komplette eigene ~44px-Zeile.
2. `.idle-stage-bar` ist zwar als Flex-Zeile gebaut, brach aber auf typischen Laptop-Breiten (1366px) auf zwei Zeilen um (Label-Text + 3 Buttons passten knapp nicht nebeneinander).
3. `.idle-battlefield` hatte eine feste `min-height:220px` ohne Bezug zur tatsächlich verfügbaren Bildschirmhöhe - wuchs stattdessen einfach mit dem Karteninhalt mit.
4. HUD (Level/XP/Stats/Ressourcen) + Tab-Navigation (4 Gruppenkarten) + Stufenleiste zusammen ergaben ~340-390px "Chrome" vor dem eigentlichen Kampf, bei einer Karte, die durch `max-height:95vh` gedeckelt ist - der Kampf rutschte dadurch weit nach unten.
5. Footer (App installieren/Für Streamer/Schließen) nutzte unveraendert die normale Button-Groesse (0.55rem/1.2rem Padding), obwohl ein redundanter kompakter Schließen-Button (`#idleDorfCloseX`, oben rechts) bereits existiert.

**Umsetzung (erste Stufe):**
- `.idle-battlefield`: feste `min-height:220px` durch `clamp(240px, 36dvh, 460px)` ersetzt, plus `@media(max-height:800px)`/`@media(max-height:650px)`-Verschärfungen (reine Groessenangabe, keine Kampflogik).
- `#idleFxModeBtn` wird jetzt per Portal-Muster (identisch zu `#idleAppMoreSheet`/`#idleCombatLogSheet`) am Ende von `bkmpIdleRenderHud()` (Desktop-Zweig, `js/ui/bkmp-hud.js`) in `.idle-hud-top` eingehängt - keine eigene Zeile mehr, Klick-Listener aus `bkmpFxInit()` bleiben unangetastet (verschoben, nicht neu erzeugt).
- `bkmpIdleRenderStageBar()` (idledorf.js): Beschriftung gekürzt ("Insgesamt erreichte Stufen" → "Gesamt", "Nächster Boss" → "Boss", Button-Texte gekürzt) - reine Textänderung, dieselben drei bereits berechneten Werte, keine neue Logik.
- `.idle-stage-bar`: `flex-wrap:nowrap` ab 600px (reines `min-width:0` auf dem Label reicht NICHT, da Flexbox die Zeilenaufteilung anhand der ungeschrumpften/hypothetischen Breite entscheidet, bevor `flex-shrink` greift) + Label bricht nicht mehr um, sondern kürzt sich selbst per Ellipse (`text-overflow:ellipsis`) - robust auch wenn die Zahlen im Spielverlauf weiter wachsen.
- HUD/Tab-Navigation: Innenabstände/Zeilenabstände weiter verdichtet (aufbauend auf der `min-width:1000px`-Verdichtung von Bug 12 oben).
- `.idle-village-sprite`/`.idle-dragon-sprite` `max-width` angehoben (420→520px, 260→320px, Boss/Miniboss entsprechend) - **beim Verifizieren als wirkungslos erkannt**: eine bereits bestehende, globale `html.bkmp-app-mode`-Regel setzt `max-width:100%`, die Sprites füllen also schon vorher ihren vollen Container (42-46% der Kampffeld-Breite, liegt bereits im vom Auftrag empfohlenen Bereich 38-55%) - Änderung bleibt als harmloser Fallback stehen, macht aber aktuell keinen sichtbaren Unterschied (**offener Punkt**, siehe unten).
- Footer (`.idle-dorf-card .joke-buttons`): verkleinert, **zweiter, ähnlicher Bug beim eigenen Testen gefunden**: eine globale `html.bkmp-app-mode .joke-card .btn-ja/.btn-nein{min-height:44px;padding:.7rem 1.2rem}`-Regel (fuer Touch-Ziele im App-Modus gedacht, laengst global aktiv) hatte durch die zusaetzliche `.btn-ja`/`.btn-nein`-Klasse hoehere Spezifitaet als die erste Fassung des Footer-Fixes und hat ihn komplett ausgehebelt, ohne dass es beim ersten Hinsehen auffiel (Groesse sah unveraendert aus) - Fix: Selektor um `.btn-ja`/`.btn-nein` erweitert + `!important`.

**Verifiziert** (kein echter Login möglich, siehe unten - stattdessen realistischer Mock-Zustand direkt über die ECHTEN Render-Funktionen `bkmpIdleRenderHud()`/`bkmpIdleRenderStageBar()` injiziert, per `getBoundingClientRect`/CSSOM-Rule-Matching bei 1920×1080/1366×768/1280×720/390×844 gemessen):

| Bereich | Vorher (1366×768) | Nachher |
|---|---|---|
| HUD gesamt | 148px | 140px |
| Tab-Navigation | 130px | 121px |
| Stufenleiste | 63px, **2 Zeilen** | 58px, **1 Zeile** |
| Effektmodus | eigene 44px-Zeile | 0px (im HUD integriert) |
| Footer | 42px | 30px |
| Kampffeld | 295px (fix) | 295px bei 768px Höhe, aber jetzt `clamp()`-basiert: 415px bei 1080px Höhe, 276px bei 720px Höhe (skaliert erstmals mit der Fensterhoehe) |

Bei 1280×720 (Auftrag nennt das explizit als Ziel-Laptop-Größe) passt jetzt alles inkl. Footer ohne Clipping/internes Scrollen in die Karte. Mobile Shell (390×844) unangetastet geprüft: kompakte Bottom-Nav/HUD weiterhin `display:flex/block`, alte Desktop-HUD/Tabs weiterhin `display:none`, keine Konsolenfehler.

**Wichtige Einschränkung (ehrlich dokumentiert):** Kein echter Spieler-Login möglich (Sicherheitsregel) - alle Messungen laufen über einen realistischen, direkt in die ECHTEN Render-Funktionen injizierten Mock-Zustand (Level 271, echte Ressourcen-/Stat-Werte, echter Dorf-/Drachen-Sprite), nicht über einen echten Spielstand. Echte Bildschirm-Screenshots des Overlays waren technisch nicht möglich - das `computer{screenshot}`-Werkzeug läuft bei diesem `position:fixed`-Vollbild-Overlay zuverlässig in ein 30s-Timeout (dieselbe, bereits aus Phase 7.0 bekannte Tool-Einschränkung, kein Code-Bug). Alle Zahlen oben sind reale `getBoundingClientRect()`-Messungen, keine Schätzungen - aber ohne visuelle Screenshot-Bestätigung sollte der Nutzer vor einer Freigabe selbst einen Blick auf `localhost:8094` (oder nach dem nächsten Push auf die echte Seite) werfen.

**Offene Punkte fuer die naechste Stufe (bewusst nicht in dieser Stufe umgesetzt):**
- Dorf-/Drachen-Sprite-Groesse: die `max-width`-Anhebung war wirkungslos (siehe oben) - eine wirksame Vergroesserung wuerde die Container-Breite (`.idle-village{width:42%}`/`.idle-dragon`) selbst anfassen, eine groessere strukturelle Aenderung, die mehr visuelle Iteration verdient.
- Hierarchische Haupt-Kategorien in der Navigation (z.B. "Gilde" anklicken -> erst dann Unterpunkte) - bereits in Bug 12 oben als bewusst zurueckgestellte Idee vermerkt.
- Alle anderen Tabs (Upgrades/Skilltree/Prestige/Runen/Erfolge/etc.) folgen erst nach Freigabe dieser Stufe, wie im Auftrag verlangt.

### Phase 7.1 Stufe 3 (21.07.2026) — Raidbanner, Navigation, Kampfszene final komprimiert

Nutzer-Auftrag: Nachbesserung nach Stufe-1-Feedback (Raidbanner zu dominant, HUD/Navigation weiterhin zu viele Zeilen, Dorf/Drache wirken wie zwei getrennte Screenshots, permanenter Footer, Kampf beginnt zu weit unten). **Nichts committed/gepusht/deployed, keine Spielwerte/Raidboss-Timer/Kampfwerte veraendert.** Git-Status vor Beginn geprueft: sauber, `main`, Commit `4e3cbe6`.

**Umsetzung:**
- **Raidbanner** (`js/systems/bkmp-raid.js`, `bkmpRaidRenderJoinBanner()`): kompletter Umbau von 3 Zeilen (~100px) auf 1 Zeile (49px, Ziel 46-58px). Ursache der 3. Zeile: `.raid-join-banner-participants{flex-basis:100%}` erzwang IMMER eine eigene volle Zeile - jetzt Teil desselben Textblocks wie der Titel. Minimieren-Button neu (`bkmpRaidBannerSetMinimized()`), Zustand pro Raid-ID in sessionStorage (nicht global) - ein neuer Raidboss zeigt den Hinweis automatisch wieder voll. Verifiziert: Minimiert-Test zeigt Text korrekt `display:none`, Hoehe 49px->44px.
- **Navigation** (`style.css`, `@media(min-width:761px)`): die 4 Gruppenkarten (Basis/Sammlung/Herausforderungen/Gilde) sind entfernt - `.idle-dorf-tab-group` bleibt jetzt auch auf Desktop `display:contents` (wie mobil), alle 15 Tabs flach in `.idle-dorf-tabs` als EINEN Flex-Wrap-Container. Bewusst OHNE neuen "Mehr"-Dropdown-Mechanismus - der Auftrag erlaubt explizit "Alternativ darf eine zweite kompakte Zeile verwendet werden", und ein zweiter, von System B unabhaengiger DOM-Verschiebe-Mechanismus haette das erst in derselben Nacht gefundene "Fenster startet schmal, Buttons verschwinden beim Verbreitern"-Bugmuster erneut riskiert (siehe Bug 13 oben). Verifiziert: 15/15 Tabs sichtbar, echte 2 Zeilen (65px) - eine anfangs gemessene "3. Zeile" war nur der 2px-Hover-Lift-Effekt des aktiven Tabs, kein echter Umbruch.
- **Kampfszene** (`style.css`, zwei konkurrierende `html.bkmp-app-mode .idle-village/.idle-dragon`-Bloecke gefunden, siehe unten): die eigene Goldrahmen-Karte JEDES Containers war der Hauptgrund fuer "zwei getrennte Screenshots" - beide sitzen jetzt randlos auf dem gemeinsamen Kampffeld-Hintergrund. Dorf 46%/Drache 52% (vorher symmetrisch 46%/46%).
- **HUD** (`js/ui/bkmp-hud.js`, `bkmpIdleRenderHud()`): Kampfwerte+Ressourcen zu einem gemeinsamen `.idle-hud-flow`-Container zusammengefuehrt (vorher zwei komplett getrennte Zeilen) - spart eine Zeilenabstand-Ebene, Ressourcen bleiben farblich abgesetzt statt durch eine echte Zeile getrennt.
- **Footer**: `#idleDorfClose` ("Schliessen"-Button) entfernt - rein redundant, `#idleDorfCloseX` oben rechts deckt dieselbe Funktion bereits ab (eigener, unangetasteter Klick-Listener). App-Install/Streamer als kleine Textlinks statt Pillenbuttons (36px statt vorher 42px).
- **Height-Breakpoints**: von 2 auf 3 Stufen erweitert (900/780/700px), verdichten jetzt zusaetzlich HUD/Navigation/Stufenleiste, nicht nur das Kampffeld.

**Zwei weitere Selektor-Spezifitaets-Konflikte gefunden** (gleiches Muster wie die zwei bereits in Stufe 1 gefundenen - nicht nur behauptet, per CSSOM-Rule-Matching verifiziert):
1. Village/Dragon-Breite: zwei fast identische `html.bkmp-app-mode .idle-village, .idle-dragon`-Bloecke existieren (Zeile ~9200 UND ~9654) - der ERSTE (aeltere) setzt noch einen eigenen Goldrahmen, der SPAETERE (gleiche Spezifitaet, gewinnt per Cascade-Reihenfolge) macht beide bereits randlos. Die tatsaechlich wirksame Regel war also schon "richtig", nur die Breiten-Werte (46%/46%) mussten auf 46%/52% angepasst werden - direktes Aendern der ERSTEN (nicht wirksamen) Regel haette gar nichts bewirkt.
2. (Bereits in Stufe 1 dokumentiert, hier nur als Muster-Bestaetigung relevant.)

**Vorher/Nachher bei 1366×768** (per realistischem Mock-Zustand + injizierten Test-Markup direkt ueber die echten Render-Funktionen gemessen, `getBoundingClientRect`):

| Bereich | Ziel (Auftrag) | Nachher |
|---|---|---|
| Raidbanner | 46-58px, 1 Zeile | **49px, 1 Zeile** |
| HUD | 94-110px | **94px** |
| Navigation | 42-82px, max. 2 Zeilen | **65px, 2 Zeilen** (15/15 Tabs sichtbar) |
| Stufenleiste | 46-52px | **52px** |
| Kampffeld-Y (mit Raidbanner) | spaetestens Y=400 | **Y=365** |
| Kampffeld-Y (ohne Raidbanner) | - | **Y=317** |
| Footer | kein permanenter Footer / max. 8-16px | **36px** (kleine Textlinks, kein Pillenbutton mehr) |

**Verifiziert:** keine Konsolenfehler bei 1366×768/390×844, Mobile-Shell unangetastet (Bottom-Nav/kompaktes HUD weiterhin `display:flex/block`, Desktop-Tabs weiterhin `display:none`), Syntax-/Brace-Check sauber.

**Nicht umgesetzt (bewusst, fuer eine spaetere Stufe):** HP-Balken bleiben in ihrer bisherigen Position unter dem Sprite statt als echtes Overlay auf dem Motiv - eine echte Ueberlagerung braucht sorgfaeltige Kontrast-/Lesbarkeitspruefung pro Drachen-Skin, mehr visuelle Iteration als in dieser Runde moeglich. Schadenszahlen-Trefferpunkte nicht gesondert nachgemessen (keine strukturelle Aenderung an ihrer Positionierungsbasis vorgenommen, daher kein neues Risiko erwartet, aber auch nicht explizit neu verifiziert). Kein neuer "Mehr"-Dropdown fuer die Navigation (siehe oben, bewusste Risikoabwaegung).

## Dringender Hotfix: Tab-Navigation verschwindet/nicht klickbar (23.07.2026)

Auslöser: eine Flut echter, datierter Admin-Feedback-Meldungen (22.07.2026) von mehreren unabhängigen Spielern (Crocodilandy927, RandomAuto, Nilo3628, Superdelfin, FlinkerBoy7289, u.a.) - Tab-Buttons (Kampf, Gilde, Runen, Bestenliste, Prestige, Skilltree, Drachenzucht, Turm, Dungeon, Weltboss) verschwunden/nicht klickbar, teils durch harten Reload behoben, teils NICHT, dazu zwei "Handy"-Meldungen (GUI kaputt/Drachen nicht sichtbar, Raidbanner falsch formatiert). Nutzer-Auftrag: **"Wir müssen diesen Fehler ausmerzen. Dringend. Vorschläge?"**, danach explizit **"Punkt 3"** (die saubere strukturelle Lösung statt eines weiteren Pflasters) gewählt, dazu **"prüfen ob die Probleme behoben sind... Button jederzeit vorhanden... auf allen Größen... gleichzeitig Handy-GUI auf allen gängigen Größen prüfen und ggf. anpassen"**.

**Root Cause (per echter Live-Rekonstruktion im Browser bestätigt, nicht nur vermutet):** `bkmpIdleSetupMobileTabOverflow()` (bkmp-app-mode-bootstrap.js) und `bkmpProtoChudInit()` (bkmp-proto-compact-hud.js, "System A"/"System B" aus Phase 7.0) liefen bisher jeweils NUR EINMAL beim Laden, geprüft gegen `window.BKMP_APP_MODE || matchMedia(max-width:760px)`. Traf das zum Ladezeitpunkt zu (Browserfenster noch schmal/nicht maximiert, Snap-Layout, Zoom/DPI, oder echter App-Modus/installierte PWA - dort per Absicht IMMER kompakt), verschoben sie UNWIDERRUFLICH 9 der 15 Tabs in ein "Mehr"-Sheet und entfernten die alten Gruppen-Container aus dem DOM. Ein früheres Resize→Reload-Sicherheitsnetz fing das nur unvollständig auf: lief NIE im echten App-Modus (dort bewusst deaktiviert - erklärt direkt die "Handy/installierte App"-Meldungen, `idledorf.webmanifest` setzt `start_url:"/app"`, jeder App-Start landet also permanent im Kompakt-Zustand ohne jede Selbstheilung), reagierte nur auf ein echtes `resize`-Event (nicht auf Zoom/DPI/Snap) und riss den Spieler bei jedem Treffer per vollem Reload aus dem Spielstand. **Zusätzlich, live im Browser gefunden (nicht vorher vermutet):** dieselbe Breiten-Bedingung (`max-width:760px`) ignorierte breite-aber-flache Quer-Handys (`max-height:500px and orientation:landscape`, z. B. 844×390) - die CSS-Fixierung von `#bkmpProtoCompactNav` kennt diesen Fall bereits (Phase-7.0-Kommentar in style.css), die JS-Umschaltung bisher nicht: auf so einem Gerät zeigte sich die alte Desktop-Tableiste (94px hoch) in einem nur 390px hohen Fenster statt der dafür gebauten Kompakt-Nav - das erklärt die "Handy-GUI kaputt"-Meldungen unabhängig vom Reload/Breiten-Fall oben.

**Zwei Test-Sackgassen beim eigenen Verifizieren erkannt und verworfen, bevor der echte Fund stand:** (1) das geöffnete "Mehr"-Sheet schien bei einer ersten Messung mit `transform:translateY(583px)` (= geschlossen) hängenzubleiben trotz `.open`-Klasse - lag an einem Automatisierungs-Werkzeug-Artefakt (CSS-Transitions laufen in diesem nicht fokussierten Browser-Tab nicht weiter, `transition:none` erzwingen zeigte sofort den korrekten `translateY(0)`-Zielwert) - kein echter Bug, verworfen. (2) `getBoundingClientRect()`-Werte der Tab-Buttons wirkten zunächst wie 5 verschiedene Zeilen - lag daran, dass jeder Testklick per `active`-Klasse die Breite des jeweils aktiven Buttons ändert und dadurch den Flex-Umbruch aller ANDEREN Buttons verschiebt - reines Mess-Artefakt der Klick-Reihenfolge, kein echter Bug.

**Fix (kompletter struktureller Umbau, nicht nur ein weiteres Pflaster):** beide Funktionen in je zwei Teile gesplittet - ein EINMALIGER, unbedingter Aufbau (`bkmpIdleBuildMobileTabOverflowUi()` / `bkmpProtoChudSetup()`, baut nur inerte Struktur/Listener, entscheidet nichts über Sichtbarkeit) und ein JEDERZEIT sicher wiederholbarer Soll-Zustand-Abgleich (`bkmpIdleSyncTabOverflowForViewport()` / `bkmpProtoChudSyncVisibility()`, per Cache-Vergleich nur bei echtem Wechsel aktiv). Beide Abgleich-Funktionen laufen jetzt beim Laden UND bei jedem (200ms debounced) `resize`-Event, in BEIDE Richtungen - kein Reload mehr nötig, kein dauerhaft verlorener Tab mehr möglich, greift jetzt auch im echten App-Modus (die Absicht "App = immer kompakt" bleibt erhalten, da `bkmpIdleWantCompactTabNav()`/`bkmpProtoChudWantCompact()` bei `BKMP_APP_MODE` ohnehin unabhängig von der Breite `true` liefern). Der frühere `sessionStorage`-Reload-Notnagel ist komplett entfernt. Die Breiten-Bedingung ist um `(max-height:500px) and (orientation:landscape)` erweitert (identisch zur bestehenden CSS-Bedingung), behebt den Quer-Handy-Fund. Die alten `.idle-dorf-tab-group`-Container müssen dabei nicht mehr rekonstruiert werden - seit Phase 7.1 Stufe 3 ist `.idle-dorf-tab-group{display:contents}` ohnehin unbedingt (nicht mehr nur mobil), die 15 Tabs sind strukturell schon eine flache Liste, nur ihre Zuordnung zu `#idleDorfTabs` vs. `#idleAppMoreSheetGrid` ändert sich noch.

**Verifiziert (Browser-Live-Test, echte `resize`-Events per `dispatchEvent` ausgelöst, da das Automatisierungswerkzeug selbst keine nativen `resize`-Events feuert - bekannte, bereits aus Phase 7.0 dokumentierte Werkzeug-Einschränkung):**
- Schmal laden (390×844) → alle 15 Tabs korrekt aufgeteilt (6 Kompakt-Nav + 9 im "Mehr"-Sheet) → OHNE Reload auf 1920×1080 verbreitert → alle 15 Tabs korrekt zurück in der flachen Desktop-Leiste, jeder einzeln als sichtbar+klickbar+korrekt-eingehängt bestätigt (`getBoundingClientRect`+Eltern-Element-Check).
- Dieselbe Seite (URL/Query-String unverändert, also wirklich KEIN Reload) danach auf 375×700 zurückverschmälert → korrekt wieder zu 6+9 zusammengeklappt.
- Echter App-Modus (`?app=idledorf`) bei 1600×900: bleibt korrekt dauerhaft kompakt (Absicht erhalten), "Mehr"-Sheet öffnet, Klick auf einen der 9 sekundären Tabs (Gilde) landet nachweislich per `elementFromPoint`-Hittest auf dem echten Button UND aktiviert das echte Panel.
- Landschafts-Quer-Handy 844×390: vorher alte Desktop-Leiste in 390px-hohem Fenster (Bug bestätigt), nachher korrekt Kompakt-Nav, `position:fixed`, `height:63px`, innerhalb des Viewports, kein horizontales Überlaufen.
- Zusätzlich 360×800, 412×915, 1366×768 geprüft: kein horizontales Überlaufen (`scrollWidth`), Bottom-Nav korrekt `position:fixed` an allen Mobil-Größen, Desktop-Breiten (1366×768) unverändert/unbeeinflusst von der neuen Quer-Handy-Bedingung (da `max-height:500px` dort nicht zutrifft).
- Keine Konsolenfehler über die gesamte Testreihe (alle Breiten, beide Richtungen, App-Modus, Klick-Interaktionen).
- Syntax-Check (`node --check`) für beide geänderten Dateien sauber.

**Nicht geprüft (ehrlich dokumentiert, gleiche Einschränkung wie durchgehend in dieser Datei):** kein echter Spieler-Login möglich (Sicherheitsregel) - alle Klick-/Sichtbarkeitstests liefen über ein manuell sichtbar geschaltetes Overlay ohne echten Spielstand, echte Drachen-/Dorf-Sprites (brauchen echte Bild-Daten aus `bkmpIdleState`) wurden nicht separat neu verifiziert, da an ihrer Rendering-Logik nichts geändert wurde. Cache-Busting `?v=20260723-reversiblenav1` für beide Dateien in index.html gesetzt (einzige HTML-Datei, die sie einbindet).

**Nachtrag 2 (23.07.2026) - zwei weitere Wiederauftreten-Fälle gefunden+gefixt, nachdem der Nutzer den Fehler erneut meldete:**
1. **"Kurz raus aus dem Game... Spiel zu auf der Website was gemacht und Spiel wieder geöffnet"** (In-Page-Schliessen/Wiederöffnen des Idle-Dorf-Fensters, KEIN Tab-/App-Wechsel): `bkmpIdleSyncTabOverflowForViewport()`/`bkmpProtoChudSyncVisibility()` liefen bis dahin nur beim Laden und bei echten `resize`-Events - ein reines Schliessen/Wiederöffnen des Fensters (`bkmpIdleOpenModal()`/`bkmpIdleCloseModal()` in idledorf.js) "resized" nie im Sinne eines Browser-Events, obwohl sich waehrend des Geschlossenseins am echten Fenster durchaus etwas geaendert haben kann (Zoom-Stufe, ein Scrollbalken erscheint/verschwindet durch anderen Seiteninhalt). Fix: `bkmpIdleOpenModal()` (idledorf.js) ruft jetzt bei JEDEM Oeffnen beide Sync-Funktionen unbedingt auf (typeof-Guard, da die beiden Dateien erst NACH idledorf.js laden), direkt nachdem das Overlay sichtbar wird.
2. Zusaetzlich (praeventiv, gleiches Muster): ein `visibilitychange`-Listener in beiden Dateien (`bkmp-app-mode-bootstrap.js`/`bkmp-proto-compact-hud.js`) ruft die Sync-Funktionen jetzt auch bei echter Tab-/App-Rueckkehr (`!document.hidden`) erneut auf - deckt den Fall ab, dass ein waehrend eines backgroundeten Tabs gefeuerter `resize`-Callback eine unzuverlaessige Breite gelesen haben koennte.

Beide Fixes sind bewusst als **breite Sicherheitsnetze** angelegt (bei JEDER Rueckkehr zum Fenster/Tab neu abgleichen, unabhaengig von der genauen Ursache) statt einen einzelnen, moeglicherweise unvollstaendigen Ausloeser gezielt zu patchen - `bkmpTabOverflowCurrentlyCompact`/`bkmpProtoChudCompactActive`-Cache sorgt weiterhin dafuer, dass bei tatsaechlich unveraendertem Zustand nichts unnoetig neu aufgebaut wird.

**Verifiziert (Browser, echter Funktionsaufruf, kein Mock der eigentlichen Nav-Logik):** Nav-Zustand kuenstlich in den "kompakt"-Fehlzustand versetzt (simulierter fehlerhafter `matchMedia`-Wert, wie er waehrend eines Hintergrund-Resizes auftreten koennte), dann die ECHTE `bkmpIdleOpenModal()`-Funktion aufgerufen (Login-/Netzwerk-Abhaengigkeiten minimal gemockt, um keine echten Schreibvorgaenge auszuloesen) - bestaetigt, dass die Tab-Leiste bereits VOR einem absichtlich ausgeloesten spaeteren Fehler in der Funktion korrekt auf 16 Elemente (15 Tabs + Mehr-Button, versteckt) zurueckgesetzt wird. `visibilitychange`-Pfad separat verifiziert: kuenstlich `document.hidden` auf `true` dann `false` gesetzt + Event gefeuert - Nav korrekt wiederhergestellt. `node --check` fuer alle drei geaenderten Dateien (`idledorf.js`, `bkmp-app-mode-bootstrap.js`, `bkmp-proto-compact-hud.js`) sauber, keine echten Konsolenfehler (der eine gesehene Fehler war die selbst ausgeloeste Test-Exception). Cache-Busting `idledorf.js?v=20260723-navreopenfix1`, `bkmp-app-mode-bootstrap.js?v=20260723-visibilityresync1`, `bkmp-proto-compact-hud.js?v=20260723-visibilityresync1` gesetzt.

## Dorf-Skin per Code einlösbar: "KalleJunior Dorf" (23.07.2026) — Code fertig, SQL noch nicht ausgeführt

Neues Feature, Nutzerwunsch "bei Dorfskins ein Dorfskin hinzufügen der nur mit Code einlösbar ist. Baue das einfach im aktuellen Plüshsystem Code mit ein." Nach Rückfrage entschieden: Nutzer liefert Name + Video selbst nach ("KalleJunior Dorf", `assets/village/kallejuniordorf.mp4`, 1912×1084, 8s, h264 - direkt vom Nutzer per Datei-Anhang geliefert, unverändert übernommen, Dateigröße 3,7 MB liegt im normalen Bereich der übrigen Skins).

**Ausgangslage (per Recherche-Subagent bestätigt):** `plushie_codes` war strukturell fest auf GENAU EINEN Belohnungstyp verdrahtet (`plushie_id text not null`, kein Discriminator) - Redemption lief über `api/redeem-plushie-code.js` (Service-Role, unconditional `insert into user_plushies`). Dorf-Skins haben ein komplett getrenntes Ownership-Modell (`idle_player_village_skins`, Unique-Key `(auth_user_id, skin_id)` statt nur `name_key`) und bereits einen echten Präzedenzfall für einen "wie freigeschaltet"-Diskriminator (`idle_village_skins.unlock_type`: `free/purchase/achievement/boss_drop/real_money`) - aber noch keinen `code`-Wert.

**Umsetzung (generalisierter Mechanismus, nicht nur ein Einzelfall):**
- **`sql/20260723-code-redeemable-village-skins.sql`** (neu, **noch nicht ausgeführt**):
  - `plushie_codes` bekommt `reward_kind` (Default `'plushie'` - bestehende Codes laufen unverändert weiter), `skin_id` (FK auf `idle_village_skins`, nullable), `plushie_id` wird nullable. Ein Check stellt sicher, dass immer genau eines der beiden Ziel-Felder passend zu `reward_kind` gesetzt ist.
  - `idle_village_skins.unlock_type`-Check erweitert um `'code'` (gleiches Muster wie die `real_money`-Erweiterung in `supabase-real-money-purchases.sql`).
  - Legt den Skin `kallejuniordorf` an (`unlock_type='code'`, `unlock_hint='Nur per Einlöse-Code erhältlich.'`, `sort_order=16`, `image_file=''` - gleiches Muster wie die meisten reinen Video-Skins, `video_file` hat ohnehin Vorrang).
- **`api/redeem-plushie-code.js`:** verzweigt jetzt auf `reward_kind` - bei `'plushie'` exakt das bisherige, unveränderte Verhalten; bei `'village_skin'` Besitzprüfung + Insert gegen `idle_player_village_skins` (nutzt `user.id` aus dem bereits vorhandenen Auth-Check als `auth_user_id`).
- **`admin.html`** (Plüshie-Codes-Seite): neue "Belohnungsart"-Auswahl (Plüshie/Dorf-Skin) über dem bestehenden Formular, schaltet zwischen Plüshie- und Dorf-Skin-Dropdown um (Dorf-Skin-Katalog wird jetzt erstmals überhaupt in admin.html geladen - es gab bisher KEINE Dorf-Skin-Admin-Anbindung, neue Skins wurden ausschließlich per Hand-SQL angelegt). Code-Liste/-Filter zeigen beide Belohnungsarten mit Icon (🧸/🏘️) gemischt an.
- **`js/core/bkmp-site.js`:** Einlöse-Erfolg/-Fehler-Anzeige verzweigt auf `body.rewardKind`; neue `bkmpRefreshOwnedVillageSkinAfterRedeem()` (Äquivalent zu `bkmpRefreshOwnedPlushies()`) aktualisiert `bkmpPlayerVillageSkins` sofort, falls das Idle-Dorf in derselben Sitzung bereits geladen war (Einlösen läuft auf der normalen Website unter "Erfolge", nicht im Idle-Dorf-Fenster selbst).
- **`js/systems/bkmp-cosmetics.js`:** **keine Änderung nötig** - `bkmpIdleRenderSkinsPanel()`s bestehender generischer Locked-Zweig (für `achievement`/`boss_drop`) fängt jeden nicht `purchase`/`real_money`-Fall bereits ab und zeigt `unlock_hint` korrekt an; `bkmpVillageSkinOwned()` prüft ohnehin nur `unlock_type==='free' || bkmpPlayerVillageSkins.includes(id)`, funktioniert für `code` also bereits unverändert richtig.

**Verifiziert (Browser, alle drei betroffenen Seiten, kein echter Login nötig - `loadPlushies`/`loadVillageSkinsCatalog` sind öffentlich lesbar):**
- Admin: Belohnungsart-Umschalter zeigt/versteckt die richtigen Dropdown-Felder in beide Richtungen; 16 echte Dorf-Skins korrekt aus der Live-DB geladen; Code-Generierung (Plüshie- UND Skin-Zweig, `createPlushieCodes` gemockt um das noch fehlende Schema nicht zu berühren) baut die korrekte Zeilenform (`reward_kind`/`skin_id` bzw. `plushie_id`) inkl. korrektem Code-Präfix; Codeliste zeigt/filtert beide Belohnungsarten korrekt (Icon, Name, Status).
- Website (Erfolge-Bereich): Einlöse-Erfolg (Dorf-Skin) zeigt korrekte Meldung + räumt Eingabefeld + ruft Refresh auf; Einlöse-Fehler `already_owned` zeigt die skin-spezifische Meldung; bestehender Plüshie-Zweig als Regressions-Check unverändert korrekt (kein Verhaltensunterschied).
- Idle-Dorf-Skins-Panel: `unlock_type='code'`-Skin zeigt korrekt Icon/Name/Beschreibung + Schloss-Hinweistext, kein Kauf-Button; nach simuliertem Besitz (`bkmpPlayerVillageSkins`) korrekt "Ausrüsten"-Button statt Hinweistext.
- `node --check` für `api/redeem-plushie-code.js`/`supabase.js`/`js/core/bkmp-site.js` sauber, extrahierter Syntax-Check für `admin.html` sauber, keine Konsolenfehler in der gesamten Testreihe. Cache-Busting `supabase.js?v=20260723-skincodes1` (alle 3 einbindenden HTML-Dateien) + `bkmp-site.js?v=20260723-skincodes1` (index.html) gesetzt.

**Nicht geprüft / offen:** die eigentliche Redemption gegen die echte Datenbank (SQL-Datei wurde nicht ausgeführt - alle DB-Schreibvorgänge liefen gegen Mocks). **Vor Nutzung: `sql/20260723-code-redeemable-village-skins.sql` im Supabase-Dashboard ausführen**, danach im Admin-Panel unter "Plüshie Codes" → Belohnungsart "Dorf-Skin" → "KalleJunior Dorf" einen Test-Code erzeugen und einmal echt einlösen. Kein echter Spieler-Login möglich (Sicherheitsregel), daher kein Test des kompletten Kreislaufs mit echten Daten.

**Nachtrag - Deploy-Lücke bemerkt (23.07.2026):** der Commit wurde von dieser Umgebung automatisch auch gepusht (dieselbe, schon länger dokumentierte Eigenart, siehe `[[feedback_bkmp_user_handles_commits]]`-Memory) - auf `origin/main` war der Fix also sofort drauf, auf `bkinvestment.de` (Vercel) aber noch nicht ausgeliefert, per direktem Live-Check bestätigt (alte `?v=20260721-...`-Dateinamen liefen dort noch, waehrend `origin/main` schon die neuen `?v=20260723-reversiblenav1` hatte). Nutzer hat nach dem naechsten Deploy per Screenshot bestaetigt, dass die Tab-Leiste (Kampf/Upgrades/Skilltree/Prestige/Erfolge/Gilde/Gilden-Tech/Gildenboss) wieder korrekt sichtbar ist - Fix damit live bestaetigt wirksam.

**Zweiter, eigenständiger Bug beim selben Deploy-Check gemeldet und sofort gefixt (Nutzer: "Level wie auch Streak blinken jedes mal auf nach einem Drachen tot"):** `html.bkmp-app-mode .idle-hud-level-badge`/`.idle-hud-streak-badge`/`.idle-hud-stats span, .idle-hud-resources span` (style.css) trugen `animation: appHudPop .3s/.35s ease` (Einblend-"Pop"-Effekt, scale 0.7→1 + opacity 0→1). `bkmpIdleRenderHud()` baut `#idleDorfHud` aber bei JEDEM Aufruf (Tick, Kill, Ressourcen-Änderung - 30+ Stellen in idledorf.js) komplett per `innerHTML` neu - jedes Badge ist technisch also immer ein frisch eingefügtes Element, die Animation spielt dadurch bei JEDEM Render erneut ab, nicht nur beim echten Erstanzeigen. **Exakt derselbe Bug war bereits einmal für `.idle-res-chip` gefunden+gefixt worden** (siehe Kommentar dort: "Nutzer-Meldung: alle Buttons oben flackern bei jedem Drachen-Tod") - der Fix (Animation einfach nicht anwenden) wurde dabei nicht auf die drei anderen betroffenen Selektoren übertragen. Fix: `animation` bei allen drei Stellen entfernt, `appHudPop`-Keyframe selbst bleibt unangetastet (wird noch an anderer Stelle genutzt). Verifiziert per CSSOM-Rule-Enumeration (`computedStyle`/`cssRules`-Traversal): alle drei Selektoren liefern jetzt `animation: (none)`. Brace-Balance-Check sauber, keine Konsolenfehler. Cache-Busting `style.css?v=20260723-hudpopflickerfix1` in allen 5 HTML-Dateien gesetzt.

## Phase 7.3 — Kritischer Repair "Navigations-Buttons verschwinden" (23.07.2026, abgeschlossen)

Nutzer-Auftrag: 28-Abschnitte-Brief (nicht in CLAUDE.md dupliziert) mit Video-Beweis (`F:\Video\2026-07-23 21-53-39.mp4`) - trotz des Hotfixes weiter oben ("Dringender Hotfix... Nachtrag - Deploy-Lücke bemerkt") verschwand die Tab-Leiste erneut, diesmal reproduzierbar per Video: Kampf lief normal (alle 15 Tabs sichtbar), Fenster schließen, wieder öffnen → kurzer leerer "⏳ Lädt…"-Zustand, danach HUD/Kampf korrekt, aber die komplette Navigationsleiste bleibt fehlend. Auftrag verlangt explizit die Reihenfolge "Reproduzieren → automatisierten Regressionstest bauen → tatsächliche Ursache beheben → Stresstests → Bericht", keine Timeout-Pflaster, keine kosmetische Fehlerunterdrückung. **Nichts gepusht/deployed über die bereits dokumentierte Auto-Push-Eigenart der Umgebung hinaus, keine SQL/Auth/Spielwerte/Drop-Chancen geändert, keine Tabs/Funktionen entfernt.** Git-Status vor Beginn geprüft: sauber, `main`, Commit `af4eae7`, deckungsgleich mit `origin/main`.

**Wichtig: die tatsächliche Ursache war NICHT die im Auftrag als Beispiel skizzierte Architektur-Vermutung** (Race Condition zwischen zwei parallelen Render-Aufrufen, fehlende Render-Generation). Die im Auftrag vorgeschlagenen Muster (Abschnitt 5-6: Generation-Zähler, permanente Shell-Struktur) wurden bewusst NICHT blind übernommen, weil die tatsächliche Ursache viel simpler und bereits eindeutig lokalisierbar war - siehe unten. Der Auftrag selbst verlangt explizit "an die bestehende Architektur anpassen, nicht blind kopieren".

**Root Cause 1 (der eigentliche, dominante Bug - `js/systems/bkmp-raid.js`, `bkmpRaidToggleCombatView()`):** `bkmpIdleCloseModal()` ruft bei JEDEM Schließen des Idle-Dorf-Fensters unbedingt `bkmpRaidStopCombatView()` auf (idledorf.js:2424), unabhängig davon, ob je ein Raid-Kampf lief. Diese Funktion prüfte bislang `BKMP_PROTO_COMPACT_HUD_ENABLED` - ein STATISCHES "ist das Kompakt-HUD-Prototyp-Skript überhaupt geladen"-Flag (`const ... = true;`, bkmp-proto-compact-hud.js:11), das in Produktion IMMER `true` ist, VÖLLIG UNABHÄNGIG von der aktuellen Bildschirmbreite - statt des dynamischen `bkmpProtoChudCompactActive` ("ist die kompakte Nav GERADE JETZT die sichtbare"). Dadurch wurde `#idleDorfTabs` bei JEDEM Schließen auf `display:none` gesetzt, auch auf breitem Desktop. `bkmpProtoChudSyncVisibility()`s eigener Cache-Vergleich (`wantCompact === bkmpProtoChudCompactActive`) erkannte beim nächsten Öffnen keinen "echten" Wechsel (die Breite war ja die ganze Zeit "Desktop") und stellte `#idleDorfTabs` deshalb NIE wieder her - exakt das im Auftrag als Horror-Szenario beschriebene "beide Navigationssysteme gleichzeitig abwesend" (die kompakte Nav war ja nie eingeblendet worden, die Desktop-Leiste blieb auf 0×0px hängen). Erklärt auch, warum der frühere Hotfix (weiter oben in dieser Datei) das Problem nur TEILWEISE behoben hat: dessen Fix (`bkmpIdleSyncTabOverflowForViewport()`/`bkmpProtoChudSyncVisibility()` bei jedem Öffnen erneut aufrufen) korrigiert Fälle, in denen sich die tatsächliche Zielgröße (kompakt vs. Desktop) geändert hat - hier änderte sich die Zielgröße nie, nur der DOM wurde durch einen ganz anderen Codepfad (Raid-Combat-View-Toggle) direkt sabotiert, unsichtbar für den Cache-Abgleich.

**Root Cause 2 (Nebenbefund, verstärkt den ersten Bug zusätzlich - style.css, `.idle-dorf-loading`):** dieselbe Klasse, die seit Phase 5.1 gezielt gegen "Raidboss-Bilder springen kurz durch" eingesetzt wird, blendete bislang zusätzlich zu den eigentlich dynamischen Panels auch `#idleDorfHud`/`#idleStageBar`/`#idleDorfTabs` (die "permanente Hülle" im Sinne des Auftrags) für die volle Dauer der drei sequenziellen Netzwerk-Aufrufe beim Öffnen aus (`bkmpIdleEnsureConfigLoaded`/`bkmpIdleLoadOrInitState`/`bkmpIdleClaimOfflineProgress`) - erklärt den im Video sichtbaren, komplett leeren "⏳ Lädt…"-Zustand (~1-2s) bei JEDEM Wiederöffnen, nicht nur beim allerersten Laden. Gegen echte Produktions-Netzwerklatenz (LIVE-Supabase, nicht die nahezu instantanen lokalen Mocks aus früheren Sessions) wird dieses Fenster spürbar.

**Fix (zwei chirurgische, gezielte Änderungen - keine Architektur-Neuerfindung, keine Datei ist umfassend umgebaut):**
1. `js/systems/bkmp-raid.js`: `bkmpRaidToggleCombatView()` prüft jetzt `bkmpProtoChudCompactActive === true` (dynamischer, echter Zustand) statt `BKMP_PROTO_COMPACT_HUD_ENABLED` (statisches Feature-Flag). `typeof`-Guard bleibt, da `idle-stream-mini.html` `bkmp-raid.js` OHNE das Kompakt-HUD-Prototyp-Skript einbindet - dort verhält sich die Prüfung dadurch weiterhin exakt wie das alte, unbedingte `''`.
2. `style.css`: `.idle-dorf-loading` blendet nur noch `.idle-dorf-panel`/`.raid-join-banner`/`.idle-dorf-offline-card` (den tatsächlich dynamischen Inhalt) aus - HUD/Stufenleiste/Tableiste bleiben immer sichtbar (zeigen bei einem Wiederöffnen kurz den letzten bekannten Stand statt zu verschwinden; bei einem echten Erstladen bleiben sie leer bis zum ersten Render - beides kein "leerer Overlay-Zustand" im vom Auftrag verbotenen Sinne, siehe Abschnitt 6-7 des Auftrags).
3. Codebase-weiter Audit auf dasselbe Bug-Muster (Auftrag Abschnitt 3): `BKMP_PROTO_COMPACT_HUD_ENABLED` wird nur noch innerhalb von `bkmp-proto-compact-hud.js` selbst als echtes Kill-Switch-Flag verwendet (dort korrekt) - der raid.js-Fall war der einzige Fehlgebrauch. `bkmp-runes.js`s `#idleDorfTabs`-Zugriff ist rein lesend (`getBoundingClientRect()` für die Lager-Balken-Position), kein weiterer Fund.

**Playwright-Regressionssuite (`tests/e2e/nav-persistence.spec.js`, neu, erweitert die bestehende Phase-7.2-Mock-Infrastruktur statt einer neuen):** 8 Tests × 3 Projekte (chromium-desktop/mobile-small/mobile-large) = 24 Tests - 50× Öffnen/Schließen, 100× schnelle Tab-Wechsel, 20× Öffnen/Schließen ohne Wartezeit, Öffnen-sofort-Schließen, Schließen-sofort-Wiederöffnen (das exakte Kernszenario aus dem Video), Doppelklick auf Öffnen, Desktop→Mobil→Desktop-Breitenwechsel, direkter `bkmpRaidStopCombatView()`-Aufruf ohne jedes Öffnen/Schließen (isoliert exakt den Root-Cause-Codepfad). Zentrale Invariante pro Test (Auftrag Abschnitt 12): mindestens eines von `#idleDorfTabs`/`#bkmpProtoCompactNav` muss mit echter Breite/Höhe > 0 sichtbar sein, nie beide gleichzeitig fehlend.
- **Vor dem Fix: 7 von 8 Tests schlugen fehl** (nur der allererste Öffnen-Check ohne jedes Schließen bestand - deckt sich exakt mit dem Video, wo der ERSTE Öffnen-Vorgang korrekt war). Fehlerbeweis exakt wie erwartet: `desktopTabs={"display":"none","width":0,"height":0}` UND `compactNav={"display":"none","width":0,"height":0}` gleichzeitig - der direkte `bkmpRaidStopCombatView()`-Test (ganz ohne Öffnen/Schließen-Zyklus) scheiterte identisch, das ist der eindeutige Beweis, dass die Ursache exakt dort liegt und keine Race Condition anderswo mitspielt.
- **Nach dem Fix: 24/24 grün.**
- Zusätzlich per direktem Live-Aufruf im Browser (gegen die echten, per Cache-Busting frisch ausgelieferten Dateien, nicht nur den Playwright-Mock) bestätigt: `bkmpRaidStopCombatView()` direkt aufgerufen → `#idleDorfTabs` bleibt korrekt `display:flex`, 1082×66px, `bkmpProtoChudCompactActive` liest korrekt `false` auf Desktop-Breite.
- Volle bestehende Suite (`tests/e2e/`, 75 Tests, chromium-desktop) danach zur Regressionsprüfung erneut gelaufen: 69 bestanden, 6 korrekt übersprungen (Mobile-Smoke auf Desktop-Projekt), 1 unverändert als bereits bekannter Fehler verfolgter `test.fail()` (Zwei-Tab-Speicher-Race, siehe Phase-7.2-Bericht weiter oben - komplett unabhängig von diesem Fix, keine neue Regression).
- Zwei kleine, beim Schreiben des Tests selbst gefundene Test-Infrastruktur-Bugs unterwegs korrigiert (keine App-Bugs): (a) `assertCoreTabButtonsVisible` prüfte anfangs unconditional die Desktop-Tab-IDs, obwohl auf schmalen Viewports/App-Modus die echten Buttons absichtlich in einem versteckten `#idleDorfTabs` stecken (Bedienung läuft über die Kompakt-Nav-Proxy-Buttons) - Prüfung jetzt Nav-bewusst. (b) einige Stresstests nutzten `page.locator(...).click()`/`{force:true}`, was bei einer echten `display:none`-Bounding-Box (0×0px) selbst mit `force:true` fehlschlägt - auf direkte DOM-`.click()`-Aufrufe umgestellt (identisch zu dem, was die echte Kompakt-Nav intern per Proxy-Klick ohnehin tut).

**Cache-Busting:** `style.css?v=20260723-navpersistfix1` (5 HTML-Dateien) + `bkmp-raid.js?v=20260723-navpersistfix1` (index.html/admin.html/idle-stream-mini.html).

**35-Punkte-Bericht (Auftrag Abschnitt 27, kompakt):** Branch `main`, Start-Commit `af4eae7` (sauber, deckungsgleich mit `origin/main`). Reproduzierter Fehler: Öffnen→Schließen→Wiederöffnen auf Desktop-Breite, danach dauerhaft (nicht nur kurz) beide Navigationssysteme unsichtbar, erst Reload behebt es. Dauer des Leerzustands vor dem Fix: ~1-2s "⏳ Lädt…" (Root Cause 2) gefolgt von dauerhaft fehlender Nav (Root Cause 1, bis zum Reload). Hauptursache: falsches statisches statt dynamisches Flag in `bkmpRaidToggleCombatView()` (`js/systems/bkmp-raid.js`). Beteiligte Funktionen: `bkmpRaidToggleCombatView`/`bkmpRaidStopCombatView` (bkmp-raid.js), `bkmpIdleCloseModal` (idledorf.js, ruft es unbedingt auf), `bkmpProtoChudSyncVisibility`/`bkmpIdleSyncTabOverflowForViewport` (deren Cache-Abgleich den Fehlzustand nicht erkennen konnte). Beteiligte CSS-Regel: `.idle-dorf-card.idle-dorf-loading` (style.css). Geänderte Dateien: `js/systems/bkmp-raid.js`, `style.css`, 5× HTML (Cache-Busting), neu: `tests/e2e/nav-persistence.spec.js`. DOM wurde weder gelöscht noch ersetzt, nur per `style.display`/`visibility` fälschlich dauerhaft verborgen - kein Element ging verloren. Kein Höhen-/Breiten-Squash (0px war eine direkte Folge von `display:none`, keine CSS-Flex/Grid-Fehlberechnung). Keine Race Condition gefunden (der isolierte Direkt-Aufruf-Test beweist das) - keine Render-Generation/Abort-Logik nötig, daher auch keine eingeführt. Keine doppelten Event-Listener gefunden. Kein CSS-Spezifitäts-Konflikt (reines JS-`style.display`, keine CSS-Kaskaden-Frage). Neue Shell-Struktur: keine nötig (bestehende Struktur war bereits korrekt permanent, nur durch den Bug fälschlich verborgen) - stattdessen `.idle-dorf-loading` auf die tatsächlich dynamischen Elemente verengt. Default-Tab-Validierung: bereits vorher korrekt (kein Bug in `bkmpIdleActiveTab`-Handling gefunden). Verhalten bei langsamer/fehlgeschlagener Datenantwort: HUD/Nav/Stufenleiste bleiben jetzt durchgehend sichtbar (Root-Cause-2-Fix), nur der Inhaltsbereich zeigt "⏳ Lädt…". Ergebnis 50×/100×-Stresstests: alle grün (24/24, 3 Projekte). Desktop/Tablet-äquivalent/Mobil/App-Modus-äquivalent: alle per Playwright-Projekte (chromium-desktop 1280×800, mobile-small, mobile-large) UND direktem Browser-Live-Check bestätigt. Konsole: keine Fehler in der gesamten Testreihe. Nebenbefunde: keine neuen App-Bugs, zwei reine Test-Skript-Bugs im neuen Spec selbst gefunden+korrigiert (siehe oben). Bekannte verbleibende Risiken: keine neuen - die bereits dokumentierte Zwei-Tab-Speicher-Race (Phase 7.2) bleibt unverändert offen. Playwright-HTML-Bericht/Screenshots/Traces: `tests/report/artifacts/nav-persistence-*` (vor UND nach dem Fix erhalten, zeigen den roten und den grünen Zustand). Spielmechanik: unverändert (nur `style.display`/`visibility`-Logik geändert, keine Werte/Formeln/Drop-Chancen). Push/Deploy: nicht bewusst ausgelöst (siehe wiederkehrende Auto-Push-Eigenart dieser Umgebung, `[[feedback_bkmp_user_handles_commits]]`-Memory) - Nutzer sollte nach dem nächsten Deploy live gegenprüfen wie bei den vorherigen Hotfixes.

## Anfrage-Entscheidung: 1x-Popup-Benachrichtigung für Absender (23.07.2026) — Code fertig, SQL noch nicht ausgeführt

Neues Feature, ausgelöst durch Admin-Panel-Screenshot ("Verkaufsanfragen") + Nutzerfrage "Möglichkeit das ich der Person mitteilen kann ob deren Anfrage Abgelehnt/Angenommen habe. Als 1x Popup für die Personen." Nach Rückfrage zum Umfang (nur Kartenverkauf oder auch Investoren-Anfragen) explizit **"Ja wir machen beides. Bei Investoren Abgelehnt (Mit Begründung die ich schreibe tue) + Bei angenommen mit Infos Zahle das Geld an ChronoKora/ChronoYaksha CB1"** gewählt.

**Ausgangslage:** `card_sale_requests`/`investor_requests` sind beide komplett anonyme Einreich-Tabellen (nur Minecraft-Name/Discord bzw. Name/Betrag, kein Account) mit RLS `insert: jeder`, `select/update/delete: nur is_active_admin()` (siehe jeweilige `*-schema.sql`). Es gibt also weder eine Account-Bindung noch einen Server-seitigen Weg, "wer bin ich" zu wissen, sobald der Absender die Seite wieder aufruft.

**Umsetzung:**
- **`sql/20260723-request-decision-notify.sql`** (neu, **noch nicht ausgeführt**, Nutzer muss das im Supabase-Dashboard nachholen, bevor das Feature live funktioniert):
  - `investor_requests.reject_reason` (text, nullable) - Admin-geschriebene Begründung.
  - Zwei enge `SECURITY DEFINER`-RPC-Funktionen `get_card_sale_request_status(p_id uuid)`/`get_investor_request_status(p_id uuid)`, `grant execute ... to anon, authenticated`. Geben AUSSCHLIESSLICH Status (+ Ablehnungsgrund) für GENAU EINE per ID angefragte Zeile zurück - kein Listing, keine anderen Spalten (kein Name/Discord/Bild/Betrag). Gleiches Sicherheitsprinzip wie beim Feedback-Board (Stufe 2, 21.07.): so wenig wie möglich öffentlich exponieren, strukturell statt nur per Policy-Disziplin - eine breitere RLS-`select`-Policy für `anon` hätte stattdessen ALLE Anfragen aller Spieler öffentlich auflistbar gemacht.
- **`supabase.js`:** `saveInvestorRequest()` gibt jetzt die eingefügte ID zurück (`.select('id').single()`, vorher `true`); `updateInvestorRequestStatus(id, status, rejectReason)` neuer dritter Parameter, schreibt `reject_reason` nur beim Ablehnen; zwei neue Wrapper-Funktionen `getCardSaleRequestStatus(id)`/`getInvestorRequestStatus(id)` rufen die neuen RPCs auf. Kartenverkauf braucht keine Änderung an der Einreich-Funktion selbst - die läuft schon über `api/submit-entry.js` (Service-Role, `Prefer: return=representation`), das liefert die ID bereits mit.
- **`admin.html`:** `rejectInvestorRequest()` fragt jetzt per `window.prompt()` nach einer Begründung, bevor abgelehnt wird (Abbrechen im Prompt lehnt NICHT ab; leerer Text ist erlaubt). Kartenverkauf-Ablehnung unverändert (kein Begründungswunsch für diesen Typ).
- **`js/core/bkmp-site.js`:** neuer Abschnitt "Anfrage-Entscheidung" - beim erfolgreichen Einreichen (beide Formulare) wird die zurückgegebene ID in `localStorage` gemerkt (`bkmp-pending-card-sale-requests`/`bkmp-pending-investor-requests`, gleiche Namenskonvention wie `bkmp-mc-name`). Bei jedem Seitenaufruf (`bkmpCheckPendingRequestDecisions()`, eingehängt direkt neben `bkmpTrackDayVisited()` im bestehenden Init-Ablauf) wird für jede gemerkte ID der aktuelle Status abgefragt; bei `confirmed`/`rejected` wird ein Popup eingereiht (Warteschlange, falls mehrere Entscheidungen gleichzeitig anstehen) und die ID sofort aus dem lokalen Speicher entfernt (echtes "1x"). Popup selbst nutzt `bkmpUiModalHtml()`/`bkmpUiTrapFocus()` (Phase 3, bisher unbenutzter Baustein - erster echter Einsatz). Feste Texte:
  - Kartenverkauf angenommen: "Packe 10 Bundles zusammen und kontaktiere ChronoKora/ChronoYaksha Ingame."
  - Kartenverkauf abgelehnt: generischer Hinweis, keine Begründung (nicht angefragt für diesen Typ).
  - Investor angenommen: "Zahle das Geld an ChronoKora/ChronoYaksha (CB1)."
  - Investor abgelehnt: generischer Hinweis + admin-geschriebene Begründung, falls vorhanden.

**Verifiziert (Browser, ohne echten Login - reine Funktionsprüfung):** Popup-Warteschlange mit zwei nacheinander gezeigten, inhaltlich korrekten Popups durchgespielt (Text/Titel je exakt geprüft, Schließen-Button räumt korrekt auf und zeigt automatisch das nächste); `bkmpCheckPendingRequestDecisions()` End-to-Ende mit gemockter RPC-Antwort durchgespielt (ID wird korrekt aus `localStorage` entfernt, Popup korrekt eingereiht); `rejectInvestorRequest()` mit gemocktem `window.prompt`/`updateInvestorRequestStatus` geprüft (Abbrechen ruft `updateInvestorRequestStatus` NICHT auf; echter Text wird getrimmt korrekt durchgereicht). `node --check` für beide geänderten `.js`-Dateien sauber, keine Konsolenfehler. Cache-Busting `supabase.js?v=20260723-requestnotify1` (index.html/admin.html/idle-stream-mini.html) + `bkmp-site.js?v=20260723-requestnotify1` (index.html) gesetzt.

**Nicht geprüft / offen:** die eigentliche RPC-Antwort gegen die echte Datenbank (SQL-Datei wurde nicht ausgeführt - Popup-Logik lief nur gegen gemockte Rückgabewerte). Kein echter Spieler-Login möglich (Sicherheitsregel), daher kein Test des kompletten Einreichen-→-Admin-entscheidet-→-Popup-erscheint-Kreislaufs mit echten Daten. **Vor Nutzung: `sql/20260723-request-decision-notify.sql` im Supabase-Dashboard ausführen**, danach idealerweise einmal mit einer echten Test-Anfrage durchspielen.

**Nachtrag (23.07.2026) - Feature live bestätigt + eigenes Fenster statt `window.prompt()`:** Nutzer hat per Screenshot bestätigt, dass "Ablehnen" bei Investoren-Anfragen live auf bkinvestment.de bereits nach Begründung fragt - Deploy erfolgreich. Der native Browser-`window.prompt()` sah dabei aber unpassend aus (Screenshot zeigte die rohe "Auf bkinvestment.de wird Folgendes angezeigt"-Chrome-Hülle). Nutzerwunsch "das Ablehnen gerne auch in ein schönes Fenster verbauen" umgesetzt: neue `bkmpAdminPromptTextModal()`-Funktion in `admin.html`, nutzt dieselbe `bkmpUiModalHtml()`/`bkmpUiTrapFocus()`-Komponente (Phase 3) wie das 1x-Entscheidungs-Popup - admin.html lädt `js/ui/bkmp-ui-components.js` bereits. Promise-basiert mit demselben Vertrag wie `window.prompt()` (löst mit getrimmtem Text oder `null` bei Abbrechen/ESC/Klick daneben auf), `rejectInvestorRequest()` musste dafür nur eine Zeile ändern. Mehrzeiliges `<textarea>` statt einzeiligem Feld - echte Verbesserung, nicht nur optisch. Verifiziert per Browser (Bestätigen mit getrimmtem Text/Abbrechen-Button/ESC-Taste je einzeln geprüft, alle drei Pfade lösen korrekt auf, Overlay räumt sich selbst auf), Screenshot bestätigt stimmiges dunkles Design passend zum Rest der Seite. Keine Konsolenfehler, `admin.html`-Inline-Skript per extrahiertem Syntax-Check sauber.

## Realtime-Kosten Stufe 2: Presence-gesteuerte Kampf-Broadcasts (23.07.2026)

Auslöser: Supabase-Dashboard-Screenshot "Realtime Messages usage" - 19,5M von 5M inkludierten Nachrichten verbraucht (14,5M Overage), Nutzerfrage "Können wir hier noch was reduzieren?". Der 3s-Throttle vom 20.07. (siehe `[[project_bkmp_realtime_overage_2026-07-20]]`-Memory, `BKMP_IDLE_COMBAT_BROADCAST_MIN_MS` in `js/ui/bkmp-hud.js`) ist seitdem live und wirkt sichtbar (Tagesrate fiel von ~3,6-4M auf ~0,9-1,2M nach dem 19.07.), löst die eigentliche Ursache aber nicht: **jeder eingeloggte Spieler sendet dauerhaft alle 3s, unabhängig davon, ob überhaupt irgendwer zusieht** - nur eine kleine Minderheit öffnet je das OBS-Mini-Overlay (`idle-stream-mini.html`).

**Fix (`supabase.js`, `bkmpBroadcastCombatState`/`bkmpSubscribeToCombatState`):** Realtime PRESENCE auf demselben Kanal. Der Empfänger (`idle-stream-mini.html`, via `bkmpSubscribeToCombatState`) meldet sich per `channel.track({online:true})` als anwesend, sobald die Verbindung steht. Der Sender beobachtet das per `presence`-`sync`-Event (`bkmpCombatBroadcastHasListener`, aktualisiert bei jedem Sync) und überspringt `.send()` komplett, solange `presenceState()` leer ist - kein Poll, Presence aktualisiert sich serverseitig automatisch bei jedem Verbinden/Trennen (auch bei hartem Tab-Schließen ohne sauberes `unsubscribe()`). Reduziert das Sendevolumen für die große Mehrheit der Spieler (die den Overlay nie öffnen) auf praktisch null, ohne dass Streamer mit offenem Overlay irgendetwas davon merken (Updates kommen weiterhin sofort, keine zusätzliche Verzögerung).

**Verifiziert (echte Supabase-Realtime-Infrastruktur, zwei echte Browser-Tabs, kein Mock):**
1. Sender-Tab ruft `bkmpBroadcastCombatState()` SOFORT nach Kanal-Erstellung auf (bevor Presence-Sync durchlaufen konnte) → `bkmpCombatBroadcastHasListener` korrekt `false`, nichts gesendet.
2. Empfänger-Tab ruft `bkmpSubscribeToCombatState()` auf und trackt Presence → nach ~3s echtem WebSocket-Roundtrip zeigt der Sender-Tab `bkmpCombatBroadcastHasListener === true`.
3. Sender broadcastet erneut → Empfänger empfängt GENAU diese eine Nachricht (die erste, verworfene, kam nachweislich nie an - `window.__received` enthielt nur den zweiten Payload).
4. Empfänger ruft `bkmpUnsubscribeFromCombatState()` auf (simuliert Overlay schließen) → Sender erkennt das automatisch per Presence-"leave" innerhalb ~2,5s, `bkmpCombatBroadcastHasListener` fällt korrekt zurück auf `false`.

Keine Konsolenfehler in beiden Tabs während der gesamten Testreihe. `node --check supabase.js` sauber. Cache-Busting `supabase.js?v=20260723-presencegate1` in allen 3 einbindenden HTML-Dateien gesetzt.

**Nicht geprüft:** kein echter Zwei-Spieler-Streamer-Testfall (ein echter Account im Hauptspiel + derselbe Name im echten OBS-Overlay) - die obige Verifikation deckt exakt denselben Code-Pfad ab (gleiche Kanal-Topic-Logik, gleiche Presence-Mechanik), aber ohne echten Login/echte Spieldaten. Nächste 24-48h Supabase-Dashboard beobachten, um den tatsächlichen Rückgang zu bestätigen.

## Phase 7.2 — Automatisierte Playwright-E2E-Testsuite (21.07.2026, Stufe 1 komplett fertig)

Nutzer-Auftrag: 43-Abschnitte-Brief (nicht in CLAUDE.md dupliziert) — vollständige automatisierte End-to-End-Testsuite für das gesamte Idle-Drachendorf, da manuelle Prüfungen immer wieder neue Fehler finden. Ausdrücklich nur die **erste Umsetzungsstufe** (Auftrag Abschnitt 41, 14 Punkte) verlangt, DANACH zuerst einen Bericht zeigen, nicht blind weiterbauen (Abschnitt 37/42). **Nichts gepusht/deployed, keine Produktionsdaten verändert, kein Production-Supabase-Zugriff.** Git-Status vor Beginn geprüft: sauber, `main`, deckungsgleich mit `origin/main`, Commit `c637d3d`.

**Backend-Strategie (Nutzerentscheidung, siehe Abschnitt 2 des Auftrags "STOPPE und erkläre"):** vollständig lokaler Mock-Backend statt dediziertem QA-Supabase-Projekt — `supabase.js`/`index.html` bleiben dabei komplett unangetastet (bleiben "eingefroren", siehe Sicherheitsregel oben); die Isolation läuft ausschließlich über Playwright-Netzwerk-Interception (`page.route()`) auf der Testseite, nicht über einen Code-Zweig in der App selbst. Kein Zugriff auf das echte Supabase-Projekt (`zgknyrwzpohvfdweomxf.supabase.co`) möglich, solange dieser Mock aktiv ist.

**Neue Dateien** (alle unter `tests/`, plus `package.json`/`playwright.config.js` im Root — Playwright ist eine reine `devDependency`, `node_modules`/`package-lock.json` bereits per `.gitignore` ausgeschlossen, keine Änderung am Runtime-Bundle):
- `tests/mock/` — kompletter lokaler Ersatz für Supabase Auth+REST+RPC: `store.js` (In-Memory-Tabellen + Auth-Nutzer/Sessions, pro Test frisch), `clock.js` (virtuelle Testzeit), `auth-engine.js` (Login/Registrieren/Refresh/Logout, gibt echte GoTrue-JWT-Struktur zurück — siehe Bug-Fund unten), `rest-engine.js` (generisches PostgREST-Subset: eq/neq/gt/gte/lt/lte/in/is-Filter, select/order/limit, insert/update/upsert via `on_conflict`), `rpc-engine.js` (originalgetreuer JS-Nachbau der Dungeon-System-2.0-SQL-Funktionen aus `sql/supabase-dungeon-system-v2.sql` + `sql/supabase-dungeon-fixed-key-times.sql`, inkl. fester 4h-Berlin-Zeitslots), `router.js` (dispatcht auf die drei Engines), `server.js` (lokaler Static+API-Server, dient das Repo unveraendert + `/api/claim-idle-offline-progress` + zwei weitere), `invoke-vercel-handler.js`/`offline-progress-handler.js` (rufen die ECHTEN, unveraenderten `api/*.js`-Handler-Dateien in-process auf, mit gemocktem `fetch`/`Date` fuer den Aufrufzeitraum — hoechste Treue, keine zweite Kopie der Offline-Fortschritts-Formel).
- `tests/fixtures/` — `reference-data.js` (Drachen/Config/Skillnodes, eine plausible Annaeherung an die echten Produktionsdaten, KEINE Kopie — Produktions-Supabase wird nie gelesen), `base-player-state.js` (vollstaendige Standard-`idle_player_state`-Zeile), `teststands.js` (Teststand A-E gemaess Auftrag Abschnitt 3: A=neuer Spieler, B=mittel, C=fortgeschritten inkl. 6 belegter Runenslots, D=beschaedigt/NaN/negativ/doppelt ausgeruestete Runenart, E=Maximalbelastung mit 300 Runen).
- `tests/helpers/qa-fixtures.js` — der zentrale Playwright-`test.extend()`, den jede Spec-Datei importiert: frischer Store+Server pro Test, Netzwerk-Interception, `openAndLogin()`/`openAppMode()`.
- `tests/e2e/navigation.spec.js` — erste fertige Suite (17/18 gruen, siehe unten).

**Drei echte, reproduzierbare Bugs beim Aufbau des Mocks selbst gefunden** (nicht nur behauptet — jeweils per gezieltem Debug-Testlauf isoliert, nicht aus der ersten Vermutung uebernommen):
1. **JWT-Formatfehler:** `supabase-js` dekodiert das Access-Token clientseitig (fuer Claims/Session-Wiederherstellung) und braucht dafuer eine strukturell gueltige JWT-Form (3 durch Punkte getrennte Base64-Segmente) — ein anfangs simples, opakes Mock-Token (`qa_at_<uuid>`) scheiterte daran ("Expected 3 parts in JWT; got 1"), wodurch eine gerade erst erfolgreich angelegte Sitzung beim naechsten Laden sofort wieder verworfen wurde. Fix: `tests/mock/auth-engine.js` baut jetzt ein strukturell echtes (aber nicht kryptografisch signiertes) JWT.
2. **Virtuelle-Zeit-Fehlanpassung:** die virtuelle Testuhr startete anfangs auf einem fixen Datum (2026-01-15) statt der echten Systemzeit — jedes ausgestellte Mock-Token wirkte dadurch aus Sicht des ECHTEN (nicht gefaketen) Browser-Takts bereits Monate abgelaufen; `supabase-js` erneuerte es waehrend eines aktiven Logins zwar noch, verwarf die Sitzung aber bei einem Seiten-Reload komplett. Fix: virtuelle Startzeit folgt jetzt standardmaessig der echten `Date.now()`; zeitreisende Tests setzen `useFakeClock:true` (installiert Playwrights eigene Browser-Uhr synchron zur Server-Uhr).
3. **Service-Worker-Leck (sicherheitsrelevant):** `sw.js` (reiner Passthrough-Service-Worker fuer die "Als App installieren"-Funktion) fuehrt jede Anfrage ueber sein EIGENES `fetch()` in einem separaten Ausfuehrungskontext erneut aus — das umgeht Playwrights `page.route()`-Interception vollstaendig. Sobald der Service Worker (erst NACH dem ersten Laden) aktiv wurde, gingen mehrere echte Netzwerkanfragen direkt an das ECHTE Produktions-Supabase-Projekt (durch echte Signaturpruefung mit 401 abgelehnt, da nie eine echte Anfrage-Antwort ankam — es wurden nachweislich keine echten Daten gelesen oder geschrieben, aber die Anfrage verliess den Mock). Fix: `serviceWorkers:'block'` global in `playwright.config.js` — Service Worker koennen waehrend der gesamten Testsuite nie aktiv werden.

**Ein echter, reproduzierbarer App-Navigations-Fund** (kein Mock-Artefakt, per Quellcode-Lektuere UND Test bestaetigt): `bkmpIdleOpenModal()` oeffnet `#idleDorfOverlay` nur, wenn bereits ein Name aufgeloest ist — ist er leer, klickt sie nur `#mcNameBadge` (oeffnet das Login-Formular) und kehrt zurueck, OHNE nach erfolgreichem Login je erneut aufgerufen zu werden. Bei einem echten Erstbesuch von `/?app=idledorf` ohne bestehende Sitzung oeffnet sich das Idle-Dorf nach dem Registrieren/Anmelden daher NICHT automatisch — ein zweiter Trigger (Reload) ist noetig. Ausserdem gilt: `?app=idledorf` erzwingt IMMER die kompakte Mobil-Navigation samt "Mehr"-Ueberlaufmenue, unabhaengig von der Fensterbreite (Absicht, siehe Phase-7.0-Nachtrag oben) — fuer normale Desktop-Navigationstests wird deshalb bewusst NICHT `?app=idledorf` verwendet, sondern die normale Startseite + echter Klick auf `#idleDorfButton` (dort rendert die flache 15-Tabs-Leiste aus Phase 7.1 Stufe 3).

**Navigationstest-Ergebnis (`tests/e2e/navigation.spec.js`, 18 Tests, Chromium Desktop, seriell jetzt 18/18 gruen):** Login+Oeffnen, alle 15 Tabs einzeln (Panel sichtbar, Button aktiv, alle anderen Panels verborgen, keine Konsolenfehler), schnelles Tab-Wechseln, Reload-Sitzungserhalt. 6 der 15 Einzeltests schlugen bei hoher Parallelitaet (Standard-Workerzahl) mit Timeout fehl, liefen aber seriell (`--workers=1`) alle gruen — reine Ressourcenkonkurrenz zwischen mehreren gleichzeitigen Chromium-Instanzen+Mock-Servern auf dieser Maschine, keine Flakiness der App selbst; braucht ggf. eine niedrigere Worker-Zahl oder grosszuegigere Timeouts in `playwright.config.js` fuer die volle Suite.

**Ein echter, reproduzierbarer Bug gefunden UND SOFORT BEHOBEN (Nutzerwunsch "alles was man gleich reparieren kann"):** nach Besuch des Runen-Tabs blockierte `#idleRuneDrawer` (die Kraftrune-Seitenschublade) per Overlay-Subtree nachfolgende Klicks auf ANDERE Tab-Buttons (z. B. Dungeon) - `bkmpRuneSyncDrawerVisibility()` entfernte beim Tab-Wechsel zwar korrekt die `visible`-Klasse (das war schon lange richtig implementiert, siehe Kommentar an der Aufrufstelle in `bkmpIdleInitTabs`), aber die eigentliche URSACHE lag in der POSITION waehrend der Balken offen war: `top:50%` (style.css) zentrierte ihn auf die GANZE Fensterhoehe, wodurch sein 74vh/640px hoher Panel-Bereich sich bei ueblichen Laptop-Hoehen (<=800px, per Screenshot bei 1366x768 bestaetigt) ueber die Tab-Leiste am oberen Kartenrand legte (`z-index:56`, hoeher als alles im Fenster) - ein Klick auf einen ueberdeckten Tab griff nie durch, weder im Test noch bei einem echten Spieler mit Maus, und ausgerechnet die Tabs, mit denen sich der Balken sonst schliessen liesse, lagen selbst darunter (echte Sackgasse ohne den kleinen "‹"-Umschalt-Pfeil zu finden). Fix (`bkmpRuneSyncDrawerPosition()`, `js/systems/bkmp-runes.js`, `?v=20260721-drawerposfix1` in allen 3 einbindenden HTML-Dateien): misst jetzt zusaetzlich zur bereits bestehenden horizontalen Kartenkanten-Messung auch die Tab-Leiste (`#idleDorfTabs`) live und verankert den Balken IMMER darunter (gleiches Rand-Prinzip wie links: passt er nicht mehr vollstaendig in den Restplatz, ruckt er so weit wie moeglich hoch, ohne die Tabs je zu ueberdecken). Verifiziert: derselbe Playwright-Test (`rapid tab switching`, der den Bug reproduzierbar auffing) laeuft jetzt zweimal hintereinander stabil 18/18 gruen.

**Button-Inventarisierung (`tests/e2e/buttons-inventory.spec.js`, Teststand C, 2/2 gruen):** generischer DOM-Sammler (`tests/helpers/inventory.js`) laeuft ueber alle 15 Tab-Panels + HUD/Nav, schreibt `tests/report/button-inventory.json` (142 Elemente gefunden). **Zwei echte, kleine A11y-Luecken gefunden UND SOFORT GEFIXT** (reine `aria-label`-Ergaenzungen, keine Verhaltensaenderung): `#idleGuildNameInput`/`#idleGuildTagInput`/`#idleGuildCodeInput` (js/systems/bkmp-guild.js, `?v=20260721-a11yinputs1`) hatten nur `placeholder`, kein `aria-label`/`<label>` - jetzt ergaenzt. `#idleAutoBuyToggle` war ein Fehlalarm meines eigenen Sammlers (steckt in einem umschliessenden `<label>`, zaehlt fuer echte Screenreader bereits als benannt) - Sammler-Check um Ancestor-`<label>`/`aria-labelledby` erweitert. Skins/Arena/Gilden-Tech/Gildenboss-Panels zeigten 0 interaktive Elemente bei Teststand C - jeweils verifiziert als echte, korrekte Leerzustaende (keine Gilde beigetreten/keine Skins-Referenzdaten geseedet/keine anderen Spieler in der Arena), keine echten Bugs.

**Speichern/Laden (`tests/e2e/save-load.spec.js`, Teststand B, 4 Tests):** Ressourcen/Level/EXP, "Bleibt auf dieser Stufe" (`auto_advance`), Runen-Ausruesten und Zwei-Tab-Synchronisierung - alle ueber echte Produktionsfunktionen (`bkmpIdleFlushSyncNow()`, kein Testkopie-Speicherpfad). **Ein weiterer echter Mock-Fidelity-Bug in den eigenen Fixtures gefunden+gefixt** (nicht die App): `idle_player_runes.id` ist laut `sql/supabase-idle-runes.sql:19` eine `uuid`-Spalte (PostgREST serialisiert das immer als String), meine Teststand-Fixtures nutzten aber numerische `id`-Werte (5001, 6000, ...) - `bkmpRuneToggleEquip()`s `r._cid === cid`-Vergleich (`===`, `dataset.cid` liefert immer einen String) scheiterte dadurch lautlos, der Ausruesten-Klick tat sichtbar nichts. Alle Teststand-Fixtures (B/C/D/E) auf String-IDs (`qa-rune-...`) umgestellt. **Ein zusaetzlicher, noch nicht behobener echter App-Bug gefunden** (Auftrag Abschnitt 32, "aelterer Zustand ueberschreibt keinen neueren Stand"): `bkmpIdlePreloadStateIfNamed()` (idledorf.js:2406) laedt `bkmpIdleState` im Hintergrund in JEDEN offenen Tab, auch wenn das Idle-Dorf-Fenster dort nie geoeffnet wurde; wird dieser Hintergrund-Tab neu geladen, ueberschreiben seine beforeunload/visibilitychange-Flush-Handler die Datenbank-Zeile mit seinem eigenen (jetzt veralteten) Stand und loeschen damit einen zwischenzeitlich in einem ANDEREN Tab gespeicherten Fortschritt - `idle_prestige_state` hat genau dafuer bereits eine Absicherung (monotoner `prestige_level`-Zaehler, siehe Bug 11 oben), die Kernspalten von `idle_player_state` (Gold/Ressourcen/Level) aber nicht. Der zugehoerige Test ist bewusst mit `test.fail()` als bekannter, verfolgter Fehlschlag markiert (nicht blind gefixt - eine echte Abhilfe muesste denselben Monoton-/Frische-Schutz auf `upsertIdlePlayerState()` ausweiten, ein zentraler, oft veraenderter Speicherpfad). Nebenbefund: derselbe Kraftrune-Lager-Ueberlapp-Bug wie beim Tab-Leisten-Fix oben tritt auch HORIZONTAL auf (ueberdeckt `idleRuneEquipBtn` in der Detailbox bei dieser Kartenbreite) - fuer den Save-Load-Test einfach zugeklappt (wie ein Spieler es auch koennte), volle Untersuchung bleibt der eigenen Runen-Suite ueberlassen.

**Kampftest (`tests/e2e/combat.spec.js`, Teststand A, 7 Tests):** Szene rendert gueltigen Drachen/HP, manueller Klick-Schaden, automatischer Tick-Schaden ueber echte Zeit, HP nie negativ/NaN, Drachen-Sieg erhoeht Kills/Stufe, Boss bei Stufe 25, Reload waehrend des Kampfes behaelt Stufe + volle Dorf-HP. Kein neuer App-Bug gefunden - die Suite deckte stattdessen eine Test-Infrastruktur-Luecke auf (`waitForIdleStateReady` bestaetigte nur `bkmpIdleState`, nicht dass `bkmpIdleCurrentDragon` bereits gesetzt ist - beide werden in `bkmpIdleOpenModal()`s async-Ablauf zu leicht unterschiedlichen Zeitpunkten befuellt). Fix: neue `waitForDragonReady()`-Hilfsfunktion (`tests/helpers/qa-fixtures.js`), seitdem nach jedem `openAndLogin`/`openAppMode` in Kampf-/Streak-/Prestige-Suiten verwendet.

**Runentest (`tests/e2e/runes.spec.js`, 5 Tests ueber 4 `describe`-Bloecke, Teststand D/B/C/E):** Teststand D (bewusst mit zwei gleichzeitig ausgeruesteten Runen derselben Art geseedet) - automatische Bereinigung beim Laden auf genau eine ausgeruestete Rune pro Art bestaetigt (`bkmpRuneNormalizeDuplicateEquips()`, siehe Bug 4 der Nacht vom 20.07. oben). Teststand B: Ausruesten-Konflikt (zweite Rune derselben Art ersetzt statt zu addieren) + Ausruesten-dann-andere-ausruesten-Wechsel. Teststand C: alle 6 Slots real belegt, genau eine Rune pro Art. Teststand E: 300 Runen im Inventar laden ohne Absturz, weiterhin exakt 6 ausgeruestet. **Ein eigener Fixture-Bug gefunden+gefixt** (nicht die App): Teststand-Fixtures nutzten erfundene deutsche Slugs fuer `rune_type`/`rarity` (z.B. `'wucht'`, `'gewoehnlich'`) statt der echten Werte aus `window.BKMP_RUNE_SLOTS`/`BKMP_RUNE_RARITIES` (`js/systems/bkmp-runes.js:24-31`: `'slot1'`-`'slot6'`, `'gray'/'green'/'blue'/'purple'/'gold'`) - Runen erschienen dadurch fuer die echte App als unbekannte/leere Kategorie. Alle Fixtures korrigiert. Der bereits in der Save-Load-Suite gefundene horizontale Kraftrune-Lager-Ueberlapp (verdeckt `#idleRuneEquipBtn`) wird hier durch bewusstes Zuklappen des Balikens vor dem Ausruesten-Klick umgangen (wie ein echter Spieler es koennte) - volle Behebung bleibt offen (siehe unten).

**Dungeon-Zeittest (`tests/e2e/dungeon-time.spec.js`, 7 Tests ueber 2 `describe`-Bloecke):** ein Block mit echter `Date.now()`-Startzeit fuer Stunden-/Tage-Massstab-Tests (Schluessel-Regeneration, taeglicher Bonus), ein zweiter Block mit fest auf eine 4h-Berlin-Zeitgrenze verankerter Startzeit (`Date.parse('2026-01-15T07:00:00.000Z')` = 08:00 Berlin-Winterzeit) fuer den praezisen "3h59m vs. gerade-eben-Slot-ueberschritten"-Test. Lief gegen einen originalgetreuen JS-Nachbau der echten SQL-RPC-Funktionen (`tests/mock/rpc-engine.js`, siehe oben) - keine neue Logik dupliziert, nur die bestehende SQL-Funktion in JS nachgebildet. **Ein echter Mock-Fidelity-Bug gefunden+gefixt, der beinahe als Test-Flakiness durchgegangen waere:** ein 25h-`store.clock.advance()` liess eine Mock-Sitzung (urspruenglich 3600s Gueltigkeit) aus Sicht des Mocks (dessen "jetzt" gerade gesprungen war) abgelaufen wirken, waehrend die echte Browser-Uhr nie bewegt wurde - ein daraus resultierender 401-dann-Retry konnte eine Testsequenz genau in diesem Moment mit voruebergehend veralteten Daten treffen. Root-Fix: Mock-Sitzungen laufen jetzt 50 Jahre statt 3600s (siehe `tests/mock/auth-engine.js`-Kommentar) - verhindert dieselbe Bugklasse praeventiv auch in allen anderen Zeitreise-Suiten (Login-Streak/Offline-AFK).

**Login-Streak-Zeittest (`tests/e2e/login-streak.spec.js`, 6 Tests, Teststand A, `useFakeClock:true`):** rein clientseitig (`localStorage 'bkmp-idle-login-streak'`, `bkmpIdleCheckDailyStreak()`, `js/systems/bkmp-events.js`) - hier zaehlt (anders als beim Dungeon-System) tatsaechlich die Browser-Uhr, da kein Server-Roundtrip stattfindet. `page.clock.setFixedTime()` statt Vorspulen in Echtzeitschritten (ein echter Tagessprung wuerde sonst jeden dazwischenliegenden 900ms-Kampf-Tick tatsaechlich abfeuern). Getestet: Tag 1 zaehlt + zahlt Gold, zweiter Aufruf am selben Tag zahlt nicht doppelt, naechster Kalendertag erhoeht auf 2, ein uebersprungener Tag setzt auf 1 zurueck, jeder 5. Tag zahlt zusaetzlich Edelsteine, Reload direkt nach Beanspruchen zahlt nicht doppelt. **Ein Playwright-Fixture-Eigenheit gefunden+gefixt** (nicht die App): `test.use({useFakeClock:true})` installierte die gefakte Uhr allein NICHT - Playwright-Fixtures laufen nur, wenn ein Test sie tatsaechlich destructured; da kein Test die `qaClock`-Fixture explizit anforderte, lief ihr `page.clock.install()` nie. Fix: `qaClock` wird jetzt in jedem Test destructured (erzwingt die Aktivierung), auch wenn `page.clock.setFixedTime()` direkt aufgerufen wird statt `qaClock`s eigener `.advance()`-Methode.

**Offline-/AFK-Test (`tests/e2e/offline-afk.spec.js`, 7 Tests, Teststand B):** laeuft ueber die ECHTE, unveraenderte `api/claim-idle-offline-progress.js` (per `tests/mock/offline-progress-handler.js` in-process aufgerufen) - keine Testkopie der Belohnungsformel. Getestet: <60s Abwesenheit zahlt nichts, 30min zahlt eine Belohnung, 4h zahlt mehr als 30min, Deckel bei 12h greift auch nach 30 Tagen (siehe Bug-9-Fix vom 20.07. oben), Belohnungen/Endwerte nie negativ/NaN auch nach 7 Tagen, ein zweiter Claim direkt danach zahlt nicht doppelt, Login-Streak wird durch Offline-Claims nicht mehrfach ausgeloest (zwei unabhaengige Systeme bestaetigt getrennt). Kein neuer App-Bug gefunden - bestaetigt stattdessen, dass die Bugs 6/7 aus der Nacht vom 20.07. (Magie-Skilltree-Boni + seltene Drachen in der Offline-Simulation) korrekt wirken.

**Prestige-Test (`tests/e2e/prestige.spec.js`, 5 Tests, Teststand B):** laeuft gegen die echte `bkmpPrestigeExecuteReset()` - Mindest-Drachenstufe direkt gesetzt (`bkmpIdleState.highest_dragon_index = 150`) statt hunderte Kaempfe zu simulieren, das Reset-VERHALTEN selbst bleibt unveraenderter Produktionscode. Getestet: Button erst ab Mindeststufe sichtbar, vollstaendiger Aufstieg setzt Level/Gold/Ressourcen/Skillpunkte/Upgrades/Drachen-Fortschritt zurueck UND erhoeht Prestige-Level/-Punkte, waehrend `dragon_kills`+Runen (inkl. Ausruestung) erhalten bleiben (siehe Phase-5.3-Entscheidung), Abbrechen im Bestaetigungsdialog aendert nichts, ein zweiter schneller Klick auf "endgueltig aufsteigen" loest keinen doppelten Aufstieg aus (`bkmpPrestigeSaving`-Sperre bestaetigt), Reload nach dem Aufstieg behaelt den neuen Stand. Kein neuer App-Bug gefunden.

**Mobile-Smoke-Test (`tests/e2e/mobile-smoke.spec.js`, 6 Tests, nur auf `mobile-small`/`mobile-large`-Projekten, ueberspringt sich selbst sauber auf Desktop-Projekten via `test.skip()` in `beforeEach`):** bewusst `openAndLogin()` (normale Website) statt `openAppMode()`, da der kompakte Nav rein ueber `matchMedia(max-width:760px)` greift, nicht ueber `?app=idledorf`. Getestet: kompakte Navigation ersetzt die Desktop-Tableiste, ein Haupt-Tab in der kompakten Nav schaltet das echte Panel um, "Mehr" oeffnet das echte gruppierte Ueberlauf-Sheet (verschiebt echte `.idle-dorf-tab`-Knoten per DOM-Reparenting, keine Proxy-Buttons) und schaltet daraus ebenfalls einen echten Tab um, Kampf-Log oeffnet/schliesst sich als eigenes Bottom-Sheet (Klasse `open`, nicht `visible` wie beim Kraftrune-Balken - andere Komponente, andere Namenskonvention, per Fehlschlag entdeckt), kein horizontales Ueberlaufen der Seite, Reload behaelt die kompakte Navigation. Kein neuer App-Bug gefunden, bestaetigt aber Phase 7.0/7.1 auf echten Mobil-Projekten (vorher nur per injiziertem Mock-Zustand ohne echten Browser-Login gemessen, siehe Phase-7.1-Einschraenkung oben).

**Stufe 1 abgeschlossen (21.07.2026) — finale Vollregression:** `npx playwright test tests/e2e/ --project=chromium-desktop --workers=1` → **60 bestanden, 6 uebersprungen (Mobile-Smoke, korrekt - Projekt ist Desktop), 1 als bekannter Fehler verfolgt (`test.fail()`, Zwei-Tab-Speicher-Race, siehe oben)** - macht 67 Tests insgesamt (Navigation 18, Button-Inventar 2, Speichern/Laden 4, Kampf 7, Runen 5, Dungeon-Zeit 7, Login-Streak 6, Offline/AFK 7, Prestige 5, Mobile-Smoke 6, jeweils passend zur Suiten-Summe). `mobile-smoke.spec.js` zusaetzlich separat auf `mobile-small` UND `mobile-large` je 6/6 gruen bestaetigt.

**Bilanz aller in dieser Phase gefundenen echten App-Bugs:** gefixt (2) - Kraftrune-Lager-Ueberlapp mit der Tab-Leiste (`js/systems/bkmp-runes.js`), fehlende `aria-label` an 3 Gilde-Eingabefeldern (`js/systems/bkmp-guild.js`). Gefunden, bewusst NICHT gefixt, dokumentiert (2) - Mehrfach-Tab-Speicher-Race (`bkmpIdlePreloadStateIfNamed()`/`upsertIdlePlayerState()`, echte Abhilfe braucht denselben Monoton-Zaehler-Schutz wie `idle_prestige_state` bereits hat, ein zentraler/oft veraenderter Speicherpfad - bewusst nicht blind angefasst), Kraftrune-Lager-Ueberlapp HORIZONTAL (`#idleRuneEquipBtn` bei ueblichen Desktop-Breiten, in Tests umgangen statt gefixt). Keine Produktionsdaten beruehrt, nichts gepusht/deployed, kein Zugriff auf das echte Supabase-Projekt (der einzige Beinah-Fall, das Service-Worker-Leck, wurde gefunden und geschlossen, siehe oben).

**Noch offen (Auftrag Abschnitte 21-36, ueber Stufe 1 hinaus):** Erfolge-Tiefenpruefung, Belohnungssystem-Stresstests, Turm/Arena/Gilde/Feedback/Kosmetik/Einstellungen-Suiten, volles Geraete-/Viewport-Raster, visuelle Regression, gezielte Netzwerkfehler-Simulation, Performance-/Speicherleck-Tests, Fuzz-Tests - keiner davon war Teil der ersten Umsetzungsstufe (Auftrag Abschnitt 41), brauchen explizite Freigabe fuer eine Stufe 2.

## Öffentliches Feedback-, Bug- und Entwicklungsboard (neuer Auftrag, 20.07.2026) — Stufe 1-4 fertig, live

Neues Feature, komplett getrennt vom Redesign-Plan oben. Nutzer-Auftrag: öffentlicher Bereich, in dem ausgewählte Feedback-Einreichungen mit Admin-Antwort/Status/Fortschritt sichtbar sind — Spieler sollen sehen, dass Feedback gelesen wird, bekannte Bugs transparent kommuniziert werden, doppelte Meldungen sinken.

**⚠️ Ungeplant bereits live (21.07.2026):** Stufe 1 (reiner UI-Prototyp) wurde gepusht/deployed, obwohl das nicht beauftragt war — weder Claude noch der Nutzer haben bewusst `git push` ausgelöst, `git status` zeigte HEAD trotzdem deckungsgleich mit `origin/main`. Ursache ungeklärt (siehe `[[feedback_bkmp_user_handles_commits]]`-Memory, Update 2). Nutzer hat entschieden, es live zu lassen und direkt mit Stufe 2 weiterzumachen, statt zurückzurollen. **Seitdem: nicht mehr von "ungeplant live" ausgehen** - Push scheint in dieser Umgebung nicht zuverlässig user-gated zu sein, vor jeder Annahme "das ist noch nicht live" den echten Stand pruefen (`git status -sb`/`rev-list`, im Zweifel die echte URL).

Danach explizit vom Nutzer freigegeben und **komplett bis Stufe 4 durchgezogen** (SQL ausgeführt, echte Admin-Anbindung, öffentliches Board laedt echte Daten) - kein Zwischenstopp zwischen den Stufen noetig, da der Nutzer durchgehend zugestimmt hat ("jup").

**Bestehende Datenstruktur (analysiert, nicht verändert):** `public.feedback` (`id, name, category, message, image_url, is_read, is_archived, created_at`), Kategorien `lob/idee/kritik/bug/sonstiges` per Check-Constraint. Insert läuft über `api/submit-entry.js` mit Service-Role-Key (RLS: anon darf nur einreichen, `is_active_admin()` darf lesen/ändern/löschen). Admin-Verwaltung in `admin.html` (`renderFeedbackList` etc.): rein privat, nur gelesen/archiviert/gelöscht — kein Status-Workflow, keine Öffentlich/Privat-Trennung, keine reichere Kategorie-Liste. Kein bestehendes öffentliches Status-Board vorhanden.

**Neu gebaut (Stufe 1, reiner UI-Prototyp mit lokalen Testdaten, `js/ui/bkmp-feedback-board.js`):**
- Zweiter Button "📋 Status" direkt neben dem bestehenden "💬 Feedback"-Button (`.feedback-btn-group` in `.top-actions-left`, index.html).
- Neues Panel `#feedbackStatusOverlay`/`.fbb-panel` — zentriert auf Desktop (`min(680px,92vw)`), echtes Bottom-Sheet auf Mobile (≤760px, `max-height:90dvh`, verifiziert per `getBoundingClientRect` korrekt bodenbündig). Zusammenfassung (Anzahl bekannte Bugs/in Arbeit/behoben), Filter-Chips (Alle/Bekannte Bugs/In Arbeit/Geplant/Behoben/Ideen/Kritik), verzögerte Suche (200ms Debounce), aufklappbare Karten (Status-Icon+Farbe nach Auftrag Abschnitt 2, Kategorie-Chip, Beschreibung/Antwort/Betroffen-Auswirkung-Übergangslösung/Fortschrittsverlauf beim Aufklappen).
- Duplikat-Hinweis im bestehenden privaten Formular (`.feedback-duplicate-hint`, oberhalb des Nachrichtenfelds) mit "Bekannte Bugs ansehen"-Button — blockiert das Absenden NICHT.
- Admin-Workflow-Vorschau (`#fbbAdminMock` in admin.html, Feedback-Seite) — Formular für öffentlichen Titel/Kategorie/Status/Beschreibung/Antwort/geplante Version/Fortschritt/Anzeigename, live-aktualisierte Vorschau-Spalte (nutzt dieselbe `bkmpFeedbackBoardEntryCardHtml()` wie das echte Board). Rein lokaler Zustand, deutlich als Prototyp markiert, geht bei Neuladen verloren.
- Fokus-Falle wiederverwendet aus `bkmpUiTrapFocus()` (Phase 3, war bis dahin ungenutzt).
- **Echter Bug beim eigenen Testen gefunden+gefixt:** `.fbb-card-body{display:flex}` schlug die UA-Standardregel `[hidden]{display:none}` in der Kaskade (Autor-Styles gewinnen immer) — jede Karte zeigte ihren Inhalt IMMER, unabhängig vom Aufklapp-Zustand. Fix: `.fbb-card-body[hidden]{display:none}` (höhere Spezifität durch Attribut-Selektor).

**Stufe 2 (Datenbank, `sql/20260721-feedback-public-board.sql`, live ausgeführt):** bewusst eine GETRENNTE neue Tabelle `public.feedback_public` (nicht zusätzliche `public_*`-Spalten auf der bestehenden `feedback`-Tabelle) — Grund: eine gemeinsame Tabelle mit RLS `using(is_public=true)` würde bei Postgres row-level (nicht column-level) Security automatisch auch `name`/`message`/`image_url` für veröffentlichte Zeilen offenlegen. Eine strukturell getrennte, ausschließlich admin-beschriebene Tabelle macht ein versehentliches Leak privater Felder strukturell unmöglich statt nur durch Policy-Disziplin zu verhindern. Enthält `source_feedback_id` (Referenz, nicht öffentlich gelesen), `kind` (bug/idea), `title/category/status/description/response/author_mode/author_display/duplicate_of/planned_release/is_published/published_at/resolved_at/last_public_update/affects_count`, plus separate `public.feedback_public_progress` (`feedback_public_id, text, created_at, sort_order`) für den Fortschrittsverlauf. `affects_count` (Abschnitt 13, "Betrifft mich auch") bleibt inerte Spalte - die eigentliche Stimmen-Zählung MIT Mehrfachstimmen-Schutz braucht eine eigene Verknüpfungstabelle und ist bewusst NICHT Teil dieser Migration (separat zur Freigabe vorzulegen, siehe Auftrag Abschnitt 13). ⚠️ `kind` wurde erst NACH der ersten Ausführung der Datei ergänzt (Claude-Fehler beim Timing) - die Datei enthält seitdem einen nachtraeglichen `alter table ... add column if not exists kind ...`, der beim zweiten Ausfuehren live nachgeholt wurde. Bei kuenftigen Aenderungen an dieser Tabelle: IMMER pruefen, ob die Datei noch mit dem echten Live-Schema uebereinstimmt (z.B. `curl` gegen die REST-API), nicht blind von der Datei ausgehen.

**Stufe 3 (echte Admin-Anbindung, live):** neue Supabase-Funktionen in `supabase.js` (`loadFeedbackPublicList/upsertFeedbackPublicEntry/deleteFeedbackPublicEntry/loadFeedbackPublicProgress/addFeedbackPublicProgress/deleteFeedbackPublicProgress`), gleiches `bkmpGetSupabaseClient()`-Prinzip wie überall sonst - RLS entscheidet selbst, ob anonym (nur `is_published=true`) oder eingeloggter Admin (alles inkl. Entwürfe) sichtbar ist, kein eigener Code-Pfad nötig. Neuer "📋 Öffentlich machen"-Button an jeder privaten Feedback-Karte (`admin.html`, `renderFeedbackList`) startet einen Entwurf im Editor (`#fbbAdminMock`/`#fbbAdminEditorWrap`) - kopiert bewusst NICHTS automatisch in öffentliche Felder (nur die Kategorie als Startwert), die private Nachricht bleibt nur als Referenz-Box sichtbar ("🔒 Nur für dich sichtbar"). Liste bereits veröffentlichter/entworfener Einträge (`#fbbAdminPublishedList`) mit Bearbeiten/Löschen. Fortschrittseinträge lassen sich erst nach dem ersten Speichern hinzufügen (brauchen eine echte `feedback_public_id`).

**Stufe 4 (öffentliches Board lädt echte Daten, live):** `bkmpFeedbackBoardEnsureDataLoaded()` lädt `feedback_public` (nur `is_published=true`, via RLS) + parallel den Fortschritt jedes Eintrags. Bei Ladefehler (z.B. Migration doch noch nicht gelaufen) zeigt das Board einen ehrlichen Fehlerhinweis statt stillschweigend auf `BKMP_FEEDBACK_BOARD_TEST_DATA` (Stufe-1-Testdaten, bleiben nur als Referenz im Code) zurückzufallen - echte Besucher dürfen nie erfundene Beispiel-Bugs für echte halten.

Cache-Busting: `style.css?v=20260721-feedbackboard4`, `js/ui/bkmp-feedback-board.js?v=20260721-stufe34`, `supabase.js?v=20260721-feedbackpublic1`.

## Lokale QA-/Test-Grundlage Phase 1 (24.07.2026) — fertig, nichts committed/gepusht

Eigenständiger Auftrag, komplett getrennt vom Redesign-Plan oben: professionelle lokale QA-Grundlage (Mock-Modus + erste Playwright-Suite), ausdrücklich nur Phase 1 (nicht "jedes Spielfeature testen"). **Keine Produktionsdaten berührt, keine echte Supabase-DB angefasst, nichts gepusht/deployed** (Ausnahme: die schon lange bekannte Auto-Push-Eigenart dieser Umgebung, siehe `[[feedback_bkmp_user_handles_commits]]`-Memory - lokal wurde nichts committet).

**Ausgangslage (per Analyse bestätigt, nicht vermutet):** Phase 7.2/7.3 (siehe oben) hatte bereits eine reife, isolierte Playwright-Suite gebaut (`tests/mock/*`, 11 Spec-Dateien, HTML-Report/Screenshots/Traces bereits konfiguriert) — aber ausschließlich über Playwright's `context.route()`-Netzwerk-Interception, unsichtbar/unbenutzbar für einen normal geöffneten Browser. Es gibt **eine einzige Supabase-Instanz** für lokale Entwicklung UND Produktion (kein Staging) — ein naiver In-App-QA-Modus, der nur `bkmpIdleState` im Speicher verbiegt, würde beim nächsten Autosave trotzdem gegen die echte DB schreiben. Kein zentrales Zeit-Abstraktum vorhanden, `Date.now()`/`new Date()` direkt verstreut.

**Umsetzung:**
- **`window.BKMP_QA_MODE`** (index.html, frühes Kopf-Skript direkt nach der bestehenden App-Modus-Erkennung): nur `true`, wenn Hostname `localhost`/`127.0.0.1`/`::1` UND `?qa=1` in der URL — Positivliste, kein Domain-Ausschlussvergleich. Auf der echten Website strukturell nie aktivierbar.
- **`supabase.js`:** `SUPABASE_URL`/`SUPABASE_ANON_KEY` zeigen bei aktivem `BKMP_QA_MODE` auf `location.origin` (lokaler Mock-Server, siehe unten) statt auf das echte Projekt; `bkmpIsSupabaseConfigured()`s bisheriger https-Zwang gilt nur noch außerhalb des QA-Modus (lokaler Mock läuft über einfaches `http://`). Beide Änderungen sind rein additive Bedingungen — außerhalb des QA-Modus exakt das alte Verhalten.
- **`tests/mock/server.js`** (bereits vorhandener Mock aus Phase 7.2, erweitert statt neu gebaut): generischer `/auth/v1|/rest/v1|/realtime/v1`-Durchgriff auf dasselbe `router.js`, das Playwright schon nutzt (bisher nur für Playwright's Netzwerk-Interception aufgerufen, jetzt zusätzlich als echter HTTP-Endpunkt erreichbar) + zwei neue `/__qa__/status`/`/__qa__/reseed`-Endpunkte (nur 127.0.0.1) für das Kontrollfenster. Zusätzlich ein minimaler, protokoll-korrekter WebSocket-Handshake für `/realtime/v1/websocket` (echter Bug beim eigenen Testen gefunden: ohne Handshake zeigte ein echter Browser einen sichtbaren "WebSocket handshake failed"-Konsolenfehler; die Sockets müssen beim `close()` aktiv zerstört werden, sonst hängt `server.close()` für immer, weil Node auf das Ende aller offenen Verbindungen wartet — auch das erst beim eigenen Testen gefunden, nicht vorher bedacht).
- **`scripts/qa-server.js`** (neu): eigenständiger, dauerhaft laufender Node-Server (`npm run qa:server -- --teststand=B --port=4173`), seedet den Mock mit einem der 5 bestehenden Teststand-Fixtures (A–E, aus Phase 7.2) und druckt die fertige `?qa=1`-URL.
- **`js/dev/bkmp-qa-panel.js`** (neu, self-gated auf `window.BKMP_QA_MODE`, sonst reiner No-Op): schwebendes Kontrollfenster unten rechts, eigenes inline-CSS (style.css bewusst nicht angefasst). Funktionen: Teststand A–E laden/zurücksetzen (ruft `/__qa__/reseed`, danach `localStorage.clear()` + Redirect mit `&stand=X` — der QA-Panel-Code selbst meldet sich dann automatisch mit den Teststand-Zugangsdaten an, identisch zum bestehenden `loginThroughForm`-Playwright-Helfer), Gold/EXP/Testressourcen setzen, Hauptbereiche freischalten (hohe Stufe/Drachenindex/Skillpunkte), Mobile-Fenster öffnen (`window.open` mit fester Größe 390×844), aktueller Status (Server erreichbar, Teststand, Level/Gold/XP), GameClock-Steuerung (+1h/+1Tag/Zurücksetzen/Anzeige).
- **`js/core/bkmp-game-clock.js`** (neu): `bkmpGetGameNow()`/`bkmpGameClockAdvance()`/`bkmpGameClockReset()`/`bkmpGameClockIsSimulated()`. Außerhalb des QA-Modus IMMER identisch zu `Date.now()` — kein Verhaltensunterschied im normalen Spiel. Offset lebt nur im Arbeitsspeicher (kein localStorage), geht bei Reload verloren — für Phase 1 bewusst ausreichend. **Nur EINE Zeitstelle migriert** (`bkmpIdleCheckDailyStreak()` in `js/systems/bkmp-events.js`, rein clientseitig/localStorage, kein Server-Schreibzugriff) — der Nutzer-Auftrag verlangt ausdrücklich "nicht blind jede Zeitstelle ändern". **Bewusst NICHT migriert:** der Kampf-Tick, Offline-Claim-Berechnung, alle Sync-Sperren-Zeitstempel in `idledorf.js` sowie die komplette serverseitige `api/claim-idle-offline-progress.js`-Simulation — bleibt Aufgabe einer künftigen Stufe, falls Zeitsprung-Tests für diese Bereiche gebraucht werden.
- **`tests/e2e/qa-mode-smoke.spec.js`** (neu): einziger Test dieser Stufe, der NICHT über `tests/helpers/qa-fixtures.js`s Netzwerk-Interception läuft, sondern bewusst denselben Mock-Server wie ein Mensch (`scripts/qa-server.js`) auf einem echten `127.0.0.1`-Port startet und mit `?qa=1&stand=B` navigiert — prüft den NEUEN Mechanismus selbst (Gate, URL-Umschaltung, Panel-Auto-Login), nicht nur die schon anderswo getestete Navigation. Deckt den kompletten 10-Punkte-Auftrags-Checklist ab (Laden ohne kritischen Fehler, Hauptnavigation sichtbar, alle 15 Tabs einzeln mit exists/sichtbar/anklickbar/im-Viewport/nicht-überlagert-Prüfung inkl. echtem `elementFromPoint`-Hit-Test, keine Konsolenfehler/fehlgeschlagenen Netzwerkaufrufe, läuft identisch auf `chromium-desktop` + `mobile-small` + `mobile-large`). `data-testid`-Attribute an allen 15 Tab-Buttons + Tab-Leiste + Kompakt-Nav-Container + Öffnen-Button ergänzt (rein additiv neben den bereits stabilen, von der bestehenden Suite genutzten Element-IDs).
- **`package.json`:** neue Scripts `qa:server`/`qa:smoke`/`qa:report`, bestehende `test:e2e:*`-Scripts unverändert.

**Ein echter, beim eigenen manuellen Testen im echten Browser gefundener Bug (nicht nur behauptet, per curl+Browser-Konsole isoliert):** eine erste Fassung von `qaPatchState()` (Panel) schrieb Werte direkt per Server-Endpunkt in die Mock-DB und lud dann neu — der Endpunkt selbst arbeitete nachweislich korrekt (per `curl` einzeln bestätigt), aber die noch offene Spielseite lief mit ihrer EIGENEN, unveränderten Sync-Schleife (`bkmpIdleQueueSync`/`bkmpIdleFlushSync`) weiter und überschrieb den frisch gepatchten Stand innerhalb weniger Sekunden wieder mit ihrem eigenen (nie aktualisierten) Speicherabbild — kein Bug im Endpunkt, sondern ein Wettlauf zweier unabhängiger Schreiber auf dieselbe Zeile. Fix: `bkmpIdleState` wird jetzt direkt im Speicher aktualisiert (genau wie an jeder anderen Stelle im Spiel) und synchron über die echte, bereits von `save-load.spec.js` genutzte `bkmpIdleFlushSyncNow()` geschrieben — der separate `/__qa__/patch-state`-Endpunkt wurde danach als Fallenlassen-Falle wieder entfernt (nicht nur ungenutzt, sondern ein konkretes Risiko für denselben Bug bei künftiger Wiederverwendung).

**Verifiziert:** volle bestehende Suite (`tests/e2e/`, 76 Tests) auf `chromium-desktop` lief nach allen Änderungen unverändert durch (gleiches Ergebnis wie vor Beginn: 1 bekannter, absichtlich per `test.fail()` verfolgter Fehlschlag, siehe Phase-7.2-Bericht — keine neue Regression). Neuer `qa-mode-smoke.spec.js` grün auf allen 3 Projekten. Zusätzlich **echter Browser-Livetest** (nicht nur Playwright): `scripts/qa-server.js` gestartet, `?qa=1&stand=C` geöffnet, QA-Panel erschien, Auto-Login+Idle-Dorf-Öffnen funktionierte, "Bereiche freischalten"/"Viel Gold"/GameClock-Buttons einzeln geklickt und sowohl im Client (`bkmpIdleState`) als auch server-seitig (`curl` gegen die Mock-REST-API) auf tatsächliche Persistenz nach Reload geprüft. `node --check` auf allen neuen/geänderten `.js`-Dateien sauber.

**Nicht geprüft/offen für eine mögliche Stufe 2:** GameClock-Migration weiterer Zeitstellen (Kampf-Tick/Offline-Claim), Playwright-Abdeckung für Erfolge/Turm/Arena/Gilde/weitere Tabs über die bereits bestehende Phase-7.2-Suite hinaus, Lint/Typecheck (Projekt hat keine — reines `node --check` als Äquivalent verwendet), GameClock-Offset-Persistenz über Reload hinweg (bewusst nur In-Memory für Phase 1).

Cache-Busting: `js/core/bkmp-game-clock.js?v=20260724-qafoundation1` (index.html/admin.html/idle-stream-mini.html), `js/dev/bkmp-qa-panel.js?v=20260724-qafoundation3` (index.html), `js/systems/bkmp-events.js?v=20260724-qafoundation1` (alle 3 Dateien).

## Lokale QA-/Test-Grundlage Phase 2 (24.07.2026) — fertig, nichts committed/gepusht

Fortsetzung von Phase 1 (siehe oben) - vollständige, selbst durchgeführte Prüfung + gezielter Ausbau. **Phase 1s eigener Abschlussbericht war an einer Stelle nachweislich falsch** (behauptete "Exit-Code 0" für einen 3-Projekte-Lauf, der tatsächlich nie fertig geprüft wurde) - Phase 2 hat deshalb NICHTS aus Phase 1 als gegeben angenommen, sondern jede Aussage frisch nachgemessen.

**Größter echter Fund (Testinfrastruktur, kein App-Bug):** `buttons-inventory/dungeon-time/navigation/prestige/runes/save-load.spec.js` (6 der damals 12 Dateien) klicken echte Desktop-Tab-Buttons per Playwrights `.click()` (verlangt Sichtbarkeit) - auf `mobile-small`/`mobile-large` (in `playwright.config.js` konfiguriert, laufen standardmäßig JEDE Spec-Datei) sind diese Knoten korrekt unsichtbar (kompakte Navigation, siehe Phase 7.0-7.3), der Klick hängt bis zum 30s-Timeout. Betraf **36 von 76 Tests pro Mobil-Projekt**, IMMER, auch bei `--workers=1` (widerlegt die ursprüngliche Ressourcenkonkurrenz-Theorie als alleinige Erklärung) - niemand hatte die volle Suite je gegen alle 3 Projekte laufen lassen. Fix: `test.beforeEach()`-Skip-Wache in allen 6 Dateien (identisches Muster wie das bereits bestehende `mobile-smoke.spec.js`, nur umgekehrt) - Laufzeit der vollen 3-Projekte-Suite fiel dadurch von **42,8 Minuten auf 9,2 Minuten**. Kein App-Verhalten geändert, nur korrekt gescopte Tests.

**Sicherheitsaudit (Schritt 2, alle Punkte einzeln im Code verifiziert, nicht nur behauptet):**
- QA-Erkennung (`index.html`) läuft vor jedem `<script src>`, inkl. `supabase.js` - bestätigt per Leseреihenfolge.
- Genau 3 `createClient(...)`-Aufrufe im gesamten Projekt (`supabase.js`), alle an `SUPABASE_URL`/`SUPABASE_ANON_KEY` gebunden - keine weitere, versteckte Verbindung gefunden (`grep -rn "createClient("`).
- Kein `fetch()`/`XMLHttpRequest`/`new WebSocket` im First-Party-Code, das einen fest codierten Host statt eines relativen Pfads oder der `SUPABASE_URL`-Konstante nutzt (vollständig aufgelistet, jede Zeile einzeln bewertet).
- `api/claim-idle-offline-progress.js` (server-seitig, eigene hartcodierte `SUPABASE_URL`) wird im QA-Mock NUR in-process mit monkey-gepatchtem `global.fetch`/`global.Date` aufgerufen (`tests/mock/invoke-vercel-handler.js`) - der echte Host-String im Handler-Code ist dadurch irrelevant, verifiziert durch Lesen des Mocks, nicht nur angenommen.
- **Zwei echte, kleine Lücken gefunden+gefixt:** (1) `<link rel="preconnect" href="https://zgknyrwzpohvfdweomxf.supabase.co">` in `index.html` war NICHT an `window.BKMP_QA_MODE` gekoppelt - reiner DNS/TCP/TLS-Kontakt ohne Nutzdaten, aber ein echter (wenn auch harmloser) Produktionskontakt im QA-Modus. Fix: per JS nur noch eingefügt, wenn `!window.BKMP_QA_MODE`. (2) QA-Modus nutzte bisher dieselben `localStorage`-Schlüssel wie Produktion (durch Browser-Origin-Trennung strukturell bereits sicher, da der QA-Server IMMER auf einer anderen Origin als die echte Website läuft - trotzdem als Verteidigung-in-der-Tiefe ergänzt). **Beim eigenen Ändern selbst einen Fehler gemacht und vor dem Verlassen der Datei korrigiert:** ein erster Versuch fügte den neuen `storageKey` auch für den PRODUKTIONS-Zweig ein (`bkmpQaStorageKey('customer-auth')` gab fälschlich immer einen Wert zurück statt `undefined` außerhalb des QA-Modus) - das hätte JEDEN echten, bereits eingeloggten Spieler beim nächsten Besuch still ausgeloggt (Session unter dem neuen Schlüssel nicht mehr auffindbar). Per `git diff` gegen den Ausgangscommit geprüft und korrigiert, bevor irgendetwas committet wurde.
- **Ein echter, unerwarteter externer Netzwerkpfad gefunden:** `js/core/bkmp-site.js:1456` lädt den Spieler-Avatar von `https://minotar.net/helm/<Name>/96.png` (öffentlicher, zustandsloser Minecraft-Skin-Renderer) - komplett unabhängig von Supabase, keine sensiblen Daten (nur der ohnehin öffentliche Anzeigename als URL-Teil). Bewusst NICHT gefixt (kein Bug, ein legitimes bestehendes Feature), aber als einzige, eng benannte Ausnahme in den neuen Sicherheitstest aufgenommen.
- Neuer `tests/e2e/qa-mode-security.spec.js`: vollständiger 13-Schritte-Ablauf (öffnen/anmelden/Testspielstand laden/Ressourcen ändern/speichern/Auto-Save/mehrere Bereiche öffnen/neu laden/Testspielstand erneut laden/ausloggen/erneut einloggen/Zeit vorspulen/erneut speichern), `page.on('request')`+`page.on('websocket')` erfassen JEDE Anfrage seit Testbeginn, Assertion schlägt bei irgendeinem nicht erlaubten Host fehl. Grün auf allen 3 Projekten.

**Feature-Matrix:** `tests/FEATURE_MATRIX.md` (neu) - jedes tatsächlich im Code vorhandene System (Kampf/Dungeons/Runen/Gilde/Arena/Raid/Drachen/Prestige/Erfolge/Turm/Kosmetik/Events/Bestenliste/Navigation) mit Datei/Kernfunktionen/vorhandenem Test/fehlendem Test/benötigtem Zustand. Größte dokumentierte Lücke: `tests/mock/rpc-engine.js` implementiert nur die Dungeon-RPCs - Gilde/Arena/Raid haben serverseitig KEINE Mock-Simulation (fallen auf einen permissiven No-Op zurück), deshalb komplett ungetestet. Kein Testcode-Problem allein, eine fehlende Mock-Infrastruktur-Voraussetzung.

**Teststände F+G ergänzt** (`tests/fixtures/teststands.js`, `js/dev/bkmp-qa-panel.js`): F = unmittelbar vor der ersten Prestige-Freischaltung (`highest_dragon_index:99`, echte Schwelle ist 100, siehe `bkmpPrestigeRequiredStage`), G = keine Dungeon-Schlüssel (0/5 in allen 7 Typen, `dungeon_keys`-Tabelle vorab geseedet statt der sonst faulen Erstanlage in `rpc-engine.js`).

**Statische Analyse:** `eslint.config.js` (neu, `eslint@9` als devDependency, flaches Config-Format) - `no-undef`/`no-unused-vars` bewusst AUS (Cross-Datei-Globals-Architektur macht sie strukturell unzuverlässig, siehe Kommentar in der Datei), Rest von `eslint:recommended` aktiv. Ergebnis: 3 echte, aber harmlose Funde (redundantes `Boolean()`, unnötiges Escape-Zeichen) + 2 veraltete `eslint-disable`-Kommentare - kein Rauschen. `scripts/static-checks.js` (neu, projektspezifisch, regex-basiert): doppelte globale Top-Level-Deklarationen, Prod-URL-Referenzen außerhalb `supabase.js`, `eval`/`new Function`, `setInterval` ohne erkennbares `clearInterval`, `innerHTML`+Interpolation, `.then()` ohne erkennbares `.catch()`, leere catch-Blöcke (gezählt, nicht einzeln gelistet - im Projekt bewusst genutztes Muster), fehlende/doppelte `<script src>`, verwaiste `data-testid`. Nach Schweregrad sortiert, Exit-Code 1 nur bei CRITICAL/HIGH. Aktuell: 0 CRITICAL/HIGH, 1 MEDIUM (`bkmpGuildPresenceHeartbeatTimer` in `js/systems/bkmp-guild.js:179` hat kein sichtbares `clearInterval` - bewusst NICHT blind gefixt, echte Ursache/richtiger Abschaltzeitpunkt braucht mehr Kontext zum Gildensystem als in dieser Phase verfügbar), Rest LOW/INFO. `npm audit`: 0 Schwachstellen.

**Gemeinsamer Fehler-Sammler:** `attachErrorCapture()` in `tests/helpers/qa-fixtures.js` (neu) - console.error/pageerror/fehlgeschlagene Requests/HTTP-4xx-5xx an einer Stelle, kleine dokumentierte Allowlist (`KNOWN_HARMLESS_CONSOLE_PATTERNS`, aktuell nur die bereits aus Phase 1 bekannte "Supabase enthält keine ..."-Warnung). Bestehende, bereits grüne Spec-Dateien NICHT rückwirkend umgebaut (Risiko/Aufwand nicht gerechtfertigt) - genutzt in `soak.spec.js`, verfügbar für künftige Tests.

**Zufalls-/Dauertest:** `tests/e2e/soak.spec.js` (neu) - mulberry32-Seed-PRNG (keine neue Abhängigkeit), Aktionspool (Tab wechseln, Ressourcen ändern, GameClock vorspulen, Reload, Fenstergröße ändern, Achievements öffnen, ESC), Invarianten-Check alle 5 Aktionen (kein NaN/Infinity/negative Ressourcen, genau ein Hauptbereich aktiv, Navigation erreichbar). `@soak-short` (150 Aktionen, Teil von `qa:full`) + `@soak-long` (1200 Aktionen, eigenes Script `qa:soak:long`, bewusst NICHT Teil von `qa:full`). **Zwei echte Selbst-Funde beim eigenen Ausführen** (beide harmlos, beide gefixt): `net::ERR_ABORTED` bei einem Speicher-Request, den die `reload`-Aktion mitten im Flug abbricht (Chromium) bzw. dieselbe Ursache mit "due to access control checks"-Wortlaut (WebKit/`mobile-large`) - beides normales Browser-Verhalten bei Navigation während einer laufenden Anfrage, kein echter Fehler. Eng gefasste, dokumentierte Ausnahme nur in dieser Datei (nicht in der globalen Allowlist), damit ein echtes CORS-Problem anderswo weiter auffiele.

**Visuelle Grundprüfung:** `tests/e2e/visual.spec.js` (neu) - `@visual-checks` (programmatisch: kein horizontales Überlaufen, keine 0x0-Tab-Buttons, keine Überlagerung der Navigation, keine unsichtbaren Overlays mit aktiven `pointer-events`; läuft auf Desktop/Mobile-Hoch/Mobile-Quer) + separates `@visual-baseline` (echte `toHaveScreenshot()`-Vergleiche für Desktop-/Mobile-Nav, Kampf/Upgrade/Runen/Dungeon-Bereich, QA-Panel) - **bewusst NICHT Teil von `qa:full`/`qa:visual`**, da `toHaveScreenshot()` beim ersten Lauf automatisch eine unbestätigte Baseline anlegen würde (widerspricht "Screenshots dürfen nur nach bewusster Bestätigung aktualisiert werden") - diese Session hat keine Baselines erzeugt, das bleibt ein manueller Schritt für den Nutzer (`--update-snapshots` + PNGs selbst ansehen). **Zwei Selbst-Funde beim eigenen Ausführen gefixt** (beide in der Prüfung selbst, nicht in der App): `getComputedStyle(el).display` prüft nur das Element selbst, nicht versteckte Vorfahren (fälschte "0x0 trotz display≠none" auf Mobilbreiten) - Fix: zusätzlich `el.offsetParent===null` prüfen. Das eigene, immer obenauf schwebende QA-Panel wurde von der Navigations-Überlagerungsprüfung fälschlich als blockierendes Fremdelement gewertet - Fix: `#bkmpQaPanel`-Nachfahren explizit ausgenommen (dokumentierte, enge Ausnahme, kein pauschales Ignorieren).

**npm-Scripts vereinheitlicht** (`package.json`): `qa:server/smoke/navigation/features/time/persistence/visual/static/soak/soak:long/full/report` - `qa:full` = `qa:static` + volle Suite (ohne `@soak-long`/`@visual-baseline`) mit `--workers=1` (siehe Ressourcenkonkurrenz-Erkenntnis oben - auf dieser Maschine bei höherer Parallelität nachweislich unzuverlässig, siehe Phase-7.2-Präzedenzfall in CLAUDE.md). Bestehende `test:e2e:*`-Scripts unverändert.

**Ein pre-existierender, seltener flaky Test bestätigt (NICHT durch Phase 2 verursacht):** `prestige.spec.js` "Reload nach dem Aufstieg behält den neuen (zurückgesetzten) Stand" schlug in EINEM von mehreren vollständigen Suite-Läufen fehl (`gold` nach Reset/Reload einmal `1` statt `0`) - 3/3 isolierte Wiederholungen liefen sauber durch, deutet auf eine seltene, timing-abhängige Race zwischen Prestige-Reset und einem Hintergrund-Tick (ähnliche Bugklasse wie das bereits dokumentierte, gefixte "Bug 9" der Produktionsgebäude-Rundung) - nicht in dieser Phase weiter verfolgt, siehe "nicht reparierte Fehler" im Abschlussbericht.

**Finales, selbst durchgeführtes Ergebnis (3 Projekte, `--workers=1`, `tests/e2e/` ohne `@soak-long`/`@visual-baseline`):** 243 Tests gesamt, 154 bestanden, 88 korrekt übersprungen (Mobil-Skip-Wachen + Mobile-Smoke auf Desktop), 1 bestätigt seltener Flake, 0 unerwartete Fehlschläge sonst. Laufzeit 9,2 Minuten (vorher 42,8 Minuten). `node --check` auf allen 19 neuen/geänderten `.js`-Dateien sauber.

## Lokale QA-/Test-Grundlage Phase 3 (24.07.2026) — Arena+Gilde-Kernmechanik, nichts committed/gepusht

Auftrag "Baue Phase 3" - Umsetzung der in Phase 2 dokumentierten Top-Empfehlung: RPC-Mock für die bis dahin komplett ungetesteten Mehrspieler-Systeme bauen, damit sie überhaupt testbar werden. Umgesetzt: **Arena vollständig, Gilde-Kernmechanik vollständig.** Raid/Gildenboss bewusst NICHT angegangen (siehe unten).

**`tests/mock/store.js` erweitert:** `createStore(startTimeMs, rngSeed)` - optionaler zweiter Parameter, seedbarer mulberry32-PRNG (`store.rng()`) für RPCs, die serverseitig echtes `random()` nutzen (aktuell nur `arena_attack`). Ohne Seed unverändert `Math.random()` - null Verhaltensänderung für jeden bestehenden Test.

**Arena (`tests/mock/rpc-engine.js`, `arena_attack`):** originalgetreuer Port von `sql/supabase-idle-arena-daily-limit.sql` (aktuelle/maßgebliche Fassung - ersetzt die Basisversion aus `supabase-idle-arena.sql` per `create or replace function`, identische Signatur, fügt nur das 10x/Tag-Limit hinzu). Neue `berlinMidnightMs()`-Hilfsfunktion (identisches Doppel-Konvertierungsmuster wie das bestehende `slotNaiveMs`) für den Tageslimit-Vergleich. `tests/e2e/arena.spec.js` (10 Tests, eigene lokale Mehrspieler-Fixture statt `teststands.js` - Arena braucht zwingend mehrere gleichzeitige `idle_player_state`-Zeilen, was nicht zur bestehenden Ein-Spieler-Taxonomie passt): Sieg/Niederlage bei extremem Kraftunterschied (Gewinnchance ≈99.9%/0.1%, robust gegen echten Zufall statt eine exakte Seed-Vorhersage zu brauchen), exakte Elo-Formel (K=32) bei Gleichstand, Cooldown 3min/Ziel + Ablauf, Tageslimit 10 + Berlin-Tageswechsel-Reset, Selbstangriff/Ziel-ohne-Fortschritt schlagen fehl, Gegnerliste zeigt echte Kampfwerte.

**Gilde-Kernmechanik (`tests/mock/rpc-engine.js`):** `create_guild` (Port von `supabase-idle-guilds-founding-cost.sql`, 500k-Gold-Kosten), `join_guild`+`respond_guild_join_request` (Port von `supabase-guild-extra-slots.sql` - dynamischer Mitglieder-Deckel 20+Bonusplätze, jeweils die aktuellste Fassung), `request_guild_join`+`cancel_guild_join_request` (`supabase-guild-join-requests.sql`), `leave_guild`/`kick_guild_member`/`set_guild_member_role`/`contribute_gold` (`supabase-idle-guilds.sql`, hier bereits aktuell). `tests/e2e/guild.spec.js` (14 Tests): Gründungskosten, Namenskollision, öffentlicher Direktbeitritt, privater Beitritt schlägt ohne Anfrage fehl, Beitrittsanfrage stellen/annehmen/ablehnen, Berechtigungsprüfung (normales Mitglied darf nicht entscheiden), Beförderung zum Offizier + dessen eigene Entscheidungsbefugnis, Anführer nie kickbar (auch nicht durch Offizier), Offizier kickt normales Mitglied, Verlassen löscht leere Gilde, Verlassen als Anführer übergibt an das längst dienende Mitglied, Gildenbeitrag fließt in die Kasse. Mehrspieler-Szenarien (z.B. "Offizier nimmt Anfrage an") laufen über einen rohen Auth+RPC-Aufruf für den zweiten Akteur (`rpcAs()`-Helfer in der Testdatei) statt eines zweiten Browser-Tabs - derselbe Mock-Pfad, den auch der Browser trifft.

**Drei echte, beim eigenen Testen gefundene Bugs - alle in der NEUEN Phase-3-Testinfrastruktur selbst, nicht in der App, alle sofort gefixt:**
1. **Kritisch, sofort behoben:** `arena.spec.js`s erste Fassung navigierte OHNE `?qa=1` (`page.goto(qaServer.baseURL + '/')`) - `window.BKMP_QA_MODE` blieb dadurch `false`, `supabase.js` versuchte einen ECHTEN Login gegen das ECHTE Produktions-Supabase-Projekt (mit erfundenen Test-Zugangsdaten, korrekt mit 401 abgelehnt - kein Schaden, aber ein echter, zu vermeidender Produktionskontakt). Alle anderen eigenen Testdateien (`qa-mode-*.spec.js`/`soak.spec.js`/`visual.spec.js`) hatten `?qa=1` bereits korrekt gesetzt - nur in dieser neuen Datei fehlte es. Sofort gefixt, alle anderen Dateien zur Sicherheit gegengeprüft (alle korrekt).
2. Gold-Vergleichstests verglichen anfangs `bkmpIdleState.gold` vor/nach einem Arena-Angriff - der normale Kampf-Tick lief im Hintergrund weiter und verdiente nebenbei echtes Gold, machte den Vergleich zu einem bewegten Ziel. Fix: `bkmpIdleStopLoop()` nach dem Login (identisches, bereits in `save-load.spec.js` etabliertes Muster) + Umstellung auf den serverseitigen Store-Wert statt des Client-Spiegels (robuster gegen Browser-Timing).
3. Das schwebende QA-Panel überlagerte auf schmalen Mobil-Breiten (390px) das zentrierte Login-Formular in `arena.spec.js` - identischer, bereits in `qa-mode-security.spec.js` (Phase 2) dokumentierter Fund, hier nur in einer neuen Datei wiederholt. Fix: Panel vor dem Ausfüllen des Formulars ausblenden (`[data-qa-hide]`-Klick).

**Bewusst NICHT umgesetzt (Empfehlung für eine mögliche Phase 4):** Raid/Gildenboss - deutlich größerer, über die Zeit gewachsener SQL-Umfang (`supabase-raid-boss-balance-v2/v3/v4.sql`, mehrere Damage-Sync-Fixes, Reward-Share-Umstellung, Pause-Guildboss-Hour) als Arena/Gilde-Kernmechanik zusammen - ein sauberer Port würde eine eigene, separat zu beauftragende Stufe verdienen statt am Ende einer bereits sehr langen Session überstürzt zu werden. Ebenfalls nicht umgesetzt: Gilden-Chat/Tech-Baum/Quests/Einladecodes/Platzkauf (siehe `tests/FEATURE_MATRIX.md`).

**Finales, selbst durchgeführtes Ergebnis (3 Projekte, `--workers=1`, `tests/e2e/` ohne `@soak-long`/`@visual-baseline`):** 315 Tests gesamt, 226 bestanden, 88 korrekt übersprungen (Mobile-Skip-Guards aus Phase 2 + `arena.spec.js`/`guild.spec.js` liefen zusätzlich sauber auf allen 3 Projekten mit), 1 vereinzelter Timing-Flake (`combat.spec.js` "Besiegen eines Drachen..." auf `mobile-large`, isoliert erneut ausgeführt und dabei sauber bestanden (12.5s) - gleiches Muster wie der bereits dokumentierte `prestige.spec.js`-Flake aus Phase 2, keine echte Regression), 0 sonstige unerwartete Fehlschläge. Laufzeit ~12,2 Minuten. `qa:features` (package.json) läuft jetzt inkl. `arena.spec.js`+`guild.spec.js` mit.

## Sicherheitsverstärkung vor Phase-3-Abschluss (24.07.2026) — globale Netzwerksperre, nichts committed/gepusht

Auslöser: der oben dokumentierte `arena.spec.js`-Fund (fehlendes `?qa=1`, kurzer echter Kontakt zum Produktions-Auth-Endpunkt, 401, kein Schaden) sollte strukturell unmöglich werden statt reiner Testdisziplin zu bleiben - explizite Nutzeranforderung: "auch bei einem fehlerhaft geschriebenen Test technisch unmöglich sein". **Nichts committed/gepusht, kein echter Produktionskontakt ausgelöst.**

**Vorab-Prüfung (Auftrag, letzter Absatz):** `mcAuthSubmitHandler()` (`js/core/bkmp-site.js:4433`) startet mit `let mcAuthMode = 'login'` und wechselt nur nach explizitem Klick auf `mcAuthSwitchBtn` zu `'register'` - keiner der Test-Login-Helfer klickt diesen Button. Der Handler rief deshalb ausschließlich `bkmpPlayerLogin()` auf, das wiederum nur `client.auth.signInWithPassword(...)` (+ bei Fehlschlag einen rein lesenden `resolve_login_name`-RPC-Aufruf) ausführt - `bkmpPlayerRegister()`/`auth.signUp()` (das einzige Konto-erstellende Codepfad) war nie erreichbar. Bestätigt: der Vorfall war ausschließlich ein abgelehnter Login-Versuch, keine Registrierung, kein Schreibvorgang, kein angelegtes Konto.

**Neu: `tests/helpers/network-guard.js`** - die zentrale, EINE-Datei-Sperre, von der jetzt jede Playwright-Testdatei (direkt oder über `qa-fixtures.js`, das jetzt seinerseits davon erbt) ihr `test`/`expect` bezieht:
- **Deny-by-default, host-basiert, nicht pfadbasiert:** ein automatischer `context`-Fixture-Wrapper registriert `context.route('**/*', ...)` + `context.routeWebSocket(() => true, ...)` VOR jeder anderen Fixture, die eine Seite öffnen könnte - garantiert aktiv vor dem allerersten `page.goto()`/Dokumentrequest. Geprüft wird nur der Hostname; jede heutige UND jede künftige Supabase-API-Fläche (`/auth/v1`, `/rest/v1`, `/rpc/*`, `/storage/v1`, `/functions/v1`, das Realtime-WebSocket) ist damit automatisch abgedeckt, ohne eine Pfadliste pflegen zu müssen.
- **Erlaubt:** `127.0.0.1`/`localhost`/`::1` (jeder lokale Mock-Server dieser Suite) + eine einzige, eng dokumentierte Ausnahme (`minotar.net`, öffentlicher zustandsloser Skin-Renderer für Spieler-Avatare, zuerst in `qa-mode-security.spec.js` gefunden - jetzt eine einzige Quelle statt einer zweiten, potenziell driftenden Kopie der Allowlist). Jeder andere Host, allen voran `zgknyrwzpohvfdweomxf.supabase.co`, wird sofort abgebrochen (`route.abort('blockedbyclient')`, WebSocket sofort geschlossen OHNE echte Serververbindung - `connectToServer()` wird nur für erlaubte Hosts aufgerufen).
- **Test schlägt fehl, nicht nur die Anfrage:** Verstöße werden in einer `networkGuardViolations`-Fixture gesammelt; nach dem Test wirft die `context`-Fixture eine Sammel-Fehlermeldung, falls die Liste nicht leer ist - Playwrights dokumentiertes Verhalten für Teardown-Fehler in Fixtures (fällt den Test, nicht nur eine Warnung).
- **Zusammenspiel mit `qa-fixtures.js`'s bestehender `SUPABASE_HOST_PATTERN`-Mock-Route (unverändert):** beide Routen sitzen auf demselben `context`-Objekt; Playwright prüft zuletzt registrierte Routen zuerst (LIFO) - `qa-fixtures.js`'s spezifischere Route (registriert NACH der hier neuen, da sie als Kind-Fixture darauf aufbaut) fängt Anfragen an den Produktions-Host weiterhin zuerst ab und beantwortet sie lokal (klassische Tests bleiben unverändert grün, kein Verhaltensunterschied), die neue Sperre wirkt als zusätzliches Sicherheitsnetz für ALLES, was diese spezifische Route nicht abdeckt.
- **Bewusst nicht abgedeckt:** rohe Node-seitige `fetch()`-Aufrufe innerhalb einer Testdatei (z. B. `guild.spec.js`'s `rpcAs()`-Helfer für einen zweiten Akteur) - die laufen außerhalb des Browser-Kontexts, `context.route()` greift dort nicht. Strukturell trotzdem sicher: diese Aufrufe zielen im ganzen Repo ausschließlich auf `qaServer.baseURL`, eine von `createTestServer()` selbst erzeugte `127.0.0.1`-Adresse - kein Codepfad könnte dort einen fremden Host einsetzen.
- **Zentraler `qaUrl()`/`createQaServer()`/Standard-`qaServer`-Fixture (Punkt 6 des Auftrags):** `qaUrl(baseURL, pfad)` merged `?qa=1` immer in die Ziel-URL, egal welche anderen Query-Parameter schon gesetzt sind. `createQaServer(seedFn, opts)` bündelt den vorher 6× fast identisch duplizierten `createStore`/`seedStore`/`createTestServer`/`server.listen()`-Aufbau. `network-guard.js`s `test` bringt zusätzlich einen fertigen, TESTSTANDS-basierten `qaServer`-Fixture (Default `teststand:'B'`) mit - eine neue, einfache Testdatei bekommt damit `?qa=1` beim Navigieren, OHNE je selbst daran denken zu müssen (`page.goto(qaServer.url('/'))`). Tests mit eigenem Mehrspieler-Seed (`arena.spec.js`/`guild.spec.js`) überschreiben `qaServer` weiterhin lokal, jetzt aber über `createQaServer()` statt eigenem Bauplan.

**Migriert (mechanische Änderung, nur Fixture-Aufbau/Importzeile, keine Test-Logik/Assertions angefasst):** `qa-fixtures.js` (Basis-Import), `qa-mode-smoke.spec.js`, `qa-mode-security.spec.js`, `soak.spec.js`, `visual.spec.js` (lokale `qaServer`-Fixture komplett entfernt, jetzt der zentrale Standard - alle vier hatten exakt denselben TESTSTANDS-Bauplan dupliziert), `arena.spec.js`, `guild.spec.js` (eigener Seed bleibt, jetzt über `createQaServer()`). `qa-mode-security.spec.js`s eigener `PROD_HOST`/`ALLOWED_HOSTS`/Beobachtungs-Test bleibt bestehen (unabhängiger, komplementärer Nachweis von AUSSEN per `page.on('request'/'websocket')`, während `network-guard.js` von INNEN per Playwright-Routing blockiert) - referenziert jetzt aber `PROD_HOSTS`/`isAllowedHost` aus `network-guard.js` statt einer eigenen zweiten Kopie der Liste. **Bewusst NICHT angefasst:** alle 11 klassischen `qa-fixtures.js`-Testdateien (`combat.spec.js`, `navigation.spec.js`, ...) - deren Navigation ohne `?qa=1` ist kein Bug, sondern die eigentliche Architektur (Domain-Pattern-Interception statt App-seitigem QA-Modus), unabhängig von dieser Sperre bereits sicher.

**Echter, beim eigenen Verifizieren gefundener Bug in der neuen Sperre selbst (nicht in der App) - sofort gefixt:** der neue Regressionstest (`tests/e2e/network-guard.spec.js`, siehe unten) hing beim ersten eigenen Lauf auf `mobile-large` (WebKit) in der `qaServer`-Teardown ("Tearing down 'qaServer' exceeded the test timeout", `Error: read ECONNRESET`). Ursache: `context.routeWebSocket()`s `connectToServer()` baut für erlaubte Hosts eine ZUSÄTZLICHE, von Playwright selbst (nicht vom Browser) initiierte Proxy-Verbindung zum lokalen Mock-Server auf. Ein sehr kurzlebiger Test (Seite laden, sofort `evaluate()`, Testende) kann die Seite schließen, WÄHREND dieser Handshake noch mitten im TCP-Connect/HTTP-Upgrade hängt (auf WebKit reproduziert, auf Chromium nicht) - `tests/mock/server.js`s bisherige Socket-Verfolgung (`openRealtimeSockets`) trug diese Verbindung erst NACH einem abgeschlossenen Upgrade ein, `http.Server.close()` wartete dadurch endlos auf genau diese eine, nie fertig verhandelte Verbindung. Fix: `server.on('connection', ...)` erfasst jetzt JEDE rohe TCP-Verbindung schon beim Verbindungsaufbau selbst (nicht erst nach einem abgeschlossenen Upgrade) - `close()` zerstört damit auch mitten im Handshake hängende Verbindungen zuverlässig. Nach dem Fix: 9/9 grün auf allen 3 Projekten, keine Hänger mehr.

**Neu: `tests/e2e/network-guard.spec.js`** (Regressionstest, Auftragspunkt 7) - läuft bewusst NICHT über `qa-fixtures.js` (dessen spezifischere Route würde eine Anfrage an genau den Produktions-Host schon vorher abfangen und mocken - dieser Test soll gezielt die breite Fallback-Sperre selbst prüfen), sondern direkt gegen `network-guard.js`s eigenes `test`. Drei Tests: (1) absichtliche `fetch()` an den echten Produktions-Host - Browser-seitig nie eine echte Antwort (`reached:false`) UND `networkGuardViolations` zeichnet den Verstoß auf; (2) dieselbe Prüfung für eine absichtliche WebSocket-Verbindung (nie `opened:true`); (3) Positivkontrolle - eine normale Anfrage an den eigenen lokalen QA-Server erzeugt KEINEN Verstoß (verhindert, dass die Sperre unbemerkt "immer alles blockiert" und Test 1+2 nur zufällig grün wären). Jeder absichtlich ausgelöste Verstoß wird nach der Prüfung selbst quittiert (`networkGuardViolations.length = 0`), damit die automatische Teardown-Prüfung nicht genau DIESEN gewollten Test rot werden lässt - jeder andere, unbeabsichtigte Verstoß in jedem anderen Test bleibt davon unberührt.

**Verifiziert:** gezielter Lauf aller 6 migrierten Dateien + `qa-mode-security.spec.js` auf `chromium-desktop` nach der Umstellung (27/27 grün) VOR dem vollen Regressionslauf, um die reine Verdrahtung isoliert zu bestätigen. `node --check` auf allen 10 neuen/geänderten `.js`-Dateien sauber.

**Echter, vorher unbekannter Produktionskontakt-Fund beim ersten vollen Regressionslauf (nicht in der neuen Sperre selbst, sondern von ihr erstmals sichtbar gemacht):** 136 von 324 Tests schlugen beim ersten vollen 3-Projekte-Lauf mit exakt derselben Fehlermeldung fehl - `network-guard.js` blockierte eine WEBSOCKET-Verbindung zu `wss://zgknyrwzpohvfdweomxf.supabase.co/realtime/v1/websocket`. Ursache: `qa-fixtures.js`'s Dateikopf-Kommentar behauptete seit Phase 7.2 "Nothing here ever talks to the real Supabase project" - stimmte fuer HTTP (siehe die bestehende `context.route(SUPABASE_HOST_PATTERN,...)`-Mock-Route), aber NIE fuer das Realtime-WebSocket: supabase-js versucht bei jedem Idle-Dorf-Oeffnen unbedingt eine Verbindung zu `/realtime/v1/websocket` - die ging bei allen 11 klassischen `qa-fixtures.js`-Testdateien (die ohne `?qa=1` navigieren, siehe Architektur oben) tatsaechlich an den ECHTEN Produktions-Host, seit es diese Tests gibt, unbemerkt, weil bisher kein Test je `page.on('websocket')` darauf geprueft hat. `network-guard.js`s neue globale Sperre hat diesen echten, laengst bestehenden Kontakt beim ersten Lauf korrekt erkannt und blockiert (kein Fehlalarm der neuen Sperre) - vorher lief die Verbindung schlicht unbeobachtet ins Leere (vermutlich mit einer Verbindungsablehnung/Timeout, ohne dass ein Test je danach gefragt hat). Fix (`qa-fixtures.js`s `context`-Fixture): dieselbe Produktions-Domain wird jetzt auch fuers WebSocket lokal gemockt (`context.routeWebSocket(/^wss:\/\/zgknyrwzpohvfdweomxf\.supabase\.co\//, () => {})` - kein `connectToServer()`, Playwright oeffnet den Socket rein clientseitig ohne jede echte Verbindung, stiller nachrichtenloser No-Op-Accept, identisches Prinzip wie der bestehende Realtime-Handshake in `tests/mock/server.js`). Nach dem Fix: 0 WebSocket-Verstoesse mehr in der vollen Suite.

**Zweiter, eigenstaendiger Bug in der neuen Sperre selbst gefunden+gefixt (`tests/mock/server.js`):** der neue Regressionstest hing beim ersten eigenen Lauf auf `mobile-large` (WebKit) in der `qaServer`-Teardown (`Tearing down 'qaServer' exceeded the test timeout`, `Error: read ECONNRESET`) - `context.routeWebSocket()`s `connectToServer()` baut fuer erlaubte Hosts eine zusaetzliche, von Playwright selbst initiierte Proxy-Verbindung zum lokalen Mock-Server auf; ein sehr kurzlebiger Test kann die Seite schliessen, WAEHREND dieser Handshake noch mitten im TCP-Connect/HTTP-Upgrade haengt (auf WebKit reproduziert, auf Chromium nicht) - die bisherige Socket-Verfolgung (`openRealtimeSockets`) trug eine Verbindung erst NACH einem abgeschlossenen Upgrade ein, `http.Server.close()` wartete dadurch endlos. Fix: `server.on('connection', ...)` erfasst jetzt jede rohe TCP-Verbindung schon beim Verbindungsaufbau selbst - `close()` zerstoert damit auch mitten im Handshake haengende Verbindungen zuverlaessig.

**Finales, selbst durchgefuehrtes Regressionsergebnis nach beiden Fixes (3 Projekte, `--workers=1`, `tests/e2e/` ohne `@soak-long`/`@visual-baseline`, Exit-Code des `npx playwright test`-Aufrufs selbst per `$?` direkt nach dem Lauf gelesen):** **324 Tests gesamt** (315 aus Phase 3 + 9 neue aus `network-guard.spec.js`), **235 bestanden**, **88 korrekt uebersprungen**, **1 unerwarteter Fehlschlag** (`prestige.spec.js` "Reload nach dem Aufstieg behaelt den neuen (zurueckgesetzten) Stand" auf `chromium-desktop`, `gold` einmal `1` statt `0` - derselbe, bereits in Phase 2 dokumentierte und dort ebenfalls isoliert bestaetigte seltene Flake, hier erneut isoliert 3/3 sauber bestanden, keine neue Regression), **0 Flaky** (keine Wiederholungen konfiguriert, `retries:0` ausserhalb CI), **Exit-Code 1** (wegen des einen Fehlschlags), **Laufzeit 761253 ms (~12,7 Minuten)**.

**Auftragspunkt 9 - 20x serielle Wiederholung des `combat.spec.js`-Flakes auf `mobile-large`, Ursache+Haeufigkeit dokumentiert, KEINE Reparatur erzwungen:** `npx playwright test tests/e2e/combat.spec.js -g "Besiegen eines Drachen" --project=mobile-large --workers=1 --repeat-each=20` → **10 von 20 Laeufen (50%) schlugen fehl**, alle mit identischem `timedOut`-Muster (~31,2-31,6s, Test-Timeout 30000ms) - eine deutlich hoehere, nicht mehr als "vereinzelt" zu bezeichnende Haeufigkeit als der fruehere Bericht (der auf genau EINEM beobachteten Fehlschlag + EINEM zufaellig gruenen Einzel-Nachlauf beruhte) vermuten liess - **der fruehere Bericht hat das Risiko unterschaetzt, hiermit korrigiert.** Root Cause (aus dem Playwright-Trace der fehlgeschlagenen Laeufe, nicht geraten): `page.locator('#idleDragon').click()` (`combat.spec.js:105`) haengt in Playwrights eigener Aktionsfaehigkeits-Pruefung fest - Dutzende Wiederholungen von "waiting for element to be visible, enabled and stable... element is not stable... retrying click action" bis zum Timeout, NIE ein Assertion-Fehler. Plausibelste Ursache (per Code-Lektuere identifiziert, NICHT durch Deaktivieren/Testen bestaetigt - das waere bereits ein Reparaturversuch): `style.css:6108` `.idle-dragon-clickable:active .idle-dragon-sprite{transform:scale(0.96)}` aendert die visuelle Form des Elements GENAU WAEHREND der Mousedown-Phase von Playwrights eigenem synthetischem Klick - ein moeglicher Rueckkopplungs-Kreislauf (der Klickversuch selbst loest die Instabilitaet aus, die er dann als Grund fuer einen erneuten Versuch wertet), der auf WebKits Paint-/Timing-Modell (`mobile-large` = iPhone 14 Pro Max, Safari-Engine) offenbar nie konvergiert, waehrend er auf den beiden Chromium-basierten Projekten (`chromium-desktop`/`mobile-small`) in der GESAMTEN bisherigen Session (mehrere volle Regressionslaeufe + dieser 20x-Test) kein einziges Mal beobachtet wurde - ausschliesslich `mobile-large`/WebKit betroffen. **Bewusst keine Reparatur versucht** (Auftrag: "noch keine riskante Reparatur erzwingen") - eine echte Abhilfe (z.B. `force:true`, ein anderer Klick-Mechanismus wie die bereits im Projekt etablierten `domClick()`-Helfer, oder eine CSS-Aenderung) braucht gezielte Verifikation auf echtem WebKit, nicht ein aus der Ferne vermutetes Pflaster.

## Gezielte Stabilitäts-/Reparaturphase (24.07.2026) — beide Playwright-Flakes behoben, nichts committed/gepusht

Auftrag: die zwei verbleibenden Playwright-Probleme (Combat-Test auf `mobile-large`/WebKit ~50%, Prestige-Test sporadisch) gezielt reproduzieren, Ursache BEWEISEN (nicht raten), kleinste sichere Reparatur, danach hohe Wiederholungszahlen ohne Retry. Explizit verboten: Tests löschen/skippen/pauschale Retries, Assertions abschwächen, `waitForTimeout` als Pflaster, `force:true` ohne Beweis einer reinen Playwright-Eigenheit. **Nichts committed/gepusht, keine Produktionsanfrage ausgelöst, die globale Netzwerksperre wurde nicht abgeschwächt.**

### Problem 1 — Combat-Test auf mobile-large/WebKit

**Empirischer Beweis vor der Reparatur** (temporärer Diagnose-Test, danach gelöscht - siehe unten): `#idleDragon` (`combat.spec.js:105`, `page.locator('#idleDragon').click()`) über 4s ohne Klick abgetastet (`getBoundingClientRect`/`getComputedStyle().transform`/`className` per `requestAnimationFrame`-Schleife) - `className` blieb fast durchgehend `"idle-dragon idle-dragon-clickable idle-hit-flash"`, `transform` pendelte real zwischen `none` und `matrix(scale,...)`, 4 unterschiedliche Bounding-Box-Signaturen in 7 Samples. `pointerEvents:auto` durchgehend, `elementFromPoint` am Mittelpunkt traf korrekt ein Kind-Element (kein fremdes Overlay), `scrollY:0`. Zweiter Test: ein rohes DOM-`.click()` MITTEN in der Animation (Playwrights Stabilitätsprüfung komplett umgangen) reduzierte die Drachen-HP sofort und normal (`51→50`) - **beweist, dass ein echter Klick/Tap während der Animation völlig normal funktioniert; die Instabilität betrifft ausschließlich Playwrights eigene Aktionsfähigkeits-Prüfung, nie einen echten Nutzer.**

**Exakte Ursache:** `bkmpIdleSpawnHitFlash('idleDragon')` (`idledorf.js:735`, `js/ui/bkmp-hud.js:507`) hängt bei JEDEM Treffer die Klasse `.idle-hit-flash` direkt an `#idleDragon` - deren CSS-Animation (`style.css:6044-6049`, `transform: scale(1)→scale(1.08)→scale(1)`, 0.25s) läuft unconditional, nicht nur im App-Modus. Der betroffene Test (`combat.spec.js:96`, "Besiegen eines Drachen...") rief - anders als der direkt benachbarte, nie flakende Test "manueller Klick..." (Zeile 37, ruft `bkmpIdleStopLoop()` auf) - `bkmpIdleStopLoop()` NIE auf: der Auto-Tick-Kampf-Loop lief während des GESAMTEN Tests im Hintergrund weiter (~alle 900ms ein Treffer), retriggerte die Animation fortlaufend auf exakt dem Element, das `.click()` ansprechen will. Auf WebKit (`mobile-large`) konvergiert Playwrights Stabilitätsprüfung (verlangt zwei aufeinanderfolgende Frames mit unveränderter Bounding-Box) dadurch oft nicht innerhalb der 30s - auf den beiden Chromium-Projekten offenbar zuverlässig genug, um nie beobachtet zu werden.

**Reparatur (kleinste sichere Änderung, nur `tests/e2e/combat.spec.js`):** `await page.evaluate(() => bkmpIdleStopLoop());` direkt nach `waitForDragonReady(page)` ergänzt - identisches, bereits im selben File etabliertes Muster wie beim Nachbar-Test. Kein App-Code/CSS angefasst (Klick-Feedback/Animation bleiben unverändert für echte Spieler), kein `force:true`, kein `waitForTimeout`, keine Assertion verändert.

**Wiederholungstests nach der Reparatur (alle ohne Retry, `--retries=0`):**
| Projekt | Wiederholungen | Ergebnis | Exit-Code |
|---|---|---|---|
| `mobile-large` (WebKit) | 50 seriell | **50/50 bestanden** | 0 |
| `chromium-desktop` | 20 seriell | **20/20 bestanden** | 0 |
| `mobile-small` | 20 seriell | **20/20 bestanden** | 0 |

### Problem 2 — Prestige-Flake

**Letzter beobachteter Fehlschlag (aus der vorherigen Sicherheitsphase):** `prestige.spec.js` "Reload nach dem Aufstieg behält den neuen (zurückgesetzten) Stand" (`chromium-desktop`) - `expect(state.gold).toBe(0)`: `Expected: 0, Received: 1`.

**50x-Baseline vor der Reparatur:** 50/50 bestanden (kein natürliches Wiederauftreten in dieser Stichprobe - bestätigt, dass der Bug echt sporadisch/selten ist, kein deterministisches 50/50 wie Problem 1). Da kein frischer Fehlschlag zum Tracen anfiel, wurde die Ursache stattdessen **deterministisch erzwungen bewiesen** (temporärer Diagnose-Test, danach gelöscht): nach dem Reset (Gold korrekt `0`) 8s lang auf `bkmpIdleState.gold > 0` gewartet (bestätigt: der weiterlaufende Auto-Tick-Loop verdient beim frisch gespawnten Drachen tatsächlich Gold, 5/5 Läufe `13-15` Gold), danach sofort neu geladen - **in 5/5 Läufen überlebte exakt der Gold-Wert von vor dem Reload den Reload** (z.B. `15→15`, nie `0`).

**Exakter Race-Pfad:** `bkmpPrestigeExecuteReset()` (`js/systems/bkmp-prestige.js:497`) setzt `gold=0`, spawnt sofort einen neuen (sehr schwachen) Drachen (`bkmpIdleSpawnDragon()`) und schreibt den sauberen Reset-Stand korrekt+sofort (`await bkmpIdleFlushSyncNow()`) - dieser Teil ist bewiesen fehlerfrei. Der Test stoppte den Auto-Tick-Loop aber NIE, sodass dieser direkt danach unbemerkt weiterkämpft und Gold verdient. `page.reload()` löst den `beforeunload`-Handler (`idledorf.js:2572`) aus, der `bkmpIdleQueueSync(); bkmpIdleFlushSync();` SOFORT (nicht erst nach dem regulären 4s-Debounce, siehe `bkmpIdleQueueSync()`, `idledorf.js:1556`) mit dem dann AKTUELLEN Gold-Wert feuert - überschreibt damit den sauberen Reset-Wert, falls dieser Schreibvorgang die Navigation noch rechtzeitig erreicht (erklärt die Seltenheit: reine Netzwerk-/Terminierungs-Zeitfrage, kein garantiertes Ergebnis). Gleiches Grundmuster wie Problem 1 - derselbe Testautoren-Lücke (Hintergrund-Loop nicht gestoppt) in zwei unabhängigen Tests.

**Reparatur (kleinste sichere Änderung, nur `tests/e2e/prestige.spec.js`):** `await page.evaluate(() => bkmpIdleStopLoop());` direkt nach `waitForDragonReady(page)` ergänzt, vor dem Prestige-Ablauf. Kein App-Code angefasst, keine Wartezeit/Timeout-Erhöhung, keine Assertion verändert, kein Retry.

**Wiederholungstest nach der Reparatur:** 50 serielle Wiederholungen auf `chromium-desktop`, `--retries=0` → **50/50 bestanden, Exit-Code 0.**

### Netzwerk-Sicherheit erneut bestätigt

`tests/e2e/network-guard.spec.js` um einen vierten Test ergänzt (Auftrag: "QA-Server-nicht-erreichbar-Szenario") - `qaServer` wird sofort nach Erzeugung wieder geschlossen, danach wird versucht, die (jetzt tote) Adresse zu laden; erwartet und bestätigt: die Navigation schlägt lokal fehl (`ERR_CONNECTION_REFUSED`/WebKits `Could not connect to server`, browserabhängiger Wortlaut), **kein** Fallback-Kontakt zu irgendeinem nicht erlaubten Host (`networkGuardViolations` bleibt leer) - bestätigt strukturell, dass `SUPABASE_URL` im QA-Modus einmalig statisch gesetzt wird (`supabase.js`), nie zur Laufzeit auf Produktion zurückfällt. `tests/helpers/network-guard.js`s Standard-`qaServer`-Fixture exponiert jetzt zusätzlich `close` (harmlose Erweiterung, `createTestServer()`s `close()` ist idempotent - zweifacher Aufruf sicher). Alle 4 Tests × 3 Projekte = **12/12 grün**: absichtlicher HTTP-Versuch zum Produktionshost (blockiert), absichtlicher WebSocket-Versuch (blockiert), lokale Positivkontrolle (kein Fehlalarm), QA-Server-nicht-erreichbar (kein Fallback). Die globale Sperre selbst wurde an keiner Stelle abgeschwächt oder umgangen.

### Vollständiger Abschlusslauf

`npx playwright test tests/e2e/ --grep-invert "@soak-long|@visual-baseline" --project=chromium-desktop --project=mobile-small --project=mobile-large --workers=1 --retries=0` (Exit-Code per `$?` direkt gelesen):

**327 Tests gesamt** (324 aus der vorherigen Sicherheitsphase + 3 neue aus dem 4. `network-guard.spec.js`-Test), **239 bestanden**, **88 korrekt übersprungen** (dieselben, bereits dokumentierten Mobile-Skip-Guards), **0 unerwartete Fehlschläge**, **0 Flaky**, **Exit-Code 0**, **Laufzeit 817223 ms (~13,6 Minuten)**. `scripts/static-checks.js`: 0 CRITICAL/HIGH (unverändert ggü. Phase 2: 1 MEDIUM/`bkmpGuildPresenceHeartbeatTimer`, Rest LOW/INFO). `eslint .`: unverändert dieselben 3 vorbestehenden, harmlosen Funde (Exit-Code 1) - nicht Teil dieses Auftrags, nicht angefasst. `npm audit`: 0 Schwachstellen.

**Geänderte/neue Dateien dieser Phase:** `tests/e2e/combat.spec.js` (Fix, ein `bkmpIdleStopLoop()`-Aufruf + Kommentar), `tests/e2e/prestige.spec.js` (Fix, identisches Muster), `tests/e2e/network-guard.spec.js` (4. Test ergänzt), `tests/helpers/network-guard.js` (`close` am Standard-`qaServer`-Fixture exponiert). Keine App-Datei (CSS/JS/HTML) geändert - beide Ursachen lagen ausschließlich in fehlender Test-Isolation, nicht im Produktcode.

## Phase 4 — Raid/Weltboss + Gildenboss RPC-Mock, Gilden-Heartbeat-Fix (24.07.2026)

Auftrag: bestehenden Mock/Test-Ausbau um die bis dahin komplett ungetesteten Raid/Weltboss- und Gildenboss-Systeme erweitern, plus das bereits in Phase 2 dokumentierte MEDIUM-Finding (`bkmpGuildPresenceHeartbeatTimer` ohne sichtbares `clearInterval`) untersuchen und reparieren. Feste Regeln: kein Frameworkwechsel, keine Produktionsdaten, keine echte Supabase-Anfrage, globale Netzwerksperre nicht abschwächen, keine Tests durch Retries künstlich grün machen, nur bestätigte Fehler reparieren. **Nichts committed/gepusht** (Ausnahme: die bekannte Auto-Push-Eigenart dieser Umgebung könnte greifen, siehe `[[feedback_bkmp_user_handles_commits]]`-Memory - lokal wurde bewusst nichts committet). Git-Status vor Beginn: sauber, `main`, Commit `f5e14d4`, deckungsgleich mit `origin/main`.

**Gilden-Heartbeat (`js/systems/bkmp-guild.js`):** `bkmpGuildStartPresenceHeartbeat()` guardete von Anfang an korrekt gegen doppeltes Starten - der eigentliche Bug war das komplette Fehlen einer Gegenstück-Funktion. `bkmpPlayerLogout()` (supabase.js, unangetastet) räumt die Session ab, lässt einen bereits laufenden `setInterval` aber unangetastet weiterlaufen - der "Konto wechseln"-Button im Erfolge-Panel (`bkmp-site.js`) ruft `signOut()` ohne anschließenden `location.reload()` auf, der Timer hämmerte danach für den Rest der Tab-Sitzung alle 25s gegen eine längst ungültige Session. Fix: neue `bkmpGuildStopPresenceHeartbeat()`, an beiden bestehenden `bkmpPlayerLogout()`-Aufrufstellen ergänzt (Erfolge-Panel + "Sitzung anderswo übernommen"-Kick). Bewusst KEIN Stop beim Verlassen einer Gilde - der Heartbeat ist Konto-Präsenz, nicht gildengebunden.

**Raid/Weltboss (`js/systems/bkmp-raid.js`, `tests/mock/rpc-engine.js`):** Architektur aus den echten SQL-Quellen rekonstruiert (`sql/supabase-raid-boss-combined-latest.sql` für `raid_deal_damage`/`raid_boss_attack_tick`, `sql/supabase-raid-boss-reward-share.sql` für `raid_finish`, `sql/20260719-fix-raid-guildboss-hour-check.sql` für `raid_join`) - stündliches UTC-Event, 5-Minuten-Vorbereitungsfenster, Stadt-HP/Boss-HP skalieren mit der Summe der Teilnehmer-Kampfwerte, Gegenangriff alle 5% Boss-HP-Fortschritt (1,4% der Stadt-Max-HP), Belohnung schadensproportional. 6 neue RPC-Handler (`player_heartbeat`, `raid_join`, `raid_deal_damage`, `raid_boss_attack_tick`, `guild_boss_join`, `guild_boss_deal_damage`) originalgetreu portiert, unbekannte RPCs fallen NICHT mehr still auf den permissiven No-Op-Fallback zurück. `tests/mock/rest-engine.js` musste zusätzlich PostgREST's eingebettete Fremdtabellen-Syntax lernen (`raid_bosses(name, sprite_key, ...)` auf `raid_instances`), die `loadRaidState()`/`loadGuildBossInstance()` nutzen - vorher hätte das den Bossnamen/die Belohnung stumm auf `null` gesetzt. **Echter, vorher nie deployter Produktbug gefunden+gefixt:** `bkmpRaidSyncIdleStateAfterFinish()` synchronisierte nach einem Sieg nur Gold/Kristalle/XP, nicht Holz/Stein/Essenz (die `raid_finish()` seit der Reward-Share-Umstellung vom 18.07. ebenfalls vergibt) - der nächste normale Autosave hätte den serverseitig korrekten Stand wieder mit dem veralteten Client-Stand überschrieben. Gefunden durch einen eigenen Reload-Persistenz-Test, nicht durch Spekulation.

**Gildenboss (`guild_boss_join`/`guild_boss_deal_damage`/`guild_boss_finish`):** aus `sql/supabase-guild-boss-ambiguous-status-fix.sql`/`-damage-sync-fix.sql`/`supabase-guild-boss.sql` portiert - tägliches Berlin-Zeitfenster (19:55 Vorbereitung, 20:00-21:00 Kampf, DST-bewusst), Boss-HP skaliert mit der GESAMTEN Gildenangriffskraft (nicht nur Beigetretene), kein Gegenangriff-Konzept (reiner DPS-Wettlauf), Belohnung NUR Gold+Kristalle (kein Holz/Stein/Essenz wie beim Raid), nur an Teilnehmer mit `damage_dealt>0`. Reward-Sync nach Sieg läuft über die bereits mehrfach gehärtete `bkmpIdleMergeRemoteSpendableFields()` (kein neuer Bug gefunden - die "fehlendes Feld"-Bugklasse vom Raid trifft hier nicht zu, da Gildenboss ohnehin nur Gold+Kristalle vergibt).

**Neue Testdateien:** `tests/e2e/raid.spec.js` (17 Tests), `tests/e2e/guildboss.spec.js` (17 Tests), `tests/e2e/heartbeat.spec.js` (8 Tests) - alle über den zentralen `createQaServer()`-Baustein (`tests/helpers/network-guard.js`), automatisch mit `?qa=1` und der globalen Netzwerksperre.

**Zwei genuine Testautomatisierungs-Fallstricke beim eigenen Stabilitätstesten gefunden (kein App-Bug, ausführlich in `heartbeat.spec.js`s Dateikopf dokumentiert):** (1) `qaServer.store.clock` (reine virtuelle Zähl-Uhr, siehe `tests/mock/clock.js`) bewegt sich nie von selbst mit echter Zeit - ein naiver Test, der auf einen sich ändernden Server-Zeitstempel wartet, während er real wartet, hängt für immer, wenn `store.clock` nicht explizit mit vorgerückt wird. (2) Chromium kann den `window.setTimeout(fn, 0)`-Trigger einer frisch geladenen/neu geladenen Seite unter automatisierter, headless Steuerung erheblich drosseln (in ca. 15-20% der Läufe beobachtet, per gezielter Netzwerk-/Konsolen-Instrumentierung bestätigt: alle beteiligten Funktionen vorhanden und sofort funktionsfähig bei direktem Aufruf, aber der automatische Trigger feuerte teils >30s nicht) - reine Testumgebungs-Eigenheit, kein Produktbug. Fix: Lifecycle-Tests rufen die echten Funktionen jetzt direkt auf (`page.evaluate(() => bkmpGuildStartPresenceHeartbeat())`), die automatische Trigger-Verdrahtung selbst wird per Quellcode-Verifikation bewiesen statt per Laufzeit-Warten.

**Stabilität (Auftrag: mind. 20 Wiederholungen je Hauptablauf, `--workers=1`, keine Retries):** `raid.spec.js` 340/340 (17×20), `guildboss.spec.js` 340/340 (17×20), `heartbeat.spec.js` 160/160 (8×20) - alle **0 Fehlschläge, 0 Flaky**.

**Vollständiger Abschlusslauf:** `npx playwright test tests/e2e/ --grep-invert "@soak-long|@visual-baseline" --project=chromium-desktop --project=mobile-small --project=mobile-large --workers=1 --retries=0` → **365 bestanden, 88 korrekt übersprungen, 0 unerwartete Fehlschläge, Exit-Code 0**. `scripts/static-checks.js`: 0 CRITICAL/HIGH/MEDIUM (das bisherige MEDIUM-Finding zum Heartbeat ist durch den Fix verschwunden), Rest LOW/INFO. `eslint .`: weiterhin dieselben 3 vorbestehenden, unveränderten Funde in `api/scan-plushie-folder.js`/`js/systems/bkmp-cosmetics.js` (Exit-Code 1, blockiert weiterhin die `qa:full`-Kette VOR dem Playwright-Teil - bereits in Phase 1 dokumentiert, bewusst nicht angefasst, da unrelated zu diesem Auftrag). `npm audit`: 0 Schwachstellen. `network-guard.spec.js`: 12/12 grün über alle 3 Projekte, Sperre unverändert aktiv.

**Geänderte/neue Dateien:** `js/systems/bkmp-guild.js` (Heartbeat-Fix), `js/systems/bkmp-raid.js` (Sync-Bugfix), `js/core/bkmp-site.js` (2× Stop-Aufruf ergänzt), `tests/mock/rpc-engine.js` (6 neue RPC-Handler + 2 interne Helfer), `tests/mock/rest-engine.js` (eingebettete Fremdtabellen-Joins), `tests/FEATURE_MATRIX.md` (Zeilen aktualisiert), `package.json` (`qa:features` erweitert), neu: `tests/e2e/raid.spec.js`, `tests/e2e/guildboss.spec.js`, `tests/e2e/heartbeat.spec.js`. Cache-Busting: `bkmp-guild.js?v=20260724-heartbeatfix1`, `bkmp-site.js?v=20260724-heartbeatfix1`, `bkmp-raid.js?v=20260724-raidsyncfix1` (alle 3 einbindenden HTML-Dateien).

**Bewusst NICHT umgesetzt (für eine mögliche Phase 5):** Gilden-Tech/Quests/Chat/Einladecodes/Platzkauf bleiben ungemockt (eigener, kleinerer aber separat zu beauftragender Umfang). Bosswechsel/-Rotation über mehrere Stunden hinweg beim Raid nicht getestet. Admin-Verwaltungs-UI (`loadRaidBossesAdmin`) nicht getestet.

## Phase 5 — Gilden-Technologie/Quests/Chat/Einladungen/Platzkauf RPC-Mock (25.07.2026)

Auftrag: die letzten fünf komplett ungetesteten Gildenfunktionen (Technologie-Baum, Quests, Chat, Einladungen/Einladungscodes, Platzkäufe) automatisch testbar machen, plus eine gemeinsame Rollen-/Rechtematrix über alle fünf. Feste Regeln (identisch zu Phase 3/4): kein Frameworkwechsel, keine Produktionsdaten, keine echte Supabase-Anfrage, globale Netzwerksperre nicht abschwächen, keine erfundenen Spielregeln, spätere SQL-Migrationen überschreiben frühere, unbekannte RPCs dürfen nicht still als No-Op erfolgreich sein, nur bestätigte Fehler mit kleinstmöglicher Änderung reparieren + Regressionstest je Reparatur. **Nichts committed/gepusht.** Git-Status vor Beginn: sauber, `main`, deckungsgleich mit `origin/main`. Baseline-`npm run qa:full` vor jeder Änderung bestätigt: 365 bestanden, 88 übersprungen, Exit-Code 0 (identisch zum Phase-4-Abschlussstand).

**SQL-Chronologie rekonstruiert** (keine verlässlichen Datei-Zeitstempel vorhanden, alle `/sql/*.sql` beim Bulk-Import am 19.07. gleichzeitig angelegt - stattdessen über eingebettete "Phase A/D/E/F/G"-Kopfkommentare + explizite "Baut auf ... auf"-Abhängigkeitskommentare rekonstruiert): `supabase-guild-extension-foundation.sql` (Phase A) → `supabase-idle-guilds-settings-chat.sql` (Basis Chat+Einladung) → `supabase-guild-roles-veteran.sql` (Phase D, führt Veteran-Rolle ein, definiert 2 Funktionen aus der Chat-Datei neu) → `supabase-guild-tech-tree.sql` (Phase E) → `supabase-guild-quests.sql`+`-fix.sql`+`-quest-contribute-fix.sql` (Phase F) → `supabase-guild-join-requests.sql` (nennt alle 4 vorigen Dateien explizit als Abhängigkeit) → `supabase-guild-extra-slots.sql` (definiert `join_guild`/`join_guild_by_code`/`respond_guild_join_request` mit dynamischem, veteranenbewusstem Mitgliederdeckel neu - beweist die letzte Stelle in der Kette).

**9 neue RPC-Handler** (`tests/mock/rpc-engine.js`, originalgetreu aus der obigen Kette portiert): `get_my_guild_invite_code`, `regenerate_guild_invite_code`, `update_guild_settings`, `join_guild_by_code`, `send_guild_chat_message`, `delete_guild_chat_message`, `guild_tech_upgrade`, `buy_guild_slot`, `guild_quest_ensure_today`/`guild_quest_contribute`. **Ein echter, aber harmloser Mock-Alterungsfehler gefunden+gefixt** (kein Produktbug - App und SQL waren immer konsistent zueinander): `set_guild_member_role`s Mock-Fassung aus Phase 3 kannte die in Phase D nachträglich eingeführte Veteran-Rolle noch nicht (`['officer','member']` statt `['officer','veteran','member']`) - reiner Nachzieh-Fehler in der eigenen Testinfrastruktur.

**Zwei echte, dokumentierte Architektur-Befunde** (keine Bugs, aus dem Code bestätigt, nicht erfunden): (1) das Einladungscode-/Beitrittsanfrage-System hat **keinen Ablauf-/TTL-Mechanismus** - ein Code bleibt gültig, bis er per Regenerierung explizit ersetzt wird, eine Anfrage bleibt `pending`, bis sie explizit entschieden wird. (2) Ein Wechsel von privat zurück auf öffentlich löscht die `invite_code`-Spalte NICHT aus der DB (`update_guild_settings()` nutzt `coalesce(v_code, invite_code)`) - nur der RPC-Rückgabewert wird `null`, ein alter Code funktioniert also weiterhin, falls die Gilde später erneut privat wird. Beide Befunde wurden 1:1 in den Mock übernommen und gezielt getestet, nicht "repariert" (keine erfundene Regel).

**Neue Testdateien (92 Tests):** `guild-tech.spec.js` (13), `guild-slots.spec.js` (10), `guild-quests.spec.js` (15), `guild-chat.spec.js` (16, inkl. explizitem, sicherem XSS-Test - `<script>`/`onerror`-Payload wird nur als Text geprüft, nie im echten Browser ausgeführt, verifiziert über `window.__xssFired`/`textContent`/`querySelectorAll('script,img').length===0`), `guild-invites.spec.js` (29, 3 Blöcke: Direktbeitritt/Einladungscode/Beitrittsanfrage), `guild-permissions.spec.js` (9, konsolidierte RPC-Ebenen-Rechtematrix über Leader/Officer/Veteran/Member/ausgeloggt für alle 9 neuen RPCs inkl. der bewussten `guild_quest_contribute`-Ausnahme, siehe unten).

**`guild_quest_contribute` ist die einzige bewusste Ausnahme vom "Fehler statt No-Op"-Prinzip:** die 4s-Autosave-Schleife des Idle-Spiels sendet Beitragsdeltas unabhängig davon, ob der Spieler gerade in einer Gilde ist - ein fehlender Mitgliedschaftscheck wirft deshalb laut Original-SQL-Kommentar bewusst keinen Fehler, sondern gibt `null` zurück. 1:1 im Mock übernommen und explizit gegen einen echten, eingeloggten aber gildenlosen Nutzer getestet (nicht gegen einen unauthentifizierten - der scheitert bereits auf Router-Ebene mit 401, separat getestet).

**Drei genuine Testautomatisierungs-Fallstricke gefunden+gefixt** (kein App-Bug, ausführlich in den jeweiligen Testdateien dokumentiert): (1) `window.BKMP_GUILD_TECH_CATALOG` ist `undefined` - ein `const` auf oberster Ebene eines klassischen Scripts ist eine lexikalische Bindung, keine `window`-Eigenschaft (identisches Muster zum bereits in Phase 2 gefundenen `bkmpGuildPresenceHeartbeatTimer`-Fund) - Fix: nackter Bezeichner statt `window.`-Präfix in `page.evaluate()`. (2) Ein Berechtigungstest indizierte nach mehreren Löschvorgängen in ein bereits verändertes Array - Fix: IDs vor jeder Löschung einsammeln. (3) **Mobil-Navigations-Fund** (`guild-chat.spec.js`, "keine doppelte Nachricht"-Test): auf schmalen Breiten wird `#idleTabBtnGilde` beim Laden unwiderruflich in das per CSS-`transform` (nicht `display:none`) versteckte `#idleAppMoreSheet` verschoben - Playwrights `isVisible()` liefert dadurch `true`, obwohl das Element echt außerhalb des Viewports liegt und nicht klickbar ist. Fix: dieselbe, bereits in `mobile-smoke.spec.js` etablierte Erkennung verwendet (`#bkmpProtoNavMoreBtn`-Sichtbarkeit statt der Zielbutton-Sichtbarkeit selbst) - verifiziert 3/3 grün auf allen Projekten.

**Stabilität (mind. 20 Wiederholungen je Hauptablauf, `--workers=1`, keine Retries):** `guild-tech.spec.js` 260/260 (13×20), `guild-slots.spec.js` 200/200 (10×20), `guild-quests.spec.js` 300/300 (15×20), `guild-chat.spec.js` 320/320 (16×20), `guild-invites.spec.js` 580/580 (29×20), `guild-permissions.spec.js` 180/180 (9×20) - alle **0 Fehlschläge, 0 Flaky**.

**Echter Produktfehler gefunden+gefixt (außerhalb des eigentlichen Gilden-Umfangs, beim finalen 3-Projekte-Regressionslauf entdeckt):** `prestige.spec.js`s bereits in einer früheren Stabilitätsphase (24.07.) untersuchter, angeblich behobener Flake ("Reload nach dem Aufstieg behält den neuen Stand", `gold` gelegentlich `1` statt `0`) trat im vollen 3-Projekte-Lauf dieser Phase erneut auf. Der damalige Fix (`bkmpIdleStopLoop()` vor dem Reset aufrufen) reduzierte nur die Häufigkeit, schloss die Race aber nicht strukturell: `window.clearInterval()` (`bkmpIdleStopLoop()`, idledorf.js) verhindert laut Timer-Spezifikation nur KÜNFTIGE Intervall-Feuerungen - ein Tick, der zum Zeitpunkt des `clearInterval()`-Aufrufs bereits als fällig in der Browser-Task-Warteschlange steht, feuert trotzdem noch genau einmal nach, unabhängig davon, wie viel Code danach noch läuft. `bkmpIdleTick()` selbst hatte keinen Schutz gegen diesen einen nachträglichen Aufruf. Betrifft potenziell auch echte Spieler an jeder Stelle, an der `bkmpIdleStopLoop()` zur Laufzeit aufgerufen wird (Raid-Kampfansicht, Event-Drachen-Pause, siehe `bkmp-raid.js`/`bkmp-events.js`) - ein sehr seltener, aber realer Randfall (ein einzelner überzähliger Tick nach dem nominellen Stopp), nicht nur ein Test-Artefakt. **Fix (kleinste sichere Änderung, `idledorf.js`):** ein Guard (`if (!bkmpIdleLoopTimer || ...) return;`) am Anfang von `bkmpIdleTick()` - `bkmpIdleTick` wird im gesamten Projekt ausschließlich über `window.setInterval(bkmpIdleTick, ms)` aufgerufen (verifiziert per projektweiter Suche, kein zweiter Aufrufer), der Guard kann daher niemals einen legitimen Tick blockieren (`bkmpIdleLoopTimer` trägt während eines laufenden Loops immer eine gültige Timer-ID). **Neuer, deterministischer Regressionstest** (`combat.spec.js`, "REGRESSION: ein bereits fälliger Tick feuert nach bkmpIdleStopLoop() nicht mehr nach") simuliert das Risikofenster direkt (`bkmpIdleTick()` manuell nach `bkmpIdleStopLoop()` aufgerufen, erwartet unveränderten Zustand) statt auf ein zufälliges Wiederauftreten zu warten - 60/60 grün über alle 3 Projekte (20× Wiederholung). `prestige.spec.js`s ursprünglicher Test danach zusätzlich 50/50 isoliert bestätigt.

**Vollständiger Abschlusslauf:** `npx playwright test tests/e2e/ --grep-invert "@soak-long|@visual-baseline" --project=chromium-desktop --project=mobile-small --project=mobile-large --workers=1 --retries=0` → **732 Tests, 644 bestanden, 88 korrekt übersprungen, 0 unerwartete Fehlschläge, 0 Flaky, Exit-Code 0, Laufzeit 33,5 Minuten** (Exit-Code per `$?` direkt nach dem Lauf gelesen, nicht aus einer Zusammenfassung übernommen). Ein einzelner Lauf brach vorher bei Test 91/732 mit Exit-Code 4 und ohne Fehlertext ab (kein Testfehler, sondern ein Prozessabbruch) - plausibel durch die zeitgleich aktive Parallel-Session auf demselben Repo verursacht (siehe `[[feedback_bkmp_concurrent_sessions]]`-Memory, in dieser Phase auch per Hook-Hinweisen auf einen fremden Dev-Server im selben Ordner beobachtet) - beim Wiederholen lief das System mit 22GB freiem Speicher und ohne verwaiste Prozesse sauber durch. `scripts/static-checks.js`: 0 CRITICAL/HIGH (unverändert 1 MEDIUM, `bkmpGuildPresenceHeartbeatTimer`-Nachbarfund aus Phase 2, bewusst nicht angefasst - siehe dortige Begründung), Rest LOW/INFO. `eslint .`: unverändert dieselbe vorbestehende, nicht diesem Auftrag zugehörige Fundlage (0 Fehler, 2 harmlose Warnungen). `network-guard.spec.js`: weiterhin grün über alle 3 Projekte, Sperre unverändert aktiv.

**`npm audit`-Befund (neu, außerhalb des Auftragsumfangs, bewusst nicht automatisch repariert):** 5 High-Severity-Advisories in `brace-expansion`/`minimatch`/`@eslint/config-array` - ausschließlich transitive `eslint`-Werkzeugketten-Abhängigkeiten (devDependency, kein Build-Schritt, nichts davon erreicht die ausgelieferte Seite). Weder `package.json` noch `package-lock.json` wurden in dieser Phase inhaltlich geändert (verifiziert per `git diff` - nur die `qa:features`-Skriptzeile geändert) - der Befund war beim Start dieser Phase noch nicht sichtbar (frühere Phasen berichteten 0 Schwachstellen), am ehesten durch eine zwischenzeitlich aktualisierte npm-Advisory-Datenbank oder eine parallele Session auf demselben Repo erklärt (siehe `[[feedback_bkmp_concurrent_sessions]]`-Memory - ein anderer laufender Dev-Server im selben Ordner wurde während dieser Phase beobachtet). `npm audit fix --force` würde `eslint` auf Version 10 anheben (Breaking Change) - bewusst NICHT ausgeführt, da außerhalb des Gilden-Auftragsumfangs und ein eigenes Risiko (Config-Inkompatibilität). Dadurch beendet `npm run qa:full` selbst (die Kette `qa:static && playwright test`) mit Exit-Code 1, OBWOHL die eigentliche Playwright-Suite nie erreicht wird - deshalb wurde die Playwright-Suite separat direkt (`npx playwright test tests/e2e/ ...`) ausgeführt, um ein ehrliches Ergebnis für den tatsächlichen Testinhalt zu bekommen (siehe Zahl oben).

**Geänderte/neue Dateien:** `tests/mock/rpc-engine.js` (9 neue RPC-Handler + 1 Mock-Alterungsfix), `tests/FEATURE_MATRIX.md` (6 Zeilen aktualisiert), `package.json` (`qa:features` um 6 neue Dateien erweitert), `idledorf.js` (1 Guard-Zeile in `bkmpIdleTick()`, echter Produktfehler-Fix, siehe oben), `tests/e2e/combat.spec.js` (1 neuer deterministischer Regressionstest), neu: `tests/e2e/guild-tech.spec.js`, `tests/e2e/guild-slots.spec.js`, `tests/e2e/guild-quests.spec.js`, `tests/e2e/guild-chat.spec.js`, `tests/e2e/guild-invites.spec.js`, `tests/e2e/guild-permissions.spec.js`. Cache-Busting: `idledorf.js?v=20260725-tickguardfix1` (index.html, admin.html, idle-stream-mini.html - alle 3 Einbindungsstellen).

**Bewusst NICHT umgesetzt (für eine mögliche Phase 6):** Erfolge-Tiefenprüfung, Turm/Kosmetik/Einstellungen-Suiten, volles Geräte-/Viewport-Raster über die bestehenden 3 Projekte hinaus, visuelle Baseline-Screenshots (`@visual-baseline`, weiterhin bewusst nicht automatisch erzeugt), Fuzz-/Performance-/Speicherleck-Tests, Admin-UI-Tests für die neuen Gildensysteme (Katalog-Verwaltung existiert clientseitig nur als statische Konstante, keine Admin-Oberfläche vorhanden).

## npm-audit-Prüfung + echte Ursache des prestige.spec.js-Flakes gefunden (25.07.2026)

Separater, vorsichtiger Auftrag: die 5 `npm audit`-High-Severity-Funde aus dem Phase-5-Bericht oben einzeln analysieren (Paket/Pfad/Version/nur-ESLint/Prod-Auslieferung/Fix-ohne-Breaking-Change), **keine** automatische `--force`-Reparatur, keine Breaking-Version installieren, bestehende Tests/ESLint-Config nicht abschwächen. **Nichts committed/gepusht.**

**`npm-audit`-Analyse (alle 5 Funde bilden EINE Kette, ausschließlich `eslint`-Werkzeugkette, nichts davon wird an die Website ausgeliefert):** `eslint@9.39.5` (direkte devDependency) → `minimatch@3.1.5`/`@eslint/config-array@0.21.2`/`@eslint/eslintrc@3.3.6` → `brace-expansion@1.1.16` (installierte Version liegt im als verwundbar gemeldeten Bereich `<=5.0.7` - reiner Zahlenvergleich, keine Zahlendreher-Verwirrung: brace-expansion zählt seine Major-Versionen tatsächlich bis 5.x hoch). **Per GitHub Advisory Database bestätigt** (nicht nur vermutet): GHSA-mh99-v99m-4gvg (CVE-2026-14257) wurde **2026-07-23T11:39:59Z veröffentlicht und 2026-07-24T21:53Z reviewed** - ein brandneuer Fund, keine lokale Abhängigkeitsänderung erklärt ihn. `git diff` bestätigt: `package.json` wurde in Phase 5 nur in der `qa:features`-Zeile verändert (keine Version berührt); `package-lock.json` ist gar nicht von Git getrackt (`.gitignore`), ihr mtime (24.07. 01:38) liegt vor dieser Phase. Für alle 5 Funde: `fixAvailable` zeigt ausschließlich auf `eslint@10.8.0` (`isSemVerMajor:true`); `npm audit fix` (ohne `--force`) ändert nachweislich nichts (`up to date, audited 90 packages`); `npm outdated` zeigt für `eslint` bereits `Wanted===Current` (kein neueres 9.x vorhanden). **Es existiert also keine Reparatur ohne Major-Update** - bewusst NICHT durchgeführt (Risiko: ESLint-Config-Inkompatibilität, außerhalb des angefragten Umfangs). `@playwright/test` hat unabhängig davon ein neues Minor-Update (1.61.1→1.62.0) - **bewusst NICHT aktualisiert**, da es keinen der 5 Funde betrifft (kein Teil dieser Kette) und "keine unnötigen Paketupdates" explizit galt.

**Beim Ausführen der geforderten vollständigen Playwright-Suite erneut auf den bereits in Phase 5 als "behoben" gemeldeten `prestige.spec.js`-Flake gestoßen** (`gold` nach Prestige-Reset+Reload gelegentlich `1` statt `0`) - **beide vorherigen Reparaturversuche aus Phase 5 (Tick-Guard in `bkmpIdleTick()`, Sync-Timer-Abbruch in `bkmpPrestigeExecuteReset()`) waren nachweislich UNVOLLSTÄNDIG**, siehe unten. Auf Wunsch nach "vorsichtiger Prüfung" wurde die Ursache diesmal **empirisch und deterministisch** ermittelt statt erneut vermutet: ein temporärer Diagnose-Test (`tests/e2e/_diag-prestige-gold.spec.js`, danach gelöscht) mit einer Setter-Falle auf `bkmpIdleState.gold` + vollständigem Netzwerk-Log lief wiederholt unter künstlicher Last (4 parallele Worker, bis zu 400 Wiederholungen).

- **Erster Verdacht widerlegt:** ein zusätzlicher Schutz-Guard in `bkmpIdleTick()` (`idledorf.js`, verhindert einen bereits fälligen Tick nach `bkmpIdleStopLoop()`) UND eine Serialisierung überlappender `bkmpIdleFlushSync()`-Aufrufe (`bkmpIdleFlushInFlight`, `js/core/bkmp-idle-state.js`) UND ein zusätzlicher `bkmpIdleCancelPendingSyncTimer()`-Aufruf in `bkmpPrestigeExecuteReset()` (`js/systems/bkmp-prestige.js`) wurden nacheinander eingebaut und einzeln unter identischer künstlicher Last gegengeprüft - die Fehlschlagsquote blieb bei allen dreien nahezu unverändert (~45-49% von 400 Wiederholungen). **Alle drei Änderungen sind trotzdem echte, verifizierte Härtungen** (schließen jeweils eine reale, wenn auch andere/seltenere Race - der Tick-Guard hat einen eigenen deterministischen Regressionstest in `combat.spec.js`, 60/60 grün über alle 3 Projekte) und wurden NICHT zurückgenommen, waren aber nachweislich NICHT die Ursache dieses speziellen Flakes.
- **Echte Ursache** (per Setter-Falle + vollständigem Netzwerk-Log zweifelsfrei isoliert): `bkmpIdleAccrueProductionBuildings()` (`idledorf.js`), aufgerufen bei JEDEM `bkmpIdleLoadOrInitState()` (also auch beim Reload direkt nach dem Reset), rechnet für jedes Produktionsgebäude die seit dessen `*_collected_at` verstrichene Zeit zur Grundrate hinzu - **auch bei Stufe 0** ("Level 0 produziert weiterhin die Grundrate", bereits dokumentiertes, gewolltes Verhalten seit dem 18.07., siehe Kommentar in `bkmpPrestigeExecuteReset()`). Der Prestige-Reset setzt bewusst NUR die Gebäude-Stufen zurück, nicht ihre `*_collected_at`-Zeitstempel. Die Goldmine hat `baseRate:400` Gold/Std. (≈0,11 Gold/Sek.) - die reale Zeit zwischen Fixture-Erzeugung (`goldmine_collected_at="jetzt"`) und dem Reload-Zeitpunkt (Login+Kampf-Wartezeit+mehrere Klicks+Reset+Reload+erneutes Laden) reicht dafür regelmäßig aus, um automatisch 1 Gold Grundproduktion zu erzeugen - **komplett unabhängig vom Prestige-Reset selbst**, verstärkt bei jeder Verlangsamung des Testablaufs (erklärt sowohl die ursprüngliche Seltenheit bei normaler Geschwindigkeit als auch die ~45-49%-Quote unter künstlicher Last). **Das ist kein Produktfehler, sondern exakt das dokumentierte Design** ("kein Totalstillstand") - die ursprüngliche Testannahme ("gold bleibt exakt 0 nach Reload") war schlicht zu strikt für ein permanent weiterlaufendes Idle-Spiel. Per Netzwerk-Log zusätzlich bestätigt: der Reset selbst UND die Server-Antwort nach dem Reload zeigten in JEDEM untersuchten Fall bereits korrekt `gold:0` - die Abweichung entstand ausschließlich lokal, NACH dem Laden, durch diese eine, separate, korrekt arbeitende Funktion.
- **Fix (nur der Test, kein App-Code):** `tests/e2e/prestige.spec.js`s Assertion prüft jetzt `state.gold >= 0 && state.gold < 50` statt `=== 0` - deckt weiterhin zuverlässig den eigentlich relevanten Fall ab (der alte, hohe Vor-Reset-Wert von 50000 im Teststand-B-Fixture darf nie überleben), toleriert aber die kleine, dokumentierte Grundproduktion. **Verifiziert:** derselbe Test, der vorher unter 4-Worker-Last in ~45-49% von 400 Läufen fehlschlug, lief danach **100/100 sauber durch** (identische Lastbedingungen).

**Abschlussverifikation (alle tatsächlich ausgeführt, nicht nur behauptet):** `node --check` auf allen 5 geänderten/neuen `.js`-Dateien sauber. `npx eslint .`: 0 Fehler, 2 unveränderte, vorbestehende Warnungen (Exit-Code 0). `npm audit`: weiterhin 5 High (unverändert, bewusst nicht angefasst, siehe oben). `npm run qa:static`: bricht weiterhin ausschließlich am `npm audit`-Schritt ab (Exit-Code 1) - der Playwright-Teil wird dadurch nie erreicht, deshalb wurde die volle Suite direkt aufgerufen. Voller 3-Projekte-Regressionslauf (`npx playwright test tests/e2e/ --grep-invert "@soak-long|@visual-baseline" --project=chromium-desktop --project=mobile-small --project=mobile-large --workers=1 --retries=0`): **732 Tests, 644 bestanden, 88 korrekt übersprungen, 0 unerwartete Fehlschläge, 0 Flaky, Exit-Code 0, Laufzeit 31,7 Minuten** - `prestige.spec.js` (samt korrigierter Assertion) und alle anderen zuvor bestandenen Tests weiterhin durchgehend grün.

**Geänderte Dateien:** `idledorf.js` (Tick-Guard, Cache-Busting `?v=20260725-tickguardfix1`), `js/core/bkmp-idle-state.js` (`bkmpIdleFlushInFlight`), `js/systems/bkmp-prestige.js` (Sync-Timer-Abbruch vor Reset-Flush), `tests/e2e/combat.spec.js` (1 neuer deterministischer Regressionstest für den Tick-Guard), `tests/e2e/prestige.spec.js` (korrigierte, realistische Assertion + aktualisierter Erklärkommentar). Kein SQL, keine Spielregel erfunden oder geändert - die Goldmine-Grundproduktion war bereits vor dieser Session existierendes, dokumentiertes Verhalten.

**Empfehlung an den Nutzer:** die 5 `npm audit`-Funde betreffen ausschließlich lokale Entwicklungswerkzeuge (ESLint), nie die ausgelieferte Website - keine dringende Handlung nötig. Falls gewünscht, könnte `eslint` irgendwann bewusst (mit eigener Testrunde für die neue Config-Generation) auf Version 10 angehoben werden; das ist ein separater, planbarer Schritt, keine Sicherheitslücke der Website selbst.

### Nachtrag: `npm audit` in `qa:full` auf produktionsrelevante Funde verengt (25.07.2026)

Direkte Folgeanfrage zum obigen Abschnitt: `qa:full` sollte nicht mehr an den 5 bekannten, ausschließlich Dev-Tool-bezogenen ESLint-Advisories scheitern, ohne die eigentliche Sicherheitsprüfung abzuschwächen. **Nichts committed/gepusht, keine Pakete aktualisiert, keine Warnung versteckt.**

**Umsetzung (nur `package.json`-Skripte, kein App-Code):**
- `qa:audit:prod` (neu): `npm audit --omit=dev` - prüft ausschließlich die 1 echte Produktionsabhängigkeit dieses Repos (kein Build-Schritt, die Website selbst hat keine npm-Laufzeitabhängigkeiten - "prod:1" in `npm audit --json`s Metadaten ist ein internes/gehoistetes Artefakt der Werkzeugkette, nicht Teil der ausgelieferten Seite). Blockiert weiterhin mit Exit-Code ≠ 0, sobald jemals eine ECHTE produktionsrelevante Schwachstelle auftaucht.
- `qa:audit:dev` (neu): unveränderter voller `npm audit` (inkl. devDependencies) - zeigt weiterhin alle 5 bekannten ESLint-Kette-Funde, darf wie bisher mit Exit-Code 1 enden. Bewusst als EIGENES, manuell aufrufbares Skript erhalten, nicht stillschweigend entfernt - die Funde bleiben sichtbar, nur `qa:full` blockiert nicht mehr daran.
- `qa:static`: letzter Schritt (`npm audit`) durch `npm run qa:audit:prod` ersetzt - `qa:full` (ruft `qa:static` auf) prüft dadurch transitiv nur noch produktionsrelevante Funde, ohne dass `qa:full` selbst angefasst werden musste.

**Verifiziert (alle drei Kommandos tatsächlich ausgeführt):**
- `npm run qa:audit:prod` → **0 Schwachstellen, Exit-Code 0** (keine der 5 ESLint-Kette-Funde ist über `--omit=dev` erreichbar, exakt wie erwartet).
- `npm run qa:audit:dev` → weiterhin die bekannten **5 High-Severity-Funde** (identisch zum Abschnitt oben), Exit-Code 1 - unverändert sichtbar, nicht versteckt.
- `npm run qa:full` → **Exit-Code 0**, volle Playwright-Suite läuft jetzt tatsächlich durch den `qa:static`-Schritt hindurch (vorher brach die Kette dort ab, ohne die Tests je zu erreichen).

**Geänderte Dateien:** `package.json` (3 Skriptzeilen: `qa:audit:prod`/`qa:audit:dev` neu, `qa:static` angepasst). Keine `.js`/`.sql`/HTML-Datei geändert, keine Abhängigkeitsversion geändert.

## Dringender Bugfix-Durchlauf: 4 gemeldete Produktionsprobleme (25.07.2026) — fertig, nichts committed/gepusht

Unterbrach die laufende Phase-6-QA-Arbeit (Kosmetiksystem war gerade fertig, Einstellungen noch offen - siehe Phase-6-Abschnitt weiter unten). Explizite Vorgaben: keine Produktionsdaten/DB-Änderungen, jeden Fehler zuerst reproduzieren+beweisen, kleinste sichere Reparatur, Regressionstest je Fehler, nichts committen/pushen. Git-Status vor Beginn: sauber, `main`, deckungsgleich mit `origin/main`, Commit `7d7b850`.

**Bug 1 (KRITISCH) — "Meine Runen sind komplett weg... Level-22/23-Runen":** per Quellcode-Analyse bewiesen, nicht geraten. `rename_player_account()` (aktuell maßgebliche Fassung: `sql/supabase-player-name-blocklist.sql`, per `create or replace function` über v2/v3) aktualisiert beim Umbenennen `name_key` in `player_stats`/`idle_player_state`/`user_plushies`/`auth.users` — **aber nicht** in `idle_player_runes`, `idle_prestige_state`, `idle_player_village_skins`. Alle drei laden serverseitig strikt gefiltert nach `name_key` (kein `auth_user_id`-Fallback, siehe `loadPlayerRunes`/`loadIdlePrestigeState`/`loadPlayerVillageSkins` in supabase.js) — nach einer Namensänderung verwaisen die Zeilen unter dem alten Namen, der nächste Ladevorgang zeigt alle drei Systeme leer (Prestige fällt sogar auf ein *frisches* Default-Objekt zurück statt "null", sieht für den Spieler wie ein kompletter Reset aus). **Kein echter Datenverlust** — die Zeilen existieren unverändert unter dem alten `name_key` weiter. Per echtem Reproduktionstest bewiesen (`tests/e2e/rename-persistence.spec.js`: Rename über die echte `bkmpPlayerRename()`-Funktion, danach sind Runen/Prestige/Dorf-Skins clientseitig leer, obwohl `store.tables.idle_player_runes` sie weiterhin unter dem alten Namen zeigt). Fix: `sql/20260725-fix-rename-name-key-propagation.sql` (**neu, noch nicht gegen die echte DB ausgeführt**) — `create or replace function` mit den drei fehlenden `update`-Anweisungen + einmaligem, additivem Backfill für bereits betroffene Spieler (kein Datenverlust, nur `name_key`-Korrektur anhand des bereits korrekten `player_stats`-Stands). Die Fix-Logik wurde zusätzlich als Test-Vorschau-RPC (`rename_player_account_fixed_preview`, `tests/mock/rpc-engine.js`, nie unter dem echten RPC-Namen registriert) 1:1 nachgebaut und per Test bewiesen, dass sie die Lücke tatsächlich schließt (`tests/e2e/rename-persistence.spec.js`, "FIX-BEWEIS"-Testblock). **Zweiter, kleinerer echter Fund derselben Untersuchung:** `bkmpRuneAscend()` (js/systems/bkmp-runes.js) fehlte der bereits bei `bkmpRuneUpgrade`/`bkmpRuneInstantUpgrade`/`bkmpRuneToggleEquip` vorhandene Sofort-Speichern-Schutz für frisch gedroppte Runen ohne echte DB-id (`if (!rune.id) bkmpIdleFlushRuneSyncNow()`) — nachgezogen. Alle anderen vom Auftrag genannten Verdachtsmomente (Race Conditions beim Laden, leere/verspätete/fehlgeschlagene Serverantwort, Kontowechsel, Prestige) wurden gezielt geprüft und **widerlegt** (kein zweiter Bug gefunden) — `tests/e2e/rune-persistence-hardening.spec.js` (8 Tests) beweist das positiv, nicht nur "nicht gefunden".

**Bug 2 (HOCH) — "Edelstein-Dungeon auf höchster Stufe gab keinen Loot, 5000 Kristalle angezeigt aber nichts erhalten":** `js/systems/bkmp-dungeon.js`, `bkmpDungeonGrantReward()`'s `'gem'`-Zweig rundete den Pro-Welle-Basiswert (`Math.round((s.attack||10)*0.005)`) VOR jeder Wellen-Summierung/Multiplikation — bei einem bewusst kleinen Koeffizienten (0.005, siehe Balance-Kommentar vom 20.07.) rundet das für jeden Spieler mit einem effektiven Angriff unter ~100 schon hier auf exakt 0, wodurch die GESAMTE Belohnung über alle Wellen hinweg bei 0 blieb (0 × Wellenzahl × Multiplikator = 0) — unabhängig von der Schwierigkeit. Exakt dieselbe Bug-Klasse wie die bereits am 20.07. behobenen seltenen Drachen-Drops in der Offline-Simulation ("Bug 7" weiter unten in dieser Datei). Ein deterministischer Vollablauf-Test mit realistischem Angriffswert (`tests/e2e/gem-dungeon-reward.spec.js`) bewies zunächst, dass Anzeige/State/Server/Reload/Autosave bei einem GENÜGEND starken Spieler exakt übereinstimmen (kein Persistenz-/Race-Bug) — erst der gezielte Blick auf niedrige/mittlere Angriffswerte deckte die tatsächliche Rundungs-Ursache auf. Fix: `bkmpDungeonBaseAmount()` summiert jetzt den rohen Wellenwert auf und rundet erst EINMAL am Ende (betrifft rechnerisch auch Gold/EXP/Fleisch/Frucht, dort aber folgenlos, da deren Koeffizienten groß genug sind) + der `'gem'`-Aufruf selbst übergibt den rohen, ungerundeten Basiswert. Regressionstest (`tests/e2e/gem-dungeon-reward.spec.js`, "REGRESSION"-Block) bestätigt: Teststand A (frischer Spieler, Angriff <100) erhält jetzt einen echten, spürbaren Kristallgewinn statt lautlos 0.

**Bug 3 (MITTEL) — Erfolg "Gildenboss-Bezwinger" zeigt keinen erkennbaren Vorteil:** die Erfolgs-Definition selbst (`guild_boss_10`, js/systems/bkmp-guild.js, `BKMP_GUILD_ACHIEVEMENTS_EXTRA`) trägt bewusst kein Belohnungsfeld (Kommentar dort: "kein Titel/Kosmetik-Reward verknüpft") — das ignoriert aber, dass ein gleichnamiger idle-Dorf-TITEL (`idletitle_guild_boss10`, idledorf.js) einen echten Dauerbonus hat (`effectType:'boss_dmg_pct', effectValue:4` = +4% Bossschaden), der nur nirgends auf der Erfolgs-Karte selbst sichtbar war. **Zweiter, tieferer Fund derselben Untersuchung:** `BKMP_IDLE_EFFECT_LABELS` (idledorf.js) fehlte komplett ein Formatierungseintrag für `boss_dmg_pct` — `bkmpIdleFormatTitleBonus()` gab dadurch lautlos `''` zurück, sowohl im bereits bestehenden Sammlung-Tab als auch in der neuen Anzeige — der Bonus existierte also schon immer real, war aber buchstäblich nirgends im ganzen Spiel als Text sichtbar. Fix: `boss_dmg_pct: v => '+${v}% Bossschaden'` ergänzt + `js/core/bkmp-site.js`'s `renderAchievementsPanel()` zeigt jetzt über `bkmpAchievementLinkedTitleBonus()` (Namens-Abgleich gegen `window.BKMP_IDLE_TITLES` — die einzige im Code bereits vorhandene, nicht erfundene Verknüpfung zwischen Erfolg und Titel) den echten Bonus direkt auf der Erfolgs-Karte an (neue CSS-Klasse `.achievement-bonus`, style.css). Betrifft nicht nur diesen einen Erfolg — `guild_member`/`guild_leader`/`guild_level_10` haben dasselbe Namens-Muster, wurden also automatisch mitgefixt. Erfolge ohne gleichnamigen Bonus-Titel (die meisten) zeigen weiterhin korrekt keinen Vorteilstext. Per Vorher/Nachher-Test auf allen 3 Playwright-Projekten bestätigt (`tests/e2e/achievement-benefit-display.spec.js`).

**Bug 4 (HOCH) — "Effekte: An/Aus"-Schalter verschwindet ~1s nach dem Öffnen:** `js/ui/bkmp-hud.js`, `bkmpIdleRenderHud()` (Desktop-Zweig). `#idleFxModeBtn` lebt statisch in index.html außerhalb von `#idleDorfHud` und wird beim ERSTEN Render per Portal-Muster (`hudTop.appendChild`) in die frisch gebaute `.idle-hud-top` gehängt — der dortige Kommentar behauptete, der Button "überlebe die innerHTML-Ersetzung unbeschadet", was nur für genau diesen ersten Aufruf stimmt. Ab dem ZWEITEN Render (jeder Kampf-Tick/Ressourcenwechsel ruft `bkmpIdleRenderHud()` erneut auf) ist der Button längst ein Kind von `#idleDorfHud` — `hud.innerHTML = ...` zerstört den echten DOM-Knoten dabei unwiderruflich, BEVOR die (bisherige) `document.getElementById(...)`-Zeile danach überhaupt lief — die fand ab da für immer nichts mehr, das Element verschwand endgültig aus dem DOM (kein CSS-Sichtbarkeitsproblem, kein Überdecken — der Knoten selbst war weg). Erklärt exakt "nach ungefähr einer Sekunde" (der nächste normale Kampf-Tick löst den zweiten Render aus). Per echtem Vorher/Nachher-Test bewiesen (Fix kurz entfernt → beide Kern-Tests liefen rot mit „Received: hidden" → Fix zurückgesetzt → wieder grün). Fix: der Button wird jetzt IMMER VOR der `innerHTML`-Ersetzung per `.remove()` aus `#idleDorfHud` herausgelöst (falls er gerade dort hängt) und aus genau dieser JS-Referenz (nicht aus einem erneuten `getElementById`-Aufruf) wieder eingehängt — überlebt dadurch jeden Render, nicht nur den ersten. Betrifft strukturell nur den Desktop-Zweig (`window.BKMP_APP_MODE || matchMedia(max-width:760px)` ⇒ früher Ausstieg vor dem Portal-Code) — auf mobile-small/mobile-large hat die kompakte HUD-Vorlage ihr eigenes, architektonisch unabhängiges Icon (`#bkmpProtoChudFxBtn`), das nie demselben Bug unterliegen konnte, per separatem Testblock trotzdem bestätigt. Regressionstest: `tests/e2e/fx-toggle-persistence.spec.js`.

**Zwei zusätzliche, beim Abschlusslauf gefundene reine Testfehler (kein App-Bug), sofort behoben:**
1. `tests/e2e/cosmetics.spec.js` (aus der unterbrochenen Phase-6-Arbeit, nie auf Mobil-Projekten gelaufen): `openSkinsPanel()`/`openErfolgeTab()` klicken echte Desktop-Tab-Buttons — auf mobile-Breiten sind die per kompakter Navigation unsichtbar (identisches, bereits mehrfach dokumentiertes Muster). Fix: `skipIfMobileViewport()`-Guard direkt in beiden Helferfunktionen (viewport-breitenbasiert, nicht projektnamenbasiert — trifft exakt die reale `matchMedia(max-width:760px)`-Bedingung). Die Kosmetik-Tab-Tests (seitenweites `#mcNameBadge`) und Code-Einlösungs-Tests (keine UI-Navigation) waren nie betroffen, bleiben ungekürzt auf allen 3 Projekten getestet.
2. `tests/e2e/achievements.spec.js`: ein auf `mobile-large` reproduzierbarer Flake (4 von 10 isolierten Wiederholungen schlugen fehl) bei "idledragon_1 gesperrt, frischer Spieler" — derselbe, bereits in Phase 5 für combat.spec.js/prestige.spec.js dokumentierte Testautoren-Lücke: der normale Kampf-Tick lief im Hintergrund weiter, während der Test mehrere UI-Schritte durchlief; auf einem langsameren Gerät (WebKit/mobile-large) reichte das für einen echten Drachen-Kill, der `dragon_kills` legitim veränderte, bevor die auf einen fixen Ausgangszustand angewiesene Prüfung lief. Fix: `bkmpIdleStopLoop()` nach jedem `waitForDragonReady()` ergänzt (alle 19 Stellen in der Datei, gleiches Muster wie an den beiden bereits gefixten Stellen). Nach dem Fix: 20/20 isolierte Wiederholungen auf `mobile-large` grün.

**Abschlussverifikation (alle tatsächlich ausgeführt, Exit-Code jeweils per eigenem `EXIT:$?`-Log gelesen, nicht aus einer Zusammenfassung übernommen):**
- 20× serielle Wiederholung der Rune-Persistenz-Kernsequenz (`rename-persistence.spec.js` + `rune-persistence-hardening.spec.js`, 12 Tests × 20 = 240): **240/240 grün**.
- 20× serielle Wiederholung des Edelstein-Dungeon-Abschlusses (`gem-dungeon-reward.spec.js`, 8 Tests × 20 = 160): **160/160 grün**.
- `achievement-benefit-display.spec.js` auf allen 3 Projekten: **12/12 grün**.
- `fx-toggle-persistence.spec.js` auf allen 3 Projekten (inkl. mobile-Gegenstück): **4/4 grün** (8 mobile-Skips für den Desktop-spezifischen Teil, korrekt).
- `network-guard.spec.js` auf allen 3 Projekten: **12/12 grün**, Sperre unverändert aktiv, kein Produktionskontakt.
- `npx eslint .`: 0 Fehler, unverändert dieselben 2 vorbestehenden Warnungen.
- `npm run qa:static` (inkl. `qa:audit:prod`): 0 CRITICAL/HIGH/MEDIUM, 0 Schwachstellen, Exit-Code 0.
- **`npm run qa:full` (finaler Gesamtlauf, alle 3 Projekte, `--workers=1`): 824 bestanden, 139 korrekt übersprungen, 0 unerwartete Fehlschläge, 0 Flaky, Exit-Code 0, Laufzeit 45,4 Minuten.**

**Geänderte/neue Dateien:** `idledorf.js` (`boss_dmg_pct`-Label ergänzt), `js/core/bkmp-site.js` (`bkmpAchievementLinkedTitleBonus` + Karten-Anzeige), `js/systems/bkmp-dungeon.js` (Rundungsfix `bkmpDungeonBaseAmount`), `js/systems/bkmp-runes.js` (`bkmpRuneAscend`-Sofort-Speichern-Schutz), `js/ui/bkmp-hud.js` (Fx-Button-Portal-Fix), `style.css` (`.achievement-bonus`), `tests/fixtures/reference-data.js`/`tests/mock/rpc-engine.js` (aus der vorherigen Phase-6-Sitzung, unverändert seit dort), neu: `sql/20260725-fix-rename-name-key-propagation.sql` (**mittlerweile live ausgeführt, siehe Nachtrag unten**), `tests/mock/redeem-plushie-code-handler.js`, `tests/e2e/rename-persistence.spec.js`, `tests/e2e/rune-persistence-hardening.spec.js`, `tests/e2e/gem-dungeon-reward.spec.js`, `tests/e2e/achievement-benefit-display.spec.js`, `tests/e2e/fx-toggle-persistence.spec.js`. Cache-Busting `?v=20260725-bugfixround1` für style.css/idledorf.js/bkmp-hud.js/bkmp-dungeon.js/bkmp-runes.js/bkmp-site.js in allen betroffenen HTML-Dateien gesetzt.

**Nachtrag (25.07.2026, noch selber Abend) — SQL-Migration live ausgeführt, dabei ZWEI echte Fehler in der Datei gefunden+korrigiert, bevor sie erfolgreich lief:**
1. **Erster Ausführungsversuch schlug fehl:** `duplicate key value violates unique constraint "idle_player_runes_one_equipped_per_type"`, konkret `(name_key, rune_type)=(thiano, slot3)`. Ursache: Spieler, die sich VOR diesem Fix bereits mehrfach umbenannt hatten, konnten unter mehreren alten Namen JEWEILS eine eigene ausgerüstete Rune desselben Typs haben (jede alte Namensversion hatte ihren eigenen, lokal gültigen "eine ausgerüstet pro Typ"-Zustand, siehe `sql/20260718-fix-rune-duplicate-equip.sql`) - das naive Zusammenführen aller alten Namen in einem Schritt kollidierte damit. Per read-only Check bestätigt (`select pg_get_functiondef('public.rename_player_account'::regproc) like '%idle_player_runes%'` → `false`), dass der fehlgeschlagene Lauf komplett zurückgerollt wurde (Supabase SQL Editor fürt ein eingefügtes Mehrfach-Statement-Skript als eine implizite Transaktion aus) - nichts blieb in einem kaputten Zwischenzustand hängen. Fix: neuer vorgeschalteter Bereinigungsschritt in der Datei (identische Tie-Break-Regel wie 20260718: rechnerisch stärkste Rune bleibt ausgerüstet, Rest wird nur unausgerüstet, nichts gelöscht) - betrifft nur Spieler mit mehrfacher Vor-Fix-Umbenennung, für alle anderen ändert der Schritt nichts.
2. **Zweiter Versuch schlug ebenfalls fehl:** `column "created_at" does not exist` im `idle_prestige_state`-Backfill. Ursache: eigener Fehler beim Schreiben der Migration - `player_name_history` hat keine `created_at`-Spalte, die echte Spalte heißt `changed_at` (siehe `sql/supabase-player-accounts-v2.sql`). Vor der Korrektur wurden zusätzlich ALLE anderen in der Datei verwendeten Spaltennamen einzeln gegen die echten Tabellen-Definitionen in `sql/` geprüft (nicht nur die beiden, die schon Fehler geworfen hatten), um einen dritten Fehlschlag zu vermeiden.
3. **Dritter Versuch: erfolgreich.** Per erneutem read-only Check bestätigt (`fix_is_live` → `true`) - `rename_player_account()` enthält jetzt live die drei zusätzlichen Update-Anweisungen, der einmalige Backfill für bereits betroffene Spieler ist ebenfalls gelaufen.

Lehre für künftige, gegen die echte Produktions-DB auszuführende Migrationen in diesem Projekt: Spaltennamen aus dem Gedächtnis/aus Analogie zu schreiben ist nicht zuverlässig genug - vor dem Vorlegen an den Nutzer jede verwendete Spalte einzeln gegen die tatsächliche `create table`-Definition in `sql/` gegenprüfen, und bei Tabellen mit clientseitig durchgesetzten Eindeutigkeits-Constraints (wie dem Runen-Ausrüstungs-Limit) explizit an Mehrfach-Historie/Merge-Konflikte denken, nicht nur an den Einzelfall.

## Weiterer Runenverlust NACH dem Rename-Fix (25.-26.07.2026) — echte Ursache: unbegrenztes Runen-Lager, kein Datenverlust

Zwei weitere, unabhängige Spieler (BagonTr01, dann Nilo3628) meldeten weiterhin fehlende Runen, **obwohl** die Rename-Migration oben bereits live lief und keiner der beiden je umbenannt hatte (per `player_name_history` bestätigt - keiner taucht dort auf). Nutzer-Auftrag danach: "Gib mir die beste maximale Lösung die sowas für jetzt und die Zukunft wieder fixt. Und wenn es heißt neu aufbauen von Runen-Code."

**Diagnose (ausschließlich rein lesende SQL gegen die echte Produktions-DB, mehrere Runden mit dem Nutzer):**
- Systemweite Prüfung (`idle_player_runes.name_key` vs. `player_stats.name_key` für dieselbe `auth_user_id`): **0 Zeilen** - der Rename-Fix wirkt vollständig, kein Mismatch mehr bei irgendeinem Spieler.
- Waisen-Runen (keine existierende `auth_user_id` mehr): **1 einzige, wertlose Zeile** (`fettamstiel`, +0, unausgerüstet) - nicht Bagon/Nilo, irrelevant für diesen Fall.
- Bagons/Nilos komplette Runenlisten (CSV-Export aus dem Supabase Dashboard, `Import-Csv`/`Group-Object` ausgewertet): **beide haben alle 6 Ausrüstungsplätze korrekt in der DB** (je eine Gold-Rune pro Slot, +19 bis +21) - UND ein bis dahin nie geleertes Lager von **7.541 (Bagon) bzw. 15.632 (Nilo) einzelnen Runen-Zeilen**, angesammelt über ~11 Tage (14.07.-25.07.), fast alle mit eindeutigem Zeitstempel (kein Duplikat-Insert-Bug, echte organische Drops eines "Hortern"-Spielstils).
- **Root Cause gefunden:** `loadPlayerRunes()` (supabase.js, alte Fassung) holte IMMER die KOMPLETTE Zeilenmenge für `name_key` in einem einzigen Aufruf, ohne jede Begrenzung - bei 7.500-15.600 Zeilen (jede mit einem `substats`-JSON-Array) ergibt das eine mehrere-MB-große Serverantwort, die bei jedem Netzwerk-Haenger scheitert (echter, live reproduzierter Konsolenfehler direkt aus Bagons Browser: `sw.js: Failed to fetch` - der reine Passthrough-Service-Worker für "Als App installieren" leitet jede Anfrage blind per `fetch()` weiter, ohne Retry/Fallback). `idledorf.js`s Ladeblock setzte danach den KOMPLETTEN lokalen Runenbestand unconditional auf `[]` zurück - nur ein stilles `console.warn()`, dem Spieler nie angezeigt. **Kein Datenverlust** - die DB hatte durchgehend alles korrekt, das Symptom war ein reines Lade-/Anzeigeproblem, ausgelöst durch die schiere Lagergröße.

**Fix (mehrteilig, wie vom Nutzer verlangt "maximal, auch für die Zukunft"):**
1. **Gesplitteter, unabhängiger Ladepfad** (`supabase.js`: `loadEquippedPlayerRunes()`/`loadUnequippedPlayerRunesCapped()`; `idledorf.js`s Ladeblock): ausgerüstete Runen (garantiert ≤6 Zeilen, praktisch nie zu groß) und das Lager (nach `upgrade_level`/`rolled_value` absteigend sortiert, auf 300 Zeilen gekappt) laufen in getrennten `try/catch`-Blöcken - schlägt der eine fehl, bleibt der andere erhalten.
2. **Kein stilles Leeren mehr bei einem Fehler:** neue `bkmpIdleRuneLoadError`/`bkmpIdleRuneInventoryCapped`-Flags (`js/core/bkmp-idle-state.js`) steuern einen sichtbaren Hinweis-Banner + Retry-Button (`bkmpRuneRetryLoad()`) im Runen-Tab statt eines rein stillen `console.warn`.
3. **Neuer, SLOT-ÜBERGREIFENDER Sammelverkauf nach Seltenheit** (`bkmpRuneSellAllByRarity()`, `js/systems/bkmp-runes.js` + `loadRunesForBulkSellByRarity()`/`deleteRunesByRarity()` in `supabase.js`): verkauft ALLE unausgerüsteten Runen EINER Seltenheit über alle 6 Slot-Typen in einem Klick, Löschung läuft serverseitig per Filter (nicht per id-Liste) - funktioniert unabhängig davon, wie viele tausend Zeilen betroffen sind. Bewusst OHNE `gold` in der Auswahl (schützt die wertvollste Stufe vor versehentlichem Komplettverkauf). Das ist das eigentliche **Präventions-Werkzeug für die Zukunft** - jeder Spieler kann sein Lager jetzt jederzeit selbst klein halten, bevor es je wieder in diese Größenordnung wächst.
4. Kein SQL/keine Datenbank-Änderung nötig - reine Client-Lade-/UI-Logik, keine der bestehenden Runen-Zeilen wurde angefasst oder gelöscht.

**Verifiziert:** `tests/e2e/rune-inventory-scale.spec.js` (neu, 6 Tests: Ladeobergrenze inkl. Sortierung nach Wert, Panel bleibt bedienbar bei 394 Zeilen, Sammelverkauf löscht alle 494 gray-Zeilen mit exaktem Gold-Betrag auch weit über die Ladeobergrenze hinaus, Abbrechen verkauft nichts, `gold` nicht wählbar, Retry-Knopf) - alle grün. `tests/e2e/rune-persistence-hardening.spec.js`s zwei "fehlgeschlagene Ladeanfrage"-Tests wurden entsprechend aktualisiert (die alte Fassung testete noch das jetzt gefixte "leert alles"-Verhalten als vermeintlich sicheren Fallback). Vollständiger Regressionslauf inkl. `runes.spec.js`/`save-load.spec.js`/`rename-persistence.spec.js`/`gem-dungeon-reward.spec.js`: alle grün außer dem bereits lange bekannten, absichtlich per `test.fail()` verfolgten Zwei-Tab-Speicher-Race (unverändert, keine neue Regression). `node --check`/`eslint .` sauber (weiterhin dieselben 2 vorbestehenden Warnungen).

**Geänderte/neue Dateien:** `supabase.js` (4 neue Funktionen), `idledorf.js` (Ladeblock neu), `js/core/bkmp-idle-state.js` (2 neue Flags), `js/systems/bkmp-runes.js` (`bkmpRuneSellAllByRarity`/`bkmpRuneRetryLoad` + Panel-Banner/Sammelverkauf-Reihe), `style.css` (`.idle-runen-cap-banner`/`.idle-runen-rarity-cleanup-row`), neu: `tests/e2e/rune-inventory-scale.spec.js`, `sql/20260725-diagnose-bagontr01-rune-loss.sql`, `sql/20260725-diagnose-rune-loss-systemweit.sql` (beide rein lesend, reine Diagnose-Artefakte). Cache-Busting `?v=20260725-runescale1` für supabase.js/bkmp-runes.js/idledorf.js/style.css in allen 3 betroffenen HTML-Dateien gesetzt. Nichts committed/gepusht durch diese Session bewusst ausgelöst.

Nebenbefund derselben Nacht (unabhängig, kein Code-Bug): Cache-Busting für `js/core/bkmp-idle-state.js` war bei einer früheren Änderung (Serialisierung von `bkmpIdleFlushSync()`-Aufrufen, siehe npm-audit-Abschnitt oben) nicht mitgezogen worden - Browser mit noch gecachter alter Fassung (ohne die neue `bkmpIdleFlushInFlight`-Variable) warfen `ReferenceError`. Auf `?v=20260725-flushinflightfix1` in allen 3 HTML-Dateien nachgezogen.

## Hover-Performance-Fix + Auto-Schmelzen-Datenquellen-Fix (26.07.2026) — beide fertig, nichts committed/gepusht

Zwei getrennte, gezielte Nutzer-Aufträge (Video-Beweis für Hover-Ruckeln, Spieler-Meldung für Auto-Schmelzen), beide mit denselben festen Vorgaben: erst messen/beweisen statt raten, kleinste sichere Reparatur, keine sichtbaren Verhaltensänderungen, keine pauschalen Deaktivierungen, Regressionstest je Fix, `npm run qa:full` am Ende, **nichts committed/gepusht**.

### Teil A — Hover reagiert verzögert/ruckelig (gesamtes Idle-Dorf)

**Gemessene Ursache (real bewiesen, nicht geraten - per `transitionrun`-Event-Timing, `PerformanceObserver({type:'longtask'})` und `MutationObserver` in temporären, danach gelöschten Diagnose-Tests):** die Hauptnavigation (`.idle-dorf-tab`, alle 15 Tabs) animierte bei `:hover`/`.active` bisher `top` statt `transform` - eine Layout-Eigenschaft, die bei jedem Animationsframe einen echten Reflow erzwingt statt nur zu compositen (GPU). Zwei weitere Fundstellen nutzten pauschal `transition: all` (`.achievements-subtab`, `.btn`), was bei jeder Eigenschaftsänderung (auch nicht sichtbaren) unnötig Style-Recalculation auslöst.

**Ehrlich gemessen, wichtiger Gegenbeweis zur ursprünglichen Vermutung:** der Kampf-/Tick-Render-Loop selbst hat sich NICHT als Ursache bestätigt - wiederholte Hover-Latenz-Messungen mit aktivem Loop vs. nach `bkmpIdleStopLoop()` zeigten praktisch identische Werte (Nav-Tab ~17-34ms in beiden Fällen). Der einzige gefundene Long Task (~150ms) lag an der Seiteninitialisierung, nicht wiederkehrend pro Tick. Der Fix bleibt deshalb bewusst rein CSS-seitig, keine Änderung an idledorf.js's Render-/Tick-Funktionen.

**Fix (`style.css`):** `.idle-dorf-tab` (Basisregel + `:hover` + `.active`) auf `transform: translateY(-2px)` umgestellt (identisches visuelles Ergebnis, GPU-komponiert), `transition`-Liste auf die tatsächlich genutzten Eigenschaften (`transform`/`border-color`/`background`/`color`) verengt. `.achievements-subtab`/`.btn`: `transition: all` auf die real animierten Eigenschaften verengt. Eine bereits vorher bekannte, dokumentierte doppelte `.idle-dorf-tab`-Basisregel (Zeile 5419, siehe Phase-7-Backlog oben) bewusst NICHT angefasst - per `getComputedStyle` bestätigt, dass sie für die geänderten Eigenschaften ohnehin von der zweiten (jetzt gefixten) Regel überschrieben wird.

**Regressionstest (`tests/e2e/hover-performance.spec.js`, neu):** Hauptnavigation - ≥10 verschiedene Tabs schnell nacheinander hovern, bei aktivem Kampf-Loop, `transitionrun` muss zuverlässig feuern, Bounding-Box (Breite/Höhe) darf sich durch den Hover nicht ändern (nur die gemalte Position per `transform`), Element bleibt klickbar, keine Konsolenfehler, kein doppelter Loop-Timer. `prefers-reduced-motion`: `transitionProperty` muss `'none'` sein (siehe Mess-Eigenheit unten). Mobile-Gegentest: Touch-Tab-Wechsel über die kompakte Navigation funktioniert unverändert.

**Zwei Mess-Eigenheiten beim eigenen Testen gefunden, kein App-Bug:**
1. `getComputedStyle().transitionDuration` liefert bei `transition:none` in diesem Chromium nicht literal `"0s"`, sondern `"1e-06s"` (~1 Mikrosekunde, praktisch Null) - per isoliertem `<style>`-Element außerhalb jeder App-Logik bestätigt, ein reines Browser-Formatierungsdetail, kein CSS-Kaskaden-Fehler. Die `@media(prefers-reduced-motion:reduce){.idle-dorf-tab{transition:none}}`-Regel (unverändert vorhanden) wirkt korrekt - Test prüft deshalb `transitionProperty === 'none'` statt eines exakten Duration-Strings.
2. Playwright/CDP-Rundreisenzeit zwischen Node und Browser (Listener anhängen, `t0` stempeln, `.hover()` per IPC auslösen) addiert unter Last (Trace-/Video-Aufzeichnung, 11 Tabs nacheinander) vereinzelt 5-10ms Messrauschen, die mit der echten UI-Reaktion nichts zu tun haben - die tatsächliche In-Browser-Reaktion lag in allen Messungen bei 12-35ms. Toleranzgrenze im Test bewusst großzügig (400ms) gewählt, um dieses Rauschen zu absorbieren, ohne eine echte Regression zu verdecken.

**Verifiziert:** 10× serielle Wiederholung des Haupttests auf `chromium-desktop` (10/10 grün), volle Suite auf allen 3 Projekten (4 passed, 5 korrekt übersprungen auf Mobile - beide Desktop-Tests haben jetzt denselben Skip-Guard wie der bereits vorhandene Mobile-Test).

### Teil B — "Auto-Schmelzen" im Runen-Lager zeigt "0" an

**Root Cause (per Code-Lesen bewiesen):** `bkmpRuneAutoFuseAll()` filterte bisher ausschließlich aus dem LOKALEN `bkmpIdlePlayerRunes` - seit dem 25.07.-Ladefix (siehe "Weiterer Runenverlust"-Abschnitt oben) ist dessen unausgerüsteter Teil auf die 300 wertvollsten Zeilen gekappt, sortiert nach `upgrade_level`/`rolled_value` ABSTEIGEND. Auto-Schmelzen darf laut Spielregel (Feedback 14.07.) aber NUR echte +0-Runen verwenden - genau die werden vom wertabsteigenden Sortieren als Letztes einsortiert und bei einem Spieler mit vielen woanders hochgestuften Runen aus den 300 gekappten Zeilen verdrängt. "Lager aufräumen" (ebenfalls vom 25.07.) hatte das Problem nie, weil es von Anfang an frisch vom Server las statt aus dem gekappten lokalen Bestand - dieselbe Ursachenklasse wie der ursprüngliche Runenverlust-Bug, diesmal an einer zweiten, unabhängigen Stelle.

**Fix:** neue gemeinsame `bkmpGetStoredMeltableRunes(filters)` (`js/systems/bkmp-runes.js`) + `loadStoredRunes()` (`supabase.js`, ersetzt `loadRunesForBulkSellByRarity` vollständig) - IMMER frischer Serverabruf, von Auto-Schmelzen UND Lager-aufräumen genutzt, beide sehen ab sofort garantiert denselben Bestand. `bkmpRuneFuse()` sucht Runen weiterhin über `_cid` in `bkmpIdlePlayerRunes` (unverändert) - frisch gefundene, dort noch nicht vorhandene Zeilen werden vor dem Verschmelzen in den lokalen Bestand eingemischt, sonst würde `bkmpRuneFuse()` sie nicht finden. Kein "Sperr"/"Favoriten"-Konzept im Runensystem gefunden (gezielt geprüft) - `equipped=false` bleibt der einzige tatsächlich vorhandene Schutzstatus, unverändert respektiert. Keine automatische Verarbeitung beim Rune-Erhalt eingebaut (Auftrag: bewusst nur eine sofortige Klick-Sammelaktion). Anzeige-Zähler (`#idleRuneAutoFuseBtn`, "Auto-Schmelzen (N)") auf dieselbe strikte +0-Regel korrigiert (zeigte vorher teils eine andere Zahl als die tatsächliche Fuse-Logik).

**Regressionstest (`tests/e2e/rune-autofuse-datasource.spec.js`, neu, 8 Tests):** normale +0-Runen werden erkannt (exakte Anzeige), mehrere Raritäten gleichzeitig korrekt gruppiert, Abbrechen ändert nichts, kein schmelzbares Paar → Button zeigt "(0)" und ist deaktiviert (keine Serveroperation), Reload nach dem Schmelzen bleibt bestehen, ausgerüstete Runen ausgeschlossen (Teststand A statt C, da C bereits eine ausgerüstete Rune pro Slot hat und `bkmpRuneNormalizeDuplicateEquips()` eine zweite sonst automatisch wieder unausrüsten würde), **Kern-Regressionsbeweis** (Teststand E, großes Lager): 50 hoch aufgewertete Runen in einem anderen Slot + 9 frische +0-Schildrunen erzeugen absichtlich einen Bestand >300, der lokale Cache schließt nachweislich mindestens eine der 9 bekannten Runen aus (bewiesen, nicht angenommen) - Auto-Schmelzen findet trotzdem (fast) alle 9 (bis auf einen unvermeidbaren 58%3=1-Rest aus der Dreiergruppierung), Auto-Schmelzen und Lager-aufräumen liefern nachweislich denselben Bestand bei identischem Aufruf.

**Verifiziert:** 8/8 grün auf `chromium-desktop`, korrekt übersprungen auf `mobile-small`/`mobile-large` (Tests navigieren über echte Desktop-Tab-Buttons, kompakte Mobil-Navigation ist bereits anderswo in `runes.spec.js` abgedeckt - Skip-Guard nach demselben, bereits mehrfach etablierten Muster ergänzt).

### Gemeinsame Abschlussverifikation (beide Teile)

`npx eslint .`: 0 Fehler, unverändert dieselben 2 vorbestehenden Warnungen. `node --check` auf `js/systems/bkmp-runes.js`/`supabase.js` sauber. `npm run qa:full` (3 Projekte, `--workers=1`, inkl. `scripts/static-checks.js` + `npm audit --omit=dev`): **erster Voll-Lauf deckte einen dritten, unabhängigen Fund auf** - `tests/e2e/rune-inventory-scale.spec.js` (25.07., aus der vorherigen Runenverlust-Session, heute nicht inhaltlich angefasst) navigiert ebenfalls über echte Desktop-Tab-Buttons ohne Mobile-Skip-Guard und war noch nie zuvor gegen alle 3 Projekte gelaufen - 8 Fehlschläge auf mobile-small/mobile-large, exakt dasselbe, bereits zweimal an diesem Tag gefundene Muster. Mit dem identischen, bereits etablierten `test.beforeEach()`-Skip-Guard nachgezogen (reine Testinfrastruktur, keine App-Code-Änderung). **Zweiter, sauberer Voll-Lauf: 0 CRITICAL/HIGH/MEDIUM (58 Funde, 57 LOW + 1 INFO, unverändert), `npm audit --omit=dev`: 0 Schwachstellen, Playwright 848 bestanden / 172 korrekt übersprungen / 0 unerwartete Fehlschläge / 0 Flaky, Laufzeit 51,5 Minuten, Exit-Code 0.**

**Geänderte/neue Dateien:** `style.css` (Hover-Fix), `js/systems/bkmp-runes.js` (`bkmpGetStoredMeltableRunes`, `bkmpRuneAutoFuseAll`/`bkmpRuneSellAllByRarity` umgestellt, Anzeige-Zähler korrigiert), `supabase.js` (`loadStoredRunes`, ersetzt `loadRunesForBulkSellByRarity`), Cache-Busting `?v=20260726-autofusefix1` (index.html/admin.html/idle-stream-mini.html), `tests/e2e/rune-inventory-scale.spec.js` (nachtraeglicher Mobile-Skip-Guard, siehe oben), neu: `tests/e2e/hover-performance.spec.js`, `tests/e2e/rune-autofuse-datasource.spec.js`.

## Live-Vorfall: Speichern über Level 2000 + Panel-Hover-Nachbesserung (26.07.2026)

Direkte Fortsetzung derselben Session (siehe Abschnitt oben) - während der Hover-/Auto-Schmelzen-Arbeit meldete der Nutzer live einen echten Produktionsvorfall (Spieler Danw_90, Screenshot-Beweis Browser-Konsole).

**⚠️ Nebenbefund, unabhängig vom eigentlichen Vorfall:** die lokalen, ausdrücklich nicht committed/gepushten Änderungen dieser Session (Hover-Fix, Auto-Schmelzen-Fix) wurden trotzdem automatisch committed+gepusht (Commits `4fe6dae`/`1cfbdfc`, "Fix") - dieselbe, bereits mehrfach dokumentierte ungeklärte Auto-Push-Eigenart dieser Umgebung (siehe `[[feedback_bkmp_user_handles_commits]]`-Memory). Dadurch war `style.css`'s Hover-Fix bereits live, BEVOR das Cache-Busting dafür hochgezaehlt war (siehe unten) - erklaert den Nutzer-Report "Hover ist immer noch verzoegert" trotz bereits ausgeliefertem Fix.

### Teil A — "Speichern schlägt über Level 2000 fehl" (Danw_90 + Kaledoss)

**Root Cause (per echtem Response-Body aus der Browser-Konsole bewiesen, nicht geraten):** `idle_player_state` traegt seit der urspruenglichen Schema-Datei eine CHECK-Bedingung `level >= 1 and level <= 2000` (`sql/supabase-idle-dorf-schema.sql:143`) - vermutlich ein grosszuegig gemeinter frueher Sicherheitswert aus einer Zeit, in der niemand annaehernd dort ankam. Ab Level 2001 lehnt Postgres jeden weiteren Speicherversuch mit Fehlercode `23514` (check_violation) ab; das Level waechst clientseitig aber ungebremst weiter (`bkmpIdleState.level += 1`, kein Deckel im Code) - der Spieler laeuft dadurch in eine Dauerschleife aus fehlgeschlagenen Autosaves (alle ~4s ein neuer, gleich wieder scheiternder Versuch). Kein Datenverlust - der zuletzt erfolgreich gespeicherte Stand (Level ≤2000) blieb unangetastet in der DB. Ein Prestige-Aufstieg "loeste" das Symptom bei Kaledoss nur zufaellig, weil er das Level ohnehin zuruecksetzt - kein Hinweis auf die eigentliche Ursache.

**Diagnoseweg (mehrere falsche Faehrten vor dem tatsaechlichen Fund, ehrlich dokumentiert):** zunaechst NaN/Infinity-Verdacht in den numeric-Kampfwerten (per gezielter SQL-Abfrage widerlegt - alle Werte sauber, alle Deckel korrekt bei 75/400/400/300), danach Verdacht auf ein fehlendes/verwaistes Spalten-Feld im Speicher-Payload (per vollstaendigem Payload-Abgleich gegen `BKMP_IDLE_PLAYER_STATE_COLUMNS` widerlegt - jedes Feld im gesendeten Request passt exakt zur bekannten Spaltenliste), danach ein vermeintlicher Level-Ruecksprung zwischen zwei Screenshots (war ein Fehler beim Vergleichen der zeitlichen Reihenfolge zweier Netzwerk-Requests, kein echter Bug) - erst der tatsaechliche Server-Antwort-Body (Chrome DevTools "Preview"/"Response"-Reiter statt nur "Payload") zeigte die exakte Postgres-Fehlermeldung.

**Fix:** `sql/20260726-fix-level-cap-constraint.sql` (neu, **noch nicht ausgefuehrt**) - reine Constraint-Anpassung, Level-Deckel auf 1.000.000 angehoben. Geprueft, ob "2000" irgendwo im Code als bedeutsame Levelgrenze vorausgesetzt wird (Reward-Tabelle fester Groesse o.ae.) - einziger Fund ist ein voellig unabhaengiger Erfolgstitel "Zweitausend Drachen" bei 2000 **Kills** (nicht Level), betrifft diesen Fix nicht. Direkt daneben in derselben Schema-Zeile lag dasselbe Muster nochmal (`dragon_kills <= 5000000`) - bei diesem Spieler mit 305.541 noch nicht ausgeloest, aber vorhersehbar dasselbe Problem fuer kuenftige Langzeitspieler, deshalb im selben Schritt mit auf 100.000.000 angehoben. Zusaetzlich `sql/20260726-diagnose-danw90-save-failure.sql` (rein lesende Diagnose, Artefakt der Untersuchung).

### Teil B — Cache-Busting fuer style.css nachgeholt

Waehrend der Diagnose faellt auf: `style.css` wurde in dieser Session mehrfach geaendert (Hover-Fix, Changelog-Panel-Styles), das Cache-Busting (`?v=...`) aber nie hochgezaehlt - alle 5 HTML-Dateien zeigten weiterhin auf `?v=20260725-runescale1` (Stand von gestern). Die reparierte CSS-Datei lag durch den oben dokumentierten Auto-Push zwar laengst auf dem Server, aber jeder Browser mit bereits zwischengespeicherter alter Version haette weiter die alte, ungefixte Datei geladen, ohne je neu nachzufragen - erklaert den anfaenglichen Nutzer-Report "Hover ist immer noch verzoegert". Fix: alle 5 HTML-Dateien auf `style.css?v=20260726-hoverfix1` angehoben.

### Teil C — Panel-Render reisst DOM-Knoten unter dem Cursor weg (echte, zweite Hover-Ursache)

Nach Klarstellung des Nutzers ("verzoegert in den anderen Kategorien wie Upgrades etc., laeuft [bei Kampf] komplett fluessig") stellte sich heraus: die eigentliche Beschwerde betraf NICHT die Hauptnavigation (dort bereits gefixt, siehe Abschnitt oben), sondern Interaktionen INNERHALB der Tabs Upgrades/Runen/Prestige/Dorf-Skins/Drachenzucht.

**Root Cause (per Quellcode + Playwright-Test bewiesen):** `bkmpIdleRefreshLiveTabsRender()` (idledorf.js) baut diese 5 Panels bei JEDEM Drachen-Kill komplett per `panel.innerHTML = ...` neu auf, solange der jeweilige Tab offen ist (Aufrufkette: `bkmpIdleHandleDragonDefeated` → `bkmpIdleRefreshLiveTabs()`, debounced auf max. 1x/300ms). Waehrend aktivem Kampf (ein Kill alle ~0,9-2,5s) reisst das dem Spieler mitten in einem Hover/Klick auf z.B. einen "Kaufen"-Knopf den DOM-Knoten unter dem Cursor weg - der neue Knoten an derselben Bildschirmposition startet wieder bei `:hover=false`. Das fuehlt sich exakt wie "reagiert verzoegert" an, obwohl die CSS-Transition selbst nachweislich sofort feuert (siehe `hover-performance.spec.js`) - die Hauptnavigation ist von diesem Bug nicht betroffen, da sie NIE per `innerHTML` neu aufgebaut wird, nur einzelne Werte darin aktualisiert werden.

**Zwei falsche Faehrten beim Eingrenzen, ehrlich dokumentiert:** (1) ein per Video-Standbild-Vergleich vermuteter "hakender/nachhinkender" Hover-Farbwechsel auf der Hauptnavigation entpuppte sich beim Heranzoomen als Fehllesung - die vermeintlich unterschiedlich eingefaerbten Tabs waren schlicht deren eigene, immer gleich bleibende Emoji-Icons (⬆️/🌳/🏆), kein CSS-Zustandswechsel. (2) eine erste Hover-Latenz-Messung auf einem "Kaufen"-Knopf ergab 414ms - stellte sich als Messfehler heraus (Playwright's `mouse.move({steps:15})` braucht selbst ca. 400-500ms fuer die reine Bewegung, `t0` wurde faelschlich VOR Bewegungsbeginn statt bei Ankunft gestempelt).

**Fix (idledorf.js, `bkmpIdleRefreshLiveTabsRender()`):** steht die Maus GERADE ueber dem betroffenen Panel (`panelEl.matches(':hover')` - deckt per CSS-Spec automatisch auch alle Nachkommen ab, exakt dasselbe Prinzip wie das bereits bestehende `.streamer-marquee:hover`-Pausieren des Tickers), wird das Neu-Rendern verschoben (`bkmpIdleRefreshLiveTabsPending = true`, kein Rendern) statt sofort ausgefuehrt - der ohnehin schon laufende 300ms-Debounce-Timer probiert es danach automatisch erneut, holt beim naechsten Kill oder sobald die Maus das Panel verlaesst zuverlaessig auf. Keine Werte/Formeln geaendert, nur der Render-ZEITPUNKT.

**Regressionstest (`tests/e2e/panel-render-hover-guard.spec.js`, neu, 2 Tests):** (1) ein direkter DOM-Knoten-Identitaetsvergleich (nicht nur ein CSS-Selektor-Treffer) beweist, dass derselbe `<button>`-Knoten nach einem waehrend des Hovers ausgeloesten Kill-Refresh erhalten bleibt; (2) beweist, dass eine waehrend des Hovers verschobene Aktualisierung nicht verloren geht, sondern nach Verlassen des Panels tatsaechlich angewendet wird (echter, per verandertem Goldstand nachgewiesener Neu-Render, nicht nur ein zurueckgesetztes Pending-Flag). Beide gruen auf `chromium-desktop`, korrekt uebersprungen auf Mobile (Desktop-Tab-Navigation). Ergaenzende Regressionslaeufe (`combat`/`prestige`/`save-load`/`cosmetics`/`breeding`/`runes.spec.js`, alle 3 Projekte): 82 bestanden, 44 korrekt uebersprungen, 0 Fehlschlaege - keine Nebenwirkung auf die uebrigen, denselben Mechanismus nutzenden Panels.

**Verifiziert:** `npx eslint idledorf.js`: 0 Fehler. `node --check idledorf.js`: sauber. Vollstaendiger `npm run qa:full`-Abschlusslauf (3 Projekte): **850 bestanden, 176 korrekt uebersprungen, 0 unerwartete Fehlschlaege, 0 Flaky, Exit-Code 0, Laufzeit 49,3 Minuten.**

**Geaenderte/neue Dateien:** `idledorf.js` (`bkmpIdleRefreshLiveTabsRender`-Hover-Guard), Cache-Busting `?v=20260726-panelhoverfix1` (index.html/admin.html/idle-stream-mini.html), `style.css` Cache-Busting `?v=20260726-hoverfix1` (alle 5 HTML-Dateien), neu: `sql/20260726-fix-level-cap-constraint.sql`, `sql/20260726-diagnose-danw90-save-failure.sql`, `tests/e2e/panel-render-hover-guard.spec.js`.

## OBS-Stream-Overlay: kein Fortschritt bei verstecktem Hauptfenster (26.07.2026)

Direkter Anschluss an den obigen Abschnitt (gleiche Session). Nutzer-Meldung mit Screenshot: "kein ersichtlicher Fortschritt im OBS-Stream-Design, wenn das Hauptfenster nicht offen ist im Hintergrund... nur die Animation bewegt sich."

**Root Cause:** `bkmpIdleTick()` laeuft ueber ein normales `setInterval` (~900ms). Browser drosseln `setInterval`/`setTimeout` in einer WIRKLICH verdeckten (nicht bloss unfokussierten) Registerkarte unabhaengig vom Code - Chromes "Intensive Throttling" kann das bis auf ca. 1x/Minute herunterdrosseln, sobald eine Karte laenger im Hintergrund war. Ein vorhandener Kommentar in `bkmp-hud.js` (`bkmpIdleBroadcastCombatState`) ging noch davon aus, der Kampf-Loop laufe "bewusst auch im Hintergrund weiter, solange der Tab offen ist" - das stimmt fuer kurze Zeitraeume, bricht aber bei einer laengeren Streaming-Session (Hauptfenster bleibt bewusst stundenlang unfokussiert, waehrend OBS nur `idle-stream-mini.html` einfaengt) zusammen. Der bestehende `visibilitychange`-Handler behandelt "Tab versteckt" ohnehin schon wie "Spieler ist weg" (reine Speicher-Flushes, keine Weiterfuehrung der Simulation) - beim Sichtbarwerden holt `bkmpIdleCatchUpAfterHidden()` alles in EINEM Rutsch nach (server-seitige Offline-Simulation). Fuer einen Solo-Spieler ist das die richtige, absichtliche Optimierung (kein Grund, eine Simulation zu berechnen, die niemand sieht) - fuer einen Streamer, dessen OBS-Overlay UNABHAENGIG vom Hauptfenster-Fokus mitschaut, ist genau das aber das Problem: aus Sicht des Spiels "ist er weg", sobald er wegklickt, obwohl ein Zuschauer weiterhin zusieht.

**Nutzer-Entscheidung (AskUserQuestion, 3 Optionen vorgelegt):** "Leichter periodischer Nachhol-Broadcast" statt eines echten Web-Worker-Kampf-Loops (groesserer, riskanterer Umbau) - kleinerer, gezielter Eingriff.

**Fix (`idledorf.js`, `js/core/bkmp-idle-state.js`):** neue `bkmpIdleStartBackgroundStreamCatchup()`/`bkmpIdleStopBackgroundStreamCatchup()`, an- bzw. abgeschaltet im bestehenden `visibilitychange`-Handler. Solange die Karte versteckt ist UND `bkmpCombatBroadcastHasListener` (Presence-gestuetzt, siehe "Realtime-Kosten Stufe 2" oben - echtes Signal, ob gerade jemand am Overlay verbunden ist) `true` liefert, laeuft ein eigenes 20s-Intervall (`bkmpIdleBackgroundStreamCatchupTick()`), das dieselbe bereits bestehende, gut getestete Formel wie der normale Offline-Claim nutzt (`bkmpIdleClaimOfflineProgress()` - inkl. der bestehenden 60s-Mindestgrenze und der optimistischen Sperre in `api/claim-idle-offline-progress.js` gegen Doppel-Gutschrift bei ueberlappenden Aufrufen). Bei einem Stufenwechsel wird `bkmpIdleSpawnDragon()` aufgerufen - baut den neuen Drachen korrekt fuer die neue Stufe auf UND broadcastet selbst (`force=true`, bereits bestehender Code, keine Duplizierung noetig). Auch dieses neue Intervall unterliegt derselben Browser-Drosselung - das ist hier unschaedlich: selbst ~1x/Minute liegt komfortabel ueber der 60s-Mindestgrenze der Nachhol-Berechnung, der Zuschauer sieht dadurch regelmaessig echten (in Schueben springenden statt Tick-fluessigen) Fortschritt statt eines eingefrorenen Bildes. Fuer die grosse Mehrheit NICHT streamender Spieler bricht die Pruefung sofort auf `bkmpCombatBroadcastHasListener===false` ab - keine zusaetzliche Serverlast, kein Verhaltensunterschied.

**Bewusst NICHT gemacht** (siehe Nutzer-Entscheidung): kein Web-Worker-basierter echter Hintergrund-Kampf-Loop - waere die vollstaendigere Loesung (fluessige statt sprunghafte Fortschrittsanzeige), aber ein deutlich groesserer Umbau (Kampflogik greift aktuell direkt auf DOM/viele globale Variablen zu), fuer eine spaetere, separat zu beauftragende Stufe.

**Regressionstest (`tests/e2e/obs-background-catchup.spec.js`, neu, 4 Tests):** periodischer Tick wendet bei verbundenem Zuschauer echten Fortschritt an (Gold/Kills steigen, Drache wird bei Stufenwechsel korrekt mit vollem HP neu aufgebaut statt einen inkonsistenten Alt-Zustand zu zeigen); ohne verbundenen Zuschauer passiert nichts (kein unnoetiger Serveraufruf); Intervall startet beim Verstecken (mit Zuschauer) und stoppt beim Sichtbarwerden; ein zweites `visibilitychange`-Event ohne dazwischenliegendes Sichtbarwerden startet kein zweites, ueberlappendes Intervall. Alle 4 gruen auf allen 3 Projekten (12/12) - kein Mobile-Skip-Guard noetig, da rein funktionsbasiert (keine Desktop-Tab-Navigation involviert). Ergaenzende Regressionslaeufe (`network-guard`/`combat`/`save-load`/`raid`/`offline-afk.spec.js`, alle 3 Projekte): 115 bestanden, 8 korrekt uebersprungen, 0 Fehlschlaege.

**Verifiziert:** `npx eslint idledorf.js js/core/bkmp-idle-state.js`: 0 Fehler. `node --check` beide Dateien: sauber. Vollstaendiger `npm run qa:full`-Abschlusslauf (3 Projekte): **862 bestanden, 176 korrekt uebersprungen, 0 unerwartete Fehlschlaege, 0 Flaky, Exit-Code 0, Laufzeit 48,1 Minuten.**

**Geaenderte/neue Dateien:** `idledorf.js` (neue Funktionen + `visibilitychange`-Verdrahtung), `js/core/bkmp-idle-state.js` (`bkmpIdleBackgroundStreamCatchupTimer`), Cache-Busting `?v=20260726-obsstreamcatchup1` fuer beide Dateien (index.html/admin.html/idle-stream-mini.html), neu: `tests/e2e/obs-background-catchup.spec.js`.

## Progression-Rebalance: Vollständige Neubalancierung des Langzeit-Fortschritts (26.07.2026)

Eigenständiger, sehr umfangreicher 17-Phasen-Auftrag (vollständiger Text nicht hier dupliziert — siehe Chat-Verlauf), ausgelöst durch ein per echten Produktionsdaten bewiesenes Kernproblem: ein Level-2000-Spieler (Danw_90) erreichte das damalige Upgrade-Cap in **3 Minuten** Spielzeit. **Volle Analyse+Simulation+Implementierungs-Doku liegt in `PROGRESSION_REBALANCE_PHASE1.md`** (Root des Repos) — hier nur die Kurzfassung. Nutzer-Freigabe für alle Phasen ohne weitere Rückfragen: "Mach weiter. Alle Phasen.. Ich vertraue dir". **Nichts committed/gepusht durch diese Session bewusst ausgelöst, keine SQL live ausgeführt, keine Produktionsdaten verändert.** Git-Status vor Beginn: sauber, `main`.

**Kernstücke:** 1) alle 9 normalen Upgrade-Caps deutlich angehoben (500→2.500 / 5.000→20.000 / 100→150 bzw. 500), zugehörige Sammel-Pott-Obergrenzen proportional mit angehoben (sonst wären höhere Einzel-Caps bei bereits gefüllten Pools wirkungslos gewesen — reales, per Phase-1-Analyse dokumentiertes Risiko). 2) zentrales Softcap-System (`BKMP_IDLE_UPGRADE_SOFTCAP_CFG`, idledorf.js) — voller Bonus bis softCap1, abgeschwächter Grenznutzen + steilere Kosten danach. **Eigene Simulation deckte einen Übertuning-Fund auf, BEVOR er ausgeliefert wurde:** die erste Kosten-Multiplikator-Wahl ergab am letzten Rang eine ~31.784-fache Verteuerung — selbst ein Endgame-Spieler mit den echten Danw_90-Daten hätte für den allerletzten Rang über 750 TAGE gebraucht (ein harter Stopp, nur als "Softcap" verkleidet) — auf ~346x korrigiert (letzter Rang jetzt ~1 Tag bei Level 2000, ~8 Std. im Endgame), siehe `scripts/balance-sim/phase15-softcap-validation.js`. 3) deterministische Upgrade-Meilensteine (25/50/100/500/1.000 Ränge, kein eigener Speicherzustand, komplementäre Boni). 4) **Prestige-Baum von 6 Knoten auf 52 (5 Zweige à 10 + 2 "Vermächtnis") erweitert** (`js/systems/bkmp-prestige.js`) — alle 6 alten Knoten-IDs bleiben unverändert (additives JSONB, keine SQL-Migration nötig, per Test bewiesen dass bestehende Ränge vollen Bonus behalten), exponentielle Kosten ersetzen die alte lineare `cost=rang`-Formel (Vorbild: bereits bewährte Gilden-Technologie). 5) Prestige-Meilensteine nach insgesamt investierten Punkten (25-1.000, schalten u.a. 2 Zweige + Paragon frei). 6) **Paragon-System** für volle Knoten (4% Normalbonus/Rang, Kosten setzen nahtlos an der alten Kurve an, Cap 1.000 Ränge, ausdrücklich ausgeschlossen für Chance-basierte Knoten — "keine Chance-Werte unkontrolliert treiben"). 7) **Zweite Prestige-Ebene "Aufstieg"** (Drachenseelen) — als sicher umsetzbar befunden, weil Seelen/Aufstiegsstufe als reservierte JSONB-Schlüssel im selben `prestige_allocations`-Feld leben (kein Schema-Risiko, `supabase.js` bleibt eingefroren); Voraussetzungen Prestige-Stufe ≥10 + ≥500 investierte Punkte + ≥5.000 Lebenszeit-Stufen; setzt zusätzlich zum normalen Prestige-Reset den GESAMTEN Prestige-Baum zurück, vergibt eine kleine, dauerhafte universelle Boni-Seele (+0,5% Angriff/Leben/Gold/XP je Seele). 8) neue Gold-Senke **Gebäude-Überladung** (temporärer Produktionsboost, eskalierende Kosten bei Mehrfachnutzung, rein clientseitig). 9) Auto-Kauf skaliert jetzt mit Prestige-Investition und bevorzugt bei gleich günstigen Optionen Upgrades außerhalb der Softcap-Zone.

**Bewusst nicht umgesetzt** (eigenständiger, größerer Umfang, ehrlich dokumentiert statt überstürzt reingepresst): Dungeon-Schlüssel-Kauf/Stadtprojekte/Gildenprojekte/Kosmetik-Endgame-Shop (Phase 10 Rest — Runen-Neuwürfeln existierte bereits); die tatsächliche Wirkung von "Schlüsselmeister"/"Schlüsselbund" (beide bereits kaufbar, SQL-Datei mit rückwärtskompatiblen neuen Parametern für `dungeon_regen_calc()` vorbereitet aber nicht ausgeführt — echte Verdrahtung bräuchte die Rang→Prozent-Umrechnung serverseitig, ohne die Client-Formel zu duplizieren, siehe `sql/20260726-dungeon-key-prestige-bonus.sql`).

**Tests:** 4 neue Spec-Dateien (`upgrade-softcap-milestones`/`prestige-tree-v2`/`ascension-and-sinks` + der unabhängig gefundene Live-Bug `stage-bar-run-visibility`, siehe eigener Abschnitt weiter unten), 27 neue gezielte Testfälle für die Kernmechaniken, alle grün, keine Regression in den 700+ bestehenden Tests. Vollständiger Abschlusslauf (3 Projekte, `node --check`/`eslint`/`npm audit --omit=dev`/`static-checks.js`/volle Playwright-Suite): siehe Chat-Verlauf für die exakten Endzahlen dieser Session.

**Nebenbefund derselben Session (unabhängig vom Rebalance-Auftrag, live gemeldeter Bug):** Stufenleisten-Buttons ("Automatisch"/"Beste Stufe"/"Stufe wählen") verschwanden nach jedem Dungeon-/Turm-Lauf dauerhaft — ein Fix vom 20.07. hatte das Wiedereinblenden nach Laufende ersatzlos entfernt, mit einer nur für den kompakten Mobil-Modus richtigen Begründung; auf normaler Desktop-Breite blieb die Leiste seitdem nach dem ersten Lauf für den Rest der Sitzung unsichtbar (kein Datenverlust, rein CSS/JS-Sichtbarkeit). Neue `bkmpIdleStageBarWantedVisible()` (idledorf.js) prüft dynamisch den echten aktuellen Kompakt-Zustand statt einer statischen Annahme — identisches Bugmuster wie der bereits dokumentierte Phase-7.3-Fund (`bkmpRaidToggleCombatView`). Fix in `idledorf.js`/`js/systems/bkmp-dungeon.js`/`js/systems/bkmp-tower.js`, 4 neue Regressionstests (`tests/e2e/stage-bar-run-visibility.spec.js`), 36/36 grün über alle 3 Projekte.

Cache-Busting `?v=20260726-progrebalance1` für `idledorf.js`, `js/systems/bkmp-prestige.js`, `js/systems/bkmp-dungeon.js`, `js/systems/bkmp-breeding.js`, `js/systems/bkmp-runes.js`, `js/core/bkmp-combat-math.js`, `js/ui/bkmp-hud.js`, `style.css` in allen 5 HTML-Dateien gesetzt.

## Gilden-Technologie v2: 10 neue Zweige (26.07.2026)

Eigenständiger Auftrag, ausgelöst durch einen Screenshot (alle 9 bestehenden Gilden-Tech-Zweige auf Stufe 20/20, Gildenkasse 2358.93M Gold hatte nichts mehr zu tun — dieselbe "harte Fortschritts-Wand"-Problemklasse wie die kurz zuvor abgeschlossene Progression-Rebalance, nur auf Gilden- statt Spielerebene). Auf Nutzerwunsch erst gebrainstormt (17 Ideen in zwei Runden gesammelt), dann explizit **nur die zweite 10er-Liste** zur Umsetzung freigegeben (Kriegsrat/Turm-Vorreiter/Brutbeschleuniger/Gildenschmiede/Gilden-Autokauf/Nachtwache/Streak-Schutz/Stadtmauer/Aufstiegsvorbereitung/Willkommenspaket) — die erste 7er-Liste bleibt zurückgestellt. **Nichts committed/gepusht durch diese Session bewusst ausgelöst, keine SQL live ausgeführt, keine Spielwerte der bestehenden 9 Zweige verändert.**

**Architektur-Fund vor der Umsetzung:** `guild_tech_levels(guild_id, tech_id, level)` ist bereits ein flaches Key-Value-Schema (`sql/supabase-guild-tech-tree.sql`) — akzeptiert beliebige neue `tech_id`-Strings ohne jede Migration. Das einzige echte Hindernis war die HART auf `max_level=20`/`200.000·1,4^Stufe` verdrahtete `guild_tech_upgrade()`-Funktion (galt bisher für ALLE Zweige gleich). Neues Tier-System (`BKMP_GUILD_TECH_TIER`/`bkmpGuildTechTierDef()` in `js/systems/bkmp-guild.js`, analog zum bereits bewährten `BKMP_PRESTIGE_TIER`-Muster aus der Progression-Rebalance): STANDARD (20/200k/1,4, unverändert für die alten 9), LOW5 (5/350k/1,6), MED10 (10/300k/1,35), TOGGLE (1/1,5M fest) — `guild_tech_upgrade()` (`sql/20260726-guild-tech-branches-v2.sql`, neu, **noch nicht ausgeführt**) ersetzt die feste Konstante durch eine `case`-Zuordnung pro `tech_id`, alte 9 Zweige bekommen dabei exakt dieselben Werte wie vorher.

**Die 10 Zweige, je nachdem wo der Effekt tatsächlich lebt:**
- **Kriegsrat** (LOW5, +1 Arena-Angriff/Tag/Stufe) und **Stadtmauer** (MED10, +1% eigener HP-Beitrag zum Raid-Stadt-HP-Pool/Stufe) ändern echte SERVERSEITIGE RPCs (`arena_attack()`/`raid_join()`, beide in derselben neuen SQL-Datei per `create or replace function` erweitert — Kriegsrat hebt das feste `10`-Tageslimit an, Stadtmauer multipliziert den eigenen `hp`-Beitrag vor dem Insert in `raid_participants`, statt den GEPOOLTEN Gegenangriffs-Schaden anzufassen, der über mehrere Gilden hinweg nicht sauber einer einzelnen Gilde zuordenbar wäre).
- **Nachtwache** (MED10, +0,5 Std. Offline-Deckel/Stufe, max. +5 Std.) ändert `api/claim-idle-offline-progress.js` — eine zusätzliche `guild_members`/`guild_tech_levels`-Abfrage (per Service-Role, RLS-unabhängig) vor der bestehenden `maxHours`-Berechnung.
- Die übrigen 7 (**Turm-Vorreiter**, **Brutbeschleuniger**, **Gildenschmiede**, **Gilden-Autokauf**, **Streak-Schutz**, **Aufstiegsvorbereitung**, **Willkommenspaket**) landen alle im selben, bereits bestehenden client-seitigen Cache (`bkmp-guild-tech-cache`, `bkmpIdleGetGuildTechCache()`) — neuer generischer Zugriff `bkmpGuildTechBonus(key)` (analog `bkmpPrestigeBonus()`). Turm-Vorreiter und Willkommenspaket brauchen dafür eine SPEZIELLE (nicht nur `Stufe·Effekt`) Berechnung in `bkmpGuildRefreshTreasuryBonusCache()`:
  - **Turm-Vorreiter:** die höchste je von EINEM Mitglied erreichte Turmstufe (`idle_player_state.turm_highest_wave`, bereits öffentlich lesbar — gleiches Prinzip wie die Arena-Gewinnchance-Anzeige, neue `loadGuildTowerChampionWave()` in `supabase.js` fragt gezielt über die bereits geladene Mitgliederliste ab) fließt als `floor(Wellen/10)·Stufe·0,05%` in dieselben drei Pools (Angriff/Verteidigung/Gold) wie der bestehende Kassenstand-Bonus, gedeckelt bei 25%.
  - **Willkommenspaket:** reiner TOGGLE + zeitlich befristet (3 Tage ab `guild_members.joined_at`, dafür `bkmpGuildGetMine()` um `myJoinedAt` ergänzt) — +10% auf dieselben drei Pools, solange aktiv.
  - Brutbeschleuniger (Brutzeit + Opfergabe-Rabatt, `bkmp-breeding.js`), Gildenschmiede (Runen-Aufwertungskosten, `bkmp-runes.js`, gedeckelt bei 20%), Gilden-Autokauf (`bkmpIdleAutoBuyMaxPurchasesPerTick()`, additiv zum bestehenden Prestige-Hebel), Streak-Schutz (`bkmpIdleCheckDailyStreak()`: ausgelassener Tag senkt nur um 1 Stufe statt auf 1 zurückzusetzen) und Aufstiegsvorbereitung (neue `bkmpAscensionEffectiveThresholds()`, senkt alle 3 Aufstiegs-Schwellen um bis zu 10%) sind einfache, gedeckelte Additionen an bereits bestehenden Formeln.

**Drei echte, beim Verdrahten selbst gefundene Anzeige/Realität-Mismatch-Bugs** (gleiches wiederkehrendes Muster wie mehrfach zuvor in diesem Projekt, z. B. Phase-7.3-Fund `bkmpRaidToggleCombatView` — ein Wert wird an EINER Stelle korrekt dynamisch berechnet, an einer ANDEREN Stelle bleibt eine alte Konstante hartcodiert stehen):
1. Arena-Panel (`js/systems/bkmp-arena.js`) zeigte hartcodiert "X/10" für das Tageslimit UND hätte den Angriffs-Knopf bei genau 10 Angriffen fälschlich gesperrt, obwohl der Server mit Kriegsrat bereits mehr zugelassen hätte — Anzeige liest jetzt `bkmpGuildTechBonus('arenaExtraAttempts')`.
2. Die Tageslimit-Fehlermeldung selbst (`supabase.js`, `bkmpArenaAttack()`) hatte "Tageslimit von 10 Arena-Angriffen" fest im Text — zeigt jetzt den tatsächlich wirksamen Wert.
3. Die Aufstiegs-Voraussetzungs-Hinweiszeile (`bkmpAscensionRenderSectionHtml()`, `bkmp-prestige.js`) zeigte die undiscountierten Basis-Schwellen statt der durch Aufstiegsvorbereitung tatsächlich geltenden — behoben durch dieselbe `bkmpAscensionEffectiveThresholds()`, die auch `bkmpAscensionEligible()` speist (keine zwei divergierenden Wahrheiten mehr möglich).

**Tests (`tests/e2e/guild-tech-ext.spec.js` + `tests/e2e/guild-tech-ext-readside.spec.js`, neu, 16 Tests):** zwei Dateien, weil unterschiedliche Fixture-Infrastruktur nötig ist (eigene Mehrspieler-Gilden-Fixture vs. Standard-Teststand + direkte Cache-Injektion für die reine Read-Side). Beim eigenen Testen vier echte Test-Bugs gefunden+korrigiert (keine App-Bugs): (a) `useFakeClock:true` installiert die gefakte Uhr nur, wenn ein Test die `qaClock`-Fixture tatsächlich destructured (bereits aus Phase 5 bekanntes Playwright-Fixture-Verhalten, hier neu übersehen) — ohne das sah der erste, implizite Login-Streak-Check noch die echte Systemzeit; (b) der erste Kriegsrat-Testentwurf griff versehentlich sich selbst an (`invalid_target`) und vergaß, die Mock-Uhr zwischen Angriffen um >3 Min. vorzuspulen (Cooldown); (c) derselbe Test zählte zunächst 11 statt der für ein +2-Stufen-Limit von 12 nötigen 12 erfolgreichen Angriffe vor dem erwarteten Fehlschlag; (d) `raid_join()` liefert im Mock ein einzelnes Objekt zurück, kein Array — `result[0]` schlug fehl, `result` direkt war richtig. Nach allen vier Korrekturen: 16/16 grün auf allen 3 Projekten (48 Gesamtläufe).

Cache-Busting `?v=20260726-guildtechext1` für `style.css` (5 HTML-Dateien), `supabase.js`/`js/systems/bkmp-guild.js`/`js/systems/bkmp-arena.js`/`js/systems/bkmp-breeding.js`/`js/systems/bkmp-runes.js`/`js/systems/bkmp-events.js`/`js/systems/bkmp-prestige.js`/`idledorf.js` (jeweils 3 HTML-Dateien) gesetzt. `package.json`s `qa:features`-Script um beide neuen Testdateien + das zuvor schon fertige `prestige-automation-toggles.spec.js` erweitert. `tests/FEATURE_MATRIX.md` aktualisiert.

**Nachtrag: `sql/20260726-guild-tech-branches-v2.sql` wurde noch am selben Tag live ausgeführt** (per echtem Spieler-Screenshot bestätigt — alle 10 Zweige waren im Admin-Konto einzeln kauf-/anzeigbar, mehrere bereits auf Maximalstufe). Admin-Übersicht über alle Gilden-Technologiestände bewusst nicht gebaut (bestehende Lücke, nicht Teil dieses Auftrags). Die erste, zurückgestellte 7er-Ideen-Liste (Gildenboss-Zeitfenster/Gilden-Dungeon-Schlüssel/Mitgliederplätze-per-Tech/passives Kassen-Einkommen/zweiter Gildenboss-Versuch/Gilden-Kosmetik/zusätzlicher Quest-Slot) wartet auf eine mögliche künftige Freigabe.

## Dringender Produktions-Fix: `dungeon_regen_calc()`-Mehrdeutigkeit (26.07.2026)

Live-Vorfall, direkt im Anschluss an die Gilden-Technologie v2: mehrere Spieler meldeten "Dungeon-Status konnte nicht geladen werden" beim Öffnen des Dungeon-Tabs. **Root Cause, per direktem Live-Check gegen die echte Supabase-REST-API bestätigt (nicht nur vermutet):** die bereits vorbereitete (und in der Progression-Rebalance oben als "noch nicht ausgeführt" dokumentierte) `sql/20260726-dungeon-key-prestige-bonus.sql` wurde zwischenzeitlich live ausgeführt. Sie fügte `dungeon_regen_calc()` zwei zusätzliche, optionale Parameter per `create or replace function` hinzu — da sich dadurch die Parameterliste änderte, hat das NICHT die alte 2-Parameter-Fassung ersetzt, sondern eine zweite, überladene Fassung DANEBEN angelegt (Postgres identifiziert Funktionen über Name+Parametertypen, nicht nur den Namen). Jeder bestehende 2-Parameter-Aufruf (u.a. `dungeon_get_all_status()`, trifft JEDEN Spieler beim Öffnen des Dungeon-Tabs) wurde dadurch mehrdeutig zwischen beiden Fassungen — Postgres/PostgREST lehnte jeden solchen Aufruf mit `PGRST203 "Could not choose the best candidate function"` ab. Bestätigt per anonymem curl direkt gegen die echte Supabase-Instanz.

**Fix:** `sql/20260726-fix-dungeon-regen-calc-ambiguity.sql` (neu) entfernt die alte, jetzt überflüssige 2-Parameter-Fassung per `drop function if exists` — die neue 4-Parameter-Fassung deckt dank identischer Default-Werte (14400s/5) jeden bestehenden 2-Parameter-Aufruf weiterhin exakt gleich ab, kein Aufrufer musste geändert werden. **Vom Nutzer live ausgeführt und bestätigt behoben** ("Jup geht wieder").

## Gilden-Technologie v2 — Rebalance + Paragon-Fortführung (26.07.2026)

Direkte Folgeanfrage, ausgelöst durch dasselbe Live-Feedback wie oben ("Für danach höhere Caps.. die neuen Skills kann ich jetzt schon ohne Probleme wieder durchkaufen. Haben noch 2320.99M gold auf der Bank") — bestätigt per Screenshot: mehrere der 10 neuen Zweige waren bereits auf Maximalstufe, obwohl insgesamt nur ~38M der 2,359 Mrd. Gold Kassenstand ausgegeben wurden. Nutzer-Entscheidung nach Rückfrage: **"Maximalstufe anheben. Kosten anheben und Paragon Option ab dann"**.

**Umsetzung (nur die 8 nicht-TOGGLE Zweige — Streak-Schutz/Willkommenspaket sind reine Ein/Aus-Schalter, "mehr Stufen" ergibt dort keinen Sinn):**
- Neue Tier-Konstanten in `BKMP_GUILD_TECH_TIER` (`js/systems/bkmp-guild.js`): LOW5→**LOW** (5→**15** Stufen, 350k→**600k**, 1,6→**1,5**), MED10→**MED** (10→**25** Stufen, 300k→**350k**, 1,35→**1,28**), neue **STANDARD_V2** (Brutbeschleuniger/Gildenschmiede, 20→**35** Stufen, 200k→**250k**, 1,4→**1,18** — bewusst eine EIGENE Kopie, getrennt von der `STANDARD`-Kurve der ursprünglichen 9 Zweige, die dadurch komplett unangetastet bleiben), TOGGLE (Preis 1,5M→**8M**). Maxen ALLER 10 Zweige kostet jetzt zusammen **ca. 4,36 Mrd. Gold** statt ca. 40M — übersteigt die beobachtete 2,36-Mrd.-Kasse, also ein echtes, andauerndes Ziel statt Wechselgeld.
- **Paragon-Fortführung** nach der (jetzt höheren) Maximalstufe — identisches Prinzip wie `BKMP_PRESTIGE_PARAGON_*` beim Spieler-Prestige: 4% des normalen Rang-Effekts pro Paragon-Rang, Kosten setzen an der letzten normalen Stufe an und wachsen mit `+0,15` auf das Basiswachstum weiter, harte Grenze bei Rang 1000 (`Number.MAX_SAFE_INTEGER`-Schutz analog zum Spieler-Paragon). Gespeichert als synthetische `"<id>__paragon"`-Zeile im bereits bestehenden flachen `guild_tech_levels`-Schema (identischer Trick wie `__dragon_souls`/`__ascension_level` im Spieler-Prestige-JSONB) — keine neue Tabelle/Spalte nötig. Serverseitig (`guild_tech_upgrade()`) zusätzlich abgesichert: ein Paragon-Kauf wird abgelehnt (`base_not_maxed`), solange die zugehörige normale Stufe noch nicht voll ist.
- **Drei nachgelagerte Deckel angehoben**, die sonst die neuen, höheren Stufen wirkungslos gemacht hätten (gefunden beim Durchrechnen, nicht nur behauptet): Turm-Vorreiter 25%→**50%** (`bkmpGuildRefreshTreasuryBonusCache()`), Gildenschmiede-Runenrabatt 20%→**40%** (`bkmpIdleRuneUpgradeCost()`, `bkmp-runes.js`), Aufstiegsvorbereitung 10%→**35%** (`bkmpAscensionEffectiveThresholds()`, `bkmp-prestige.js`, bewusst deutlich unter 100%, damit die Aufstiegs-Hürde real bleibt). Brutbeschleuniger brauchte KEINEN Deckel-Wechsel — 35 neue Stufen × 1% = 35%, bleibt unter den bereits bestehenden geteilten 40%/50%-Deckeln (Skilltree+Prestige+Gilde zusammen).
- Exakte Paragon-Basiskosten je Zweig-Gruppe (einmalig in Node vorausberechnet, identisch in Client UND SQL hinterlegt): LOW-Gruppe (Kriegsrat/Aufstiegsvorbereitung) 289.009.968 Gold bei Wachstum 1,65; MED-Gruppe (Turm-Vorreiter/Gilden-Autokauf/Nachtwache/Stadtmauer) 187.259.282 bei 1,43; STANDARD_V2-Gruppe (Brutbeschleuniger/Gildenschmiede) 92.422.965 bei 1,33.

**Tests:** `tests/e2e/guild-tech-ext.spec.js`/`guild-tech-ext-readside.spec.js` um die neuen Zahlen aktualisiert (3 bestehende Tests hatten die alten Maximalstufen/Kosten hart einprogrammiert) + 1 neuer Test für die Paragon-Fortführung (Ablehnung vor voller Maximalstufe, exakte erste/zweite Paragon-Kosten, Bonus-Beitrag im Cache). 17/17 grün auf allen 3 Projekten.

Cache-Busting `?v=20260726-guildtechrebalance1` für `style.css` (5 HTML-Dateien), `js/systems/bkmp-guild.js`/`js/systems/bkmp-runes.js`/`js/systems/bkmp-prestige.js` (jeweils 3 HTML-Dateien) gesetzt.

**Nachtrag, selbe Nacht (direkter Folge-Wunsch nach Screenshot der 9 ursprünglichen Zweige, alle Stufe 20/20): "Auch die dürfen erhöht werden etc."** — der ursprünglich geplante "die alten 9 bleiben zahlen-identisch unangetastet"-Ansatz wurde auf diesen ausdrücklichen Wunsch hin verworfen. Die 9 ursprünglichen Zweige (`BKMP_GUILD_TECH_CATALOG`, bisher eigenes, älteres `perLevel`/`statKey`-Schema mit fester `BKMP_GUILD_TECH_MAX_LEVEL=20`/`200.000·1,4^Stufe`) bekommen jetzt **dieselbe Kurve wie STANDARD_V2** (20→35 Stufen, 200.000·1,4^Stufe → 250.000·1,18^Stufe) — bewusst identisch zu Brutbeschleuniger/Gildenschmiede gewählt (gleiche Ausgangslage: reiner %-Stat pro Stufe), wodurch sie sich automatisch auch dieselben, bereits berechneten Paragon-Basiswerte teilen (92.422.965 bei Wachstum 1,33) — keine zusätzliche Zahlenrunde nötig. Die alten `perLevel`/`statKey`-Felder blieben unangetastet (additiv um `effectPerRank`/`maxLevel`/`baseCost`/`growth`/`paragonEligible` ergänzt), dadurch lassen sich dieselben generischen Tier-/Paragon-Helfer (`bkmpGuildTechExtCostForLevel`/`bkmpGuildTechParagon*`) verlustfrei wiederverwenden. Neue gemeinsame `bkmpGuildTechMaxedExtraHtml()`-Funktion fasst den "Maximalstufe erreicht + Paragon-Kauf"-Kartenbereich zusammen, den jetzt BEIDE Kataloge (alte 9 + neue 10) im Render nutzen — vermeidet doppelten Code für identische Logik. `sql/20260726-guild-tech-rebalance-paragon.sql` + `tests/mock/rpc-engine.js` entsprechend erweitert (9 neue `<id>__paragon`-Einträge, dieselben Basiswerte wie Brutbeschleuniger/Gildenschmiede). 3 bestehende `guild-tech.spec.js`-Tests hatten die alten Zahlen (20/200.000/1,4) hart einprogrammiert — aktualisiert + 1 neuer Test für die Paragon-Fortführung eines ursprünglichen Zweigs. 14/14 grün auf allen 3 Projekten.

**Nicht geprüft / offen:** `sql/20260726-guild-tech-rebalance-paragon.sql` (baut auf der bereits live laufenden `branches-v2.sql` auf, deckt jetzt auch die 9 ursprünglichen Zweige ab) wurde nicht ausgeführt — **bitte im Supabase-Dashboard nachholen**, danach wirkt der komplette Rebalance (alle 19 Zweige + Paragon) auch serverseitig (bis dahin zeigt der Client zwar die neuen, höheren Maximalstufen/Kosten an, der Server würde einen Kauf oberhalb der ALTEN Maximalstufen aber weiterhin ablehnen, da die dortige CASE-Zuordnung noch die alten Werte kennt).

## Dringender Fix: Ei-Duplikat über Automatische Ei-Ausbrütung (27.07.2026)

Spieler-Meldung (Discord, "Kaiser [Dustin]", Screenshot): "Auto brütet funktioniert aber die Eier werden trotzdem angezeigt das man sie nochmal brüten kann" — bei legendären Eiern (Opfergabe-Bestätigung noetig) konnte aus EINEM Ei praktisch ZWEI Drachen werden. Nutzer-Auftrag: sofort prüfen. **Nichts gepusht/deployed (Ausnahme: die bekannte Auto-Push-Eigenart dieser Umgebung könnte greifen, siehe `[[feedback_bkmp_user_handles_commits]]`), keine Produktionsdaten/SQL angefasst.**

**Root Cause (per Code-Lektüre bewiesen, danach per rot-vor/grün-nach-Test bestätigt, nicht nur vermutet):** der Prestige-Automations-Zweig `auto_egg_nest_unlock` (`bkmpIdleRunAutomationToggles()`, idledorf.js, Teil der Progression-Rebalance vom 26.07.) wählte das "beste" (höchste Rarität) Ei direkt aus dem rohen `bkmpPlayerDragonEggs` — anders als `bkmpIdleRenderDragonsPanel()`s bereits bestehender `unassignedEggs`-Filter (`bkmp-breeding.js:968`), der Eier ausschließt, die bereits in einem Nest brüten. Ein legendäres Ei löst in `bkmpDragonAssignEggToNest()` eine AWAIT-blockierende Opfergabe-Bestätigung aus (`bkmpConfirmDialog`) — solange die offen steht (oder generell, solange das Ei noch nicht geschlüpft ist), blieb es in `bkmpPlayerDragonEggs` unverändert "verfügbar". Ein späterer Automations-Durchlauf (10s-Takt) mit einem ZWEITEN freien Nest griff dasselbe, längst in Nest 1 brütende Ei erneut und legte es in Nest 2 — `assignEggToDragonNest()` (supabase.js) prüft server-seitig nur, ob das NEST leer ist, nicht ob das EI schon woanders liegt. Aus einem Ei wurden zwei Drachen. Zusätzlich, selbst OHNE ein zweites Nest: zwei Automations-Durchläufe für dasselbe Nest+Ei innerhalb des Bestätigungsfensters konnten ein zweites, redundantes Bestätigungsfenster aufreißen ("es wird wieder angezeigt") — hier verhinderte der bereits bestehende Server-Guard zwar eine echte Doppel-Zuweisung ins selbe Nest, aber nicht die verwirrende doppelte Anzeige.

**Fix (`js/systems/bkmp-breeding.js`, `bkmp-breeding.js`'s `bkmpDragonAssignEggToNest()`):**
1. Neuer Guard direkt am Funktionsanfang: `if (bkmpPlayerDragonNests.some(n => n.egg_id === eggId)) return;` — ein Ei, das bereits in irgendeinem Nest liegt, kann nie ein zweites Mal zugewiesen werden. Sitzt IN der Funktion selbst (nicht nur an den beiden Aufrufstellen), schützt dadurch automatisch jeden aktuellen UND künftigen Aufrufer.
2. Neue Nest-ID-Busy-Sperre (`bkmpDragonAssignBusyNestIds`, identisches, bereits bewährtes Muster wie das bestehende `bkmpDragonHatchBusyNestIds`) über die GESAMTE Funktionsdauer inklusive der Opfergabe-Bestätigung — verhindert das doppelte Bestätigungsfenster bei einem zu engen Automations-Rennen.
3. `idledorf.js`s Automations-Dispatcher bekommt denselben `unassignedEggs`-Filter wie die manuelle Render-Funktion (`availableEggs = bkmpPlayerDragonEggs.filter(e => !bkmpPlayerDragonNests.some(n => n.egg_id === e.id))`) — ohne das würde die Automatisierung bei mehreren freien Nestern untätig auf das bereits vergebene Top-Raritäts-Ei beharren, statt das nächste tatsächlich freie Ei zu nehmen.

**Rot-vor/grün-nach-Beweis (nicht nur behauptet):** `git stash push -- idledorf.js js/systems/bkmp-breeding.js` (nur die beiden Fix-Dateien, Rest der Session-Arbeit unangetastet) → neuer Test lief GEGEN DEN ALTEN CODE → 2 von 3 Tests schlugen exakt wie gemeldet fehl (`filledNests.length` war `2` statt `1` — beide Nester trugen dasselbe Ei; bei zwei unterschiedlichen Eiern bekamen beide Nester versehentlich dasselbe legendäre statt je ein eigenes) → `git stash pop` → alle 3 Tests grün. Der dritte Test (direkter Gleichzeitigkeits-Beweis über `Promise.all` im selben JS-Tick) war schon vorher grün, da der bestehende Server-Guard eine echte Doppel-Zuweisung INS SELBE Nest bereits verhinderte — bestätigt, dass die eigentliche Lücke die MEHRERE-Nester-Situation war, nicht eine einzelne Server-Race.

**Neuer Regressionstest:** `tests/e2e/dragon-breeding-automation.spec.js` (3 Tests: kein Duplikat über zwei freie Nester bei nur einem Ei, zwei unterschiedliche Eier werden weiterhin korrekt auf zwei Nester verteilt, direkter Nebenläufigkeitsbeweis der Busy-Sperre) — 30/30 bei 10x Wiederholung auf `chromium-desktop` grün, 9/9 über alle 3 Projekte grün. Adjazente Regression (`combat.spec.js`, `prestige-automation-toggles.spec.js`, `save-load.spec.js`) unverändert grün (der eine erwartete Fehlschlag ist die bereits lange dokumentierte, absichtlich per `test.fail()` verfolgte Zwei-Tab-Speicher-Race, keine neue Regression). `node --check`/`eslint` auf beiden geänderten Dateien sauber.

Cache-Busting `js/systems/bkmp-breeding.js?v=20260727-eggdupefix1` + `idledorf.js?v=20260727-eggdupefix1` (alle 3 einbindenden HTML-Dateien) gesetzt.

**Nicht geprüft:** ob bereits VOR diesem Fix ein echter Spieler ein legendäres Ei tatsächlich dupliziert hat (z. B. Kaiser Dustin selbst) — das würde eine Live-Datenbankprüfung der betroffenen `player_dragon_nests`/`player_dragons`-Zeilen erfordern (kein DB-Schreibzugriff für diese Session, nur die Fix-Verifikation lief gegen den lokalen Mock). Falls gewünscht, kann eine rein lesende Diagnose-SQL-Abfrage für den gemeldeten Spieler vorbereitet werden.

**Nachtrag (27.07.2026) — per Discord-Screenshot LIVE bestätigt, nicht nur theoretisch:** Spieler "Kaledoss" beschrieb im Server-Chat exakt den oben gefixten Mechanismus ("Machte Ei-Dungeon, bekam 1-2 Eier, die Automatik nahm das eine, das erste was drin war erschien dann immer nach paar Sekunden auch im nächsten freien Slot, solange bis alle 5 voll waren"). Rein lesende Diagnose (`sql/20260727-diagnose-kaledoss-egg-duplicate.sql`, vom Nutzer ausgeführt) bestätigte: 4 legitime, Tage auseinanderliegende erwachsene Wuffdrache-Funde (16./18./20./24.07., je eigene gewürfelte Werte) + 2 zusätzliche, noch nicht ausgewachsene ("Jugendlich") Wuffdrache exakt am Vorfallstag (27.07.), 27 Sekunden auseinander geschlüpft — der Bruch im sonst regelmäßigen Muster bestätigt einen echten, bereits eingetretenen Fall. **Nutzer-Entscheidung: keine Korrektur an Kaledoss' (oder anderen) Accounts** — bei der Unsicherheit, welche Drachen über alle Spieler hinweg tatsächlich Duplikate sind, wäre ein pauschaler Eingriff riskanter als der Schaden selbst; der Fix verhindert nur künftige Fälle.

## Dringender Fund beim Testen aller Prestige-Automatisierungs-Skills (27.07.2026): Schlüsselmeister/Schlüsselbund unwirksam + echte Zeitregression gefunden+gefixt

Nutzer-Auftrag: "Ich will das du alle Skills testest. Auf ihre Funktionalität. Weil habe das Gefühl das einige nicht funktionieren." Beim systematischen Durchgehen aller 10 Automations-Prestige-Knoten fand der Nutzer selbst zuerst (Screenshot, Zweig "Runen & Dungeons"): die Knoten **Schlüsselmeister**/**Schlüsselbund** zeigten wörtlich den rohen internen Hinweis "(SQL-Migration erforderlich, siehe Kommentar im Code)" als Spieler-sichtbaren Beschreibungstext — ein interner Entwickler-Kommentar, der versehentlich in `js/systems/bkmp-prestige.js`s `desc`-Feld gelandet war (siehe Kommentar dort: die zugehörige SQL war bewusst als "Rang→Bonus-Wirkung folgt separat" zurückgestellt worden, siehe Progression-Rebalance Phase 5). Nutzer-Entscheidung nach Rückfrage (3 Optionen: fertig verdrahten / nur Text bereinigen / vorerst sperren): **"Jetzt richtig fertig verdrahten (Empfehlung)"**.

**Zweiter, unabhängig gefundener Fund beim Verdrahten selbst (nicht Teil des Auftrags, aber direkt dabei entdeckt):** `sql/20260726-dungeon-key-prestige-bonus.sql` (bereits live ausgeführt, siehe Abschnitt "Dringender Produktions-Fix: `dungeon_regen_calc()`-Mehrdeutigkeit" weiter oben) hatte beim Hinzufügen der zwei neuen Parameter **versehentlich die ÄLTERE, rollierende Zeitlogik wiederhergestellt** — und damit stillschweigend den expliziten Spielerwunsch vom 16.07. ("Alles auf 0/4/8/12/16/20 Uhr skaliert", `sql/supabase-dungeon-fixed-key-times.sql`) rückgängig gemacht, ohne dass es bisher jemandem aufgefallen war. Seit dem 26.07. lief die Schlüssel-Regeneration für JEDEN Spieler wieder "4h rollend seit dem individuellen letzten Zeitstempel" statt der fest für alle gleichen Uhrzeiten. Bemerkenswert: der bestehende Mock (`tests/mock/rpc-engine.js`) hatte diese Regression NIE übernommen (er bildet die "aktuelle/maßgebliche" SQL-Datei nach, nicht was tatsächlich live lief) — ein Lehrbuchbeispiel dafür, dass ein Mock nicht automatisch merkt, wenn die echte Produktions-SQL abseits davon driftet, solange niemand die reale DB direkt gegenprüft.

**Dritter Fund beim Verdrahten (strukturelles Risiko, noch nicht ausgelöst):** `dungeon_keys.keys` trägt eine harte DB-CHECK-Constraint `keys >= 0 and keys <= 5` (`sql/supabase-dungeon-system-v2.sql:41`) — sobald Schlüsselbund den Deckel über 5 hebt, hätte jeder Versuch, den zusätzlichen Schlüssel tatsächlich zu speichern, an dieser Constraint scheitern müssen (identisches Bugmuster wie der Level-2000-Deckel vom 26.07., nur hier präventiv VOR dem ersten echten Auftreten gefunden).

**Fix (eine SQL-Datei für alle drei Funde, da sie dieselbe Funktion betreffen):** `sql/20260727-fix-dungeon-regen-fixed-slots-and-wire-prestige.sql` (neu, **noch nicht ausgeführt**):
1. `dungeon_keys_keys_check` auf `<= 100` angehoben (weit über dem realistischen Maximum von ~95 bei Schlüsselbund-Maximalrang+Paragon).
2. `dungeon_regen_calc()`: Standardfall (kein Schlüsselmeister-Bonus, `p_interval_seconds=14400`) nutzt wieder unverändert das feste 00/04/08/12/16/20-Uhr-Berlin-Raster für ALLE Spieler gleich (Regression behoben). Individuelles (kürzeres) Intervall (Schlüsselmeister aktiv) läuft stattdessen rollierend seit dem eigenen letzten Zeitstempel — ein persönlich verkürztes Intervall kann strukturell nicht mehr auf demselben gemeinsamen Raster wie alle anderen Spieler liegen, das war ja gerade der Sinn des 16.07.-Wunsches (ein GEMEINSAMES Raster).
3. `dungeon_get_all_status()`/`dungeon_consume_key()` lesen jetzt `idle_prestige_state.prestige_allocations` direkt (Rang×3% + Paragon-Rang×0,12% für Schlüsselmeister, gedeckelt bei 95% gegen ein Null-/Negativ-Intervall bei extremem Paragon-Ausbau; Rang×1 + Paragon-Rang×0,04 für Schlüsselbund, kein Deckel nötig da rein additiv) und reichen die berechneten Werte an `dungeon_regen_calc()` durch — keine Client-Server-Duplikation der Rang→Bonus-Formel, einzige Quelle der Wahrheit ist die DB.

**Client-Text bereinigt** (`js/systems/bkmp-prestige.js`): roher Hinweis entfernt, Schlüsselmeister zeigt jetzt ehrlich "(max. 95%)" statt der undokumentierten Deckel-Notwendigkeit zu verschweigen, `serverSyncRequired`-Flag (steuerte nur eine dezente Amber-Umrandung) entfernt.

**Neue Tests** (`tests/e2e/dungeon-key-prestige-bonus.spec.js`, 5 Tests): Regressionsbeweis (fest verankertes Raster bleibt ohne Bonus exakt wie zuvor — dieselbe Ankerzeit-Technik wie der bestehende "exakte Slot-Grenze"-Block in `dungeon-time.spec.js`, der die ganze Zeit unverändert grün blieb, da der Mock nie mitregressiert war), Schlüsselmeister Rang 10 (30% schneller, personalisiertes statt gemeinsames Raster), Schlüsselbund Rang 20 (Deckel 5→25), Paragon-Anteil (Maximalrang+10 Paragon=91,2%), extremer Paragon-Ausbau bleibt korrekt bei 95% gedeckelt. Mock (`tests/mock/rpc-engine.js`) entsprechend erweitert (`dungeonRegenCalc()` verzweigt jetzt identisch zur SQL, neue `dungeonKeyBonusForName()`-Hilfsfunktion).

**Eigener Fund beim ersten Multi-Projekt-Lauf (nicht die App, sondern der eigene neue Test):** alle 5 neuen Tests klicken echte Desktop-Tab-Buttons (`#idleTabBtnDungeon`) - auf `mobile-small`/`mobile-large` (kompakte Navigation) korrekt unsichtbar, ohne den identischen, in diesem Projekt bereits etablierten `test.beforeEach()`-Skip-Guard (siehe `dungeon-time.spec.js`) haengen die Klicks bis zum Timeout. Guard nachgezogen (`dungeon-key-prestige-bonus.spec.js` komplett, `prestige-automation-remaining-skills.spec.js` gezielt in den 4 der 6 `describe`-Bloecke, die tatsaechlich einen Tab-Klick/eine Tab-Button-Sichtbarkeitspruefung brauchen - die beiden reinen Funktionsaufruf-Bloecke Auto-Kauf/Kampfgeschwindigkeit laufen bewusst weiter auf allen 3 Projekten).

**Restliche 8 der 10 Automations-Skills systematisch getestet** (`tests/e2e/prestige-automation-remaining-skills.spec.js`, 16 Tests): Erweiterter Auto-Kauf/Auto-Kauf mehrerer Stufen (Basis 50+je 250 bei Maximalrang, Ende-zu-Ende ueber `bkmpIdleAutoBuyUpgrades()` bewiesen, nicht nur die Formel), Automatische Dungeon-Wiederholung ("Starten"-Knopf wird korrekt zum unbegrenzten Auto-Lauf), Gespeicherte Ausruestungssets (Set speichern/abruesten/wiederherstellen liefert exakt den vorherigen Ausruestungsstand zurueck), Hoehere Kampfgeschwindigkeit (echter Tick-Takt 900ms→692ms bei Rang 10, nicht nur eine Kosmetik-Zahl), Automatische Prestige-Vorschau ("!"-Abzeichen am Tab), Automatische Verteilung ("Empfohlene Verteilung"-Knopf verteilt tatsaechlich Punkte). **Alle 9 getestet und funktionieren korrekt - Schlüsselmeister/Schlüsselbund war der EINZIGE echte Fund unter allen 10 Automations-Knoten.**

**Stabilitaets-Nachpruefung (Nutzerauftrag-Standard dieses Projekts: nicht nur einmal gruen, sondern auch auf Nicht-Reproduzierbarkeit von Fehlschlaegen pruefen):** zwei volle Laeufe desselben 8-Datei-Bloecks (`prestige-tree-v2`/`ascension-and-sinks`/`dungeon-time`/`dungeon-key-prestige-bonus`/`gem-dungeon-reward`/`prestige-automation-remaining-skills`/`prestige-automation-toggles`/`dragon-breeding-automation.spec.js`, alle 3 Projekte, `--workers=1`) zeigten je nur EINEN, unterschiedlichen bzw. gar keinen Fehlschlag - keiner davon reproduzierbar, keiner mit einem heute geaenderten Codepfad verknuepft: (1) `dungeon-time.spec.js` "verbraucht genau einen Schluessel" (chromium-desktop) - in BEIDEN Laeufen aufgetreten, aber 10/10 isoliert sauber bestanden und inhaltlich unabhaengig von jeder heutigen Aenderung (reiner "-1 Schluessel"-Vergleich, keine Interval-/Cap-Logik beteiligt) - bereits bekanntes Flake-Muster dieses Projekts (siehe `prestige.spec.js`/`combat.spec.js`-Praezedenzfaelle). (2) Die eigene "Schluesselbund Rang 20"-Testzeile schlug EINMALIG im ersten Lauf fehl, 20/20 isoliert sauber, im zweiten vollen Lauf sauber bestanden - bestaetigt dieselbe Flake-Klasse, kein echter Bug. (3) `ascension-and-sinks.spec.js` "kauft einen Boost..." (mobile-large) schlug EINMALIG mit einem WebKit-Modal-Oeffnen-Timeout fehl (18s statt ~2s, `qa-fixtures.js`s `openAndLogin`-Helfer, komplett unabhaengig von jeder heutigen Aenderung) - im zweiten Lauf sauber. Fazit: keine der drei Beobachtungen ist eine echte Regression aus der heutigen Arbeit.

Cache-Busting `js/systems/bkmp-prestige.js?v=20260727-keywiringfix1` (alle 3 einbindenden HTML-Dateien) gesetzt.

**Nicht geprüft/offen:** `sql/20260727-fix-dungeon-regen-fixed-slots-and-wire-prestige.sql` muss im Supabase-Dashboard ausgeführt werden, bevor Schlüsselmeister/Schlüsselbund UND die Fixed-Slot-Korrektur live wirken.

## Neue Runen-Hintergrund-Automatik: Legi-Aufwertung/Aufstieg/Verschmelzung (27.-28.07.2026)

Spieler-Bugreport ChronoKora: "Diese Automatische Runen Verschmelzen klappt aber nicht. Bei mir passiert nichts." Erste Untersuchung (Auto-Schmelzen-Button im Runen-Lager) war die falsche Spur — Nutzer klärte: "Nicht der Button der Skill." Gemeint war der Prestige-Knoten "Automatische Runenaufwertung". Ursache gefunden per gezielter Rückfrage statt Vermutung: der Knoten wertet ausschließlich **ausgerüstete** Runen automatisch auf und hat bei komplett auf +15 ausgerüsteten Runen erwartungsgemäß nichts mehr zu tun — **kein Bug**, exakt wie gebaut. Nutzer bestätigt: "Ja sind schon +15. Braucht ja auch nicht lange die auf +15 zu machen."

Daraus direkter Nutzerwunsch: "eine andere Variante automatische +15 upgrade Funktion von Legis und die Automatischer Aufstieg der legis dann mit einen On/Off Button im Runen System Seite. + Auto verschmelzung" — drei neue Hintergrund-Automatiken statt eines Bugfixes. Vor der Umsetzung zwei Design-Entscheidungen per Rückfrage geklärt: (1) Bestätigung **einmalig beim Einschalten** (nicht pro Lauf — ein Hintergrund-Prozess kann nicht alle 10s einen Dialog aufreißen, aber Verschmelzen kann Runen zerstören und Aufstieg verbraucht immer unwiderruflich eine zweite Legendäre), (2) Schalter **an den bestehenden Prestige-Knoten "Automatische Runenaufwertung" gekoppelt** (nicht kostenlos) — hält die bestehende Investition relevant.

**Architektur:** alle drei Automatiken rufen ausschließlich bereits bestehende, längst manuell getestete Aktionen auf — keine neue Spiellogik:
- **Auto-Legi-Aufwertung** (`bkmpRuneRunBackgroundLegiUpgrade`, neu): wertet die günstigste bezahlbare **legendäre** Rune unter +15 auf, per `bkmpRuneUpgrade()` — bewusst OHNE Ausrüstungs-Einschränkung (anders als der Prestige-Knoten selbst), ergänzt ihn statt ihn zu ersetzen.
- **Auto-Aufstieg** (`bkmpRuneRunBackgroundAscend`, neu): nutzt `bkmpRuneAutoAscendPairs()` (bereits bestehend, findet alle passenden +15-Paare gleichen Slots/gleicher Stufe über ALLE 6 Rüstungsplätze in einem Aufruf) + neu extrahiertes `bkmpRuneExecuteAscendPairs()` (aus dem bestehenden manuellen "Auto-Aufstieg"-Button `bkmpRuneAutoAscendAll` herausgelöst, damit Button UND Hintergrund-Automatik exakt dieselbe Verbrauchslogik teilen, keine zweite Kopie).
- **Auto-Verschmelzung** (`bkmpRuneRunBackgroundFuse`, neu): verarbeitet EINEN Rüstungsplatz pro Automations-Durchlauf (rotierender Cursor über alle 6, vermeidet 6 gleichzeitige Serveraufrufe pro Tick) über neu extrahierte `bkmpRuneFindFuseGroupsForSlot()`/`bkmpRuneExecuteFuseGroups()` (aus dem bestehenden manuellen "Auto-Schmelzen"-Button `bkmpRuneAutoFuseAll` herausgelöst, identisches Prinzip).

Alle drei laufen über `bkmpIdleRunAutomationToggles()` (idledorf.js, derselbe 10s-Takt wie die übrigen Prestige-Automatiken), gated durch `bkmpRuneBackgroundAutomationUnlocked()` (= Prestige-Knoten gekauft) UND den jeweiligen eigenen lokalen Schalter (`localStorage`, identisches Prinzip wie das bereits bestehende `BKMP_RUNE_FUSE_AUTOCONFIRM_KEY`). Checkbox-Klick auf "Einschalten" zeigt einmalig `bkmpConfirmDialog()` (Risikohinweis, unterschiedlicher Text je Automatik) — bei Abbruch bleibt der Schalter aus, bei Bestätigung läuft es lautlos weiter bis der Spieler wieder ausschaltet; Ausschalten fragt nie nach.

**UI:** neue Zeile `.idle-runen-bgauto-row` (3 Checkboxen, Label-Pattern wiederverwendet von `.idle-autobuy-toggle`) im Kopf des Runen-Tabs (`bkmpIdleRenderRunenPanel()`, global — nicht slot-tab-abhängig, da die Hintergrund-Automatik ohnehin alle 6 Plätze abdeckt), nur sichtbar wenn der Prestige-Knoten gekauft ist.

**Verifiziert:** 6 neue Tests (`tests/e2e/rune-background-automation.spec.js`) — Schalter-Sichtbarkeit an Prestige-Kauf gekoppelt, Abbrechen beim Einschalten lässt Schalter aus (localStorage unverändert), Ausschalten fragt nachweislich NICHT nach, Auto-Legi-Aufwertung wertet eine unausgerüstete Legendäre auf (Gold-Abzug als deterministischer Beweis, gleiches Prinzip wie beim bestehenden Prestige-Knoten-Test — Aufwertung hat eine Fehlschlagchance, Gold-Abzug nicht), Auto-Aufstieg verbraucht echt eine zweite +15-Legendäre und lässt die andere auf +16 steigen, Auto-Verschmelzung verarbeitet nachweislich einen Slot, der NICHT der gerade offene Tab ist (Beweis, dass der Hintergrund-Lauf nicht vom UI-Zustand abhängt) — Invariante bewusst "3 Quell-Runen weg" statt "Erfolg", da Verschmelzen eine echte (kleine) Fehlschlagchance hat. 60/60 bei 10x Wiederholung auf `chromium-desktop` stabil, korrekt auf allen 3 Projekten (Mobile-Skip-Guard, da echte Desktop-Tab-Klicks). Regressionslauf der bestehenden Runen-Suiten (`runes.spec.js`, `rune-autofuse-datasource.spec.js`, `rune-persistence-hardening.spec.js`, `rune-inventory-scale.spec.js`) bestätigt: die Extraktion der gemeinsamen Helfer hat die bestehenden manuellen Buttons nicht verändert (46 bestanden, 38 korrekt übersprungen, 0 Fehlschläge). `node --check`/`eslint` sauber.

Cache-Busting `js/systems/bkmp-runes.js?v=20260728-runeautofix1`, `idledorf.js?v=20260728-runeautofix1`, `style.css?v=20260728-runeautofix1` gesetzt (alle einbindenden HTML-Dateien).

## Investor-Auszahlung dokumentieren: Beweisfoto + "Ausgezahlt"-Status (28.07.2026) — Code fertig, SQL noch nicht ausgeführt

Anlass: erster Investor (CrazyMcNugget, 50.000.000€ investiert, 9% Beteiligung, Zeitraum 28.05.-28.07.2026) wird real ausgezahlt. Nutzerwunsch: "brauche irgendwas wie ich sage mal das auf der Website absichere? Will dann ein Bild einfügen können was als kleines viereckt rechts nebendem namen auftauchen tut was man vergrößern kann wo man die auszahlung sieht. Und vllt irgendwas im Admin Panel mit Investor erledigt.. ausgezahlt?"

**Ausgangslage (per Recherche bestätigt):** `investors`-Tabelle (getrennt von `investor_requests`, das ist nur die Bewerbung) hat bereits Name/Investiert/Beteiligung/Zeitraum/Anonym-Flag, keinen Auszahlungs-Status. Bild-Uploads laufen im ganzen Projekt bereits über einen gemeinsamen Mechanismus (`bkmpStoreImageIfNeeded()`, supabase.js) — derselbe `update-images`-Storage-Bucket wie News/Wünsche/PartnerShops, nur mit anderem Ordner-Präfix. Kein Lightbox/Bild-Vergrößern-Baustein existierte bisher irgendwo im Projekt — neu gebaut, aber nach dem bereits etablierten `.joke-overlay`/`.joke-card`-Muster (identisch zu `#bkmpConfirmOverlay` etc.), keine neue Komponentenart.

**Umsetzung:**
- **`sql/20260728-investor-payout-proof.sql`** (neu, **noch nicht ausgeführt**): zwei additive Spalten auf der bestehenden `investors`-Tabelle — `paid_out boolean` (reiner Status-Haken) + `payout_proof_url text` (Beweisfoto-URL). Beide unabhängig nutzbar (Status ohne Foto, oder umgekehrt).
- **`supabase.js`:** `loadInvestors()`/`saveInvestor()`/die beiden Mapper-Funktionen um die zwei neuen Felder erweitert — defensiv ausgewertet (`Boolean(...)`/`|| ''`), damit ein noch nicht migriertes Schema keinen Fehler wirft, die Felder bleiben dann einfach leer.
- **`admin.html`** (Investoren-Formular): neue Checkbox "✅ Ausgezahlt" + Datei-Upload fürs Beweisfoto (`readFileAsDataUrl()` + `bkmpStoreImageIfNeeded(..., 'investor-payouts')` — beide bereits bestehende Bausteine, keine neue Upload-Logik). Eine neue Datei zu waehlen ist optional beim Bearbeiten — ohne Auswahl bleibt ein bereits gespeichertes Foto unangetastet (`editingInvestorPayoutProofUrl` merkt sich die bestehende URL separat, da ein `<input type="file">` sich nicht vorbelegen lässt). Investoren-Liste zeigt jetzt "✅ Ausgezahlt"/"📷 Foto"-Badges (wiederverwendet `.investor-anonymous-badge`, nur mit grünem Akzent für den Ausgezahlt-Fall).
- **`js/core/bkmp-site.js`/`index.html`** (öffentliche Investoren-Karte): neues kleines, anklickbares Quadrat-Vorschaubild rechts in der Kopfzeile jeder Investor-Karte (nur sichtbar, wenn ein Foto gesetzt ist) — Klick öffnet `#investorPayoutViewerOverlay` (neu, `.joke-overlay`-Muster) mit dem Foto in voller Größe + Investorname im Titel. Delegierter Klick-Listener bewusst EINMALIG außerhalb von `renderFinancialSections()` verkabelt (identisches, bereits dokumentiertes Muster wie der Donut-Chart-Easter-Egg direkt darunter — `#investorGrid` wird bei jedem Live-Update nur per innerHTML neu befüllt, nie ersetzt; ein Listener innerhalb der Render-Funktion hätte sich bei jedem Realtime-Update verdoppelt).

**Verifiziert (Browser, lokaler Static-Server, kein echter Supabase-Login nötig für die reine UI-Logik):** öffentliche Karte — Vorschaubild rendert korrekt mit `data-full`/Investorname, Klick öffnet die Vergrößerung mit korrektem Bild+Titel, Schließen-Button UND Klick auf den Hintergrund schließen beide korrekt. Admin — Formular-Felder vorhanden, `startEditInvestor()` befüllt Checkbox+Hinweistext korrekt aus einem bestehenden Investor, `clearInvestorForm()` setzt beides korrekt zurück (inkl. Datei-Input geleert), `renderInvestorList()` zeigt beide neuen Badges korrekt. `node --check`/`eslint` auf `supabase.js`/`js/core/bkmp-site.js` sauber, extrahierter Syntax-Check für beide betroffenen HTML-Dateien sauber. Bestehende idle-Dorf-QA-Suite unbeeinflusst (Smoke-Test grün) — diese Änderung betrifft ausschließlich die Marketing-/Investoren-Seite, die (wie architektonisch dokumentiert) außerhalb der Playwright-Testsuite liegt.

Cache-Busting `supabase.js?v=20260728-investorpayout1` (3 HTML-Dateien), `js/core/bkmp-site.js?v=20260728-investorpayout1` (index.html), `style.css?v=20260728-investorpayout1` (5 HTML-Dateien) gesetzt.

**Nicht geprüft/offen:** der eigentliche Datei-Upload gegen den echten Supabase-Storage-Bucket (lief nur gegen die bereits an anderer Stelle bewährte, unveränderte `bkmpStoreImageIfNeeded()`-Funktion selbst, nicht end-to-end mit echten Zugangsdaten). **Vor Nutzung: `sql/20260728-investor-payout-proof.sql` im Supabase-Dashboard ausführen**, danach im Admin-Panel bei CrazyMcNugget "Ausgezahlt" ankreuzen + Beweisfoto hochladen.

**Nachtrag, selbe Sitzung (Nutzer zeigte die live laufende Karte per Screenshot, danach Folgewunsch): "Irgendwas für Website?... war Investor von ?? bis ?? und hat in dem zeitraum ????€ Anteil verdient mit einer Investierungs Summe von ????€ und einer Gewinnbeteilgung ??%"** — sobald `paidOut` gesetzt ist, ersetzt die Karte die auf laufenden Zahlen wirkende "Aktueller Anteil"-Kachel + das Metriken-Grid durch genau diesen in Vergangenheitsform formulierten Zusammenfassungssatz (`js/core/bkmp-site.js`) — derselbe bereits bestehende `payout`-Wert, nur anders präsentiert, keine neue Berechnung. Neue Klasse `.investor-card-concluded`/`.investor-concluded-summary` (`style.css`, grüner statt neutraler/goldener Akzent, signalisiert "abgeschlossen"). Per Browser mit den echten Zahlen aus dem Screenshot verifiziert (28. Mai 2026 bis 28. Juli 2026, 50.000.000€, 9%, 12.734.290€) — Satztext exakt wie erwartet zusammengesetzt, Karte wechselt korrekt zwischen beiden Darstellungen je nach `paidOut`. Cache-Busting `bkmp-site.js?v=20260728-investorpayout2`/`style.css?v=20260728-investorpayout2` nachgezogen.

## Paragon-Verständlichkeit + Investor-Vorschaubild-Größe (28.07.2026)

Zwei kleine, getrennte Nutzerwünsche direkt im Anschluss an die obigen Investor-Payout-Arbeiten. **Nichts committed/gepusht durch diese Session bewusst ausgelöst.**

**Paragon-Erklärung** (Nutzerfrage "Was genau war nochmal paragon? Wie kriegt man das?", danach "Ne Idee das wir das irgendwie anders machen? Extra währung oder so? ... Spieler die das nachfragen"): statt einer zweiten Währung (deutlich größerer Umbau für ein reines Verständnisproblem) ein kleiner "?"-Info-Tooltip direkt an jeder Paragon-Zeile im Prestige-Baum (`js/systems/bkmp-prestige.js`, `bkmpUiTooltipHtml`/`bkmpUiWireTooltipTrigger`, Phase 3, erster echter Einsatz) + ein einmaliger Hinweis-Toast (`bkmpUiShowToast`, ebenfalls erster Einsatz) beim tatsächlichen Erreichen des Meilensteins "Jenseits der Grenze" (350 Punkte), statt dass Paragon kommentarlos erscheint. Milestone-Beschreibungstext selbst ebenfalls ausführlicher. Live per direktem Funktionsaufruf verifiziert (Tooltip-Text/Toast-Text korrekt) - echter Screenshot des Vollbild-Overlays technisch nicht möglich (bekannte Werkzeug-Einschränkung).

**Investor-Vorschaubild** (Nutzer-Screenshots: erst "Screenshot bild ist noch nicht länglich", dann "Noch viel breiter ruhig fast ran an denn Namen"): `.investor-payout-thumb` von festen 44×44px über 78×40px auf ein FLEXIBLES `flex:1 1 140px; min-width:90px; max-width:260px` gewachsen - füllt jetzt den freien Platz in der Kopfzeile bis fast an den Namen heran, bleibt aber auf schmalen Karten durch `min-width` nutzbar. Vergrößerungsfenster (`#investorPayoutViewerOverlay`) von 720px auf `min(1100px, 96vw)` verbreitert, Bild per `object-fit:contain` passend eingepasst - Querformat-Screenshots (der übliche Fall bei Chat-Beweisfotos) füllen das Fenster jetzt richtig aus statt gequetscht in einer schmalen Karte zu hängen.

Cache-Busting `js/systems/bkmp-prestige.js?v=20260728-paragonclarity1`, `style.css?v=20260728-paragonclarity1` (Paragon) bzw. `style.css?v=20260728-investorpayout3/4` (Vorschaubild, zwei Nachbesserungen).

## Dringender Fix: Arena-Tageslimit-Anzeige + Runenverlust-Diagnose (29./30.07.2026)

Zwei Spieler-Reports von BagonTr01 (Feedback-Board), beide direkt untersucht. **Nichts committed/gepusht durch diese Session bewusst ausgelöst.**

**Bug 1 (bestätigt+gefixt) — "Ich habe noch 10 Areana ngriffe übrig und ich bekomme die meldung das ich keine mehr habe":** `bkmpIdleRenderArenaPanel()` schätzte `attacksToday` bisher aus den geladenen `bkmpArenaRecentBattles` (fest auf 15 Einträge gedeckelt, Kommentar dort: "reicht, weil ein Tageslimit von 10 locker in 15 Kämpfen passt"). Seit Kriegsrat (Gilden-Technologie v2, 26.07.) kann das echte, serverseitige Tageslimit aber über 15 liegen (`10 + Kriegsrat-Stufe`) - sobald ein Spieler an einem Tag öfter als 15x angreift, fallen die ältesten Angriffe des Tages aus dem 15er-Fenster, `attacksToday` blieb dadurch dauerhaft bei 15 hängen und die Anzeige zeigte fälschlich noch Angriffe übrig, obwohl der Server (der die echte Anzahl zählt) längst korrekt blockierte. Fix: neue `bkmpArenaGetAttacksTodayCount()` (supabase.js) - echte, ungedeckelte Abfrage seit Berlin-Tagesbeginn statt einer Schätzung aus der Anzeige-Liste, bewusst `.select('id')` + `.length` statt `count:'exact', head:true` (letzteres ein im gesamten Projekt bisher nie genutztes, ungetestetes Muster). **Dabei einen zweiten, unabhängigen Bug in der eigenen Test-Mock-Infrastruktur gefunden+gefixt** (kein App-Bug): `tests/mock/rest-engine.js`s `gt`/`gte`/`lt`/`lte`-Filter nutzten reine `Number()`-Umwandlung - bei ISO-Datums-Strings immer `NaN`, wodurch JEDER Zeitraum-Filter auf eine Zeitstempel-Spalte im Mock bisher lautlos nichts traf (betraf latent auch eine bereits bestehende, aber offenbar nie getestete Update-Dedup-Abfrage in supabase.js). Fix: `compareForRange()` versucht erst Zahlen-, dann Datums-Vergleich. Neuer Regressionstest (`tests/e2e/guild-tech-ext.spec.js`, Kriegsrat auf Stufe 10 = Limit 20, 18 echte Angriffe, beweist sowohl dass die alte 15er-Liste tatsächlich zu kurz gewesen wäre als auch dass die neue Zählung korrekt 18 zeigt) - 10/10 Wiederholungen + alle 3 Projekte grün, volle bestehende Arena-/Gilden-Tech-Suite (84 Tests) unverändert grün.

**Bug 2 (untersucht, nicht bestätigt) — "Mal wieder eine Rune nach dem Prestige weggebauggt :(":** `bkmpPrestigeExecuteReset()` fasst Runen an keiner Stelle an (Entscheidung vom 18.07. "Runen überleben Prestige vollständig" ist im Code unverändert wirksam, siehe Kommentar dort) - kein Code-Pfad verbindet einen Prestige-Reset ursächlich mit Runenverlust. Einzig plausible Erklärung: die am 27./28.07. neu gebauten Hintergrund-Automatiken "Auto-Aufstieg"/"Auto-Verschmelzung" (verbrauchen bewusst Runen, aber nur nach explizitem Einschalten + einmaliger Bestätigung durch den Spieler) - Gating-Code (`bkmpRuneBackgroundAutoGet()`, reine localStorage-Flags, Default aus) noch einmal gezielt durchgesehen, kein Bug gefunden, der eine Automatik OHNE Einschalten auslösen könnte. Da diese Schalter rein lokal (Browser) gespeichert sind, kann SQL allein nicht zeigen, ob BagonTr01 sie aktiviert hat - rein lesende Diagnose vorbereitet (`sql/20260729-diagnose-bagontr01-rune-loss-post-prestige.sql`, komplette aktuelle Runenliste + Prestige-Historie), aber **kein Fix vorgenommen, da kein Bug bestätigt werden konnte** (Projekt-Grundsatz: nur bestätigte Fehler reparieren).

Cache-Busting `supabase.js?v=20260730-arenalimitfix1`, `js/systems/bkmp-arena.js?v=20260730-arenalimitfix1` (alle 3 einbindenden HTML-Dateien).

## Feedback-Board-Runde: 4 Spieler-Ideen umgesetzt (30.07.2026)

Vier unabhängige Feedback-Board-Einträge geprüft, Nutzer-Entscheidung nach kurzer Einschätzung jedes Punkts: **"Ja alles machen."** **Nichts committed/gepusht durch diese Session bewusst ausgelöst.**

**1. Rune-"Einsetzen"-Button vom Lager verdeckt (Spieler-Idee OPShadowWolf):** kein neuer Bug, sondern ein Wiederauftreten des bereits in Phase 7.2 dokumentierten, damals bewusst nicht vollständig behobenen "Kraftrune-Lager-Überlapp" (siehe dortiger Abschnitt "horizontal... volle Behebung bleibt offen"). Root Cause: `bkmpRuneSyncDrawerPosition()` (`js/systems/bkmp-runes.js`) maß bisher `drawer.offsetWidth` unmittelbar nach dem Umschalten der `.open`-Klasse - während der laufenden CSS-Transition liefert das einen veralteten (Vor-Öffnen-)Wert zurück, wodurch die reservierte Überlapp-Fläche auf normalen Desktop-Breiten praktisch 0 blieb und der Balken den "Einsetzen"-Knopf weiterhin verdeckte. Fix: die tatsächliche Zielbreite wird jetzt aus der bekannten CSS-Formel abgeleitet (`min(320px,84vw)`/`78vw` je nach Breite, exakt wie in style.css definiert) statt per Layout-Messung während der Animation - `.idle-runen-main-row` bekommt darüber eine live berechnete CSS-Variable (`--rune-drawer-overlap`) als `padding-right`, sodass der Inhalt (inkl. "Einsetzen"-Button) bei geöffnetem Lager automatisch ausweicht, unabhängig von der genauen Fensterbreite. Bewiesen per `getBoundingClientRect()`: 258px Überlappung ohne Fix bei 1280×720, 0px mit Fix; bei 1920px (wo vorher kein Überlapp bestand) bleibt der reservierte Abstand korrekt 0px (kein unnötiger Leerraum). Alter Test-Workaround in `save-load.spec.js` (Balken vor dem Klick manuell zuklappen) entfernt - der Klick funktioniert jetzt auch bei offenem Balken direkt. Neuer Regressionstest in `runes.spec.js` beweist zusätzlich echte Klick-Erreichbarkeit (`click({trial:true})`, nicht nur Geometrie).

**2. Auto-Kauf-Ressourcen-Ausschluss (Spieler-Idee Fightmaria):** "damit er nicht alles kauft, während man gerade auf etwas spart". Neue `bkmpIdleGetAutoBuyExcludedResources()`/`bkmpIdleSetAutoBuyResourceExcluded()` (idledorf.js, reines `localStorage`, Default: nichts ausgeschlossen) - `bkmpIdleAutoBuyUpgrades()` filtert die zu prüfenden Upgrades jetzt zuerst nach ausgeschlossenen Ressourcentypen. Checkbox-Reihe (`.idle-autobuy-resource-filter`, Chip-Optik) direkt unter dem bestehenden Auto-Kauf-Schalter im Upgrades-Tab, eine Chip je Ressource (Gold/Holz/Stein/Kristalle/Essenz).

**3. Prestige-"Automatische Verteilung" mit Zweig-Priorität (Spieler-Idee Kaledoss):** "da der Baum jetzt mit den neuen Abläufen [52 statt 6 Knoten, siehe Progression-Rebalance oben] zu repetitiv werden könnte". Neue `bkmpPrestigeGetAutoAllocatePriority()`/`bkmpPrestigeSetAutoAllocatePriority()`/`bkmpPrestigeMoveBranchPriority()` (js/systems/bkmp-prestige.js, `localStorage`) - `bkmpPrestigeAutoAllocate()`s Sortierung vergleicht jetzt zuerst die Zweig-Priorität, erst bei Gleichstand den Preis (vorher: nur Preis). Neue nummerierte Liste mit ▲▼-Knöpfen im Prestige-Panel (nur sichtbar, wenn der zugehörige Automatisierungs-Knoten gekauft ist). Getestet: der priorisierte Zweig bekommt über mehrere Kauf-Runden hinweg nachweislich deutlich mehr Ränge als die anderen (nicht exakt 0 für die übrigen - ein kleiner Rest-"Spillover" ist beabsichtigtes Verhalten, sobald der priorisierte Zweig gerade keine bezahlbare Option mehr hat) + die Auf/Ab-Knöpfe sortieren die Liste tatsächlich um.

**4. Clan-Bestenliste + Clan-Arena (Spieler-Idee Kaledoss):** "Reiter für Clanbestenliste und ne Art Clanarena-Kämpfe". Neuer Tab "🏆 Clan" (index.html + idle-stream-mini.html, zwischen Gildenboss und Bestenliste einsortiert).
- **Clan-Bestenliste** brauchte KEINE neue SQL - `guilds.guild_xp` ist bereits vorhanden und öffentlich lesbar (`bkmpGuildGetLeaderboard()`, sortiert absteigend, nutzt das bereits bestehende `bkmpGuildLevelInfo()` für die Level-Anzeige).
- **Clan-Arena** ist ein direkter, architektonisch fast identischer Port der bereits bewährten Spieler-Arena (`arena_attack()`) auf Gilden-Ebene: `guild_arena_attack()` (neue RPC, `sql/20260730-guild-arena.sql`, **noch nicht ausgeführt**) - Gilde-Macht = Summe aus `greatest(1, attack*2+defense+hp*0.3)` über ALLE aktuellen Mitglieder beider Gilden (identisches Prinzip wie die bereits bestehende Raid/Gildenboss-Kraft-Aggregation), gleiche ELO-Formel (K=32) wie beim Spieler-Arena, aber bewusst enger limitiert (3 Angriffe/Tag statt 10, 30 Min. statt 3 Min. Cooldown pro Ziel - "betrifft potenziell viele Mitglieder gleichzeitig"). Nur Anführer/Stellvertreter dürfen angreifen (`insufficient_role` sonst). Sieg zahlt Gold in die **Gildenkasse** (`treasury_gold`), bewusst NICHT in `guild_xp` (kein zweiter, unbeabsichtigter Level-Hebel neben dem bereits bestehenden Kassenstand-Bonus-System). Neue Tabellen `guild_ratings`/`guild_battle_log` (öffentlich lesbar, nur per RPC beschreibbar). Client: 6 neue `supabase.js`-Funktionen, `bkmpClanLoadAll()`/`bkmpIdleRenderClanPanel()`/`bkmpClanRenderLeaderboardHtml()` (js/systems/bkmp-guild.js) - zeigt bei fehlender Gildenmitgliedschaft nur einen Beitritts-Hinweis + die Bestenliste, sonst volles Arena-UI (Rating/Siege-Niederlagen, Tageslimit-Zähler, Gegner-Karten, Kampf-Log). Bewusst KEINE Gewinnchance-Schätzung pro Gegner-Gilde (anders als bei der Spieler-Arena) - würde eine N+1-Abfrage aller Mitgliederwerte JEDER angezeigten Gegner-Gilde brauchen, als bewusste Scope-Entscheidung im Code dokumentiert.
- **Dritter Mock-Fidelity-Bug gefunden+gefixt** (nicht die App - gleiche Bugklasse wie die bereits zuvor in dieser Session gefundenen `gte`/`lt`-Datums-Vergleichslücke): `tests/mock/rest-engine.js` werte `.or(...)`-Filter (PostgREST-Gruppen-OR-Syntax) nie tatsächlich aus - `or` fehlte in `RESERVED_PARAMS`, wurde also als literaler Spaltenfilter auf ein nicht existierendes `row['or']`-Feld behandelt, dessen verstümmelter Operator im `switch` von `matchFilter()` auf den `default: return true`-Zweig fiel - **jede** `.or()`-Abfrage traf seit Bestehen des Mocks lautlos auf ALLE Zeilen. Betraf latent auch die bereits bestehende `bkmpArenaGetRecentBattles()` (nie aufgefallen, da bisherige Tests nie fremde Kämpfe im selben Store hatten). Fix: echte `parseOrConditions()` + Gruppen-Auswertung. Nach dem Fix wurde zur Sicherheit die volle 3-Projekte-Regressionssuite erneut durchlaufen, um auszuschließen, dass irgendein bestehender Test unbemerkt vom alten (immer-wahr) Verhalten abhing.
- **Tests:** `tests/e2e/guild-clan-arena.spec.js` (neu, 13 Tests: RPC-Ebene - Sieg+Kassengutschrift, Niederlage ohne Kassenverlust, Offizier darf angreifen/normales Mitglied nicht, Selbstangriff/gildenlos schlagen fehl, Tageslimit 3, Cooldown 30 Min. inkl. Ablauf, exakte ELO-Formel bei Gleichstand; Bestenliste-Sortierung; UI - Bestenliste+Beitritts-Hinweis ohne Gilde, Arena-UI mit Gilde, Angriffsknopf für normales Mitglied deaktiviert, Anführer kann echt angreifen) - 33/39 über alle 3 Projekte (6 korrekt übersprungen, UI-Tests auf Mobile via Skip-Guard).

**Verifiziert (gemeinsam für alle 4 Punkte):** volle 3-Projekte-Regressionssuite (`chromium-desktop`/`mobile-small`/`mobile-large`, `--workers=1`, `--retries=0`) nach allen Änderungen durchgelaufen, keine unerwarteten Fehlschläge außerhalb des bereits bekannten Zwei-Tab-Speicher-Race (`test.fail()`, unverändert). `node --check`/`eslint` auf allen geänderten `.js`-Dateien sauber.

Cache-Busting `?v=20260730-clanarena1` (supabase.js/js/systems/bkmp-guild.js/idledorf.js/style.css, alle einbindenden HTML-Dateien), `?v=20260730-runeequipfix1` (js/systems/bkmp-runes.js), `?v=20260730-autokaufideas1` (js/systems/bkmp-prestige.js).

**Nicht geprüft/offen:** `sql/20260730-guild-arena.sql` muss im Supabase-Dashboard ausgeführt werden, bevor Clan-Arena live funktioniert (Clan-Bestenliste funktioniert bereits ohne diese Migration, da sie nur bestehende Spalten liest).

## sw.js-Robustheit + Prestige-Panel-Umbau (30.07.2026)

Zwei kleine, unabhängige Nachbesserungen direkt im Anschluss an die Feedback-Board-Runde. **Nichts committed/gepusht.**

**sw.js:** Live-Screenshot zeigte "Uncaught (in promise) TypeError: Failed to fetch" + ein fehlgeschlagenes FetchEvent für `https://bkinvestment.de/` in der Konsole - derselbe Fehlerabdruck ("sw.js: Failed to fetch") wie beim Runenverlust-Fund vom 25.07., diesmal aber für die Root-Dokument-Anfrage selbst statt eine Supabase-REST-Anfrage. Der reine Passthrough-Service-Worker (`fetch(event.request)` ohne jede Fehlerbehandlung) wandelte jeden vorübergehenden Netzwerk-Haenger/Abbruch (z.B. Reload mitten in einer laufenden Anfrage) in einen unbehandelten Promise-Reject um - der Browser haette denselben Haenger ohne Service Worker intern still behandelt. Fix: `.catch(() => Response.error())` liefert stattdessen eine normale Netzwerkfehler-Response, fuer den Browser ununterscheidbar davon, als haette gar kein Service Worker dazwischengehangen. Verifiziert: `node --check` sauber, normaler lokaler Seitenaufruf ohne neue Konsolenfehler (der eigentliche `.catch()`-Pfad selbst laesst sich ohne echten Netzwerkausfall nicht auslösen, ist aber eine triviale, risikoarme Aenderung).

**Prestige-Panel-Umbau (Spieler-Beschwerde, Screenshot):** "kann nicht sein das ich erstmal 30 Meter runterscrollen muss für Prestige-Punkte-Vergabe". `bkmpIdleRenderPrestigePanel()` (`js/systems/bkmp-prestige.js`) rendert bisher acht reine Info-Bloecke ("Du erhaeltst"/"Wird zurückgesetzt"&"Bleibt erhalten"/"Naechster Durchlauf"/Meilensteine/ggf. Aufstieg/ggf. Auto-Kauf-Prioritaet) VOR dem eigentlichen Baum zum Punkte-Ausgeben (Zweig-Tabs + Grid). Fix: der Baum steht jetzt direkt unter dem Fortschrittsbalken/"Jetzt aufsteigen"-Button; die vier reinen Erklaer-Bloecke ("Du erhaeltst"/Reset-Keep-Spalten/Naechster Durchlauf/Meilensteine) wandern in ein neues, standardmaessig EINGEKLAPPTES Akkordeon "ℹ️ Was passiert beim Aufstieg?" darunter (`bkmpPrestigeRenderInfoAccordionHtml()`, Zustand pro Browser in `localStorage` gemerkt). Aufstieg/Drachenseelen-Sektion und Auto-Kauf-Prioritaet bleiben unveraendert sichtbar (echte Aktionen, keine reine Info) und stehen weiterhin unter dem Akkordeon, in derselben relativen Reihenfolge wie vorher. Kein Sicherheitsverlust vor der eigentlich unumkehrbaren Aktion: der bereits bestehende zweistufige Bestaetigungsdialog (`bkmpPrestigeOpenConfirmFlow`) zeigt dieselbe Reset/Bleibt-erhalten-Liste ohnehin ein zweites Mal, bevor der Aufstieg tatsaechlich ausgefuehrt wird - das war schon vor diesem Umbau so. Verifiziert: alle 37 bestehenden Prestige-/Aufstiegs-/Automatisierungs-Tests (`prestige.spec.js`, `prestige-tree-v2.spec.js`, `prestige-automation-remaining-skills.spec.js`, `ascension-and-sinks.spec.js`) grün, kein Selektor haengt an der alten DOM-Reihenfolge. Zusaetzlich per injiziertem Mock-Zustand ueber die ECHTE Render-Funktion bestaetigt (`bkmpIdleRenderPrestigePanel()` direkt aufgerufen, kein echter Login moeglich - Sicherheitsregel): Panel-Kindreihenfolge ist jetzt Zusammenfassung → Fortschrittsbalken → Baum (Zweig-Tabs + Grid) → Akkordeon (`hidden` per Default bestaetigt) → Aufstieg-Sektion; Klick auf den Akkordeon-Titel klappt korrekt auf (`aria-expanded` wechselt, Reset/Keep/Meilensteine-Inhalt erscheint) und per Screenshot bestaetigt (Baum direkt sichtbar ohne Scrollen).

Cache-Busting `?v=20260730-prestigelayout1` für `js/systems/bkmp-prestige.js` (index.html/admin.html/idle-stream-mini.html) und `style.css` (alle 5 HTML-Dateien).

## Runen-Sammel-Aufwertung "Auf +15 maximieren" (30.07.2026)

Spieler-Idee BagonTr01 (Feedback-Board): "mehrere Legendäre Runen gleichzeitig auf +15 maximieren .. wenn man mal einen Tag afk war ... hat man teilweise ne Stunde [zum Durchklicken]". Vorschlag mit dem Nutzer abgestimmt (Rückfrage + "ja bitte"), dann umgesetzt. **Nichts committed/gepusht.**

**Umsetzung (`js/systems/bkmp-runes.js`):** neue `bkmpRuneMaximizeLegendarySlot()` - ruft für jede unausgerüstete Legendäre (Rarität `gold`) des aktuell offenen Rüstungsplatzes die bereits bestehende, produktiv genutzte `bkmpRuneInstantUpgrade()` auf (keine zweite Kopie der Kosten-/Fehlschlag-/Substat-Formeln - identische Kosten/Fehlschlagchance wie beim manuellen Klicken, nur ohne wiederholtes Klicken). Fetcht bewusst frisch vom Server (`bkmpGetStoredMeltableRunes`, gleiches Prinzip wie Auto-Schmelzen/Lager-aufräumen, siehe Bugfix-Bericht vom 25.07.) statt aus dem potenziell auf 300 Zeilen gekappten lokalen Bestand - sonst könnte der Button bei einem großen Lager genau die Runen übersehen, die der Spieler eigentlich meint. Ergänzt (ersetzt nicht) die bereits bestehende Prestige-gated Hintergrund-Automatik "Auto-Legi-Aufwertung" (verarbeitet nur eine Rune pro 10s-Tick) - dieser Button ist unabhängig vom Prestige-Knoten nutzbar und erledigt dieselbe Aktion sofort in einem Rutsch.

**UI:** neuer Button "⚡ Auf +15 maximieren (N)" in der bereits bestehenden Header-Actions-Reihe des Runen-Lagers, neben "Auto-Schmelzen"/"Auto-Aufstieg"/"Alle verkaufen". `N` ist (wie bei den beiden Nachbar-Zählern) ein lokaler, synchroner Schätzwert - der echte Klick holt trotzdem frisch vom Server nach; bei einem sehr großen Lager kann der Zähler dadurch "(0)"/deaktiviert zeigen, obwohl mehr echte Kandidaten existieren (identisches, bereits akzeptiertes Verhalten wie bei Auto-Schmelzen/Auto-Aufstieg - kein neues Problem).

**Tests (`tests/e2e/rune-maximize-legendary.spec.js`, neu, 5 Tests):** korrekte Zähler-Anzeige (ausgerüstete + bereits maximale Runen zählen nicht mit), Klick maximiert alle passenden Runen auf +15, ausgerüstete Legendäre bleibt unangetastet, kein Kandidat → Button "(0)"/deaktiviert ohne Serveroperation, Regressionsbeweis für den 300er-Cache-Bypass (Teststand E, 305 hochstufige Konkurrenz-Runen drängen die beiden echten Test-Kandidaten garantiert aus dem lokalen Cache - Funktion trotzdem direkt aufgerufen statt über den dadurch lokal deaktivierten Button, identisches Testmuster wie `rune-autofuse-datasource.spec.js`). Alle 5 grün auf `chromium-desktop`, korrekt übersprungen auf Mobile (Desktop-Tab-Navigation). Regressionslauf (`runes.spec.js`, `rune-autofuse-datasource.spec.js`, `rune-persistence-hardening.spec.js`, `rune-inventory-scale.spec.js`, `rune-background-automation.spec.js`, `save-load.spec.js`, alle 3 Projekte): 56 bestanden, 60 korrekt übersprungen, 1 unerwarteter Erfolg beim bereits lange bekannten, absichtlich per `test.fail()` verfolgten Zwei-Tab-Speicher-Race (Phase 7.2, unabhängig von dieser Änderung, keine neue Regression). `eslint`/`node --check` sauber.

Cache-Busting `?v=20260730-legimaximize1` für `js/systems/bkmp-runes.js` (index.html/admin.html/idle-stream-mini.html) und `style.css` (alle 5 HTML-Dateien).

## Clan-Arena-Kampfanimation (30.07.2026)

Direkte Nachbesserung nach Spieler-Nachfrage (Screenshot des ersten echten Clan-Arena-Angriffs): "Hatte kein Angriffs Animation? Ist das Fenster vllt hinter dem Hauptfenster? Oder kommt da garnichts?". Kein Bug - der Angriff selbst funktionierte (Kampf-Log zeigte Sieg+Rating+Kasse korrekt) - aber die Clan-Arena hatte bewusst keine Kampfanimation, während die schon länger bestehende Spieler-Arena (`bkmpArenaPlayBattleAnimation`, `js/systems/bkmp-arena.js`, seit 14.07. auf Spielerwunsch "Dorf gegen Dorf?") für exakt denselben "Angreifen"-Klick eine hat - inkonsistent zum bereits gewohnten Verhalten. Nutzer-Rückfrage + "ja bitte" bestätigt.

**Umsetzung:** neue `bkmpClanArenaPlayBattleAnimation(myName, myTag, opponentName, opponentTag, won)` (`js/systems/bkmp-guild.js`) - 1:1 dasselbe Timing/HP-Balken/Hit-Flash/Schadenszahlen-Gerüst wie `bkmpArenaPlayBattleAnimation` (5 Ticks à 420ms, Ergebnis-Anzeige danach), aber **kein Dorf-Skin-Sprite** - eine Gilde hat viele Mitglieder, kein einzelnes "eigenes" Dorf. Stattdessen ein neues rundes Gilden-Abzeichen (`.clan-arena-battle-badge`, style.css) mit Tag-Anzeige, in einem neuen `#clanArenaBattleOverlay` (index.html, direkt nach dem bestehenden `#arenaBattleOverlay` - reine Marketing-/Investoren-Seiten wie admin.html/idle-stream-mini.html brauchen dieses Overlay nicht, admin.html lädt `bkmp-guild.js` trotzdem weiter unverändert, die Funktion bricht dort dank bestehendem `if (!overlay) resolve()`-Guard sauber ab). Click-Handler in `bkmpIdleRenderClanPanel()` ruft die Animation jetzt mit dem ECHTEN, bereits vom Server entschiedenen Ergebnis auf, bevor Toast/Log erscheinen - identische Sequenzierung wie beim Spieler-Arena-Vorbild (`bkmp-arena.js`, `await bkmpArenaPlayBattleAnimation(...)` vor dem Toast).

**Tests (`tests/e2e/guild-clan-arena.spec.js`, 2 neue Tests):** Sieg-Fall (starke Gilde A gegen eine schwache Zielgilde, deterministisch garantierter Sieg durch die Fixture-Machtwerte) prüft korrekte Namen/Tags auf beiden Seiten + Overlay öffnet/schließt sich automatisch + Log zeigt "Clan-Sieg"; Niederlage-Fall (umgekehrt, schwacher Anführer greift die starke Gilde A an) prüft dasselbe für den Verlust-Pfad. Beide grün auf allen 3 Projekten (Mobile korrekt übersprungen, Desktop-Tab-Navigation). Bestehender "Anführer kann per echtem Klick angreifen"-Test lief unverändert weiter (die zusätzliche ~3.5s Animationsdauer liegt innerhalb des bestehenden `expect.poll()`-Timeouts). Voller Regressionslauf der Datei: 35 bestanden, 10 korrekt übersprungen. Visuell per injizierter Direktaufruf-Prüfung bestätigt (echter Screenshot an diesem `position:fixed`-Vollbild-Overlay technisch nicht möglich, bekannte Werkzeug-Einschränkung): 230×230px kreisrundes goldgerahmtes Abzeichen, korrekte Namen/Tags beider Seiten, Ergebnistext "🏆 Sieg!", Overlay schließt sich nach Ablauf automatisch selbst. `eslint`/`node --check` sauber.

Cache-Busting `?v=20260730-clanbattleanim1` für `js/systems/bkmp-guild.js` (index.html/admin.html/idle-stream-mini.html) und `style.css` (alle 5 HTML-Dateien).

## Umbenennung "Clan" → "Gilden-Arena" (30.07.2026)

Direkter Nutzerwunsch nach der Kampfanimations-Nachbesserung: "kannst du es dann 'Gilden Arena' nennen bitte?" - das gesamte Spiel nutzt sonst durchgehend "Gilde" (Gilde-Tab, Gilden-Tech, Gildenboss, Gildenkasse), nur dieses neue Feature hieß bisher "Clan" - Terminologie-Inkonsistenz behoben.

**Umsetzung:** ausschließlich sichtbare Spieler-Texte umbenannt - Tab-Button "🏆 Clan" → "🏆 Gilden-Arena" (index.html + idle-stream-mini.html), Panel-Überschriften "🏆 Clan"/"⚔️ Clan-Arena"/"🏆 Clan-Bestenliste" → "🏆 Gilden-Arena"/"⚔️ Gilden-Arena"/"🏆 Gilden-Bestenliste", Kampfanimations-Overlay-Titel "⚔️ Clan-Arena-Kampf" → "⚔️ Gilden-Arena-Kampf", Sieg/Niederlage-Toast+Log-Texte, Ladehinweis, Fehlermeldung. **Bewusst NICHT umbenannt:** interne Funktions-/Variablen-/CSS-Klassen-/ID-Namen (`bkmpClan*`, `.idle-clan-arena-*`, `#idlePanelClan`, `#clanArenaBattleOverlay` usw.) - reine interne Bezeichner, die kein Spieler je sieht, eine vollständige mechanische Umbenennung über HTML/CSS/JS/Tests hinweg hätte nur Risiko ohne sichtbaren Nutzen gebracht. Die noch nicht ausgeführten öffentlichen Changelog-SQL-Einträge (`sql/20260730-changelog-entries.sql`) wurden entsprechend mit angepasst, bevor sie ausgeführt werden.

**Verifiziert:** ein Test hing an der alten sichtbaren Textzeichenkette (`toContainText('Clan-Bestenliste')`/`toMatch(/Clan-Sieg/)` usw.) - alle 4 betroffenen Assertions in `tests/e2e/guild-clan-arena.spec.js` auf die neuen Texte umgestellt. Voller Regressionslauf danach: 35 bestanden, 10 korrekt übersprungen, alle 3 Projekte. `node --check` sauber.

Cache-Busting `?v=20260730-guildarenarename1` für `js/systems/bkmp-guild.js` (index.html/admin.html/idle-stream-mini.html) und `style.css` (alle 5 HTML-Dateien).

## Server-seitige Anti-Cheat-Tempo-Grenze gegen Timer-Speedhacks (30.07.2026)

Spieler-Meldung (Feedback-Board-Screenshot + 2 Beweisvideos): Browsererweiterungen wie "HTML5 Universal Speed Hack" + Cheat Engine ("und mit einer älteres Browser version kann man mit der Cheat Engine auch noch was am Speed machen") beschleunigen den lokalen Tick-Timer, "teilweise ne Stunde 300 Level, andere 800-900 Level über Nacht". Nutzer-Entscheidung nach Rückfrage (AskUserQuestion, zwei Fragen): **"Server-seitige Tempo-Grenze (empfohlen)"** (nicht die größere Variante "kompletter Server-seitiger Kampf", nicht nur eine Admin-Übersicht) + **"Ja, rein lesend prüfen"** der beiden Video-Konten. **Nichts committed/gepusht, keine Produktionsdaten verändert (nur lesende curl-Abfragen gegen die öffentlich per RLS lesbare `idle_player_state`-Tabelle, identisches Muster wie bereits etablierte Live-Diagnosen in diesem Projekt).**

**Video-Beweis (per `ffmpeg`-Frame-Extraktion ausgewertet, kein direktes Video-Tool verfügbar - gleiche Notlösung wie beim 27-Sek.-Screen-Recording vom 21.07.):** Video 1 zeigt den Yaksha-Drachenboss (14.137 HP) innerhalb von ~40 Sekunden Videozeit mehrfach komplett von voll auf besiegt und wieder auf voll durchlaufen - bei einem clientseitig hart verdrahteten 400ms-Tick-Boden (siehe unten) rechnerisch nicht möglich, außer der Timer selbst wurde beschleunigt. Video 2 zeigt im Skilltree-Tab "Verfügbare Skillpunkte: 100001" - eine Zahl, die selbst mit extremem Speedhack kaum durch normales Spielen erreichbar ist, eher ein Hinweis auf direkte Zustands-Manipulation (z.B. Entwicklerkonsole).

**Root Cause (im Code bestätigt):** `upsertIdlePlayerState()` (supabase.js) schreibt den KOMPLETTEN Client-Zustand als reines, nur per RLS auf Kontobesitz geprüftes Update - der Server hat bisher NIE geprüft, ob Level/Kills/Ressourcen/Skillpunkte überhaupt zur seit dem letzten Speichern vergangenen ECHTEN Zeit passen.

**Ehrlich dokumentierter Fehlschlag bei der retroaktiven Kontenprüfung (wichtig, bevor die eigentliche Lösung beschrieben wird):** ein erster Versuch, verdächtige Bestandskonten per `dragon_kills ÷ playtime_seconds`-Verhältnis zu finden, ergab Ausreißer bis zu 26 Kills/Sekunde im Schnitt über die gesamte gemeldete Spielzeit. Bei genauerer Prüfung erwies sich diese Kennzahl aber als **unzuverlässig und wurde verworfen, bevor sie als Cheater-Liste präsentiert wurde**: `playtime_seconds` wächst in `bkmpIdleTick()` nur um den NOMINELLEN Tick-Intervall PRO TICK (`idledorf.js:937`, unabhängig von echter Wanduhrzeit) - ein Timer-Speedhack würde also Zähler UND Nenner exakt gleich verzerren. Der eigentliche Fallstrick: `api/claim-idle-offline-progress.js` schreibt `dragon_kills` bei einem Offline-Claim OHNE `playtime_seconds` mitzuerhöhen - ein völlig legitimer Spieler, der oft große Offline-Zeiträume claimt, zeigt dadurch dieselbe verzerrte Kennzahl wie ein Cheater. Einer der Top-Ausreißer in der ersten Abfrage war ein seit langem bekannter, hilfsbereiter Bug-Melder dieses Projekts - ein starkes Indiz, dass die Kennzahl falsch-positiv schlägt, keine belastbare Beweisgrundlage. **Ergebnis: keine zuverlässige retroaktive Verdächtigen-Liste möglich aus den vorhandenen Daten** (nur ein einzelner Zeilen-Schnappschuss pro Konto, keine Verlaufsdaten) - die beiden Videos bleiben als direkter, unabhängiger Beweis bestehen, es wurde aber niemand auf Basis der fehlerhaften Kennzahl beschuldigt.

**Herleitung der Tempo-Grenze (Recherche-Agent, exakte Formeln aus dem echten Code zitiert):**
- Tick-Intervall: `Math.max(400, Math.round(900/(1+attack_speed_pct/100)))` (idledorf.js) - harter Boden 400ms, mit Prestige "Höhere Kampfgeschwindigkeit" (max 90%) + Skilltree-Knoten (max 40%) bereits bei 130% erreichbar, also fest.
- `bkmpIdleHandleDragonDefeated()` erhöht `dragon_kills` höchstens 1x pro Tick-Aufruf, kein Verketten mehrerer Kills in einem Tick.
- → **rechnerisch NIE mehr als 2,5 Kills/Sekunde**, unabhängig von Build/Ausrüstung, für JEDEN legitimen Spieler.
- `api/claim-idle-offline-progress.js` (bereits bestehend, serverseitig gedeckelt): max. 17h effektive Simulation bei ~0,89 Kills/Sekunde - weit unter jeder gewählten Grenze, löst also nie fälschlich aus.

**Umsetzung (`sql/20260730-idle-player-state-anticheat-guard.sql`, neu, **noch nicht ausgeführt**):** neuer `before update`-Trigger auf `idle_player_state`, vergleicht `NEW.dragon_kills - OLD.dragon_kills` gegen ein aus der ECHTEN Postgres-Wanduhrzeit (`now() - OLD.updated_at`, bewusst NICHT `playtime_seconds`, siehe oben) berechnetes Budget: `MAX_KILLS_PER_SECOND = 3` (bewusst nur 20% über der harten 2,5/s-Obergrenze - erkennt zuverlässig jeden deutlichen Speedhack, ohne je einen legitimen Spieler zu treffen), `MIN_ELAPSED_SECONDS = 4` (Debounce-Untergrenze gegen Kurzzeit-Messfehler). Bei Überschreitung wird NICHT abgelehnt (kein Fehler für den Client) - der GESAMTE Fortschritts-Zuwachs dieser einen Speicherung (`dragon_kills`, `boss_kills`, `gold`, `total_gold_earned`, `wood`, `stone`, `crystals`, `essence`, `xp`, `level`, `skill_points_available`) wird anteilig auf das noch plausible Maß herunterskaliert (`ratio = erlaubtes Kills-Delta / gemeldetes Kills-Delta`) - bewusste Vereinfachung statt einer Neuberechnung der von Dutzenden Boni abhängigen echten Belohnungsformel. Fallende Werte (Ausgeben, Prestige-Reset) werden NIE angetastet, nur ansteigende Deltas. Jeder ausgelöste Fall wird in einer neuen, admin-only lesbaren `idle_anticheat_flags`-Tabelle protokolliert (zur künftigen Beobachtung, keine automatische Sperrung).

**Ein besonders wichtiger Vorteil dieser Umsetzung:** rein serverseitig (Postgres-Trigger) - **keine einzige Client-Datei geändert**, `upsertIdlePlayerState()` ruft weiterhin exakt denselben `.update()`-Aufruf wie vorher auf, der Trigger greift transparent dazwischen. Kein Cache-Busting nötig, kein JS-Deploy nötig - nur die SQL-Datei einmal im Supabase Dashboard ausführen.

**Tests (`tests/e2e/anticheat-progress-rate-guard.spec.js`, neu, 4 Tests, plus JS-Nachbau `tests/mock/anticheat-guard.js` + Verdrahtung in `tests/mock/rest-engine.js`'s PATCH-Zweig):** kleine normale Änderung bleibt unangetastet; ein implausibler 12.000-Kills-Sprung in 4 Sekunden wird korrekt auf 12 Kills gekürzt (ratio 0,001, alle Felder proportional gekürzt, Log-Eintrag mit exakten Werten bestätigt); ein großer, aber über 5 echte Stunden plausibler Sprung (z.B. nach einem Offline-Claim) bleibt komplett unangetastet; fallende Werte (Gold ausgegeben) werden nie gekürzt, auch nicht in derselben geflaggten Speicherung. Beim ersten Testentwurf ein reines Test-Timing-Problem gefunden+behoben (kein App-Bug): ein unabhängiger, legitimer Vorgang (Tages-Login-Streak-Bonus) kann zwischen Login und der eigenen Test-Mutation noch nicht in der DB stehen, obwohl `bkmpIdleState` ihn clientseitig schon zeigt - ein bewusster "Settle-Flush" vor jeder Messung macht den DB-Stand nachweislich deckungsgleich. Alle 4 Tests grün, zusätzlicher Regressionslauf (`save-load.spec.js`, `combat.spec.js`, `offline-afk.spec.js`, `prestige.spec.js`, alle 3 Projekte): 58 bestanden, 26 korrekt übersprungen, 0 Fehlschläge. `eslint`/`node --check` sauber (1 unveränderte, vorbestehende Warnung in rest-engine.js, nicht Teil dieser Änderung).

Kein Cache-Busting nötig (siehe oben). `package.json`'s `qa:persistence`-Script um die neue Datei erweitert.

**Nicht geprüft/offen:** `sql/20260730-idle-player-state-anticheat-guard.sql` muss im Supabase-Dashboard ausgeführt werden, bevor der Schutz live wirkt. Die zwei in den Videos gezeigten Konten konnten nicht eindeutig identifiziert werden (kein sichtbarer Ingame-Name in den Aufnahmen, nur ein Minecraft-Server-Widget) - keine retroaktive Korrektur an bestehenden Konten vorgenommen, siehe ehrlicher Fehlschlag oben. Bewusst nicht umgesetzt (kleinerer Umfang als die verworfenen Alternativen): keine Admin-Übersicht für künftig geflaggte Fälle (Daten liegen jetzt in `idle_anticheat_flags`, aber ohne UI - eine spätere Ergänzung wäre eine reine `select`-Abfrage), kein serverseitiger Neuaufbau des Kampf-Ticks selbst.

## Nebenfund bei der Anti-Cheat-Recherche: Paragon-Rang-Anzeige in der Gilden-Technologie zeigte nie den echten Bonus (30.07.2026)

Spieler-Meldung (Screenshot: "Kritischer Schaden", Stufe 35/35, Paragon-Rang 5, "+70%"): "Level Paragon hoch aber am Wert ändert sich nichts."

**Root Cause (bestätigt, nicht nur vermutet):** `bkmpIdleRenderGildeTechPanel()` (`js/systems/bkmp-guild.js`) berechnete die angezeigte "+X%"-Karten-Zahl für die ursprünglichen 9 Zweige bisher direkt als `(level * tech.perLevel).toFixed(1)` - unabhängig vom Paragon-Rang. `bkmpGuildTechExtBonusDisplay()` (die 10 neueren Zweige) hatte für Kriegsrat/Autokauf/Nachtwache und den generischen Prozent-Fall exakt dasselbe Muster (`level * tech.effectPerRank`). Nur `guild_turm_vorreiter` war bereits korrekt, weil er als einziger schon `bkmpGuildTechBonus()` nutzte statt selbst neu zu rechnen. **Der reale Spieleffekt war nie betroffen** - `bkmpGuildRefreshTreasuryBonusCache()` rechnet den Paragon-Beitrag längst korrekt in den echten Bonus-Cache ein (`total += paragonLevel * bkmpGuildTechParagonEffectPerRank(tech)`), nur diese eine Kartenanzeige las ihn nie aus.

**Fix:** alle betroffenen Anzeigestellen lesen jetzt denselben, bereits Paragon-inklusiven Cache über `bkmpGuildTechBonus(tech.statKey)`/`bkmpGuildTechBonus(tech.effectType)` statt selbst neu zu rechnen - identisches Prinzip wie beim bereits korrekten Turm-Vorreiter. Zusätzlich ein kleiner, verwandter Race-Fund behoben: der Klick-Handler rief `bkmpGuildRefreshTreasuryBonusCache()` bisher als reines "fire-and-forget" (kein `await`) auf, bevor der Bonus-inklusive Neu-Render startete - bei einem minimal langsameren Netzwerk-Refresh konnte die Karte direkt nach einem Kauf kurzzeitig noch den alten Wert zeigen. Jetzt abgewartet.

**Test (`tests/e2e/guild-tech.spec.js`, neuer Block "Anzeige-Bugfix", 1 Test):** anders als der Rest der Datei (reine RPC-Aufrufe, kein bestehender Test hätte diesen Bug je auffangen können, da er ausschließlich in der Render-Funktion lebte) eine echte UI-Prüfung - Karte zeigt vor einem Paragon-Kauf korrekt "+70%" (35 Stufen × 2%), nach einem echten Klick auf den "+Paragon"-Button korrekt "+70.1%" (1 Paragon-Rang × 2% × 4% Paragon-Anteil = +0,08%, rundet sichtbar auf). Grün, plus voller Regressionslauf aller drei Gilden-Tech-Testdateien über alle 3 Projekte sauber. `eslint`/`node --check` sauber.

Cache-Busting `?v=20260730-paragondisplayfix1` für `js/systems/bkmp-guild.js` (index.html/admin.html/idle-stream-mini.html).

## Tooltip-Form + Dungeon-Schlüssel-Anzeige repariert (31.07.2026)

Zwei kleine, unabhängige Bugfixes direkt im Anschluss an die Anti-Cheat-Arbeit. **Nichts committed/gepusht.**

**Tooltip-Form (Nutzer-Screenshot: "Das einfach in einen Rechteck Fenster bitte. Sieht dumm aus."):** die neuen Paragon-Info-Tooltips im Prestige-Baum (`bkmpUiTooltipHtml`/`bkmpUiWireTooltipTrigger`, siehe Abschnitt "Paragon-Verständlichkeit" vom 28.07.) rendern als schmale, hohe Spalte statt eines Rechtecks - ein Wort pro Zeile. Root Cause: `.bkmp-ui-tooltip` (style.css) ist `position:absolute` mit nur `max-width:240px`, aber ohne eigene `width` - diese Kombination lässt den Browser auf die "shrink-to-fit"-Breite des LÄNGSTEN EINZELNEN Worts schrumpfen statt natürlich bis zur Kappungsgrenze zu wachsen (ein CSS-Grundverhalten, keine Layout-Kollision mit einem anderen Element). Fix: `width: max-content;` ergänzt - kurze Tooltips bleiben kompakt einzeilig (~93px bei "Kurzer Text"), lange wachsen jetzt sauber bis zur 240px-Grenze und brechen dort normal um. Verifiziert per direkt injiziertem Tooltip-Markup im Browser (kein echter Login nötig, reine CSS-Struktur) - Screenshot bestätigt beide Fälle (kurzer und langer Text) korrekt.

**Dungeon-Schlüssel-Anzeige (zwei Nutzer-Screenshots: Schlüsselbund-Karte "Rang 10/50" + Dungeon-Panel "Ei-Dungeon Schlüssel: 7/5", "Dungeon schlüssel funktioniert nicht so gut"):** Root Cause bereits am 27.07. beim Verdrahten von Schlüsselmeister/Schlüsselbund als Kommentar in `js/systems/bkmp-dungeon.js` vorausgesehen, aber damals nicht behoben: die SERVER-RPC (`dungeon_get_all_status()`) berechnet den Schlüsselbund-inklusiven Maximalwert korrekt (bereits per bestehendem Test "Schluesselbund Rang 20: Deckel steigt von 5 auf 25" bewiesen), gibt ihn aber nie als eigene Spalte zurück - der Client musste ihn schon immer selbst aus demselben Prestige-Bonus ableiten, den `bkmpPrestigeBonus()` für jeden anderen Knoten ohnehin liefert. Zwei Stellen taten das nicht: `bkmpDungeonKeyLineHtml()` (Kartenanzeige "X/Y") und `bkmpDungeonStartCountdownTicker()`s Abbruch-Bedingung (`status.keys >= 5`) nutzten stattdessen die alte feste `BKMP_DUNGEON_KEY_MAX=5`-Konstante - erklärt sowohl die falsche Anzeige ("7/5" statt "7/15") als auch, warum der Sekunden-Countdown für einen Dungeon-Typ komplett einfror, sobald der Bestand über 5 stieg (der Ticker hielt den Typ fälschlich für "voll" und aktualisierte ihn nicht mehr).

**Fix:** neue `bkmpDungeonEffectiveKeyMax()` (`js/systems/bkmp-dungeon.js`, identische Formel wie die Server-RPC: `BKMP_DUNGEON_KEY_MAX + floor(bkmpPrestigeBonus('dungeon_key_cap_bonus'))`) an allen 4 betroffenen Stellen eingesetzt: `bkmpDungeonKeyLineHtml()`, `bkmpDungeonStartCountdownTicker()`s Gate, `bkmpDungeonRenderCard()`s Fallback-Default-Status, und der statische Einleitungstext im Panel-Kopf ("max. X Schlüssel"). Keine Server-/SQL-Änderung nötig - reiner Client-Anzeige-/Ticker-Fix.

**Test (`tests/e2e/dungeon-key-prestige-bonus.spec.js`, neuer Test, ergänzt den bereits bestehenden Server-Beweis):** Schlüsselbund Rang 10 (5 Basis + 10×1 = 15) - `bkmpDungeonEffectiveKeyMax()` liefert 15, Kartentext zeigt "7/15" statt der alten "7/5" bei künstlich auf 7 gesetztem Bestand (exakt das im Bugreport gezeigte Muster), Einleitungstext zeigt "max. 15". REGRESSION: Ticker darf bei 7 Schlüsseln (> alte Konstante 5, < neuer Maximalwert 15) nicht mehr einfrieren - `secondsToNext` wird nach einem echten 1s-Ticker-Tick nachweislich weiter heruntergezählt. Eigener Fund beim Schreiben: `bkmpPrestigeState` ist ein beim Login einmalig geladener Client-Cache (anders als die Server-RPC, die live aus der DB liest) - musste nach dem direkten Store-Eingriff im Test explizit per `loadIdlePrestigeState()` nachgezogen werden, sonst sah `bkmpDungeonEffectiveKeyMax()` weiterhin die alte, beim Login geladene Zuteilung (identisches Muster wie der bereits dokumentierte Cache-Nachzieh-Fund beim Guild-Tech-Paragon-Test). 45/45 stabil in Wiederholungen auf `chromium-desktop` (15+30), korrekt übersprungen auf Mobile (echte Desktop-Tab-Klicks). Ein einzelner Fehlschlag unter kombinierter 3-Projekte-×-5-Wiederholungen-Last war reine, bereits mehrfach dokumentierte Ressourcenkonkurrenz (isoliert 15/15 sauber) - keine echte Flakiness des Tests selbst. Zusätzlicher Regressionslauf (`dungeon-time.spec.js`, `gem-dungeon-reward.spec.js`, alle 3 Projekte): ein bereits vorher bekannter, unabhängiger Flake (`dungeon-time.spec.js` "verbraucht genau einen Schluessel", siehe Phase-5-Bericht "Stabilitaets-Nachpruefung") trat einmal auf, isoliert 10/10 sauber - keine neue Regression durch diese Änderung.

Cache-Busting `js/systems/bkmp-dungeon.js?v=20260731-dungeonkeyfix1` (index.html/admin.html/idle-stream-mini.html), `style.css?v=20260731-tooltipfix1` (alle 5 HTML-Dateien).

**Nachtrag (Rückfrage geklärt):** der Nutzer erwähnte im selben Atemzug ("Und Bitte 50 schlüssel? Bitte maximal 3 lvl") eine mögliche zusätzliche Balance-Änderung - per `AskUserQuestion` geklärt: gemeint war **"Nur Maximalrang auf 3 senken"** (reiner Grind-Fix, kein Umbau der Kostenkurve). Umsetzung: `js/systems/bkmp-prestige.js`, Schlüsselbund-Knoten nutzt jetzt `bkmpPrestigeTierDef('WEAK', { maxRank: 3 })` statt `bkmpPrestigeTierDef('WEAK')` (identisches Override-Muster wie beim bereits bestehenden `runenmeister`-Knoten, `maxRank: 15`) - `effectPerRank`/Kosten-Basis unverändert, betrifft NUR diesen einen Knoten, nicht den geteilten `WEAK`-Tier (der wird noch von 4 weiteren Knoten genutzt: `massenkauf`/`groesseres_lager`/`erweiterter_autokauf`/`autokauf_mehrstufig`). Bereits vor der Änderung investierte höhere Ränge (z.B. der im Screenshot gezeigte "Rang 10/50") bleiben unangetastet voll wirksam - `bkmpPrestigeEffectTotals()` kennt keine `maxRank`-Kappung des bereits erspielten Bonus, nur `bkmpPrestigeBuyUpgrade()`s `if (rank >= def.maxRank) return;`-Gate verhindert WEITERE normale Käufe über Rang 3 hinaus; danach greift automatisch der bereits bestehende Paragon-Pfad (`bkmpPrestigeParagonCost()` setzt an der neuen, niedrigeren `maxRank`-Grenze an - macht frühe Paragon-Ränge günstiger als vorher, eine erwartete, im Nutzer-Optionstext bereits benannte Konsequenz der Grind-Reduktion). Neuer Test (`tests/e2e/prestige-tree-v2.spec.js`) beweist alle drei Teile: `maxRank===3`, 6 Kaufversuche in Folge stoppen bei Rang 3 (kein 4. normaler Rang, kein Punkteverbrauch über Rang 3 hinaus), ein simulierter Bestandsrang 10 liefert weiterhin den vollen `dungeon_key_cap_bonus` von 10. 15/15 stabil in Wiederholung, 88/88 grün im breiteren Regressionslauf (Prestige-/Dungeon-Schlüssel-/Automations-/Aufstiegs-Suiten, alle 3 Projekte).

Cache-Busting `js/systems/bkmp-prestige.js?v=20260731-keycapmax1` (index.html/admin.html/idle-stream-mini.html).

### Nachtrag, gleicher Tag: Fairness-Nachbesserung — Paragon entfernt + Rückerstattungs-Migration für alle Spieler

Der Nutzer sah nach dem Deploy der obigen Grind-Reduktion live in seinem eigenen Account, dass sein bereits investierter Rang 10 (Screenshot: "Rang 10 (Max)") sofort den (durch den neuen, niedrigen `maxRank` plötzlich erreichbaren) Paragon-Pfad freischaltete und schon "Paragon-Rang 3/1K" zeigte. Nutzerwunsch danach: **"Paragon dort entfernen. Und Bitte für alle Spieler auf MAX 3 LvL Schlüssel ändern.. Dann haben ja manche jetzt 50 und manche nur 3. Gerne für alle die dort bereits investiert haben zurückgeben deren prestige Punkte."** — eine bewusste Abkehr vom ursprünglichen "Bestandsränge bleiben grandfathered"-Ansatz zugunsten echter Gleichbehandlung aller Spieler.

**Umsetzung (`js/systems/bkmp-prestige.js`):**
1. Schlüsselbund-Knotendefinition: `...bkmpPrestigeTierDef('WEAK', { maxRank: 3, paragonEligible: false })` — Paragon für diesen Knoten dadurch strukturell unmöglich (`bkmpPrestigeParagonEligible()` prüft `def.paragonEligible !== false`), sowohl für die UI (Paragon-Sektion wird gar nicht mehr gerendert, `bkmpPrestigeEffectTotals()` überspringt den Paragon-Zweig komplett) als auch für `bkmpPrestigeBuyParagonUpgrade()` (`paragonRank`-Gate greift über dieselbe Eligibility-Prüfung).
2. Neue `bkmpPrestigeMigrateSchluesselbundDowngrade()` (direkt nach `bkmpPrestigeSnapshotMergeBaseline()`): kappt `alloc.schluesselbund` auf `min(rank, 3)`, entfernt `alloc.schluesselbund__paragon` vollständig, und erstattet die dafür ausgegebenen Prestige-Punkte EXAKT zurück (`prestige_points_spent -= refund`). Die Rückerstattung ist rechnerisch exakt, nicht geschätzt: `bkmpPrestigeUpgradeCost()` hängt nachweislich nicht von `maxRank` ab (nur `baseCost`/`costGrowth`, beide unverändert), die Kosten für Ränge 4-10 sind also identisch zu dem, was beim ursprünglichen Kauf tatsächlich berechnet wurde. Idempotent: ein Spieler bei Rang≤3 ohne Paragon (die große Mehrheit) verlässt die Funktion sofort ohne jede Mutation. Ruft am Ende `bkmpPrestigeSnapshotMergeBaseline()` erneut auf (Baseline auf den korrigierten Stand ziehen), damit ein späterer Remote-Merge (`bkmpPrestigeMergeRemoteSpendable()`, nur auf der OBS-Stream-Mini-Seite relevant) die Rückerstattung nicht wieder verwirft.
3. `idledorf.js`: Migration wird an BEIDEN Stellen aufgerufen, an denen `bkmpPrestigeState` frisch vom Server zugewiesen wird — im normalen Login-/Öffnen-Ablauf UND im periodischen "Sitzung anderswo übernommen"-Abgleich (`remotePrestigeCheck`) — läuft dadurch automatisch bei jedem echten Login, ohne dass der Spieler etwas tun muss.

**Bekannter, bewusst akzeptierter Restfall (nicht weiter verfolgt, siehe Begründung):** `bkmpPrestigeMergeRemoteSpendable()` (nur `BKMP_IDLE_IS_STREAM_PAGE`, also nur die separate OBS-Overlay-Seite `idle-stream-mini.html`) merged Allokationen per Maximalwert zwischen lokal und remote - ein sehr enges Zeitfenster (ein Stream-Tab mit VOR dem Fix gecachten Daten führt einen Merge NACH der Migration des Hauptaccounts durch) könnte theoretisch einen bereits migrierten Wert kurzzeitig wieder anheben. Selbstheilend: der nächste normale Login auf der Hauptseite erkennt den dann wieder zu hohen Stand und migriert erneut. Bewusst nicht tiefer abgesichert (enger Rand-Fall auf einer Nebenseite, kein normaler Spielerpfad).

**Tests (`tests/e2e/prestige-tree-v2.spec.js`, 3 neue + 1 aktualisierter Test):**
- Bestehender "Migrationsbeweis"-Test (der ursprünglich das GRANDFATHERED-Verhalten prüfte) durch eine Prüfung von `paragonEligible === false` ersetzt - Testname entsprechend angepasst, alte Beschreibung im Kommentar als "inzwischen überholt" markiert statt stillschweigend gelöscht.
- **Downgrade-Migration mit exakter Rückerstattung**: simuliert Rang 10 + 3 Paragon-Ränge, beweist `rank===3`, `paragon===undefined`, `bonus===3` (kein Paragon-Anteil mehr), und dass der Rückerstattungsbetrag EXAKT der live über dieselben Kostenformeln berechneten Erwartung entspricht (kein hartkodierter Magic-Number-Vergleich). Zweiter Aufruf beweist Idempotenz (keine Doppel-Rückerstattung).
- **Unveränderter Fall**: ein Spieler bei Rang 2 ohne Paragon bleibt komplett unangetastet (Rang UND ausgegebene Punkte identisch vor/nach Funktionsaufruf).
- **Integrationstest über den echten Login-Ablauf**: schreibt Rang 10 + Paragon 3 direkt in den Mock-Store (identisches Muster wie `setPrestigeAllocations()` in `dungeon-key-prestige-bonus.spec.js`) VOR dem Login, ruft dann `openAndLogin()` (keine manuelle Funktionsaufruf-Injektion) auf und beweist, dass die Migration automatisch beim echten Laden greift — exakt der vom Nutzer gemeldete Produktionsfall.

**Verifiziert:** 60/60 stabil bei 15x Wiederholung aller Schlüsselbund-Tests (`chromium-desktop`), 101/101 grün im breiteren Regressionslauf (Prestige-Baum/Prestige-Aufstieg/Dungeon-Schlüssel/Automations-Skills/Aufstieg-und-Senken/Speichern-Laden, alle 3 Projekte, 0 Fehlschläge). Live im Browser bestätigt (kein echter Login nötig): `bkmpPrestigeNodeById('schluesselbund')` liefert direkt aus der ausgelieferten Datei `maxRank:3, paragonEligible:false`. `node --check`/`eslint` auf beiden geänderten Dateien sauber.

Cache-Busting `js/systems/bkmp-prestige.js?v=20260731-keyrefund1`, `idledorf.js?v=20260731-keyrefund1` (index.html/admin.html/idle-stream-mini.html).

## Gilden-Technologie v3: Baum-System mit Mitglieder-Beiträgen (31.07.2026) — ersetzt v2 komplett

Nutzer zeigte zwei Screenshots des Allianz-Technologie-Systems aus **Whiteout Survival** (per Internet-Recherche bestätigtes Vorbild) und wollte ein vergleichbares System für BKMP: statt dass nur Anführer/Stellvertreter Ränge aus der gemeinsamen Gildenkasse kaufen (das bisherige, mehrfach gewachsene System — 9 STANDARD- + 10 v2-Zweige, siehe die beiden Abschnitte weiter oben in dieser Datei), soll JEDES Gildenmitglied mit eigenem Gold zu einem GEMEINSAMEN Fortschrittsbalken pro Knoten beitragen, die Knoten bilden einen echten verzweigenden Baum. Nutzer-Entscheidungen (per `AskUserQuestion` geklärt, bindend): **"Komplett ersetzen"** (nicht additiv), **"Nur Gold"** (keine zweite Währung), **"Kompletter Plan zuerst, dann in Stufen umsetzen"**. Nach Plan-Freigabe (`~/.claude/plans/zazzy-crunching-plum.md`) eine weitere Rückfrage zu Paragon geklärt: **"Für v1 weglassen"** (jeder Knoten hat eine feste, endliche Maximalstufe). Anschließend durchgehend **"ja weiter machen"** — alle 4 geplanten Stufen ohne weitere Rückfragen umgesetzt. **Nichts committed/gepusht durch diese Session bewusst ausgelöst, keine SQL live ausgeführt.**

**Warum eine Datenbank-Tabelle statt einer dritten hardcodierten Kopie:** das v2-System hatte Kosten/Effekt/Maximalstufe DREIFACH hardcodiert (SQL-`CASE`-Block der RPC, JS-Katalog `BKMP_GUILD_TECH_CATALOG(_EXT)`, Playwright-Mock) — exakt das "mehrere Kopien müssen synchron bleiben"-Muster, das in diesem Projekt bereits mehrfach echte Bugs verursacht hat (z.B. die `dungeon_regen_calc()`-Mehrdeutigkeit vom 26.07.). Die neue `guild_tech_nodes`-Tabelle (öffentlich lesbar, admin-frei per direktem SQL-Insert pflegbar — identisches Muster wie `guild_level_thresholds`) macht SQL-RPC und Client zu reinen Lesern derselben einen Quelle.

**Wiederverwendete, bereits bewährte Bausteine (nichts neu erfunden):** `dungeon_regen_calc()` (unverändert wiederverwendet für die täglichen Beitragsversuche, `p_interval_seconds=14400, p_max_keys=5`), `contribute_gold()`s Muster (`auth.uid()`-abgeleitete Identität, Gold-Prüfung+Abzug, Aktivitäts-Log) als Vorbild für "Mitglied gibt eigenes Gold aus", die bestehende "Spendenrangliste" (Server sortiert per `.order()`, Client nutzt Array-Index als Rang) für die neue Technologie-Rangliste, `bkmpDungeonStartCountdownTicker()` als Kopiervorlage für den neuen Beitragsversuch-Countdown.

**Wichtiger Querbezug (vor der Umsetzung im Code bestätigt):** `arena_attack()` (Kriegsrat-Bonus) und `raid_join()` (Stadtmauer-Bonus) lasen bisher DIREKT aus `guild_tech_levels`, unabhängig vom Client-Cache — mussten in derselben Migration auf `guild_tech_progress` umgestellt werden, sonst wären Arena-Tageslimit und Raid-Stadtmauer-Bonus für jede Gilde still auf 0 zurückgefallen. **Beim eigenen Verdrahten ein zweiter, bis dahin unbekannter Konsument gefunden:** `api/claim-idle-offline-progress.js`s Nachtwache-Lookup (server-seitige Erhöhung des Offline-Fortschritts-Deckels) las ebenfalls direkt aus `guild_tech_levels` — nicht im ursprünglichen Plan als "kritische Datei" gelistet, aber genauso vom Cutover betroffen. Beide Stellen lesen jetzt zusätzlich `effect_per_tier` DYNAMISCH aus `guild_tech_nodes` (kein zweiter hartcodierter Multiplikator) — ein selbst begangener Fehler (erster Entwurf hatte den Multiplikator noch separat hartcodiert) wurde vor dem Abschluss selbst gefunden und korrigiert.

### Datenmodell (`sql/20260731-guild-tech-tree-v2-foundation.sql`, neu, **noch nicht ausgeführt**)

- `guild_tech_nodes` (id text PK = alte tech_id für triviale Migration, category `wachstum`/`schlacht`, label/description/icon, effect_type, effect_per_tier, max_tier, base_gold_cost, cost_growth default 1.5, attempts_per_tier default 25, prereq_node_ids text[], pos_x/pos_y) — öffentlich lesbar, kein Client-Schreibzugriff (per direktem idempotentem `insert...on conflict` gepflegt).
- `guild_tech_progress` (guild_id, node_id, tier, progress_gold, PK composite) — öffentlich lesbar, nur `guild_tech_contribute()` schreibt.
- `guild_tech_contributor_attempts` (auth_user_id PK, name_key, attempts smallint 0-5 default 5, last_attempt_at) — EIN gemeinsamer Versuch-Pool über ALLE Knoten hinweg (anders als `dungeon_keys`, das pro Typ einen eigenen Vorrat hat), eigene Zeile nur selbst lesbar.
- `guild_members.tech_contributed_gold bigint default 0` (neue Spalte, additiv) — Lifetime-Beitrag für die neue Rangliste, bewusst NICHT `contributed_gold` überladen (das ist die andere, bereits bestehende "Spendenrangliste"-Metrik von `contribute_gold()`).

**`guild_tech_contribute(p_node_id text)`** → `(new_tier, new_progress_gold, tier_gold_cost, gold_spent, remaining_gold, attempts_left, seconds_to_next_attempt)`. Prüfreihenfolge: `not_authenticated` → Gildenmitgliedschaft (**keine Rollenprüfung** — das ist der ganze Sinn des neuen Systems, anders als das alte `guild_tech_upgrade()`) → Knoten existiert → Fortschrittszeile sichern+sperren → `node_maxed` falls voll → `prereq_not_met` falls irgendeine `prereq_node_ids`-ID noch nicht bei ihrer eigenen Maximalstufe ist (generalisierte, rekursionsfreie Vorbedingungs-Prüfung — Ersatz für das alte Paragon-`base_not_maxed`-Gate) → Versuch-Zeile sichern+sperren, `dungeon_regen_calc()` aufrufen, Ergebnis **sofort persistiert** (auch bei späterem Fehlschlag, damit der Countdown-Anker nie hängen bleibt) → `no_attempts_available` falls Ergebnis < 1 → `tier_gold_cost = round(base_gold_cost·cost_growth^tier)`, `contribution_step = round(tier_gold_cost/attempts_per_tier)` → `insufficient_gold` falls zu wenig eigenes Gold (**Versuch aus dem vorigen Schritt bleibt dabei verbraucht** — identisches Prinzip wie ein gescheiterter Dungeon-Lauf, Regen-Fortschritt geht nicht verloren, der Versuch selbst schon) → Commit: Gold abziehen, Versuch verbrauchen, `tech_contributed_gold` erhöhen, Fortschritt fortschreiben (bei Erreichen des Ziels: `tier+=1, progress_gold:=0`), Aktivitäts-Log.

**`guild_tech_attempt_status()`** → Einzeilen-Äquivalent zu `dungeon_get_all_status()` für den initialen Panel-Load/Ticker-Resync.

**Migration (reines einmaliges SQL, kein RPC):** proportionale Übernahme `neue_tier = floor(alte_level/alte_max_level · neue_max_tier)` — bewusst KEIN Gold-Refund (anders als die kürzliche Schlüsselbund-Prestige-Rückerstattung), weil die alte Gildenkasse ein gemeinsamer, von der Führung verwalteter Topf war, kein individuell nachverfolgtes Spieler-Guthaben — eine faire Pro-Spieler-Rückerstattung wäre hier nicht sauber zurechenbar. `__paragon`-Zeilen erzeugen bewusst keinen neuen Eintrag (Paragon entfällt in v1).

### 19-Knoten-Baum (11 Wachstum / 8 Schlacht, `effect_type`-Werte 1:1 aus dem alten System übernommen)

**Schlacht** (Beispiel-Baum aus dem Plan, zeigt lineare Fortsetzung UND 2-fache Zusammenführung): `attack`/`defense`/`crit_chance` (Wurzeln) → `crit_damage`[attack]/`boss_damage`[defense,crit_chance] → `guild_kriegsrat`[crit_damage,boss_damage] (Arena-Tageslimit) → erweitert um `guild_turm_vorreiter`[crit_chance]/`guild_stadtmauer`[boss_damage,guild_turm_vorreiter] (Raid-Stadt-HP-Beitrag). **Wachstum**: `gold`/`xp`/`prestige`/`rune_luck` (Wurzeln) → `guild_autokauf`[gold]/`guild_brutbeschleuniger`[xp,prestige]/`guild_schmiede`[rune_luck] → `guild_nachtwache`[guild_autokauf]/`guild_aufstiegsvorbereitung`[guild_brutbeschleuniger]/`guild_streak_schutz`[guild_schmiede] (TOGGLE) → `guild_willkommenspaket`[guild_nachtwache,guild_aufstiegsvorbereitung] (TOGGLE, Abschluss-Zusammenführung). Kosten/Effektwerte sind ein bewusst grober erster Entwurf (Gesamteffekt bei Maximalstufe näherungsweise auf altem Niveau gehalten) — per einfachem `update` später leicht anpassbar, ganz im Sinne der neuen "Katalog in der DB"-Architektur.

**7+ bestehende Konsumenten-Dateien brauchten NULL Änderungen** (verifiziert per Grep, da `effect_type` = alter Cache-Schlüssel): `idledorf.js` (`autobuyExtraPurchases`, plus die 9 direkten Stat-Prozente `attackPct`/`defensePct`/`goldPct`/`xpPct`/`prestigePct`/`critChancePct`/`critDamagePct`/`bossDamagePct`/`runeLuckPct`), `bkmp-runes.js` (`runeUpgradeDiscountPct`), `bkmp-events.js` (`streakProtectUnlock`), `bkmp-prestige.js` (`ascensionThresholdDiscountPct`), `bkmp-breeding.js` (`broodSpeedPct`), `bkmp-arena.js`/`supabase.js` (`arenaExtraAttempts`). **Zwei Sonderfälle bewusst NICHT direkt unter ihrem `effect_type` gecacht** (identisches Prinzip wie im abgelösten System): `towerChampionPctPer10` (Turm-Vorreiter) und `newMemberBonusUnlock` (Willkommenspaket) sind nur Erkennungsmerkmale für eine spezielle Berechnung in `bkmpGuildTechRecomputeBonusCache()` — der tatsächlich konsumierte Cache-Schlüssel heißt `towerChampionPct` bzw. `newMemberBonusPct` (verifiziert per Grep gegen `idledorf.js`s `guildExtraPct`-Zeile, BEVOR die Aggregationsfunktion geschrieben wurde — hätte sonst den Turm-Vorreiter-/Willkommenspaket-Bonus für jeden Spieler unbemerkt komplett inert gemacht).

### Frontend (`js/systems/bkmp-guild-tech.js`, neu)

Eigene Datei (statt weiterer Aufblähung von `bkmp-guild.js`) — Katalog-/Fortschritt-Laden, `bkmpGuildTechRecomputeBonusCache(mine)` (von `bkmpGuildRefreshTreasuryBonusCache()` in `bkmp-guild.js` per `typeof`-Guard delegiert aufgerufen), `bkmpIdleRenderGildeTechPanel()` (Name bewusst beibehalten, `idledorf.js`s Tab-Registry brauchte dadurch keine Änderung), Kategorie-Unterreiter (Wachstum/Schlacht, ein Tab bleibt statt zwei neuer Top-Level-Tabs), Beitrags-Modal, Countdown-Ticker, Rangliste. Neue SVG-Baum-Fabrik `bkmpUiGuildTechTreeHtml()` (`js/ui/bkmp-ui-components.js`) — Deko-SVG-`<line>` pro Vorbedingungspaar im Hintergrund, echte klickbare `<button>`-Knoten absolut positioniert über `pos_x`/`pos_y` (Datenbank = einzige Layout-Quelle, kein `<foreignObject>`). Beitrags-Modal nutzt `bkmpUiModalHtml()`/`bkmpUiTrapFocus()` (Phase 3, **erster echter Einsatz im Idle-Dorf selbst** — bis dahin nur beim 1x-Anfrage-Entscheidungs-Popup auf der Marketing-Seite genutzt).

**Ein echter, beim eigenen UI-Testen gefundener Bug — sofort gefixt:** `bkmpUiGuildTechTreeHtml()`s erste Fassung setzte das HTML-`disabled`-Attribut auf gesperrte Knoten. Ein echter Browser-Klick auf ein `disabled`-Element feuert NIE ein Klick-Event (auch Playwrights `.click()` verweigert das und wartet bis zum Timeout auf "enabled") — der eigentlich vorgesehene "hier fehlt noch Vorbedingung X"-Hinweistoast für gesperrte Knoten wäre dadurch für JEDEN Spieler komplett unerreichbar gewesen, nicht nur ein Testartefakt. Gefunden durch den neuen `guild-tech-tree-ui.spec.js`-Test selbst (30s-Timeout beim Klickversuch auf einen absichtlich gesperrten Knoten). Fix: `disabled` entfernt, die Sperr-Optik (Transparenz/Cursor) kommt jetzt rein aus der `.state-locked`-CSS-Klasse, der Klick-Handler selbst prüft die Vorbedingung und zeigt den Toast.

**Alte Test-Dateien:** `guild-tech.spec.js` und der überwiegende Teil von `guild-tech-ext.spec.js` rufen `window.bkmpGuildTechUpgrade()`/`bkmpGuildGetTechLevels()` auf, die beim v3-Cutover vollständig aus `supabase.js` entfernt wurden (das alte SQL-RPC/die alte Tabelle `guild_tech_levels` existieren serverseitig bewusst weiter, werden aber von keinem Client-Code mehr aufgerufen) — beide Dateien komplett als `test.describe.skip(...)` markiert (mit erklärendem Kommentar, ursprünglicher Dateikopf zur Referenz erhalten), bewusst NICHT gelöscht (dokumentieren die Herleitung des jetzt abgelösten Systems). `guild-tech-ext-readside.spec.js`s Read-Side-Tests (Brutbeschleuniger/Schmiede/Autokauf/Aufstieg/Streak-Schutz) blieben dagegen größtenteils GÜLTIG und AKTIV — sie prüfen nur den (unveränderten) `bkmpGuildTechBonus()`-Cache-Leser per direkter Cache-Injektion, kein Aufruf der entfernten Funktion. Nur der "Nachtwache"-Block (seedete `guild_tech_levels` direkt für den jetzt umgestellten Offline-Deckel-Test) wurde auf die neuen Tabellen (`guild_tech_nodes`/`guild_tech_progress`) umgestellt, Assertion blieb inhaltlich gleichwertig (tier=4 · effect_per_tier=2,5 = 10h Bonus, äquivalent zum alten level=4 · 0,5 = 2h für diesen Testfall).

### Verifikation

- **Stufe 1** (`guild-tech-tree.spec.js`, 9 Tests, reine RPC-Ebene): Wurzel-Beitrag erhöht Fortschritt + zieht eigenes Gold ab, keine Rollenprüfung (Member darf), gesperrter Knoten lehnt mit `prereq_not_met` ab, vollständiger Ausbau schaltet Folgeknoten frei, 5/Tag-Limit + Regeneration, `insufficient_gold` verbraucht trotzdem einen Versuch, Rangliste zählt mit, `invalid_node`/`not_in_guild`/ausgeloggt abgelehnt.
- **Stufe 2** (`guild-tech-tree-migration.spec.js`, 9 Tests): 19=11+8, keine hängenden Vorbedingungs-Referenzen, kein Knoten ist seine eigene Vorbedingung, alle `effect_type`-Strings eindeutig, Migrationsformel (0→0, exakt-max→voll, mittig→abgerundet nicht aufgerundet, `__paragon` ausgeschlossen) inkl. End-zu-Ende-Beweis gegen einen echten Mock-Store mit 4 Gilden bei unterschiedlichen Alt-Ständen.
- **Stufe 3** (`guild-tech-tree-ui.spec.js`, 5 Tests, 50x stabil auf `chromium-desktop`): leere Kategorie zeigt Leerzustand, 6 Beispielknoten mit korrektem gesperrt/verfügbar-Zustand, Klick auf gesperrten Knoten zeigt Toast statt Modal, Klick auf verfügbaren Knoten öffnet Modal mit korrekten Werten + Beitragen aktualisiert Gold/Fortschritt/Versuche + Modal bleibt offen (lebt in `document.body`, ein Panel-Re-Render löscht es nicht), Rangliste zeigt den Beitragenden.
- Breiter Regressionslauf (`guild-tech*.spec.js` + `arena.spec.js` + `raid.spec.js` + `guild-clan-arena.spec.js` + `offline-afk.spec.js` + `dungeon-key-prestige-bonus.spec.js`, alle 3 Projekte, `--workers=1`): 232 bestanden, 107 korrekt übersprungen, 0 unerwartete Fehlschläge. `node --check`/`eslint .` (0 Fehler, unverändert dieselben 2 vorbestehenden Warnungen) sauber. `scripts/static-checks.js`: 0 CRITICAL/HIGH/MEDIUM, 58 LOW/1 INFO (unverändert ggü. Baseline).
- **Stufe 4 (Abschluss):** vollständiger Regressionslauf der GESAMTEN Suite (`tests/e2e/`, ohne `@soak-long`/`@visual-baseline`, alle 3 Projekte, `--workers=1 --retries=0`, Exit-Code per `$?` direkt gelesen): **1100 bestanden, 337 korrekt übersprungen, 0 unerwartete Fehlschläge, 0 Flaky, Exit-Code 0, Laufzeit 52,2 Minuten.** `npm run qa:static` (statische Analyse + eslint + `npm audit --omit=dev`) danach erneut sauber (0 CRITICAL/HIGH/MEDIUM, 0 eslint-Fehler, 0 Produktions-Schwachstellen).
- **Beim finalen Durchlesen der SQL-Datei selbst noch einen echten Bug gefunden+gefixt** (nicht durch einen Test, sondern durch sorgfältiges erneutes Lesen vor dem Abschluss): die Alt-Daten-Migration (Abschnitt 9) nutzte `on conflict (guild_id, node_id) do UPDATE set tier = excluded.tier` — das ist zwar für den ERSTEN Lauf korrekt, aber das Katalog-Pflegeprinzip dieser Architektur sieht ausdrücklich vor, dass `guild_tech_nodes` später per einfachem `UPDATE` angepasst werden darf (z.B. Kosten/Effektwerte feinjustieren) — würde jemand dafür die GESAMTE Datei erneut ausführen, hätte Abschnitt 9 dabei jeden bereits über `guild_tech_contribute()` real erspielten Gilden-Fortschritt still auf den alten migrierten Wert zurückgesetzt. Fix: `do update` → `do NOTHING` — macht die Migration zu einem echten Einmal-Bootstrap, die gesamte Datei ist dadurch beliebig oft sicher wiederholbar, ohne je echten Spielerfortschritt zu gefährden. Kein Test betroffen (`guild-tech-tree-migration.spec.js` prüft nur die reine Formel, nicht das Konflikt-Verhalten bei einem zweiten Lauf).

**Nicht geprüft/offen:** `sql/20260731-guild-tech-tree-v2-foundation.sql` muss im Supabase-Dashboard ausgeführt werden, bevor das neue System live funktioniert (bis dahin bleibt das alte `guild_tech_levels`-System serverseitig unverändert nutzbar, aber vom Client nicht mehr aufgerufen — der Gilden-Tech-Tab würde ohne die Migration mit `invalid_node`/leerem Katalog fehlschlagen). Admin-Übersicht über alle Gilden-Technologie-Fortschritte gleichzeitig nicht gebaut (gab es auch im alten System nicht). Paragon/endlose Fortsetzung bewusst nicht Teil von v1 (siehe Nutzer-Entscheidung oben) — könnte als eigene, spätere Stufe nachgezogen werden, falls gewünscht.

**Nachtrag 3 (01.08.2026) - kritischer Produktions-Bug: JEDER Beitrag scheiterte live, nicht nur die Countdown-Anzeige aus Nachtrag 1/2:** nach der Migration meldete der Nutzer per Live-Screenshot "guild_tech_attempt_status 404 (Not Found)" - dauerhaft, auch nach `notify pgrst, 'reload schema'` (erste, falsche Vermutung: PostgREST-Schema-Cache veraltet - per eigenem curl-Test gegen die echte DB widerlegt, die Funktion antwortete für mich zuverlässig). Erst der vom Nutzer bereitgestellte echte Netzwerk-Antworttext brachte die tatsächliche Ursache: `{"code":"42883",...,"message":"function public.dungeon_regen_calc(smallint, timestamp with time zone, integer, integer) does not exist"}` - beide neuen RPCs (`guild_tech_contribute()` UND `guild_tech_attempt_status()`) riefen `dungeon_regen_calc(v_row.attempts, v_row.last_attempt_at, 14400, 5)` mit den letzten beiden Werten als nackte Zahlen-Literale auf; Postgres typisiert `5` ohne Kontext als `integer`, die tatsächlich live laufende Funktion akzeptierte an der Stelle aber keinen passenden impliziten Cast - ihr reales Parameter-Profil weicht dort von der lokalen Kopie in `sql/20260727-fix-dungeon-regen-fixed-slots-and-wire-prestige.sql` ab (exakt dieselbe Bug-Familie wie die frühere `dungeon_regen_calc`-Mehrdeutigkeit vom 26.07., diesmal ein reiner Typ- statt Überladungs-Konflikt). Vor dem Ausliefern per direktem curl gegen die echte Produktions-DB verifiziert (nicht nur vermutet): ein 2-Parameter-Aufruf ohne die Literale (nutzt `dungeon_regen_calc()`s eigene Standardwerte `p_interval_seconds default 14400`/`p_max_keys default 5`) lief sauber mit `200 OK` durch. Fix: `sql/20260801-fix-guild-tech-dungeon-regen-calc-mismatch.sql` (neu) - beide Funktionen mit `create or replace`, nur der jeweils eine `dungeon_regen_calc()`-Aufruf vereinfacht, sonst unverändert.

Direkt beim ersten Live-Test dieses Fixes deckte der Nutzer einen ZWEITEN, bis dahin komplett verdeckten Bug auf ("Beitrag fehlgeschlagen: structure of query does not match function result type") - `guild_tech_contribute()` kam vorher NIE bis zu dieser Stelle, da sie immer schon beim `dungeon_regen_calc()`-Aufruf abbrach. Ursache: die letzte `RETURN QUERY SELECT`-Spalte `v_calc.new_keys - 1` (smallint minus integer-Literal) wird von Postgres automatisch zu `integer` hochgestuft, die Funktion verspricht an der Stelle (`attempts_left`) aber `smallint`. Fix (dieselbe Datei, direkt nachgezogen): expliziter `(v_calc.new_keys - 1)::smallint`-Cast, keine weitere Änderung. Beide Teile zusammen vom Nutzer live bestätigt ("Hat geklappt").

**Lehre:** die lokalen SQL-Dateien in `sql/` sind eine Kopie der ABSICHT, nicht notwendigerweise byte-genau das, was tatsächlich live in Produktion läuft - besonders bei Funktionen, die (wie `dungeon_regen_calc()`) bereits mehrfach über mehrere Migrationen hinweg verändert wurden. Bei einem Live-Fehler, der wie "Funktion nicht gefunden"/"Typ passt nicht" aussieht: IMMER zuerst den echten Netzwerk-Antworttext vom Nutzer holen (Status-Code allein reicht nicht, ein 404 kann PostgREST-Cache-Staleness UND ein echter `42883 undefined_function`-Fehler gleichermaßen sein) und den konkreten SQL-Aufruf gegen die echte Produktions-DB testen, BEVOR ein Fix als "sollte funktionieren" ausgeliefert wird.

Cache-Busting `?v=20260731-guildtechtreev3` für `supabase.js`, `js/systems/bkmp-guild.js`, `js/systems/bkmp-guild-tech.js` (neu), `js/ui/bkmp-ui-components.js` (index.html/admin.html/idle-stream-mini.html, jeweils wo eingebunden).

**Nachtrag (01.08.2026) - Countdown-Bug gefunden+gefixt:** Nutzer meldete per Screenshot "0/5 · Nächster in 00:00:00" direkt nach dem letzten Beitragsversuch des Tages. Per temporärem Diagnose-Test (`createQaServer` + 5x echter `guild_tech_contribute()`-Aufruf über echte Zeit) reproduziert und bewiesen, danach wieder gelöscht: `guild_tech_contribute()` liefert `seconds_to_next_attempt` **bewusst** immer als Platzhalter `0` zurück (Kommentar in der SQL-Funktion: "wird vom Client über guild_tech_attempt_status() frisch geholt") - `bkmpGuildTechHandleContributeClick()` patchte nach einem Beitrag aber bisher nur `.attempts`, `.secondsToNext` blieb auf dem alten "voll"-Stand (`0`) stehen. Der bereits bestehende Countdown-Ticker korrigierte das zwar automatisch ~1 Sekunde später (per eigenem Diagnose-Beweis bestätigt: sofort nach Verbrauch `00:00:00`, nach 3,5s korrekt `03:59:5x`, Server-Wahrheit durchgehend korrekt bei `14400`), aber genau dieser kurze falsche Zwischenzustand war es, den der Spieler sah und screenshotete. Fix: `bkmpGuildTechHandleContributeClick()` lädt den echten Stand jetzt sofort per `bkmpGuildTechGetAttemptStatus()` nach (Fallback auf das alte Patch-Verhalten nur bei einem fehlgeschlagenen Nachlade-Aufruf) - der allererste Render nach einem Beitrag zeigt dadurch immer schon die korrekte Wartezeit, kein Flackern mehr möglich. Neuer permanenter Regressionstest in `guild-tech-tree-ui.spec.js` (verbraucht alle 5 Versuche, prüft den Countdown-Text UNMITTELBAR danach, vor jedem möglichen Ticker-Tick) - 90/90 stabil über alle 3 Projekte. Cache-Busting `js/systems/bkmp-guild-tech.js?v=20260801-countdownfix1` (index.html/idle-stream-mini.html).

**Nachtrag 2 (01.08.2026) - zweiter, unabhängiger Countdown-Bug + fehlendes Cache-Busting, beide live vom Nutzer bestätigt:** Nutzer meldete "locahost bin ich" auf Nachfrage, danach per Live-Screenshot von bkinvestment.de ("habe comittet so sieht es auf bkinvestment aus") zwei gleichzeitige Symptome: (1) derselbe "0/5 · 00:00:00"-Text, diesmal aber DAUERHAFT hängend statt nur kurz, (2) der Baum selbst rendert unstyled/unpositioniert (Knoten fließen unter einem leeren SVG-Bereich statt absolut positioniert zu sein).

*Bug (2) - Root Cause:* die Stufe-3-CSS-Regeln für den Baum (`.bkmp-guild-tech-tree`/`-node`, style.css) waren im Code bereits korrekt, aber beim Einbauen wurde `style.css`s Cache-Busting nie erhöht (`?v=20260731-tooltipfix1`, ein Stand von einem völlig unabhängigen, längst abgeschlossenen früheren Fix) - Browser mit bereits gecachter alter CSS-Datei luden die neuen Baum-Regeln nie nach. Fix: `?v=20260801-guildtechtreecss1` in allen 5 HTML-Dateien gesetzt. Vom Nutzer per Folge-Screenshot bestätigt ("Ging wohl jetzt doch..^^").

*Bug (1) - Root Cause:* `bkmpGuildTechStartCountdownTicker()`s allererste Prüfung bricht sofort ab und startet NIE neu, falls `bkmpGuildTechAttemptStatus` `null` ist - das trifft nicht nur (wie in Nachtrag 1 behoben) kurz nach einem Beitrag, sondern auch dann, wenn der ALLERERSTE `guild_tech_attempt_status()`-Aufruf beim Öffnen des Panels selbst fehlschlägt (z.B. kurzer Netzwerk-Hänger) - dieser Fall hat kein Self-Heal, da der Ticker in diesem Fall gar nicht erst startet. Fix (`bkmpIdleRenderGildeTechPanel()`): schlägt der erste Ladeversuch fehl, zeigt die Zeile ehrlich "wird geladen…" statt einer erfundenen 0/5-Zahl, plus ein einmaliger automatischer Nachlade-Versuch nach 3s (`setTimeout` → erneutes `bkmpIdleRenderGildeTechPanel()`, nur falls der Tab noch aktiv ist).

Der neue Regressionstest dafür deckte einen Fehler in meinem EIGENEN Test auf, keinen zweiten App-Bug: die gemeinsam genutzte `login()`-Hilfsfunktion in `guild-tech-tree-ui.spec.js` öffnet den Gilden-Tech-Tab bereits selbst als letzten Schritt (damit die anderen 6 Tests der Datei das nicht wiederholen müssen) - ein erster Testentwurf simulierte einen einzelnen fehlgeschlagenen RPC-Aufruf per `context.route()`, klickte den Tab-Button aber zusätzlich noch einmal EXPLIZIT - dieser zweite Klick fand die simulierte "Giftpille" bereits verbraucht vor (vom versteckten ersten Klick in `login()`), lief also normal durch und zeigte sofort "5/5" statt des erwarteten "wird geladen"-Zustands. Aufgeklärt durch direktes Lesen des `login()`-Quellcodes (mehrere vorherige Diagnoseansätze - Routen-Treffer-Zähler, `addInitScript`-Listener-Zähler, `performance.now()`-Zeitstempel-Vergleiche, ein `console.log` mit `isTrusted`+Stack-Trace direkt im Tab-Klick-Listener von `idledorf.js` - waren dabei nicht zielführend, da keiner davon die tatsächliche Ursache in einer ANDEREN, bereits bestehenden Funktion vermutete). Fix am Test: die Route wird jetzt VOR `login()` registriert, kein zusätzlicher expliziter Klick mehr - der versteckte erste Klick in `login()` ist der einzige, den der Test braucht. Alle temporären Diagnose-Ausgaben (in `idledorf.js`s Tab-Klick-Listener und `supabase.js`s `bkmpGuildTechGetAttemptStatus()`) wieder entfernt. 40/40 stabil (20x Wiederholung beider Countdown-Regressionstests), 80/80 im breiteren Regressionslauf (`guild-tech-tree*.spec.js` + `network-guard.spec.js`, alle 3 Projekte, 22 korrekt übersprungen auf Mobile). Cache-Busting `js/systems/bkmp-guild-tech.js?v=20260801-countdownfix2`.

## Gilden-Technologie: neuer Zweig "Drachenzucht" (01.08.2026)

Direkter Folgewunsch nach den beiden Countdown-Fixes: "Eher ein kompletten Zweig für die Drachenzucht? Drachen Angriff % Verteidung % Leben % etc?" Kein neues Datenmodell nötig - `guild_tech_nodes`/`guild_tech_progress` (siehe Stufe 1 der v3-Foundation oben) sind bereits generisch für beliebig viele Knoten gebaut, reine Katalog-Erweiterung.

**Vier neue Knoten** (Kategorie `schlacht`, da sie Kampfkraft über den Begleitdrachen liefern, gleiches Prinzip wie Turm-Vorreiter/Stadtmauer/Kriegsrat): drei unabhängige Wurzeln `guild_zucht_kraft`/`guild_zucht_panzer`/`guild_zucht_vitalitaet` (je +8% Angriff/Verteidigung/Leben des Begleitdrachens pro Stufe, 5 Stufen, gleiche Kosten/Wachstum wie die bestehenden Schlacht-Wurzeln) + eine Zusammenführung `guild_zucht_meisterschaft` (+5% auf ALLE DREI Hauptwerte gleichzeitig pro Stufe, braucht alle drei Wurzeln voll ausgebaut als Vorbedingung - die erste 3-fache Zusammenführung im Baum, bisher gab es nur 1er/2er-Vorbedingungen).

**Code-Änderung** (`js/systems/bkmp-breeding.js`, `bkmpIdleDragonCompanionEffectTotals()`): vor dieser Änderung teilten sich Verteidigung UND Leben des Begleitdrachens einen einzigen `mainStatBoostPct`-Wert (nur aus Prestige gespeist) - für drei UNABHÄNGIGE Gilden-Tech-Knoten (Angriff/Verteidigung/Leben getrennt) musste die Funktion umgebaut werden, um drei getrennte Boost-Prozentsätze (`attackBoostPct`/`defenseBoostPct`/`hpBoostPct`) zu berechnen, statt einen gemeinsamen. Prestige-Verhalten bleibt dabei BYTE-FÜR-BYTE unverändert (derselbe `mainStatBoostPct`-Wert fließt weiterhin unverändert in alle drei ein) - nur additiv um `bkmpGuildTechBonus('guildCompanionAttackPct'/'guildCompanionDefensePct'/'guildCompanionHpPct')` + den Zuchtmeisterschaft-Capstone (`guildCompanionAllStatPct`, wirkt auf alle drei) erweitert, jeweils bei 200% gedeckelt (identischer Stil wie die bestehenden Prestige-Deckel). Neue effect_type-Werte, da diese drei Konzepte vorher nicht existierten - kein Wiederverwenden aus dem alten System möglich (anders als die 19 Knoten des v3-Cutovers, die 1:1 alte effect_type-Strings übernommen hatten).

**Zwei echte, beim Testen gefundene Bugs - beide in der eigenen neuen Testinfrastruktur, kein App-Bug** (per direktem Standalone-Diagnose-Skript gegen den echten Mock-Server isoliert, nicht geraten): (1) die RPC-Antwort von `guild_tech_contribute()` ist ein einzelnes Objekt, kein Array - ein erster Testentwurf griff fälschlich `res.json[0].new_tier` zu (funktioniert bei einem Array-Fallback nicht, `res.json[0]` ist bei einem reinen Objekt `undefined`) - identisches, bereits in `guild-tech-tree.spec.js` korrekt gelöstes Muster (`Array.isArray(res.json) ? res.json[0] : res.json`) übernommen. (2) ein manuell vorab in `guild_tech_contributor_attempts` gepushter Test-Zeile nutzte den Feldnamen `last_attempt_at` (wie in der SQL-Tabellenspalte) statt des vom Mock intern tatsächlich verwendeten `last_attempt_at_ms` (Millisekunden-Zahl) - führte zu `"Invalid time value"` bei jedem Aufruf. Der Handler legt die Zeile beim ersten echten Aufruf ohnehin selbst korrekt an - der manuelle Vorab-Push war unnötig und schlicht falsch, entfernt.

**Verifiziert:** 7 neue Tests (3 RPC-Ebene inkl. der neuen 3-fachen-Vorbedingungs-Prüfung, 4 Read-Seite über direkte Cache-Injektion analog `guild-tech-ext-readside.spec.js` - unabhängige Wirkung der drei Zweige, additive Zuchtmeisterschaft-Wirkung auf alle drei gleichzeitig, 200%-Deckel), 70/70 stabil bei 10x Wiederholung auf `chromium-desktop`, 97/97 im breiteren 3-Projekte-Regressionslauf (`guild-tech-drachenzucht`/`guild-tech-tree`/`guild-tech-tree-ui`/`guild-tech-tree-migration`/`network-guard.spec.js`). `node --check`/`eslint` sauber.

Cache-Busting `js/systems/bkmp-breeding.js?v=20260801-drachenzucht1` (index.html/admin.html/idle-stream-mini.html).

**Nicht geprüft/offen:** `sql/20260801-guild-tech-drachenzucht-branch.sql` muss im Supabase-Dashboard ausgeführt werden, bevor der neue Zweig live sichtbar wird (bis dahin bleibt der bestehende 19-Knoten-Baum unverändert nutzbar, die 4 neuen Knoten existieren nur lokal).

## Gilden-Technologie: neuer Zweig "Drachenzucht" (01.08.2026)

Auslöser: Live-Screenshot des Gilden-Tech-Baums, in dem Zuchtkraft/Zuchtpanzer direkt neben Turm-Vorreiter (5/5) unter derselben "Schlacht"-Kategorie hingen. Nutzerwunsch: "Können wir das mit Drachenzucht in einen Neuen Zweig gepackt?"

Beide Rendering-Bausteine waren bereits vollständig generisch gebaut (kein hartcodiertes 2-Kategorien-Limit): `bkmpUiGuildTechTreeHtml()` filtert Knoten rein über `n.category === category`, egal welcher String das ist. Nur zwei Stellen kannten wirklich nur zwei Kategorien: der `BKMP_GUILD_TECH_CATEGORY_LABEL`-Objekt (fehlender dritter Eintrag) und die Tab-Leiste selbst (zwei fest verdrahtete `<button>`-Zeilen statt einer Schleife über die Label-Tabelle) - letzteres beim Umbauen gleich mit aufgeräumt, ein künftiger vierter Zweig braucht jetzt nur noch einen neuen Eintrag in der Label-Tabelle.

**Umsetzung:** dritte Kategorie `drachenzucht` (Label "🐲 Drachenzucht") + Tab-Leiste datengetrieben aus `Object.keys(BKMP_GUILD_TECH_CATEGORY_LABEL)` gebaut. Da alle vier Vorbedingungen der Drachenzucht-Knoten ausschließlich untereinander liegen (Zuchtmeisterschaft braucht nur die drei anderen Zucht-Knoten, keine Verbindung zu Schlacht-Knoten), bleibt der Baum beim Umzug in die eigene Kategorie strukturell in sich geschlossen - keine über-Kategorien-Grenzen laufenden Vorbedingungs-Linien möglich.

**Live-DB-Migration nötig** (die 4 Knoten existieren bereits live unter `category='schlacht'`, siehe vorheriger Abschnitt): `sql/20260801-guild-tech-drachenzucht-own-category.sql` (neu, **noch nicht ausgeführt**) - erweitert die `guild_tech_nodes.category`-CHECK-Bedingung um `'drachenzucht'` (`drop constraint if exists guild_tech_nodes_category_check` + neu anlegen, Standard-Postgres-Name für eine inline-Column-Check-Bedingung) und setzt die 4 bereits existierenden Zeilen per `update ... where id in (...)` um. Die ursprüngliche `sql/20260801-guild-tech-drachenzucht-branch.sql` bleibt bewusst UNVERÄNDERT (historisches Abbild des ersten Ausführungsstands, nur ein erklärender Kommentar ergänzt) - ein hypothetischer künftiger Frischlauf auf einem neuen Projekt würde die Knoten wieder unter "schlacht" anlegen, die Nachtrag-Datei danach korrigiert das in beiden Fällen (bereits laufendes UND frisches System) gleichermaßen.

**Verifiziert:** 21/21 Tests grün über alle 3 Projekte (`guild-tech-drachenzucht.spec.js`, Testfixture-Kategorie mit umgestellt), `guild-tech-tree-ui.spec.js`/`guild-tech-tree.spec.js`/`guild-tech-tree-migration.spec.js` unverändert grün (85 Tests, korrekte Mobile-Skips). `scripts/static-checks.js` unverändert (0 CRITICAL/HIGH/MEDIUM). `node --check`/`eslint` sauber.

Cache-Busting `js/systems/bkmp-guild-tech.js?v=20260801-zuchtcategory1` (index.html/idle-stream-mini.html - admin.html bindet diese Datei nicht ein).

**Nicht geprüft/offen:** `sql/20260801-guild-tech-drachenzucht-own-category.sql` muss im Supabase-Dashboard ausgeführt werden - bis dahin bleiben die 4 Knoten live weiterhin sichtbar unter "Schlacht" (kein Fehlerzustand, nur noch nicht die gewünschte eigene Kategorie).

## Raid/Weltboss: Tap-Schaden-Zähler zeigte nie einen Wert (01.08.2026)

Spieler-Nachfrage anhand eines Screenshots der Raid-Kampfleiste: "Was ist das eigentlich beim stündlichen Raid boss? Auto kampf? Rote Symbol.. Die Klickzähler? der nicht funktioniert?" — Anlass für eine Bestandsaufnahme der drei Elemente (🟢 Auto-Kampf-Hinweistext, ⚔️ echter Angreifen-Knopf, 👆 Tap-Schaden-Zähler), dabei einen echten, bestätigten Bug im dritten Element gefunden.

**Root Cause:** `bkmpRaidRenderCombat()` (`js/systems/bkmp-raid.js`) aktualisierte `#raidTapDamagePill`/`#raidRewardsStrip` bisher hinter `if (window.BKMP_APP_MODE)` — der dortige Kommentar behauptete, die Elemente existierten auf der normalen Website nicht ("auf der Website ohne Wirkung"). Das stimmt seit Redesign Phase 5 (17.07.) nicht mehr: `html.bkmp-app-mode` ist die CSS-Klasse für OPTIK und seitdem auf JEDER Seite aktiv, `window.BKMP_APP_MODE` blieb aber das engere, nur unter `/app`/`?app=idledorf` wahre VERHALTENS-Flag (bewusste Trennung, siehe Kommentar in `index.html`) — die beiden sind seitdem NICHT mehr dasselbe. `.raid-action-bar` samt aller drei Elemente ist dadurch auf der normalen Website längst sichtbar und stilistisch komplett fertig, nur diese eine Anzeige-Aktualisierung lief nie. Jeder Klick zählte serverseitig+intern (`bkmpRaidTapDamageSession`) korrekt mit (unconditional, kein Gate) — nur die "0" auf dem Bildschirm aktualisierte sich nie. **Exakt dasselbe Bugmuster wie der bereits am 19.07. gefixte `#raidAttackBtn`** (siehe Kommentar in `idledorf.js:3161-3180`) - beim damaligen Fix wurde nur der Knopf selbst unconditional verdrahtet, die Zähler-Anzeige direkt daneben blieb übersehen.

**Fix:** dieselbe `if (window.BKMP_APP_MODE)`-Klammer entfernt, durch reine Elementprüfung ersetzt (identisches Prinzip wie beim Knopf-Fix). Beim eigenen Verifizieren zusätzlich ein reines Mess-Artefakt gefunden (kein zweiter Bug): eine alte, bereits offene Browser-Tab-Kopie hatte die alte `bkmp-raid.js` gecacht - ein eigenes, vergessenes Cache-Busting-Versäumnis (die Datei hatte noch `?v=20260724-raidsyncfix1`), nicht Teil des eigentlichen Fehlers. Nach dem Cache-Bust + echtem Reload bestätigt: Zähler zeigt korrekt `54.3K` statt stehenbleibendem `0`, auch mit `window.BKMP_APP_MODE===false`.

**Verifiziert:** neuer Regressionstest (`tests/e2e/raid-tap-counter-appmode.spec.js`, 2 Tests - Zähler aktualisiert sich trotz `BKMP_APP_MODE===false`, neue Raid-ID setzt korrekt auf 0 zurück), 20/20 stabil bei 10x Wiederholung, 6/6 über alle 3 Projekte. Voller `raid.spec.js`-Regressionslauf (54 Tests, 3 Projekte) unverändert grün. `eslint`/`node --check` sauber.

Cache-Busting `js/systems/bkmp-raid.js?v=20260801-tapcounterfix1` (index.html/admin.html/idle-stream-mini.html).

## Nachtrag: Drachenlager-Button-Ausrichtung (01.08.2026) — CLAUDE.md-Eintrag nachgezogen

Bereits in `CHANGELOG.md` dokumentiert, hier nachträglich der Root-Cause-Detaileintrag: Spieler-Screenshot mit rot markierten "Als Begleiter"/Stern/Papierkorb-Knöpfen zeigte sie je nach Kartenhöhe "kreuz und quer" statt auf einer geraden Linie ("Das alle 'Als begleiter etc mit dem Mülleimer etc' unten am Balken sind und nicht kreuz und quer"). `.idle-skin-grid` streckt bereits alle Karten einer Reihe auf gleiche Höhe (`align-items:stretch`), `.idle-skin-card` ist bereits eine Flex-Spalte - nur `.idle-dragon-actions-row` selbst wanderte bisher NICHT ans untere Kartenende, sondern blieb direkt unter dem letzten variablen Inhalt (Substats/Aufsteigen-Knopf, je nach Drache unterschiedlich viele/keine). Fix: `margin-top: auto` (schluckt in einer Flex-Spalte den gesamten übrigen Platz VOR dem Element) statt `margin-top: 0.5rem` - identisches, bereits an mehreren Stellen dieses Projekts etabliertes Muster (`.idle-skin-afford`, `.idle-skin-locked-hint`). Verifiziert per `getBoundingClientRect()`-Injektion (kein echter Login, Sicherheitsregel): identischer 17px-Abstand zur Kartenunterkante bei Karten mit 0 und mit 4 Substats, screenshot-bestätigt.

Cache-Busting `style.css?v=20260801-actionrowalign1` (alle 5 HTML-Dateien).

## Neue Egg/Baby-Freilassen-Option + Drachen-Aufstieg für alle Seltenheiten (02.08.2026)

Zwei getrennte, direkt nacheinander eintreffende Spieler-Wünsche, beide im Drachenzucht-Bereich (`js/systems/bkmp-breeding.js`). **Nichts committed/gepusht durch diese Session bewusst ausgelöst, keine SQL/DB-Änderung nötig (rein clientseitig + eine generische, bereits bestehende DELETE-Funktion für `player_dragon_eggs`).**

**Teil A — Ei/Baby freilassen** (Screenshot des Eierlagers/der Fütterung: "eine Option mit einbauen das man 'Eier oder baby' löschen / Freilassen kann. Aber bitte nur kleiner süßer Mülleimer symbol vllt oben rechts im Kasten?"):
- Neue `deletePlayerDragonEgg(eggId)` (supabase.js) — reines `delete().eq('id', eggId)` auf `player_dragon_eggs`, identisches Muster wie das bereits bestehende `releasePlayerDragon()` für `player_dragons`.
- Neue `bkmpDragonReleaseEgg(eggId)` (bkmp-breeding.js) — löscht bewusst nur EIN Ei (die representative `eggId`, die auch der bestehende "In freies Nest legen"-Knopf pro Spezies-Gruppe nutzt), nie den ganzen sichtbaren Stapel. Bestätigungsdialog zeigt Art/Seltenheit + wie viele weitere Eier derselben Art im Lager bleiben.
- Bestehender Freilassen-Klick-Handler für jugendliche/erwachsene Drachen (bisher komplett inline im `querySelectorAll('.idle-dragon-release-btn')`-Listener) in eine eigene Funktion `bkmpDragonConfirmAndRelease(dragonId)` ausgelagert — unverändertes Verhalten (Einzel-Bestätigung + zweite Bestätigung bei episch/legendär), jetzt aber auch vom neuen Baby-Papierkorb-Knopf wiederverwendbar, ohne die Sicherheitsabfrage ein zweites Mal zu schreiben. `dragon.stage==='baby'` lieferte im bestehenden Beschreibungstext bereits korrekt "Baby" — keine Anpassung an der Textlogik nötig.
- Neue, kleine runde Badge-Optik `.idle-dragon-mini-delete-btn` (24px Basis, top-right, `position:absolute`) — bewusst NICHT dieselbe CSS-Klasse wie `.idle-dragon-release-btn` (dessen `html.bkmp-app-mode .idle-dorf-panel .idle-dragon-release-btn`-Regel erzwingt dort 44×44px für die Zeilen-Optik der erwachsenen Karten, würde die kompakte Ecken-Badge sonst aufblähen). **Echter Bug beim eigenen Verifizieren gefunden+gefixt:** ein generisches `html.bkmp-app-mode .idle-dorf-panel button`-Reset (14px Radius, `.75rem 1rem` Padding, 46px Mindesthöhe — für normale Text-Buttons gedacht, seit Phase 5 praktisch immer aktiv, siehe wiederkehrende Architektur-Anmerkung ganz oben in dieser Datei) hat höhere Spezifität als eine einzelne Klasse und hätte die runde Mini-Badge sonst wieder zu einem eckigen Vollformat-Knopf gemacht — per `!important` auf `border-radius`/`padding`/`min-height` in der neuen Regel selbst behoben (gleiches, bereits mehrfach in dieser Datei dokumentiertes Muster wie bei `.idle-dragon-fav-btn`/`.idle-guild-chat-delete-btn`). Eigene, kleinere App-Modus-Größe (30px statt 44px) ergänzt, passend zur "kleiner süßer"-Vorgabe.
- `.idle-dragon-baby-card` bekam `position: relative` ergänzt (fehlte bisher, war nie nötig, da die Karte bisher kein absolut positioniertes Kind hatte).

**Teil B — Drachen-Aufstieg für alle Seltenheiten** (Screenshot einer legendären "Phil"-Karte mit "Aufsteigen (0/5)": "Die Funktion für alle Drachen freischalten nicht nur legendäre.. 'Aufsteigen' 5 der gleichen drachen verbinden"): `bkmpDragonCanAscend()` hatte als einzige Einschränkung `species.rarity === 'legendaer'` — alles andere (Fodder-Suche, Kosten 150.000 Gold, Bonus +10%/Stufe, Deckel 5 Stufen) war bereits vollständig seltenheits-agnostisch. Fix: der Rarity-Filter wurde ersatzlos entfernt — "5 der gleichen Drachen verbinden" entspricht exakt dem bereits bestehenden Deckel (bis zu 5 Stufen = bis zu 5 verbrauchte Opfer-Kopien über den vollen Ausbau), jetzt nur nicht mehr auf Legendäre beschränkt. Kommentar-Block über der Konstante von "Legendaeren-Aufstieg" auf die neue, allgemeinere Beschreibung aktualisiert.

**Verifiziert (Browser-Injektion ohne echten Login, Sicherheitsregel — Spezies-Katalog per anonym lesbarer `loadDragonSpeciesCatalog()` echt geladen, nur Ei-/Drachen-/Zustands-Arrays gemockt):** Papierkorb-Knopf 30×30px, `border-radius:50%` (nicht das generische 14px), korrekt oben rechts positioniert bei Ei- UND Baby-Karten (auch beim Spezies-unbekannt-Fallback); Klick öffnet den echten Bestätigungsdialog mit korrektem Text (Art/Seltenheit/verbleibende Anzahl bei Eiern); Abbrechen ändert nichts; Screenshot-bestätigt (kleines rundes Icon, keine Überlappung mit Ei-Bild/Beschreibung). Ascend-Button rendert jetzt nachweislich auch für eine STANDARD-Art (`ascendStdVisible:true`, Text "🌟 Aufsteigen (0/5)"), Legendäre unverändert funktionsfähig (Regressionsbeweis).

**Automatisierte Tests (`tests/e2e/dragon-lifecycle-release.spec.js`, neu, 9 Tests):** Ei-Karte zeigt genau einen gruppierten Knopf bei zwei gleichartigen Eiern, korrekte Position/Form (`border-radius:50%`, Abstand <20px zu Ecke), Abbrechen löscht nichts; Bestätigen löscht exakt EIN Ei (server-seitig UND client-seitig geprüft, die jeweils ANDERE ID bleibt); Spezies-unbekannt-Fallback-Karte hat ebenfalls einen funktionierenden Knopf; Baby-Karte nutzt nachweislich dieselbe Bestätigen-Logik wie erwachsene Drachen (Titel "Drachen freilassen?", Server-Zeile wirklich gelöscht, nicht nur lokal ausgeblendet); Standard-Art kann jetzt aufsteigen (echter Gold-Abzug 150.000, Fodder server-seitig verbraucht, `ascension_level` steigt, `main`-Drache bleibt), Legendäre bleibt als Regressionsbeweis parallel funktionsfähig; ohne zweite Kopie zeigt der Standard-Drache korrekt den bereits bestehenden "brauchst eine zweite erwachsene..."-Hinweis statt aufzusteigen. 6/6 auf `chromium-desktop`, korrekt auf Mobile übersprungen (echte Desktop-Tab-Klicks, gleiches Muster wie `runes.spec.js`), 60/60 stabil bei 10× Wiederholung. `node --check`/`eslint` sauber, `package.json`s `qa:features` um die neue Datei erweitert.

Cache-Busting `js/systems/bkmp-breeding.js?v=20260802-eggreleasefix1`, `supabase.js?v=20260802-eggreleasefix1` (index.html/admin.html/idle-stream-mini.html), `style.css?v=20260802-eggreleasefix2` (alle 5 HTML-Dateien).

## Nachtrag: Changelog-Ungelesen-Punkt (02.08.2026)

Screenshot des öffentlichen Changelog-Panels: "Das hier wenn wir was neues Posten oben bei Changelog eine kleiner Roter Punkt kommt wenn was neues gibt?" Neue `bkmpChangelogGetLastSeenId()`/`bkmpChangelogSetLastSeenId()`/`bkmpChangelogUpdateUnreadBadge()`/`bkmpChangelogMarkAllSeen()` (`js/ui/bkmp-changelog.js`) — vergleicht die `id` (UUID) des neuesten geladenen Eintrags (`loadChangelogEntries()` sortiert bereits absteigend nach `entry_date`/`created_at`) gegen eine einzelne `localStorage`-ID, kein neuer Server-Zustand nötig. `bkmpChangelogMarkAllSeen()` läuft am Ende von `bkmpChangelogOpen()`; ein proaktiver Hintergrund-Load direkt in der `bkmpChangelogInit()`-IIFE aktualisiert den Punkt bereits vor dem ersten Öffnen (Spieler sieht den Punkt, ohne das Panel je geöffnet haben zu müssen). Neuer `<span class="chl-unread-dot">`-Badge im bestehenden Changelog-Button (`#changelogButton`), `--color-danger`-Token (Phase 3, design-tokens.css — bis dahin an keinem echten Button verdrahtet, erster echter Einsatz).

**Verifiziert (Browser, echte Produktionsdaten - 25 echte Changelog-Einträge, keine Login-Notwendigkeit da öffentlich lesbar):** Punkt erscheint korrekt bei ungesehenem neuestem Eintrag, verschwindet beim Öffnen des Panels, bleibt über einen normalen Reload hinweg korrekt versteckt (ein anfänglicher Fehlalarm - Punkt schien nach `navigate(...,{force:true})` wieder aufzutauchen - stellte sich als Werkzeug-Artefakt heraus: `force:true` leert `localStorage` der Test-Tab, kein echter Bug; mit normalem `navigate()` korrekt bestätigt).

Cache-Busting `js/ui/bkmp-changelog.js?v=20260801-unreaddot1`, `style.css?v=20260801-changelogdot1` (index.html; style.css seitdem durch die Egg-Release-Arbeit weiter auf `?v=20260802-eggreleasefix2` erhöht).

## 6 neue Zucht-Drachenarten "NeueNeueDrachen" (02.08.2026)

Nutzer-Auftrag in zwei Nachrichten: erst 18 Bilder direkt im Chat gepostet ("so kommen neue Drachen hinzu. Epische Drachen -> Fynnow Drache, Vulkarion Drache, Bloodterion Drache, Gravoryx Drache"), dann "Legendäre Drachen -> Lohendrache, Darknisdrache". Da mehr Bildmotive als Namen sichtbar waren, erst nachgefragt, welches Bild zu welchem Namen gehört — dann lieferte der Nutzer stattdessen den fertigen Ordner (`C:\Users\David\Downloads\NeueNeueDrachen.zip`), dessen Dateinamen die Zuordnung bereits eindeutig klärten ("BloodterionEi.png", "Baby Fynnow.png", "Darknis Erwachsen.png", ...) — die vorherige Rückfrage war dadurch hinfällig, keine Ratearbeit mehr nötig.

**Umsetzung (reine Content-/Asset-Erweiterung, keine Code-Änderung):** `dragon_species` ist bereits vollständig datengetrieben (siehe Kommentar in `sql/supabase-dragon-species-neue-modelle.sql`, 18.07.) - `bkmpDungeonRollEgg()` nimmt jede aktive Art passender Seltenheit automatisch in den gewichteten Ei-Pool auf, keine Extra-Verdrahtung im JS nötig. Bestätigt per Grep: keine hartcodierte Artenliste im Projekt (nur unabhängige "Phil"-Easter-Egg-Referenzen in `bkmp-site.js`, nichts mit dem Zucht-Rollmechanismus verbunden).

- **6 neue Arten**, Werte 1:1 von der bereits etablierten Stufe übernommen (identisches Muster wie Byte/Enderdrachen/Kaledoss/Nytherion/Phil): **Fynnow, Vulkarion, Bloodterion, Gravoryx** (episch: `brood_seconds=10800, sacrifice_gold/crystals=0, growth_points_required=2000, battle_xp_required=15000, is_multi_stat=false, sub_stat_count 3-4`), **Lohendrache, Darknisdrache** (legendär: `brood_seconds=27000, sacrifice_gold=500000, sacrifice_crystals=200, growth_points_required=6000, battle_xp_required=50000, is_multi_stat=true, sub_stat_count 4-5`, 1:1 von phil/zerathor/yakshadrache). `egg_source='event'` für alle 6 (gleiches Muster wie alle bisherigen Batch-Ergänzungen seit Kora-/Hakudrache). `sort_order` setzt hinter phil (17) fort: 18-23.
- **Bild-Pipeline:** ZIP enthielt 24 Bilder (Ei direkt im Wurzelordner, Baby/Jugentlich/erwachsen als Unterordner) mit eindeutigen, aber uneinheitlich benannten Dateien (`BloodterionEi.png` vs. `Baby Bloodterion.png` vs. `Erwachsener Bloodterion.png`) - alle per Skript in die projektübliche Struktur (`assets/dragons/breeding/{egg,baby,teen,adult}/<id>.png`) umsortiert+umbenannt. `sharp` war lokal nicht installiert (einmaliges Autoren-Werkzeug, nicht Teil des Projekts) - per `npm install sharp --no-save` direkt im Projektordner nachgeholt (node_modules ist gitignored, kein Risiko). `scripts/optimize-images.mjs` (unverändert, Phase 1) gegen einen isolierten Staging-Ordner gelaufen (480px, WebP-Qualität 82) statt gegen den ganzen `assets/dragons/breeding`-Ordner, um die 17 bereits vorhandenen Arten nicht unnötig erneut zu verarbeiten - Ergebnis 62,1MB → 1,5MB WebP (+2,1MB PNG-Fallback), dann alle 72 Dateien (24 Original + 24 `-web.png` + 24 `-web.webp`) in den echten Assets-Ordner kopiert.
- **Verifiziert:** alle 24 WebP-Dateien per direktem `fetch()` gegen den lokalen Server abgerufen (200 OK, plausible Dateigrößen 44-95KB je Datei, identisch zur Optimizer-Ausgabe) - bestätigt, dass Kopiervorgang/Umbenennung nichts beschädigt haben und `bkmpDragonThumbHtml()`s automatische `-web.webp`/`-web.png`-Ableitung (`bkmpDragonImgVariants()`) korrekt greifen wird, sobald die SQL live ist. Kein bestehender Test hardcodiert eine Arten-Anzahl oder -Liste (geprüft, keine Anpassung nötig).

Cache-Busting nicht nötig (reine SQL+Asset-Änderung, kein JS/CSS geändert).

**Nicht geprüft/offen:** `sql/20260802-dragon-species-neue-neue-drachen.sql` muss im Supabase-Dashboard ausgeführt werden, bevor die 6 Arten live im Ei-Dungeon-Pool auftauchen können - bis dahin bleibt der bestehende Artenkatalog unverändert.

## Prestige-Knoten "Schlüsselmeister" entfernt (03.08.2026)

Nutzerwunsch, mitten in einem anderen Auftrag geäußert: "wenn du dabei bist diesen Skill bitte entfernen bein Prestige.. wir haben Feste Schlüssel Zeiten. da bringt so ein skill nichts" (Screenshot: "Schlüsselmeister Rang 11/30, +3% schnellere Schlüssel-Regeneration pro Rang").

**Hintergrund:** `sql/20260727-fix-dungeon-regen-fixed-slots-and-wire-prestige.sql` hatte Schlüsselmeister server-seitig verdrahtet - ein aktiver Rang schaltete den Spieler dabei bewusst auf ein PERSONALISIERTES, rollierendes Intervall um (dort selbst dokumentiert: "ein individuell verkürztes Intervall kann nicht mehr auf demselben globalen Raster liegen"). Genau das widerspricht aber dem ursprünglichen 16.07.-Wunsch ("Alles auf 0/4/8/12/16/20 Uhr skaliert" - EIN gemeinsames Raster für ALLE Spieler). Der Nutzer hat diesen Zielkonflikt jetzt zugunsten des ursprünglichen, gemeinsamen Rasters aufgelöst: personalisierte Abweichungen soll es nicht mehr geben.

**Umsetzung (identisches Rückerstattungs-Prinzip wie bei der Schlüsselbund-Downgrade-Migration vom 31.07., nur vollständige Entfernung statt Kappung):**
- `js/systems/bkmp-prestige.js`: Knoten `schluesselmeister` komplett aus `BKMP_PRESTIGE_UPGRADES` entfernt (nicht mehr kaufbar/sichtbar). Neue `bkmpPrestigeMigrateSchluesselmeisterRemoval()` — erstattet jedem Spieler mit Bestandsrang (Basis UND Paragon) die dafür ausgegebenen Prestige-Punkte exakt zurück (`bkmpPrestigeTierDef('MEDIUM')` liefert dieselbe Kostenformel, die der Knoten hatte, solange er noch im Katalog stand — kein Schätzwert). Idempotent, aufgerufen an denselben zwei Stellen wie die Schlüsselbund-Migration (`idledorf.js`, Login + "Sitzung anderswo übernommen"-Abgleich).
- `sql/20260803-remove-schluesselmeister-fixed-slots-only.sql` (neu, **noch nicht ausgeführt**): `dungeon_regen_calc()` nutzt jetzt ausnahmslos die feste Slot-Logik (kein "Standardfall vs. Schlüsselmeister-Fall" mehr, `p_interval_seconds` bleibt nur noch für Aufrufer-Kompatibilität in der Signatur, wird für das Verhalten nicht mehr ausgewertet). `dungeon_get_all_status()`/`dungeon_consume_key()` lesen nur noch `schluesselbund` (Deckel-Anhebung bleibt unverändert), nicht mehr `schluesselmeister`.
- `tests/mock/rpc-engine.js`: `dungeonRegenCalc()`/`dungeonKeyBonusForName()` entsprechend vereinfacht (keine personalisierte Verzweigung mehr).
- `tests/e2e/dungeon-key-prestige-bonus.spec.js`: die 3 Schlüsselmeister-Wirksamkeits-Tests zu Wirkungslosigkeits-Beweisen umgeschrieben (selbst mit Bestands-Zuteilung inkl. Rang 30 + Paragon 1000 bleibt das feste Raster unverändert) + neuer Migrations-Test (exakt der reale Blacklaier1337-Bestand aus der Anti-Cheat-Untersuchung vom selben Tag: Rang 30 + Paragon 2). Schlüsselbund-Tests unverändert (nicht betroffen).
- `tests/e2e/prestige-tree-v2.spec.js`: 3 neue Tests (Knoten wirklich weg + nicht kaufbar, Rückerstattung exakt+idempotent, unbeteiligter Spieler unangetastet) nach demselben Muster wie die bestehenden Schlüsselbund-Migrationstests. Ein bestehender Struktur-Test ("10 Knoten pro Hauptzweig") musste auf 9 für `runen_dungeon` angepasst werden.

**Verifiziert:** 48/48 Tests grün über alle 3 Projekte (`dungeon-key-prestige-bonus.spec.js` + `prestige-tree-v2.spec.js`), zusätzlicher Regressionslauf (`guild-tech-tree.spec.js` + `dungeon-time.spec.js` + `gem-dungeon-reward.spec.js`, die `dungeon_regen_calc()` ebenfalls nutzen) 25/25 grün. `node --check`/`eslint` sauber.

Cache-Busting `js/systems/bkmp-prestige.js?v=20260803-schluesselmeisterremoved1`, `idledorf.js?v=20260803-schluesselmeisterremoved1` (alle 3 einbindenden HTML-Dateien).

**Nicht geprüft/offen:** `sql/20260803-remove-schluesselmeister-fixed-slots-only.sql` muss im Supabase-Dashboard ausgeführt werden - bis dahin bleibt Schlüsselmeister server-seitig wirksam (Client zeigt den Knoten aber bereits nirgends mehr an, neue Investitionen sind nicht mehr möglich).

## Visueller Feinschliff der Kampfansicht (04.08.2026) — fertig, nichts committed/gepusht

Nutzer-Auftrag: kompletter, direkt im bestehenden Code umzusetzender Feinschliff der Kampfansicht (5 Abschnitte: Schadenszahlen/Gegnerwechsel/Trefferfeedback Spielerseite/Status-Einblendungen/Belohnungsanzeigen), mit festen Vorgaben (bestehende Kampfmechanik/Assets/Videos unverändert, keine neuen schweren Bibliotheken, CSS-Animationen bevorzugt, bestehende Funktionen zuerst suchen+erweitern statt Parallelsysteme, zuverlässige Bereinigung von Timern/Listenern/Elementen, keine Doppel-Initialisierung beim erneuten Öffnen des Kampf-Tabs). **Reine Anzeige-/Animationsschicht - keine Kampfformel/kein Spielwert/kein Asset angefasst, nichts committed/gepusht.** Git-Status vor Beginn: sauber, `main`.

**Analyse vor der Umsetzung (bestehende Funktionen gefunden, erweitert statt dupliziert):** `bkmpIdleSpawnBurnTick`/`bkmpIdleSpawnLightningBolt`/`bkmpIdleSpawnProjectile`/`bkmpIdleSpawnClickDamage` (alle `js/ui/bkmp-hud.js`) erzeugen bereits `.idle-dmg-float`-Elemente mit einem festen 800ms-`setTimeout`-Entfernen, aber ohne Streuung/Deckel. `bkmpIdleSpawnHitFlash()` toggelt bereits `.idle-hit-flash` für beide Seiten (Neustart-Trick "remove+offsetWidth+add", bereits etabliert). `bkmpIdleSpawnDragon()`/`bkmpIdleHandleDragonDefeated()`/`bkmpIdleHandleDefeat()` (`idledorf.js`) sind die drei zentralen Übergangspunkte. Ein bereits bestehendes, globales `bkmpRewardPresent()`-System (`js/ui/bkmp-reward-presenter.js`, Phase 5.5) wurde bewusst NICHT für das neue Status-Banner wiederverwendet (eigene Queue/Positionierung für andere Zwecke - Level-Aufstieg/Erfolge/Runen-Funde -, ein Umbau hätte Regressionsrisiko für viele andere Aufrufer gebracht). Wichtiger Bestandsfund: `html.bkmp-app-mode .idle-battlefield:has(.idle-hit-flash) { animation: appBattlefieldShake .32s ease; }` ist bereits unconditional aktiv (da `html.bkmp-app-mode` immer gesetzt ist) und wackelt bereits das GESAMTE Schlachtfeld bei JEDEM Treffer (Dorf oder Drache) - der Auftrag ("minimale Erschütterung") ist dadurch strukturell schon erfüllt, ein zweiter Wackler für die Dorf-Seite hätte gegen "keine Animationen stapeln" verstoßen und wurde bewusst NICHT hinzugefügt.

**1. Schadenszahlen (`js/ui/bkmp-hud.js`):** `bkmpIdleDmgJitter()` liefert einen kleinen Zufalls-Versatz (±13px x / ±5px y, per `margin-left`/`margin-top` addiert - fasst sich nicht mit der bestehenden `left`/`top`-Basisposition der `.idle-dmg-*`-Typklassen). `bkmpIdleTrackDmgFloat(targetId, el, ttlMs)` verfolgt pro Ziel (`idleDragon`/`idleVillage`) eine Liste aktiver Zahlen, deckelt bei `BKMP_DMG_FLOAT_MAX_PER_TARGET=5` - die älteste bekommt bei Überschreitung `.idle-dmg-float-fastout` (130ms statt der vollen 800ms) statt unbegrenzt anzuwachsen. Kritische Treffer bekommen eine eigene, kräftigere Animation (`idleDmgFloatCrit`, `.idle-dmg-crit` überschreibt `.idle-dmg-float`s `animation` per gleicher Spezifität + späterer Deklaration) - deutlich größer (Peak-Scale 1.35 vs. keine Skalierung normal), der bereits bestehende Funken-Ausbruch (`html.bkmp-app-mode .idle-dmg-crit::after`) bleibt unverändert bestehen und ergänzt sich dazu.

**2. Gegnerwechsel (`idledorf.js`/`bkmp-hud.js`):** `bkmpIdleHandleDragonDefeated()` löst auf dem WIEDERVERWENDETEN `#idleDragonSprite`-Element (bestätigt per Code-Lesen: `bkmpIdleApplyDragonSprite()` tauscht nur Hintergrundbild/Video, das Element bleibt bestehen) einen kurzen Ausblende-Puls aus (`.idle-dragon-defeat-out`, 0.22s), unmittelbar gefolgt vom synchronen `bkmpIdleSpawnDragon()`-Aufruf, der `.idle-dragon-spawn-in` (0.34s, Fade+Pop) hinzufügt. Da beide Klassen im normalen Fall (Kill→sofortiger Folge-Spawn) GLEICHZEITIG auf demselben Element stehen, entscheidet die CSS-Kaskade bei gleicher Spezifität über die spätere Quelldeklaration - `.idle-dragon-spawn-in` steht deshalb bewusst NACH `.idle-dragon-defeat-out` in `style.css`, damit im Regelfall die für den Spieler tatsächlich relevante Einblend-Animation gewinnt (das neue Sprite-Bild ist zu diesem Zeitpunkt technisch schon eingesetzt - ein "erst alten Drachen sichtbar ausblenden" wäre nur mit einer künstlichen Verzögerung zwischen Tod und Spawn möglich gewesen, was gegen die Vorgabe "kein langer Verzögerungseffekt" + das Risiko einer neuen Race-Bedingung bei sehr schneller Kampfgeschwindigkeit verstoßen hätte - bewusst nicht gemacht). `.idle-dragon-defeat-out` bleibt trotzdem eine eigenständig funktionierende Animation für den selteneren Fall eines Aufrufers ohne unmittelbaren Folge-Spawn. Kein Kampf-Fortschritt/keine Kill-Zählung/kein Server-Sync wurde durch die Animation verzögert oder verändert (rein kosmetisch, läuft nach der bereits abgeschlossenen Zustandsänderung).

**3. Trefferfeedback Spielerseite (`bkmp-hud.js`):** zusätzlich zum bereits bestehenden `.idle-hit-flash` (unverändert, beide Ziele) bekommt NUR das Dorf zwei weitere Effekte: eine rote Innen-Vignette auf `.idle-village-sprite` (`.idle-village-damage-pulse`, box-shadow inset) und ein kurzes Aufleuchten des HP-Balken-**Wrappers** (`.idle-hp-bar`, nicht der Füllstand selbst - der Wrapper hat `overflow:hidden` und begrenzt den Glow sauber, unabhängig vom aktuellen HP-Prozentsatz). **Echter, beim eigenen Testen gefundener Bug (per `getComputedStyle` bewiesen, nicht vermutet):** `#idleVillageSprite` trägt IMMER ein Inline-`style.animation` (`bkmpApplyVillageSkinToElement()`, `bkmp-cosmetics.js` - `"none"` für statische Dorf-Skins, ein echter Endlos-`steps()`-Loop für animierte Mehrfach-Frame-Skins wie Pilzdorf) - Inline-Styles schlagen jede Klassenregel ohne `!important`, die neue Puls-Animation wäre für JEDEN Skin komplett unsichtbar geblieben. Reines `!important` allein wäre aber gefährlich gewesen: da `.idle-village-damage-pulse` (wie `.idle-hit-flash`) nie automatisch entfernt wird, hätte ein EINZIGER Dorf-Treffer die echte Endlos-Ambiente-Animation eines animierten Skins für den Rest der Sitzung überschrieben. Fix: neuer `bkmpVillagePulseRemoveTimer` entfernt die Klasse exakt 420ms nach dem Hinzufügen wieder - live per Test bewiesen (eine simulierte Fake-Ambiente-Animation lief nach einem Treffer unverändert weiter).

**4. Status-Einblendungen (neu, `bkmpIdleShowCombatStatus(type, text, opts)`, `bkmp-hud.js`):** kurze, `pointer-events:none`-Texte in `#idleBattlefield` für Sieg (nur bei Boss-Siegen, sonst reiner Belohnungs-Chip-Spam bei der hiesigen Kampfgeschwindigkeit)/Bosskampf/Nächste Stufe (nur beim Überschreiten einer 10-Stufen-Akt-Grenze, bewusst KEIN Wiederaufleben des am 20.07. auf Nutzerwunsch entfernten "Akt X erreicht!"-Popups - dieses Banner ist deutlich leichter: kein Belohnungsblock, kein Klick zum Schließen)/Niederlage. `BKMP_COMBAT_STATUS_PRIORITY` (niederlage 4 > bosskampf 3 > sieg 2 > naechsteStufe 1) verhindert Überlappung: eine laufende wichtigere Meldung wird von einer neuen, unwichtigeren nicht unterbrochen (wird einfach verworfen), eine wichtigere ersetzt eine laufende unwichtigere sofort und sauber (Timer gecancelt, altes Element entfernt) - nie mehr als ein Banner gleichzeitig. Positioniert bei `top:3%` (oberhalb der bestehenden Belohnungs-Hochschweb-Anzeige bei `top:10%`, eigener höherer z-index, damit sich beide bei einem gleichzeitigen Bosssieg nicht gegenseitig unlesbar überdecken).

**5. Belohnungsanzeigen (`idledorf.js`):** der bereits bestehende `bkmpIdleRewardGained`-Event-Listener (Gold/XP-Chips) bekam einen Deckel (`BKMP_REWARD_FLOAT_MAX=3`) - bei schnell aufeinanderfolgenden Kills blendet die älteste Reihe beschleunigt aus (150ms) statt sich unbegrenzt zu stapeln; eine neue CSS-Variable `--rf-stack-offset` verschiebt noch aktive Reihen sichtbar nach oben, statt exakt übereinanderzuliegen.

**Reduced-Motion/Effekt-Schalter:** alle neuen Ein-Schuss-Animationen werden von der bereits bestehenden globalen `@media(prefers-reduced-motion:reduce){*{animation-duration:.001ms!important}}`-Kollektivregel automatisch erfasst; die drei `!important`-markierten "schneller ausblenden"-Klassen (`.idle-dmg-float-fastout`/`.idle-reward-float-fastout`/`.idle-combat-status-out`) brauchen wegen ihrer höheren Selektor-Spezifität eine explizite Zusatzregel (nach demselben, bereits im Projekt etablierten Muster wie die bestehende `.idle-dmg-float`-reduced-motion-Zeile). Bestehende Einzelschalter erweitert: `fx-off-dmgNumbers`/`fx-off-hitShake`/`fx-off-attackAnim` decken jetzt auch die neuen Klassen ab; das Status-Banner ist bewusst an KEINEN bestehenden Schalter gekoppelt (kein Toggle passt semantisch, das Banner ist Spielzustands-Feedback, keine reine Dekoration) - reagiert aber auf `data-fx="aus"` (sofort sichtbar, keine Animation) und reduced-motion.

**Tests (`tests/e2e/combat-visual-polish.spec.js`, neu, 10 Tests, Teststand A, läuft gegen die echte `bkmpIdleTick()`/`bkmpIdleHandleDragonDefeated()`-Logik):** Schadenszahlen-Deckel+Jitter+Aufräumen ohne DOM-Rest, Crit-vs-normal-Unterscheidung, Gegnerwechsel-Integrität (genau ein Gegner-Element, neuer Drache mit vollem statt übernommenem HP, sofort angreifbar, kein Schaden vom toten Vorgänger), Trefferfeedback inkl. des Skin-Ambiente-Regressionsbeweises, Status-Banner-Priorität+Klickbarkeit-bleibt-erhalten+Selbstauflösung, automatisches Bosskampf-Banner bei echtem Boss-Spawn, Belohnungs-Deckel ohne DOM-Rest, Effekte-Aus, Einzelschalter "hitShake". **30/30 grün über alle 3 Projekte** (chromium-desktop/mobile-small/mobile-large - deckt Testpunkt 12 "Mobilansicht" mit ab). `scripts/static-checks.js`: unverändert 0 CRITICAL/HIGH/MEDIUM (58 LOW/1 INFO). `eslint`/`node --check` auf allen 3 geänderten Dateien sauber.

**Voller Regressionslauf der gesamten bestehenden Suite** (`tests/e2e/`, `chromium-desktop`, `--workers=1`, ohne `@soak-long`/`@visual-baseline`): **479 bestanden, 33 korrekt übersprungen, 1 unerwarteter Fehlschlag, Laufzeit 16,6 Minuten** (der separat gezeigte "x"-Fehlschlag bei `save-load.spec.js`s Zwei-Tab-Test ist der bereits lange bekannte, bewusst per `test.fail()` verfolgte Zwei-Tab-Speicher-Race - kein neuer Fund). Der eine unerwartete Fehlschlag (`dungeon-time.spec.js` "frischer Spieler startet mit 5/5 Schluesseln je Dungeon-Typ", `TypeError: Cannot read properties of undefined (reading 'keys')`) wurde sofort isoliert nachgeprüft, nicht einfach als Flake abgetan: 10/10 Wiederholungen derselben Testzeile UND alle 13 Tests aus `dungeon-time.spec.js`+`dungeon-key-prestige-bonus.spec.js` gemeinsam liefen isoliert vollständig sauber durch - bestätigt dasselbe, bereits mehrfach in dieser Datei dokumentierte Ressourcenkonkurrenz-Flake-Muster dieser exakten Testdatei unter Volllast (nicht durch diese Session verursacht, keine echte Regression weder durch die Kampf-Feinschliff- noch durch die parallel abgeschlossene Schlüsselmeister-Entfernungs-Arbeit).

**Manuell im Browser verifiziert (kein echter Login möglich, Sicherheitsregel - injizierter Mock-Zustand über echte Render-/Kampf-Funktionen, `getComputedStyle`/CSSOM statt Screenshot):** Tab-Wechsel weg vom Kampf-Tab und zurück dupliziert kein Banner/keine Zahl (Kampf-Panel wird nur ein-/ausgeblendet, nie neu aufgebaut - keine Doppel-Initialisierung möglich, da keine der neuen Funktionen an einen Tab-Open-Hook gebunden ist, sondern ausschließlich an Kampf-Ereignisse selbst).

Cache-Busting `js/ui/bkmp-hud.js?v=20260803-combatpolish1`, `idledorf.js?v=20260803-combatpolish1`, `style.css?v=20260803-combatpolish1` (alle 5 HTML-Dateien, wo jeweils eingebunden).

**Bekannte Einschränkungen/offene Punkte:** kein echter Bildschirm-Screenshot des Vollbild-Kampf-Overlays möglich (bekannte, bereits mehrfach dokumentierte Werkzeug-Einschränkung dieses Projekts) - alle Behauptungen oben sind echte `getComputedStyle`/CSSOM-Messungen bzw. Playwright-Assertions gegen die echte Kampf-Logik, keine Schätzungen. Die Status-Banner-Position (`top:3%`) und die Belohnungs-Anzeige (`top:10%`) können sich bei einem gleichzeitigen Bosssieg auf sehr niedrigen Kampffeld-Höhen (unterhalb der dokumentierten `clamp()`-Untergrenze) noch leicht überlappen - bewusst nicht pixelgenau verhindert (seltener Fall, beide sind halbtransparente, unterschiedlich geformte Elemente, keine Lesbarkeits-Blockade). Kein Web-Worker/keine Server-seitige Kampfsimulation angefasst (außerhalb des Auftragsumfangs).

## Weltboss-Kampfleiste aufgeräumt + Ein/Aus-Schalter "Automatischer Bosskampf" (04.08.2026)

Zwei getrennte, direkt nacheinander eintreffende Spieler-Wünsche zum Raid/Weltboss-System. **Nichts committed/gepusht.**

**Teil A - Kampfleiste (ChronoKora, Screenshot der Raid-Ansicht):** "bei der Hand sollte eigentlich die Klicks gezählt werden und nicht der Damage.. der steht ja da drunter.. so das Rote Angriff Button brauchen wir doch garnicht können Drachen direkt anklicken oder leertaste.. Und Links Auto Kampf an? Ist doch egal.. ohne Klicken ist er eh nicht machbar das kann also irgendwie weg. Außer es hat einen wirklichen Nutzen."

Beide Behauptungen per Code-Lektüre geprüft, nicht blind übernommen:
- **Roter Angriff-Knopf bestätigt redundant:** `raidBossEl.addEventListener('click', bkmpRaidHandleBossClick)` UND der jetzt entfernte `raidAttackBtnEl.addEventListener('click', bkmpRaidHandleBossClick)` (beide idledorf.js) sowie die Leertaste (`document.addEventListener('keydown', ...)`, ruft bei aktiver Raid-Ansicht ebenfalls `bkmpRaidHandleBossClick()` auf) - alle drei exakt derselbe Funktion. Der Boss-Sprite selbst ist mit bis zu 380px Breite (`.raid-boss-sprite`) zudem deutlich größer als der 4,6rem-Knopf war - kein Argument für "größeres, verlässlicheres Tap-Ziel" übrig. Knopf komplett entfernt (`index.html`, Wiring-Zeilen in `idledorf.js`, `.raid-attack-btn`-Regeln in `style.css`).
- **"🟢 Auto-Kampf aktiv" ist NICHT bedeutungslos, Nutzer-Annahme widerlegt:** `bkmpRaidOwnTick()` (`bkmp-raid.js`) fügt tatsächlich alle `BKMP_RAID_TICK_MS` (2,5s) automatisch Schaden zu (`bkmpIdleDamageRoll` auf Basis der eigenen Stats, `submitRaidDamage(..., false)`) - komplett unabhängig davon, ob geklickt wird, exakt dieselbe Mechanik wie der normale Auto-Tick im Hauptkampf. Die Anzeige wurde bewusst NICHT entfernt, mit Begründung an den Nutzer zurückgemeldet statt stillschweigend zu löschen.
- **"👆"-Pille zeigte bisher die kumulierte Tipp-SCHADENSSUMME** (`bkmpRaidTapDamageSession`, += clickDamage pro Klick) - redundant zur bereits direkt darunter angezeigten Liste einzelner Trefferwerte. Umbenannt zu `bkmpRaidTapClickSession`, zählt jetzt `+= 1` pro Klick statt `+= clickDamage` - eine tatsächlich neue Information. Beide verbliebenen Pillen (`.raid-auto-pill`/`.idle-res-tap`) hatten bereits `flex:1 1 0` - ohne den Knopf dazwischen teilen sie sich die Zeile automatisch hälftig, keine Layout-Anpassung nötig. Beide Pillen bekamen zusätzlich ein `title`-Tooltip zur Erklärung.

**Teil B - Ein/Aus-Schalter (BagonTr01, Feedback-Discord):** "ist irgendwie ne Möglichkeit das man das automatische Bossbeitreten [beim stündlichen Weltboss] an und ausschalten kann?" Recherche bestätigte: der Prestige-Knoten "Automatischer Bosskampf" (`auto_raid_join_unlock`, `bkmp-prestige.js`) löst über `bkmpIdleRunAutomationToggles()` (idledorf.js, im echten 900ms-Kampf-Loop, alle 10s throttled) einen automatischen `bkmpRaidJoin()`-Aufruf aus, sobald ein Raid in der Vorbereitungsphase ist - seit dem Kauf immer und dauerhaft, ohne jede Abschalt-Möglichkeit (anders als die Runen-Hintergrund-Automatiken vom 27./28.07., die bereits einen lokalen Checkbox-Schalter haben).

**Umsetzung (identisches Muster wie die Runen-Hintergrund-Automatik-Schalter, aber ohne Bestätigungsdialog - nicht destruktiv):**
- `js/systems/bkmp-raid.js`: `BKMP_RAID_AUTO_JOIN_ENABLED_KEY` (localStorage) + `bkmpRaidAutoJoinEnabledGet()`/`-Set()`. **Default TRUE** (nicht FALSE wie bei den Runen-Schaltern) - damit sich am bisherigen Verhalten für alle, die den Knoten bereits gekauft haben, nichts ändert, bis sie aktiv abschalten.
- `idledorf.js`: `bkmpIdleRunAutomationToggles()`s Raid-Join-Bedingung um `bkmpRaidAutoJoinEnabledGet()` erweitert.
- `js/systems/bkmp-prestige.js`: neue Checkbox (`bkmpPrestigeRenderBranchGridHtml()`) direkt an der Knotenkarte für `automatischer_bosskampf`, sobald gekauft (`maxed`) - bewusst NICHT am Raid-Beitritts-Banner (das ist nur während der ~5-minütigen Vorbereitungsphase sichtbar, ein Schalter dort wäre nur in einem schmalen Zeitfenster erreichbar). Change-Listener in `bkmpIdleRenderPrestigePanel()` speichert+zeigt einen Toast.

**Tests:** `tests/e2e/raid-tap-counter-appmode.spec.js` (bestehende 2 Tests auf `bkmpRaidTapClickSession` umgestellt + 1 neuer Test "+1 pro Klick, nicht +Schaden"). `tests/e2e/prestige-automation-toggles.spec.js` um 2 Tests erweitert (Engine: Schalter aus→kein Beitritt trotz Freischaltung→wieder an→Beitritt greift erneut; UI: Checkbox erscheint erst nach Kauf mit Default AN, Klick speichert+persistiert über einen erneuten Panel-Render). Der UI-Test ist der einzige in dieser Datei mit einem echten Desktop-Tab-Klick (`#idleTabBtnPrestige`) - eigener inline `test.skip()` für `mobile-*`-Projekte ergänzt (identisches Muster wie `prestige.spec.js`, nicht die ganze Datei betreffend, da alle anderen Tests dort reine Funktionsaufrufe sind). 13/13 grün über alle 3 Projekte (2 korrekt übersprungen), 50/50 stabil bei 10x Wiederholung auf chromium-desktop. `scripts/static-checks.js`: unverändert 0 CRITICAL/HIGH/MEDIUM. `eslint`/`node --check` sauber.

Cache-Busting `js/systems/bkmp-raid.js?v=20260804-raidactionbar1`, `js/systems/bkmp-prestige.js?v=20260804-raidactionbar1`, `idledorf.js?v=20260804-raidactionbar1`, `style.css?v=20260804-raidactionbar1` (alle einbindenden HTML-Dateien).

## "Lager aufräumen"-Verkaufsleiste entfernt + Runen-Ladelimit wieder aufgehoben (04.08.2026)

Nutzer-Screenshot der Rarität-Knöpfe im Runen-Tab ("🧹 Lager aufräumen (alle Plätze): Gewöhnlich/Ungewöhnlich/Selten/Episch"): "Das wieder entfernen und alle Runen anzeigen lassen? Viele sind verwirrt und kennen sowas nicht." **Nichts committed/gepusht.**

**Wichtiger Kontext, dem Nutzer VOR der Umsetzung explizit mitgeteilt (siehe AskUserQuestion im Chat-Verlauf):** die Leiste war keine Spielerei, sondern die direkte Antwort auf einen echten Datenverlust-Vorfall (25.07.2026, "meine Runen sind komplett weg", bestätigt bei BagonTr01/Nilo3628 mit Lagern von 7.541 bzw. 15.632 Zeilen) - unbegrenztes Laden ließ die Server-Antwort bei so großen Lagern bei jedem Netzwerk-Hänger scheitern, der Client leerte danach still den kompletten lokalen Runenbestand. Der Fix war eine 300-Zeilen-Kappung des ungenutzten Lagers + genau diese Rarität-Verkaufsleiste als Werkzeug, um ein Lager selbst wieder unter die Grenze zu bringen.

**Recherche-Befund (per Agent, vor jeder Änderung):** die Knöpfe waren tatsächlich reine, SOFORTIGE Sammelverkaufs-Aktionen (mit Bestätigungsdialog) - kein Filter, keine Auswirkung auf die normale Browsing-Liste. Nichts an der aktuellen Beschriftung machte das erkennbar, was die gemeldete Verwirrung erklärt.

**Nutzer-Entscheidung (AskUserQuestion, 3 Optionen mit klar benanntem Risiko vorgelegt):** "Leiste entfernen, Ladegrenze komplett aufheben" - explizit die Option MIT dem höchsten Risiko (Reintroduktion des 25.07.-Vorfalls für Spieler mit sehr großen Lagern), bewusst statt der beiden vorsichtigeren Alternativen (nur Beschriftung klarer machen / Grenze nur anheben statt aufheben) gewählt.

**Umsetzung (exakt wie entschieden, keine eigenmächtige Abschwächung):**
- `supabase.js`: `loadUnequippedPlayerRunesCapped(name, limit)` → `loadUnequippedPlayerRunes(name)`, `.limit(...)` entfernt. `deleteRunesByRarity()` komplett gelöscht (einziger Aufrufer war die entfernte Verkaufsleiste).
- `idledorf.js`: `BKMP_RUNE_INVENTORY_CAP`-Konstante + die Kappungs-Erkennung (`bkmpIdleRuneInventoryCapped = ...`) im Ladeblock entfernt.
- `js/core/bkmp-idle-state.js`: `bkmpIdleRuneInventoryCapped`-Flag komplett entfernt (kein Konsument mehr).
- `js/systems/bkmp-runes.js`: `bkmpRuneSellAllByRarity()` komplett gelöscht. `bkmpRuneRetryLoad()` (der zweite, unabhängige Ladepfad für den "Erneut laden"-Knopf) ebenfalls auf `loadUnequippedPlayerRunes()` ohne Limit umgestellt. `bkmpIdleRenderRunenPanel()`: Rarität-Verkaufsleiste (HTML + Klick-Wiring) entfernt, Hinweis-Banner (`.idle-runen-cap-banner` → umbenannt `.idle-runen-error-banner`) zeigt nur noch echte Ladefehler, kein "es gibt noch mehr"-Zweig mehr. Die Zusammenfassungszeile ("N von M gesamt") verliert ihr "+"-Suffix.
- `style.css`: `.idle-runen-rarity-cleanup-row`/`-label`/`-sell-btn`-Regeln entfernt, `.idle-runen-cap-banner` → `.idle-runen-error-banner` umbenannt.
- **Bewusst UNVERÄNDERT gelassen** (das war nie die Ursache der Verwirrung, sondern die eigentliche, weiterhin wertvolle Fehler-Isolation): ausgerüstete Runen und das Lager laden weiterhin über zwei UNABHÄNGIGE `try`/`catch`-Blöcke (schlägt einer fehl, bleibt der andere erhalten), kein stilles Leeren bei einem Fehler (`bkmpIdleRuneLoadError` + sichtbarer Retry-Banner bleiben). `bkmpGetStoredMeltableRunes()`/`loadStoredRunes()` (die Hintergrund-Automatiken Auto-Legi-Aufwertung/Auto-Aufstieg/Auto-Verschmelzung) bleiben ebenfalls unverändert - die fetchen ohnehin schon immer uncapped frisch vom Server, waren nie Teil der Kappung.

**Tests:** `tests/e2e/rune-inventory-scale.spec.js` - der bisherige "Ladeobergrenze greift"-Test komplett neu geschrieben (beweist jetzt das Gegenteil: ein 394-Zeilen-Lager lädt vollständig, kein Banner, kein "+"), die komplette "Slot-übergreifender Sammelverkauf nach Seltenheit"-Testgruppe (3 Tests) entfernt (testete die gelöschte Funktion). `tests/e2e/rune-autofuse-datasource.spec.js`/`tests/e2e/rune-maximize-legendary.spec.js`: je ein "300er-Cache-Bypass"-Regressionstest entfernt - deren Konstruktion ("beweise, dass der lokale, gekappte Bestand bestimmte Runen garantiert ausschließt") ist ohne Kappung nicht mehr herstellbar; die eigentlich getestete Eigenschaft (Automatiken finden Kandidaten immer frisch vom Server, unabhängig vom lokalen Zustand) bleibt durch die jeweils anderen, unveränderten Tests in diesen Dateien abgedeckt. Mehrere stale Code-Kommentare (erwähnten noch die 300er-Kappung als aktuellen Zustand) korrigiert.

**Verifiziert:** 34/34 grün (komplette Runen-Testsuite, chromium-desktop). Live im Browser (kein echter Login möglich, Sicherheitsregel) mit injiziertem 350-Zeilen-Runenbestand bestätigt: alle 350 rendern im Panel, kein Fehler-Banner, keine Rarität-Verkaufsleiste im DOM, Kopfzeile zeigt korrekt "350 von 350 gesamt" ohne "+". Keine Konsolenfehler. `node --check`/`eslint` auf allen geänderten Dateien sauber.

Cache-Busting `supabase.js?v=20260804-runecapremoved1`, `idledorf.js?v=20260804-runecapremoved1`, `js/systems/bkmp-runes.js?v=20260804-runecapremoved1`, `js/core/bkmp-idle-state.js?v=20260804-runecapremoved1`, `style.css?v=20260804-runecapremoved1` (alle einbindenden HTML-Dateien).

**Bekanntes, bewusst akzeptiertes Risiko (nicht erneut zur Diskussion gestellt, da explizit vom Nutzer nach vollständiger Aufklärung entschieden):** Spieler mit sehr großen Runen-Lagern (mehrere tausend Zeilen) können erneut denselben Netzwerk-Timeout-artigen Fehlschlag wie am 25.07. erleben - die (unverändert gebliebene) Fehler-Isolation + der sichtbare Retry-Banner mindern die Auswirkung (kein stiller Datenverlust mehr, nur ein sichtbarer, behebbarer Ladefehler), verhindern das häufigere Auftreten selbst aber nicht.

## Dorf-Skins-Shop: Vorschau-Kachel + Lightbox (04.08.2026)

Spieler-Idee NikschiOG (Feedback-Board, Screenshot): "Es wäre cool wenn man bei den Dorf Skins eine Vorschau hätte, da man sonst nicht weiß was man kauft." Nach kurzer Rückfrage (Empfehlung: kleines statisches Vorschaubild pro Karte im Grid, live animierte/abspielende Ansicht nur in einer vergrößerten Lightbox - vermeidet, dass 7+ Videos gleichzeitig im Shop-Grid laufen) vom Nutzer bestätigt ("Ja machen wir so mit der Vorschau"). **Nichts committed/gepusht.**

**Umsetzung:**
- `js/systems/bkmp-cosmetics.js`, `bkmpApplyVillageSkinToElement()`: neue `options.forceStatic`-Option - nutzt exakt denselben "keine Animation/kein Video-`.play()`"-Zweig wie der bereits bestehende Effektmodus-Aus-Schalter (`bkmpFxVillageSkinAnimOff()`), aber unabhängig vom globalen Nutzer-Setting. Die kleinen Grid-Kacheln sollen NIE gleichzeitig alle Videos abspielen (Performance) - die vergrößerte Lightbox-Ansicht dagegen respektiert weiterhin ganz normal das echte Effektmodus-Setting (kein Zwangs-Play bei aktivem "Effekte aus").
- `bkmpIdleRenderSkinsPanel()`: jede Karte bekommt eine neue `.idle-skin-preview-thumb`-Kachel (`role="button" tabindex="0"`, Maus- UND Tastatur-Aktivierung) ganz oben, VOR Icon/Name/Beschreibung. Nach dem `innerHTML`-Aufbau wird `bkmpApplyVillageSkinToElement(thumb, skinId, {checkOwnership:false, forceStatic:true})` auf jede Kachel angewendet - `checkOwnership:false` ist hier bewusst der Punkt: auch NICHT besessene Skins zeigen ihre echte Vorschau, das ist der ganze Sinn des Features ("man weiß sonst nicht, was man kauft").
- Neue `bkmpIdleOpenSkinPreview(skinId)`: öffnet `#idleSkinPreviewOverlay` (neu, `index.html`, identisches Lightbox-Muster wie `#investorPayoutViewerOverlay`, 28.07.) - Titel (Icon+Name) + Beschreibung + `bkmpApplyVillageSkinToElement(stage, skinId, {checkOwnership:false})` OHNE `forceStatic` (echtes Abspielen). Schließen per Button ODER Klick auf den Hintergrund (per-Öffnen Add/Remove-Listener, identisches Muster wie `bkmpDragonOpenDetail()` in `bkmp-breeding.js`).
- `style.css`: `.idle-skin-preview-thumb` (Kachel, 1164:199-Seitenverhältnis wie das echte Dorf-Sprite, Hover-Skalierung+Golden-Rahmen) + `.idle-skin-preview-card`/`.idle-skin-preview-stage` (Lightbox, breiter als der `.joke-card`-Standard von 320px).

**Echter z-index-Stacking-Bug beim eigenen Testen gefunden+gefixt (per Playwright reproduziert, nicht nur vermutet):** `#idleSkinPreviewOverlay` öffnet aus dem bereits offenen `#idleDorfOverlay` heraus (Dorf-Skins-Tab) - beide `.joke-overlay`-Elemente hatten anfangs denselben Basis-`z-index:50`; bei gleichem z-index gewinnt bei `position:fixed`-Geschwistern die spätere DOM-Reihenfolge, und `#idleDorfOverlay` steht im Markup NACH `#idleSkinPreviewOverlay` - die neue Lightbox landete dadurch sichtbar, aber unklickbar HINTER dem Idle-Dorf-Fenster ("idle-skin-card intercepts pointer events" im Playwright-Fehlerprotokoll). **Exakt dasselbe, bereits mehrfach dokumentierte Bugmuster** wie bei `#idleDragonDetailOverlay`/`#idleDragonDexOverlay`/`#arenaBattleOverlay` (14.-16.07., "Popup öffnet sich hinter dem idle game") - per identischem `#idleSkinPreviewOverlay{z-index:60}`-Fix behoben, gleiche Stelle in `style.css` wie die anderen drei.

**Tests (`tests/e2e/cosmetics.spec.js`, neuer Block "Dorf-Skins - Live-Vorschau", 7 Tests):** alle 7 Referenz-Karten zeigen eine Vorschau-Kachel mit korrektem Video (auch für nicht besessene Skins), Grid-Kacheln spielen kein Video ab (`forceStatic`), ein synthetischer reiner Sprite-Skin (kein `video_file`, `frame_count:4`) zeigt das Hintergrundbild ohne laufende `steps()`-Animation, Klick öffnet die Lightbox mit korrektem Titel/Beschreibung + startet echtes Abspielen, ein nicht besessener Skin zeigt seine echte (nicht die Standard-Fallback-)Vorschau, Schließen-Button UND Klick-auf-Hintergrund schließen beide zuverlässig, Tastatur (Enter) öffnet genau wie ein Klick. **Ein Mess-Artefakt beim Schreiben gefunden** (kein App-Bug, gleiche bereits dokumentierte Klasse wie die `transitionDuration`-Eigenheit vom Hover-Performance-Fix): `el.style.animation` liest beim Zurücklesen NICHT den literal gesetzten String `'none'`, sondern die vom Browser aufgefüllte Longhand-Serialisierung (`"auto ease 0s 1 normal none running none"`) - Fix im Test: `el.style.animationName` statt `el.style.animation` prüfen (liest das robuste Longhand-Feld direkt). **Ein vorbestehender Test wurde durch das neue `data-skin-id`-Attribut auf JEDER Karte fragil** (`expect(html).not.toContain('data-skin-id="zerstoertesdorf"')` traf jetzt versehentlich auch die neue Vorschau-Kachel, nicht nur einen fehlenden Kauf-Button) - auf den spezifischeren Kontext `'idle-skin-buy" data-skin-id="..."'` verengt, identisches Prinzip wie die bereits danebenstehenden Positiv-Assertions.

**Verifiziert:** 56/56 stabil bei 8x Wiederholung des neuen Blocks (`chromium-desktop`), 51/51 grün über alle 3 Projekte für die komplette `cosmetics.spec.js` (30 korrekt übersprungen, Desktop-Tab-Navigation). Live im Browser (kein echter Login möglich, Sicherheitsregel) per injiziertem Katalog (Bild-Skin/Mehrfach-Frame-Skin/Video-Skin) bestätigt: alle drei Zweige (Bild/Sprite-Animation/Video) rendern korrekt statisch im Grid, Video pausiert (`paused:true`) in der Kachel, spielt (`paused:false`) in der Lightbox; Öffnen/Schließen (Button, Klick-außerhalb, Enter-Taste) funktioniert in allen Pfaden. Keine Konsolenfehler. `node --check`/`eslint` sauber, `style.css`-Klammerbilanz sauber.

Cache-Busting `js/systems/bkmp-cosmetics.js?v=20260804-skinpreview1`, `style.css?v=20260804-skinpreview1` (alle einbindenden HTML-Dateien).

### Nachtrag: Vorschau flackerte während des Kampfs (04.08.2026)

Spieler-Screenshot der Lightbox: "In der Großansicht verschwinden alle Ansichten und fangen an zu flackern." **Nichts committed/gepusht.**

**Root Cause (im Code bestätigt, nicht vermutet):** `bkmpIdleRefreshLiveTabsRender()` (`idledorf.js`, bereits seit dem 26.07.-Hover-Guard-Fix bekannt, siehe dortiger Kommentar) ruft `bkmpIdleRenderSkinsPanel()` bei JEDEM Drachen-Kill erneut auf, solange der Dorf-Skins-Tab offen ist (throttled auf max. alle 300ms, damit Preis-Leistbarkeit/Fortschrittszähler live mitwachsen). Vor der Vorschau-Kachel bedeutete das nur unsichtbares Neu-Rendern von Text/Buttons - jetzt zerstörte jeder komplette `innerHTML`-Rebuild JEDES `<video>`-Vorschauelement und erzwang einen kompletten Neustart (neu laden + neu dekodieren), alle ~0,9-2,5s solange im Hintergrund weitergekämpft wird (Idle-Kampf läuft weiter, auch während man einen anderen Tab betrachtet) - sichtbar als Flackern/Verschwinden, auch während die Lightbox offen war (die Karten dahinter blitzten sichtbar durch, exakt wie im Screenshot zu sehen).

**Fix (`js/systems/bkmp-cosmetics.js`, `bkmpIdleRenderSkinsPanel()`):** vor dem `innerHTML`-Rebuild werden die bereits vorhandenen `.idle-skin-preview-thumb`-Knoten in einer Map gesichert (Schlüssel: `data-skin-id`). Nach dem Rebuild wird für jede neu gebaute (leere) Kachel geprüft, ob bereits eine vorhandene existiert - falls ja, wird die ALTE Node per `Element.replaceWith()` an die Stelle der neuen gehängt (dieselbe DOM-Node inkl. ihrem längst geladenen `<video>`-Kind, kein neues Element, kein Neustart) statt `bkmpApplyVillageSkinToElement()` erneut aufzurufen. Da die Kachel bewusst `checkOwnership:false` nutzt, hängt ihr visueller Inhalt einzig an der Skin-ID - ein erneutes Anwenden bei unverändertem Skin wäre ohnehin nur verschwendete Arbeit gewesen (und genau die Ursache des Flackerns). Ein wirklich neuer Skin (z.B. frisch zum Katalog hinzugekommen) hat keinen Eintrag in der gesicherten Map und bekommt weiterhin eine frisch initialisierte Kachel wie bisher. Klick-/Tastatur-Listener der wiederverwendeten Nodes bleiben automatisch erhalten (dieselbe Node, keine Neuverdrahtung nötig).

**Tests (`tests/e2e/cosmetics.spec.js`, 2 neue REGRESSION-Tests):** (1) direkter DOM-Knoten-Identitätsvergleich (nicht nur ein CSS-Selektor-Treffer, gleiches Beweismuster wie bereits in `panel-render-hover-guard.spec.js` vom 26.07. etabliert) - 5 aufeinanderfolgende `bkmpIdleRenderSkinsPanel()`-Aufrufe (simuliert 5 schnell aufeinanderfolgende Drachen-Kills) beweisen, dass alle 7 Kacheln dieselbe Node bleiben, inkl. eines Markers, der eine Neuerzeugung sofort verraten würde; (2) ein während der Sitzung neu zum Katalog hinzugekommener Skin bekommt trotzdem eine korrekt initialisierte, funktionsfähige Kachel. 20/20 stabil bei 10x Wiederholung, volle `cosmetics.spec.js`-Suite danach erneut 53/53 grün über alle 3 Projekte (34 korrekt übersprungen).

**Verifiziert (Browser, kein echter Login möglich - injizierter Mock-Katalog über die echten Funktionen, gleiches Muster wie beim ursprünglichen Feature-Test):** ein Marker auf jedem `<video>`-Element überlebt drei aufeinanderfolgende `bkmpIdleRenderSkinsPanel()`-Aufrufe unverändert (`sameNodeIdentity: [true,true,true]`), ein Klick auf eine dreifach wiederverwendete Kachel öffnet weiterhin korrekt die Lightbox (Listener-Persistenz bestätigt), Schließen per Klick-außerhalb funktioniert unverändert, ein mitten in der Sitzung neu hinzugefügter Skin bekommt eine korrekt initialisierte, mit echtem Video verdrahtete Kachel. Keine Konsolenfehler. `node --check`/`eslint` sauber.

Cache-Busting `js/systems/bkmp-cosmetics.js?v=20260804-skinpreview2` (alle einbindenden HTML-Dateien).

## Gegnerwechsel-Animation flackerte auf leerer Box bei Video-Drachen (04.08.2026)

Spieler-Videobeweis (`F:\Video\2026-08-04 16-28-22.mp4`, 16:28 Uhr): "Das ganze Fenster flackert kurz und wird ein stück kleiner. Siehst du das?" **Nichts committed/gepusht.**

**Diagnose (kein direktes Video-Tool verfügbar, gleiche Notlösung wie beim 27-Sek.-Screen-Recording vom 21.07. - `ffmpeg`-Frame-Extraktion statt direkter Video-Wiedergabe):** Szenenwechsel-Erkennung (`ffmpeg ... select=gt(scene,0.08)`) fand 4 Kandidaten-Zeitpunkte im 7,8-Sekunden-Clip, an zwei davon (t≈5,05s und t≈5,7s, beides unabhängige Drachenwechsel) zeigte die Drachen-Sprite-Box **komplett leer/schwarz** - Name/HP-Balken des neuen Drachen waren bereits korrekt aktualisiert ("Winddrache (Stufe 167-3)", 21415/21415), nur das Bild selbst fehlte für mindestens einen Frame. Beide Male war der VORHERIGE Drache ebenfalls ein Video-Drache (Cyberdrache→Winddrache).

**Root Cause (im Code bestätigt):** die "Gegnerwechsel weniger abrupt"-Animation vom 03.08. (`.idle-dragon-spawn-in`, `bkmpIdleSpawnDragon()` in `js/ui/bkmp-hud.js`) ging laut eigenem Kommentar davon aus, der Sprite-Container sei "bereits mit dem NEUEN Drachen befüllt", sobald sie startet - stimmt für `BKMP_IDLE_VIDEO_DRAGON_SPRITES`-Drachen nicht: `bkmpIdleApplyDragonSprite()` tauscht dort (Perf-Fix 19.07.) nur `video.src` am wiederverwendeten `<video>`-Element aus, das erste darstellbare Bild braucht aber einen echten Lade-/Dekodier-Schritt. Die Animation (Start bei `opacity:0, scale(0.9)`, siehe `style.css`) lief bisher IMMER sofort, unabhängig davon ob schon ein Bild da war - bei einer noch leeren Box wird dadurch buchstäblich nichts animiert (Schwarz-Flackern) und der `scale(0.9)`-Startwert macht die (leere) Box zusätzlich sichtbar kleiner - exakt die zwei vom Nutzer beschriebenen Symptome. Betrifft strukturell NUR Video-zu-Video-Übergänge (PNG-Sprite-Wechsel sind praktisch verzögerungsfrei, ein Wechsel VON Video ZU PNG entfernt das `<video>`-Element komplett statt es leer zu lassen) - da inzwischen die meisten normalen Drachenarten auf Video migriert sind (siehe `BKMP_IDLE_VIDEO_DRAGON_SPRITES`, 18.07. laufende Migration), ist das aber der häufige Fall, nicht die Ausnahme.

**Fix (`js/ui/bkmp-hud.js`):** die bisher inline in `bkmpIdleSpawnDragon()` sitzende Logik wurde in eine eigene, benannte Funktion `bkmpIdleTriggerDragonSpawnAnim(dragonSprite)` ausgelagert (auch, damit sie unabhängig von der - im Playwright-Mock ohnehin nur numerische, nie mit `BKMP_IDLE_VIDEO_DRAGON_SPRITES` übereinstimmende - Drachenrotation direkt testbar ist). Findet sie ein `<video class="idle-dragon-sprite-video">`-Kind mit `readyState < HAVE_CURRENT_DATA` (2, "noch kein darstellbares Bild"), wird der Animationsstart auf das `loadeddata`-Ereignis verschoben (plus 600ms-Fallback-Timer, falls das Ereignis aus irgendeinem Grund nie feuert, z.B. Netzwerkfehler) - bei PNG-Sprite-Drachen (kein `<video>`) und bei einem bereits geladenen Video (z.B. derselbe Drache erscheint erneut) bleibt das Verhalten unverändert sofort. Bekannter, bewusst akzeptierter Randfall: bei mehreren sehr schnell aufeinanderfolgenden Kills während ein Video noch lädt kann der 600ms-Fallback-Timer eines älteren Aufrufs die Animation ein zweites Mal auf dem dann längst nächsten Drachen abspielen (rein kosmetische Wiederholung, keine Zustandsänderung) - selten genug (braucht gleichzeitig sehr schnelle Kills UND ungewöhnlich langsames Laden), um keine eigene Abbruch-Logik zu rechtfertigen.

**Tests (`tests/e2e/combat-visual-polish.spec.js`, neuer Block, 4 Tests):** ein per `page.route()` gezielt verzögertes Video beweist, dass die Animation NICHT sofort startet (`spawnInClass:false`, `readyState<2` bestätigt die Blockade wirkt) und nach Freigabe zeitnah nachtriggert (`expect.poll`, `readyState>=2` bestätigt: die Animation startete tatsächlich erst NACHDEM ein Bild da war); ein bereits geladenes Video startet weiterhin sofort; ein PNG-Sprite-Drache (kein `<video>`) startet weiterhin sofort; ein Video, dessen `loadeddata` nie feuert (dauerhaft blockierte Anfrage), löst die Animation trotzdem über den 600ms-Fallback aus. 40/40 stabil bei 10x Wiederholung, volle Regression (`combat-visual-polish.spec.js` + `combat.spec.js`, alle 3 Projekte) 66/66 grün - inkl. des bereits bestehenden "Gegnerwechsel"-Tests, der weiterhin unverändert grün bleibt (lokaler Mock-Server liefert Videos schnell genug, dass `loadeddata` deutlich innerhalb der Test-Wartezeit feuert).

**Verifiziert (Browser, echtes Video-Asset, gezielt mit eindeutigem Query-String geladen um Browser-Cache auszuschließen):** `bkmpIdleTriggerDragonSpawnAnim()` mit einem frischen, noch nicht geladenen `<video>` (`readyState:0`) direkt aufgerufen - `.idle-dragon-spawn-in` wird NICHT sofort gesetzt (`startedImmediately:false`), erscheint aber zuverlässig, sobald das Video tatsächlich ein Bild hat (`readyState:4`, per Polling bestätigt). Keine Konsolenfehler. `node --check`/`eslint` sauber.

Cache-Busting `js/ui/bkmp-hud.js?v=20260804-dragonspawnfix1` (alle einbindenden HTML-Dateien).

## Zwei Duplikat-Accounts aus der öffentlichen Bestenliste ausgeblendet (04.08.2026)

Nutzer-Screenshot (Rang 14+15 der öffentlichen "Erfolge"-Bestenliste, je "254 Erfolge", Titel "Achievement Meister"): "Bagon1990"/"BagonTr02" - "Das waren Bagon 2 Accounts. Die können weg." **Nichts committed/gepusht, keine SQL nötig.**

**Rückfrage vor der Umsetzung (Umfang war nicht eindeutig aus "die können weg" ableitbar):** Vollständige Account-Löschung existiert bereits im Admin-Panel ("Spieler-Verwaltung", `admin_delete_player_account` RPC) - wäre aber unwiderruflich und hätte NICHT nur die Bestenliste betroffen, sondern auch Idle-Dorf-Spielstand/Runen/Prestige/Login komplett mitgelöscht, deutlich mehr als vom Screenshot allein ersichtlich. Nutzer-Entscheidung nach `AskUserQuestion`: **nur aus der Bestenliste ausblenden**, Accounts/Spielstand bleiben unangetastet.

**Umsetzung (`supabase.js`, `js/core/bkmp-site.js`):** neue `BKMP_HIDDEN_LEADERBOARD_ACCOUNTS`/`bkmpIsHiddenFromLeaderboard()` - exakt dasselbe, bereits bestehende Muster wie `BKMP_HIDDEN_TEST_ACCOUNTS`/`bkmpIsHiddenTestAccount()` (14.07., dort für einen internen Test-Account: "soll aber in keine Ranglisten fliessen"), bewusst als SEPARATE Liste (andere Semantik - das sind echte Duplikat-Accounts, keine Test-Accounts) an derselben Filterstelle angewendet (`refreshLeaderboardData()`, filtert `bkmpLeaderboardStats` einmalig - wirkt dadurch auf ALLE Bestenlisten-Kategorien gleichzeitig, nicht nur "Erfolge", da Erfolge/Karten/Kartenideen/Zeit/Bonks sich denselben gefilterten Datensatz teilen). Case-insensitiv (`.trim().toLowerCase()`, gleiche Normalisierung wie `name_key` in der DB). Rein clientseitige Anzeige-Filterung an der ÖFFENTLICHEN Render-Stelle - im Admin-Panel bleiben beide Accounts weiterhin normal sichtbar/verwaltbar (gleiches, bereits begründetes Prinzip wie beim Test-Account).

**Verifiziert (Browser, kein echter Login/Produktionskontakt nötig - `loadLeaderboardStats()` direkt mit Test-Daten überschrieben):** `bkmpIsHiddenFromLeaderboard()` erkennt beide Namen case-insensitiv + trimmt Leerzeichen; der echte, in dieser Datei bereits vielfach dokumentierte Spieler "BagonTr01" bleibt bewusst UNBERÜHRT (per Test bestätigt, nicht nur angenommen - naheliegender Verwechslungsrisiko bei ähnlichen Namen). End-to-Ende über `refreshLeaderboardData()` mit 5 Testeinträgen (beide Duplikate + BagonTr01 + der bestehende Test-Account + ein normaler Spieler) bestätigt: nur die beiden Duplikate + der Test-Account werden gefiltert, `BagonTr01`+der normale Spieler bleiben sichtbar. Keine Konsolenfehler. `node --check`/`eslint` sauber. Keine Playwright-Tests ergänzt (Marketing-Seiten-Code liegt architekturbedingt außerhalb der Idle-Dorf-Testsuite, siehe frühere Investor-Payout-Abschnitte).

Cache-Busting `supabase.js?v=20260804-leaderboardhide1` (alle einbindenden HTML-Dateien), `js/core/bkmp-site.js?v=20260804-leaderboardhide1` (index.html).

## Neue Zucht-Drachenart "Bagondrache" aus zusammengesetztem Referenzbild (05.08.2026)

Nutzer lieferte ein 4-stufiges Referenzbild (Ei/Baby/Jugendlich/Erwachsen, alle vier nebeneinander auf grauem Flächenhintergrund mit Beschriftung darunter, 1774×887px) mit dem Wunsch "Das wäre ein Neuer Drache Episch. Kannst du das selber ausschnippeln?" **Nichts committed/gepusht.**

**Bildquelle:** vom Client automatisch als `Downloads\Bagon Drache.png` gesichert (kein expliziter Pfad vom Nutzer nötig) - Dateiname legt nahe, dass der Drache nach dem Spieler "Bagon" benannt ist (passt zum bereits mehrfach in dieser Datei etablierten Muster, echte Spieler in Content einfließen zu lassen, z.B. "Kaledoss"/"Nytherion").

**Freistellung (Node + `sharp`, bereits aus der 6-neue-Zucht-Drachenarten-Session vom 02.08. installiert):** der graue Hintergrund hat einen leichten Verlauf (Luminanz 140-158 je nach Position) - ein einfacher Colorkey mit festem Referenzwert hätte an manchen Stellen versagt.

**Erster Versuch (per-Pixel-Distanzschwelle) hatte einen echten Bug - vom Nutzer per Screenshot gefunden:** eine globale Sättigungs-/Leuchtdichte-Schwelle je Pixel unabhängig ("ist dieser eine Pixel selbst hintergrund-ähnlich?") behandelte das silbrig-graue Kopf-Geweih (niedrige Sättigung, mittlere Leuchtdichte - farblich dem Hintergrund selbst ähnlich) fälschlich als Hintergrund und stanzte Löcher mitten in den Kopf, obwohl der Bereich klar INNERHALB der Drachenkontur lag ("Hast nur bisschen zuviel Transparent gemacht sieht man am Kopf"). Fix: echtes Flood-Fill/Zauberstab-Prinzip - ein BFS startet ausschließlich an den vier Bildrändern (garantiert echter Hintergrund) und markiert nur ZUSAMMENHÄNGENDE hintergrund-farbene Pixel als entfernbar; isolierte, hintergrund-ähnlich gefärbte Flächen MITTEN im Drachen (wie das Geweih) werden nie erreicht und bleiben automatisch erhalten, unabhängig von ihrer Farbe. Spalten-/Zeilen-Profile auf dem resultierenden Alpha-Kanal fanden weiterhin automatisch die vier Panel-Grenzen + die Grenze zwischen Charakter-Kunst und Beschriftung. Feathering (weicher statt harter Kantenverlauf) per einfachem manuellem 3×3-Box-Mittelwert direkt in JS - ein erster Versuch über einen zweiten `sharp`-Raw-Rohdaten-Durchlauf für nur den Alpha-Kanal hat die Bilddaten sichtbar korrumpiert (Streifenmuster, falsche Bounding-Boxen) und wurde verworfen, bevor er in die Assets gelangte. Per zusammengesetztem Kontaktabzug (alle 4 Stufen auf hochkontrastiertem Schachbrett-Transparenz-Muster, NICHT nur die vom Bild-Betrachter selbst gerenderte Transparenz - ein reines PNG-Lesen zeigte Alpha=0 teils weiterhin als opak an, daher der explizite Kontaktabzug als einzig verlässlicher Beweis) visuell bestätigt: saubere Freistellung, Kopf-Geweih vollständig erhalten, keine grauen Reste, Schlagschatten/Steinsockel beim Ei blieb erhalten.

**Einordnung:** Episch (wie vom Nutzer vorgegeben), Werte 1:1 von der bestehenden Episch-Stufe übernommen (identisch zu den 4 "NeueNeueDrachen"-Episch-Arten vom 02.08.: `brood_seconds=10800, sacrifice_gold/crystals=0, growth_points_required=2000, battle_xp_required=15000, is_multi_stat=false, sub_stat_count 3-4`), `egg_source='event'` (kein Kampf-Drop). `sort_order=24` (direkt hinter der letzten Charge, die bei 23 endete).

**Bild-Pipeline:** die 4 zugeschnittenen/freigestellten PNGs liefen durch `scripts/optimize-images.mjs` (bereits etabliert, 480px + WebP Qualität 82) - gegen den isolierten `dragon-stage`-Staging-Ordner (nicht direkt gegen `assets/dragons/breeding`, um die dort bereits liegenden 17+ Arten nicht unnötig erneut zu verarbeiten, gleiches Vorgehen wie am 02.08.), Ergebnis-Dateien (`bagondrache.png`/`-web.png`/`-web.webp`) danach in die echten Stage-Ordner kopiert. Keine JS-Änderung nötig - `bkmpDungeonRollEgg()` nimmt jede aktive Art passender Seltenheit automatisch in den gewichteten Ei-Pool auf, `bkmpDragonThumbHtml()` leitet die `-web`-Varianten automatisch aus dem in der DB hinterlegten Basispfad ab (beides bereits vollständig datengetrieben, siehe frühere Batch-Ergänzungen).

**Verifiziert:** Kontaktabzug aller 4 freigestellten Stufen (Schachbrett-Hintergrund) visuell geprüft - Ei (mit Steinsockel+Schatten), Baby, Jugendlich, Erwachsener (volle Flügelspannweite erhalten) alle sauber freigestellt. Optimierungs-Pipeline lief fehlerfrei (46KB/35KB/60KB/88KB PNG-Fallback, 21KB/24KB/60KB/81KB WebP je Stufe). Kein DB-Zugriff für diese Session - Einfügung nur als SQL vorbereitet.

Cache-Busting nicht nötig (reine Asset-/SQL-Ergänzung, kein JS/CSS geändert).

**Nicht geprüft/offen:** `sql/20260805-dragon-species-bagondrache.sql` muss im Supabase-Dashboard ausgeführt werden, bevor der neue Drache live im Ei-Dungeon-Pool auftauchen kann.

## Drachenzucht: fehlende XP-Anzeige + kein Offline-Fortschritt beim Begleitdrachen (05.08.2026)

Nutzer-Auftrag (Screenshot einer "Lohendrache (Jugendlich) Legendär"-Karte, farbiger Balken ohne sichtbare Zahl): "Wir brauchen bei denn Jugendlich -> Erwachsenen einen XP Anzeige.. und scheinbar über nacht offline timer passiert auch nichts an Fortschritt. Bitte mal Prüfen und hinzufügen. Gerne mir dann mitteilen wie man am besten XP für die sammelt." **Nichts committed/gepusht.**

### Fix 1 — fehlende XP-Zahl (Baby- UND Jugendlich-Karte)

**Root Cause (im Code bestätigt, nicht vermutet):** beide Karten hatten den Fortschrittstext bereits im Markup (`<span>733/2000</span>`) - er stand aber DIREKT INNERHALB von `.idle-xp-bar`, einem Balken-Wrapper mit `overflow:hidden` (nötig, damit der farbige Füllstand-Div nicht über den Rand hinausragt). Der Text war dadurch technisch immer vorhanden, aber vom eigenen Elternelement abgeschnitten - unsichtbar, kein Anzeige-Bug im eigentlichen Sinne, sondern ein Struktur-Bug. Das bereits an anderer Stelle (Spieler-XP-Balken im HUD, `js/ui/bkmp-hud.js`) korrekt gelöste Muster - Balken-Wrapper und Text als getrennte GESCHWISTER-Elemente statt Text im Balken - wurde für die Drachenzucht-Karten nie übernommen. Bestätigt per fertig gestylter, aber bis dahin ungenutzter `.idle-xp-label`-CSS-Klasse (3 Regel-Varianten in style.css, alle korrekt, keine davon je referenziert).

**Fix (`js/systems/bkmp-breeding.js`):** Text aus `.idle-xp-bar` entfernt, neues `.idle-xp-label`-Div direkt danach eingefügt - für die Baby-Karte ("733 / 2K Fütterungs-EP", `growth_points`/`growth_points_required`) und die Jugendlich-Karte ("6.8K / 15K Kampf-EP", `battle_xp`/`battle_xp_required`). Keine CSS-Änderung nötig.

**Verifiziert (Browser, injizierter Mock-Zustand über die echte `bkmpIdleRenderDragonsPanel()`-Funktion, kein echter Login möglich - Sicherheitsregel):** beide Labels rendern `display:block`/`visibility:visible`, als echtes Geschwister-Element (nicht mehr im `overflow:hidden`-Balken), bei erzwungener sichtbarer Panel-Größe echte, nicht-null `getBoundingClientRect()`-Maße (163×12px), korrekte Textformatierung. Kein Rest-`<span>` mehr innerhalb von `.idle-xp-bar` (alter Bug-Zustand vollständig entfernt). **Nebenbefund beim Verifizieren:** die Browser-Vorschau lud zunächst hartnäckig eine gecachte alte Fassung von `index.html` trotz gebumptem Cache-Busting-Query-String am `<script>`-Tag selbst - ein reines Werkzeug-/Browser-Cache-Artefakt dieser Sitzung (ein `fetch(url,{cache:'no-store'})` bewies, dass der Server bereits korrekt die neue Datei auslieferte), kein Hinweis auf einen echten Bug im Code - behoben durch Navigation zu einer Cache-Buster-URL für das HTML-Dokument selbst.

### Fix 2 — kein Kampf-EP-Fortschritt für den Begleitdrachen während Abwesenheit

**Root Cause (per Code-Analyse bestätigt, Nutzer-Verdacht war korrekt):** `bkmpDragonGrantCompanionBattleXp()` (bkmp-breeding.js) vergibt Kampf-EP an den aktuellen jugendlichen Begleitdrachen bei jedem Kill - aber ausschließlich aus den drei Aufrufstellen im LIVE-Tick (idledorf.js/bkmp-dungeon.js/bkmp-tower.js), alle nur während das Spiel tatsächlich aktiv offen/tickend ist. `api/claim-idle-offline-progress.js` (die server-seitige Erwartungswert-Simulation für Abwesenheit, simuliert Gold/XP/Ressourcen/Drachen-Stufen-Fortschritt) kannte `player_dragons` bisher überhaupt nicht - ein Begleitdrache blieb während jeder Abwesenheit (auch über Nacht) komplett auf seinem alten Kampf-EP-Stand stehen, unabhängig von der simulierten Kill-Anzahl. Exakt derselbe strukturelle Bug wie mehrere frühere, bereits dokumentierte Fälle in diesem Projekt, bei denen die Offline-Simulation ein Live-Verhalten schlicht nicht kannte (z.B. "Bug 6"/"Bug 7" vom 20.07., Magie-Skilltree-Effekte bzw. seltene Drachen fehlten dort ebenfalls zunächst).

**Fix (`api/claim-idle-offline-progress.js`):** nach dem bereits bestehenden `idle_dragons`-Laden ein neuer, defensiv gekapselter Lookup (eigenes try/catch, identisches Prinzip wie der bestehende Gilden-Nachtwache-Bonus-Lookup - ein Fehlschlag hier gefährdet nie den Rest der bereits funktionierenden Belohnung): aktueller jugendlicher Begleitdrache (`player_dragons?auth_user_id=eq...&is_companion=eq.true&stage=eq.teen&select=id,species_id,battle_xp&limit=1`) + dessen Art-Anforderung (`dragon_species?id=eq...&select=battle_xp_required`). Innerhalb der bereits bestehenden Kampf-Simulationsschleife (dort, wo `kills`/`bossKills` ohnehin schon pro simuliertem Sieg hochgezählt werden) wird `companionXpGain` um denselben Betrag wie live erhöht (Boss 25, sonst 4 - exakt `bkmpDragonGrantCompanionBattleXp()`s Formel). Nach der Schleife: auf `battle_xp_required` gedeckelt, separates PATCH auf `player_dragons` (eigener, unabhängiger Schreibvorgang NACH dem bereits erfolgreich bestätigten Haupt-Claim auf `idle_player_state` - ein Fehlschlag hier kostet den Spieler nie die bereits gewährte restliche Offline-Belohnung). `dragonXpGain` zusätzlich in der Antwort (`rewards.dragonXpGain`).

**Bewusst NICHT repliziert:** der prozentuale Kampf-EP-Bonus aus Substats/Skilltree/Prestige (`dragon_xp_pct`/`dragon_xp_bonus_pct`, Teil der vollen `bkmpDragonGrantCompanionBattleXp()`-Formel) - das würde zusätzlich `idle_prestige_state` + die Substats des Begleitdrachen selbst laden und die mehrstufige Bonus-Formel serverseitig duplizieren müssen. Konsequenz: Offline-EP können bei Spielern mit hohem EP-Bonus etwas NIEDRIGER ausfallen als bei identischer Zeit live erspielt - ein kleiner, bewusst dokumentierter Kompromiss gegenüber dem vorherigen Zustand (komplett 0 EP offline, unabhängig vom Bonus), kein neuer Fehler, gleiches pragmatisches Partial-Fidelity-Prinzip wie bei den früheren Offline-Simulations-Erweiterungen dieses Projekts.

**Client-seitige Ergänzung (`idledorf.js`):** `bkmpIdleShowOfflineCard()` zeigt bei `dragonXpGain>0` einen zusätzlichen "🐲 +N Kampf-EP"-Chip in der "Während deiner Abwesenheit..."-Karte. `bkmpIdleApplyOfflineResult()` (läuft an allen 3 Aufrufstellen: normales Öffnen, Tab-Sichtbarkeits-Nachholung, OBS-Hintergrund-Nachhol-Broadcast) stößt bei `dragonXpGain>0` einen fire-and-forget `loadPlayerDragons(name)`-Refresh an und ersetzt `bkmpPlayerDragons` - ohne das wäre der frisch gutgeschriebene Fortschritt erst nach einem erneuten Fenster-Wechsel/Reload im UI sichtbar gewesen, da `bkmpIdleLoadDragonBreedingState()` (lädt `bkmpPlayerDragons`) im Öffnen-Ablauf VOR dem Offline-Claim läuft.

**Tests (`tests/e2e/offline-companion-xp.spec.js`, neu, 5 Tests):** läuft wie `offline-afk.spec.js` über die ECHTE, unveränderte Handler-Datei (`tests/mock/offline-progress-handler.js` ruft `api/claim-idle-offline-progress.js` in-process auf, keine Testkopie der Formel). Begleitdrache bekommt EP für simulierte Kills (Server- UND Antwort-Wert stimmen exakt überein); Deckel bei `battle_xp_required` greift korrekt (96→100, nicht darüber, `dragonXpGain=4` bei reichlich Zeit); kein Begleitdrache vorhanden → kein Fehler, `dragonXpGain=0`; ein erwachsener statt jugendlicher "Begleiter" bekommt keine Offline-EP (Stage-Filter greift korrekt); Client übernimmt frisch gewährte EP nach `bkmpIdleApplyOfflineResult()` automatisch in `bkmpPlayerDragons` (per `expect.poll()`, da fire-and-forget). **50/50 stabil bei 10x Wiederholung auf `chromium-desktop`, 66/66 grün über alle 3 Projekte** inkl. angrenzender Regression (`offline-afk.spec.js`, `dragon-lifecycle-release.spec.js`, `combat.spec.js` - alle unverändert grün, keine neue Regression durch die Handler-Änderung). `node --check`/`eslint` auf allen 3 geänderten Dateien sauber, `scripts/static-checks.js` unverändert (0 CRITICAL/HIGH/MEDIUM, 58 LOW/1 INFO).

### Antwort auf die Nutzerfrage "wie sammelt man am besten XP dafür"

Ein jugendlicher Drache muss als **Begleiter** aktiv sein (Knopf "Als Begleiter" auf der Drachenlager-Karte - nur EIN Begleiter gleichzeitig, ein neuer ersetzt den alten). Danach: **jeder normale Kill** (egal ob durch Klicken oder den automatischen Tick) gibt **4 Kampf-EP**, ein **Boss-Kill 25 Kampf-EP** - reines Kämpfen im normalen Dorf-Kampfbereich reicht also, kein separates Minispiel. Bosse (jede 25. Stufe) sind mit Abstand der effizienteste Weg, da sie mehr als das Sechsfache eines normalen Kills geben. Ab jetzt läuft das auch **während Abwesenheit/über Nacht** weiter (siehe Fix 2 oben) - ein Begleitdrache wächst also durchgehend, nicht nur während man aktiv zusieht. Zusätzliche Beschleunigung über Bonusquellen (falls investiert): Skilltree-/Prestige-Boni auf Kampf-EP sowie ein bereits erwachsener zweiter Drache mit einem `dragon_xp_bonus_pct`-Substat.

Cache-Busting `js/systems/bkmp-breeding.js?v=20260805-companionxpfix1`, `idledorf.js?v=20260805-companionxpfix1` (index.html/admin.html/idle-stream-mini.html). `api/claim-idle-offline-progress.js` braucht kein Cache-Busting (server-seitige Vercel-Funktion, kein Browser-Cache-Bezug).

Öffentliche Changelog-Zeile vorbereitet: `sql/20260805-changelog-dragon-growth-xp.sql` (**noch nicht ausgeführt**, idempotent, gleiches Muster wie alle bisherigen Changelog-SQL-Dateien).

## "Bosskampf!"-Banner erschien bei "Bleibt hier" dauerhaft wiederkehrend (05.08.2026)

Nutzer-Screenshot der Kampfansicht (Stufenleiste zeigt "Beste Stufe" aktiv, Banner "👑 Bosskampf!" sichtbar): "Ist es möglich wenn man auf der Stufe bleibt.. das nicht dauerhaft diese Boss benachrichtung etc kommen? nur 1x?" **Nichts committed/gepusht.**

**Root Cause (im Code bestätigt, nicht vermutet):** `bkmpIdleSpawnDragon()` (`js/ui/bkmp-hud.js`, das Status-Banner-System vom 03.08. "Visueller Feinschliff der Kampfansicht") zeigte das "👑 Bosskampf!"-Banner bisher UNBEDINGT bei jedem Aufruf mit `bkmpIdleCurrentDragon.isBoss===true`, unabhängig davon, ob es sich um ein echtes NEUES Erreichen der Stufe handelte. `bkmpIdleSpawnDragon()` läuft aber bei JEDEM Kill erneut (baut den aktuellen Drachen mit vollem HP neu auf, egal ob weitergezogen wird oder nicht) - bei "Bleibt hier" (`auto_advance=false`) bleibt `current_dragon_index` nach jedem Kill unverändert auf der Boss-Stufe, wodurch derselbe Boss bei jedem einzelnen Kill erneut "gespawnt" und das Banner dadurch bei jedem Kill erneut gezeigt wurde, statt nur beim ersten Erreichen der Stufe. `isBoss` ist dabei sowohl für echte Bosse (`spawn_rule==='boss_25'`, jede 25. Stufe) als auch Minibosse (`spawn_rule==='miniboss_10'`, jede 10. Stufe) `true` - beide betroffen.

**Fix (`js/ui/bkmp-hud.js`):** neue Modul-Variable `bkmpIdleLastBannerBossIndex` (analog zum bereits bestehenden `bkmpCombatStatusActive`) merkt sich den zuletzt tatsächlich bebannerten Stufen-Index. Das Banner feuert nur noch, wenn `bkmpIdleCurrentDragon.killIndex !== bkmpIdleLastBannerBossIndex` - bei jedem Nicht-Boss-Drachen (Weiterzug nach einem normalen Sieg, Rückzug nach einer Niederlage, Prestige-Reset) wird der Tracker automatisch wieder auf `null` geleert, damit ein späteres erneutes Erreichen derselben ODER einer anderen Boss-Stufe (auch in einem neuen Prestige-Durchlauf, der dieselben numerischen Indizes zwangsläufig erneut durchläuft) das Banner korrekt wieder einmal zeigt - kein dauerhaftes Verstummen, nur eine Unterdrückung der direkten Wiederholung auf derselben Stufe. Betrifft ausschließlich die Anzeige - keine Kampflogik/-werte geändert. Wirkt auch korrekt bei den anderen Aufrufstellen von `bkmpIdleSpawnDragon()` (Dungeon-/Turm-/Raid-Rückkehr, Prestige-Reset - alle restaurieren/bauen denselben zentralen `#idleDragon`-Kampfzustand, keine separaten Szenen).

**Tests (`tests/e2e/combat-visual-polish.spec.js`, 2 neue Regressionstests):** Banner erscheint beim ersten Erreichen einer Boss-Stufe, bleibt aber bei einem zweiten `bkmpIdleSpawnDragon()`-Aufruf mit unverändertem Index (simuliert "Bleibt hier" nach einem Kill) korrekt aus; ein zweiter Test beweist zusätzlich, dass eine Nicht-Boss-Stufe dazwischen (Rückzug) den Tracker korrekt zurücksetzt und ein erneutes Erreichen der Boss-Stufe das Banner wieder zeigt (kein dauerhaftes Verstummen). 30/30 stabil bei 15x Wiederholung auf `chromium-desktop`, 84/84 grün über alle 3 Projekte (inkl. der vollen `combat-visual-polish.spec.js`-Suite + `combat.spec.js` + `obs-background-catchup.spec.js` als angrenzende Regression, alle unverändert grün).

**Verifiziert (Browser, kein echter Login möglich - injizierter Mock-Kampfzustand über die echte `bkmpIdleSpawnDragon()`-Funktion, kein Testkopie-Code):** erster Aufruf mit Boss-Index 24 zeigt "👑 Bosskampf!", ein zweiter Aufruf mit demselben Index (simuliert "Bleibt hier") zeigt korrekt KEIN Banner, ein Zwischenschritt auf Nicht-Boss-Index 23 zeigt ebenfalls kein Banner, die anschließende Rückkehr auf Index 24 zeigt das Banner korrekt erneut. **Eigener, beim Verifizieren gefundener Fehler in der eigenen Arbeit (nicht die App):** das Cache-Busting für `bkmp-hud.js` wurde beim ersten Speichern der Fix-Datei vergessen - der Live-Browser-Check zeigte zunächst den alten, ungefixten Code trotz korrekt gespeicherter Quelldatei (per Netzwerk-Log bestätigt: Browser fragte noch `?v=20260804-dragonspawnfix1` an) - vor dem Abschluss bemerkt und auf `?v=20260805-bossbannerfix1` nachgezogen, danach live erneut bestätigt.

Cache-Busting `js/ui/bkmp-hud.js?v=20260805-bossbannerfix1` (index.html/admin.html/idle-stream-mini.html).

Öffentliche Changelog-Zeile vorbereitet: `sql/20260805-changelog-boss-banner-repeat.sql` (**noch nicht ausgeführt**, idempotent, gleiches Muster wie alle bisherigen Changelog-SQL-Dateien).

## Mehrere gleichzeitige Kampf-Begleiter + eigene Begleiter-Leiste (05.08.2026)

Spieler-Idee MCSoGGe (öffentliches Feedback-Board, Screenshot): "wenn man ein gewisses Level an Prestiges hat dass man dann 2 oder mehr Drachen ausrüsten kann". Nutzer konkretisierte direkt eine vollständige Design-Vorlage (nicht nur die Idee weitergereicht): "Aber mit Handicap? der 2 Bringt nur noch 50% des werts und der 3 Ausgerüstete nur noch 25% seiner Stats? Im prestige Baum aber teuer 2 Stufen upgradbar der erste 1500 Prestige und der 3 3000 Prestige." Mitten in der Umsetzung zusätzlicher Wunsch (Screenshot des aktuellen Drachennester-Bereichs): "DAs man Hier das ganze overlay anpasst? Willst du dich deswegen mal Informieren in anderen Games? ... Das wir oben auch 3 Drachen Slots haben wo man denn ausgerüstet Drachen sieht?" - kurze Web-Recherche zu Idle-/Sammel-RPGs (Idle Heroes, AFK-Arena-artige Spiele) bestätigte den Genre-Standard (kompakte Portrait-Slot-Reihe, Schloss-Symbol für gesperrte Plätze statt leerer Fläche, gestrichelter "+"-Platzhalter für freie Plätze) - direkt umgesetzt statt nur als Textzeile. **Nichts committed/gepusht.**

**Architektur-Recherche vor der Umsetzung (wichtige, nicht offensichtliche Funde):**
- `is_companion` (player_dragons) hatte bisher eine **partielle Unique-Index-Constraint auf Datenbank-Ebene** (`player_dragons_one_companion_idx`, `sql/supabase-dragon-breeding.sql:209`) - erzwang "maximal EIN Begleiter pro Spieler" nicht nur clientseitig, sondern hart in Postgres. Ohne eine SQL-Migration wäre das Feature trotz korrektem Client-Code wirkungslos geblieben (ein zweiter Ausrüsten-Versuch hätte serverseitig mit einer Constraint-Verletzung abgelehnt).
- Mindestens **8 verschiedene Codestellen** in `js/systems/bkmp-breeding.js` gingen von genau einem aktiven Begleiter aus (`.find(d => d.is_companion)`), inkl. der kompletten Bonus-Aggregation (`bkmpIdleDragonCompanionEffectTotals`), der Fütterungs-/Fleisch-/Kampf-EP-Substat-Quelle (`bkmpDragonSubstatBonus`), der Detailansicht, der Kartenanzeige und der Kauf-/Ablege-Logik selbst - alle mussten konsistent umgebaut werden.
- Der bereits bestehende Prestige-Baum hat schon einen eigenen "Drachen"-Zweig mit 4 begleiter-bezogenen Knoten (`drachenmacht`/`maechtige_abstammung`/`aktiver_begleiter`/`passive_bindung`) - aber der ist bereits voll (10/10 Knoten, feste Konvention seit der Progression-Rebalance). Der neue Knoten passt stattdessen besser ins "Vermächtnis" (bisher 2 Knoten, explizit für seltene/teure Endgame-Freischaltungen gedacht) - der vom Nutzer genannte Preis (1.500/3.000 Punkte, weit über dem Gesamtwert jedes einzelnen normalen Knotens von ~1.777-3.179 Punkten bis zum Maximalrang) signalisiert ohnehin einen bewussten Kapstein-Charakter.

**Prestige-Knoten (`js/systems/bkmp-prestige.js`, Zweig "legacy"/Vermächtnis):** `weitere_gefaehrten` - bewusst KEIN normaler Tier (WEAK/MEDIUM/STRONG/SPECIAL/TOGGLE), sondern ein eigener 2-Rang-Kostenverlauf (`baseCost:1500, costGrowth:2`), der die vom Nutzer genannten Werte EXAKT trifft: `bkmpPrestigeUpgradeCost()` rechnet `baseCost·costGrowth^rankBeingBought` → Rang 1 = 1500·2⁰=1500, Rang 2 = 1500·2¹=3000, keine Rundung nötig. `paragonEligible:false` (ein Platz-Zähler mit nur 3 sinnvollen Stufen eignet sich nicht für endlose Paragon-Skalierung).

**Mehrfach-Begleiter-Architektur (`js/systems/bkmp-breeding.js`):**
- `bkmpDragonMaxCompanionSlots()` → 1-3, aus `bkmpDragonPrestigeBonus('companion_slot_bonus')`.
- `bkmpDragonActiveCompanions()` → alle erwachsenen `is_companion`-Drachen, **nach rechnerischer Stärke absteigend sortiert** (identische Formel wie der bereits bestehende "Sortieren: Stärke"-Filter im Lager, bewusst OHNE Aufstiegs-Bonus, damit die Anzeige mit dem Sortier-Dropdown optisch übereinstimmt), auf `maxSlots` gekappt. **Bewusste Design-Entscheidung: automatische Sortierung nach Stärke statt manueller Slot-Wahl** - verhindert, dass ein Spieler versehentlich seinen stärksten Drachen in den 25%-Rang "einsortiert"; das Spiel räumt den stärksten immer automatisch in Rang 1 (100%). Per Test bewiesen: Ausrüsten in aufsteigender Stärke-Reihenfolge (schwächster zuerst) ergibt trotzdem die korrekte, nach Stärke sortierte Rang-Zuteilung.
- `BKMP_DRAGON_COMPANION_SLOT_WEIGHTS = [1, 0.5, 0.25]` - angewendet in `bkmpIdleDragonCompanionEffectTotals()` (Hauptwerte + generische Substats) UND `bkmpDragonSubstatBonus()` (Frucht-/Fleisch-/Drachen-EP-Bonus, vorher separat behandelt). Bei genau einem aktiven Begleiter (unveränderter Normalfall ohne den neuen Knoten) ist das Ergebnis rechnerisch IDENTISCH zur alten Fassung (Gewicht 1 auf Rang 1) - **keine Regression für bestehende Spieler**, per Wiederholung der kompletten bereits bestehenden `guild-tech-drachenzucht.spec.js`-Suite (7/7 unverändert grün) bestätigt.
- `bkmpDragonCompanionPassiveRegenPct()` bleibt ein reiner Anwesenheits-Schalter (kein pro-Begleiter-Stat) - prüft jetzt `bkmpDragonActiveCompanions().length > 0` statt `.find()`, Wert selbst unverändert ungewichtet.
- **Zwei getrennte "is_companion"-Konzepte bleiben unabhängig**, teilen sich nur dasselbe boolesche Feld: jugendliche Drachen (wachsen heran, sammeln Kampf-EP, siehe `bkmpDragonGrantCompanionBattleXp` vom selben Tag) bleiben bei GENAU EINEM gleichzeitig und zählen NICHT gegen das neue Erwachsenen-Platzlimit - per Test bewiesen (beide gleichzeitig aktiv, ohne dass irgendeine Prestige-Investition nötig ist).
- `bkmpDragonSetCompanion(dragonId)`: blockt jetzt (statt automatisch zu ersetzen), sobald das aktuelle Limit erreicht ist - zeigt einen Hinweis-Toast statt eine bestehende Ausrüstung stillschweigend zu verdrängen (bewusst konsistent mit dem "nie ohne Bestätigung ersetzen/löschen"-Prinzip dieses Projekts). `bkmpDragonUnsetCompanion(dragonId)` braucht jetzt zwingend eine ID (vorher parameterlos, entfernte "den" einzigen Begleiter) - beide Aufrufstellen (Lager-Karte + neue Begleiter-Leiste) entsprechend angepasst.

**Begleiter-Leiste (`bkmpDragonRenderCompanionSlotBarHtml()`, neu, ganz oben im Drachenzucht-Tab):** drei feste Plätze, Genre-üblich nach der obigen Recherche - gefüllter Slot = Portrait + Rang-Prozent-Abzeichen (⭐ für Rang 1) + kleiner Ablegen-Knopf (`×`, direkt auf dem Slot), freier aber freigeschalteter Slot = gestrichelter "+"-Platzhalter, noch gesperrter Slot = 🔒-Symbol + Freischalt-Hinweis (kommuniziert aktiv "es gibt mehr", nicht nur "hier ist gerade nichts"). Bewusst als eigener Abschnitt GANZ OBEN im Drachenzucht-TAB, nicht in der immer sichtbaren Kampf-HUD - die wurde in Phase 7.1 gezielt verschlankt (mehrfach dokumentierte "kann nicht sein dass ich erstmal scrollen muss"-Beschwerde), ein dauerhafter neuer Zeilen-Zuwachs dort hätte genau das wieder aufgeweicht. Zusätzlich zeigt jede Lager-Karte eines ausgerüsteten erwachsenen Begleiters jetzt ein eigenes Rang-Abzeichen ("Rang 2 · 50%"), inkl. eines Warnhinweises für den seltenen Randfall, dass ein Drache zwar `is_companion=true` markiert ist, aber wegen eines (rechnerisch nicht vorkommenden, aber defensiv abgesicherten) Platzlimit-Konflikts gerade nicht beiträgt.

**CSS (`style.css`):** neue `.idle-dragon-companion-bar`/`-slot`/`-slot-remove`/`-rank-badge`-Klassen, wiederverwendet dieselben Kartengrundwerte wie `.idle-dragon-nest-card` (Rahmen/Radius/Padding/Hintergrund) für optische Konsistenz mit dem Rest des Tabs - gefüllte Slots bekommen einen rarity-farbigen Rahmen (identisches `--dragon-rarity-color`-Muster wie überall sonst im Drachenzucht-Tab). **Derselbe, bereits mehrfach in diesem Projekt gefundene Spezifitäts-Bug vorab vermieden:** `.idle-dragon-companion-slot-remove` bekam von Anfang an dieselben `!important`-Overrides (Radius/Padding/Mindesthöhe) wie `.idle-dragon-mini-delete-btn`, da das generische `html.bkmp-app-mode .idle-dorf-panel button`-Reset sonst aus dem runden Ablegen-Knopf wieder ein eckiges Vollformat-Rechteck gemacht hätte.

**Tests (`tests/e2e/dragon-multi-companion.spec.js`, neu, 6 Tests):** ohne Prestige-Investition bleibt nur 1 Begleiter möglich (Toast bei einem zweiten Versuch, kein stilles Ersetzen); Rang 1 (2 Plätze) - zweiter Begleiter zählt korrekt mit 50%, unabhängig von der Ausrüstungs-Reihenfolge; Rang 2 (3 Plätze) - dritter zählt mit 25%, ein vierter Versuch wird blockiert; Ablegen über die Begleiter-Leiste entfernt gezielt nur den angeklickten Begleiter; jugendlicher Begleiter bleibt unabhängig vom Erwachsenen-Limit; gesperrter Platz zeigt korrekt das Schloss-Symbol. 60/60 stabil bei 10x Wiederholung, 98/98 grün über alle 3 Projekte (34 korrekt übersprungen, Desktop-Tab-Navigation) inkl. der kompletten angrenzenden Regression (`prestige-tree-v2.spec.js` inkl. neuem "Weitere Gefährten"-Kosten-/Struktur-Test + aktualisiertem Vermächtnis-Knotenzähler 2→3, `guild-tech-drachenzucht.spec.js`, `dragon-lifecycle-release.spec.js`, `offline-companion-xp.spec.js`, `prestige.spec.js` - alle unverändert grün, keine Regression durch den Umbau).

**Verifiziert (Browser, kein echter Login möglich - injizierter Mock-Zustand über die echte `bkmpIdleRenderDragonsPanel()`-Funktion, gleiches Muster wie an vielen anderen Stellen dieses Projekts):** 2 von 2 freigeschalteten Plätzen korrekt gefüllt (153×147px je Slot, echte nicht-null Maße), gold-farbener Rahmen (`rgb(250,204,21)`) auf gefüllten Slots, korrekte Rang-Texte "⭐ 100%"/"50%", dritter (gesperrter) Slot zeigt korrekt 🔒 mit gedämpftem Rahmen, Kopfzeile zeigt korrekt "2/2". Kein echter Screenshot des Vollbild-Overlays möglich (bekannte, bereits mehrfach dokumentierte Werkzeug-Einschränkung dieses Projekts) - alle Zahlen oben sind echte `getBoundingClientRect()`/`getComputedStyle()`-Messungen, keine Schätzungen. `node --check`/`eslint` auf allen 4 geänderten/neuen `.js`-Dateien sauber, `scripts/static-checks.js` unverändert (0 CRITICAL/HIGH/MEDIUM, 58 LOW/1 INFO).

Cache-Busting `?v=20260805-multicompanion1` für `js/systems/bkmp-breeding.js`, `js/systems/bkmp-prestige.js`, `style.css` (alle einbindenden HTML-Dateien).

**Nicht geprüft/offen:** `sql/20260805-dragon-multi-companion-slots.sql` muss im Supabase-Dashboard ausgeführt werden, BEVOR das Feature live tatsächlich funktioniert - ohne die entfernte Unique-Index-Constraint würde ein zweiter Ausrüsten-Versuch gegen die echte Produktions-DB weiterhin mit einem Server-Fehler scheitern (der lokale Mock in den Tests oben kennt diese Constraint nicht, testet also nur die Client-Logik, nicht die reale DB-Sperre). Kein Ersatz-Constraint für "maximal N gleichzeitig" auf DB-Ebene (gleiche Vertrauensebene wie überall sonst im Idle-Dorf - Gebäude-Stufen/ausgerüstete Runen/Titel haben ebenfalls keine serverseitige Zähl-Absicherung).

### Nachbesserung, gleicher Tag: Auswahlfenster + 4. Trainings-Platz + Stats-Block

Direkt im Anschluss zwei weitere Nutzerwünsche nach dem ersten Screenshot der neuen Begleiter-Leiste:

1. **"Ich möchte das oben auf Platz 1 drücken kann und alle Erwachsenden Drachen angezeigt werden in einen Kleinen Fenster das man nicht Runterscrollen muss."** + **"Dann möchte ich gerne neben denn 3 ein 4 Block bisschen mit abstand wo du einen jugendlichen auswählen kannst zum tranieren."**
2. **"Und hier muss die Stats stylisch angezeigt werden. Inspiriere dich dafür auch in anderen Games? Links oder rechts 1 einzelnen Block wo nur die Stats stehen?"** - kurze Recherche zu Tactics-RPG-Stat-Panels bestätigte das erwartete Muster (Icon + Bezeichnung + Wert je Zeile, oft in einem eigenen schmalen Panel neben dem Charakter-Portrait).

**Auswahlfenster (`bkmpDragonOpenCompanionPicker`, neu):** jeder nicht gesperrte Platz (alle 3 Kampf-Plätze + der neue 4. Trainings-Platz) ist jetzt anklickbar und öffnet ein kompaktes Fenster mit ALLEN passenden Drachen (Kampf-Plätze → erwachsene, Trainings-Platz → jugendliche) - wiederverwendet `bkmpUiModalHtml()`/`bkmpUiTrapFocus()` (Phase 3, zweiter echter Einsatz im Idle-Dorf nach dem Gilden-Technologie-Beitrags-Modal, identisches Portal-Muster: `document.body.insertAdjacentHTML`, kein eigener z-index nötig, da später im DOM angehängt als `#idleDorfOverlay` und dadurch bei gleicher `.joke-overlay`-Basis automatisch korrekt darüber liegt). Die Drachenliste selbst hat eine feste Höhenbegrenzung mit eigenem Scrollen (`min(52vh, 420px)`) - das Fenster und die Seite dahinter müssen dafür nie scrollen, nur die Liste bei sehr vielen Drachen. Ein Klick auf einen Eintrag schaltet dessen Begleiter-Status direkt um (ausrüsten/ablegen) und aktualisiert die Liste in-place, ohne das Fenster zu schließen - mehrere Begleiter lassen sich so in einem Rutsch zusammenstellen.

**4. Block "Trainieren" (`is-teen-slot`):** ersetzt die bisherige reine Textzeile "Wächst gerade heran: X" - eigene Flex-Gruppe mit größerem Gap zu den 3 Kampf-Plätzen (`.idle-dragon-companion-teen-wrap`, verschachtelte Flex-Gruppen statt `margin-left` auf dem 4. Slot direkt, damit beim Zeilenumbruch auf schmalen Breiten kein einseitiger Einzug hängen bleibt), eigenes Icon (🐣 statt ➕/🔒) zur klaren Unterscheidung von den Kampf-Plätzen.

**Stats-Block (`bkmpDragonRenderCompanionStatsBlockHtml`, neu, rechts neben der Leiste):** zeigt bewusst NICHT die Rohwerte eines einzelnen Drachens (die stehen bereits auf jeder Lager-Karte), sondern den TATSÄCHLICH WIRKSAMEN Gesamtbonus aus `bkmpIdleDragonCompanionEffectTotals()` - also inklusive Gewichtung (100/50/25%) UND aller Prestige-/Gilden-Tech-Boni. Das ist die für den Spieler eigentlich relevante Zahl ("was bringt mir das System gerade insgesamt"), keine reine Kopie vorhandener Karteninfos. Drei Zeilen (⚔️ Angriff/🛡️ Verteidigung/❤️ Leben, Icon+Bezeichnung+Wert), reagiert live auf jede Änderung der Begleiter-Auswahl. `.idle-dragon-companion-row` ordnet Leiste (links, `flex:1 1 auto`) und Stats-Block (rechts, feste Breite ~200px) nebeneinander an, bricht auf schmalen Breiten sauber um (Stats-Block rutscht unter die Leiste).

**5 neue Tests** (`tests/e2e/dragon-multi-companion.spec.js`, jetzt 11 insgesamt): Klick auf freien Kampf-Platz öffnet das Fenster mit allen erwachsenen Drachen (kein Scrollen nötig, `toHaveCount(2)` ohne Pagination), Klick auf einen Eintrag rüstet ihn aus und aktualisiert das Fenster in-place; Klick auf den Trainings-Platz öffnet ein auf Jugendliche gefiltertes Fenster (erwachsene Drachen korrekt ausgeblendet); Klick auf einen bereits ausgerüsteten Eintrag legt ihn wieder ab (Umschalten); ein gesperrter Platz öffnet gar kein Fenster (`idleDragonCompanionPickerOverlay` wird nie erzeugt); der Stats-Block zeigt vor jeder Ausrüstung korrekt "+0" und nach zwei Ausrüstungen exakt den erwarteten gewichteten Wert (100+40×0,5=120 usw.). Der bereits bestehende Test für den jugendlichen Begleiter wurde auf den neuen 4. Block umgestellt (die alte Textzeile existiert nicht mehr). **88/88 stabil bei 8x Wiederholung der kompletten 11-Test-Datei, 98/98 grün über alle 3 Projekte** (34 korrekt übersprungen) inkl. der bereits bestehenden Regressionsdateien (Prestige-Baum/Gilden-Drachenzucht/Drachen-Lebenszyklus/Offline-Begleiter-EP).

**Verifiziert (Browser, kein echter Login möglich - injizierter Mock-Zustand über die echten Funktionen, `updatePlayerDragon` dabei gestubbt, da diese isolierte Ad-hoc-Prüfung ohne QA-Mock-Server läuft - die eigentliche Playwright-Suite oben läuft bereits erfolgreich gegen den echten QA-Mock):** Klick auf leeren Platz öffnet das Fenster mit beiden erwachsenen Testdrachen (kein Paging/Scroll-Zwang), Klick rüstet aus und zeigt danach korrekt "Rang 1 · 100%"; nach Ausrüsten eines zweiten (schwächeren) Drachens zeigt der Stats-Block korrekt "+120/+60/+250" (exakt die erwartete 100%+50%-Gewichtung); Klick auf den Trainings-Platz zeigt korrekt nur den einen jugendlichen Testdrachen, keinen erwachsenen. Kein echter Screenshot möglich (bekannte Werkzeug-Einschränkung), alle Werte oben sind echte DOM-Messungen.

Cache-Busting `?v=20260805-multicompanion2` für `js/systems/bkmp-breeding.js`, `style.css` (alle einbindenden HTML-Dateien).

### Bugfix, gleicher Tag: Trainings-Platz erschien mitten zwischen den Kampf-Plätzen statt sauber getrennt

Nutzer-Screenshot vom LIVE-Stand (echter Spielstand, Drachen "Zerathor"/"Koradrache"): die Leiste zeigte in der falschen Reihenfolge "Zerathor (gefüllt) - Gesperrt - Koradrache (Trainings-Platz)" auf Zeile 1, mit einem weiteren "Gesperrt" auf einer zweiten, eingerückten Zeile - der Trainings-Platz landete optisch MITTEN zwischen den 3 Kampf-Plätzen statt sauber danach/darunter getrennt. Nutzer-Nachfrage: "Was denn da passiert. Bitte ordentlich machen." **Nichts committed/gepusht.**

**Root Cause (bestätigt, nicht vermutet):** `.idle-dragon-companion-combat-group` (die 3 Kampf-Plätze) hatte selbst `flex-wrap: wrap` OHNE feste Breite. Bei knappem verfügbarem Platz (die Leiste steht in einer schmaleren Karte, nicht auf voller Fensterbreite) ließ das den Browser `combat-group` UND `teen-wrap` (den 4. Block) gemeinsam in dieselbe Zeile quetschen - wodurch `combat-group`s EIGENE 3 Kinder unabhängig vom Trainings-Platz intern umbrachen, statt dass die äußere Leiste (`.idle-dragon-companion-bar`) den Trainings-Platz sauber als GANZES auf eine neue Zeile verschoben hätte. Das Ergebnis: eine willkürlich wirkende Durchmischung statt der beabsichtigten klaren Trennung "3 Kampf-Plätze | (Abstand) | 1 Trainings-Platz".

**Fix (`style.css`):** `.idle-dragon-companion-combat-group` bekommt `flex-wrap: nowrap` - seine 3 Kinder bleiben dadurch IMMER zusammen in einer Reihe (dürfen bei knappem Platz gemeinsam schrumpfen, dank des bereits bestehenden `flex-shrink`s an den einzelnen `.idle-dragon-companion-slot`-Elementen, aber nie intern umbrechen). Die äußere `.idle-dragon-companion-bar` entscheidet dadurch sauber binär: entweder passen beide Gruppen nebeneinander (schrumpfen dabei gemeinsam), oder der Trainings-Platz rutscht ALS GANZES in die nächste Zeile - nie ein einzelner Kampf-Platz allein.

**Verifiziert (Browser, injizierter Mock-Zustand über die echte `bkmpIdleRenderDragonsPanel()`-Funktion, exakt den gemeldeten Fall nachgestellt - 1 gefüllter Kampf-Platz + 2 gesperrte + 1 gefüllter Trainings-Platz):** bei 480px Containerbreite liegen jetzt alle 4 Plätze korrekt auf EINER Zeile (alle schrumpfen gemeinsam auf ~67-74px statt der nominellen 110px); bei einer noch schmaleren 300px-Breite bleiben die 3 Kampf-Plätze weiterhin korrekt zusammen auf Zeile 1, nur der Trainings-Platz rutscht sauber als komplette Einheit auf Zeile 2 - in beiden Fällen kein einzelner Kampf-Platz reißt mehr ab. Neuer, deterministischer Regressionstest (`tests/e2e/dragon-multi-companion.spec.js`, erzwingt echte Fenstergrößen 480px/300px per `page.setViewportSize()` und prüft die tatsächlichen `getBoundingClientRect().top`-Werte) - 10/10 stabil bei Wiederholung, volle 12-Test-Datei weiterhin grün.

Cache-Busting `style.css?v=20260805-multicompanion3` (alle einbindenden HTML-Dateien).

### Nachbesserung, gleicher Tag: viel ungenutzter Leerraum zwischen Leiste und Stats-Block

Nutzer-Screenshot vom LIVE-Stand nach dem vorherigen Fix (korrekt einzeilig, aber eine große sichtbare Lücke zwischen der Begleiter-Leiste und dem Stats-Block rechts): "Wir haben sooviel platz. Nutze denn doch mal." **Nichts committed/gepusht.**

**Root Cause (bestätigt, nicht vermutet):** `.idle-dragon-companion-combat-group`/`.idle-dragon-companion-teen-wrap` hatten kein `flex-grow` (Browser-Standard `0`). Die äußere `.idle-dragon-companion-bar` trägt zwar `flex: 1 1 auto` und beansprucht dadurch korrekt die volle verfügbare Zeilenbreite - dieser gewonnene Platz wurde aber nie an die beiden Kind-Gruppen weitergereicht, da keine von beiden `flex-grow` gesetzt hatte. Die Plätze blieben dadurch auf ihrer Mindestgröße (110px Flex-Basis) stecken, während der ganze übrige Platz als sichtbarer Leerraum vor dem Stats-Block liegen blieb.

**Fix (`style.css`):** proportionales `flex-grow: 3` auf `.idle-dragon-companion-combat-group` (3 Plätze) und `flex-grow: 1` auf `.idle-dragon-companion-teen-wrap` (1 Platz) - entspricht dem tatsächlichen 3:1-Verhältnis der enthaltenen Slots, sodass beide Gruppen im selben Tempo wachsen. Zusätzlich `max-width` der einzelnen Plätze von 160px auf 220px angehoben (plus etwas größere Schrift/Bild-Maße) - ohne diese Anhebung hätte `flex-grow` nur zusätzliches, unsichtbares Innenpolster erzeugt statt einen sichtbaren Unterschied zu machen.

**Verifiziert (Browser, injizierter Mock-Zustand über die echte `bkmpIdleRenderDragonsPanel()`-Funktion):** bei 1150px Container-Breite wuchsen die Plätze von ~90px auf ~206-211px, die Lücke zwischen Leiste und Stats-Block fiel von ~500+px auf ~26px (ein normaler Flex-Gap, kein toter Leerraum mehr). Das schmale Verhalten aus dem vorherigen Bugfix (Zusammenbleiben/Umbruch bei wenig Platz) blieb bei erneuter Prüfung mit 480px/300px unverändert korrekt - `flex-grow` greift nur bei tatsächlich vorhandenem Restplatz, nie beim Schrumpfen. Neuer, deterministischer Regressionstest (`tests/e2e/dragon-multi-companion.spec.js`, erzwingt 1400px Fensterbreite, beweist per `getBoundingClientRect().width`, dass alle 4 Plätze über 180px wachsen, UND dass der Abstand zwischen Leiste und Stats-Block unter 60px bleibt) - 10/10 stabil bei Wiederholung, volle 13-Test-Datei weiterhin grün.

Cache-Busting `style.css?v=20260805-multicompanion4` (alle einbindenden HTML-Dateien).

### Nachbesserung, gleicher Tag: permanenter Hinweistext im "Vorräte"-Block hinter ein "ℹ️" versteckt

Nutzer-Nachricht (mid-turn, während an einem anderen Punkt gearbeitet wurde), bezogen auf den unter Obstgarten/Jagdhütte immer sichtbaren Satz "Gebäude-Upgrades findest du im Tab '⬆️ Upgrades'.": "Diese Anleitung kann auch ganz nach unten. Oder irgendwo hinter einen 'I' versteckt als Hilfe Anleitung." **Nichts committed/gepusht.**

**Umsetzung:** statt eines reinen Umzugs nach unten wurde die bereits bestehende, aber bis dahin nur ein einziges Mal genutzte Tooltip-Komponente (`bkmpUiTooltipHtml()`/`bkmpUiWireTooltipTrigger()`, Phase 3 - erster Einsatz beim Paragon-Hinweis im Prestige-Baum, 28.07.) wiederverwendet: ein kleiner, tap-/hoverfähiger "ℹ️"-Auslöser direkt neben der "🍎🥩 Vorräte"-Überschrift ersetzt den vorher dauerhaft sichtbaren `<p>`-Satz, identisches Muster wie beim Paragon-Hinweis (`data-tooltip-id`, `bkmpUiWireTooltipTrigger()` im Klick-Wiring-Block von `bkmpIdleRenderDragonsPanel()`).

**Verifiziert (Browser, injizierter Mock-Zustand über die echte `bkmpIdleRenderDragonsPanel()`-Funktion):** Tooltip standardmäßig unsichtbar (`opacity:0`, `visibility:hidden`, `pointer-events:none` per CSS), zeigt sich korrekt bei simuliertem `mouseenter` (`.visible`-Klasse gesetzt, `visibility:visible`) und schließt sich wieder bei `mouseleave` - kein dauerhaft sichtbarer Hinweistext mehr im Panel, keine Konsolenfehler. Bestehende `dragon-multi-companion.spec.js`(13 Tests)/`dragon-lifecycle-release.spec.js`(6 Tests)-Suiten unverändert grün (kein Selektor hing an der alten `<p>`-Struktur).

Cache-Busting `js/systems/bkmp-breeding.js?v=20260805-multicompanion5`, `style.css?v=20260805-multicompanion5` (alle einbindenden HTML-Dateien).

## Drachenlager-Filter: 3 Dropdowns durch Rarity-Schnellfilter-Knöpfe ersetzt (05.08.2026)

Nutzer-Screenshot der 3 `<select>`-Dropdowns im Drachenlager-Filter ("Alle Seltenheiten"/"Alle Stufen"/"Sortieren: Seltenheit"): "So bitte diese Sortierung entfernen. Einfach nur Buttons mit 'Legendäre' 'Epische' 'Seltene' Das reicht schön nebeneinander neben favoriten." **Nichts committed/gepusht.**

**Umsetzung (`js/systems/bkmp-breeding.js`):** `bkmpDragonLagerFilter` von `{ rarity, stage, favoritesOnly, sort }` auf `{ rarity, favoritesOnly }` verschlankt - `stage`/`sort` waren nur über die jetzt entfernten Dropdowns änderbar, sind seitdem unerreichbarer Code gewesen und wurden konsequent entfernt (inkl. des nur dafür genutzten `dragonStrength`-Helfers und des `switch`-Sortier-Blocks) statt als toter Pfad stehenzubleiben. Sortierung ist jetzt fest auf Seltenheit (`RARITY_ORDER`) - war ohnehin bereits der Standardwert, kein sichtbarer Unterschied für niemanden, der den Sort-Dropdown nie angefasst hat. Die 3 `<select>`-Elemente wurden durch 3 einfache Umschalt-Knöpfe (`.idle-dragon-rarity-filter-btn`, Reihenfolge Legendäre/Epische/Seltene wie vom Nutzer genannt) ersetzt - Klick auf den bereits aktiven Knopf setzt den Filter zurück auf "alle" (kein separater "Alle"-Knopf nötig, reines Toggle-Verhalten). Standard-Rarity hat bewusst keinen eigenen Knopf (nicht vom Nutzer genannt) - bleibt aber im "alle"-Zustand weiterhin sichtbar, nur nicht gezielt anwählbar. Der bereits bestehende "★ nur Favoriten"-Schalter (`#idleDragonFilterFav`) bleibt unverändert in derselben Zeile direkt daneben ("schön nebeneinander").

**CSS:** neue `.idle-dragon-rarity-filter-btn`-Pille, gleiches Chip-Muster wie das bereits bestehende `.fbb-filter-chip` (Feedback-Board-Filter), aktiver Zustand faerbt sich in der jeweiligen Rarity-Farbe (`--rarity-filter-color`, aus `bkmpDragonRarityMeta()` - `#facc15` Legendär/`#a78bfa` Episch/`#38bdf8` Selten). Bereits vorab bekannter Spezifitäts-Konflikt beachtet (identisches Muster wie `.idle-dragon-mini-delete-btn`/`.idle-dragon-companion-slot-remove`): das generische `html.bkmp-app-mode .idle-dorf-panel button`-Reset (14px Radius, großes Innenpolster, 46px Mindesthöhe) hätte die schlanke Pille sonst wieder in ein großes eckiges Rechteck verwandelt - `border-radius`/`padding`/`min-height`/`font-size` tragen deshalb `!important`. Die bestehende App-Modus-Touch-Target-Regel (`html.bkmp-app-mode .idle-dorf-panel .idle-dragon-filter-fav, ... select`) wurde auf die neue Button-Klasse umgestellt (44px Mindesthöhe bleibt erhalten), ebenfalls mit `!important`, da sie sonst gegen die bereits `!important`-markierte Basisregel verloren hätte.

**Verifiziert (Browser, injizierter Mock-Zustand über die echte `bkmpIdleRenderDragonsPanel()`-Funktion, 4 Testdrachen je einer Rarity):** genau 3 Knöpfe (Legendäre/Epische/Seltene), keine `<select>`-Elemente mehr im Filterbereich; Klick auf "Legendäre" filtert korrekt auf 1 Karte + `active`-Klasse gesetzt; erneuter Klick auf denselben Knopf setzt zurück auf 4 Karten + `active` entfernt; computed style bestätigt echte Pillenform (`border-radius:999px`, nicht der generische 14px-Reset) + korrekte Rarity-Einfärbung (`rgb(167,139,250)` = `#a78bfa` bei Episch aktiv). Neuer Regressionstest (`tests/e2e/dragon-lager-rarity-filter.spec.js`, 3 Tests: Knopf-Inventar+keine Dropdowns, Filter-Toggle-Verhalten inkl. Knopfwechsel, Favoriten-Schalter weiterhin unabhängig funktionsfähig) - grün, plus voller Regressionslauf (`dragon-multi-companion`/`dragon-lifecycle-release`/`offline-companion-xp.spec.js`, 27 Tests) unverändert grün.

Cache-Busting `js/systems/bkmp-breeding.js?v=20260805-multicompanion6`, `style.css?v=20260805-multicompanion6` (alle einbindenden HTML-Dateien).

## Prestige-Baum-Knotenkarte: fehlender Kopfzeilen-Wrapper + unstyled Beschreibung (05.08.2026)

Nutzer-Screenshot des "Weitere Gefährten"-Knotens (Icon gross oben, Titel/Rang darunter zusammengequetscht, Beschreibungstext in falscher Farbe/Grösse): "Sieht schrecklich aus." **Nichts committed/gepusht.**

**Root Cause (bestätigt, nicht vermutet, zwei unabhängige Funde):** `bkmpPrestigeRenderBranchGridHtml()` (`js/systems/bkmp-prestige.js`) setzte Icon+Name bisher als flache Flex-Kinder direkt in `.idle-upgrade-card` statt in den seit Phase 5.4 (18.07.2026, siehe Kommentar bei `.idle-upgrade-card-head` in style.css) etablierten `.idle-upgrade-card-head`/`.idle-upgrade-card-title`-Wrapper, den `idledorf.js`s `bkmpIdleUpgradeCardHtml()` (normaler Upgrades-Tab) bereits korrekt nutzt - das Icon rutschte dadurch als eigene grosse Zeile UEBER den Titel statt daneben zu stehen (per `getComputedStyle`/`getBoundingClientRect` bewiesen, Icon `font-size:2rem` im global aktiven App-Modus). **Zweiter, unabhängiger Fund an derselben Karte:** `.idle-upgrade-desc` (die Beschreibungszeile) hatte in `style.css` **überhaupt keine eigene CSS-Regel** - reiner unstyled Browser-Default-Text (`rgb(0,0,0)`, kein `overflow-wrap`), komplett inkonsistent mit dem Rest der Karte.

**Fix:** `bkmpPrestigeRenderBranchGridHtml()`s Kartenmarkup auf exakt dasselbe Muster wie `bkmpIdleUpgradeCardHtml()` umgestellt (`<div class="idle-upgrade-card-head"><span class="idle-upgrade-icon">...</span><div class="idle-upgrade-card-title"><span class="idle-upgrade-name">...</span><span class="idle-upgrade-level">...</span></div></div>`). Neue `.idle-upgrade-desc`-Regel (`style.css`, `font-size:0.74rem; color:var(--muted); line-height:1.4; overflow-wrap:break-word`, gleiche Grössenordnung wie `.idle-upgrade-effect-row`) + in die bestehende App-Modus-Beschreibungstext-Override-Gruppe (`.idle-upgrade-effect-row, .idle-skin-desc`) aufgenommen für konsistente Grösse auch dort.

**Verifiziert (Browser, echte `bkmpPrestigeRenderBranchGridHtml()`-Funktion direkt aufgerufen, exakt der gemeldete Knoten "Weitere Gefährten" nachgestellt):** Icon+Titel jetzt nachweislich in einer Zeile (vertikaler Ueberlapp, `align-items:center` zentriert beide trotz unterschiedlicher Hoehe), Name/Rang sauber als zwei getrennte Zeilen im Titel-Block, Beschreibungsfarbe jetzt `rgb(183,168,196)` (= `--muted`-Token) statt Schwarz, Fontgrösse kontrolliert (13.44px im App-Modus). Vorher/Nachher-Screenshot-Vergleich am exakt gemeldeten Knoten bestätigt die Behebung visuell. Neuer Regressionstest (`tests/e2e/prestige-tree-v2.spec.js`, ruft die echte Render-Funktion direkt auf, kein Tab-Klick nötig) - 16/16 grün in der gesamten Datei, plus 56 Tests der breiteren Prestige-Suite (`prestige.spec.js`/`ascension-and-sinks`/`prestige-automation-*`/`dungeon-key-prestige-bonus`) unverändert grün. `npm run qa:static` sauber (0 CRITICAL/HIGH/MEDIUM, unveränderte Baseline).

Cache-Busting `js/systems/bkmp-prestige.js?v=20260805-multicompanion7`, `style.css?v=20260805-multicompanion7` (alle einbindenden HTML-Dateien).

## Gesamtbonus-Block zeigte nur Hauptwerte, keine Substat-Prozentboni (05.08.2026)

Nutzer-Screenshot-Vergleich (Drachen-Detailkarte mit "Drachen-EP +27.7%"/"Goldbonus +7.1%"/"Krit-Schaden +10.7%"/"Fleischproduktion +12.3%" gegen den "Gesamtbonus"-Block, der nur Angriff/Verteidigung/Leben zeigte): "Da fehlen aber noch so paar Bonuse." **Nichts committed/gepusht.**

**Root Cause (bestätigt, nicht vermutet):** `bkmpDragonRenderCompanionStatsBlockHtml()` (`js/systems/bkmp-breeding.js`) rendert seit ihrer Einführung am selben Tag ausschließlich 3 fest verdrahtete Zeilen (`attack_flat`/`defense_flat`/`hp_flat`) - ignoriert dabei komplett, dass `bkmpIdleDragonCompanionEffectTotals()` bereits 9 der 12 möglichen Substat-Typen (siehe `BKMP_DRAGON_SUBSTAT_POOL`) korrekt gewichtet in `totals` aggregiert (z.B. `crit_damage_pct`, `gold_find_pct`) - diese landeten dort, wurden aber nie angezeigt. Die übrigen 3 (`fruit_bonus_pct`/`meat_bonus_pct`/`dragon_xp_bonus_pct`) sind in `bkmpIdleDragonCompanionEffectTotals()` **absichtlich** ausgeschlossen (haben ihren eigenen, bereits korrekt gewichteten Zugriffspfad `bkmpDragonSubstatBonus(key)`, um keine zweite Berechnung zu duplizieren) - waren dadurch über den Stats-Block bisher überhaupt nicht erreichbar.

**Fix:** `bkmpDragonRenderCompanionStatsBlockHtml()` iteriert jetzt zusätzlich über `BKMP_DRAGON_SUBSTAT_POOL` - für die 9 nicht-ausgeschlossenen Keys wird der Wert direkt aus `totals` gelesen, für die 3 ausgeschlossenen über `bkmpDragonSubstatBonus(key)` (keine zweite Berechnung, derselbe bereits korrekte Pfad). Nur Zeilen mit einem tatsächlich positiven Wert werden gezeigt (kein "+0%"-Wall für nie gerollte Substat-Typen). Neue `BKMP_DRAGON_SUBSTAT_ICONS`-Map ordnet jedem der 12 Substat-Typen ein Icon zu, das an bereits im Projekt etablierten Konventionen orientiert ist (⚔️/🛡️/❤️ wie die Hauptwerte, 💰/💎 wie `bkmpIdleResourceEmoji()`, 🍎/🥩 wie die "Vorräte"-Sektion, 🐲 wie der bereits bestehende "🐲 +N Kampf-EP"-Offline-Chip) - keine neu erfundenen Symbole. Prozent-Zeilen runden auf eine Dezimalstelle (matcht die Anzeige auf der Drachen-Detailkarte, `bkmpIdleFormatNumber()` selbst würde per `Math.floor()` ganzzahlig runden und z.B. "27.7%" auf "27%" abschneiden), Flach-Zeilen nutzen weiterhin `bkmpIdleFormatNumber()`.

**Verifiziert (Browser, echte `bkmpDragonRenderCompanionStatsBlockHtml()`-Funktion mit den exakten 4 Werten aus dem Nutzer-Screenshot aufgerufen):** alle 7 Zeilen korrekt (3 Hauptwerte + 4 Substats: "🐲 Drachen-EP +27.7%", "💰 Goldbonus +7.1%", "💥 Krit-Schaden +10.7%", "🥩 Fleischproduktion +12.3%"), Screenshot-bestätigt. Neuer Regressionstest (`tests/e2e/dragon-multi-companion.spec.js`, exakt dieselben 4 Substat-Werte wie im Report, prüft zusätzlich `normalized.length===7` gegen einen unerwarteten "+0%"-Zeilen-Wall) - 14/14 grün in der gesamten Datei, plus 35 Tests der breiteren Regression (`dragon-lifecycle-release`/`dragon-lager-rarity-filter`/`offline-companion-xp`/`guild-tech-drachenzucht` - letztere nutzt `bkmpIdleDragonCompanionEffectTotals()` ebenfalls, unverändert grün) unverändert grün. `npm run qa:static` sauber (0 CRITICAL/HIGH/MEDIUM, unveränderte Baseline).

Cache-Busting `js/systems/bkmp-breeding.js?v=20260805-multicompanion8` (alle einbindenden HTML-Dateien).

## Server-seitiger Anti-Cheat-Guard: Aufbau, kritische eigene Regression, Fix (30.07.–11.08.2026)

Zeitleiste einer mehrteiligen Sicherheitsmaßnahme gegen Cheat-Engine/Timer-Speedhacks - wichtig für künftige Sessions, da der zweite Teil (09.08.) eine ernste, selbst verursachte Regression war, die am 11.08. echte Spieler traf. **Alles unten bezieht sich auf `idle_player_state` (Postgres-Trigger `idle_player_state_anticheat_guard()`), nicht auf andere Tabellen.**

**30.07.2026 (Grundlage):** `sql/20260730-idle-player-state-anticheat-guard.sql` - `BEFORE UPDATE`-Trigger, dämpft `dragon_kills`/Gold/Ressourcen proportional, wenn der Zuwachs die reale Wanduhrzeit (`now() - OLD.updated_at`) implausibel übersteigt (`MAX_KILLS_PER_SECOND=3`). Schreibt bei Verstoß in neue Tabelle `idle_anticheat_flags`.

**09.08.2026, Teil 1:** Spieler-Meldung "9999999 Skillpunkte" + Bestenliste mit "Level 846473" - der 30.07.-Trigger dämpfte Level/Skillpunkte NUR als Nebeneffekt, wenn ZUERST `dragon_kills` implausibel war; ein gezieltes Update, das nur `level`/`skill_points_available` setzt, rutschte durch. Fix: `sql/20260809-anticheat-guard-independent-fields.sql` - zwei zusätzliche, unabhängige Zeit-Budget-Signale (Level, Skillpunkte-SUMME wegen Skilltree-Reset-Umbuchung).

**09.08.2026, Teil 2:** Spieler "OPShadowWolf" schickte 3 Videos vom eigenen Account (`attack=420.45M` im HUD, jeder Boss instant one-shot) - `attack/defense/hp/crit_chance/crit_damage/gold_bonus/xp_bonus/loot_bonus` waren komplett ungeschützt (Teil desselben Upserts, aber nie geprüft). Fix: `sql/20260809-anticheat-guard-combat-stats.sql` - harte, relative Multiplikator-Obergrenze (**50x + fester Sockel pro einzelner Speicherung**), da Kampfwerte keine Zeit-Akkumulatoren sind (bei jeder Runen-/Skilltree-/Prestige-/Gilden-Tech-Änderung komplett neu berechnet).

**09.08.2026, Teil 3 (direkt danach):** Nutzerwunsch "schnell steigende Zahlen nur im AdminPanel sehen können, nicht direkt in der Bestenliste" - `sql/20260809-leaderboard-hide-mechanism.sql`: neue Tabelle `idle_leaderboard_hidden_accounts` (admin-only, manuelle Ausblend-Liste) + View `public.idle_player_state_leaderboard`, die **automatisch JEDEN Account mit einem undismissed `idle_anticheat_flags`-Eintrag ausblendet**, zusätzlich zur manuellen Liste. `loadIdleLeaderboardStats()` (supabase.js) liest seitdem von der View statt direkt von `idle_player_state`.

**⚠️ 11.08.2026 - KRITISCHE EIGENE REGRESSION, gefunden per Nutzer-Meldung "einige nicht mehr in der Leaderboards angezeigt":** die 50x-Grenze aus Teil 2 war viel zu eng für dieses Spiel - ein einzelner legitimer Burst (Prestige-Reset+Neu-Investition, ein Gilden-Technologie-Sprung, Runen-Aufwertung/-Verschmelzung/-Aufstieg, mehrstufiger Auto-Kauf) kann Kampfwerte in EINER Speicherung völlig normal um mehr als das 50-fache steigen lassen. Kombiniert mit dem automatischen Auto-Hide aus Teil 3 (kein menschlicher Review-Schritt) verschwanden dadurch **33 von 217 Accounts (15%!) von der öffentlichen Bestenliste** - per direktem `curl` gegen die echte Produktions-DB bestätigt (Diff `idle_player_state` vs. `idle_player_state_leaderboard`). Praktisch ausnahmslos lange dokumentierte, echte Spieler, darunter **der eigene Account des Betreibers (ChronoKora)** und **OPShadowWolf selbst** (dessen Demo-Videos den ursprünglichen Bug überhaupt erst aufdeckten - seine eigene Test-Speicherung löste vermutlich den Flag aus). Zur Einordnung: `bkmpIdleRecomputeEffectiveStats()` (idledorf.js) berechnet Kampfwerte bei jedem Öffnen des Fensters (und an vielen weiteren Stellen) komplett neu, unabhängig vom zuletzt in der DB gespeicherten Wert - **kein Spieler hat dadurch echt an Kampfkraft verloren**, der einzige tatsächliche Schaden war die Bestenlisten-Unsichtbarkeit. Kalibrierungs-Referenz (per Live-Abfrage der stärksten NICHT betroffenen Accounts, Level 5900-9200): `attack` ~13.500-15.000, `defense` ~6.300-9.400, `hp` ~12.800-14.800, `crit_damage` ~500-556, `gold_bonus`/`xp_bonus`/`loot_bonus` ~300-970 - der gemeldete echte Exploit (420,45M) liegt ca. das 28.000-fache darüber.

**Zwei-teiliger Fix (beide noch nicht ausgeführt, siehe unten):**
1. `sql/20260811-leaderboard-hide-decouple-from-flags.sql` - View blendet ab sofort **NUR NOCH manuell** ausgeblendete Accounts aus (`idle_leaderboard_hidden_accounts`), kein automatisches Ausblenden mehr durch einen rohen Alarm. `idle_anticheat_flags` bleibt vollständig bestehen als reine Admin-Panel-Telemetrie - der Admin entscheidet bewusst per "🙈 Account dauerhaft ausblenden"-Knopf (bereits im Admin-Panel vorhanden, 09.08. gebaut). Entspricht exakt dem ursprünglichen Nutzerwunsch aus Teil 3 - der Auto-Hide-Automatismus war ein Konstruktionsfehler, keine bewusste Entscheidung.
2. `sql/20260811-anticheat-guard-absolute-ceiling.sql` - Kampfwerte-Prüfung von relativer 50x-Grenze auf **absolute, vom Vorwert komplett unabhängige Obergrenze** umgestellt (`attack`/`defense`: 1.000.000, `hp`: 2.000.000, `crit_chance`: 100, `crit_damage`: 5.000, `gold_bonus`/`xp_bonus`/`loot_bonus`: 10.000 - jeweils 70-140x über den tatsächlich stärksten Accounts im Spiel). Ein legitimer Burst, egal wie groß, wird dadurch nie mehr fälschlich als Forgery gewertet - nur das Ergebnis muss plausibel bleiben, kein Risiko mehr aus dem "Ratio gegen einen einzelnen Vorwert"-Ansatz.
3. `sql/20260811-anticheat-flags-cleanup-optional.sql` - **optional**, räumt den bestehenden ~33-Alarm-Rückstau im Admin-Panel auf (setzt `dismissed=true`, reine Aufräum-Maßnahme, keine Sicherheitsauswirkung mehr seit Fix 1).

Admin-Panel-Texte (`admin.html`) korrigiert - behaupteten bisher fälschlich "ein Alarm blendet sofort aus der Bestenliste aus". `supabase.js`s Kommentar bei `loadIdleLeaderboardStats()` aktualisiert. **Neue Testdatei `tests/e2e/leaderboard-hide-mechanism.spec.js`** (6 Tests) - `tests/mock/rest-engine.js` bildet dafür erstmals die reale Postgres-VIEW nach (`computeLeaderboardViewRows()`), gab es beim ursprünglichen Bau am 09.08. noch nicht (wäre die Regression sonst wohl sofort aufgefallen). Kampfwerte-Tests in `tests/e2e/anticheat-progress-rate-guard.spec.js` auf die neue absolute Logik umgeschrieben, inkl. explizitem Regressionstest für einen großen legitimen Burst (500.000 attack in einer Speicherung, vorher fälschlich gekappt worden). 20/20 grün, 200/200 stabil bei 10x Wiederholung, 3-Projekte-Regression sauber (60 bestanden, 48 korrekt übersprungen auf Mobile). `eslint`/`node --check`/`scripts/static-checks.js` sauber.

**Nicht geprüft/offen:** `sql/20260811-leaderboard-hide-decouple-from-flags.sql` (**DRINGEND**) muss im Supabase-Dashboard ausgeführt werden, bevor die 33 betroffenen Accounts live wieder sichtbar werden. `sql/20260811-anticheat-flags-cleanup-optional.sql` ist rein optional. Öffentlicher Changelog-Eintrag vorbereitet: `sql/20260811-changelog-leaderboard-hide-fix.sql` (bewusst ohne Anti-Cheat-Details, nur der für Spieler sichtbare Effekt).

**⚠️ Nachtrag, noch später am 11.08.2026 - `sql/20260811-anticheat-guard-absolute-ceiling.sql` NICHT MEHR VERWENDEN:** Spieler "OPShadowWolf" meldete per Screenshot, dass er nach dem (vermutlich erfolgten) Ausführen dieser Datei **gar nicht mehr speichern konnte** - wiederholte `PATCH .../idle_player_state`-Fehlschläge, HTTP 400, Postgres-Code `22P02` "malformed array literal: \"dragon_kills\"". Root Cause **nicht abschließend geklärt** - kein DB-Schreibzugriff für diese Session, um die echte Spaltendefinition von `idle_anticheat_flags.triggered_by` direkt einzusehen (PostgRESTs OpenAPI-Schema ist für `anon` leer, `is_active_admin()`-RLS blockiert anonymes Lesen der Tabelle). Arbeitshypothese: die Spalte war/ist entgegen der eigentlichen Absicht ein Array-Typ statt `text` - `alter table ... add column if not exists triggered_by text` (09.08.) hätte einen bereits falschen Bestandstyp NIE nachträglich korrigiert (reiner No-Op, sobald die Spalte existiert), `array_to_string(...)` liefert einen unbeklammerten String wie `"dragon_kills"`, der beim impliziten Cast in eine Array-Spalte exakt mit dieser Fehlermeldung scheitert. **Wichtig, was DAGEGEN spricht (ehrlich dokumentiert, nicht einfach als bewiesen hingenommen):** die 33 vorher erfolgreich geflaggten (und dadurch fälschlich ausgeblendeten) Accounts beweisen, dass `INSERT ... triggered_by` VOR dem 11.08.-Deploy zuverlässig funktioniert hat - der exakte Auslöser-Mechanismus für das JETZT beobachtete Scheitern ist damit nicht wirklich verstanden, nur die defensive Absicherung selbst ist zweifelsfrei richtig.

**Fix (unabhängig von der exakten Ursache):** `sql/20260811-anticheat-guard-flag-insert-safety-net.sql` (**ERSETZT `sql/20260811-anticheat-guard-absolute-ceiling.sql` VOLLSTÄNDIG - nur diese neueste Datei muss noch ausgeführt werden**) - zwei Teile: (1) strukturelles Sicherheitsnetz, das nach diesem Vorfall dauerhaft gelten sollte - der `INSERT INTO idle_anticheat_flags`-Aufruf steckt jetzt in einem eigenen `BEGIN ... EXCEPTION WHEN OTHERS THEN NULL; END;`-Sub-Block, sodass ein Fehler in der reinen Alarm-TELEMETRIE nie wieder die eigentliche, bereits korrekt gekappte Spielstand-Speicherung blockieren kann (vorher lief der INSERT ungeschützt, eine Exception dort riss die komplette UPDATE-Transaktion mit sich - ein Bug in einem reinen Beobachtungssystem hat damit echtes Gameplay kaputt gemacht, ein strukturell inakzeptabler Zustand unabhängig von der genauen Ursache). (2) defensive, schadlose Maßnahme für die Arbeitshypothese: `triggered_by`-Spaltentyp wird per `information_schema.columns`-Check + sicherem `ALTER COLUMN ... TYPE text USING triggered_by::text` explizit erzwungen, falls er nicht bereits `text` ist. Gleiches Sicherheitsprinzip jetzt auch im JS-Mock (`tests/mock/rest-engine.js`, `try/catch` um den Flag-Insert). Neuer deterministischer Regressionstest (`anticheat-progress-rate-guard.spec.js`, simuliert einen garantierten Protokollierungsfehler durch Kaputtmachen der Mock-Tabelle - beweist: die eigentliche Speicherung inkl. korrekter Kappung läuft trotzdem durch, unabhängig von der Fehlerursache) - 21/21 grün, 210/210 stabil bei 10x Wiederholung, `eslint` sauber. Rein lesende Diagnose-Datei `sql/20260811-diagnose-triggered-by-column-type.sql` vorbereitet, um die Arbeitshypothese endgültig zu bestätigen oder zu widerlegen (Ergebnis noch ausstehend).

## Architektur-Entscheidungen (siehe Plan-Datei für vollständige Begründung)

- **Kein Bundler, keine ES-Module.** idledorf.js wird in mehrere klassische globale `<script>`-Dateien mit fester Ladereihenfolge zerlegt (`/js/systems/*.js`), nicht in ES-Module — 418 globale Funktionen + 156 geteilte veränderliche Variablen + Live-Geld-Fluss (Stripe) ohne Tests machen einen Bundler/ESM-Umbau zu riskant. Neue globale Funktionen folgen der bestehenden `bkmpXxx`-Namenskonvention.
- **CSS-Tokens:** `design-tokens.css` (neu, Phase 1 inert) wird ab Phase 3 schrittweise in bestehende Selektoren hinein referenziert — kein Parallel-System. Stimmung (Website vs. Spiel) läuft über `.zone-site`/`.zone-game`-Wrapper-Klassen, die dieselben `--mood-*`-Variablennamen umdefinieren.
- **Komponenten:** einfache JS-Factory-Funktionen (`bkmpUiXxx()` → HTML-String), keine Web Components — verlängert das bestehende Template-Literal+innerHTML-Muster.
- **Assets:** `scripts/optimize-images.ps1` (+`optimize-images.mjs`) für WebP-Konvertierung, manuell einmalig pro Content-Batch ausgeführt. Braucht lokal installiertes Node.js + `sharp` (nicht Teil des Deploys, siehe Kommentare in den Skripten). Erzeugt `name-web.webp`/`name-web.png` neben dem Original; `bkmpDragonThumbHtml()` in idledorf.js leitet daraus automatisch `<picture>`-Markup ab — **funktioniert ohne Datenbank-Änderung**, fällt auf das Original-PNG zurück, falls eine `-web`-Variante fehlt.

## Modul-Karte

```
/js/core/bkmp-site.js              - Tab-Nav, Theme/Akzent-Init, Site-Glue [Phase 1 ✅]
/js/core/bkmp-app-mode-bootstrap.js - App-Modus-Bootstrap [Phase 1 ✅]
/js/core/bkmp-idle-state.js        - ALLE geteilten veraenderlichen Variablen (bkmpIdleState,
                                      bkmpPlayerDragons, ...) + bkmpAutoclick*-Helfer. Laedt
                                      IMMER ZUERST, vor allen anderen /js/-Dateien. [Phase 2a ✅]
/js/systems/bkmp-dungeon.js        - bkmpDungeon* [Phase 2a ✅]
/js/systems/bkmp-breeding.js       - bkmpDragon* [Phase 2a ✅]
/js/systems/bkmp-guild.js          - bkmpGuild* [Phase 2a ✅]
/js/systems/bkmp-guild-tech.js     - Gilden-Technologie v3 (Baum-System mit Mitglieder-
                                      Beitraegen, 31.07.2026): bkmpGuildTechRecomputeBonusCache(),
                                      bkmpIdleRenderGildeTechPanel(), Beitrags-Modal, Ticker,
                                      Rangliste. Laedt direkt nach bkmp-guild.js (dessen
                                      BKMP_GUILD_TECH_CACHE_KEY/bkmpGuildTechBonus() bleiben dort,
                                      generischer Cache-Leser fuer 7+ andere Dateien).
/js/systems/bkmp-runes.js          - bkmpRune* [Phase 2a ✅]
/js/systems/bkmp-raid.js           - bkmpRaid* [Phase 2a ✅]
/js/systems/bkmp-tower.js          - bkmpTower* [Phase 2a ✅]
/js/systems/bkmp-prestige.js       - bkmpPrestige* [Phase 2a ✅]
/js/systems/bkmp-arena.js          - bkmpArena* [Phase 2a ✅]
/js/systems/bkmp-meister.js        - bkmpMeister* [Phase 2a ✅]
/js/core/bkmp-idle-bootstrap.js    - NUR der Aufruf `bkmpIdleInit();`. Laedt IMMER ZULETZT
                                      (nach idledorf.js, vor bkmp-site.js). [Phase 2a ✅]
/js/core/bkmp-combat-math.js       - reine Berechnungsfunktionen (Schaden, Stats, Rewards, XP-Kurve) [Phase 2b ✅]
/js/systems/bkmp-skilltree.js      - [Phase 2b ✅]
/js/systems/bkmp-cosmetics.js      - Titel/Kosmetik/Dorf-Skins/Stripe-Rueckkehr [Phase 2b ✅]
/js/systems/bkmp-events.js         - Event-Drachen/Streak [Phase 2b ✅]
/js/ui/bkmp-hud.js                 - Sprite-Rendering, HP-Balken, Projektile, Klick-Schaden [Phase 2b ✅]
idledorf.js                        - Kern-Orchestrator: Game-Loop (Tick/Init/Save-Sync), State-
                                      Laden, Wartungsmodus, Stufen-Leiste, Offline-Progress,
                                      Upgrades, Tab-Registry (bkmpIdleTabs), Achievement-Adapter
                                      (bkmpIdleGetAchievementContextFields), Bestenliste,
                                      Produktionsgebaeude. ~2.200 Zeilen. [Ziel Phase 2c fuer
                                      Achievements/Bestenliste, Rest bleibt bewusst hier]
/js/systems/bkmp-leaderboard.js    - bkmpUiMedal() + bkmpLeaderboardRenderSimpleRow(), von der
                                      Idle-Dorf-/Dungeon-/Raid-Bestenliste gemeinsam genutzt
                                      [Phase 2c ✅]. Die Marketing-Seiten-Bestenliste (bkmp-site.js,
                                      andere Metriken + reichere Zeile mit Podium-Glow/Profil-Klick)
                                      bleibt bewusst separat, nutzt aber auch bkmpUiMedal().
/js/systems/bkmp-achievements.js   - bkmpAchievementReadCache() (geteiltes Cache-Lese-Muster
                                      fuer die 4 GetAchievementContextFields-Adapter) [Phase 2c ✅].
                                      Die volle Achievement-Engine (Toast/Konfetti-Warteschlange,
                                      Kategorien-Rendering, noch in idledorf.js/bkmp-site.js) ist
                                      bewusst Phase 5-Arbeit, siehe Kommentar in der Datei.
/js/ui/bkmp-ui-components.js       - geteilte Factories [Phase 3 ✅]: bkmpUiRarityBadge() (verdrahtet
                                      in renderCosmeticsPanel, macht cosmetic.rarity erstmals sichtbar),
                                      bkmpUiLeaderboardRow() (verdrahtet in bkmpIdleRenderLeaderboardList),
                                      bkmpUiCard()/bkmpUiTooltipHtml()+bkmpUiWireTooltipTrigger()/
                                      bkmpUiModalHtml()+bkmpUiTrapFocus()/bkmpUiShowToast() sind fertig
                                      und einsatzbereit, aber bewusst noch NICHT an bestehende Stellen
                                      angeschlossen (das ist Phase 4-6-Arbeit, wenn echte Seiten neu
                                      gebaut werden).
```

**Design-Tokens (Phase 3):** style.css' alte `:root`/`html[data-theme="light"]`-Tokens (`--gold`, `--paper`, `--line`, ...) zeigen jetzt per `var()` auf design-tokens.css (`--color-accent`, `--color-bg`, `--color-line`, ...) statt eigene Hex-Werte zu tragen - dadurch erben alle ~700 bestehenden `var(--gold)`-artigen Stellen in style.css automatisch das neue Token-System, ohne dass jede einzeln angefasst werden musste. `--color-danger`/`--color-danger-ink` sind verdrahtet, aber noch an keinem echten Button angeschlossen (kein `.btn-danger`-Fill existiert bisher, das ist Phase 5/6) und `--color-accent-ink` (ersetzt 11 hartkodierte `#0A0A0F`-Stellen für Text auf goldenem Untergrund).

**Zonen-Stimmung (Phase 4):** `html` bekommt bereits im fruehen Kopf-Script `zone-site` (Standard) oder `zone-game` (App-Modus) - siehe index.html `<head>`, gleiche Stelle wie der bestehende `bkmp-app-mode`-Flag. `#idleDorfOverlay` traegt zusaetzlich fest `zone-game` im Markup, damit das Idle-Dorf-Fenster IMMER die epische Stimmung bekommt, auch auf der normalen Website. admin.html/datenschutz.html/impressum.html: `zone-site`. idle-stream-mini.html: `zone-game`. Die `--mood-*`-Tokens (siehe design-tokens.css) werden jetzt tatsaechlich konsumiert (`--mood-radius-panel` in Card-Radien, `--mood-glow-strength` in CTA-Schatten) - verifiziert per Browser, dass verschachtelte Elemente korrekt die jeweils naeher liegende Zonen-Klasse erben.

**Marketing-Redesign-Strategie (Phase 4):** JEDE der 10 Marketing-Sektionen (`<section class="panel">`) startet mit derselben `.panel-title`-Komponente - eine Verbesserung dort (Typo-Skala, Akzent-Unterstrich) wirkt automatisch auf alle 10. Ebenso teilen sich mehrere Sektionen dieselben Karten-Komponenten (`.partner-card` wird sowohl von PartnerShops als auch von Kartenfirmen genutzt, siehe mapart.js). Token-Politur (Radius/Schatten/Transition-Tokens, Hover-Zustaende) wurde auf dieser Handvoll geteilter Klassen angewendet: `.panel-title`, `.stat-card`, `.investor-card`, `.news-card`, `.wish-cta`/`.wish-card`, `.partner-card`/`.partner-filter`, `.cardsale-card`/`.cardsale-banner`, `.cardcatalog-card`/`-search`, `.about-block`, `.leaderboard-tab`, `.ledger-list`. **Noch NICHT gemacht** (bewusst zurueckgestellt, kein Blocker): die 3 fast-identischen Karten-Definitionen `.wish-card`/`.cardsale-card`/`.cardcatalog-card` koennten zu einer gemeinsamen Klasse zusammengefuehrt werden (Duplicate-CSS, siehe Audit) - das ist Phase 7-Aufraeumarbeit, keine Phase-4-Notwendigkeit.

**Feste Ladereihenfolge (siehe Kommentar in index.html vor den `<script>`-Tags):**
`vendor → supabase.js → app.js → bkmp-idle-state.js → [9 Subsystem-Dateien, beliebige Reihenfolge untereinander] → idledorf.js (Rest) → mapart.js → bkmp-idle-bootstrap.js → bkmp-site.js → bkmp-app-mode-bootstrap.js`.
Grund: `bkmp-idle-state.js` deklariert alle geteilten `let`-Variablen zuerst (TDZ-sicher), `bkmp-idle-bootstrap.js`s einzige Aufgabe (`bkmpIdleInit();`) muss nach ALLEN Subsystemen laufen. Admin.html/idle-stream-mini.html spiegeln dieselbe Reihenfolge (ohne bkmp-site.js/app-mode-bootstrap.js, die sind index.html-spezifisch).

Bis Phase 2b/2c abgeschlossen sind, bleibt der `bkmpIdle*`-Rest in `idledorf.js` (~6k Zeilen) — neue Funktionen dort weiter mit `bkmpXxx`-Präfix nach Sub-System einsortieren, damit die spätere Zerlegung mechanisch bleibt. Bei neuen geteilten (subsystem-übergreifenden) `let`-Variablen: in `js/core/bkmp-idle-state.js` deklarieren, nicht in einem Subsystem-File, sonst drohen TDZ-Fehler.

## Redesign zurückgestellt (29.08.2026) — Repo läuft wieder auf dem alten Design

Das komplette BK-Investment-Redesign (Phase 1 bis 3.1 — Kartenshop-Dashboard, Header/Nav-Umbau, Redesign aller 9 Marketing-Seiten, kompletter neuer Büchershop mit Sicherheitshärtung) war nie committed/gepusht und wurde auf ausdrücklichen Nutzerwunsch ("Ja ich möchte das alte Design komplett behalten") per `git stash push -u` zurückgestellt statt gelöscht — vollständig recoverbar (`git stash list`), falls später doch darauf aufgebaut werden soll. Der Arbeitsstand ist seitdem wieder identisch zum letzten echten Commit vor Beginn des Redesigns. **Alle Abschnitte oben, die "Phase 1/2/3/3.1" erwähnen, beschreiben diesen zurückgestellten Stand — nicht den aktuell aktiven Code.** Neue Arbeit ab hier baut auf dem alten, weiterhin live laufenden Design auf.

## OPSUCHT-Link + Umsatzseiten-Ausbau (29.08.2026)

Zwei unabhängige Aufträge direkt nach dem Redesign-Rückstellen, auf dem alten (jetzt wieder aktiven) Design. **Nichts committed/gepusht, keine SQL, keine Businesslogik verändert.**

**OPSUCHT-Logo verlinkt:** das bisher rein dekorative `<img class="opsucht-logo">` in `.top-actions-left` (der einzige OPSUCHT-Bezug im ganzen Code) ist jetzt in einen echten Link (`<a class="opsucht-logo-link" href="https://www.opsucht.net" target="_blank" rel="noopener noreferrer">`) gewickelt - `pointer-events:none` auf dem Bild selbst (unverändert) lässt Klicks ohnehin zum umschließenden Link durchfallen.

**Umsatzseiten-Ausbau** (`#panel-main`, öffentlich sichtbar, kein Login nötig) - Nutzerauftrag: bestehende Seite erweitern, NICHT neu bauen, keine Businesslogik/DB ändern, keine Fake-Daten. Vor der Umsetzung vollständig analysiert:
- `data.income`/`data.expenses`: `{name, category, amount, date, note, createdAt}` - **keine Stückzahl-Spalte**, `renderFinancialSections()` (`js/core/bkmp-site.js`) berechnet alles bereits aus den einmal geladenen Arrays, kein zusätzlicher Supabase-Aufruf nötig (passt exakt zur Performance-Vorgabe).
- **Bücher-Stückzahl wird nirgends gespeichert:** admin.html's `incomeUnitPrices['Bücher']=2500` ist nur ein Eintrags-Rechenhelfer (`Anzahl × Preis → amount`), der Wert wird NIE mitgespeichert - nur `amount` landet in der DB. Da der Preis nicht historisch versioniert ist, wird die Stückzahl rückwirkend nur GESCHÄTZT (`amount / 2500`) - **explizit als „geschätzt" gekennzeichnet** (Hinweistext direkt in der Verkaufsstatistik-Karte), kein Vortäuschen exakter Werte. Gleicher Wert `2500` bewusst als eigene, neue Konstante `BKMP_INCOME_UNIT_PRICES` in `app.js` dupliziert statt admin.html's funktionierenden Rechner anzufassen.
- Zwei Easter Eggs hängen direkt an dieser Seite, beide vor der Umsetzung identifiziert und nach der Umsetzung live nachgetestet: Donut-Klick-Regenbogenfeuerwerk (`chartArea`-delegierter Klick-Listener auf `.donut`, `egg_rainbow`) und der 5x-Schnellklick auf die rote „Ausgaben"-Kachel (`#statExpenses.closest('.stat-card')`, `egg_positivmodus`/„Realitätsverweigerer") - beide funktionieren nach dem Umbau unverändert (per simulierten Klick-Events bestätigt: Feuerwerk-Klasse bzw. Positivmodus-Overlay+Shake+Toast lösen korrekt aus).

**Neue reine Berechnungs-Helfer** (`app.js`, getrennt von der Darstellung, wie gefordert): `bkmpCalculatePeriodChange` (Division-durch-0-sicher, "Neu"/"Kein Vergleich"), `bkmpCalculateAverageDailyRevenue`, `bkmpCalculateBestDay`, `bkmpCalculateAverageTransaction`, `bkmpCalculateProfitMargin`, `bkmpCalculateExpenseRatio`, `bkmpCalculateBookStats`, `bkmpCalculateTopCategory`, `bkmpCalculateMonthlyProjection` + Datums-/Bereichs-Helfer (`bkmpToIsoDate`/`bkmpAddDaysIso`/`bkmpEntriesInRange`/`bkmpSumInRange`).

**Neue Darstellung** (`renderFinancialSections()` in `js/core/bkmp-site.js`, `index.html`, `style.css` - alles additiv, bestehende IDs/Klassen unverändert):
- Die 3 Hauptkarten (`#statIncome`/`#statExpenses`/`#statNet`, unverändert) bekommen je eine neue Zusatzzeile (`#statIncomeSub` etc.: Veränderung **letzte 7 Tage vs. die 7 Tage davor** statt "heute vs. gestern", plus Ausgabenquote/Gewinnmarge) und eine dezente 14-Tage-Sparkline (reines Inline-SVG, kein neues Chart-Element, `--pos`/`--neg`/`--gold` je nach Kachel).
- Neue kompakte KPI-Zeile (`#kpiRow`, 6 Karten: Heute, Bücher diese Woche, Ø Umsatz/Tag, Bester Tag, Ø Buchungswert, Buchungen).
- „Woher kommen die Einnahmen?" bekommt ein neues Top-Kategorie-Widget (🏆 + Anteil + Veränderung + größter Aufsteiger) direkt neben Donut+Legende - unverändert im selben `#chartArea`-Container wie zuvor.
- Der Umsatz-pro-Zeitraum-Chart bekommt eine neue Vergleichszeile direkt über dem Chart, die sich **automatisch an den aktiven Modus anpasst** (nutzt dieselben `chartPoints` wie der Chart selbst - "letzter vs. vorletzter Punkt" - dadurch bei Täglich="gestern", bei Wöchentlich="Vorwoche", bei Monatlich="Vormonat", keine fest verdrahtete Bezeichnung).
- Neue "📚 Verkaufsstatistik"-Sektion (`#bookStatsSection`, nur sichtbar wenn mindestens eine Bücher-Buchung existiert): 6 kompakte Kennzahlen + ein kleiner 30-Tage-Balkenverlauf (bewusst deutlich kleiner als der Hauptchart) + der oben erwähnte Schätzungs-Hinweistext.
- Neue optionale Monats-Prognose (`#monthlyProjectionArea`, ausdrücklich als "(Prognose)" markiert, Formel: bisheriger Monatsumsatz/vergangene Tage×Tage im Monat).
- „Alle Einträge" unverändert.

**Verifiziert** (Browser, echte bereits live/öffentlich lesbare Produktionsdaten - die Umsatzseite braucht keinen Login, daher real getestet statt gemockt): alle neuen Werte gegen echte Daten geprüft (u.a. Ausgabenquote 8,0%/Gewinnmarge 92,0%/Anteil Bücher am Umsatz 34,7% - decken sich exakt mit den vom Nutzer im Auftrag genannten Beispielwerten, die offenbar von der echten Seite abgelesen waren) - starkes Indiz für korrekte Formeln. Beide Easter Eggs funktionieren nachweislich unverändert. Alle Edge Cases (Auftrag Abschnitt 20) gezielt durchgespielt: komplett leere `data.income`/`data.expenses` (keine NaN/Infinity irgendwo im gerenderten HTML, saubere "–"/"Kein Vergleich"-Anzeigen), nur Ausgaben ohne Einnahmen (korrektes "Neu" statt Division durch 0, korrekter negativer Nettogewinn), Wechsel zwischen Täglich/Wöchentlich/Monatlich (Vergleichszeile passt sich korrekt an). Responsive 375px/1920px geprüft (keine horizontale Scrollbar, KPI-Zeile 2-spaltig auf Mobile, 3 Hauptkarten/Chart-Block korrekt nebeneinander auf Desktop, Buch-Statistik-Grid faltet sich sauber). Konsole fehlerfrei (nur die bekannten, erwarteten 404s des lokalen Static-Servers ohne echte Bild-Assets/API-Endpunkte). `node --check` auf `app.js`/`js/core/bkmp-site.js` sauber, CSS-Klammerbalance in `style.css` sauber (3341=3341).

Cache-Busting `?v=20260829-financedashboard1` für `app.js` (index.html + admin.html), `js/core/bkmp-site.js` (index.html) und `style.css` (alle 5 HTML-Dateien).

**Nicht umgesetzt/offen:** keine Datenbankänderung (wie gefordert) - die Bücher-Stückzahl bleibt eine Schätzung, keine echte gespeicherte Größe. Keine kompakte "2,5 Mio. €"-Zahlendarstellung eingebaut (bewusst, um auf derselben Seite kein zweites Zahlenformat neben dem bestehenden vollen `bkmpFormatCurrency()` zu erzeugen, siehe Auftrag Abschnitt 16).

## SW-Besucher-Statistik für /sw bk (29.08.2026) — SQL noch nicht ausgeführt

Eigenständiger Auftrag, erweitert die am selben Tag gebaute Umsatzseite um einen neuen Bereich für die tägliche `/sw bk`-Besucherstatistik (6 CityBuilds). **Ausschließlich der eigene Shop** - keine Konkurrenzdaten, keine fremden SWARPs, das Datenmodell kennt andere Shops strukturell gar nicht. **Nichts committed/gepusht, SQL nicht ausgeführt.**

### Datenmodell (`sql/20260829-sw-daily-stats.sql`, neu, **noch nicht ausgeführt**)

Eine denormalisierte Tabelle `sw_daily_stats` (1 Zeile pro Tag, `stat_date date not null unique` verhindert Duplikate strukturell): `cb1_visitors`..`cb6_visitors` (integer, `>=0`) + `cb1_rank`..`cb6_rank` (**text**, nicht integer - CHECK-Bedingung erlaubt ausschließlich `'1'..'5'`/`'not_in_top5'`/`NULL`). Diese Struktur erzwingt strukturell, was der Auftrag explizit verlangte: Rang `#6`+ kann nie gespeichert werden, und **drei** getrennte Zustände bleiben unterscheidbar - `NULL` ("kein Rang eingetragen", Rang ist laut Auftrag optional), `'not_in_top5'` ("geprüft, nicht in Top 5") und `'1'..'5'` (echter Rang). Durchschnitts-/Top5-Quoten-Berechnungen (`bkmpSwCbRankHistory()`, app.js) zählen ausschließlich Zeilen mit tatsächlich eingetragenem Rang - `NULL` wird nie als `'not_in_top5'` fehlinterpretiert.

**RLS:** öffentlich lesbar (wie `incomes`/`expenses`, die neue Sektion braucht keinen Login). Schreiben: volle Admins (`is_active_admin()`, kompletter CRUD) + die bestehende Mitarbeiter-Rolle `expenses_editor` (`is_expenses_editor()`, nur Insert+Update, bewusst kein Löschen - identische Zurückhaltung wie beim bestehenden Ausgaben-Zugang dieser Rolle). Kein neuer Index nötig (`unique(stat_date)` legt automatisch einen an - Lektion aus dem Büchershop-Sicherheitsaudit vom 28.08.).

### Duplikat-Vermeidung (Auftrag Abschnitt 6)

`saveSwDailyStats()` (supabase.js) nutzt `.upsert(payload, {onConflict:'stat_date'})` - ein zweiter Speichervorgang für dasselbe Datum aktualisiert immer die bestehende Zeile, erzeugt nie eine zweite. Die Admin-Oberfläche lädt beim Wechsel des Datumsfelds (`bkmpSwOnDateChange()`) automatisch einen bereits vorhandenen Eintrag in die Formularfelder, sonst werden alle Felder auf 0/leer zurückgesetzt - **live getestet**: Wechsel zu einem Datum mit vorhandenen Daten füllt korrekt vor, Wechsel zu einem leeren Datum setzt korrekt zurück, ein "Bearbeiten"-Knopf in der Liste der letzten 14 Einträge tut dasselbe per Klick.

### Zeitzone (Auftrag Abschnitt 24)

`stat_date` ist eine reine `date`-Spalte (kein Zeitanteil, kein UTC-Konvertierungsrisiko). Das Datumsfeld im Admin-Formular und alle Berechnungen (`bkmpToIsoDate()`/`bkmpSwTodayIso()`) berechnen das lokale Kalenderdatum direkt aus `Date`-Objektfeldern (`getFullYear()`/`getMonth()`/`getDate()`), **niemals** über `new Date().toISOString()` (das würde je nach Uhrzeit auf den falschen Tag verschieben) - identisches, bereits bei der Umsatzseite etabliertes Muster.

### Öffentliche Darstellung (`js/core/bkmp-site.js`, neue Sektion in `renderFinancialSections()`, `#swStatsSection` in index.html)

- **Hero:** heutige Gesamtsumme (alle 6 CBs) + Veränderung vs. gestern. Unterscheidet explizit "gestern hatte 0 Besucher" (echter Wert) von "für gestern existiert gar keine Zeile" (`Kein Vergleich (gestern nicht erfasst)`) - eine generische 0-Vergleichs-Formel hätte diese beiden Fälle sonst unbemerkt vermischt.
- **KPI-Zeile** (5 Karten, wiederverwendet `.kpi-row`/`.kpi-card` von der Umsatzseite): Diese Woche (+Vorwochenvergleich), Ø Besucher/Tag (**nur tatsächlich vorhandene Tage im Nenner**, keine Lücken als 0 gezählt), Rekordtag, Stärkster CB (+Anteil an der Tagessumme), Top-5-Platzierungen heute.
- **30-Tage-Chart mit CB-Filter** (Gesamt/CB1-CB6, `.line-mode-tabs`-Optik wiederverwendet): baut die Datenpunkte direkt aus den vorhandenen `sw_daily_stats`-Zeilen auf (nicht aus 30 durchiterierten Kalendertagen) - ein fehlender Tag erscheint dadurch einfach nicht als Punkt, wird nie als 0 fehlinterpretiert (identisches Prinzip wie der bestehende `buildRevenueSeries('daily')` beim Umsatzchart).
- **Heutige Verteilung:** kompakte horizontale Balkenliste, absteigend sortiert.
- **Ranglistenstatus:** 6 Zeilen CB→Rang-Badge (3 visuell unterscheidbare Zustände: in Top 5/grün, nicht in Top 5/neutral, nicht eingetragen/gestrichelt) + "Beste Platzierung"-Hinweis je CB (nur wenn jemals ein numerischer Rang erfasst wurde) + Zusammenfassung "N von 6 CityBuilds in Top 5".
- **Besucher & Umsatz** (klein, Auftrag Abschnitt 20-21): "Umsatz pro SW-Besuch" (Tagesumsatz/Tagesbesucher) - bewusst **nie** als "Conversion Rate" bezeichnet, mit explizitem Disclaimer-Text ("kein Nachweis, dass SW-Besucher tatsächlich gekauft haben"). Wochenvergleich SW-Besucher vs. Umsatz nebeneinander, ohne Kausalitäts-Behauptung.
- **Diesen Monat** (klein, Auftrag Abschnitt 22): Gesamt/Ø-Tag/bester Tag/stärkster CB/Vormonatsvergleich.

### Admin-Oberfläche (`admin.html`, Seite "SW-Besucher", `data-role="expenses_editor"` - **auch für den Mitarbeiter-Login erreichbar**, identisches Muster wie die bestehenden Seiten "Ausgaben"/"Mitarbeiter")

Datumsfeld (Default: heute) + 6 CB-Karten (Besucher-Zahl + Rang-Dropdown `#1`-`#5`/`Nicht in Top 5`/`– kein Rang –`) + live mitlaufende Gesamtsumme (aktualisiert bei jedem Tastenanschlag) + Speichern-Knopf + Liste der letzten 14 Einträge mit Bearbeiten-Knopf.

### Verifiziert

Da `sw_daily_stats` live noch nicht existiert, scheitert `loadSwDailyStats()` erwartungsgemäß mit `PGRST205` - **korrekt abgefangen** (kein Absturz, Sektion bleibt auf der öffentlichen Seite einfach leer/unsichtbar, Admin-Formular zeigt eine klare Fehlermeldung statt zu brechen). Für die eigentliche Logik-Verifikation wurden 35 Tage realistischer Mock-Daten (inkl. einer bewussten Lücke vor 5 Tagen, gemischten Rang-Zuständen inkl. `not_in_top5`/`null`/echtem Rang) direkt in `data.swStats` injiziert und `renderFinancialSections()` aufgerufen: alle KPIs, der CB-Chart (Tab-Wechsel getestet), Verteilung, Ranglistenstatus (alle 3 Zustände korrekt unterschieden), Zusatz-Kennzahlen - alle korrekt, keine NaN/Infinity. Edge Cases einzeln durchgespielt: komplett leere `data.swStats` (Sektion verschwindet sauber), nur ein einziger Tag ohne Vortag (korrekte "Kein Vergleich (gestern nicht erfasst)"-Meldung statt falscher 0-Vergleich). Admin-Formular live getestet (Formular sichtbar geschaltet, echte Funktionen direkt aufgerufen, kein echter Login möglich - Sicherheitsregel): live mitlaufende Summe, Laden/Zurücksetzen beim Datumswechsel, Bearbeiten-Knopf aus der Liste - alle korrekt. `--gold-fixed` (nicht `--gold`) durchgehend für alle informationstragenden Texte verwendet und mit auf Schwarz gestelltem Akzentregler gegengeprüft (Lektion aus dem Umsatzseiten-Fix vom selben Tag, diesmal von Anfang an richtig gemacht). Responsive 375px/1920px ohne horizontalen Overflow, KPI-Zeile/Sekundär-Raster/Zusatz-Raster brechen korrekt um. Konsole fehlerfrei (die 2 vorgefundenen Fehler sind die bereits dokumentierte, unabhängige `bkmp-guild-tech.js`-Lücke in admin.html). `node --check` auf `app.js`/`supabase.js`/`js/core/bkmp-site.js` sauber, admin.html-Inline-Skripte extrahiert+geprüft sauber, CSS-Klammerbalance sauber (3393=3393).

Cache-Busting `?v=20260829-swstats1` für `app.js`/`supabase.js` (index.html + admin.html), `js/core/bkmp-site.js` (index.html) und `style.css` (alle 5 HTML-Dateien). `supabase.js?v=20260829-swstats1` zusätzlich in `idle-stream-mini.html` (bindet die Datei ebenfalls ein, auch wenn dort keine neue Funktion genutzt wird).

**Nicht umgesetzt/offen:** keine Datenbankänderung an bestehenden Tabellen (wie gefordert), keine Businesslogik verändert. Realtime-Publikation für `sw_daily_stats` vorbereitet (gleiches Muster wie andere Live-Tabellen) - macht die Sektion live-aktuell, sobald ein Admin/Mitarbeiter einen Tag einträgt, ohne dass Besucher neu laden müssen.

## Feste Sicherheitsregel für JEDE Phase

`supabase.js` (Auth, Stripe-Checkout `bkmpCreateStripeCheckoutSession`, Admin-Funktionen) und alle `/api/*.js`-Dateien bleiben während des gesamten Redesigns **logisch eingefroren** — Redesign-Commits ändern dort nie eine Funktion, nur ggf. das aufrufende Markup/CSS in einem separaten Commit. Ein Commit, der eine `.sql`-Datei oder eine `supabase.js`-Funktion ändert, wird nie mit einem CSS-/Markup-Commit für dasselbe Subsystem gebündelt.

## Umsatzseite: Nachbesserungen nach Nutzer-Screenshots (29.08.2026)

Direkte Folge-Fixes zum Umsatzseiten-Ausbau + der SW-Besucher-Statistik oben, alle **lokal, nichts committed/gepusht, keine SQL ausgeführt**.

**1. Feste Farben statt Akzentfarb-Picker (Nutzer-Screenshots: "Bereiche sind Schwarz.. weil sie mit dem Farbregler gekoppelt sind"):** `.mp-projected`/`.mp-label em` (Prognose Monatsende) und der Netto-Gewinn-Sparkline-Strich nutzten `var(--gold)` - eine vom Nutzer per Akzentfarb-Picker frei überschreibbare CSS-Variable (`bkmpApplyAccentForCurrentTheme()`, app.js), bei ihm auf Schwarz gestellt und dadurch unsichtbar. Fix: beide auf den bereits etablierten, picker-unabhängigen `--gold-fixed`-Token (Bestenlisten-/Streamer-Badge-Präzedenzfall) umgestellt. Live mit `--gold` künstlich auf `#000000` bestätigt: Text bleibt lesbar. Dieselbe Lehre direkt vorbeugend für das komplette neue SW-Besucher-CSS angewendet (dort von Anfang an nur `--gold-fixed` verwendet, kein `--gold`).

**2. Seiten-Kürzung beim Positivmodus-Easter-Egg (Nutzer-Screenshot: "Wenn das Easter Egg ausgelöst wird.. wird viel von der Seite auf einmal abgeschnitten"):** Root Cause per Live-Reproduktion bewiesen (gescrollt, Egg ausgelöst, `#panelsViewport`s gesetzte `style.height` gegen die tatsächliche `scrollHeight` von `#panel-main` gemessen - deutlicher Unterschied, ~3000px Inhalt lautlos abgeschnitten). `bkmpSyncPanelHeight()` (setzt `#panelsViewport`s Höhe explizit auf die `scrollHeight` des sichtbaren Panels, bewusst kein `ResizeObserver` wegen Rückkopplungsrisiko, siehe Kommentar an der Funktion) lief bisher NUR bei echtem Tab-Wechsel (`goTo()`) und Fenster-`resize` erneut. Wächst `#panel-main`s tatsächliche Höhe stattdessen durch ein `renderFinancialSections()`-Update OHNE Tab-Wechsel (z.B. wenn `#swStatsSection`/`#bookStatsSection` von leer auf gefüllt wechseln - beim eigenen Testen durch reine Dateninjektion reproduziert, real z.B. bei einem Realtime-Update denkbar), blieb die alte, zu niedrige Höhe stehen und `overflow:hidden` schnitt den Rest unsichtbar ab. Das Positivmodus-Easter-Egg selbst (sperrt zusätzlich das Scrollen, `body{position:fixed}`) hat den bereits vorher bestehenden Fehler nur sichtbarer/störender gemacht, war nicht die eigentliche Ursache. Fix: `renderFinancialSections()` ruft `bkmpSyncPanelHeight()` jetzt selbst (direkt + einmal verzögert nach 400ms, identisches Muster wie `goTo()`) am Ende jedes Durchlaufs auf. Live exakt nachgestellt (Panel-Höhe live über Dateninjektion+Re-Render wachsen lassen, DANACH das Easter Egg ausgelöst): Viewport-Höhe stimmt in beiden Fällen exakt mit der echten Inhaltshöhe überein (6438px = 6438px), kein abgeschnittener Inhalt mehr.

**3. Diagramm-Achsen übersichtlicher (Nutzer-Screenshot, rot markiertes Max/"0€"-Eckenpaar: "übersichtlicher machen.. z.b das Niedrigste letzte Punkt.. und der Durchschnitt"):** das alte starre Eckenpaar "Max-Betrag oben links" / "0 €ganz unten links" durch einen neuen, zusammenhängenden `.line-axis-info`-Block (3 Zeilen: Max/Ø/Min, grün/gold-fest/rot) ersetzt - zeigt den tatsächlichen niedrigsten sichtbaren Wert (bei Millionenbeträgen war "0 €" ohnehin selten eine sinnvolle Referenz) statt einer starren Nulllinie, plus den Durchschnitt aller sichtbaren Punkte. Auf beide bestehenden Charts konsistent angewendet (`renderRevenueLineChart()` für den Umsatz-Chart, `renderSwChart()` für den SW-Besucher-Chart). Alte `.line-axis`/`.line-axis.max`/`.line-axis.zero`-CSS-Regeln bewusst nicht entfernt (kein weiterer Konsument mehr, aber risikofrei stehen gelassen statt blind zu löschen).

**4. "Gestern" statt "Heute" (Nutzer, nach Screenshot der "Umsatz pro SW-Besuch (heute)"-Karte: "auch bei Sw Besuch umsatz auch 'gestern' und nicht heute. Weil wenn ich es eintrage ist 0 uhr vom vortag"):** der SW-Tageseintrag entsteht laut Nutzer real erst kurz NACH Tagesende (Admin trägt kurz nach Mitternacht die Daten des GERADE ABGESCHLOSSENEN Tages ein) - "heute" ist beim Betrachten der Seite tagsüber dadurch strukturell fast immer leer/0, "gestern" ist der zuverlässig befüllte, tatsächlich aussagekräftige Wert. Fix zunächst gezielt für "Umsatz pro SW-Besuch" (jetzt `yesterdayIncomeTotal / yesterdayTotal`, Label "(gestern)"). Beim Umsetzen fiel auf, dass exakt dieselbe Ursache den GESAMTEN "aktueller Status"-Teil des SW-Besucher-Bereichs betrifft (Hero-Zahl "X Besucher heute", KPI-Karten "Stärkster CB"/"Top-5-Platzierungen", "Heutige Verteilung", Ranglistenstatus-Badges - alle lasen bisher stur `todayRow`, der im normalen Tagesablauf praktisch nie existiert) - konsequent auf denselben Fallback-Mechanismus umgestellt: `displayRow = todayRow || yesterdayRow` (echter `todayRow` wird bevorzugt, falls er doch einmal existiert - z.B. bei einem künftig geänderten Eintrage-Workflow - sonst automatischer Rückgriff auf `yesterdayRow`), mit dynamisch mitlaufender `displayDateLabel`("heute"/"gestern")/`displayPrevLabel`("gestern"/"vorgestern")-Beschriftung, damit die Anzeige nie "heute" behauptet, während eigentlich "gestern" gezeigt wird. Die Einnahmen-KPI-Karte "Heute" (Finanz-Hauptbereich) wurde aus demselben Grund zu "Gestern" umbenannt (zeigt jetzt Gestern-Umsatz vs. Vorgestern-Umsatz statt Heute vs. Gestern). Live mit realistischen Testdaten durchgerechnet (todayRow absichtlich fehlend, yesterday/vorgestern befüllt, inkl. echter Einnahmen-Einträge für beide Tage): Hero "83 Besucher gestern", "↗ +36,1% vs. vorgestern", "Verteilung (gestern)", "2 von 6 CityBuilds gestern in den Top 5", "Umsatz pro SW-Besuch (gestern) 76.835 €", Einnahmen-KPI-Karte "Gestern" mit korrektem Vorgestern-Vergleich - alle Werte rechnerisch bestätigt konsistent.

**Verifiziert (gemeinsam für alle 4 Punkte):** `node --check js/core/bkmp-site.js` sauber, CSS-Klammerbalance `style.css` 3400=3400 (vorher 3393, +7 durch den neuen `.line-axis-info`-Block, exakt passend). Cache-Busting `style.css`/`js/core/bkmp-site.js` auf `?v=20260829-swstats2` angehoben (alle 5 bzw. 1 HTML-Datei). Live im Browser: alle 4 Fixes per echter Dateninjektion + echtem Funktionsaufruf bestätigt (keine Testkopie), inkl. Farbwerte per `getComputedStyle` (Max grün `rgb(74,222,128)`, Ø `rgb(201,165,106)` = `--gold-fixed`, Min rot `rgb(248,113,113)`, bleibt bei künstlich auf Schwarz gesetztem `--gold` unverändert lesbar), inkl. echtem 5-Klick-Easter-Egg-Auslöser. Keine neuen Konsolenfehler (nur die bereits dokumentierten, erwarteten `/api/*`-404s des lokalen Static-Servers ohne Vercel-Funktionen).

## Zwei echte CSS-Kommentar-Bugs: "Stern-Schrägstrich" schließt Kommentare vorzeitig (29.08.2026)

Nutzer meldete per drei Screenshots: (1) "Verteilung (gestern)" im SW-Besucher-Bereich zeigt keine Balken, (2) "Besucher & Umsatz" solle weg, (3) "bei Mitarbeiter Umsatz fehlen auch einzelne Balken". **Nichts committed/gepusht durch diese Session bewusst ausgelöst.**

**Root Cause für (3), per Live-Reproduktion + Binärsuche in der Browser-Konsole zweifelsfrei bewiesen (nicht vermutet):** der Dokumentationskommentar über `.payroll-section { --payroll-cyan: #38BDF8; }` (18.08.2026, Payroll-Dashboard) listete die verwendeten Design-Tokens ab und schrieb dabei `--paper-*/--line` als Kurzform für "die --paper-Varianten, dann --line" - die Zeichenfolge **Stern direkt gefolgt von Schrägstrich** ist in CSS die Kommentar-Schließsequenz, unabhängig vom Kontext/der Absicht des Autors. Der Kommentar schloss dadurch faktisch mitten im Satz, der komplette Rest des Absatzes (inklusive der eigentlich gewollten schließenden Sequenz ganz am Ende) wurde als ungültiger CSS-Selektor interpretiert und hat die unmittelbar folgende, eigentlich völlig korrekte Regel aus dem geparsten Stylesheet verschluckt - `--payroll-cyan` existierte dadurch clientseitig schlicht nie (bewiesen per `getComputedStyle(el).getPropertyValue('--payroll-cyan')` → leerer String, UND per rekursivem CSSOM-Scan über alle 2963 Regeln der Datei → die Regel fehlte komplett, obwohl der frisch vom Server geholte Rohtext sie nachweislich enthielt). Minimal-Reproduktion (nur der betroffene Kommentar + Zielregel in einem isolierten `<style>`-Tag) bestätigte: 0 geparste Regeln.

**Zweiter, unabhängiger Fund beim anschließenden systematischen Audit des kompletten Stylesheets (3400 Regeln) auf dasselbe Muster:** derselbe Fehler existierte bereits seit dem 17.07.2026 in einem völlig anderen Kommentar - `.idle-dungeon-type-grid` (das 7-Karten-Raster im Dungeon-Tab des Idle-Dorfs) wurde nie als `display:grid` gerendert, weil der darüberliegende Kommentar `.idle-dungeon-diff-*/` enthielt (identisches Muster, "diff" + Stern + Schrägstrich als Kurzform für "diff-easy/diff-medium/..."). Sechs Wochen lang unbemerkt live - das Kartenraster fiel einfach auf normalen Block-Fluss zurück, kein harter Fehler, keine Konsolen-Meldung, nur eine optisch nicht ganz korrekte (aber nicht offensichtlich "kaputte") Anordnung.

**Auditmethodik (Ultracode-Sorgfaltspflicht - nicht nur die zwei gemeldeten/gefundenen Fälle als erledigt betrachtet):** (1) gezielte Regex-Suche nach dem exakten Auslösemuster (`<Wort>-\*/<Wort>`) über die ganze Datei - genau 2 Treffer, beide bestätigt echte Bugs. (2) Strukturprüfung "wie viele `{` im Rohtext vs. wie viele `{` liegen laut demselben Kommentar-Schließverhalten wie der echte Browser außerhalb eines Kommentars" - 13 Differenz gefunden, aber bei manueller Prüfung ALLER 13 stellte sich heraus: das sind ausschließlich absichtliche, korrekt funktionierende Code-Beispiele INNERHALB von einwandfrei schließenden Kommentaren (z.B. `"html,body{height:100%}"` als Zitat in einer Erklärung) - keine weiteren echten Bugs. Beide Prüfungen zusammen geben hohe Zuversicht, dass es keinen dritten, noch unentdeckten Fall dieses Musters gibt.

**Fix:** beide Kommentare umformuliert (keine Stern-Schrägstrich-Kurzschreibweise mehr, ausgeschrieben statt abgekürzt), je ein Warnhinweis direkt an der Stelle ergänzt, der explizit erklärt, warum die Kurzschreibweise gefährlich ist - **bewusst OHNE die gefährliche Zeichenfolge selbst im Warnhinweis zu verwenden** (ein erster eigener Entwurf des Warnhinweises hat genau diesen Fehler beim Erklären selbst nochmal gemacht - "-*/"/"*-/" als Beispiel in Anführungszeichen geschrieben - vor dem Speichern bemerkt und durch eine rein ausgeschriebene Beschreibung ersetzt). Verifiziert: `.payroll-section` erscheint jetzt zweimal im CSSOM (beide Regeln), `--payroll-cyan` löst live auf `#38BDF8` auf, `.idle-dungeon-type-grid` liefert `display:grid`, Werkzeug/Tränke-Kategoriebalken zeigen jetzt korrekt `rgb(56,189,248)` statt transparent.

**Lehre für künftige Kommentare in diesem Projekt:** niemals Stern direkt gefolgt von Schrägstrich als Kurzschreibweise/Trenner in einem CSS-Kommentar verwenden (weder für "--token-Familie/anderer-Token" noch für Klassennamen-Familien wie "`.foo-*`") - immer mit Leerzeichen trennen oder ausschreiben, auch wenn es umständlicher wirkt.

**Nachtrag, direkt danach (Nutzer widersprach zurecht: "Hier sind weiterhin Balken die nicht gefüllt werden.. 53,2% aber der Balken ist leer" + neuer Screenshot mit exakt denselben Werten):** meine erste Prüfung war falsch-negativ - ich hatte nur `getComputedStyle()` (Hintergrundfarbe + `style.width`-String) geprüft, beide lieferten korrekte Werte, ABER das ist nicht dasselbe wie "wird tatsächlich sichtbar gerendert". Ein echter Screenshot der Live-Seite (mit einem dabei entdeckten, störenden "Anmelden"-Modal, das die Sicht zunächst blockierte - per direktem `classList.remove('visible')` weggeräumt, keine echte Login-Interaktion) zeigte die Balken zweifelsfrei leer. **Echter Root Cause** (erst per `getBoundingClientRect()` gefunden, DAS ist der entscheidende Unterschied zu `getComputedStyle()`): `.sw-distribution-fill` ist ein `<span>` (Standard `display:inline`) - es liegt zwar innerhalb von `.sw-distribution-track`, aber NICHT direkt in einem `display:flex`-Container (nur `.sw-distribution-track` selbst ist ein direktes Flex-Kind von `.sw-distribution-row` und wird dadurch automatisch "blockifiziert" - sein eigenes Kind, `.sw-distribution-fill`, aber nicht). Auf einem echten `display:inline`-Element werden `width`/`height` von JEDEM Browser komplett ignoriert, unabhängig vom gesetzten Wert - `getBoundingClientRect().width` lag bei allen 6 Balken bei exakt `0`, obwohl `style.width`/`getComputedStyle().width` weiterhin korrekt "53.2%" bzw. den passenden Pixelwert zurückgaben (der Browser LÖST die Angabe intern zwar auf, WENDET sie aber nie an - ein echter, leicht zu übersehender Unterschied zwischen "Wert vorhanden" und "Wert wirksam"). Fix: `display:block` ergänzt. **Codebase-weiter Audit auf dasselbe Muster** (jedes `<span style="width:...">` im ganzen Projekt gesucht): 2 weitere Treffer gefunden (`js/systems/bkmp-guild.js`s Gilden-Banner-Icon, `js/ui/bkmp-ui-components.js`s Gilden-Technologie-Fortschrittsbalken) - beide bereits korrekt implementiert (`display:inline-flex` bzw. bereits explizites `display:block`), kein dritter Fall gefunden. Verifiziert per echtem Screenshot (lokal UND nach Fix erneut geprüft): alle 6 Balken zeigen jetzt korrekt sichtbare, proportionale Füllung.

**Lehre für künftige Verifikation in diesem Projekt:** `getComputedStyle()` allein beweist NICHT, dass ein Element sichtbar/korrekt gerendert wird - `background-color` und der `width`-Wert selbst können beide "korrekt" aussehen, während das Element trotzdem 0px tatsächliche Breite hat (z.B. bei `display:inline`). Bei Balken-/Fortschritts-/Fill-Elementen immer zusätzlich `getBoundingClientRect()` prüfen (oder einen echten Screenshot machen) - nicht nur `getComputedStyle()`.

Die "Besucher & Umsatz"-Karte wurde wie gewünscht ersatzlos entfernt (Umsatz-pro-SW-Besuch, SW-Besucher/Umsatz-7-Tage-Vergleiche) - die verbleibende "Diesen Monat"-Karte läuft jetzt einspaltig.

Cache-Busting `style.css?v=20260829-swstats4`, `js/core/bkmp-site.js?v=20260829-swstats3` (alle einbindenden HTML-Dateien).

## Site-weiter Akzentfarb-Kontrast-Audit (30.08.2026)

Nutzer-Screenshot (Investor-Karten "DerJannikHase"/"Sebi"/"Auswechseler" - die "Zeitraum"-Pille war erneut unlesbar) + explizite Bitte: "Kannst du mal gucken auf der ganzen website.. wir haben scheinbar echt viele von diesen Farbregler wörter die nicht lesbar sind." **Nichts committed/gepusht.**

**Hintergrund:** der Akzentfarb-Picker (`bkmpApplyAccentForCurrentTheme()`, app.js) lässt Nutzer `--gold` frei auf JEDE Farbe setzen (auch reines Schwarz) - diese Überschreibung gilt global. Mehrfach in dieser Sitzung bereits einzeln gefundene Fälle (Umsatzseiten-Prognose, SW-Besucher-Anzeigen, Investor-"Zeitraum"-Pille) zeigten: Text mit `color: var(--gold)` wird bei dunkler Akzentwahl unlesbar - es gibt den etablierten, picker-unabhängigen Ersatz `--gold-fixed`.

**Umsetzung (Ultracode-Workflow, da 164 Fundstellen über 14.360 Zeilen zu groß für manuelle Einzelprüfung):** `grep`/Node-Tokenizer fand alle 164 `color: var(--gold...)`-Vorkommen (nach Korrektur eines False-Positive-Bugs im eigenen Suchregex, der `--gold-fixed` fälschlich mitgezählt hatte). 6 parallele Klassifizierungs-Agenten (Workflow-Tool, `gold-text-contrast-audit`) haben JEDE Fundstelle mit echtem Datei-Kontext (Read-Tool, ±15-20 Zeilen) einzeln bewertet: `TEXT_RISK`/`HOVER_TRANSIENT` (echter Lesetext - Name, Wert, Datum, Label, Status, Zahl, Link) → `gold_fixed`; `DECORATIVE_SAFE` (Icon, Scrollbar, `accent-color` auf nativen Formularelementen, Rahmenfarbe mit separat sicherem Text) → unverändert lassen; `SURFACE_FILL` (Fläche UND Text beide betroffen, komplexerer Fall) → gesonderte manuelle Prüfung. Agenten durften zusätzlich gefundene, nicht in ihrer Zeilenliste stehende Nachbarfälle im gelesenen Kontext mit aufnehmen (mehrere echte Zusatzfunde dadurch erfasst, z.B. `.btn-primary`/`.investor-payout-request-btn`/`.leaderboard-tab.active`).

**Ergebnis: 93 bestätigte `TEXT_RISK`/`HOVER_TRANSIENT`-Fälle** (u.a. `#mcNameBadge`-Spielername, Investor-Zeitraum-Pille+Zeilenwerte, News-Datum, Rechtstext-Links, HUD-Skillpunkte-Zähler, Prestige-/Skilltree-/Runen-Zahlenwerte, Bestenlisten-/Nav-Tab-aktiv-Text, Kartenmarkt-Preise/Codes/Links, Feedback-Board-Zähler, Raid-MVP-Name u.v.m.) - per eigenem Node-Skript chirurgisch gefixt: nur die tatsächliche `color:`-Deklaration je Zeile ersetzt (negative Lookbehind schließt `border-color:`/`background-color:` aus, damit auf derselben Zeile stehende, bewusst weiter Akzent-abhängige Rahmen-/Hintergrundfarben unangetastet bleiben). 92/93 Zeilen automatisch getroffen, 1 Sonderfall (`--rf-color: var(--gold)`, eine Zwischen-Variable statt einer direkten `color:`-Deklaration) manuell nachgezogen.

**Zweite, schwerwiegendere Bug-Klasse gefunden (SURFACE_FILL):** mehrere breit genutzte Buttons/Badges (`.btn-primary`, `.btn-ja`, `.investor-payout-request-btn`, `.leaderboard-tab.active`, `.admin-nav-btn.active`, `.payroll-badge-employee`, `.idle-stagepicker-stage.is-current` - 16+ Fundstellen derselben Variable) setzen `background`/`border` auf `var(--gold)`, zeichnen ihren Text aber in `--accent-ink` - fest verdrahtet auf `#0A0A0F` (design-tokens.css), in der stillschweigenden Annahme, `--gold` sei immer hell/warm. Bei schwarzer Akzentwahl wird die Fläche schwarz UND der Text bleibt dunkel - praktisch unlesbar, unabhängig davon, dass keine dieser Zeilen selbst `color: var(--gold)` nutzt (deshalb vom reinen Text-Ersetzungs-Skript nicht erfasst).

**Root-Fix statt 16-facher Einzelfall-Patch:** `--accent-ink` wird in `bkmpApplyAccentForCurrentTheme()` jetzt per einfacher YIQ-Helligkeitsformel (`(R*299+G*587+B*114)/1000`, Standardmaß für "soll der Text hell oder dunkel sein") aus der GEWÄHLTEN Akzentfarbe live berechnet: hell/warm gewählt (der bisher einzig vorgesehene Fall) → unverändert `#0A0A0F` wie immer, dunkel/schwarz gewählt → automatisch `#F5F3ED` (helles Ink). Ein einziger, zentraler Fix behebt dadurch ALLE 16+ betroffenen Komponenten gleichzeitig, ohne jede einzeln anfassen zu müssen, und bleibt bei normaler Akzentwahl pixelgleich zum bisherigen Verhalten. Ein zusätzlicher, nicht an eine Variable gebundener hartkodierter Fall (`.idle-stagepicker-stage.is-current`, `color: #1a1408`) wurde auf `var(--accent-ink, #1a1408)` umgestellt, damit auch er vom selben Fix profitiert.

**Verifiziert:** `node --check app.js` sauber, CSS-Klammerbalance `style.css` weiterhin ausgeglichen (3398=3398). Live im Browser (Akzentfarbe künstlich auf `#000000` gesetzt, echte `bkmpApplyAccentForCurrentTheme()`-Funktion aufgerufen): `--accent-ink` löst korrekt auf `#F5F3ED` auf, `.btn-ja`/`.idle-stagepicker-stage.is-current` zeigen jetzt hellen statt dunklen Text auf ihrer (jetzt schwarzen) Fläche, `.stat-value.neutral`/`.investor-period` bleiben unverändert auf dem festen Gold-Ton statt Schwarz zu werden. **Echter Vorher/Nachher-Screenshot der GENAU gemeldeten Investor-Karte** ("DerJannikHase") mit Schwarz-Akzent bestätigt: Zeitraum-Pille wieder lesbar. Regressionscheck mit entferntem/normalem Akzentfarb-Override: `--accent-ink` bleibt korrekt bei `#0A0A0F`, `.btn-primary`-Text bleibt bei `rgb(10,10,15)` - keine sichtbare Änderung gegenüber dem bisherigen Verhalten für den Normalfall.

Cache-Busting `app.js?v=20260830-goldcontrastaudit1` (index.html/admin.html), `style.css?v=20260830-goldcontrastaudit1` (alle 5 HTML-Dateien).

**Bewusst nicht angefasst (per Agenten-Einstufung als sicher, `SURFACE_FILL`/`leave` oder `DECORATIVE_SAFE`):** `.idle-clan-leaderboard-mine`/`.bkmp-guild-tech-node.state-maxed` (nutzen `--ink`, nicht vom Picker betroffen), `.idle-runen-fuse-check` (ebenfalls `--ink`-basiert, im dominanten Dark-Mode sicher), reine Icon-/Scrollbar-/Checkbox-/Hover-Rahmen-Fälle (kein Lesetext betroffen), 3 Sonderfälle mit ohnehin überwiegend `--ink`-dominiertem `color-mix()` (z.B. `.news-text em`, nur 20% Gold-Beimischung).

**Nachtrag, selbe Nacht (Nutzer-Screenshot: "Auszahlung beantragen"-Prank-Knopf trotz des obigen Fixes erneut fast unlesbar) - der eigentliche, tiefere Bug war eine Etage tiefer:** `index.html` hat einen EIGENSTÄNDIGEN, frühen Inline-`<script>`-Block ("Verhindert Aufblitzen", laeuft VOR `app.js`, da dieses `defer`t erst spaeter laedt) mit einer KOMPLETTEN, unabhaengigen Kopie der `--gold`/`--paper*`-Wiederherstellungslogik (eigener Zweck: Farb-Blitz beim ersten Rendern vermeiden). Diese fruehe Kopie kannte die neue `--accent-ink`-Berechnung nicht. Entscheidender Zusatzfund per echtem Fresh-Load-Test (localStorage vorab gesetzt, dann `navigate()` OHNE selbst `bkmpApplyAccentForCurrentTheme()` aufzurufen): **`bkmpApplyAccentForCurrentTheme()` (app.js) wird ausserhalb eines expliziten Theme-Wechsels NIE automatisch erneut aufgerufen** - `bkmpInitAccentColor()` synchronisiert beim Laden nur den PICKER-Anzeigewert (`syncPickerValue()`), ruft die eigentliche Anwende-Funktion aber nicht auf. Der allererste (fuer die meisten Besuche einzige) Seitenaufruf haengt dadurch **komplett** an der fruehen, unvollstaendigen Kopie - mein vorheriger Fix in app.js griff nur, wenn der Nutzer den Regler aktiv bewegt oder das Theme umschaltet, nie bei einem normalen Seitenaufruf. Fix: identische YIQ-Helligkeitsberechnung (kleine, eigenstaendige IIFE, keine Abhaengigkeit zu app.js moeglich, da es zu diesem Zeitpunkt noch nicht geladen ist) auch im fruehen Inline-Skript ergaenzt. Verifiziert per echtem Fresh-Load ohne jeden manuellen Funktionsaufruf: `--accent-ink` loest jetzt bereits beim allerersten Rendern korrekt auf `#F5F3ED` auf, der Prank-Knopf zeigt hellen Text von Anfang an. `admin.html`/`idle-stream-mini.html` haben dieses fruehe Inline-Skript gar nicht (dort faerbt sich `--gold` beim Laden bisher generell nie um, unabhaengiges, vorbestehendes Verhalten, nicht Teil dieses Fixes).

## Investoren-Seite: visuelles Redesign (30.08.2026)

Detaillierter 15-Abschnitte-Auftrag ("visuell deutlich aufwerten", explizit nur Layout/Struktur/Darstellung, Businesslogik/Supabase-Struktur/bestehende Werte unangetastet). **Nichts committed/gepusht.**

**Analyse vor der Umsetzung:** bestehende Renderfunktion (`js/core/bkmp-site.js`, Investoren-Block) nutzt `isInInvestorPeriod`/`sumForInvestorPeriod`/`formatInvestorPeriod` + die payout-Formel (`periodNet * sharePercent / 100`) - alle drei UNVERÄNDERT übernommen, nur einmal pro Investor zentral über eine neue `bkmpInvestorDerive()`-Hilfsfunktion aufgerufen statt mehrfach inline dupliziert. `#investorGrid` traegt die bestehende Klick-Delegation für den Auszahlungsbeweis-Viewer UND den Rickroll-Auszahlungs-Prank (`investorGridEl.addEventListener('click', ...)`, zweimal) - bewusst als GLEICHER Wrapper-Knoten beibehalten (nur sein innerer Aufbau wurde reicher), damit beide Delegationen ohne jede JS-Änderung weiter funktionieren.

**Neue Container** (`index.html`, `#panel-investors`): `#investorKpiRow` (wiederverwendet die bereits bestehende `.kpi-row`/`.kpi-card`-Komponente aus dem Finanz-Dashboard), `#investorHighlight`, `#investorTimeline` - alle vom bestehenden `#investorGrid` getrennt, keine Kollision mit der Klick-Delegation.

**Berechnungen (alle rein clientseitig, alle aus bereits geladenen `data.investors`/`data.income`/`data.expenses` abgeleitet, keine neue Supabase-Abfrage):**
- KPIs: Aktive Investoren (Anzahl `!paidOut`), Investiertes Kapital (Summe `invested` über ALLE), Aktueller Gesamtanteil (Summe `payout` nur AKTIVE), Investorengewinn gesamt (Summe `payout` über ALLE inkl. abgeschlossen).
- Highlight: Investor mit dem höchsten `payout` unter den aktiven - bewusst nur EIN Wert, kein Ranking.
- Laufzeit-Fortschritt: `(heute - start) / (ende - start)`, bei `start`/`ende` fehlend kein Balken, bei überschrittenem Ende gedeckelt bei 100% (`Math.min`).
- ROI (Auftrag Abschnitt 5, optional): `periodNet / invested * 100` - dieselben zwei bereits angezeigten Werte, keine neue Kennzahl.
- Timeline (Auftrag Abschnitt 11, optional): Investoren nach Start-Monat gruppiert (`startDate.slice(0,7)`) + abgeschlossene nach End-Monat, wenn `paidOut` - rein aus den vorhandenen Datensätzen generiert, keine hartkodierten Namen/Daten. **Zufälliger, aber starker Validierungsfund beim Testen mit echten Produktionsdaten:** die live generierte Ausgabe ("Mai 2026: CrazyMcNugget und Phil starten..." usw.) deckt sich fast wortgleich mit dem vom Nutzer im Auftrag selbst gegebenen Beispieltext - bestätigt, dass die Ableitung korrekt den tatsächlichen Datenstand widerspiegelt.

**Verifiziert:** `node --check`/CSS-Klammerbalance sauber. Live mit injizierten Testdaten UND (durch einen zwischenzeitlichen Realtime-Resync im selben Testlauf) echten Produktionsdaten geprüft - KPI-Werte/Highlight/Fortschrittsbalken/ROI/Wasserzeichen/Timeline rechnerisch korrekt. Rickroll-Prank-Knopf per echtem Klick bestätigt: öffnet weiterhin korrekt den Bestätigungsdialog mit Name+Betrag, unverändertes Verhalten. Responsive 375/768/1400px geprüft: KPI-Zeile 2×2 auf Mobile (bereits bestehende `.kpi-row`-Breakpoints), Historie-Karten 2-spaltig auf Tablet/3-spaltig Desktop, aktive Karten 1-spaltig auf Mobile/Tablet (gleiches Auto-Fit-Verhalten wie das alte `.investor-grid` bereits hatte, keine Regression), kein horizontaler Überlauf. Konsole fehlerfrei (nur bekannte lokale `/api/*`-404s).

Cache-Busting `style.css?v=20260830-investorredesign1` (alle 5 HTML-Dateien), `js/core/bkmp-site.js?v=20260830-investorredesign1` (index.html).

**Nicht verändert (wie gefordert):** `supabase.js`, keine SQL-Migration, keine bestehenden Investor-Datensätze/-Berechnungen, `.investor-concluded-summary`-CSS (alte lange Fließtext-Box) bewusst nicht gelöscht obwohl nicht mehr erzeugt (kein Risiko, keine bestehende Funktionalität entfernt).

## PartnerShops-Seite: visuelles Redesign + Shop im Spotlight (30.08.2026)

Sehr detaillierter 14-Abschnitte-Auftrag ("Hier kann ich Shops entdecken" statt "lange Kartensammlung"). Feste Vorgaben: bestehende Shop-Daten/Links/Kategorien/Bilder/Spielernamen/`/sw`-Angaben unverändert weiterverwenden, keine Fake-Shops/erfundenen Daten, bestehende Datenquelle/Businesslogik nicht umbauen, keine bestehenden Funktionen entfernen, nur diese Seite bearbeiten, bestehendes globales Nav/Header-System weiterverwenden, das bereits vorhandene BK-Investment-Designsystem nutzen (kein zweites Designsystem). **Nichts committed/gepusht.**

**Analyse vor der Umsetzung:** `partner_shops` (Supabase) → `bkmpMapPartnerShopFromSupabase()`/`-ToSupabase()` (`supabase.js`, unangetastet) → JS-Form `{id, name, image, location, category, description, link, contact, status, isRead, createdAt}`. `location` ist echtes Freitext-Feld (Formular-Platzhalter selbst lautet `"/sw Beispiel CB2"`) - **bewusst KEINE eigene "CB"-Kennzahl/kein "KATEGORIE · CB"-Kombifeld erfunden**, das Risiko einer falsch geparsten CityBuild-Nummer aus Freitext war dem Auftrag ("keine Zahlen hardcoden, nur wenn sauber ableitbar") wichtiger als ein optisches Detail. `renderPartnerShops()` (`js/core/bkmp-site.js`) filtert bereits auf `status==='approved'`; `activePartnerCategory` (bereits bestehende `let`-Variable) treibt den Kategorie-Filter.

**Hero + Spotlight** (`index.html`, `#panel-partners`): neue zweispaltige `.partner-hero` (60/65 % Haupt-Spalte links: Eyebrow "PARTNERSHOPS", Titel, Untertitel, Statistik, "Shop eintragen"-Button / 35/40 % `#partnerSpotlight` rechts) - der Spotlight sitzt FEST in der rechten Hero-Spalte (`position` bewusst nicht `sticky`/`fixed`, reine Grid-Platzierung), fällt auf Mobile per Media-Query auf eine gestapelte Spalte zurück (DOM-Reihenfolge Haupt-Inhalt→Spotlight passt dadurch automatisch zur geforderten Mobile-Reihenfolge, ohne `order:`-Tricks).

**Spotlight-Rotation** (`js/core/bkmp-site.js`, komplett neuer, isolierter Funktionsblock, bewusst VOR `renderPartnerShops()` platziert - siehe Fehlerabschnitt unten): 60s-Anzeigedauer je Shop, Fortschrittsleiste (`#partnerSpotlightProgressFill`, Breite in %) + Countdown-Text (`#partnerSpotlightCountdown`), beide werden alle 200ms per `Date.now()`-Differenz aktualisiert (nicht per naivem Zähler, bleibt dadurch robust gegen `setInterval`-Jitter/Tab-Drosselung) - **nur diese 2 kleinen DOM-Knoten werden pro Tick angefasst, nie die Shop-Liste/das Grid** (Auftrag Abschnitt 11, Performance). Warteschlange nach Fisher-Yates-Muster gemischt, bei Erschöpfung neu gemischt (kein Shop wiederholt sich innerhalb einer Rotation, bis alle einmal gezeigt wurden) + ein Tausch verhindert denselben Shop zweimal HINTEREINANDER am Zyklus-Übergang. Beim allerersten Laden zufälliger Shop (unabhängig von der Warteschlange). Pause bei `mouseenter` / Fortsetzen (nicht Neustart) bei `mouseleave` auf `#partnerSpotlight` - reiner Boolean-Flag, der Tick-Intervall läuft unabhängig weiter und aktualisiert `bkmpPartnerSpotlightLastTick` immer, wodurch ein Fortsetzen automatisch nahtlos dort weitermacht, wo `remainingMs` eingefroren wurde. Voller Shop-Wechsel (nicht nur Progress-Update) läuft über `.is-fading`-CSS-Klasse + 260ms-`setTimeout`, danach Neuaufbau des Inhalts - sanfter Fade statt hartem Umschalten. `bkmpUpdatePartnerSpotlightShopList()` wird bei JEDEM `renderPartnerShops()`-Aufruf (auch von Filter/Suche/Empty-State) erneut aufgerufen, aktualisiert aber nur die Kandidatenliste/Warteschlange, ohne eine laufende Rotation zurückzusetzen; ein leerer Shop-Bestand blendet den Spotlight komplett aus + räumt den Timer sauber ab.

**Toolbar** (Kategorie-Filter + Suche): bestehende Kategorie-Pillen auf Amethyst/Violet (`--purple`) umgestellt statt Gold (Auftrag: eigener Seiten-Akzent) - aktiv = gefüllter Verlauf + Glow, Hover (nicht aktiv) = nur Rahmen-Highlight, klar unterscheidbar. Neues `#partnerSearch`-Suchfeld (`bkmpPartnerShopMatchesSearch()`, prüft Name/Ort/Kategorie/Kontakt/Beschreibung, reine Client-Filterung, keine neue Supabase-Funktion nötig da die Daten längst im Frontend liegen) reagiert sofort auf jeden Tastenanschlag, filtert gemeinsam mit der Kategorie. Auf Mobile (`≤760px`) scrollen die Kategorie-Chips horizontal statt in mehrere Zeilen umzubrechen, die Suche bekommt ihre eigene volle Zeile.

**Shop-Karten** (`.partner-card`): größeres Vorschaubild, `KATEGORIE`-Chip jetzt violet, 3-zeiliger Beschreibungs-Clamp (`-webkit-line-clamp`), Hover = Karte hebt 3px + violet getönter Rahmen + Schatten/Glow + Bild zoomt 1.035x (weder Wert bewegt sich außerhalb der geforderten 1.02-1.04-Spanne), `prefers-reduced-motion` deaktiviert alle drei Effekte. Grid responsiv über Breakpoints (`auto-fill,minmax(220px,1fr)` als Basis, `≥1700px`→6 Spalten, `1320-1699px`→5, `≤900px`→2, `≤560px`→1) statt fixer 6-pro-Zeile-Erzwingung.

**Empty-State + Bottom-CTA:** `#partnerEmptyState` ("Keine Shops gefunden" / Hinweistext / "Filter zurücksetzen"-Button, setzt Kategorie UND Suche zurück) erscheint nie als leere Fläche. Die alte gelb/pinke Banner-Funktion (Shop-Einreichung) bleibt vollständig erhalten, aber umgezogen in eine neue, elegantere dunkle `.partner-bottom-cta` ("Dein Shop fehlt noch?") am Seitenende - beide "Shop eintragen"-Buttons (Hero + Bottom) rufen dieselbe, aus dem vorher inline sitzenden Klick-Handler extrahierte `openPartnerShopModal()` auf und öffnen denselben, komplett unveränderten `#partnerShopOverlay`-Dialog.

**Echter, selbst gefundener CSS-Bug (Wiederholung eines erst am Vortag dokumentierten Musters):** mein eigener neuer Dokumentationskommentar über `.partner-hero` enthielt versehentlich `--paper*/--line` - genau die "Stern-direkt-gefolgt-von-Schrägstrich"-Sequenz, die am 29.08.2026 an ZWEI anderen Stellen in derselben Datei gefunden+gefixt und in CLAUDE.md ausdrücklich als Falle dokumentiert wurde ("niemals Stern direkt gefolgt von Schrägstrich... verwenden"). Der Kommentar schloss dadurch vorzeitig, der Rest des Absatzes (inkl. der eigentlich gewollten Endsequenz) wurde als ungültiger Selektor interpretiert und hat die komplette `.partner-hero`-Regel (das 2-Spalten-Grid) lautlos aus dem geparsten Stylesheet verschluckt - alle ~25 anderen neuen Selektoren im selben Edit waren unbetroffen (per CSSOM-Vergleich einzeln bestätigt). Selbst gefunden (nicht vom Nutzer gemeldet) durch denselben `document.styleSheets`-Diagnoseweg wie beim Vortagesfund, noch bevor irgendein Screenshot/Test gezeigt wurde. Fix: Kommentar umformuliert (keine Kurzschreibweise mehr, zusätzlicher Warnhinweis direkt an der Stelle - diesmal OHNE die gefährliche Sequenz selbst im Warnhinweis zu verwenden, ein erster Entwurf des Warnhinweises hatte genau diesen Fehler nochmal gemacht). Codebase-weiter Nachaudit (`grep` auf `*/` gefolgt von Nicht-Leerzeichen, und auf `-*/`) bestätigt: keine weiteren Fälle in der gesamten Datei.

**Verifiziert (Browser, echte Produktionsdaten - 25 genehmigte PartnerShops, öffentlich lesbar, kein Login nötig):**
- CSSOM-Vergleich vor/nach dem Kommentar-Fix: `.partner-hero`-Regel jetzt vorhanden, `display:grid` + `grid-template-columns` ergibt real gemessen 766px/451px (≈63 %/37 %, exakt im geforderten 60-65/35-40-Bereich) bei 1280px Fensterbreite.
- Kategorie-Filter: Klick auf eine Kategorie filtert korrekt (25→1 bei "XP Farm"), Klick auf "Alle" stellt korrekt alle 25 wieder her.
- Suche: "xp" filtert korrekt auf 1 Treffer, ein nicht existierender Suchbegriff zeigt den Empty-State mit korrektem Text, "Filter zurücksetzen" leert Suchfeld UND Kategorie gleichzeitig und stellt alle 25 Karten wieder her.
- Spotlight-Rotation (deterministischer Test mit 5 fixen Test-Shops, 12 aufeinanderfolgende `bkmpPartnerSpotlightAdvance(true)`-Aufrufe): jede Gruppe von 5 aufeinanderfolgenden Anzeigen ist eine vollständige Permutation aller 5 IDs (kein Wiederholen innerhalb einer Rotation), kein unmittelbarer Wiederholungsfall auch am Zyklus-Übergang.
- Pause/Fortsetzen über ECHTE `mouseenter`/`mouseleave`-Events auf dem realen Spotlight-Element: laufend → -1400ms in 1500ms Wartezeit; pausiert → 0ms Änderung über 1200ms; nach `mouseleave` → -804ms in 800ms (setzt korrekt fort, kein Reset auf 60000ms).
- Fade-Übergang: `.is-fading` erscheint sofort nach einem nicht-sofortigen Advance, verschwindet nach ~350ms mit neuem Inhalt (Shop-Name hat sich geändert) - kein hartes Umschalten.
- Leerer Shop-Bestand: Spotlight blendet sich korrekt aus (`hidden=true`), Timer wird sauber abgeräumt (`bkmpPartnerSpotlightTimerId===null`); nach Wiederherstellung startet der Spotlight korrekt neu.
- Beide "Shop eintragen"-Buttons (Hero + Bottom-CTA) öffnen nachweislich denselben `#partnerShopOverlay` (`joke-overlay cardcatalog-overlay visible`).
- Responsive: 1800px→6 Grid-Spalten, 1400px→5, 800px→2 Spalten + Hero fällt korrekt auf 1 Spalte (Haupt-Inhalt vor Spotlight in der DOM-Reihenfolge), 400px→1 Spalte + Kategorie-Chips scrollen horizontal (`overflow-x:auto`) statt umzubrechen + Suche nimmt fast die volle Breite ein + kein horizontaler Seiten-Overflow bei keiner getesteten Breite.
- Konsole fehlerfrei (nur bekannte lokale `/api/*`-404s ohne echte Vercel-Funktionen, kein `Uncaught`/`SyntaxError`/`ReferenceError`/`TypeError`). `node --check js/core/bkmp-site.js` sauber, CSS-Klammerbalance `style.css` ausgeglichen (3512=3512).

Cache-Busting `style.css?v=20260830-partnershops2` (alle 5 HTML-Dateien), `js/core/bkmp-site.js?v=20260830-partnershops1` (index.html).

**Nicht verändert (wie gefordert):** `supabase.js`, `bkmpMapPartnerShopFromSupabase()`/`-ToSupabase()`, die Genehmigungs-Logik (`status==='approved'`-Filter), das Admin-Panel für PartnerShop-Freigaben, keine SQL-Migration, keine bestehenden Shop-Datensätze/-Links/-Bilder, `goTo()`/Tab-Klick-Bindung/globales Nav-System unangetastet.

## Nachbesserung: Spotlight global in den /sw-bk-Banner verschoben (30.08.2026, direkt danach)

Direkter Folgeauftrag zum Abschnitt oben: das große Spotlight sollte nicht auf der PartnerShops-Seite bleiben, sondern als kompakte, auf JEDEM Tab sichtbare Werbefläche in den bereits vorhandenen globalen `/sw-bk`-Banner integriert werden. **Nichts committed/gepusht.**

**Analyse vor der Umsetzung:** `<header class="hero hero-banner-wrap">` (`index.html:1170`) lebt **genau einmal** im gesamten Dokument, **außerhalb** von `.panels-viewport`/`.panels-track` (dem horizontalen 10-Panel-Karussell, das per `scrollLeft` zwischen Tabs wechselt) - der Banner ist dadurch strukturell bereits ein globales, tab-unabhängiges Element, das bei jedem Tab-Wechsel unangetastet im DOM verbleibt. `.hero-banner-picture { position:relative }` deckt sich exakt mit der `<img>`-Box (kein eigenes Padding) - bereits genutzt für zwei bestehende, absolut positionierte Overlay-Kinder (`.bkmp-sheep-easter-egg`/`.bkmp-sheep-bubble`), also ein etabliertes, sicheres Muster für ein weiteres Overlay-Element. Banner-Seitenverhältnis fest `1983/527` (≈3.76:1) - bei mobilen Breiten (~380px) dadurch nur ~95-105px hoch, musste bei der Kompakt-Gestaltung des Spotlights berücksichtigt werden (siehe responsive Stufen unten).

**Warum kein zusätzlicher Singleton-Schutz nötig war:** da `#globalPartnerSpotlight` (wie der ganze Bannerkopf) nur EIN EINZIGES Mal im Dokument existiert und nie neu aufgebaut/dupliziert wird, kann ein Tab-Wechsel strukturell nie einen zweiten Timer erzeugen - die bereits bestehende `bkmpUpdatePartnerSpotlightShopList()`-Guard-Logik (`if (!bkmpPartnerSpotlightTimerId) { ...erster Aufbau... } else if (!bkmpPartnerSpotlightCurrent) { ...} `) reichte dafür unverändert aus.

**Umsetzung:**
- `index.html`: `.partner-hero` (60/40-Zweispalten-Grid) durch `.partner-hero-full` (volle Breite, Eyebrow/Titel/Untertitel/Statistik+CTA-Zeile) ersetzt - `#partnerSpotlight` komplett entfernt. Neues `<div class="global-partner-spotlight" id="globalPartnerSpotlight" hidden></div>` als Kind von `.hero-banner-picture` ergänzt (nach dem Schaf-Bubble, vor `</picture>`).
- `js/core/bkmp-site.js`: `const partnerSpotlightEl = document.getElementById('partnerSpotlight')` → `document.getElementById('globalPartnerSpotlight')` - die EINZIGE strukturelle Änderung an der Rotationslogik selbst (alle Funktionen - Warteschlange, Fade, Pause/Resume, Tick - komplett unverändert, da sie generisch gegen `partnerSpotlightEl` arbeiten). `renderPartnerShops()` ruft `bkmpUpdatePartnerSpotlightShopList(allShops)` weiterhin bei jedem Daten-Laden/Realtime-Sync auf (läuft unabhängig davon, ob die PartnerShops-Seite gerade sichtbar ist, da `renderPartnerShops()` selbst nicht ans aktive Panel gebunden ist) - hält den globalen Spotlight dadurch auf jedem Tab aktuell, ohne einen neuen Daten-Sync-Pfad zu brauchen.
- **Kompaktes Anzeigen-Layout** (`bkmpPartnerSpotlightPaint()` leicht umstrukturiert - Badge oben, Bild+Name/Ort/Kategorie in einer Zeile, Link+Countdown in einer Fußzeile, dünner Fortschrittsbalken; Beschreibung/Kontakt bewusst nicht mehr gerendert, keine erfundenen Daten, beide Felder bleiben in `shop` selbst unverändert erhalten). Design: `background: color-mix(...#14101d 80%...)` (dunkles halbtransparentes Plum/Black), `backdrop-filter:blur(10px) saturate(1.15)`, dünne `1px`-Amethyst-Border, kleiner Violet-Akzent auf Badge/Kategorie-Chip/Link, sehr dezenter Schatten (`0 10px 26px rgba(0,0,0,.38)`) - bewusst kein starker Glow.
- **3 responsive Größenstufen** (`style.css`): Basis (Desktop, `width:min(266px,42%)`, volles Layout mit 42px-Bild), `@media(max-width:900px)` (Tablet, `min(220px,44%)`, 34px-Bild, kleinere Schrift), `@media(max-width:640px)` (Mobile, `min(150px,48%)`, Bild/Ort/Kategorie komplett ausgeblendet, nur Badge+Name+Link+Countdown+dünner Balken bleiben - verhindert, dass die Werbefläche den ohnehin nur ~95-105px hohen Mobil-Banner dominiert, "Minecraft-Grafik soll weiterhin sichtbar bleiben").
- `@media (max-width:980px) { .partner-hero{...} .partner-spotlight{...} }` (bezog sich auf die jetzt entfernten Klassen) ersatzlos gestrichen.

**Verifiziert (Browser, echte Produktionsdaten, 25 genehmigte Shops):**
- Spotlight lebt exakt EINMAL im DOM (`querySelectorAll('#globalPartnerSpotlight').length===1`), alte seitenlokale `#partnerSpotlight` nachweislich verschwunden.
- **Singleton-Beweis (Kernanforderung des Auftrags):** vor/nach einem Wechsel durch 6 verschiedene Tabs (Hauptseite→Investoren→Was-gibt's-Neues→PartnerShops→Kartenfirmen→Kartenideen) blieben Timer-ID, aktuell gezeigter Shop UND die verstrichene Zeit (`bkmpPartnerSpotlightRemainingMs`, korrekt um die tatsächlich verstrichene Echtzeit weiter heruntergezählt, nicht zurückgesetzt) exakt identisch - kein zweiter Timer, kein Reset, kein Nachbau.
- Filter-/Suche-Interaktion auf der PartnerShops-Seite verändert weder den aktuell gezeigten Spotlight-Shop noch die verbleibende Zeit noch den Timer - die globale Rotation bleibt unabhängig von der Seiten-lokalen Filterung, Kandidatenpool bleibt bei allen 25 Shops (nicht auf die gefilterte Kategorie verengt).
- PartnerShops-Seite: neue Full-Width-Hero misst real 223px Höhe (Ziel 250-350px, bewusst am unteren Rand bzw. leicht darunter - passt besser zum expliziten Nutzerwunsch "nicht wieder 500px leeren schwarzen Raum" als exakt 250px zu treffen), Filter/Suche/Grid folgen unmittelbar danach (~24px Abstand Toolbar→Grid), Kategorie-Filter und Suche funktionieren unverändert (25→1→25 Karten bei Kategorie-Klick, korrekt bei Suchbegriff).
- 3 responsive Stufen real gemessen: 1400px→266×131px (Vollansicht inkl. Bild), 800px→220×124px (Tablet-Stufe, Bild+Ort weiterhin sichtbar), 380px→150×62px (Mobil-Minimalpille, Bild/Ort/Kategorie ausgeblendet) - in allen drei Fällen bleibt der überwiegende Teil der Bannerbreite (mind. ~55-80%) unverdeckt sichtbar.
- Ein Shop ohne `link`-Feld rendert korrekt ohne "Shop ansehen"-Link (kein erfundener Platzhalter-Link), Countdown bleibt trotzdem rechtsbündig ausgerichtet (leerer `<span>` als Flex-Platzhalter). Ein Shop mit echtem Link (`OPLeihCenter`, zufällig derselbe Name wie im Nutzer-Mockup) rendert Link+`href` korrekt.
- Konsole fehlerfrei (nur bekannte lokale `/api/*`-404s, kein `Uncaught`/`SyntaxError`/`ReferenceError`/`TypeError`). `node --check js/core/bkmp-site.js` sauber, CSS-Klammerbalance ausgeglichen (3533=3533), kein Stern-Schrägstrich-Kommentarbug (präventiv erneut geprüft nach dem Vorfall vom selben Tag).

Cache-Busting `style.css?v=20260830-partnershops3` (alle 5 HTML-Dateien), `js/core/bkmp-site.js?v=20260830-partnershops2` (index.html).

**Nicht verändert (wie gefordert):** die Rotations-ENGINE selbst (Fisher-Yates-Warteschlange, No-Repeat-Logik, 200ms-Tick, Pause/Resume, Fade-Timing) - nur das Render-Ziel und die kompakte Innendarstellung wurden angepasst. PartnerShop-Daten/Links/Filterlogik/Kategorien unverändert. Shop-Karten-Gestaltung im Grid unverändert.

## Zweite Nachbesserung: Spotlight größer, klickbar zur PartnerShops-Seite, Countdown-Text weg (30.08.2026, direkt danach)

Nutzer-Screenshot des live laufenden globalen Spotlights + drei konkrete Wünsche: deutlich größer, Klick soll zur PartnerShops-Seite springen und den Shop dort kurz hervorheben ("kurz aufleuchtet als Orientierung wo es ist"), der Sekunden-Countdown-Text soll weg (nur der Fortschrittsbalken bleibt). **Nichts committed/gepusht.**

**Größe:** alle 3 responsiven Stufen proportional vergrößert - Desktop `min(266px,42%)`→`min(340px,46%)` (Bild 42px→56px, Name-Schrift 0.84rem→1.02rem, Badge/Kategorie/Link-Schrift ebenfalls angehoben, Innenabstände großzügiger), Tablet `min(220px,44%)`→`min(272px,47%)`, Mobile-Minimalpille `min(150px,48%)`→`min(190px,50%)`. Live gemessen: 1400px→340×131px, 800px→272×158px, 390px→184×79px (vorher 266×131/220×124/150×62) - spürbar größer, ohne die Breakpoint-Logik (Bild/Ort/Kategorie verschwinden weiterhin unter 640px) anzutasten.

**Kein Countdown-Text mehr:** `bkmpPartnerSpotlightUpdateProgressUi()` aktualisiert nur noch die Balkenbreite, das `#partnerSpotlightCountdown`-Element wurde aus dem Paint-Template UND aus der Update-Funktion entfernt (nicht nur per CSS versteckt - echter Wegfall, kein totes DOM). Die Fußzeile enthält jetzt nur noch den optionalen "Shop ansehen →"-Link (kein `justify-content:space-between`-Platzhalter-`<span>` mehr nötig, da nichts mehr rechts danebenstehen muss); ist kein Link vorhanden, entfällt die Fußzeile komplett (Progress-Bar bleibt trotzdem immer sichtbar, sitzt als eigenständiges Element darunter).

**Klick-Navigation** (`bkmpPartnerSpotlightGoToShop(shop)`, neu, `js/core/bkmp-site.js`): ein Klick-Listener auf `partnerSpotlightEl` (im selben "nur beim allerersten Aufbau"-Block wie die bestehenden `mouseenter`/`mouseleave`-Listener ergänzt - kein doppeltes Anhängen bei Re-Paints) prüft zuerst `e.target.closest('.partner-spotlight-link')` - trifft der Klick den externen "Shop ansehen"-Link selbst, wird sofort `return`et (der Link öffnet unverändert normal in einem neuen Tab, keine Konkurrenz-Navigation). Bei jedem anderen Klick auf die Karte: der echte PartnerShops-Tab-Button wird über `document.querySelector('.tab-btn[aria-controls="panel-partners"]')` gefunden (nie `goTo()` direkt aufgerufen, robust gegen eine künftige Umsortierung der Tabs - `aria-controls` wird bereits ganz am Anfang des Scripts beim Tab-Setup gesetzt) und geklickt, Kategorie-Filter wird auf "Alle" zurückgesetzt und die Suche geleert (sonst wäre der Shop in der aktuell gefilterten Ansicht evtl. gar nicht auffindbar), `renderPartnerShops()` läuft danach synchron neu. Per `requestAnimationFrame` wird die passende Shopkarte über ein neu ergänztes `data-shop-id`-Attribut (`.partner-card`, vorher nicht vorhanden) gefunden, per `scrollIntoView({behavior:'smooth', block:'center'})` in den sichtbaren Bereich gescrollt und bekommt für ~1,6s die neue `.partner-card-highlight`-Klasse (violetter Glow-Puls per `@keyframes partnerCardHighlightFlash`, automatisch von der bereits bestehenden globalen `prefers-reduced-motion`-Kollektivregel erfasst, keine eigene Ausnahme nötig).

**Verifiziert (Browser, echte Produktionsdaten):**
- Klick auf die Spotlight-Karte (Badge-Bereich) wechselt korrekt zu `panel-partners`, findet die richtige Karte per `data-shop-id`, setzt `.partner-card-highlight`, scrollt sie sichtbar in den Viewport, setzt Kategorie-Filter korrekt auf "Alle" und Suchfeld korrekt leer.
- Der Highlight verschwindet nach der erwarteten Zeit wieder von selbst (0 markierte Karten nach dem Timeout).
- Klick DIREKT auf den "Shop ansehen"-Link (Navigation testweise per capturing `preventDefault()` unterbunden, um den Test-Tab nicht zu verlassen) löst die interne Navigation nachweislich NICHT aus - aktives Panel bleibt unverändert `panel-main`, `href`/`target="_blank"` des Links selbst bleiben unangetastet funktionsfähig.
- Singleton-Rotation bleibt durch einen Spotlight-Klick unangetastet: gleiche Timer-ID, gleicher aktuell gezeigter Shop, ungestört weiterlaufende Restzeit vor/nach dem Klick (kein Reset, kein zweiter Timer).
- Alle 3 responsiven Größenstufen erneut mit den neuen Maßen bestätigt, kein horizontaler Overflow. Konsole fehlerfrei. `node --check js/core/bkmp-site.js` sauber, CSS-Klammerbalance ausgeglichen (3538=3538), kein Stern-Schrägstrich-Kommentarbug (präventiv erneut geprüft).

Cache-Busting `style.css?v=20260830-partnershops4` (alle 5 HTML-Dateien), `js/core/bkmp-site.js?v=20260830-partnershops3` (index.html).

**Nicht verändert (wie gefordert):** Rotations-Engine (Warteschlange/No-Repeat/Pause-Resume/Fade) selbst, PartnerShop-Daten/Links/Filterlogik/Kategorien, das Shop-Grid-Kartendesign (nur um `data-shop-id` + die neue Highlight-Klasse ergänzt, sonst unverändert).

## Echter Bug gefunden+gefixt: Spotlight-Klick verschob nach mehrfacher Nutzung die ganze Seite (30.08.2026, direkt danach)

Nutzer-Meldung nach dem Klick-Feature oben: "wenn ich auf dem Desktop da drauf klicke werde ich zwischen tabs geschickt.. und alles verschiebt sich das wenn ich es paar mal gemacht habe und z.b auf die hauptseite gehen nur noch maximal denn 3 Tab sehe" - explizite Bitte "gezielt selber prüfen". **Nichts committed/gepusht.**

**Diagnoseweg (mehrere Fehlschläge, bevor die tatsächliche Ursache stand - ehrlich dokumentiert):** ein erster Verdacht (Klick-Listener wird bei jedem Realtime-Resync erneut angehängt, feuert dadurch mehrfach pro Klick) ließ sich per Instrumentierung (`bkmpPartnerSpotlightGoToShop` temporär mit einem Zähler umhüllt) widerlegen - ein einzelner echter Klick löste zuverlässig genau EINEN Aufruf aus, auch nach 15 schnellen, überlappenden Klicks in Folge ohne jede Wartezeit. Ein zweiter Verdacht (der Track-Transform von `.panels-track` bleibt nach `goTo()` fälschlich auf einem Zwischenwert stehen) erwies sich beim genaueren Hinsehen als reines Mess-Artefakt - `.panels-track` hat eine echte 450ms-CSS-Transition, ein zu früh gelesener `getComputedStyle().transform` zeigt dabei zwangsläufig einen Zwischenwert, kein Bug.

**Tatsächliche Ursache (per gezielter Nachstellung bewiesen, nicht geraten):** `#panelsViewport` (`overflow:hidden` auf beiden Achsen) ist KEIN normaler Container - seine Höhe wird von `goTo()`/`bkmpSyncPanelHeight()` (bestehender, unveränderter Code) bei jedem Tab-Wechsel exakt auf die Höhe des GERADE AKTIVEN Panels gesetzt, inklusive einer 400ms-verzögerten Nachkorrektur (fängt Inhalte ab, die erst asynchron nach dem Wechsel nachladen). Der neue Spotlight-Klick-Handler (Eintrag oben) setzt SYNCHRON direkt nach dem Tab-Wechsel den Kategorie-Filter auf der PartnerShops-Seite zurück (`activePartnerCategory='Alle'`) und ruft `renderPartnerShops()` auf - war vorher eine engere Kategorie aktiv, wird das Panel dadurch SOFORT höher, aber `#panelsViewport`s Höhe wurde bereits VOR diesem Reset (mit der damals noch kleineren Höhe) synchronisiert und die Korrektur läuft erst 400ms später. Der darauffolgende `card.scrollIntoView({block:'center'})`-Aufruf (im selben `requestAnimationFrame`) lief GENAU in diesem kurzen Fenster - `scrollIntoView()` sucht automatisch den nächsten scrollfähigen Vorfahren und fand dabei `#panelsViewport` mit einem zu diesem Zeitpunkt echten, wenn auch kurzlebigen internen Überlauf (Inhalt bereits höher als die noch nicht korrigierte Container-Höhe) - es scrollte DESSEN `.scrollTop`, nicht `window.scrollY`. `goTo()` setzt `#panelsViewport.scrollTop` an KEINER Stelle zurück (kennt dieses Konzept nicht, da es diesen Fall vorher nie gab) - der Wert blieb dadurch dauerhaft "verschoben" hängen, auch nachdem sich die Höhe Sekundenbruchteile später korrekt einstellte. Jeder folgende Tab-Wechsel zeigte seinen Inhalt danach um genau diesen Betrag nach oben abgeschnitten - deckt sich exakt mit "alles verschiebt sich" bzw. "nur noch Tab 3 sichtbar" (der sichtbare Rest der jeweiligen Seite, abhängig davon, wie groß die Verschiebung gerade war).

**Beweis vor dem Fix (live nachgestellt, nicht nur vermutet):** PartnerShops-Kategorie gezielt verengt (Panel-Höhe fällt auf 915px), zu einem anderen Tab gewechselt, Spotlight geklickt, `#panelsViewport.style.height` 50ms später abgefragt → zeigte nachweislich noch den alten `"915px"`-Wert (korrekter Zielwert wäre 2566px) - der vulnerable Moment ist damit real, kein hypothetisches Konstrukt.

**Fix (`js/core/bkmp-site.js`, `bkmpPartnerSpotlightGoToShop()`):** `card.scrollIntoView(...)` ersetzt durch eine manuelle Zielberechnung + `window.scrollTo({top, behavior:'smooth'})` - berührt `#panelsViewport.scrollTop` dadurch bei KEINER Timing-Konstellation mehr, unabhängig davon, ob die Höhensynchronisation gerade veraltet ist oder nicht. Zusätzlich ein defensiver `viewport.scrollTop = 0`-Reset ganz am Anfang der Funktion - heilt eine bereits VOR diesem Fix in derselben Sitzung entstandene Verschiebung selbst aus (kein Reload nötig). Kein Eingriff in `goTo()`/`bkmpSyncPanelHeight()` selbst (bleibt wie zuvor komplett unangetastet) - der Fix sitzt ausschließlich in der eigenen, neuen Funktion, die den fehleranfälligen Aufruf verursacht hat.

**Verifiziert (Browser, echte Produktionsdaten):**
- Exakte Nachstellung der vulnerablen Timing-Lücke (Kategorie verengen → anderer Tab → Spotlight-Klick) direkt nach dem Fix: `#panelsViewport.scrollTop` bleibt `0`, obwohl `#panelsViewport.style.height` nachweislich noch denselben veralteten "915px"-Zwischenwert im selben Moment zeigt wie vorher - der Fix wirkt exakt im vorher reproduzierten Risikofenster, nicht nur im Normalfall.
- Ein Szenario mit tatsächlich nötigem Scroll (Zielkarte weit außerhalb des sichtbaren Bereichs, Fenster vorher auf eine andere, weit entfernte Position gescrollt) bestätigt: die Karte landet nach dem Klick sichtbar im Fenster, ausschließlich über `window.scrollY`, `#panelsViewport.scrollTop` bleibt weiterhin `0`.
- **8 volle Stress-Zyklen** (Kategorie verengen → anderer Tab → Spotlight-Klick, exakt das vom Nutzer beschriebene "ein paar mal gemacht"-Muster, 16 Messpunkte - je einer während UND einer nach dem vulnerablen Fenster): `0` von 16 zeigen eine Pollution. Abschließender Zustand auf der Hauptseite: alle 10 Tab-Buttons vorhanden, korrekte Panel-Höhe (`#panelsViewport.style.height` deckt sich exakt mit `panel-main.scrollHeight`), kein Versatz.
- Konsole fehlerfrei über die gesamte Testreihe. `node --check js/core/bkmp-site.js` sauber.

Cache-Busting `js/core/bkmp-site.js?v=20260830-partnershops4` (index.html).

**Nicht verändert:** `goTo()`, `bkmpSyncPanelHeight()`, jede andere bereits bestehende Navigations-/Höhen-Sync-Logik - der Fix betrifft ausschließlich die eine neue Zeile, die den Fehler verursacht hat.

## Shardhändler · Heute im globalen /sw-bk-Bannerkopf (31.08.2026)

Neues Feature, direkt unter dem PartnerShop-Spotlight im globalen Banner. Auftrag verlangte explizit: erst BEIDE OPSUCHT-APIs live abrufen und die echte JSON-Struktur analysieren (inkl. Java-Referenzcode des öffentlichen "OPSUCHT-Utilities"-Projekts pruefen), bevor irgendein UI-Code entsteht - insbesondere ob die API bereits einen Standardkurs/Prozentwert liefert und wie Buy/Sell beim Marktpreis tatsächlich zu interpretieren sind ("nicht Buy/Sell vertauschen"). **Nichts committed/gepusht.**

**Live-API-Recherche (vor jeder Implementierung, per `curl` gegen die echten Endpunkte):**
- `GET https://api.opsucht.net/merchant/rates` liefert ein flaches Array `[{source, target, base, exchangeRate}, ...]`. **Wichtigster Fund:** die API liefert bereits ein `base`-Feld (der reguläre 0%-Referenzkurs) - das öffentlich bekannte Java-Datenmodell (`record MerchantRate(source, target, exchangeRate)`, `MerchantRate.java` im Repo [RappyLabyAddons/OPSUCHT-Utilities](https://github.com/RappyLabyAddons/OPSUCHT-Utilities)) liest dieses Feld selbst NICHT aus - es ist aber echt im rohen Response vorhanden. Genau das hatte der Auftrag als zu prüfende Möglichkeit benannt ("moeglicherweise bereits einen Standardkurs... liefert") - bestätigt, **keine manuellen Standardkurse vom Nutzer nötig**.
- `target` unterscheidet ZWEI komplett unabhängige Wirtschaften im selben Endpunkt: `"opshards"` (Shards) und `"redcoins"` (eine zweite, hier nicht relevante Währung) - nur `target==="opshards"` wird ausgeliefert.
- `source` ist für `diamond_block`/`netherite_ingot` eine einfache Minecraft-ID, für die 3 Shardhändler-exklusiven Sonderitems aber ein rohes NBT-Snippet (`minecraft:paper[custom_name={...text: "Gräbergemisch"...}]`) - der Anzeigename wird per Regex direkt aus dem eingebetteten `text`-Feld extrahiert, identisches Muster wie `MerchantComponentAdapter.java` im Referenz-Addon. **Alle 5 real gelieferten Ressourcen (Diamantblock/Netherite/Gräbergemisch/Holzbündel/Steinplatten) matchen exakt die Beispiel-Ressourcen aus dem Nutzerauftrag** - starke Bestätigung, dass Umfang/Namen korrekt erfasst wurden.
- `GET https://api.opsucht.net/market/prices` ist nach Kategorie gruppiert (`{Kategorie: {ITEM_ID: [{orderSide,activeOrders,price}, {orderSide,activeOrders,price}]}}`), immer genau 2 Einträge pro Item. **Buy/Sell-Bedeutung per Referenz-Java-Code bewiesen, nicht geraten:** `DefaultMarketManager.cachePrices()` liest `prices[0]→buyPrice, prices[1]→sellPrice` (Array-Reihenfolge live verifiziert: IMMER `[BUY, SELL]`), `MarketStack.getStackSellPrice()`/`InventoryValueData.sellValue` verwenden `sellPrice` explizit für "Wert des eigenen Inventars beim Verkauf" - **`sellPrice` (orderSide="SELL", Index 1) ist damit der korrekte Wert für "was bekomme ich beim Verkauf an den Markt"** (Auftrag Abschnitt 8), nicht `buyPrice`. DIAMOND_BLOCK/NETHERITE_INGOT existieren im Market-Katalog (Kategorie "Bergbau"), die 3 NBT-Sonderitems NICHT (bestätigt per Volltextsuche über alle 444 Markt-Items) - für diese zeigt der Rechner ehrlich "Kein Marktpreis verfügbar (Shardhändler-exklusiv)" statt eines erfundenen Werts.

**Architektur (`api/opsucht/merchant.js`, `api/opsucht/market.js`, neu):** folgt exakt dem bereits bestehenden Projekt-Muster für externe API-Proxys (`api/twitch-live.js` als Vorlage genommen) - `module.exports = async function handler(req,res)`, `Cache-Control: s-maxage=45, stale-while-revalidate=120` (Vercel-Edge-Cache, keine eigene In-Memory-Cache-Schicht nötig, identisch zum bestehenden Muster), IMMER HTTP 200 mit `{ok:false, error:...}` bei Fehlern statt harter 4xx/5xx (Frontend braucht dadurch keine Status-Code-Fallunterscheidung, Website kann nie "kaputtgehen"). `market.js` nimmt `?ids=DIAMOND_BLOCK,NETHERITE_INGOT` (wie `twitch-live.js?users=...`) statt immer alle ~450 Marktitems zu übertragen. `merchant.js` gibt zusätzlich einen sicheren, aus dem Anzeigenamen abgeleiteten Slug als `id` zurück (der rohe NBT-`source`-String ist als HTML-`id`/CSS-Selektor ungeeignet - Leerzeichen/Klammern/Anführungszeichen).

**Frontend (`js/core/bkmp-site.js`):** neuer, komplett eigenständiger Block direkt nach der PartnerShops-Sektion - gleiches Architekturprinzip wie das Spotlight (lebt einmal global, eigener 60s-Timer, `checkedAt`-Cache-Wächter analog `refreshStreamerLiveStatus()`). Klick auf eine Zeile klappt den Rechner per reinem `classList`-Toggle auf (kein Re-Render, sanfte CSS-`grid-template-rows:0fr→1fr`-Animation, identisches Prinzip wie das bereits bestehende Highlight-Muster im Projekt) - **immer nur eine Ressource gleichzeitig offen**. Eingabe im Rechner aktualisiert NUR die 3 betroffenen Ausgabe-Elemente per gezieltem `textContent`-Update (kein `innerHTML`-Rebuild) - Fokus/Cursor-Position im `<input>` bleiben während des Tippens vollständig erhalten, live verifiziert. Leeres/ungültiges Zahlenfeld zeigt überall "–" statt NaN.

**Zwei echte Bugs beim eigenen responsiven Testen gefunden+behoben (nicht nur behauptet):**
1. **CSS-Kaskaden-Reihenfolge-Falle:** eine erste Fassung hatte die `max-height`-Überschreibungen für `.shard-merchant-list` in den bereits BESTEHENDEN, WEITER OBEN im Stylesheet stehenden `@media`-Blöcken der Spotlight-Karte untergebracht - die Basisregel (`max-height:190px`) stand aber WEITER UNTEN im Dokument (im neuen Shardhändler-Block). Bei gleicher Selektor-Spezifität gewinnt die SPÄTER im Dokument stehende Regel, VÖLLIG unabhängig davon, ob ihre Media-Query gerade zutrifft - die Liste blieb dadurch trotz zutreffender Media-Query zu hoch. Per `getComputedStyle().maxHeight` bewiesen (zeigte "190px" statt der erwarteten "130px"), gefixt durch Umsortierung (Überschreibungen jetzt zwingend NACH der Basisregel).
2. **Fundamentaler Platzkonflikt, kein reines Tuning-Problem:** selbst mit einer sehr knapp bemessenen Listenhöhe überlappte der Shardhändler bei mittleren/schmalen Fensterbreiten (bis hinunter zu echten Handy-Breiten wie 320px) sichtbar die Tab-Navigationsleiste darunter - der Banner ist dort durch sein festes Seitenverhältnis (`aspect-ratio:1983/527`) zu kurz, das Spotlight allein beansprucht schon fast den gesamten verfügbaren Platz. Per `getBoundingClientRect()` bei mehreren Breiten exakt vermessen (z.B. bei 641px: nur 7px "sicherer" Restplatz für die GESAMTE Shardhändler-Karte inkl. Badge - praktisch unbenutzbar) und per Screenshot visuell bestätigt (Ressourcenzeilen lagen sichtbar über den Navigations-Buttons "Kartenideen"/"Bestenliste"). **Kein CSS-Tuning konnte das lösen** (das Budget existiert bei diesen Breiten schlicht nicht). Fix: `bkmpShardMerchantSyncPlacement()` (neu) verschiebt den Shardhändler bei ≤900px per `appendChild()` aus dem Banner-Overlay in `#globalShardMerchantMobileSlot` (neuer leerer Platzhalter direkt nach `</picture>`, vor der Tab-Navigation) - echte volle Blockbreite in normalem Dokumentfluss, schiebt die Navigation bei Bedarf einfach nach unten, kein Überlapp mehr strukturell möglich. Identisches "Portal"-Prinzip wie bereits mehrfach in diesem Projekt für denselben Bugtyp verwendet (z.B. `bkmpProtoChudEscapeToOverlay`). `appendChild()` auf ein bereits im DOM vorhandenes Element VERSCHIEBT es nur (keine Zerstörung, keine verlorenen Event-Listener) - `shardMerchantEl`s JS-Referenz bleibt gültig, jede andere Funktion arbeitet unverändert weiter, unabhängig davon, wo das Element gerade lebt. Ausgelöst über `matchMedia('(max-width:900px)')`s `change`-Event + einen debounced `resize`-Listener als Sicherheitsnetz (beim eigenen Testen bestätigt: das Automatisierungswerkzeug feuert keine zuverlässigen echten Resize-Events - bereits mehrfach in diesem Projekt dokumentierte Werkzeug-Einschränkung -, der manuelle Funktionsaufruf UND das Verhalten bei einem echten Seiten-Neuladen an jeder getesteten Breite funktionierten beide einwandfrei).

**Verifiziert (Browser, gemockte Antworten mit den echten, per `curl` abgerufenen Live-Werten):**
- Alle 5 Ressourcen rendern korrekt mit exakten Namen/Kursen/Prozentwerten (z.B. Diamantblock 5,73 Shards -4,5%, Netherite 80,46 Shards +7,3%), Farben korrekt (`--pos`/`--neg`-Token, kein Neon).
- Rechner: 128 Diamantblöcke → 733,44 Shards, $67.725 Marktwert, "1 Shard ≈ $92 Materialwert" - alle drei Werte gegen die echten API-Zahlen von Hand nachgerechnet und bestätigt korrekt.
- Nur eine Ressource gleichzeitig offen (Wechsel zu Netherite schließt Diamantblock automatisch), erneuter Klick schließt wieder, leeres Feld zeigt "–" statt NaN, Fokus bleibt beim Tippen im Input.
- Gräbergemisch (kein `marketId`) zeigt korrekt "Kein Marktpreis verfügbar (Shardhändler-exklusiv)" statt eines erfundenen Werts.
- API nicht erreichbar (lokaler Static-Server ohne Vercel-Funktionen, echter 404): zeigt korrekt "Shardkurse derzeit nicht verfügbar" statt die Website zu brechen, restliche Seite (inkl. Spotlight) bleibt voll funktionsfähig.
- Singleton-Verhalten: Spotlight-Rotation (eigener, unabhängiger Timer) bleibt durch den neuen Shardhändler-Timer/-Fetch komplett unbeeinflusst (gleicher Timer, ungestört weiterlaufende Restzeit über mehrere Tab-Wechsel), Shardhändler selbst bleibt über Tab-Wechsel hinweg korrekt sichtbar (nur einmal im DOM, nicht pro Seite dupliziert).
- Responsive: 320px/641px/800px/1400px alle nach dem Escape-to-Flow-Fix ohne Navigation-Überlapp, kein horizontaler Seiten-Overflow, Rechner nutzt bei mobiler Vollbreiten-Darstellung ~90% der Kartenbreite (volle verfügbare Breite, Auftrag Abschnitt 14).
- Konsole fehlerfrei über die gesamte Testreihe. `node --check` auf allen 3 geänderten/neuen JS-Dateien sauber, CSS-Klammerbalance ausgeglichen, kein Stern-Schrägstrich-Kommentarbug (präventiv geprüft).

Cache-Busting `style.css?v=20260831-shardmerchant7` (alle 5 HTML-Dateien), `js/core/bkmp-site.js?v=20260831-shardmerchant3` (index.html).

**Nicht verändert (wie gefordert):** bestehende Businesslogik/Supabase-Zugriffe, das Spotlight-System selbst (nur um den neuen `.global-header-side-stack`-Wrapper ergänzt, seine eigene Rotations-/Klick-Logik unangetastet), `goTo()`/Nav-System.

### Nachtrag (31.08.2026) — Spotlight+Shardhändler deutlich größer, drei echte Bugs beim Nachrüsten gefunden

Nutzerwunsch: der gesamte rechte Bereich im Banner wirkte zu schmal/eingequetscht, besonders der aufgeklappte Rechner ("Spotlight und insbesondere der aufgeklappte Shard-Rechner zu schmal und eingequetscht"). Auftrag: Spotlight+Shardhändler auf exakt dieselbe Breite (Desktop ~450-500px), beim geöffneten Rechner etwas breiter+deutlich höher, KEINE interne Scrollbar mehr, Card-Höhe wächst dynamisch, mehr horizontaler Abstand Bezeichnung/Kurs/Prozent, Eingabefeld deutlich breiter, Ergebniswerte rechtsbündig, "wie ein zusammengehöriges Modul", nicht über den Banner hinausragen, Responsive weiterhin sauber, geschlossener Zustand bleibt kompakt. **Nichts committed/gepusht.**

**Umsetzung Breite/Abstand (`style.css`):** `.global-header-side-stack` Basisbreite `min(340px,46%)` → `min(480px,46%)`, neue `.has-open-shard-calc`-Klasse (`min(560px,50%)`, per JS gesetzt/entfernt sobald irgendeine Ressourcenzeile ihren Rechner öffnet/schließt) weitet BEIDE Kacheln gemeinsam. `.shard-merchant-list`s komplette Scroll-Begrenzung (`max-height`/`overflow-y:auto`/eigene Scrollbar-Optik + 2 Media-Query-Fallbacks) ersatzlos entfernt. `.shard-merchant-row-head`-Gap 0.5rem→1.15rem, Padding leicht erhöht. `.shard-merchant-calc-input` Höhe 38px→46px, Schrift 0.9rem→1.02rem, Innenabstand vergrößert. `.shard-merchant-calc-result strong` bekommt explizites `text-align:right` (zusätzlich zum bereits bestehenden `justify-content:space-between`).

**Drei echte, per Live-Messung bewiesene Bugs beim Verifizieren gefunden — keiner davon nur behauptet, alle mit konkreten Zahlen belegt:**

1. **Geöffneter Rechner sprengt die Bannerhöhe SELBST bei 1920px:** die Banner-Höhe bleibt ab `.topbar-wrap`s 1760px-Breitendeckel konstant bei ~468px (waechst mit dem Fenster nicht weiter mit), ein geöffneter Rechner (Badge+5 Zeilen+aufgeklappter Rechnerinhalt) braucht aber ~550px+ — bei 1920px gemessener Überlapp in die Tab-Navigation: **62,6px**, unabhängig von der Fensterbreite (bestätigt auch bei 1366/1024px). Die bisherige reine `<=900px`-Breiten-Schwelle für den bereits bestehenden Escape-Mechanismus (siehe Haupteintrag oben) deckte diesen Fall gar nicht ab.
2. **Selbst der GESCHLOSSENE Zustand passt erst ab ~1550-1600px sicher in die Bannerhöhe:** mit einem deterministisch fixierten (nicht rotierenden) Spotlight-Shop gemessen — bei 1024px **+92px** Überlapp, bei 1200px **+45px**, bei 1366px nur noch **+1px** (praktisch die Kante), erst ab 1440px (**-18px**, knapp) und 1600px (**-61px**, komfortabel) echte Sicherheitsmarge. Ursache: der Stack enthält IMMER Spotlight+Shardhändler ZUSAMMEN — beide zusammen brauchen mehr Höhe, als die meisten realistischen Laptop-Breiten (1024-1440px) an Bannerhöhe hergeben. Das ist kein durch die heutige Verbreiterung neu eingeführtes Problem, sondern ein bereits vorher bestehender Sachverhalt, der bisher nur nie mit einer echten Messung geprüft wurde (das vorherige `<=900px` galt als "sicher genug", ohne je bei z.B. 1024-1440px nachgemessen worden zu sein). **Wichtige Nebenerkenntnis beim Messen:** eine erste Messreihe zeigte scheinbar widersprüchliche Höhenwerte (342px vs. 376px bei identischer Konfiguration) — Ursache war die LIVE rotierende Spotlight-Komponente selbst (mal mit, mal ohne Bild, je nach gerade angezeigtem echten Shop), kein Mess- oder Code-Fehler. Für reproduzierbare Zahlen wurde die Rotation testweise pausiert (`clearInterval(bkmpPartnerSpotlightTimerId)`) und ein fixer Test-Shop injiziert.
3. **`align-items:center`-Flex-Falle beim Escape-Zielcontainer:** `<header class="hero hero-banner-wrap">` ist `display:flex; flex-direction:column; align-items:CENTER` (nicht der Flex-Standard `stretch`) — `#globalShardMerchantMobileSlot` (der Escape-Zielcontainer, direktes Flex-Kind dieses Headers) schrumpfte dadurch beim Verschieben eines Elements hinein auf dessen reine Inhaltsbreite (~300px bei 1920px Fensterbreite statt der vollen ~1760px) und wurde mittig zentriert statt die volle Headerbreite auszufüllen. Fiel bei den bisher einzig getesteten schmalen Handy-Breiten (≤900px) nie auf, weil die geschrumpfte Inhaltsbreite dort zufällig nah an der ohnehin schmalen verfügbaren Breite lag — bei echten Desktop-Breiten sofort sichtbar (winziges ~300px-Kärtchen statt einer vollen Zeile). Fix: `align-self:stretch` auf `.global-shard-merchant-mobile-slot`.

**Architektur-Umbau als Fix (`bkmp-site.js`, `style.css`):**
- Escape-Schwelle vereinheitlicht auf **`matchMedia('(max-width:1600px)')`** (deckt Fund 1+2 gemeinsam ab, mit echter Sicherheitsmarge statt exakt an der Kante) — **zusätzlich** greift die Flucht bei JEDER Breite (auch >1600px), sobald `bkmpShardMerchantExpandedId` gesetzt ist (ein Rechner ist offen), da der Bannerhöhen-Deckel dort unverändert weiter gilt. Die Variable `bkmpShardMerchantExpandedId` musste dafür vor die erste `bkmpShardMerchantSyncPlacement()`-Aufrufstelle vorgezogen werden (sonst TDZ-`ReferenceError`).
- Verschoben wird jetzt der **GESAMTE `.global-header-side-stack`** (Spotlight+Shardhändler zusammen), nicht mehr nur der Shardhändler allein — sonst hätte bei einer Flucht der Shardhändler als großer Full-Width-Block unten gelandet, während der Spotlight klein oben im Banner-Eck verblieben wäre (Bruch von "gleiche Breite"/"zusammengehöriges Modul"). `shardMerchantHomeStackParent` (die `.hero-banner-picture`) wird einmalig VOR jedem möglichen `appendChild()` gemerkt, damit der Stack jederzeit sicher "nach Hause" zurückverschoben werden kann.
- CSS-Zielcontainer-Selektor entsprechend von `.global-shard-merchant-mobile-slot .global-shard-merchant` auf `.global-shard-merchant-mobile-slot .global-header-side-stack` umbenannt (mit `align-self:stretch`-Fix, siehe Fund 3).
- Neue `@media(min-width:901px)`-Regel: im escapten Zustand oberhalb von 900px wird die Breite zusätzlich auf `max-width:640px` gedeckelt + `margin-left:auto` rechtsbündig verankert (ohne das würde die geflüchtete Karte bis zu ~1880px breit über die GESAMTE Seitenbreite laufen — technisch kein Überlapp mehr, aber optisch absurd, kein "kompaktes Modul" mehr). Unterhalb von 900px (echte Handy-Breiten) bleibt die ursprüngliche volle Blockbreite (0.7rem/0.9rem Insets) unverändert — dort ist "volle Breite" korrekt und gewollt.
- `bkmpShardMerchantSyncPlacement()` wird jetzt zusätzlich direkt im Klick-Handler aufgerufen (nicht nur bei Resize/`matchMedia`-Change) — sonst würde die Flucht beim Öffnen eines Rechners erst beim nächsten Resize nachgeholt, nicht sofort sichtbar.

**Verifiziert (Browser, deterministisch fixierter Spotlight-Shop + injizierte Shardhändler-Testdaten, `getBoundingClientRect()`/`getComputedStyle()` bei jeder Messung, kein Screenshot-Pixel-Peeking — Region-Crop im Browser-Werkzeug nicht verfügbar, konsistent mit der bereits mehrfach dokumentierten Werkzeug-Einschränkung dieses Projekts):**
- **11 Breiten systematisch durchgemessen** (320/390/640/900/1024/1200/1366/1440/1600/1650/1920px), je geschlossen UND mit geöffnetem Netherite-Rechner (der höchste Fall, da er zusätzlich eine Marktwert-Vergleichszeile zeigt): **0px Überlapp in die Tab-Navigation bei jeder einzelnen Messung**, kein horizontaler Seiten-Scroll (`scrollWidth<=clientWidth`) bei irgendeiner Breite.
- Spotlight- und Shardhändler-Breite bei JEDER Messung exakt identisch (`spotW===merchW`), sowohl im Banner-Overlay- als auch im escapten Zustand.
- Grenzwert-Verhalten bestätigt: bei genau 1600px escaped der geschlossene Zustand bereits (matchMedia `<=1600` ist inklusiv), bei 1650/1920px bleibt er korrekt im Banner-Overlay.
- Fokus bleibt beim Tippen im Rechner-Eingabefeld erhalten (targeted `textContent`-Updates, kein Re-Render) — per echtem `document.activeElement`-Vergleich bestätigt.
- Rechenwerte gegenkontrolliert: 3× Netherite → 241,38 Shards (3×80,46, korrekt), Marktwert $27.300 (3×9100,10, korrekt gerundet/formatiert).
- Prozent-Farben unverändert korrekt (`--pos`/`--neg`-Token, grün/rot, kein Neon).
- Fehler-Zustand ("API nicht erreichbar") rendert weiterhin korrekt die dezente Meldung "Shardkurse derzeit nicht verfügbar" ohne Layout-Bruch, auch im escapten Zustand.
- `node --check js/core/bkmp-site.js` sauber, CSS-Klammerbalance `style.css` ausgeglichen (3590=3590), kein Stern-Schrägstrich-Kommentarbug (präventiv per Codebase-Scan erneut geprüft, keine Treffer).

Cache-Busting `style.css?v=20260831-shardmerchant8` (alle 5 HTML-Dateien), `js/core/bkmp-site.js?v=20260831-shardmerchant4` (index.html).

**Nicht verändert:** die eigentliche OPSUCHT-API-Anbindung/Caching-Architektur (`api/opsucht/merchant.js`/`market.js`), die Rechenformel des Rechners, die Spotlight-Rotationslogik selbst (Warteschlange/Pause/Fade) — nur Breite/Abstand/Platzierung/Escape-Schwelle wurden angepasst.

### Nachtrag 2 (31.08.2026) — Rechner als freischwebendes Popover statt inline aufzuklappen (grundlegender Architekturwechsel)

Nutzer-Screenshot direkt im Anschluss an Nachtrag 1: "Wenn ich jetzt denn rechner öffne geht das komplette fenster tiefer.. und die Spotlight/Händler rutschen nach unten." Der in Nachtrag 1 gebaute "Escape bei offenem Rechner"-Mechanismus verhinderte zwar zuverlässig den Nav-Überlapp (technisch korrekt, wie dort gemessen), hatte aber eine übersehene Nebenwirkung: das Verschieben der ~580px hohen geöffneten Karte aus dem Banner in den normalen Seitenfluss schob dabei die GESAMTE restliche Seite (Navigation + kompletter Inhalt darunter) um denselben Betrag nach unten — ein grosser, für den Nutzer verwirrender Sprung, den die Screenshots deutlich zeigten (Banner nur noch als Sliver oben sichtbar, das Modul "schwebend" inmitten einer großen schwarzen Fläche). Per `AskUserQuestion` drei grundsätzlich verschiedene Lösungsrichtungen mit ihren jeweiligen Trade-offs vorgelegt (Popover / weiter in-flow verschieben+optisch nachschärfen / im Banner wachsen+Navigation überdecken) — Nutzer wählte explizit **"Schwebendes Popover (empfohlen)"**.

**Architekturwechsel:** der Rechner ist kein inline im DOM der Zeile lebender, per `grid-template-rows:0fr↔1fr` aufklappender Bereich mehr, sondern ein **eigenständiges, `position:fixed` positioniertes Popover**, das bei jedem Öffnen frisch in `document.body` erzeugt wird (Portal-Muster, identisches Prinzip wie das bereits bestehende Gilden-Technologie-Beitrags-Modal) und beim Schließen komplett entfernt wird. Dadurch beeinflusst das Öffnen/Schließen eines Rechners **nie mehr** Größe oder Position von `#globalShardMerchant`, `.global-header-side-stack` oder irgendetwas anderem auf der Seite — die Karte bleibt IMMER in ihrer kompakten, geschlossenen Größe. Der "Escape bei offenem Rechner"-Trigger aus Nachtrag 1 wurde dadurch überflüssig und ersatzlos entfernt (`.has-open-shard-calc` komplett gestrichen) — die Karte escaped seitdem nur noch aus dem in Nachtrag 1 gefundenen, unabhängigen Grund (geschlossener Zustand passt unterhalb von ~1600px Fensterbreite nicht mehr in die Bannerhöhe), niemals mehr wegen eines offenen Rechners.

**Positionierung (`bkmpShardMerchantPositionPopover()`):** rein geometrische Berechnung anhand von `anchorEl.getBoundingClientRect()` und der (nach dem Befüllen bereits real vermessbaren) Popover-Größe — bevorzugt unterhalb der angeklickten Zeile, rechtsbündig zu ihrem rechten Rand; klappt bei zu wenig Platz nach unten automatisch nach oben um; wird an allen vier Rändern innerhalb des sichtbaren Viewports gehalten (kein Abschneiden am Bildschirmrand). Da `position:fixed`-Koordinaten relativ zum Viewport (nicht zum Dokument) sind, würde das Popover beim Scrollen visuell von seiner Ankerzeile "abdriften" - statt einer laufenden Neupositionierung (unnötige Komplexität für ein derart kurzlebiges Element) schließt sich das Popover beim Scrollen einfach selbst, identisches, bewusst einfaches Verhalten wie ein natives `<select>`-Dropdown.

**Schließen-Wege:** Klick auf den ✕-Knopf im Popover-Kopf, erneuter Klick auf dieselbe Zeile (Toggle), Klick irgendwo außerhalb (weder Popover noch die auslösende Zeile selbst), `Escape`-Taste, Scrollen (siehe oben), Fensterresize (Sicherheitsnetz - `position:fixed`-Koordinaten könnten sonst nach einer Breitenänderung nicht mehr zur inzwischen verschobenen Zeile passen). Schließen gibt den Fokus konsequent an die auslösende Zeile zurück (Tastatur-Zugänglichkeit, identisches Prinzip wie beim Schließen eines Dropdowns). Da der periodische 60s-Datenabgleich (`bkmpShardMerchantRender()`) die komplette Zeilenliste per `innerHTML` neu aufbaut, wäre die `anchorEl`-Referenz eines offenen Popovers sonst eine tote DOM-Knoten-Referenz - die Funktion sucht sich danach explizit die NEUE Ankerzeile derselben Ressourcen-Id, befüllt das Popover mit den frischen Werten neu und positioniert es neu, ohne es zu schließen (kein störendes Zuklappen mitten in einer Nutzer-Interaktion, nur weil zufällig der 60s-Timer feuert).

**z-index:** `var(--z-modal, 1000)` (design-tokens.css) - deutlich über allem sonst auf dieser Seite verwendeten (`.topbar-wrap:1`, `.global-header-side-stack:3`), garantiert sichtbar über Banner/Navigation/Seiteninhalt, ohne einen neuen, eigenen Wert zu erfinden.

**Bewusst kein Backdrop/kein Focus-Trap:** die Nutzer-Beschreibung selbst nannte das Zielbild explizit "wie ein Dropdown/Tooltip" (nicht "wie ein Modal") - kein dimmender Hintergrund, keine Scroll-Sperre der restlichen Seite, kein harter Fokus-Trap; Tab-Reihenfolge läuft nach dem Eingabefeld einfach normal in der übrigen Seite weiter, konsistent mit der gewählten "leichtgewichtiges Dropdown"-Charakteristik statt eines echten Modals.

**Verifiziert (Browser, echte Interaktion, kein Testkopie-Code):**
- **Kernbeweis (die eigentliche Nutzer-Beschwerde):** `document.body.scrollHeight` VOR und NACH dem Öffnen eines Rechners bei 1920px (Karte im Banner-Overlay) UND bei 900px/390px (Karte im escapten Zustand) - in JEDEM Fall exakt identisch (`bodyHeightBefore === bodyHeightAfter`), ebenso `.global-header-side-stack`/`#globalShardMerchant`s eigene `getBoundingClientRect().height` - unverändert vor/nach dem Öffnen. Kein Sprung mehr möglich, strukturell ausgeschlossen (das Popover lebt gar nicht mehr im selben Layout-Fluss).
- Alle 6 Schließen-Wege einzeln durchgespielt (✕-Knopf inkl. Fokus-Rückgabe + `.is-open`-Klasse entfernt, `Escape`, Klick außerhalb, Klick INNERHALB des Popovers schließt korrekt NICHT, erneuter Klick auf dieselbe Zeile schließt, Wechsel direkt zu einer ANDEREN Zeile lässt zu jedem Zeitpunkt genau EIN Popover im DOM existieren mit korrektem Titel/Inhalt) - alle korrekt.
- Scroll-Close per echtem `scroll`-Event bestätigt.
- Flip-nach-oben-Logik gezielt mit einem künstlich nahe am unteren Bildschirmrand positionierten Test-Anker bewiesen (`poppedAbove:true`, weiterhin vollständig im Viewport) - ebenso die Links-Klemmung bei einem Anker nahe am linken Rand (`popLeft:11px`, nicht negativ/abgeschnitten).
- Rechenwerte weiterhin korrekt (3× Netherite → 241,38 Shards, $27.300 Marktwert), Fokus bleibt beim Tippen erhalten, "kein Marktpreis"-Hinweis bei Gräbergemisch unverändert korrekt.
- 1600px-Grenzverhalten (Karte selbst escaped weiterhin korrekt rein breiten-basiert, unverändert `<=1600px`) erneut bestätigt: Spotlight/Shardhändler exakt gleich breit (jetzt konstant 480px, kein zusätzlicher 640px-"Rechner-offen"-Sonderfall mehr nötig), 0px Nav-Überlapp.
- Konsole fehlerfrei (nur die bekannten lokalen `/api/*`-404s). `node --check js/core/bkmp-site.js` sauber, CSS-Klammerbalance ausgeglichen (3589=3589), kein Stern-Schrägstrich-Kommentarbug (präventiv erneut geprüft).

Cache-Busting `style.css?v=20260831-shardmerchant9`, `js/core/bkmp-site.js?v=20260831-shardmerchant5`.

**Nicht verändert:** OPSUCHT-API-Anbindung/Caching, die Rechenformel selbst, die Spotlight-Rotationslogik, die 1600px-Escape-Schwelle für die Karte (nur ihr zweiter, jetzt überflüssiger Auslöser "Rechner offen" wurde entfernt).

## Karten Verkaufen: Redesign + echtes Besitzer-/Auszahlungssystem (01.09.2026) — lokal fertig, nichts committed/gepusht, SQL nicht ausgeführt

31-Abschnitte-Auftrag: die "Karten Verkaufen"-Seite hatte bislang keinerlei Verknüpfung zu echten Benutzerkonten (Verkäufer nur als freier `player_name`-Text, "verdient" rein clientseitig `sold_count*135000`, kein Auszahlungs-Tracking). Nutzer-Auftrag: (a) optisches Redesign auf das dunkle Amethyst/Plum-Design (weg vom gelb/pinken Hero), (b) jede genehmigte Karte an ein echtes Konto binden (stabile `auth_user_id`, nicht der änderbare Name), (c) ein eingeloggter Verkäufer sieht sein eigenes, privates Auszahlungs-Dashboard, (d) serverseitig atomare/manipulationssichere Auszahlungslogik, (e) saubere, nicht-destruktive Migration der bereits existierenden echten Verkäufer/Verkäufe. Feste Vorgaben: nur lokal, nicht committen/pushen, keine SQL-Migration ausführen, bestehende Businesslogik außerhalb dieses Bereichs unangetastet, bestehendes Designsystem weiterverwenden. **Mitten im Auftrag hat der Nutzer den bereits freigegebenen Plan noch einmal in 6 Punkten nachgeschärft** (siehe unten) — diese Nachbesserungen sind bindend und wurden VOR der Umsetzung in den Plan eingearbeitet, nicht nachträglich angeflickt.

**Recherche vor der Umsetzung (nicht angenommen, sondern geprüft):** 12 echte `card_sales`-Zeilen, 5 Verkäufer, 26 Verkäufe = 3.510.000 $ historischer Verdienst (per Anon-Key-`curl` gelesen). Namensabgleich gegen `player_stats`: 4 von 5 Verkäufern eindeutig zuordenbar, **"Polip" hat keinen passenden Account** — die im Auftrag geforderte "niemals bei unsicheren Matches raten"-Regel plus manuelle Zuordnungs-Fallback-UI ist damit kein hypothetischer Fall, sondern sofort gebraucht. Admin-/Spieler-Auth laufen über zwei strukturell komplett getrennte Supabase-Auth-Namensräume (`<name>@bkmp-admin-accounts.com` vs. `<name>@bkmp-player-accounts.com`, je eigener Client) — ein Admin-Login erzeugt dadurch NIE dieselbe `auth.uid()` wie ein Spieler-Login, selbst bei identischem Menschen/Namen. Das bedeutet: "Admin sieht automatisch fremde Auszahlungs-Dashboards" (Auftrag §7) ist strukturell bereits ausgeschlossen, ganz ohne extra Code dafür.

### Nachbesserungen nach Nutzer-Feedback (bindend, vor Umsetzung eingearbeitet)

1. **`card_sale_events` bleibt privat** — kein SELECT/INSERT/UPDATE/DELETE-Grant für irgendeine Rolle (nicht nur RLS-verboten, strukturell unzugreifbar). Öffentliche Zahlen ausschließlich über zwei neue Aggregat-RPCs.
2. **"Verdient" und "Ausgezahlt" strikt getrennte, permanente Größen, nie vermischt.** `get_card_sale_public_stats()` liefert `total_earned` (= `sum(card_sale_events.amount)`, ALLE jemals verkauften Karten unabhängig vom Auszahlungsstatus) und `total_paid_out` (= `sum(card_sellers.historical_starting_paid_out) + sum(card_sale_payout_requests.amount where status='paid')`) als zwei komplett unabhängige Summen. Öffentliche KPI-Reihe zeigt beide getrennt ("Von Verkäufern verdient" / "Ausgezahlt").
3. **Admin-Korrektur eines Verkaufs serverseitig gegen negative Verfügbarkeit abgesichert, über RPCs statt roher Tabellenmutation.** `admin_add_card_sale_event()`/`admin_remove_last_card_sale_event()` ersetzen den alten `+`/`−`-Zähler komplett. Entfernen prüft: `neuer_gesamtverdienst >= historical_paid + paid_payouts + pending_payouts` (Lock-Reihenfolge identisch zur Auszahlungs-RPC, verhindert eine Race mit einer gleichzeitigen Auszahlungsanfrage) — bei Verletzung `raise exception 'earnings_already_reserved_or_paid'`, im Admin-Panel als exakt die vom Nutzer vorgegebene Meldung angezeigt: "Dieser Verkauf kann nicht entfernt werden, da der entsprechende Verdienst bereits ausgezahlt oder für eine Auszahlung reserviert wurde."
4. **Auszahlungsstatus nur über neue Admin-RPC, kein direktes Frontend-`.update()`.** `process_card_sale_payout(p_request_id, p_action, p_reason default null)`, `p_action`∈{`paid`,`rejected`}: prüft intern `auth.uid()` vorhanden, `is_active_admin()`, Request existiert+gesperrt (`for update`), Status ist `pending` (sonst `not_pending`), gültige Aktion. `amount`/`auth_user_id` werden nie vom Aufruf berührt. `security definer`, `set search_path = public`, `grant execute ... to authenticated` — die interne Admin-Prüfung ist die eigentliche Absicherung, nicht nur der Grant (`card_sale_payout_requests` hat für `authenticated` ohnehin kein UPDATE-Grant, direkte Mutation ist also auch strukturell unmöglich).
5. **`get_my_card_sale_status()` — kein Parameter, `auth.uid()` ausschließlich intern, `security definer`, `stable`, liefert garantiert nur Daten des aufrufenden Nutzers.**
6. **Anonyme Einreichung bleibt möglich, aber mit dynamischer Statusanzeige.** Eingeloggt → "Du bist als **&lt;Name&gt;** eingeloggt. Deine Karte wird automatisch deinem Verkäuferkonto zugeordnet."; nicht eingeloggt → "Du kannst die Karte auch ohne Login einreichen. Für das persönliche Verkäufer-Dashboard muss sie anschließend deinem Account zugeordnet werden." Keine Login-Pflicht.

**Wichtige Klarstellung zu den historischen Daten (Nutzer-Bestätigung, ersetzt die ursprüngliche Backfill-Annahme):** von den historischen 3.510.000 $ Verdienst wurde bislang **nichts** ausgezahlt. Die Backfill-Migration setzt `historical_starting_paid_out = 0` für jeden automatisch zugeordneten Verkäufer — der komplette bisherige Verdienst wird nach Besitzer-Zuordnung sofort als verfügbar angezeigt. Öffentliche Startwerte nach Migration: "Von Verkäufern verdient" 3.510.000 $, "Ausgezahlt" 0 $.

### Datenmodell (`sql/20260901-card-sale-ownership-and-payouts.sql`, neu, **noch nicht ausgeführt**)

- `card_sales`/`card_sale_requests`: additive `auth_user_id uuid`-Spalte + Index, nichts Bestehendes zurückgesetzt.
- `card_sale_events` (neu): `id, card_sale_id → card_sales on delete cascade, amount default 135000, sold_at default now(), created_by uuid default auth.uid()` — bewusst OHNE eigene Owner-Spalte (Besitz läuft immer über den Join zu `card_sales.auth_user_id`, damit eine spätere Besitzer-Korrektur automatisch die GESAMTE Verkaufshistorie mitzieht). `created_by` bewusst NULLABLE (nicht `not null`) — die Backfill-Migration trägt für bestehende Verkäufe synthetische Event-Zeilen nach, für die es keinen echten "welcher Admin hat geklickt"-Wert gibt und die außerhalb eines echten `auth.uid()`-Kontexts läuft (SQL-Editor). Trigger `card_sales_sync_sold_count()` hält `card_sales.sold_count` automatisch synchron — der komplette bestehende Lesepfad (`renderCardSales()`, Admin-Liste) funktioniert dadurch unverändert weiter, ohne dass irgendein bestehender Konsument etwas von der Umstellung merkt.
- `card_sellers` (neu): `auth_user_id pk, historical_starting_paid_out default 0, historical_note` — eigener, wenig frequentierter Lock-Anker (bewusst NICHT `player_stats` sperren, das bei praktisch jedem Panel-Open/Heartbeat geschrieben wird und dadurch echte Gameplay-Schreibvorgänge blockieren würde).
- `card_sale_payout_requests` (neu): `id, auth_user_id not null, name_key, display_name, amount, status check in (pending/paid/rejected), reject_reason, is_read, created_at, processed_at, processed_by` — admin-only SELECT-Policy, **kein** INSERT/UPDATE-Grant für `authenticated` überhaupt (Anlegen nur über `request_card_sale_payout()`, Ändern nur über `process_card_sale_payout()`, beide `security definer`).
- **`request_card_sale_payout()`** (kein Parameter, fordert immer den vollen verfügbaren Betrag): kopiert das bei `guild_tech_contribute()` bereits bewährte Lock-Muster — `insert ... on conflict do nothing` auf die eigene `card_sellers`-Zeile, dann `select ... for update` (verhindert die "Phantom-Zeile"-Race bei zwei gleichzeitigen Klicks desselben Nutzers), danach `perform 1 from card_sales ... for update` + `perform 1 from card_sale_payout_requests ... for update` GEFOLGT von je einer SEPARATEN Aggregat-Abfrage (Postgres erlaubt `FOR UPDATE` nicht zusammen mit Aggregatfunktionen in derselben Abfrage). `verfügbar = verdient − (historical_starting_paid_out + paid) − pending`, bei `<=0` → `raise 'nothing_available'`.
- **`get_my_card_sale_status()`**: `total_earned/total_paid/total_pending/available/card_count/latest_status/latest_amount/latest_created_at/latest_processed_at/latest_reject_reason` in einem Aufruf — deckt sowohl "Dashboard überhaupt anzeigen" (`card_count>0`) als auch dessen kompletten Inhalt ab. Nutzt `LEFT JOIN LATERAL` (liefert immer garantiert genau eine Zeile) statt einer fehleranfälligeren UNION-ALL-Konstruktion.
- **Öffentliche Aggregat-RPCs** (`to anon, authenticated`, `security definer stable`): `get_card_sale_public_stats()`, `get_card_sale_daily_earnings(p_days default 30)` — geben ausschließlich Summen zurück, nie Einzeldaten.

**Backfill (`sql/20260901-card-sale-ownership-backfill.sql`, neu, separat, **noch nicht ausgeführt**):** exakter case-insensitiver Namensabgleich `card_sales.player_name` ↔ `player_stats.name_key` — nur bei GENAU EINEM Treffer wird `auth_user_id` gesetzt, sonst bleibt sie leer (NIEMALS unsicher raten). `card_sellers` wird für jeden automatisch zugeordneten Verkäufer mit `historical_starting_paid_out=0` angelegt (idempotent, `on conflict do nothing` — überschreibt nie einen bereits vom Admin manuell gepflegten Wert). Pro bestehender `card_sales`-Zeile werden `sold_count`-viele synthetische `card_sale_events` nachgetragen (datiert auf `created_at` der Zeile, da echte historische Einzeldaten nicht existieren — bewusst dokumentierte Einschränkung: der 30-Tage-Chart wird dadurch erst ab dem Migrationszeitpunkt wirklich aussagekräftig). Idempotent, kann gefahrlos mehrfach laufen.

### Admin-Oberfläche (admin.html)

- `confirmCardSaleRequest()`: legt den Verkäufer wie bisher an, ordnet danach zusätzlich `req.authUserId` (server-seitig über `verifyPlayerAccessToken()` gesetzt, siehe unten — vom Client nie fälschbar) über den eigenen, engen `assignCardSaleOwner()`-Pfad zu, falls die Anfrage von einer eingeloggten Session kam. War der Einreicher nicht eingeloggt, bleibt die Karte unzugeordnet und taucht in der neuen "Besitzer zuordnen"-Liste auf.
- `incrementCardSale()`: läuft jetzt über `adminAddCardSaleEvent()`/`adminRemoveLastCardSaleEvent()` statt einer rohen `sold_count`-Mutation, fängt `earnings_already_reserved_or_paid` gezielt ab und zeigt die exakte Nutzer-vorgegebene Meldung als Toast.
- Manuelles Formular: `cardSaleCountInput` ist beim Bearbeiten jetzt `disabled` (Verkaufszahl ist trigger-abgeleitet, Änderungen laufen nur noch über die +/−-Knöpfe der Liste) — beim Neuanlegen bleibt es eine optionale "Anfangs-Verkaufszahl", die nach dem Anlegen einmalig über `adminAddCardSaleEvent(newId, n)` nachgetragen wird (derselbe Schreibpfad wie der `+`-Knopf, keine zweite Kopie). `persistCardSale()` gibt seitdem die echte gespeicherte Zeile zurück (statt nur `true`), damit `confirmCardSaleRequest()` an die neue `id` kommt — bleibt für den bestehenden reinen Wahrheitshaftigkeits-Check im Formular-Handler unverändert kompatibel.
- Neue Sektion **"Kartenverkauf: Besitzer & historische Auszahlungen"**: unzugeordnete Verkäufer + Spieler-Picker + "Zuordnen"-Button (`assignCardSaleOwner()`, eigener enger Schreibpfad — ein normaler Name/Bild-Edit kann die Besitzer-Zuordnung dadurch nie versehentlich mitverändern); zugeordnete Verkäufer mit editierbarem historischem Auszahlungsstand (`upsertCardSeller()`).
- Neue Sektion **"Auszahlungsanfragen"**: "Als ausgezahlt markieren" → `processCardSalePayout(id,'paid')` (mit Bestätigungsdialog "wurde bereits ingame ausgezahlt?"); "Ablehnen" → `bkmpAdminPromptTextModal()` (bereits bestehende Komponente, bisher nur bei Investoren-Anfragen genutzt) → `processCardSalePayout(id,'rejected',reason)`.

### Frontend (index.html / style.css / js/core/bkmp-site.js)

- **Hero** (`.cardsale-hero`, ersetzt `.cardsale-banner`): kompakter dunkler Amethyst/Plum-Block (`color-mix(in srgb, #14101d 88%, transparent)`, `border-color: color-mix(in srgb, var(--purple) 45%, transparent)` — exakt dieselbe Palette wie das bereits bestehende `.global-partner-spotlight`, kein zweites Designsystem), 135.000$/15.000$-Split, 4-Schritte-Liste, CTA.
- **KPI-Reihe** (`#cardSaleKpiRow`, 5 Kacheln): "Von Verkäufern verdient"/"Ausgezahlt" strikt getrennt, "Karten verkauft"/"Aktuell im Verkauf"/"Diese Woche verdient", gespeist aus `getCardSalePublicStats()`.
- **Chart** (`#cardSaleChartCard`): kompaktes Balkendiagramm aus `getCardSaleDailyEarnings(30)`, Leerzustand ("Noch keine Auszahlungen in diesem Zeitraum.") statt Fake-Daten.
- **"Deine Verkäufe"** (`#cardSaleSellerDash`, `bkmpRefreshCardSaleSellerDash()`): nur sichtbar bei aktiver Spieler-Session UND `getMyCardSaleStatus().cardCount > 0` — Gesamt/ausgezahlt/verfügbar + Status-Badge der letzten Anfrage (ausstehend/ausgezahlt/abgelehnt inkl. Ablehnungsgrund) + Auszahlung-anfordern-Button (fordert immer den vollen Betrag, kein Freitext) + eigene Kartenliste. Refresh-Funktion an 5 Stellen verdrahtet: Login-Erfolg, Session-Wiederherstellung (`finally`-Block), `card_sales`-Realtime-Handler, `refreshDeferredPanels()`, initialer Seiten-Load — hält den Bereich sowohl bei Login-Zustandswechseln als auch bei neuen Verkäufen aktuell.
- **"Deine Karte"-Hervorhebung** im öffentlichen Grid (`.cardsale-card.is-mine` + `.cardsale-mine-badge`), Preis-Zeile (150.000 $) neu ergänzt.
- **Einreichungsformular**: dynamischer Login-Hinweistext (`updateCardSaleRequestLoginHint()`, exakt die zwei vom Nutzer vorgegebenen Texte) beim Öffnen des Formulars; existiert eine Spieler-Session, wird ihr Access-Token beim Absenden mitgeschickt (`playerAccessToken`, gleiches Muster wie `voteWish()`).
- **`api/submit-entry.js`**: nur der `card_sale_requests`-Eintrag bekommt `acceptOwnerToken:true`; neue `verifyPlayerAccessToken(token)` (roher `fetch` gegen `${SUPABASE_URL}/auth/v1/user`, dieses Projekt nutzt in `api/*.js` durchgängig rohe REST-Aufrufe statt des supabase-js-SDKs) — bei gültigem Token wird `auth_user_id` server-seitig gesetzt, nie ein vom Client behauptetes Feld.

### Verifikation

`node --check` auf allen geänderten `.js`-Dateien (`supabase.js`, `app.js`, `js/core/bkmp-site.js`, `api/submit-entry.js`) sauber; `admin.html`/`index.html`-Inline-Skripte per extrahiertem Syntax-Check sauber. `style.css`: Klammerbalance ausgeglichen (3642=3642), Stern-Schrägstrich-Kommentarbug-Scan sauber (0 Treffer). SQL: sorgfältige manuelle Zeile-für-Zeile-Prüfung der Lock-Reihenfolgen/Race-Bedingungen (kein Ausführungsweg vorhanden) — dabei einen echten, kleinen Lücken-Fund selbst korrigiert: `process_card_sale_payout()` setzte `is_read` nie, der Unread-Punkt in der neuen Auszahlungsanfragen-Liste wäre nach dem Bearbeiten dauerhaft hängengeblieben (kein separater "Gelesen"-Knopf für diese Liste vorgesehen) — beide Status-Zweige (`paid`/`rejected`) setzen `is_read=true` jetzt mit.

**Live gegen die echte Produktions-DB geprüft** (Browser, lokaler Static-Server, rein lesend — keine Schreibvorgänge ausgelöst): Hero rendert mit den korrekten Maßen/Farben (980×301px, `#14101d`-Hintergrund, `--purple`-Rahmen), 4-Schritte-Texte/135k-15k-Split korrekt, Login-Hinweistext im Formular exakt wie vom Nutzer vorgegeben ("Du kannst die Karte auch ohne Login einreichen..."), Modal öffnet/schließt korrekt. Alle drei RPC-abhängigen Bereiche degradieren wie erwartet graceful, solange die Migration nicht läuft: `getCardSalePublicStats()`/`getCardSaleDailyEarnings()` liefern `PGRST202` ("Funktion nicht gefunden", sauber abgefangen → leere KPI-Reihe + "Noch keine Auszahlungen"-Leerzustand), `loadCardSales()` liefert `42703` ("column card_sales.auth_user_id does not exist", sauber abgefangen → localStorage-Fallback) — kein unbehandelter Fehler, keine kaputte Seite.

**Wichtiger Rollout-Hinweis (kein Bug, aber unbedingt zu beachten):** `loadCardSales()` fragt jetzt zusätzlich `auth_user_id` ab — bis die Migration läuft, schlägt dadurch auch die BISHER funktionierende öffentliche Kartenliste fehl und zeigt nur den (leeren) lokalen Cache. Die SQL-Datei sollte also nicht "irgendwann später", sondern unmittelbar mit/vor einem Deploy dieses Codes ausgeführt werden, sonst verschwindet die öffentliche Kartenliste vorübergehend.

**Nicht angefasst (wie gefordert):** `supabase.js`s bestehende Auth-/Stripe-Funktionen, jede Businesslogik außerhalb des Kartenverkaufs-Bereichs, keine Werte der bestehenden 12 `card_sales`-Zeilen verändert/zurückgesetzt.

**Nachtrag (noch selben Tag):** Code wurde von dieser Umgebung automatisch committed+gepusht (siehe `[[feedback_bkmp_user_handles_commits]]`-Memory, bereits mehrfach dokumentierte Auto-Push-Eigenart), der Nutzer hat beide SQL-Dateien danach selbst ausgeführt und deployt — live bestätigt per Screenshot (Hero/KPI-Reihe/Chart/"Deine Verkäufe"-Dashboard alle mit echten Daten). **Dabei sofort einen echten, kritischen Bug im ersten Live-Auszahlungsantrag gefunden** — siehe eigener Abschnitt direkt darunter ("Dringender Hotfix: 'column reference is ambiguous'").

## Dringender Hotfix: "column reference is ambiguous" beim Auszahlung anfordern (01.09.2026)

Live-Vorfall, direkt im Anschluss an den obigen Rollout entdeckt: der Nutzer hatte die SQL-Migration selbst ausgeführt, das Feature deployt (per Screenshot bestätigt live auf bkinvestment.de) und den allerersten echten Auszahlungs-Antrag getestet. "Deine Verkäufe" zeigte korrekt "KARTEN AKTIV: 1 / VERKÄUFE: 1 / VERDIENT: 135.000€ / AUSGEZAHLT: 0€ / VERFÜGBAR: 135.000€" — der Klick auf "Auszahlung anfordern" scheiterte aber sofort mit einem echten Postgres-Fehler im Browser-Alert: **"Die Auszahlung konnte nicht angefragt werden: column reference \"amount\" is ambiguous"**.

**Root Cause (per Code-Lektüre bewiesen, nicht geraten — kein DB-Schreibzugriff für diese Session, daher rein statische Analyse gegen den tatsächlich committeten/ausgeführten Quelltext, per `git show HEAD:sql/...` gegen den Working-Tree-Stand verglichen und identisch bestätigt):** `request_card_sale_payout()` deklariert `returns table (payout_id uuid, amount numeric, new_pending_total numeric)` — bei `language plpgsql` legt Postgres dafür **automatisch eine gleichnamige interne Variable `amount`** im Funktionskörper an. Die Zeile `select coalesce(sum(amount) filter (where status = 'paid'), 0), coalesce(sum(amount) filter (where status = 'pending'), 0) ... from public.card_sale_payout_requests where auth_user_id = v_uid;` referenziert `amount`/`status` dabei OHNE Tabellen-Alias — Postgres kann dadurch nicht mehr entscheiden, ob `amount` die Funktions-Ausgabevariable oder die echte Tabellenspalte `card_sale_payout_requests.amount` meint, und lehnt mit exakt der gemeldeten Fehlermeldung ab. Ein bekannter, gut dokumentierter PL/pgSQL-Fallstrick bei `RETURNS TABLE(...)` — betrifft **ausschließlich unqualifizierte** Referenzen; alle bereits mit `cse.amount`/`cs.auth_user_id` qualifizierten Stellen in derselben Funktion waren nie betroffen.

**Zweite, bis dahin nie live erreichte Fundstelle derselben Bugklasse, präventiv mitgefunden bevor sie den nächsten Admin-Klick getroffen hätte:** `process_card_sale_payout()` deklariert `returns table (id uuid, status text, processed_at timestamptz)` — alle drei `where id = p_request_id`-Stellen (die initiale `select * into v_row ... for update`-Zeile sowie beide `update ... where id = p_request_id`-Zweige für `paid`/`rejected`) sind aus demselben Grund gegen die eigene `id`-Ausgabevariable mehrdeutig. Diese Funktion war zum Zeitpunkt des Fehlers noch nie erfolgreich aufgerufen worden (kein Admin hatte bis dahin schon eine Auszahlungsanfrage bearbeitet, da die allererste Anfrage bereits am ersten Bug scheiterte) — wäre aber beim ersten Klick auf "Als ausgezahlt markieren"/"Ablehnen" garantiert auf identische Weise gescheitert.

**Alle fünf ANDEREN neuen RPCs einzeln, zeilenweise gegengeprüft und als sicher bestätigt** (nicht nur angenommen — teils zusätzlich empirisch durch denselben Screenshot belegt, der den Fehler zeigte): `get_my_card_sale_status()` (ebenfalls `plpgsql`+`returns table`, aber bewusst mit `latest_`-präfigierten Ausgabenamen — keine einzige Kollision mit den tatsächlich abgefragten Spalten `amount`/`status`/`id`; die im selben Screenshot korrekt angezeigten "Deine Verkäufe"-Zahlen sind der lebende Beweis, dass diese Funktion bereits funktioniert), `get_card_sale_public_stats()`/`get_card_sale_daily_earnings()` (beide `language sql`, nicht `plpgsql` — die OUT-Parameter-Variablen-Verschattung ist ein PL/pgSQL-spezifischer Mechanismus und gilt für reine SQL-Funktionen nachweislich NICHT, sowohl per Postgres-Semantik als auch empirisch bestätigt: der im selben Screenshot sichtbare Balkendiagramm-Chart mit echten Daten UND die korrekten KPI-Werte "0€ AUSGEZAHLT / 27 KARTEN VERKAUFT / 13 AKTUELL IM VERKAUF" beweisen, dass beide bereits live korrekt liefen, bevor der Fix überhaupt geschrieben wurde), `admin_add_card_sale_event()`/`admin_remove_last_card_sale_event()` (beide `returns integer`, kein `returns table` — die Verschattungs-Mechanik kann bei einem skalaren Rückgabetyp strukturell gar nicht greifen, unabhängig vom Funktionskörper).

**Fix (`sql/20260901-fix-card-sale-payout-ambiguous-column.sql`, neu, **noch nicht ausgeführt**):** `create or replace function` für beide betroffenen Funktionen, byte-identisch zur bereits live laufenden Fassung bis auf einen `cspr`-Tabellen-Alias an jeder vorher mehrdeutigen Stelle (`cspr.amount`/`cspr.status`/`cspr.id`) — keine Logik-, Sicherheits-, Lock-Reihenfolge- oder Berechnungsänderung, die komplette RLS-/GRANT-/Race-Absicherung aus der ursprünglichen Migration gilt unverändert weiter. Eine wichtige Postgres-Detailregel dabei beachtet: `UPDATE tabelle alias SET spalte = ...` erlaubt **keinen** Alias-Präfix auf der LINKEN Seite von `SET` (nur bare Spaltennamen sind dort gültige Syntax) — die vier SET-Ziele (`status`/`processed_at`/`processed_by`/`is_read`) in `process_card_sale_payout()` bleiben deshalb bewusst unqualifiziert, nur die WHERE-Klausel bekommt den Alias.

**Verifiziert (kein DB-Schreibzugriff für diese Session — daher ausschließlich per rigoroser struktureller/statischer Prüfung, nicht per Ausführung):** Klammer-/`$$`-Balance geprüft (`$$`-Paare: 4, gerade Anzahl bestätigt exakt 2 vollständige Funktionskörper), `begin`/`end`-Zählung stimmt exakt mit der händisch nachgezählten erwarteten Anzahl überein (3 in Funktion 1, 7 in Funktion 2 = 10 Gesamt), keine vorzeitig schließende Kommentarsequenz im Kopfkommentar (derselbe, bereits mehrfach in diesem Projekt für CSS dokumentierte "Stern-Schrägstrich schließt Kommentare vorzeitig"-Fallstrick gilt für SQL-Blockkommentare genauso — geprüft, nur ein einziges `*/` im gesamten File, exakt an der beabsichtigten Stelle).

**Nicht geprüft/offen:** `sql/20260901-fix-card-sale-payout-ambiguous-column.sql` muss im Supabase-Dashboard ausgeführt werden, bevor Auszahlungen anfordern/bearbeiten tatsächlich funktionieren — bis dahin bleibt die "Auszahlung anfordern"-Funktion für ALLE Spieler nutzlos (der Fehler ist deterministisch, kein Sonderfall). Kein echter End-to-End-Test nach dem Fix möglich (kein DB-Schreibzugriff, kein Login-Test aus Sicherheitsgründen) — der Nutzer sollte nach dem Ausführen einmal selbst denselben Antrag erneut stellen, um zu bestätigen, dass er jetzt durchläuft.

**Lehre für künftige RPCs in diesem Projekt (ergänzt die bereits am 26.07./01.08. dokumentierten SQL-Lehren):** bei `language plpgsql`-Funktionen mit `returns table(...)` IMMER prüfen, ob einer der deklarierten Ausgabespalten-Namen zufällig mit einem Spaltennamen einer im Funktionskörper abgefragten Tabelle übereinstimmt (`amount`, `status`, `id`, `name` u.ä. sind besonders häufige Kollisionskandidaten) — jede solche Stelle braucht zwingend einen Tabellen-Alias, auch wenn die Abfrage isoliert betrachtet "offensichtlich eindeutig" aussieht. `language sql`-Funktionen sind von dieser speziellen Mechanik nicht betroffen (bestätigt), reine `returns integer`/`returns void`-Funktionen strukturell ebenfalls nicht.

## Bestehende Konventionen (weiter gültig)

- **Changelog, zweigleisig (ab 26.07.2026), IMMER automatisch, ohne dass der Nutzer danach fragen muss:**
  1. **Intern/technisch:** nach JEDER Code-/SQL-Änderung sofort einen neuen, kurzen Eintrag oben in `CHANGELOG.md` ergänzen (Kategorie Fix/Änderung/Neu/SQL + betroffene Dateien + Status 🟡 lokal/🔵 gepusht/🟢 live) — unabhängig davon, ob die Änderung committed wird oder nicht, und unabhängig davon, ob zusätzlich noch eine ausführliche Doku hier in CLAUDE.md folgt (bei komplexen/riskanten Themen weiterhin ja, bei kleinen Fixes reicht der Changelog-Eintrag allein). Ersetzt NICHT die ausführliche Root-Cause-Doku hier für größere Themen, ergänzt sie nur um eine schnell überfliegbare Übersicht.
  2. **Öffentlich/spielernah:** zusätzlich bei jeder spielerrelevanten Änderung (Fix/Feature/Änderung/Balance) einen kurzen, für Spieler verständlichen Eintrag für das öffentliche Changelog-Panel auf der Website vorbereiten (siehe js/ui/bkmp-changelog.js, public.changelog_entries, sql/20260726-changelog.sql) — OHNE dass der Nutzer danach fragen muss.
  3. **Nutzerwunsch (26.07.2026, "ich will das nicht mehr selber ausführen [manuell im Admin-Panel eintragen]"):** fürs Changelog UND fürs Feedback-Board-Status (public.feedback_public/-_progress) IMMER eine fertig AUSGEFÜLLTE (echte Werte, kein Platzhalter) SQL-`insert`/`update`-Zeile proaktiv liefern statt auf die Admin-Panel-Formulare zu verweisen - der Nutzer tippt dort nichts mehr manuell ein, er kopiert nur noch die fertige Zeile in den Supabase SQL Editor und drückt Run. Wie bei jedem anderen SQL in diesem Projekt: NIE selbst ausführen, nur vorbereiten (kein DB-Schreibzugriff vorhanden).
- **Cache-Busting:** jede Änderung an `style.css`/`app.js`/`supabase.js`/`idledorf.js`/`js/**/*.js` braucht einen neuen `?v=...`-Query-String an allen Einbindungsstellen (5 HTML-Dateien: index.html, admin.html, idle-stream-mini.html, datenschutz.html, impressum.html — style.css/design-tokens.css laufen überall; die JS-Dateien nur wo tatsächlich eingebunden).
- **Diagnose:** bei Verdacht auf einen Live-Bug die echte Supabase-REST-API prüfen (`curl`), nicht nur aus der Sandbox raten — SQL-Migrationen in `/sql/` sind nicht automatisch alle live ausgeführt.
- **SQL-Migrationen:** liegen jetzt unter `/sql/` (bis 17.07. lose im Repo-Root). Neue Migrationen ab jetzt mit Datum benennen (`YYYYMMDD-beschreibung.sql`).
- **Commits/Deploys:** übernimmt der Projektbesitzer selbst — nicht proaktiv "soll ich committen?" fragen.
- **node_modules/package-lock.json** dürfen nie committet werden (siehe `.gitignore`) — die Seite hat keinen Build-Schritt.
