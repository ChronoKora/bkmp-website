# BKMP Changelog

Automatisch von Claude Code gepflegt: **nach jeder Code-/SQL-Änderung kommt hier sofort ein neuer Eintrag dazu** (unabhängig davon, ob committed/gepusht wird oder nicht) — neueste zuerst. Ausführliche Root-Cause-/Verifikationsdetails zu größeren Themen stehen weiterhin zusätzlich in `CLAUDE.md`; hier nur die kurze, auf einen Blick überfliegbare Übersicht.

**Format je Eintrag:** `[Kategorie] Kurzbeschreibung — betroffene Dateien — Status`
**Kategorien:** Fix · Änderung · Neu · SQL
**Status-Zeichen:**
- 🟡 lokal — liegt nur auf diesem Rechner, nicht committed
- 🔵 gepusht — committed + auf GitHub (`origin/main`), Vercel-Deploy-Stand von hier aus NICHT prüfbar
- 🟢 live — bestätigt aktiv (SQL live ausgeführt, oder Deploy explizit gegengeprüft)

---

## 2026-08-18

- **[Fix/Änderung]** Mitarbeiter-Lohn-Formel korrigiert: statt `(Einnahmen × 50%) − seine Ausgaben` (er trug seine eigenen Materialkosten effektiv zu 100% allein) jetzt `(Einnahmen − seine Ausgaben) × 50%` — "Gewinn-Teilung", Ausgaben schmälern zuerst den gemeinsamen Gewinn-Topf, Kosten werden hälftig geteilt, wie bei einer normalen Gewinnbeteiligung. Nutzerauftrag mit Vergleichsrechnung von außen, per Rückfrage bestätigt ("Gewinn-Teilung (empfohlen)"). Beispiel (echte Live-Zahlen vom Screenshot): vorher 2.280.500 €, jetzt korrekt 3.380.500 € bei 8.961.000 € Einnahmen / 2.200.000 € eigenen Ausgaben. Summary-Karten + Kategorie-Aufschlüsselung + Admin-Hilfetext entsprechend umbenannt/angepasst (Mitarbeiter-Hilfetext bleibt unverändert kurz, siehe unten). Live getestet mit den exakten Screenshot-Zahlen (inkl. Regressionsbeweis: Admin-Ausgabe zählt weiterhin nicht mit, 0-Ausgaben-Fall zeigt weiterhin sauber "0 €" statt "-0 €"). — `admin.html` (`renderEmployeePayoutPage()`) — 🟡 lokal
- **[Änderung]** Mitarbeiter-Lohn-Hilfetext ist jetzt rollenabhängig — ein echter Admin sieht weiterhin die ausführliche interne Formel-/Regel-Erklärung (50%, Kategorienliste, "Ausgaben die DU als Admin einträgst zählen NICHT mit"), ein eingeloggter Mitarbeiter (Rolle `expenses_editor`) sieht stattdessen einen kurzen, neutralen Satz ohne Formel-/internes Detail. Statischer HTML-Default ist als Sicherheitsnetz bereits die kurze Fassung. Nutzerauftrag: "Text anpassen.. weil Mitarbeiter soll das nicht lesen." — `admin.html` (`renderEmployeePayoutPage()`, `#employeePayoutHelpText`) — 🟡 lokal
- **[SQL] [Fix]** Supabase Security Advisor CRITICAL "Security Definer View" für `idle_player_state_leaderboard` behoben — der nötige RLS-Bypass (Lesen der Admin-only `idle_leaderboard_hidden_accounts`-Tabelle) ist jetzt auf eine enge `security definer`-Funktion `is_leaderboard_hidden()` verengt (gibt nur ein Ja/Nein zurück, gleiches Muster wie `is_active_admin()`), die View selbst läuft mit `security_invoker=true`. Geprüft: `idle_player_state` war schon vorher voll öffentlich lesbar (`using (true)`), kein akuter Datenverlust, aber die View hätte künftige RLS-Verschärfungen stillschweigend ignoriert. Kein Verhaltensunterschied für Spieler/Admin. — `sql/20260818-fix-leaderboard-view-security-definer.sql` — 🟡 lokal, noch nicht ausgeführt

## 2026-08-16

- **[Neu]** Ausgaben, die der Mitarbeiter (Rolle `expenses_editor`, z.B. DerJannikhase) selbst im Admin-Panel einträgt, werden jetzt automatisch von seinem Lohn abgezogen - Nutzerauftrag: "bis dato bleibe ich ja auf seinen Ausgaben sitzen... wenn ich Ausgaben tracke soll nichts passieren" (nur SEINE eigenen Einträge sollen zählen, nicht die des Admins). Neue Spalte `expenses.entered_by_auth_user_id`, automatisch und fälschungssicher per `default auth.uid()` gesetzt - der Client schickt dieses Feld nie selbst mit, Postgres füllt es serverseitig aus dem echten Login der eintragenden Person, kann also nicht manipuliert werden. Trägt der Admin eine Ausgabe ein, steht dort seine eigene ID (zählt nicht mit) - trägt der Mitarbeiter sie ein, seine (zählt mit) - exakt die verlangte Unterscheidung, ganz ohne neues Formularfeld. Mitarbeiter-Seite zeigt jetzt 4 Karten: Auszahlung (netto, die eigentlich wichtige Zahl), Gehalt brutto (50%), Eigene Ausgaben (Abzug), Einnahmen-Basis (100%) - plus eine neue Liste seiner einzelnen abgezogenen Ausgaben. Bereits bestehende (vor der Migration eingetragene) Ausgaben bleiben unangetastet (NULL in der neuen Spalte) und zählen bewusst nicht mit - kann rückwirkend niemandem zugeordnet werden. Verifiziert per gestubbten Testdaten (3 Ausgaben: 2 vom Mitarbeiter à 800€/400€, 1 vom Admin à 5000€) - korrekt nur 1.200€ abgezogen, Admin-Ausgabe UND eine unattributierte Altzeile korrekt ausgeschlossen, Netto-Auszahlung 5.000€ Brutto − 1.200€ = 3.800€ exakt richtig, Leerfall (0 Ausgaben) zeigt sauber "0 €" statt "-0 €" (extra Guard ergänzt, da `0 * -1` in JS `-0` ergibt). `node --check` sauber, keine neuen Konsolenfehler. Nichts committed/gepusht. — neu: `sql/20260816-expenses-editor-payroll-deduction.sql` (**noch nicht ausgeführt - wichtig: bis dahin schlägt das Laden der Ausgaben-Liste fehl, da die neue Spalte angefragt aber noch nicht vorhanden ist; bestehender Fallback zeigt dann nur den zuletzt lokal zwischengespeicherten Stand statt eines Absturzes**); geändert: `admin.html`, `supabase.js` — 🟡 lokal

---

## 2026-08-15 (2)

- **[Änderung]** Mitarbeiter-Lohn-Kategorien um Raketen und Elytra erweitert (jetzt 7 statt 5: Bücher, Werkzeug, Netherite, Schmiede Vorlagen, Tränke, Raketen, Elytra) - Nutzerauftrag ("bei JannickHase sein Lohn auch raketen noch hinzufügen und Elytra"). Reine Erweiterung der bestehenden `BKMP_EMPLOYEE_WAGE_CATEGORIES`-Liste + zwei Textstellen, die die Kategorien fest ausgeschrieben hatten - die Aufschlüsselungs-Tabelle iteriert bereits automatisch über die Liste, keine Änderung dort nötig. Verifiziert per isoliert gestubbten Testdaten (2.500€ Bücher + 1.000€ Raketen + 8.000€ Elytra → korrekt 11.500€ Basis → 5.750€ Gehalt, "DerJannikhase – Gehalt" weiterhin korrekt beschriftet). `node --check` sauber. Nichts committed/gepusht. — geändert: `admin.html` — 🟡 lokal

---

## 2026-08-15 (1)

- **[Änderung]** Fehlender Stückpreis für die Einnahmen-Kategorie "Werkzeug" ergänzt (75.000 €) - Nutzermeldung. War bisher eine der 4 Kategorien ohne festen Stückpreis (Betrag musste direkt eingetragen werden), jetzt rechnet das Formular wie bei Bücher/Netherite/etc. automatisch Anzahl × Stückpreis. Verifiziert: 3 × 75.000 € = 225.000 € wird korrekt ins Betragsfeld übernommen. Nichts committed/gepusht. — geändert: `admin.html` — 🟡 lokal

---

## 2026-08-14 (3)

- **[Änderung]** Mitarbeiter-Lohn-Seite (siehe Eintrag (2) unten) für den eingeschränkten "Ausgaben-Eintragung"-Zugang (`expenses_editor`, u.a. DerJannikhase) freigeschaltet + namentlich beschriftet - Nutzerwunsch: "Soll für den Zugang auch freigeschaltet werden... richtig DerJannikHase = Gehalt". Zweiter Nav-Button bekommt dasselbe `data-role="expenses_editor"`-Attribut wie der bestehende Ausgaben-Button (beide gemeinsam sichtbar für diese Rolle, kein neues CSS nötig - dieselbe Regel matcht mehrere Elemente). Zeigt jetzt "[Name] – Gehalt (50%)" statt einer anonymen Sammelbezeichnung - Namen kommen aus `admin_profiles` (gefiltert auf `role='expenses_editor'`), wiederverwendet dieselbe `loadAdminProfiles()`-Funktion wie die bestehende "Zugänge"-Seite: RLS liefert einem vollen Admin automatisch ALLE Zugänge, einem eingeschränkten Konto (wie DerJannikhase) wegen "Read own admin profile" strukturell nur die EIGENE Zeile - kein neues SQL nötig, bleibt bei mehr als einem Zugang trotzdem EIN gemeinsamer Topf (keine Änderung an der Rechenlogik), wird dann explizit als "gemeinsamer Lohn-Topf" gekennzeichnet statt fälschlich eine Aufteilung zu suggerieren. Verifiziert per injiziertem Mock (kein echter Login möglich): DerJannikhases Sicht zeigt jetzt 2 Nav-Buttons (Ausgaben+Mitarbeiter) statt 1, echter Klick auf "Mitarbeiter" zeigt korrekt "DERJANNIKHASE – GEHALT (50%) 6.750 €" (2 Test-Einnahmen, 13.500€ Basis); volle Admin-Sicht filtert korrekt nur `expenses_editor`-Zugänge (ein zusätzlicher Admin-Account bleibt außen vor); Leerfall (kein Mitarbeiter-Zugang vorhanden) zeigt sauberen Hinweistext statt Fehler. `node --check` sauber, keine neuen Konsolenfehler. Nichts committed/gepusht. — geändert: `admin.html` — 🟡 lokal

---

## 2026-08-14 (2)

- **[Neu]** Neue "Mitarbeiter"-Seite im Admin-Panel - zeigt automatisch 50% aller Einnahmen ab dem 15.08.2026 in den Kategorien Bücher/Werkzeug/Netherite/Schmiede Vorlagen/Tränke als Lohn-Summe. Nutzer-Rückfrage vorab geklärt: eine gemeinsame Summe für alle Mitarbeiter (nicht pro Person) - Einnahmen tragen aktuell kein Feld, wer sie erzielt hat, eine Zuordnung hätte ein zusätzliches Auswahlfeld beim Eintragen gebraucht, was der Nutzer ausdrücklich nicht wollte ("wenn ich Einnahmen tracke soll sich das direkt mit anpassen"). Reine Berechnung über die bereits geladenen Einnahme-Daten - keine neue Tabelle/Spalte, kein zusätzlicher Ladevorgang, kein SQL nötig. Zeigt Gesamtsumme (50%) + zugrunde liegende Einnahmen (100%) + Aufschlüsselung nach Kategorie. Verifiziert per injizierten Testdaten über die echte `renderEmployeePayoutPage()`-Funktion (kein echter Login möglich): korrekte Summenbildung (6 Testeinnahmen über alle 5 Kategorien → 24.000€ Basis → 12.000€ Lohn, exakt erwartungsgemäß), Einnahmen vor dem 15.08. sowie andere Kategorien korrekt ausgeschlossen, echter Klick auf den neuen Nav-Button funktioniert, `renderAll()` aktualisiert die Summe automatisch bei einer neuen Einnahme (Live-Beweis: +500€ Einnahme → Summe stieg korrekt von 12.000€ auf 12.250€). `node --check` sauber, keine neuen Konsolenfehler. Nichts committed/gepusht. — geändert: `admin.html` — 🟡 lokal

---

## 2026-08-14

- **[Neu]** Neue eingeschränkte Admin-Rolle "Ausgaben-Eintragung" (`expenses_editor`) - Nutzerwunsch: neuer Mitarbeiter soll im Admin-Panel nur Ausgaben eintragen können, sonst nichts. Bestehendes Muster wiederverwendet (identisch zum bereits vorhandenen "Schaf-Zitat-Editor"-Zugang) - `is_active_admin()` bleibt unverändert (nur `admin`/`editor`), die neue Rolle fällt dort automatisch durch und hat dadurch strukturell null Zugriff auf jede andere admin-only Tabelle (Investoren, Feedback, Anti-Cheat, Spielerverwaltung, ...), ohne dass irgendetwas davon einzeln angefasst werden musste. Neue `is_expenses_editor()`-Funktion + eigenständige INSERT-Policy auf `expenses` (kein Löschen/Bearbeiten - bewusst nur "eingeben", wie vom Nutzer explizit gewünscht; Lesen war schon öffentlich). Client: neue Option im Rollen-Dropdown der "Zugänge"-Seite, `showAdmin()` zeigt für diese Rolle nur die bestehende Ausgaben-Seite (Liste+Formular), gleiches CSS-Ausblendmuster wie beim Schaf-Editor. Verifiziert per injiziertem Mock-Profil über die echte `showAdmin()`-Funktion (kein echter Login möglich - Sicherheitsregel): nur 1 Nav-Button/1 aktive Seite bei der neuen Rolle, normale Admins weiterhin unverändert alle 23 Buttons, bestehender Schaf-Editor-Pfad unverändert funktionsfähig (kein Regressionsschaden). `node --check`/CSS-Klammerbilanz sauber. Nichts committed/gepusht. — neu: `sql/20260814-expenses-editor-role.sql` (**noch nicht ausgeführt**); geändert: `admin.html`, `style.css` (Cache-Busting `?v=20260814-expenseseditor1`, alle 5 HTML-Dateien) — 🟡 lokal

---

## 2026-08-13 — DRINGEND

- **[Fix]** Discord-DM "Moni" meldete "meine Drachen alle weg". Root Cause (per curl gegen die echte Produktions-DB bestätigt): sie hatte sich am 12.08. umbenannt - `idle_player_state` folgte korrekt zum neuen Namen (level 4275, gesund), `player_dragons`/`player_dragon_eggs`/`player_dragon_nests` (sql/supabase-dragon-breeding.sql, 17.07.) wurden dagegen alle drei am 25.07. bei der gleichnamigen Runen/Prestige/Dorf-Skins-Reparatur schlicht übersehen und nie aktualisiert - **exakt derselbe Bug, dritter unabhängiger Fall derselben Klasse**. Kein echter Datenverlust - ihre Zeilen stehen unverändert unter dem alten Namen. Fix: dieselben zwei zusätzlichen `update`-Anweisungen wie am 25.07., diesmal über `auth_user_id` gefiltert (robuster als der alte `name_key`-Vergleich, alle drei Tabellen führen die Spalte bereits) + Backfill für bereits betroffene Spieler (inkl. Moni). Neue Testdatei nach demselben REPRODUKTION/FIX-BEWEIS-Muster wie `rename-persistence.spec.js` (2 Tests) + Mock-Update (`rpc-engine.js`). 6/6 grün, 60/60 stabil bei 10x Wiederholung, keine Regression in angrenzenden Drachen-Suiten. Nichts committed/gepusht. — neu: `sql/20260813-fix-rename-dragon-tables-propagation.sql` (**noch nicht ausgeführt**), `tests/e2e/rename-dragon-persistence.spec.js`; geändert: `tests/mock/rpc-engine.js` — 🟡 lokal

---

## 2026-08-11 (2) — DRINGEND

- **[Fix] KRITISCH:** direkte Folge von Eintrag (1) unten - Spieler "OPShadowWolf" meldete per Screenshot, dass er nach dem (vermutlich erfolgten) Ausführen von `sql/20260811-anticheat-guard-absolute-ceiling.sql` **GAR NICHT MEHR speichern konnte** (wiederholte `PATCH .../idle_player_state`-Fehlschläge, HTTP 400, Postgres-Code `22P02` "malformed array literal: \"dragon_kills\""). Root Cause **nicht abschließend geklärt** (kein DB-Schreibzugriff, um die echte Spaltendefinition von `idle_anticheat_flags.triggered_by` direkt einzusehen - PostgRESTs OpenAPI-Schema ist für `anon` leer, die Tabelle selbst per RLS nicht lesbar; Arbeitshypothese: die Spalte war/ist entgegen der Absicht ein Array-Typ statt `text`, `array_to_string(...)` liefert einen unbeklammerten String, der beim impliziten Cast in eine Array-Spalte genau mit dieser Fehlermeldung scheitert - `alter table ... add column if not exists` hätte einen falschen Bestandstyp nie nachträglich korrigiert). **Unabhängig von der exakten Ursache:** strukturelles Sicherheitsnetz eingebaut - ein Fehler in der reinen Alarm-Protokollierung (INSERT in `idle_anticheat_flags`) darf nie wieder die eigentliche, bereits korrekt gekappte Spielstand-Speicherung blockieren können (lief vorher ungeschützt VOR dem Funktionsende, eine Exception dort riss die komplette UPDATE-Transaktion mit). Jetzt in ein eigenes `BEGIN/EXCEPTION`-Sub-Block (SQL) bzw. `try/catch` (JS-Mock) gepackt. Zusätzlich defensiv: `triggered_by`-Spaltentyp wird per `information_schema`-Check + sicherem `USING`-Cast explizit auf `text` erzwungen, falls er es nicht schon ist (schadlos, falls die Hypothese falsch ist). Neuer deterministischer Regressionstest (simuliert einen garantierten Protokollierungsfehler, beweist: Speicherung inkl. korrekter Kappung läuft trotzdem durch) - 21/21 grün, 210/210 stabil bei 10x Wiederholung. Rein lesende Diagnose-Datei vorbereitet, um die Ursache endgültig zu bestätigen. Nichts committed/gepusht. — neu: `sql/20260811-anticheat-guard-flag-insert-safety-net.sql` (**DRINGEND, noch nicht ausgeführt - ersetzt `sql/20260811-anticheat-guard-absolute-ceiling.sql` vollständig, nur diese neueste Datei muss noch laufen**), `sql/20260811-diagnose-triggered-by-column-type.sql` (rein lesend, optional); geändert: `tests/mock/rest-engine.js`, `tests/e2e/anticheat-progress-rate-guard.spec.js` — 🟡 lokal

---

## 2026-08-11 (1)

- **[Fix] KRITISCH:** eigene Regression aus der Anti-Cheat-Arbeit vom 09.08. - Nutzer-Meldung "einige nicht mehr in der Leaderboards angezeigt". Root Cause (per curl gegen die echte Produktions-DB bestätigt, nicht nur vermutet): `sql/20260809-leaderboard-hide-mechanism.sql`s View blendete bisher AUTOMATISCH jeden Account mit irgendeinem undismissed Anti-Cheat-Alarm aus - der viel zu enge Kampfwerte-Trigger vom 09.08. (relative 50x-pro-Speicherung-Grenze) feuerte auf ganz normale Spielmechaniken (Prestige-Reset, Gilden-Tech, Runen-Aufwertung, Auto-Kauf-Bursts) und hatte dadurch **33 von 217 Accounts** von der öffentlichen Bestenliste verschwinden lassen - praktisch nur lange dokumentierte, echte Spieler, darunter der eigene Account des Betreibers (ChronoKora) und OPShadowWolf (der die Videos geschickt hatte, die den Bug überhaupt erst aufdeckten). Kein Kampfkraft-Schaden für Spieler (`bkmpIdleRecomputeEffectiveStats()` berechnet Kampfwerte bei jedem Öffnen unabhängig neu, self-healt automatisch) - der einzige echte Schaden war die Bestenlisten-Unsichtbarkeit. **Zwei-teiliger Fix:** (1) Bestenlisten-View entkoppelt - blendet ab sofort NUR noch manuell (Admin-Panel-Klick) ausgeblendete Accounts aus, ein Anti-Cheat-Alarm ist jetzt reine Telemetrie ohne automatische Wirkung, entspricht dem ursprünglichen Nutzerwunsch ("nur im AdminPanel sehen"). (2) Kampfwerte-Prüfung von relativer 50x-Grenze auf absolute, vom Vorwert unabhängige Obergrenze umgestellt (kalibriert an den tatsächlich stärksten Accounts im Spiel, riesiger Sicherheitsabstand zum gemeldeten Exploit-Wert 420,45M). Admin-Panel-Texte entsprechend korrigiert (behauptete bisher fälschlich "ein Alarm blendet sofort aus"). Neue Testdatei `leaderboard-hide-mechanism.spec.js` (6 Tests, inkl. Mock-Nachbau der echten View in `rest-engine.js` - gab es vorher gar nicht) + Kampfwerte-Tests in `anticheat-progress-rate-guard.spec.js` umgeschrieben. 20/20 grün, 200/200 stabil bei 10x Wiederholung, breite 3-Projekte-Regression sauber (60 bestanden, 48 korrekt übersprungen). `eslint`/`node --check`/`static-checks.js` sauber. Nichts committed/gepusht. — neu: `sql/20260811-leaderboard-hide-decouple-from-flags.sql` (**dringend, noch nicht ausgeführt**), `sql/20260811-anticheat-guard-absolute-ceiling.sql` (**noch nicht ausgeführt**), `sql/20260811-anticheat-flags-cleanup-optional.sql` (optional), `tests/e2e/leaderboard-hide-mechanism.spec.js`; geändert: `tests/mock/anticheat-guard.js`, `tests/mock/rest-engine.js`, `tests/e2e/anticheat-progress-rate-guard.spec.js`, `supabase.js`, `admin.html` — 🟡 lokal

---

## 2026-08-07 (4)

- **[Fix]** "verkaufen (+623)"-Button (Runen-Detailkarte) ragte auf breiten Monitoren (Nutzer-Screenshot, 2560×1440) sichtbar über den rechten Rand der Karte hinaus in den Lager-Bereich. Root Cause: seit die Detailspalte im Runen-Redesign auf eine feste Breite (`min(340px,32vw)`) gedeckelt ist, war `.idle-runen-fuse-btn`/`.idle-runen-sell-btn` (Reihe "Verschmelzen"/"verkaufen") die einzige der drei Aktions-Button-Reihen ohne `min-width:0` (die beiden anderen Reihen hatten das schon) - `white-space:nowrap` + das fehlende Flex-Schrumpfen verhinderten, dass "✨ Verschmelzen (auswählen) (N/3)" sich der schmaleren Spalte anpasst, live gemessen 72px Überlauf. Fix: beide Buttons dürfen jetzt schrumpfen und ihr Text bei Bedarf auf eine zweite Zeile umbrechen (Reihe wird dadurch ca. 8px höher, bleibt aber immer innerhalb der Kartenbreite - kein Überlauf mehr, `sellBtnOverflowsColumn` fiel von +72px auf -18px). Live verifiziert bei 2560×1440 (kein Überlauf, kein Scrollbalken, "Verschmelzen"-Text bricht sauber auf 2 Zeilen um) und 375px Mobile (kein horizontales Überlaufen, unverändert). 16/16 grün in der bestehenden Runen-Regressionssuite, keine Konsolenfehler. Nichts committed/gepusht. — `style.css` — 🟡 lokal

---

## 2026-08-07 (3)

- **[Änderung]** Runen-Detailkarte kompakter - Nutzer-Screenshot zeigte pro Sub-Stat eine zusätzliche, permanent sichtbare "Spanne +X bis +Y"-Zeile (seit 19.07.2026), die unnötig viel Höhe kostete und im Runenfenster einen kleinen vertikalen Scrollbalken erzwang. Auf ausdrücklichen Wunsch entfernt - die Information bleibt vollständig erhalten, steht jetzt ausschließlich im bereits bestehenden `title`-Tooltip des Würfel-Buttons (Maus-Hover, unverändert). Da native `title`-Tooltips in den meisten Browsern bei reinem Tastatur-Fokus NICHT erscheinen (bekannte Einschränkung), zusätzlich ein kleiner, per Tastatur-Fokus erscheinender eigener Tooltip ergänzt (`bkmpUiTooltipHtml()`, Phase-3-Baustein, bereits an anderer Stelle etabliert) - bewusst NUR auf `focus`/`blur` verdrahtet (nicht die volle `bkmpUiWireTooltipTrigger()`-Komponente mit ihrem `touchstart`-Abfangen übernommen, das hätte auf Mobilgeräten den ersten Tap in "Tooltip zeigen" statt "sofort würfeln" verwandelt - echte Reroll-Logik/-Kosten/-Werte komplett unangetastet). Feinschliff: Abstände zwischen den drei gestapelten Aktions-Button-Reihen (Aufwerten/ggf. Aufsteigen/Verschmelzen+Verkaufen) leicht verdichtet (0.7rem→0.55rem), Sub-Stat-Liste knapper an die Hauptzeile herangerückt (0.4rem→0.3rem). Verifiziert (2560×1440, injizierter Worst-Case mit 4 Sub-Stats wie im Screenshot): kein Scrollbalken mehr, keine "Spanne"-Zeile mehr sichtbar, Tooltip-Logik per direktem `dispatchEvent` bewiesen korrekt (ein reines Werkzeug-Limit dieser Sitzung - `document.hasFocus()` ist im automatisierten Browser-Fenster `false`, echte `.focus()`-Aufrufe lösen dadurch keine `focus`-Events aus, `activeElement` aktualisiert sich aber korrekt), Reroll-Klick end-to-end mit realistischer String-Rune-ID bestätigt (Kristalle abgezogen, Wert geändert - ein anfänglicher Fehlschlag lag an einer einzelnen Fixture-Rune mit numerischer statt der in Produktion ausschließlich verwendeten String-`_cid`, kein echter Bug). 35/35 grün in der bestehenden Runen-/Speichern-Regressionssuite (chromium-desktop, der eine "x" ist der bereits lange bekannte, bewusst verfolgte Zwei-Tab-Race), 375px Mobile geprüft (kein horizontales Überlaufen, keine Regression). `eslint`/`node --check`/CSS-Klammerbilanz sauber. Nichts committed/gepusht. — `js/systems/bkmp-runes.js`, `style.css` — 🟡 lokal

---

## 2026-08-07 (2)

- **[SQL]** Öffentliche Changelog-Zeilen für die 5 spielerrelevanten Themen dieser Sitzung vorbereitet, die bisher noch keinen öffentlichen Eintrag hatten (HUD-Icon-Überlapp auf breiten Monitoren, Performance-Audit, Kraftrune-Lager auf breiten Monitoren unerreichbar, Drachenlager-Sync zwischen Geräten, Runen-Ansicht-Redesign - das Mobile-Redesign + seine 4 Begleit-Fixes vom selben Tag waren bereits in `sql/20260806-changelog-mobile-redesign.sql` erfasst). Enthält einen Hinweis, dass Punkt 4 zusätzlich `sql/20260806-dragon-storage-expansions-sync.sql` voraussetzt, falls die noch nicht gelaufen ist. — neu: `sql/20260807-changelog-hud-perf-lager-redesign.sql` (**noch nicht ausgeführt**) — 🟡 lokal

---

## 2026-08-07 - Nachtrag: Auto-Push-Eigenart erneut beobachtet

Beim Vorbereiten des Runen-Redesigns (Eintrag (5) unten) fiel per `git log`/`git status` auf: obwohl für JEDE der Aufgaben vom 06.08. (Changelog-SQL-Vorbereitung, HUD-Icon-Überlapp-Fix, Performance-Audit, Kraftrune-Lager-Positionsfix, Drachenlager-Sync-Fix) ausdrücklich "nichts committen/pushen" galt und zu keinem Zeitpunkt bewusst `git commit`/`git push` ausgeführt wurde, sind **3 Commits (`3d1a545`, `96861cd`, `3b6539a`, `a3bd79b`) bereits automatisch entstanden und auf `origin/main` gelandet** - `git status -sb` zeigt `main...origin/main` ohne jede Abweichung. Dieselbe, bereits mehrfach in `CLAUDE.md` dokumentierte Auto-Push-Eigenart dieser Umgebung (siehe `[[feedback_bkmp_user_handles_commits]]`-Memory) - kein bewusstes Zutun dieser oder einer vorherigen Sitzung, keine Datenbank-/SQL-Ausführung ausgelöst (die vorbereiteten `.sql`-Dateien selbst sind zwar jetzt im Repo auf GitHub, wurden aber nicht gegen die echte DB ausgeführt). Alle betroffenen Status-Zeichen oben wurden entsprechend von 🟡 auf 🔵 korrigiert. Nur die aktuelle Runen-Redesign-Arbeit (Eintrag (5) unten) ist zum Zeitpunkt dieses Nachtrags noch echt lokal/uncommitted.

---

## 2026-08-06 (5)

- **[Änderung]** UI-/Layout-Redesign der Runen-Ansicht auf Desktop (≥1000px) - Nutzerauftrag: das große, leere mittlere Feld im Runen-Tab (links Detail-Kreis+Statbox, rechts nur ein schmaler, `position:fixed` schwebender Lager-Balken am Bildschirmrand) sinnvoll nutzen, echtes Inventar-/Item-Management-Layout statt Leerraum. Ausdrücklich reines UI-Redesign, keine neue Spiellogik, keine Balance-/Kosten-/Werte-/Datenstruktur-/Save-/DB-Änderung - alle bestehenden Funktionen (Auswahl, Aufwerten/Aufstieg/Verschmelzen/Verkaufen, alle 4 Schnellaktionen, Slot-Tabs/Zähler, Seltenheiten/Farben) bleiben unverändert erhalten, nur ihre Position im DOM ändert sich. Backup zuerst: `backups-local/runes-page-redesign-<timestamp>/`. Umsetzung: linke Spalte (`idle-runen-detail-col`, fix `min(340px,32vw)`) bündelt Kreis+Statbox wie bisher; neuer `#idleRuneLagerSlot`-Zielcontainer im großen Hauptbereich; `#idleRuneDrawer` (der bereits bestehende, unveränderte Balken samt seiner kompletten Render-/Klick-Logik) wird ab 1000px per DOM-Reparenting ("Escape to Overlay"-Portal-Prinzip, gleiches Muster wie an anderer Stelle im Projekt bereits etabliert, nur umgekehrte Richtung) dort eingebettet statt schwebend zu bleiben (`bkmpRuneSyncDrawerEmbedMode()`) - unterhalb 1000px unverändert der alte schwebende Balken, Mobile/Tablet nicht angefasst. Drei echte Bugs beim eigenen Verifizieren gefunden+sofort behoben (nicht nur behauptet): (1) eigener Tippfehler beim Einbauen - ein erklärender HTML-Kommentar wurde mit `<!--` geöffnet, aber versehentlich mit JS-Syntax `*/` statt `-->` geschlossen, wodurch der Browser-HTML-Parser den neuen Ziel-Container UND das schließende `</div>` des Hauptbereichs selbst als Kommentartext verschluckte (per `childNodes`-Inspektion + `.toString()`-Quellcode-Vergleich zweifelsfrei bewiesen, nicht geraten) - behoben, Kommentar entfernt bzw. korrekt geschlossen. (2) `bkmpIdleRenderRunenPanel()` baut den Hauptbereich bei JEDEM Aufruf (u.a. bei jedem Kampf-Tick) per `innerHTML` komplett neu - ein bereits eingebetteter Balken wäre dadurch als Nachfahre des Panels bei jedem Re-Render unwiderruflich zerstört worden (exakt dasselbe Bugmuster wie beim Dorf-Skins-Vorschau-Flackern vom 04.08.2026) - per echtem Node-Identitäts-Test bewiesen (Marker überlebt 3 aufeinanderfolgende Re-Renders nicht) und behoben: der echte Balken-Knoten wird jetzt vor jedem `innerHTML`-Rebuild aus dem Panel herausgelöst und danach in den frisch gebauten Container zurückgehängt (`bkmpRunePreserveEmbeddedDrawer()`/`bkmpRuneReattachPreservedDrawer()`) - derselbe Knoten, kein Klonen, alle Listener/Inhalt/Scroll-Position bleiben erhalten. (3) zwei Regressionstests der bestehenden Playwright-Suite (`runes.spec.js`) klickten unbedingt den jetzt im eingebetteten Zustand per Design ausgeblendeten Zuklapp-Pfeil (`#idleRuneDrawerToggle`, war früher nötig um eine Überlappung mit dem Einsetzen-Button zu umgehen - im eingebetteten Zustand strukturell nicht mehr möglich, eigene Spalte statt schwebender Balken) - Tests auf einen neuen `bkmpTestToggleDrawerIfFloating()`-Helfer umgestellt, der den Pfeil nur klickt, wenn er tatsächlich sichtbar ist (schwebender Modus). Live verifiziert: Node-Identität übersteht 3 Re-Renders inkl. echtem Kampf-Tick-Muster, kompletter Fenster-Schließen/Wiederöffnen-Zyklus behält den eingebetteten Zustand (bekanntes, in diesem Projekt mehrfach dokumentiertes Bugmuster - hier sauber), alle 6 Slot-Tabs durchgeklickt ohne Konsolenfehler, Breitenübergänge 1920→900→1920px korrekt in beide Richtungen (Balken embedded⇄floating, Klassen/Parent-Element jeweils korrekt), 768px/375px (Tablet/Mobile) unverändert im alten schwebenden Modus, kein horizontales Überlaufen. Ein beim Testen beobachtetes "Klick auf eine andere Rune wechselt die Detail-Ansicht nicht" wurde geprüft und als **vorbestehendes, vom Redesign unabhängiges Verhalten** identifiziert (reproduziert identisch im unveränderten alten 900px-Floating-Modus, komplett unabhängig von jeder CSS-/DOM-Änderung dieser Session) - bewusst nicht angefasst (außerhalb des beauftragten reinen Layout-Scopes, keine Spiellogik-Änderung erlaubt). Playwright-Regression: `runes.spec.js`+5 weitere Runen-Testdateien+`save-load.spec.js` (38 Tests, chromium-desktop) grün bis auf den einen bereits lange bekannten, bewusst per `test.fail()` verfolgten Zwei-Tab-Speicher-Race (keine neue Regression); `runes.spec.js` korrekt 12/12 übersprungen auf beiden Mobil-Projekten; `mobile-smoke.spec.js` 12/12 grün. `node --check`/CSS-Klammerbilanz sauber. Nichts committed/gepusht. — `js/systems/bkmp-runes.js`, `style.css`, `tests/e2e/runes.spec.js` — 🟡 lokal

---

## 2026-08-06 (4)

- **[Fix]** Drachenlager-Plätze zwischen Geräten nicht synchron - Spieler-Meldung "Kaledoss": 27 Plätze auf dem Handy, 67 am PC. Root Cause: `bkmpDragonStorageExpansionsBought()`/`bkmpDragonExpandStorage()` lasen/schrieben die Anzahl gekaufter Lager-Erweiterungen ausschließlich über `localStorage.getItem/setItem('bkmp-dragon-storage-expansions')` - obwohl das echter, mit Gold bezahlter Fortschritt ist (das dafür ausgegebene Gold selbst war schon immer korrekt synchronisiert, nur der Erweiterungs-Zähler nicht), nie Teil von `idle_player_state`, nie auf dem Server gespeichert - jedes Gerät hatte dadurch einen komplett unabhängigen Stand. Nutzerfrage "warum haben wir eigentlich alles in LocalStorage?" - Prüfung ergab: nicht "alles", ~50 Schlüssel im Idle-Dorf-Code, die meisten davon zurecht rein lokal (Anzeige-Präferenzen, Session-Identität, reine UI-Zustände) - der Kern-Spielfortschritt läuft bereits korrekt über Supabase. Nur eine kleine Gruppe (angefangen bei diesem Fall) wurde beim Bauen fälschlich wie eine reine Einstellung behandelt, obwohl es echter Fortschritt ist. Ein zweiter, oberflächlich ähnlich aussehender Verdachtsfall (`bkmpDungeonMarkAchievement()`, ebenfalls localStorage-only) wurde geprüft und als **kein Bug** verworfen - das gesetzte Flag wird im gesamten Code nirgends je wieder ausgelesen, hat also aktuell gar keine sichtbare Wirkung (totes Codefragment, absichtlich unangetastet gelassen). Fix: neue, echte Server-Spalte `dragon_storage_expansions_bought` auf `idle_player_state` (gleiches additives Muster wie `obstgarten_level`/`jagdhuette_level`), liest/schreibt jetzt wie jedes andere Spielfortschritts-Feld über die bestehende Speicher-Pipeline. Bereits vorhandene lokale Werte werden beim nächsten Laden auf JEDEM Gerät automatisch übernommen statt verworfen (`bkmpDragonMigrateStorageExpansionsFromLocalStorage()` - übernimmt immer den GRÖSSEREN der beiden Werte, kein Gerät verliert dadurch bereits gekauften Fortschritt, unabhängig von der Reihenfolge, in der der Spieler seine Geräte als nächstes öffnet). Live verifiziert (3 Szenarien): lokaler Wert höher als Server → wird übernommen + Sync ausgelöst; Server-Wert bereits höher als ein veralteter lokaler Rest → korrekt NICHT heruntergestuft; neuer Kauf erhöht Kapazität korrekt (20→25 bei der ersten Erweiterung) ohne jeden weiteren localStorage-Zugriff. Keine Konsolenfehler. — `supabase.js`, `js/systems/bkmp-breeding.js`, neu: `sql/20260806-dragon-storage-expansions-sync.sql` (**noch nicht ausgeführt**) — 🔵 gepusht (Commit `a3bd79b`, siehe Nachtrag unten - Auto-Push-Eigenart)

---

## 2026-08-06 (3)

- **[Fix]** Kraftrune-Lager-Balken (Runen-Tab) war auf breiten Desktop-Monitoren komplett unerreichbar - Nutzer-Screenshot (2560×1440) zeigte ihn nur noch als schmalen Rest unten rechts. Live nachgestellt+vermessen: Balken lag bei `top:1193px` mit 640px Höhe (endet bei 1833px) in einem nur 1440px hohen Fenster - 393px hingen unsichtbar unterhalb des Viewports. Root Cause: `bkmpRuneSyncDrawerPosition()`s "immer unterhalb der Tab-Leiste verankern"-Logik stammt vom 21.07. (damals eine echte horizontale Tab-Zeile oben in der Karte) und wurde beim zweispaltigen Desktop-Grid-Redesign (05.08.) nie angepasst - `#idleDorfTabs` ist dort jetzt eine echte LINKE SEITENLEISTE (814px hoch statt einer schmalen Kopfzeile), wodurch `tabsRect.bottom` fälschlich einen sehr hohen Wert lieferte und der eigentlich schützende Sicherheits-Clamp (`Math.max(minTop, ...)`) das nicht mehr korrigieren konnte. Fix: die "unterhalb der Tabs"-Regel gilt jetzt nur noch, wenn Tab-Leiste und Balken sich bei der aktuellen Breite überhaupt horizontal überlappen könnten (reine Geometrie-Prüfung, kein fester Breakpoint - erkennt automatisch sowohl die alte obere Tab-Zeile als auch die neue linke Seitenleiste korrekt); ohne Überlappung wird der Balken stattdessen wieder vertikal im Fenster zentriert (entspricht der ursprünglichen CSS-Grundregel `top:50%`), zusätzlich innerhalb des Viewports gehalten. Live verifiziert bei 2560×1440 (jetzt `top:400/bottom:1040`, vollständig sichtbar, `elementFromPoint`-Hittest auf "Auto-Schmelzen"-Button bestätigt echte Klickbarkeit), 1366×768 (weiterhin korrekt zentriert) und 390×844 Mobile (unverändert korrekt, Tab-Leiste dort ohnehin `display:none`). Keine Konsolenfehler, keine Spiellogik/-werte geändert. — `js/systems/bkmp-runes.js` — 🔵 gepusht (Commit `3b6539a`, siehe Nachtrag unten - Auto-Push-Eigenart)

---

## 2026-08-06 (2)

- **[Neu]** Vollständiger, messbarer Performance-Audit des Idle-Dorf-Kampffensters (Nutzer-Messung: 22-61% CPU, 20-48 Layouts/s, 70-92 Style-Recalcs/s, 288-312ms INP beim Offline-Karte-Schließen, trotz "Effekte: Aus"). Neue QA-only Instrumentierung (`js/dev/bkmp-perf-instrument.js`, no-op außerhalb `window.BKMP_QA_MODE`) misst DOM-Nodes/Listener-Churn/aktive Timer/Observer/Long-Tasks/Speicher live im Browser statt zu raten. 4 parallele Code-Audits (Tab-Aufbau/Timer-Observer-Inventar/Render-Frequenz/Listener-Muster) fanden den mit Abstand größten Verursacher: `bkmpIdleAutoBuyUpgrades()` löste bei aktivem Auto-Kauf JEDEN Kampf-Tick bis zu 50+ (mit Prestige-/Gilden-Bonus mehr) komplette Upgrades-Panel- UND HUD-innerHTML-Neuaufbauten aus - live bewiesen: 50 Käufe in einem Tick = vorher 50 Panel- + 50 HUD-Rebuilds, jetzt 0 Panel-Rebuilds (Upgrades-Tab meist nicht sichtbar) + 1 gebündelter HUD-Rebuild, Ressourcen/Stufen/Sync unverändert bei jedem einzelnen Kauf. Zusätzlich: 5 Stellen mit erzwungenem synchronem Reflow (`void el.offsetWidth` beim Neustarten von Hit-Flash/Sprite-Angriff/Drachen-Wechsel-Animationen, bis zu mehrfach pro Tick) auf einen rAF-basierten Neustart ohne Forced Reflow umgestellt (neue geteilte `bkmpIdleRestartAnimClass()`); Offline-Karte-Schließen-Klick auf reine Klassenumschaltung reduziert, der eigentliche `display:none`-Reflow läuft jetzt außerhalb des vom Klick gemessenen Zeitfensters (per CSS-Transition); App-Modus-MutationObserver (beobachtete bisher den kompletten `document.body` ungedrosselt) auf rAF-gebündelte Prüfung umgestellt. Keine Spielbalance/-werte/Datenbank/Assets geändert, keine Funktion entfernt (Commit/Push zum Zeitpunkt der Arbeit nicht beabsichtigt, siehe Nachtrag unten zur Auto-Push-Eigenart). Live verifiziert: 5x Öffnen/Schließen ohne kumulativen DOM-/Timer-Anstieg (3411→3412 Nodes, 11 aktive Intervalle konstant), 0 neue Konsolenfehler (frischer Tab), Desktop 2560×1440/1920×1080 + Mobile 390×844 funktional geprüft. Ehrlich dokumentierte Grenzen: Chrome-interne "Layouts/Style-Recalcs pro Sekunde"-Zähler sind aus Seiten-JS grundsätzlich nicht auslesbar (kein Chrome-API dafür) - stattdessen Long-Task-Zeit/-Anzahl als echter, standardisierter Proxy gemessen; die exakten 288-312ms INP der Meldung ließen sich im leichten QA-Testkonto nicht reproduzieren (Event-Timing-API liefert für synthetische `.click()`-Aufrufe keine Einträge) - der Fix wurde stattdessen strukturell bewiesen (Klick-Handler jetzt nachweislich nur noch ein Klassenwechsel). Backup: `backups-local/idle-performance-audit-20260806-232650/` inkl. RESTORE.txt. — `idledorf.js`, `js/ui/bkmp-hud.js`, `js/core/bkmp-app-mode-bootstrap.js`, `style.css`, neu: `js/dev/bkmp-perf-instrument.js` — 🔵 gepusht (Commit `96861cd`, siehe Nachtrag unten - Auto-Push-Eigenart)

---

## 2026-08-06

- **[Neu]** Vollständiges Mobile-First-Redesign des Idle-Dorf-Kampffensters (< 1000px) nach 23-Abschnitte-Auftrag - fester Ausgangspunkt: "Die Handysicht darf nicht länger nur eine verkleinerte bzw. zusammengedrückte Desktopansicht sein". Screenshot zeigte: riesige runde Niederlagen-Blase + doppelte Niederlagen-Anzeige gleichzeitig, abgeschnittenen Gegnernamen ("Schwa…"), dominanten AD-Free-Knopf, überdimensionierten Kampf-Log-Block, überfüllte 7er-Bottom-Nav. Lokales Backup zuerst angelegt (`backups-local/mobile-idle-redesign-20260806-170450/`, per `.git/info/exclude`, nie committed). Umsetzung: (1) Kopfzeile in klare Reihen gruppiert (Level/Icon-Buttons/XP/Ressourcen als eigene Flex-Zeilen statt wild verteilt), (2) Stufenleiste bricht jetzt sauber um statt sich zu quetschen, (3) Niederlagen-Meldung auf Mobile kompakt+einzeilig statt riesiger runder Blase + der parallel gefeuerte große Toast dort unterdrückt (Desktop komplett unverändert - eigener `bkmpIdleDefeatIsCompactMobile`-Zweig in `bkmpIdleHandleDefeat()`, prüft `bkmpProtoChudWantCompact()`), (4) Gegnername bricht jetzt in eine eigene volle Zeile um statt abgeschnitten zu werden (bewusst OHNE `order:-1` - eine erste Fassung kollidierte damit sichtbar mit den fliegenden Schadenszahlen), (5) AD-Free-Button deutlich kleiner/unauffälliger, (6) Kampf-Log-Umschalter kompakter + Chevron-Indikator, (7) Footer-Links kompakter, "Für Streamer"-Link auf Mobile ausgeblendet (Nutzerwunsch: "eher eine PC Version", Desktop unverändert), (8) Bottom-Nav von 6 Haupttabs+Mehr (überfüllt, abgeschnittene Labels) auf 4 Haupttabs (Kampf/Upgrades/Drachen/Prestige) + Mehr-Sheet (Skilltree+Dungeon dorthin verschoben) reduziert - dabei einen 50/50-Breiten-Bug gefunden+gefixt (der einzelne Mehr-Button nahm bisher die HÄLFTE der gesamten Nav-Zeile ein, `flex:4 1 0` vs. `flex:1 1 0` jetzt korrekt proportional). **Kritischer, projektweiter Fund:** drei inkonsistente Breakpoints (640px/760px/768px) gegen den einzigen echten Desktop-Kartenlayout-Breakpoint (1000px) erzeugten ein "Niemandsland" zwischen ~641-999px, in dem weder die kompakte Mobil-Nav noch das echte Desktop-Layout korrekt griff - alle relevanten Media-Queries auf `999px`/`1000px` vereinheitlicht (inkl. `bkmpProtoChudWantCompact()`/`bkmpIdleWantCompactTabNav()` in den beiden Nav-JS-Dateien). Battlefield-Mindesthöhe von 54dvh auf `clamp(340px,44dvh,620px)` reduziert (weniger Leerraum). CSS-Block bewusst am tatsächlichen Dateiende von style.css gesammelt (Cascade-Order-Fallstrick: eine erste Mitten-im-File-Platzierung verlor gegen später stehende, gleich-spezifische Altregeln - per `sed`-Extraktion+Neuanfügung am echten Dateiende korrigiert). Verifiziert an 9+ Zielauflösungen (360×800/390×844/430×932/844×390 Querformat/999px-Grenze exakt/1000px-Grenze exakt/1920×1080 Desktop-Regression) - keine Desktop-Regression (exakter Grenzwerttest 999px vs. 1000px bestätigt sauberen Umschlag). — `style.css`, `idledorf.js`, `js/prototype/bkmp-proto-compact-hud.js`, `js/core/bkmp-app-mode-bootstrap.js` — 🔵 gepusht (Commit `96861cd`, siehe Nachtrag unten - Auto-Push-Eigenart)
- **[SQL]** Öffentliche Changelog-Zeilen für das Mobile-Redesign + die 3 begleitenden Fixes (siehe unten) vorbereitet — `sql/20260806-changelog-mobile-redesign.sql` (Datei selbst **gepusht**, SQL **noch nicht gegen die DB ausgeführt** — 🔵 gepusht (Commit `3d1a545`, siehe Nachtrag unten - Auto-Push-Eigenart))
- **[Fix]** Feedback-Button (💬) und Schließen-Button (✕) oben rechts im Idle-Dorf-Fenster überlappten sich (Nutzer-Screenshot). `.idle-dorf-feedback-btn{right:2.9rem}` lag nur 2px neben `.idle-dorf-close{right:0.9rem}` vom tatsächlichen Kollisionspunkt entfernt (Lücke 32px, Buttonbreite 34px). Fix: `right:3.5rem` - per `getBoundingClientRect()` sauberer 8px-Abstand bestätigt. — `style.css` — 🔵 gepusht (Commit `96861cd`, siehe Nachtrag unten - Auto-Push-Eigenart)
- **[Fix]** Idle-Dorf-Fenster ließ den Hintergrund (`<html>`) weiterhin mitscrollen, obwohl das Vollbild-Overlay geöffnet war (Nutzer-Screenshot: sichtbarer dünner Scrollbalken "für gefühlt nichts"). Der bestehende `body.modal-open{overflow:hidden}`-Schutz (bereits an 20+ Stellen im Projekt verwendet) war nur je auf `<body>` gesetzt, nie auf `<html>` selbst - das behält durch `html,body{height:100%}` aber sein eigenes, unabhängiges Scroll-Verhalten. Per direktem `documentElement.style.overflow`-Test bewiesen (`scrollHeight` fiel von 2512px auf 844px nach manuellem Setzen). Fix: `html:has(body.modal-open){overflow:hidden}` - reine CSS-Lösung ohne JS-Änderung (kein einziger der 20+ `modal-open`-Aufrufer musste angefasst werden, erster Einsatz von `:has()` in diesem Projekt), bidirektional verifiziert (sperrt beim Öffnen, entsperrt beim Schließen). — `style.css` — 🔵 gepusht (Commit `96861cd`, siehe Nachtrag unten - Auto-Push-Eigenart)
- **[Fix]** Nachbesserung am selben Tag (zweiter Scrollbalken-Screenshot, diesmal am eigentlichen Karten-Container statt am `<html>`-Element): `.idle-dorf-card` (der echte `overflow-y:auto`-Scroll-Container) hatte 36px mehr Inhalt als sichtbare Höhe, obwohl nichts abgeschnitten wurde. Ursache: `padding-bottom:calc(6rem + env(safe-area-inset-bottom))` (96px) war für eine ÄLTERE, höhere Kompakt-Navigation bemessen - die aktuelle `#bkmpProtoCompactNav` ist nur noch 66px hoch. Fix: auf `calc(4.5rem + ...)` (72px) reduziert, Restüberlappung fiel von 36px auf 12px. Ausdrücklich gegen das ursprüngliche Problem gegengeprüft (Inhalt darf nie hinter der fixen Nav verschwinden) - am längsten bestehenden Panel ("Erfolge", 13227px Inhalt) bestätigt, dass dessen letztes Element auch bei maximalem Scroll sicher oberhalb der Nav bleibt. Die verbleibenden ~12px sind ein bewusster, kleinstmöglicher sicherer Puffer (ein fixer Wert muss für das längste mögliche Panel reichen, nicht nur für kurze wie Skilltree), keine offene Baustelle mehr. — `style.css` — 🔵 gepusht (Commit `96861cd`, siehe Nachtrag unten - Auto-Push-Eigenart)
- **[Fix]** (nur lokale Testumgebung, keine Live-Auswirkung) Zwei beim eigenen Live-Testen der Mobile-Ansicht entdeckte Symptome als reine Lücken in `tests/fixtures/reference-data.js` bestätigt, KEIN echter Produktionsbug (Live-DB per SQL-Schema-Abgleich geprüft - echte Daten sind vollständig): (1) Drachen-Sprite/Animation unsichtbar ("Gibt es einen Grund warum ich die Animationen vom Drachen nicht sehe?") - `IDLE_DRAGONS`-Fixture fehlte `sprite_key` an allen 7 Einträgen, `bkmpIdleApplyDragonSprite()` fand dadurch weder Video noch PNG-Klasse. (2) Skilltree zeigte nur 2 von 5 Zweigen ("Bei Skills werden mir nur 2 anzeigt") - `IDLE_SKILL_NODES`-Fixture hatte nur 8 minimale Einträge statt der echten 26 (drei Zweige komplett fehlend, restliche ohne Anzeige-Felder wie `name`/`icon`). Beide Fixture-Dateien additiv ergänzt (bestehende 8 Skill-Node-IDs unverändert, da `tests/fixtures/teststands.js` bereits Zuteilungen dagegen vorseedet) - Werte 1:1 aus `sql/supabase-idle-dorf-schema.sql` übernommen. QA-Mock-Server-Neustart nötig (Node-Modul-Caching, kein Hot-Reload für Fixture-Daten). Eine dabei gesehene 401-Fehlerflut stellte sich als reines Altlasten-Artefakt eines vor dem Neustart bereits offenen Browser-Tabs heraus (neuer Tab: 0 Konsolenfehler), kein dritter Bug. — `tests/fixtures/reference-data.js` — 🔵 gepusht (Commit `3d1a545`, siehe Nachtrag unten - Auto-Push-Eigenart)
- **[Fix]** Nutzer-Screenshot zeigte FX-Icon (🚫)/Feedback (💬)/Schließen (✕) dicht gedrängt/teilweise überlappend oben rechts, dazu ein abgeschnittenes Textfragment ("...e:", vermutlich der Rest von "Effekte:"). Ausgiebig live im Browser über 320-1200px + beide FX-Zustände nachgestellt - die exakte Konstellation aus dem Screenshot ließ sich damit nicht reproduzieren (in jedem getesteten Zustand nach vollständigem Laden blieb ein sauberer Abstand). Dabei aber einen echten, konkreten Rest-Bug aus dem heutigen Breakpoint-Abgleich gefunden und behoben: `bkmpIdleRenderHud()` (`js/ui/bkmp-hud.js:568`) hing beim heutigen Vereinheitlichen aller Breakpoints auf 999px noch am alten `matchMedia('max-width:760px')` fest, während `bkmpProtoChudWantCompact()`/`bkmpIdleWantCompactTabNav()` (die beiden anderen, heute bereits korrigierten Nav-Systeme) schon auf 999px liefen - im dadurch entstandenen 761-999px-Niemandsland (z. B. ein Tablet im Hochformat/ein breiteres Handy quer) hielt sich diese eine Funktion für "Desktop" und portalisierte `#idleFxModeBtn` (Original-Textknopf "🚫 Effekte: Aus") in `.idle-hud-top`, während die anderen beiden Systeme im selben Breitenbereich bereits von "kompakt" ausgingen - je nach Aufrufreihenfolge ein plausibler Kandidat für genau dieses Symptom. Fix: identischer 999px-Schwellwert. Live an 390px/850px (die vorher betroffene Lücke)/1200px verifiziert (frischer Seitenaufruf + `window.dispatchEvent(resize)`) - überall korrekt, keine Desktop-Regression, keine Konsolenfehler. Der Nutzer bekommt eine Rückmeldung, falls das exakte Symptom nach diesem Fix weiter auftritt (dann mit Geräte-/Breiteninfo genauer eingrenzbar). — `js/ui/bkmp-hud.js` — 🟡 lokal
- **[Fix]** Nutzer meldete direkt danach: "Weiterhin sind sie überlappt. auf dem 2560x Monitor" (neuer Screenshot, identisches Symptom) - entscheidender Zusatzhinweis: **echter Desktop, nicht mobil**, macht die vorherige Breakpoint-Theorie hinfällig. Bei 2560px live nachgestellt (FX-Modus wieder auf "Aus" gesetzt, um exakt das 🚫-Icon aus dem Screenshot zu treffen) - diesmal **exakt reproduziert**: `#idleFxModeBtn` (Text "🚫 Effekte: Aus", per Portal in `.idle-hud-top` gehängt, siehe `bkmpIdleRenderHud()`) maß `[2133,2243]` x-Achse, `#idleDorfFeedbackBtn` `[2163,2197]` und `#idleDorfCloseX` `[2205,2239]` - der FX-Button umschloss BEIDE anderen Buttons vollständig, weshalb im Screenshot nur Fragmente ("🚫" links, "...e:" mittig) zwischen ihnen sichtbar blieben. Root Cause: seit dem zweispaltigen Desktop-Grid-Redesign (05.08.2026) spannt `.idle-hud-top` (`grid-area:hud` = "hud hud", volle Kartenbreite über beide Spalten) die GESAMTE Kartenbreite auf - der hineinportalisierte FX-Button landet dadurch am rechten Rand genau dort, wo Feedback/Schließen bereits `position:absolute` liegen; dieselbe Kollisionsklasse wie der bereits am 18.07. für `.bkmp-proto-chud-row` gelöste Fall, hier aber nie auf die Desktop-Variante übertragen. Fix: identisches, bewährtes Muster - `padding-right:5.9rem` auf `.idle-hud-top` (reserviert exakt den von Feedback+Schließen belegten Bereich), die flexible `.idle-hud-xp-wrap` gibt dafür nach. **Beim eigenen Verifizieren einen zweiten, direkt anschließenden Bug gefunden+gefixt:** bei 1024×768 überlappte es TROTZ des Fixes erneut - `@media(max-height:780px){ html.bkmp-app-mode .idle-hud-top{padding:.3rem .5rem !important} }` (Phase 7.1 Stufe 3, 21.07., ein von der Fensterhöhe statt -breite abhängiger Kompakt-Modus) stutzte die neue `padding-right`-Reservierung per `!important` auf 0.5rem zurück, sobald die Fensterhöhe ≤780px war (unabhängig von der Breite) - eigenes `!important` ergänzt, um das zu überstimmen. Verifiziert an 1000×700/1024×768/1440×900/1920×1080/2560×1440 (inkl. der beiden zuvor betroffenen Fälle) - überall sauberer Abstand (≥5px), keine Konsolenfehler. — `style.css` — 🟡 lokal

---

## 2026-08-05

- **[Fix]** "Gesamtbonus"-Block (Begleiter-Leiste) zeigte nur Angriff/Verteidigung/Leben, ignorierte alle Substat-Prozentboni (Drachen-EP/Goldbonus/Krit-Schaden/Fleischproduktion etc.) - Nutzer-Screenshot-Vergleich mit der Drachen-Detailkarte, die diese Boni bereits korrekt zeigte: "Da fehlen aber noch so paar Bonuse." `bkmpIdleDragonCompanionEffectTotals()` berechnete 9 von 12 möglichen Substat-Typen bereits korrekt gewichtet, wurden aber nie gerendert; die restlichen 3 (Frucht-/Fleisch-/Drachen-EP-Bonus) waren dort absichtlich ausgeschlossen (eigener, bereits korrekter Zugriffspfad `bkmpDragonSubstatBonus()`) und dadurch über den Stats-Block komplett unerreichbar. `bkmpDragonRenderCompanionStatsBlockHtml()` iteriert jetzt über alle 12 möglichen Substat-Typen (aus beiden Quellen), zeigt nur tatsächlich positive Werte, neue Icon-Zuordnung orientiert an bereits etablierten Emoji-Konventionen (💰/💎 wie `bkmpIdleResourceEmoji()`, 🍎/🥩/🐲 wie andernorts bereits verwendet). Live mit den exakten Werten aus dem Screenshot verifiziert, neuer Regressionstest + 35 Tests breiterer Regression unverändert grün. — `js/systems/bkmp-breeding.js`, `tests/e2e/dragon-multi-companion.spec.js` — 🟡 lokal

- **[Fix]** Prestige-Baum-Knotenkarte ("Weitere Gefährten"): Nutzer-Screenshot "Sieht schrecklich aus" - zwei echte, unabhängige Bugs gefunden (nicht nur Geschmackssache). (1) Icon+Titel standen als flache Flex-Kinder direkt in der Karte statt im seit Phase 5.4 etablierten `.idle-upgrade-card-head`/`-card-title`-Wrapper (den der normale Upgrades-Tab bereits korrekt nutzt) - Icon rutschte dadurch als eigene grosse Zeile über den Titel statt daneben zu stehen. (2) `.idle-upgrade-desc` (Beschreibungstext) hatte in `style.css` überhaupt keine eigene Regel - reiner unstyled Browser-Default-Text (schwarz, kein overflow-wrap). Kartenmarkup auf dasselbe Muster wie `bkmpIdleUpgradeCardHtml()` (idledorf.js) umgestellt, `.idle-upgrade-desc`-Regel ergänzt. Vorher/Nachher-Screenshot am exakt gemeldeten Knoten bestätigt, neuer Regressionstest + 56 Tests der breiteren Prestige-Suite unverändert grün. — `js/systems/bkmp-prestige.js`, `style.css`, `tests/e2e/prestige-tree-v2.spec.js` — 🟡 lokal

- **[Änderung]** Drachenlager-Filter: 3 `<select>`-Dropdowns ("Alle Seltenheiten"/"Alle Stufen"/"Sortieren: Seltenheit") durch 3 einfache Umschalt-Knöpfe ersetzt (Nutzer-Screenshot: "So bitte diese Sortierung entfernen. Einfach nur Buttons mit 'Legendäre' 'Epische' 'Seltene' Das reicht schön nebeneinander neben favoriten"). `bkmpDragonLagerFilter` von `{rarity,stage,favoritesOnly,sort}` auf `{rarity,favoritesOnly}` verschlankt - `stage`/`sort` waren nur über die entfernten Dropdowns änderbar, wurden konsequent mitentfernt (inkl. totem `dragonStrength`-Helfer/Sort-`switch`) statt als unerreichbarer Code stehenzubleiben; Sortierung ist jetzt fest auf Seltenheit (war ohnehin bereits Standard). Klick auf den bereits aktiven Rarity-Knopf setzt zurück auf "alle" (kein separater "Alle"-Knopf nötig). Neue `.idle-dragon-rarity-filter-btn`-Pille (gleiches Chip-Muster wie `.fbb-filter-chip`, aktiver Zustand färbt sich in der jeweiligen Rarity-Farbe) - `!important` auf Radius/Padding/Mindesthöhe nötig, da das generische `html.bkmp-app-mode .idle-dorf-panel button`-Reset sonst höhere Spezifität hätte (bereits mehrfach dokumentiertes Muster in diesem Projekt). Live im Browser verifiziert (Knopf-Inventar, Toggle-Verhalten, Pillenform/Rarity-Farbe per `getComputedStyle`), 3 neue Tests + voller Regressionslauf (27 Tests: `dragon-multi-companion`/`dragon-lifecycle-release`/`offline-companion-xp`) grün. — `js/systems/bkmp-breeding.js`, `style.css`, `package.json`, neu: `tests/e2e/dragon-lager-rarity-filter.spec.js` — 🟡 lokal

- **[Neu]** Mehrere gleichzeitige Kampf-Begleiter + neue Begleiter-Leiste oben im Drachenzucht-Tab (Spieler-Idee MCSoGGe, Feedback-Board: "wenn man ein gewisses Level an Prestiges hat dass man dann 2 oder mehr Drachen ausrüsten kann" - vom Nutzer direkt konkretisiert: 2./3. Begleiter mit abnehmendem Wert 50%/25%, freigeschaltet über einen neuen 2-stufigen Prestige-Knoten "Weitere Gefährten" im Vermächtnis-Zweig, 1.500/3.000 Punkte). Mitten in der Umsetzung Nutzerwunsch nach einer visuellen, an anderen Idle-/Sammel-RPGs orientierten Slot-Leiste statt eines reinen Textes - kurze Recherche zum Genre-Standard bestätigt (Idle Heroes/AFK-Arena-artige Spiele: kompakte Portrait-Slot-Reihe, Schloss-Symbol für gesperrte Plätze), direkt umgesetzt. Architektur-Recherche deckte auf: `is_companion` hatte bisher eine **Datenbank-seitige** Unique-Constraint ("maximal 1 Begleiter pro Spieler") - ohne die begleitende SQL-Migration bliebe das Feature trotz korrektem Client-Code wirkungslos. Bis zu 3 erwachsene Begleiter gleichzeitig ausrüstbar (je nach Prestige-Rang), automatisch nach rechnerischer Stärke sortiert (verhindert, dass ein Spieler versehentlich seinen stärksten Drachen in den schwachen Rang setzt) - bei genau einem aktiven Begleiter (Normalfall ohne den neuen Knoten) rechnerisch identisch zur alten Fassung, keine Regression. Jugendliche "wächst heran"-Begleiter (Kampf-EP-Sammler) bleiben komplett unabhängig davon, weiterhin genau einer gleichzeitig. Neue Begleiter-Leiste (3 feste Plätze: gefüllt/leer-aber-frei/gesperrt) ganz oben im Drachenzucht-Tab, plus Rang-Abzeichen auf jeder Lager-Karte. 6 neue Tests, 60/60 stabil, 98/98 grün über alle 3 Projekte inkl. kompletter angrenzender Regression (Prestige-Baum/Gilden-Drachenzucht/Drachen-Lebenszyklus/Offline-Begleiter-EP/Prestige-Aufstieg). — `js/systems/bkmp-breeding.js`, `js/systems/bkmp-prestige.js`, `style.css`, `tests/e2e/dragon-multi-companion.spec.js`, `tests/e2e/prestige-tree-v2.spec.js`, `package.json`, neu: `sql/20260805-dragon-multi-companion-slots.sql` (**noch nicht ausgeführt - Feature bleibt bis dahin auf 1 Begleiter begrenzt**) — 🟡 lokal
- **[Neu]** Nachbesserung am selben Tag (direkter Folgewunsch nach Screenshot der neuen Leiste): Klick auf einen freien/gefüllten Platz öffnet jetzt ein kompaktes Auswahlfenster mit ALLEN passenden Drachen (kein Scrollen im Lager mehr nötig, wiederverwendet `bkmpUiModalHtml()` - zweiter echter Einsatz im Idle-Dorf) statt dass man dafür weiter unten im Lager suchen muss - ein Klick auf einen Eintrag rüstet ihn direkt aus/legt ihn ab, Fenster bleibt für weitere Auswahl offen. Neuer 4. Block ("Trainieren", eigenes 🐣-Icon, mit Abstand von den 3 Kampf-Plätzen getrennt) ersetzt die bisherige reine Textzeile für den jugendlichen "wächst heran"-Begleiter - öffnet einen auf Jugendliche gefilterten Picker. Neuer Stats-Block rechts neben der Leiste (nach kurzer Recherche zu Tactics-RPG-Stat-Panels: Icon+Bezeichnung+Wert je Zeile für ⚔️ Angriff/🛡️ Verteidigung/❤️ Leben) zeigt den tatsächlich wirksamen, gewichteten Gesamtbonus statt bloßer Einzelwerte. 5 weitere Tests (jetzt 11 insgesamt), 88/88 stabil, 98/98 grün über alle 3 Projekte. — `js/systems/bkmp-breeding.js`, `style.css`, `tests/e2e/dragon-multi-companion.spec.js` — 🟡 lokal
- **[SQL]** Öffentliche Changelog-Zeile für mehrere Kampf-Begleiter (siehe oben, inkl. Auswahlfenster/Trainings-Platz/Stats-Block) vorbereitet — `sql/20260805-changelog-multi-companion.sql` (**noch nicht ausgeführt**) — 🟡 lokal
- **[Fix]** Bugfix am selben Tag (Live-Screenshot des Nutzers): der neue Trainings-Platz (4. Block) erschien bei knappem Platz mitten zwischen den 3 Kampf-Plätzen statt sauber getrennt, ein Kampf-Platz riss dabei einzeln auf eine zweite Zeile ab. Ursache: `.idle-dragon-companion-combat-group` hatte selbst `flex-wrap:wrap` ohne feste Breite, wodurch die 3 Kampf-Plätze unabhängig vom Trainings-Platz intern umbrechen konnten statt dass die äußere Leiste den 4. Block sauber als Ganzes verschiebt. Fix: `flex-wrap:nowrap` auf der Kampf-Gruppe - die 3 Plätze bleiben jetzt immer zusammen (dürfen gemeinsam schrumpfen), der Trainings-Platz rutscht bei Bedarf als komplette Einheit darunter. Neuer Regressionstest mit erzwungenen Fenstergrößen (480px/300px), 10/10 stabil. — `style.css`, `tests/e2e/dragon-multi-companion.spec.js` — 🟡 lokal
- **[Fix]** "👑 Bosskampf!"-Banner erschien bei "Bleibt hier" dauerhaft wiederkehrend statt nur einmal (Nutzer-Screenshot der Kampfansicht). Root Cause: `bkmpIdleSpawnDragon()` zeigte das Banner unbedingt bei jedem Aufruf mit `isBoss=true` - bei "Bleibt hier" (`auto_advance=false`) bleibt `current_dragon_index` nach jedem Kill unverändert auf der Boss-Stufe, `bkmpIdleSpawnDragon()` läuft aber bei JEDEM Kill trotzdem erneut (baut denselben Boss mit vollem HP neu auf) - das Banner feuerte dadurch bei jedem einzelnen Kill erneut. Fix: merkt sich jetzt den zuletzt tatsächlich bebannerten Stufen-Index (`bkmpIdleLastBannerBossIndex`), zeigt das Banner nur noch beim tatsächlichen ERSTEN Erreichen einer Boss-Stufe; wird bei jedem Nicht-Boss-Dragon (Weiterzug, Rückzug nach Niederlage, Prestige-Reset) automatisch wieder geleert, damit ein späteres erneutes Erreichen derselben oder einer anderen Boss-Stufe korrekt wieder einmal zeigt. 2 neue Regressionstests (Banner nur beim ersten Erreichen + Banner erscheint nach Verlassen+Rückkehr wieder), 30/30 stabil bei 15x Wiederholung, 84/84 grün über alle 3 Projekte inkl. angrenzender Regression (combat/obs-background-catchup). Live im Browser bestätigt (kein echter Login möglich - injizierter Mock-Kampfzustand über die echte `bkmpIdleSpawnDragon()`-Funktion). — `js/ui/bkmp-hud.js`, `tests/e2e/combat-visual-polish.spec.js` — 🟡 lokal
- **[SQL]** Öffentliche Changelog-Zeile für den Bosskampf-Banner-Fix (siehe oben) vorbereitet — `sql/20260805-changelog-boss-banner-repeat.sql` (**noch nicht ausgeführt**) — 🟡 lokal
- **[Fix]** Zwei Bugs am Drachenzucht-Wachstum, gemeldet in einer Nachricht ("Wir brauchen bei denn Jugendlich -> Erwachsenen einen XP Anzeige.. und scheinbar über nacht offline timer passiert auch nichts an Fortschritt"): **(1) Fehlende XP-Zahl:** die Kampf-EP/Fütterungs-EP-Anzeige (`733/2000`) stand als `<span>` DIREKT IN `.idle-xp-bar` (`overflow:hidden`) statt als eigenes Geschwister-Element daneben - identisches, bereits an anderer Stelle (HUD-XP-Balken) korrekt gelöstes Muster, hier aber nie übertragen worden. Fix: Text in ein separates `.idle-xp-label`-Div (bereits fertig gestylt, bisher nur ungenutzt) verschoben, für Baby- UND Jugendlich-Karten. **(2) Kein Kampf-EP-Fortschritt über Nacht:** `bkmpDragonGrantCompanionBattleXp()` vergibt Kampf-EP an den Begleitdrachen nur bei tatsächlich laufendem Live-Tick - `api/claim-idle-offline-progress.js` (server-seitige Offline-Simulation) kannte `player_dragons` bisher überhaupt nicht, ein Begleitdrache blieb während jeder Abwesenheit komplett auf seinem alten Stand stehen, unabhängig von der Dauer. Fix: neuer, defensiv gekapselter Lookup des aktuellen jugendlichen Begleitdrachen + dessen Art-Anforderung; sammelt während der bestehenden Kampf-Simulationsschleife denselben Betrag pro Kill wie live (Boss 25, sonst 4), deckelt am Ende auf `battle_xp_required`, schreibt separat zu `player_dragons`. Bewusst NICHT repliziert: der prozentuale EP-Bonus aus Substats/Skilltree/Prestige - Offline-EP können bei Spielern mit hohem Bonus etwas niedriger ausfallen als live, ein kleiner, dokumentierter Kompromiss gegenüber vorher komplett fehlenden Offline-EP. Client zeigt den Zugewinn jetzt auch in der "Während deiner Abwesenheit..."-Karte + lädt `bkmpPlayerDragons` bei einem tatsächlichen Zugewinn automatisch neu (sonst erst nach erneutem Fensterwechsel sichtbar gewesen). 5 neue Tests laufen über die ECHTE, unveränderte Handler-Datei (keine Testkopie der Formel) - 50/50 stabil bei 10x Wiederholung, 66/66 grün über alle 3 Projekte inkl. angrenzender Regression (offline-afk/dragon-lifecycle-release/combat). — `js/systems/bkmp-breeding.js`, `api/claim-idle-offline-progress.js`, `idledorf.js`, `tests/e2e/offline-companion-xp.spec.js`, `package.json` — 🟡 lokal
- **[SQL]** Öffentliche Changelog-Zeile für den Begleitdrachen-Fix (siehe oben) vorbereitet — `sql/20260805-changelog-dragon-growth-xp.sql` (**noch nicht ausgeführt**) — 🟡 lokal
- **[Neu]** Neue Zucht-Drachenart "Bagondrache" (Episch) - Nutzer lieferte ein zusammengesetztes 4-Stufen-Referenzbild (Ei/Baby/Jugendlich/Erwachsen auf grauem Flächenhintergrund, je mit Beschriftung) und bat darum, es selbst auszuschneiden. Per Node/`sharp`-Skript automatisiert freigestellt (Sättigungs-/Leuchtdichte-basierte Inhaltserkennung statt festem Colorkey, da der Hintergrund einen leichten Verlauf hatte, weicher Alpha-Rand an den Kanten statt hartem Ausschnitt) und auf die jeweilige Bounding-Box zugeschnitten - per Vorschau-Kontaktabzug visuell bestätigt, saubere Freistellung ohne Grau-Reste. Werte 1:1 von der bestehenden Episch-Stufe übernommen (gleiches Muster wie die 4 "NeueNeueDrachen"-Episch-Arten vom 02.08.), `egg_source='event'`, `sort_order=24`. Durch `scripts/optimize-images.mjs` geschickt (480px + WebP, gleiches Verfahren wie alle anderen Zucht-Sprites) - keine JS-Änderung nötig (`bkmpDungeonRollEgg()`/`bkmpDragonThumbHtml()` sind bereits vollständig datengetrieben). — neu: `assets/dragons/breeding/{egg,baby,teen,adult}/bagondrache(.png/-web.png/-web.webp)`, `sql/20260805-dragon-species-bagondrache.sql` (**noch nicht ausgeführt**) — 🟡 lokal
- **[Fix]** Nachbesserung an der neuen Begleiter-Leiste (Nutzer-Screenshot, korrekt einzeilig aber mit großer ungenutzter Lücke zwischen den Slots und dem Stats-Block: "Wir haben sooviel platz. Nutze denn doch mal."). Root Cause: `.idle-dragon-companion-combat-group`/`-teen-wrap` hatten kein `flex-grow` (Standard 0) - obwohl `.idle-dragon-companion-bar` selbst `flex:1 1 auto` trägt und die volle Zeilenbreite beansprucht, wurde dieser freie Platz nie an die Kind-Gruppen weitergereicht, die Plätze blieben auf Minimalgröße stecken. Fix: proportionales `flex-grow:3`/`flex-grow:1` (passend zum 3-Plätze-vs-1-Platz-Verhältnis) + angehobene `max-width`/Schrift-/Bildgrößen, damit das Wachstum sichtbar wird statt nur zusätzliches Padding zu erzeugen. Live im Browser bestätigt: Lücke fiel bei 1150px Container-Breite von ~500px auf ~26px, Slot-Breite wuchs von ~90px auf ~206-211px; das schmale Verhalten aus dem vorherigen Bugfix (Zusammenbleiben/Umbruch bei wenig Platz, `flex-grow` greift nur bei positivem Restplatz) blieb bei 480px/300px unverändert bestätigt. Neuer Regressionstest beweist Wachstum bei breitem Viewport zusätzlich zum bereits bestehenden Schmal-Test. 13/13 grün. — `style.css`, `tests/e2e/dragon-multi-companion.spec.js` — 🟡 lokal
- **[Fix]** Permanent sichtbarer Hinweistext im Drachenzucht-Tab hinter ein "ℹ️"-Symbol versteckt (Nutzerwunsch: "Diese Anleitung kann auch ganz nach unten. Oder irgendwo hinter einen 'I' versteckt als Hilfe Anleitung", Bezug: der Satz "Gebäude-Upgrades findest du im Tab..." unter der Obstgarten/Jagdhütte-Vorräteanzeige). Statt eines Umzugs nach unten wiederverwendet: `bkmpUiTooltipHtml()`/`bkmpUiWireTooltipTrigger()` (Phase 3, bereits einmal für den Paragon-Hinweis im Prestige-Baum eingesetzt) - kleiner Tap-/Hover-Auslöser direkt neben der "🍎🥩 Vorräte"-Überschrift, identisches Muster wie dort. Live im Browser bestätigt: Tooltip standardmäßig unsichtbar (`opacity:0`, `visibility:hidden`, `pointer-events:none`), zeigt sich korrekt bei Hover/Fokus/Touch und schließt beim Verlassen wieder, kein dauerhaft sichtbarer Text mehr, keine Konsolenfehler. Bestehende `dragon-multi-companion.spec.js`/`dragon-lifecycle-release.spec.js`-Suiten (18 Tests) unverändert grün. — `js/systems/bkmp-breeding.js`, `style.css` — 🟡 lokal

---

## 2026-08-04 (7)

- **[Änderung]** Zwei Duplikat-/Alt-Accounts ("Bagon1990"/"BagonTr02", Screenshot: Rang 14+15 der öffentlichen "Erfolge"-Bestenliste, je "254 Erfolge") aus der öffentlichen Bestenliste ausgeblendet (Nutzerwunsch: "Das waren Bagon 2 Accounts. Die können weg"). Nach Rückfrage entschieden: **nur aus der Bestenliste ausblenden**, nicht die kompletten Accounts löschen (dafür gäbe es bereits `admin_delete_player_account` im Admin-Panel "Spieler-Verwaltung", aber das wäre unwiderruflich und hätte auch Idle-Dorf-Spielstand/Runen/Prestige/Login mitgelöscht - deutlich größere Wirkung als vom Screenshot allein ersichtlich). Gleiches, bereits bestehendes Muster wie `BKMP_HIDDEN_TEST_ACCOUNTS`/`bkmpIsHiddenTestAccount()` (14.07., dort für einen internen Test-Account) - neue, separate Liste `BKMP_HIDDEN_LEADERBOARD_ACCOUNTS`/`bkmpIsHiddenFromLeaderboard()` (`supabase.js`), an derselben Filterstelle wie der Test-Account-Filter angewendet (`refreshLeaderboardData()`, `js/core/bkmp-site.js`) - wirkt auf ALLE Kategorien der Bestenliste (Erfolge/Karten/Kartenideen/Zeit/Bonks teilen sich denselben gefilterten Datensatz), nicht nur auf "Erfolge". Rein clientseitige Anzeige-Filterung, keine SQL-Migration nötig, im Admin-Panel bleiben beide Accounts weiterhin normal sichtbar/verwaltbar. Case-insensitiv (matched `bagon1990`/`BAGONTR02`/etc.), der echte, bereits vielfach dokumentierte Spieler "BagonTr01" bleibt bewusst unberührt (per Live-Test bestätigt, nicht nur angenommen). — `supabase.js`, `js/core/bkmp-site.js` — 🟡 lokal

---

## 2026-08-04 (6)

- **[Fix]** Kampfansicht flackerte kurz + wirkte "geschrumpft" bei jedem Gegnerwechsel mit zwei Video-Drachen hintereinander (Spieler-Videobeweis, 16:28 Uhr, zwei unabhängige Momente im selben Clip: "Das ganze Fenster flackert kurz und wird ein stück kleiner"). Root Cause: die "Gegnerwechsel weniger abrupt"-Animation vom 03.08. (`.idle-dragon-spawn-in`, startet bei `opacity:0, scale(0.9)`) ging fälschlich davon aus, der Sprite-Container sei zu diesem Zeitpunkt bereits mit dem neuen Drachen befüllt - stimmt für Video-Drachen nicht: das wiederverwendete `<video>`-Element bekommt nur eine neue `src`, das erste darstellbare Bild braucht noch einen echten Lade-/Dekodier-Schritt. Die Animation lief bisher sofort auf einer buchstäblich leeren/schwarzen Box - sichtbar als kurzes Schwarz-Flackern + Schrumpfen (der `scale(0.9)`-Startwert). Per `ffmpeg`-Frame-Extraktion aus dem Videobeweis an zwei unabhängigen Stellen im Clip bestätigt (Cyberdrache→Winddrache und eine zweite, unabhängige Sequenz), beide Male eine komplett leere Sprite-Box direkt nach dem Drachenwechsel. Fix: Logik aus `bkmpIdleSpawnDragon()` in eine eigene, benannte Funktion `bkmpIdleTriggerDragonSpawnAnim()` ausgelagert - bei einem Video, dessen erstes Bild noch nicht bereitsteht (`readyState < HAVE_CURRENT_DATA`), startet die Animation jetzt erst beim `loadeddata`-Ereignis (mit 600ms-Fallback für den Fall, dass es nie feuert) statt sofort. PNG-Sprite-Drachen und bereits geladene Videos bleiben unverändert sofort. 4 neue Regressionstests (blockierter Video-Request beweist die Verzögerung + spätere Freigabe löst die Animation korrekt aus, bereits geladenes Video bleibt sofort, PNG-Drache bleibt sofort, 600ms-Fallback greift bei nie feuerndem Ereignis) - 40/40 stabil bei 10x Wiederholung, volle Regression 66/66 grün über alle 3 Projekte. Live im Browser mit echtem, gezielt verzögertem Video-Asset bestätigt (Animation startet erst bei `readyState:4`, nicht sofort bei `readyState:0`). — `js/ui/bkmp-hud.js`, `tests/e2e/combat-visual-polish.spec.js` — 🟡 lokal

---

## 2026-08-04 (5)

- **[Fix]** Dorf-Skins-Vorschau flackerte/verschwand während des Kampfs (Spieler-Meldung + Screenshot, direkt nach Einführung der Vorschau-Kachel: "In der Großansicht verschwinden alle Ansichten und fangen an zu flackern"). Root Cause: `bkmpIdleRefreshLiveTabsRender()` (idledorf.js, bereits bestehend seit 26.07.) ruft `bkmpIdleRenderSkinsPanel()` bei JEDEM Drachen-Kill erneut auf, solange der Dorf-Skins-Tab offen ist (throttled auf max. alle 300ms) - vor der Vorschau-Kachel bedeutete das nur unsichtbares Neu-Rendern von Text/Buttons, jetzt aber zerstörte jeder Rebuild JEDES `<video>`-Vorschauelement und erzwang einen kompletten Neustart (neu laden+dekodieren), alle ~0,9-2,5s solange im Hintergrund weitergekämpft wird - auch sichtbar, während die Lightbox offen war (Karten dahinter blitzten durch, siehe Screenshot). Fix: die bereits vorhandenen, längst geladenen `.idle-skin-preview-thumb`-Knoten werden vor jedem Rebuild gesichert und per `replaceWith()` unverändert (dieselbe DOM-Node, kein neues `<video>`) in die neue Karte zurückgehängt - nur ein wirklich neuer Skin bekommt eine frische Kachel. 2 neue Regressionstests (direkter DOM-Knoten-Identitätsvergleich über 5 simulierte Live-Tick-Renders + Beweis, dass ein währenddessen neu zum Katalog hinzugekommener Skin trotzdem korrekt initialisiert wird), 20/20 stabil, volle `cosmetics.spec.js`-Suite 53/53 grün über alle 3 Projekte. Live per direktem Node-Identitäts-Test im Browser bestätigt (Marker überlebt 3 aufeinanderfolgende Renders unverändert, Klick-Listener bleiben funktionsfähig). — `js/systems/bkmp-cosmetics.js`, `tests/e2e/cosmetics.spec.js` — 🟡 lokal

---

## 2026-08-04 (4)

- **[Neu]** Dorf-Skins-Shop: kleine Vorschau-Kachel an jeder Karte + vergrößerte Live-Ansicht per Klick (Spieler-Idee NikschiOG, Feedback-Board: "Es wäre cool wenn man bei den Dorf Skins eine Vorschau hätte, da man sonst nicht weiß was man kauft."). Jede Karte (auch nicht besessene - das ist ja der Sinn) zeigt jetzt ein statisches erstes Frame direkt im Grid (`bkmpApplyVillageSkinToElement()` bekam eine neue `forceStatic`-Option - verhindert, dass alle 7+ sichtbaren Karten gleichzeitig ihre Videos abspielen). Klick oder Enter öffnet eine vergrößerte Lightbox (`#idleSkinPreviewOverlay`, identisches Muster wie `#investorPayoutViewerOverlay` vom 28.07.) mit der ECHTEN, live animierten/abspielenden Ansicht - respektiert dabei ganz normal den Effektmodus-Schalter (kein Zwangs-Play bei "Effekte aus"). **Reales z-index-Stacking-Problem beim eigenen Testen gefunden+gefixt** (per Playwright reproduziert, nicht nur vermutet: "idle-skin-card intercepts pointer events") - die neue Lightbox öffnet aus dem bereits offenen Idle-Dorf-Fenster heraus, hatte anfangs denselben Basis-z-index (50) wie `#idleDorfOverlay` und lag dadurch dahinter statt davor - exakt dasselbe, bereits mehrfach dokumentierte Muster wie bei `#idleDragonDetailOverlay`/`#arenaBattleOverlay` (14.-16.07.), per identischem `z-index:60`-Fix behoben. 7 neue Tests (alle Kacheln vorhanden inkl. nicht besessener Skins, kein gleichzeitiges Video-Abspielen im Grid, reiner Sprite-Skin ohne laufende Animation, Klick öffnet Lightbox mit echtem Video/Play, nicht besessener Skin zeigt echte Vorschau statt Standard-Fallback, Schließen-Button + Klick-außerhalb, Tastatur/Enter) - 56/56 stabil bei 8x Wiederholung, 51/51 grün über alle 3 Projekte (inkl. einer dabei aufgedeckten, jetzt gefixten Testfragilität in einem VORBESTEHENDEN Test, der zu grob auf `data-skin-id` statt auf den konkreten Kauf-Button-Kontext prüfte). — `js/systems/bkmp-cosmetics.js`, `index.html`, `style.css`, `tests/e2e/cosmetics.spec.js` — 🟡 lokal

---

## 2026-08-04 (3)

- **[Änderung]** "Lager aufräumen"-Rarität-Verkaufsleiste im Runen-Tab komplett entfernt + Runen-Lager laedt wieder unbegrenzt (Nutzerwunsch, Screenshot der Knöpfe "Gewöhnlich/Ungewöhnlich/Selten/Episch": "Das wieder entfernen und alle Runen anzeigen lassen? Viele sind verwirrt und kennen sowas nicht."). Die Leiste sah wie ein Filter aus, war aber tatsächlich ein sofortiger, unwiderruflicher Sammelverkauf (mit Bestätigungsdialog) - das hat mehr Spieler verwirrt als geholfen. Auf ausdrücklichen Nutzerwunsch, **trotz des bekannten Risikos aus dem 25.07.-Vorfall** ("meine Runen sind komplett weg", ausgelöst durch ein unbegrenztes Laden bei 7.500-15.600+ Zeilen großen Lagern), wieder auf unbegrenztes Laden umgestellt: `loadUnequippedPlayerRunesCapped(name, 300)` → `loadUnequippedPlayerRunes(name)` (kein `.limit()` mehr). Die eigentliche Fehler-Isolation (ausgerüstete Runen + Lager als zwei unabhängige Ladevorgänge, kein stilles Leeren bei einem Netzwerkfehler) bleibt bestehen - das war nie die Ursache der Verwirrung, nur die zusätzliche Kappung + Verkaufsleiste ist weg. `bkmpRuneSellAllByRarity()`/`deleteRunesByRarity()` komplett gelöscht (keine anderen Aufrufer). 3 jetzt gegenstandslose "300er-Cache-Bypass"-Regressionstests entfernt (die Testkonstruktion selbst - "beweise, dass der lokale Cache etwas ausschließt" - ist ohne Cap nicht mehr konstruierbar), 1 Test komplett neu geschrieben (beweist jetzt das Gegenteil: ein 394-Zeilen-Lager lädt vollständig). Live im Browser mit injiziertem 350-Runen-Bestand verifiziert (alle 350 rendern, kein Banner, kein "+"-Suffix). — `supabase.js`, `idledorf.js`, `js/systems/bkmp-runes.js`, `js/core/bkmp-idle-state.js`, `style.css`, `tests/e2e/rune-inventory-scale.spec.js`, `tests/e2e/rune-autofuse-datasource.spec.js`, `tests/e2e/rune-maximize-legendary.spec.js` — 🟡 lokal

---

## 2026-08-04 (2)

- **[Änderung]** Weltboss-Kampfleiste aufgeräumt (Spieler-Feedback ChronoKora zum Screenshot der Raid-Ansicht). Der separate rote "⚔️ Angreifen"-Knopf ist komplett entfernt - er rief ohnehin nur denselben Klick-Handler auf wie ein Tipp auf den (deutlich größeren) Boss selbst oder die Leertaste, reine Redundanz. Die "👆"-Anzeige daneben zeigt jetzt die Anzahl der eigenen Klicks statt der kumulierten Schadenssumme (die stand bereits redundant direkt darunter in der Trefferliste) - `bkmpRaidTapDamageSession` in `bkmp-raid.js` umbenannt zu `bkmpRaidTapClickSession`, zählt jetzt +1 statt +Schaden pro Klick. Die "🟢 Auto-Kampf aktiv"-Anzeige links bleibt bewusst unverändert bestehen (Nutzer vermutete, sie sei bedeutungslos "ohne Klicken ist er eh nicht machbar" - per Code bestätigt falsch: `bkmpRaidOwnTick()` fügt tatsächlich alle 2,5s automatisch Schaden zu, unabhängig von jedem Klick, exakt wie die Anzeige verspricht). Beide Pillen teilen sich die Zeile jetzt hälftig. Neuer Regressionstest beweist "+1 pro Klick, nicht +Schaden", bestehender Klick-Zähler-Test aktualisiert. — `index.html`, `idledorf.js`, `js/systems/bkmp-raid.js`, `style.css`, `tests/e2e/raid-tap-counter-appmode.spec.js` — 🟡 lokal
- **[Neu]** Ein/Aus-Schalter für "Automatischer Bosskampf" (Spieler-Wunsch BagonTr01: "ist irgendwie ne Möglichkeit das man das automatische Bossbeitreten [beim stündlichen Weltboss] an und ausschalten kann?"). Der Prestige-Knoten löste bisher immer+dauerhaft einen automatischen Beitritt aus, sobald einmal gekauft, ohne Möglichkeit zum Abschalten. Neuer lokaler Schalter (`bkmpRaidAutoJoinEnabledGet/Set()`, localStorage, Default AN - ändert nichts am bisherigen Verhalten für alle, die ihn nie anfassen), sitzt als Checkbox direkt an der Knotenkarte im Prestige-Baum (dort statt am nur ~5 Minuten sichtbaren Beitritts-Banner, damit er jederzeit erreichbar ist). 2 neue Tests (Engine-Ebene: Schalter aus → kein Beitritt trotz Freischaltung, dann wieder an → Beitritt greift erneut; UI-Ebene: Checkbox erscheint erst nach Kauf, Default an, Klick speichert+zeigt korrekt), 50/50 stabil bei 10x Wiederholung. — `js/systems/bkmp-raid.js`, `js/systems/bkmp-prestige.js`, `idledorf.js`, `tests/e2e/prestige-automation-toggles.spec.js` — 🟡 lokal

---

## 2026-08-04

- **[Änderung]** Visueller Feinschliff der Kampfansicht (Auftrag: 5 Abschnitte - Schadenszahlen/Gegnerwechsel/Trefferfeedback Spielerseite/Status-Einblendungen/Belohnungsanzeigen). Reine Anzeige-/Animations-Ergänzungen, keine Kampfmechanik/-werte/-assets angefasst. Schadenszahlen (`bkmp-hud.js`): kleiner Zufalls-Versatz je Zahl (kein permanentes Übereinanderliegen), Deckel von 5 gleichzeitigen Zahlen pro Ziel (älteste blendet beschleunigt aus statt unbegrenzt anzuwachsen), Kritische Treffer bekommen ein eigenes, kräftigeres Pop-In (`idleDmgFloatCrit`, der bereits bestehende Funken-Ausbruch bleibt unverändert). Gegnerwechsel (`idledorf.js`/`bkmp-hud.js`): kurzer Ausblende-Puls auf dem besiegten Drachen-Sprite + Einblende-Pop auf dem neuen (dasselbe wiederverwendete `#idleDragonSprite`-Element, CSS-Kaskade sorgt bewusst dafür, dass beim üblichen sofortigen Kill→Spawn-Übergang die Einblend-Animation gewinnt). Trefferfeedback Dorf-Seite (`bkmp-hud.js`): rote Innen-Vignette auf dem Dorf-Sprite + kurzes Aufleuchten des HP-Balken-Wrappers, zusätzlich zum bereits bestehenden Bildschirm-Wackler (unverändert). **Echter Bug beim eigenen Testen gefunden+gefixt:** `#idleVillageSprite` trägt IMMER ein Inline-`style.animation` (bkmp-cosmetics.js, für Dorf-Skins) - hätte die neue Puls-Animation ohne `!important` komplett unsichtbar gemacht; mit `!important` allein hätte ein einziger Treffer die echte Endlos-Ambiente-Animation eines animierten Skins (z.B. Pilzdorf) dauerhaft überschrieben - Fix: eigener Timer entfernt die Puls-Klasse exakt nach Ablauf der 0,4s wieder, live per Test bewiesen (animierte Fake-Skin-Animation überlebt einen Treffer unverändert). Status-Banner (neu, `bkmpIdleShowCombatStatus()`): kurze, nicht-blockierende Texte in der Kampfmitte für Sieg/Bosskampf/Nächste Stufe/Niederlage, mit sinnvoller Priorisierung (Bosskampf > Sieg > Nächste Stufe, eine laufende wichtigere Meldung wird nicht von einer unwichtigeren unterbrochen) - bewusst NICHT über das bestehende `bkmpRewardPresent()`-System (das ist ein seitenweites Toast-System mit eigener Queue für Belohnungen, ein Umbau hätte Regressionsrisiko für viele andere Aufrufer gehabt). Belohnungsanzeige (`idledorf.js`): Deckel von 3 gleichzeitig gestapelten Belohnungs-Reihen bei schnellen Kills. Alle neuen Ein-Schuss-Animationen nutzen den bereits etablierten "remove+reflow+add"-Neustart-Trick, respektieren `prefers-reduced-motion` (automatisch über die bestehende Kollektivregel + explizite Ergänzungen für `!important`-Fälle) sowie die bestehenden `data-fx="aus"`/`fx-off-hitShake`/`fx-off-dmgNumbers`/`fx-off-attackAnim`-Einzelschalter. 10 neue Playwright-Tests (Deckel/Jitter/Crit-Unterscheidung/Gegnerwechsel-Integrität/Trefferfeedback inkl. Skin-Regression/Banner-Priorität-und-Klickbarkeit/Boss-Auto-Banner/Belohnungs-Deckel/Effekte-Aus/Einzelschalter), 30/30 grün über alle 3 Projekte. Voller Regressionslauf (chromium-desktop, gesamte bestehende Suite): 479/480 grün, der eine Fehlschlag isoliert 10/10 sauber nachgeprüft (bekanntes Ressourcenkonkurrenz-Flake-Muster dieser Testdatei, keine echte Regression). Öffentliche Changelog-SQL-Zeile vorbereitet. — `js/ui/bkmp-hud.js`, `idledorf.js`, `style.css`, neu: `tests/e2e/combat-visual-polish.spec.js`, `sql/20260804-changelog-combat-visual-polish.sql` (**noch nicht ausgeführt**) — 🟡 lokal

---

## 2026-08-02

- **[Neu]** Roter Punkt am "📜 Changelog"-Knopf, sobald ein neuer Eintrag im öffentlichen Changelog erschienen ist, den man noch nicht gesehen hat (Nutzerwunsch nach Screenshot: "wenn wir was neues Posten oben bei Changelog eine kleiner Roter Punkt kommt"). Vergleicht die ID des neuesten geladenen Eintrags gegen die zuletzt gesehene ID in `localStorage` - kein Server-Zustand nötig. Punkt verschwindet, sobald das Panel geöffnet wird, bleibt aber über Reloads hinweg korrekt bestehen, solange nichts Neues erschienen ist (live gegen echte Produktionsdaten verifiziert, 25 echte Einträge). — `js/ui/bkmp-changelog.js`, `style.css`, `index.html` — 🟡 lokal
- **[Neu]** Ei-/Baby-Karten im Drachenlager bekommen einen kleinen runden Papierkorb-Knopf oben rechts zum Freilassen (Nutzerwunsch: "eine Option mit einbauen das man 'Eier oder baby' löschen / Freilassen kann. Aber bitte nur kleiner süßer Mülleimer symbol vllt oben rechts im Kasten?"). Bei mehreren Eiern derselben Art löscht der Knopf bewusst nur EIN Ei (dieselbe repräsentative ID, die auch "In freies Nest legen" nutzt) - kein versehentliches Leeren eines ganzen Stapels. Babys nutzen jetzt dieselbe Bestätigen-Logik wie der bereits bestehende Freilassen-Knopf für jugendliche/erwachsene Drachen (aus dem bisherigen Inline-Klick-Handler in eine gemeinsame Funktion `bkmpDragonConfirmAndRelease()` ausgelagert, damit beide Aufrufstellen exakt dasselbe Sicherheitsverhalten teilen). Ein generisches CSS-Reset für alle Buttons im Idle-Dorf (`html.bkmp-app-mode .idle-dorf-panel button`, 14px-Radius/großes Innenpolster - für normale Text-Buttons gedacht) hätte die runde Mini-Optik sonst wieder zu einem eckigen Vollformat-Knopf gemacht - per gezieltem `!important` auf den neuen Regeln vermieden (gleiches, bereits bekanntes Muster wie bei `.idle-dragon-fav-btn`/`.idle-dragon-release-btn`). 9 neue Tests, 60/60 stabil (10x Wiederholung). — `js/systems/bkmp-breeding.js`, `supabase.js` (`deletePlayerDragonEgg`), `style.css`, neu: `tests/e2e/dragon-lifecycle-release.spec.js` — 🟡 lokal
- **[Änderung]** Drachen-Aufstieg ("🌟 Aufsteigen", zwei gleiche erwachsene Drachen zu einer stärkeren Stufe verbinden, bis zu 5 Stufen) war bisher auf legendäre Arten beschränkt - jetzt für JEDE Seltenheit freigeschaltet, exakt dieselbe Mechanik (Nutzerwunsch: "Die Funktion für alle Drachen freischalten nicht nur legendäre.. 'Aufsteigen' 5 der gleichen drachen verbinden"). Einzige Änderung: der `species.rarity === 'legendaer'`-Filter in `bkmpDragonCanAscend()` wurde entfernt - Kosten (150.000 Gold + eine zweite erwachsene Kopie), Bonus (+10%/Stufe) und Deckel (5 Stufen) bleiben für alle Seltenheiten identisch. Per Test bewiesen, dass Standard-Arten jetzt genauso aufsteigen können wie zuvor nur Legendäre, inkl. echtem Opfer-Verbrauch/Gold-Abzug/Server-Bestätigung, und dass Legendäre unverändert weiter funktionieren. — `js/systems/bkmp-breeding.js`, `tests/e2e/dragon-lifecycle-release.spec.js` — 🟡 lokal

---

## 2026-08-03 (2)

- **[SQL]** Prestige-Knoten "Schlüsselmeister" (+3% schnellere Dungeon-Schlüssel-Regeneration pro Rang) komplett entfernt (Nutzerwunsch: "wir haben Feste Schlüssel Zeiten. da bringt so ein skill nichts") - ein personalisiertes, vom gemeinsamen Raster abweichendes Tempo widersprach dem ursprünglichen Wunsch nach einem GEMEINSAMEN Zeitraster für alle Spieler. Server (`dungeon_regen_calc()`/`dungeon_get_all_status()`/`dungeon_consume_key()`) nutzt jetzt ausnahmslos das feste 00/04/08/12/16/20-Uhr-Raster, Schlüsselbund (höherer Schlüssel-Deckel) bleibt unverändert bestehen. Bereits investierte Punkte (Basis- UND Paragon-Ränge) werden jedem betroffenen Spieler beim nächsten Laden automatisch und exakt zurückerstattet (gleiches Prinzip wie die Schlüsselbund-Rückerstattung vom 31.07.). 4 neue Tests (Katalog-Entfernung, Rückerstattung exakt+idempotent, unbeteiligter Spieler bleibt unangetastet, Wirkungslosigkeit auch bei extremem Bestands-Paragon), 3 bestehende Tests umgeschrieben, 48/48 grün über alle 3 Projekte. — neu: `sql/20260803-remove-schluesselmeister-fixed-slots-only.sql` (**noch nicht ausgeführt**), geändert: `js/systems/bkmp-prestige.js`, `idledorf.js`, `tests/mock/rpc-engine.js`, `tests/e2e/dungeon-key-prestige-bonus.spec.js`, `tests/e2e/prestige-tree-v2.spec.js` — 🟡 lokal

---

## 2026-08-03

- **[Fix]** Phils Jugendlich-Sprite hatte einen weißen statt transparenten Hintergrund (Admin-Feedback: "Bei ihm muss der weisse Hintergrund noch entfernt werden"). Bestätigt per Pixel-Check: alle anderen 95 Kombinationen (24 Arten × egg/baby/teen/adult) hatten 26-66% transparente Pixel, nur `teen/phil.png` lag bei 0,0% - ein isolierter Einzelfall, keine systemische Lücke (auch die 6 gerade erst hinzugefügten NeueNeueDrachen-Bilder waren alle sauber). Behoben mit demselben Flood-Fill-Verfahren, das schon für Kaledoss verwendet wurde (`scripts/fix-kaledoss-background.ps1`) - startet an allen vier Bildrändern, färbt nur mit dem Rand verbundene naheweiße Pixel transparent, lässt einen hellen Fleck mitten im Drachen unangetastet. WebP/PNG-Web-Varianten neu erzeugt (waren sonst weiter mit dem alten, kaputten Bild gecacht). Nach dem Fix erneut alle 96 Kombinationen geprüft - keine unter 5% Transparenz mehr. — `assets/dragons/breeding/teen/phil.png` + `-web.png`/`-web.webp` — 🟡 lokal

---

## 2026-08-02 (2)

- **[Neu]** 6 neue Zucht-Drachenarten: **Fynnow, Vulkarion, Bloodterion, Gravoryx** (Episch) + **Lohendrache, Darknisdrache** (Legendär) - Nutzer lieferte alle 24 Bilder (Ei/Baby/Jugendlich/Erwachsen je Art) als ZIP mit eindeutigen Dateinamen ("BloodterionEi.png", "Baby Fynnow.png", ...), dadurch keine Ratearbeit bei der Bild-Zuordnung nötig (eine vorherige Rückfrage zur Bild-Zuordnung war damit hinfällig). Werte 1:1 von der bereits etablierten Episch-/Legendär-Stufe übernommen (identisches Muster wie Byte/Enderdrachen/Kaledoss/Nytherion/Phil vom 18.07.) - reine Katalog-Erweiterung, keine Code-Änderung nötig (`dragon_species` ist bereits vollständig datengetrieben, `bkmpDungeonRollEgg()` nimmt neue aktive Arten automatisch in den seltenheits-gewichteten Ei-Pool auf). Bilder auf max. 480px verkleinert + WebP-Variante erzeugt (62,1MB → 1,5MB WebP), gleiches Verfahren wie bei allen bisherigen Zucht-Sprites. Alle 24 WebP-Dateien per direktem Abruf gegen den lokalen Server verifiziert (laden korrekt, plausible Dateigrößen). — neu: `sql/20260802-dragon-species-neue-neue-drachen.sql` (**noch nicht ausgeführt**), 72 neue Bild-Dateien unter `assets/dragons/breeding/{egg,baby,teen,adult}/` — 🟡 lokal

---

## 2026-08-01

- **[Fix]** Drachenlager: "Als Begleiter"/Stern/Papierkorb saßen je nach Anzahl der angezeigten Substats einer Karte auf unterschiedlicher Höhe ("kreuz und quer" statt einer geraden Linie, Spieler-Screenshot). Die Aktionszeile wandert jetzt bei jeder Karte konsequent an den unteren Kartenrand, unabhängig davon, wie viel Text darüber steht. — `style.css` (`.idle-dragon-actions-row`) — 🟡 lokal
- **[Änderung]** Gilden-Technologie: Drachenzucht-Zweig (Zuchtkraft/Zuchtpanzer/Zuchtvitalität/Zuchtmeisterschaft) bekommt eine eigene dritte Kategorie-Tab "🐲 Drachenzucht" statt weiter unter "⚔️ Schlacht" direkt neben den unabhängigen Kampfwert-Knoten zu stehen (Nutzerwunsch nach Live-Screenshot: "Können wir das mit Drachenzucht in einen Neuen Zweig gepackt?"). Rein kosmetische Umsortierung - keine Änderung an Kosten/Effekten/Vorbedingungen, alle vier Vorbedingungen bleiben innerhalb der neuen Kategorie. Die Kategorie-Tab-Leiste ist jetzt datengetrieben aus der Label-Tabelle gebaut statt zwei fest verdrahteter Buttons - ein künftiger vierter Zweig braucht nur einen neuen Eintrag dort. 21/21 Tests grün (3 Projekte), keine neuen Statik-Funde. — neu: `sql/20260801-guild-tech-drachenzucht-own-category.sql` (**noch nicht ausgeführt** - die 4 Knoten stehen live weiterhin unter "Schlacht", bis diese Datei läuft), geändert: `js/systems/bkmp-guild-tech.js`, `tests/e2e/guild-tech-drachenzucht.spec.js`, `sql/20260801-guild-tech-drachenzucht-branch.sql` (nur Kommentar-Nachtrag, keine Werte geändert) — 🟡 lokal
- **[Fix]** Raid/Weltboss: der Tap-Schaden-Zähler ("👆 0") in der Kampfleiste aktualisierte sich auf der normalen Website nie, obwohl jeder Klick real Schaden verursachte (Spieler-Nachfrage: "Die Klickzähler? der nicht funktioniert?"). Ursache: die Anzeigeaktualisierung stand hinter `if (window.BKMP_APP_MODE)` - ein Kommentar dort behauptete fälschlich, die Elemente existierten außerhalb der App nicht; tatsächlich ist die ganze Kampfleiste seit dem Redesign (17.07.) immer sichtbar. Exakt dasselbe Bugmuster wie der bereits am 19.07. gefixte "⚔️ Angreifen"-Knopf daneben - die Zähler-Anzeige direkt neben dem Knopf wurde beim damaligen Fix übersehen. Fix: unconditional wie der Angreifen-Knopf selbst. Beim eigenen Testen zusätzlich ein reines Mess-Artefakt gefunden (keine Bug): ein alter Browser-Tab hatte noch die alte Datei gecacht, ein `?v=...`-Cache-Busting-Versäumnis meinerseits, nicht Teil des eigentlichen Fehlers. Neuer Regressionstest (20/20 stabil). — `js/systems/bkmp-raid.js`, neu: `tests/e2e/raid-tap-counter-appmode.spec.js` — 🟡 lokal
- **[SQL]** Kritischer Produktions-Bug behoben: "guild_tech_attempt_status 404 (Not Found)" + "Beitrag fehlgeschlagen" - JEDER Beitrag zum Gilden-Technologie-Baum scheiterte live, nicht nur die Countdown-Anzeige (Nutzer-Live-Report, per Browser-Netzwerk-Tab-Antworttext auf die echte Ursache zurückgeführt: "function public.dungeon_regen_calc(smallint, timestamp with time zone, integer, integer) does not exist"). Ursache: `guild_tech_contribute()`/`guild_tech_attempt_status()` riefen `dungeon_regen_calc(..., 14400, 5)` mit nackten Zahlen-Literalen auf - deren Typ passte nicht exakt zur tatsächlich live laufenden Funktionssignatur (gleiche Bug-Familie wie die frühere `dungeon_regen_calc`-Mehrdeutigkeit vom 26.07., diesmal ein reiner Typ-, kein Überladungs-Konflikt). Fix vor dem Ausliefern gegen die echte Datenbank verifiziert (nicht nur vermutet): letzte beiden Argumente weggelassen, die Funktion nutzt jetzt ihre eigenen Standardwerte. Direkt beim ersten Live-Test deckte der Fix einen ZWEITEN, bis dahin verdeckten Bug auf ("structure of query does not match function result type") - `v_calc.new_keys - 1` wird von Postgres automatisch zu `integer` hochgestuft, obwohl die Funktion an der Stelle `smallint` verspricht; war vorher nie sichtbar, weil die Funktion davor immer schon beim ersten Bug abbrach. Beide Teile in derselben Datei behoben, vom Nutzer live bestätigt ("Hat geklappt"). — `sql/20260801-fix-guild-tech-dungeon-regen-calc-mismatch.sql` (neu, **ausgeführt, live bestätigt**) — 🟢 live (Nutzer bestätigt)
- **[Neu]** Gilden-Technologie: neuer Zweig "Drachenzucht" (Nutzerwunsch: "Eher ein kompletten Zweig für die Drachenzucht? Drachen Angriff % Verteidung % Leben % etc?"). Drei unabhängige Wurzelknoten (Zuchtkraft/Zuchtpanzer/Zuchtvitalität - je +8% Angriff/Verteidigung/Leben des Begleitdrachens pro Stufe) + eine Zusammenführung (Zuchtmeisterschaft, braucht ALLE DREI Wurzeln voll ausgebaut, +5% auf alle drei Hauptwerte gleichzeitig) - erste Zusammenführung mit drei statt nur ein/zwei Vorbedingungen im Baum. Wirkt nur, solange ein erwachsener Begleitdrache aktiv ist (identisches Verhalten zu den bereits bestehenden Prestige-Gegenstücken). Kein neues Datenmodell nötig, reine Katalog-Erweiterung der bestehenden `guild_tech_nodes`-Tabelle. 7 neue Tests (RPC-Ebene + Read-Seite über direkte Cache-Injektion), 70/70 stabil. — neu: `sql/20260801-guild-tech-drachenzucht-branch.sql` (**noch nicht ausgeführt**), `tests/e2e/guild-tech-drachenzucht.spec.js`; geändert: `js/systems/bkmp-breeding.js` (`bkmpIdleDragonCompanionEffectTotals` um vier neue Effekt-Typen erweitert), `package.json` — 🟡 lokal
- **[Fix]** Gilden-Technologie: nach dem letzten Beitragsversuch des Tages zeigte der Countdown kurzzeitig fälschlich "00:00:00" statt der echten ~4h Wartezeit (Nutzer-Screenshot, per Diagnose-Test reproduziert). Ursache: `guild_tech_contribute()` liefert `seconds_to_next_attempt` bewusst immer als Platzhalter 0 zurück (echte Berechnung braucht den vollen `guild_tech_attempt_status()`-Aufruf) - der Client patchte nach einem Beitrag bisher nur `.attempts`, `.secondsToNext` blieb auf dem alten "voll"-Stand stehen, bis der Countdown-Ticker es eine Sekunde später selbst korrigierte. Fix: der echte Stand wird nach jedem Beitrag sofort nachgeladen, kein falscher Zwischenzustand mehr sichtbar. Neuer Regressionstest (90/90 stabil über 3 Projekte). — `js/systems/bkmp-guild-tech.js`, `tests/e2e/guild-tech-tree-ui.spec.js` — 🟡 lokal
- **[Fix]** Gilden-Technologie: zweiter, unabhängiger Countdown-Bug gefunden (Nutzer meldete "0/5 · Nächster in 00:00:00" dauerhaft hängend, live auf bkinvestment.de bestätigt) - schlägt der ALLERERSTE `guild_tech_attempt_status()`-Ladeversuch beim Öffnen des Panels fehl (z.B. kurzer Netzwerk-Hänger), blieb der Status dauerhaft `null` und der Countdown-Ticker bricht bei einem `null`-Status sofort ab, ohne je einen erneuten Versuch zu starten - die irreführende Anzeige blieb bis zum Verlassen+Neuöffnen des Tabs stehen. Fix: bei einem fehlgeschlagenen ersten Ladeversuch zeigt die Zeile jetzt ehrlich "wird geladen…" statt einer erfundenen 0/5-Zahl, plus ein einmaliger automatischer Nachlade-Versuch nach 3s. Neuer Regressionstest (40/40 stabil, simuliert einen einzelnen fehlgeschlagenen RPC-Aufruf beim ersten Laden). — `js/systems/bkmp-guild-tech.js`, `tests/e2e/guild-tech-tree-ui.spec.js` — 🟡 lokal
- **[Fix]** Beim selben Live-Report fiel zusätzlich auf, dass der Gilden-Tech-Baum auf bkinvestment.de unstyled/ungeordnet rendere (Knoten flossen unpositioniert unter einem leeren SVG-Bereich). Ursache: ein vergessenes Cache-Busting - die Stufe-3-CSS-Regeln für den Baum (`.bkmp-guild-tech-tree`/`-node`) waren bereits im Code, `style.css`s `?v=...` wurde beim Einbauen aber nie erhöht, gecachte Browser luden dadurch nie die neuen Regeln. Fix: `?v=20260801-guildtechtreecss1` gesetzt, vom Nutzer per Folge-Screenshot live bestätigt. — `index.html`, `admin.html`, `idle-stream-mini.html`, `datenschutz.html`, `impressum.html` — 🟢 live (Nutzer bestätigt)
- **[Änderung]** Gilden-Level-Deckel von 30 auf 100 angehoben, deutlich steilere XP-Kurve für die neuen Stufen (Nutzerwunsch: "es gibt schon paar Gilden die lvl 30 max haben und sich langweilen.. also ruhig auf lvl 100 direkt und wie gesagt die Gold kurve muss steigen"). Levels 1-30 komplett unverändert (kein Migrationsrisiko für bereits erreichten Fortschritt), die gesamte Level-Logik (`guild_level_for_xp()`, `bkmpGuildLevelInfo()`, Fortschrittsbalken-UI) war bereits vollständig datengetrieben - reine neue Tabellenzeilen genügen, keine Code-Änderung an der Kernlogik nötig. Kurven-Wachstumsverhältnis steigt jetzt linear von 1,16x auf 1,25x pro Stufe (vorher fiel es bis Level 30 kontinuierlich Richtung 1,14x) - Level 100 landet bei ~4,9 Billiarden XP. Dabei einen bereits bestehenden, bisher nie aufgefallenen Anzeigefehler gefunden+gefixt: `bkmpIdleFormatNumber()` kannte nur Abkürzungen bis 'M' (Millionen) - ein Wert wie Level 30s 14.300.000.000 XP erschien dadurch fälschlich als "14300M" statt "14,3B". Neue B/T/Qa-Stufen ergänzt (rückwärtskompatibel, K/M-Ausgabe für Werte <1 Mrd. unverändert). Vier neue Erfolgs-/Titel-Meilensteine für den erweiterten Bereich (Level 40/60/80/100, gleiches Namens-Verknüpfungsmuster wie die bestehenden 5/10/20er). 4 neue Tests (10x je 3 Projekte stabil). — neu: `sql/20260801-guild-level-cap-100.sql` (**noch nicht ausgeführt**), `tests/e2e/guild-level-cap-and-number-format.spec.js`; geändert: `js/core/bkmp-combat-math.js` (`bkmpIdleFormatNumber`), `js/systems/bkmp-guild.js` (4 neue Erfolge), `idledorf.js` (4 neue Titel) — 🟡 lokal

---

## 2026-07-31

- **[Neu]** Gilden-Technologie v3: kompletter Umbau von "Anführer/Stellvertreter kauft Ränge aus der Gildenkasse" auf ein Baum-System mit Mitglieder-Beiträgen (Nutzerwunsch nach Whiteout-Survival-Vorbild-Screenshots, per Rückfrage geklärt: komplett ersetzen, nur Gold, kein Paragon in v1). JEDES Mitglied trägt eigenes Gold zu einem gemeinsamen, Vorbedingungs-verketteten Fortschrittsbalken pro Knoten bei (keine Rollenprüfung mehr), 5 geteilte Beitragsversuche/Tag (`dungeon_regen_calc()` wiederverwendet), 19 Knoten (11 Wachstum/8 Schlacht) als echter verzweigender SVG-Baum mit Zusammenführungen. Katalog+Fortschritt leben jetzt in der Datenbank (`guild_tech_nodes`/`guild_tech_progress`) statt dreifach hardcodiert (SQL+JS-Katalog+Test-Mock) — die strukturelle Kernverbesserung dieses Umbaus. Effekt-Cache-Schlüssel 1:1 aus dem alten System übernommen, dadurch brauchten 7+ bestehende Konsumenten-Dateien (idledorf.js/bkmp-arena.js/bkmp-runes.js/bkmp-events.js/bkmp-prestige.js/bkmp-breeding.js/supabase.js) keine Änderung. `arena_attack()`/`raid_join()`/`api/claim-idle-offline-progress.js` (Kriegsrat/Stadtmauer/Nachtwache) mussten auf die neue Tabelle umgestellt werden, sonst wären sie beim Cutover still auf 0 zurückgefallen. Proportionale Alt-Daten-Migration (kein Gold-Refund, da die alte Gildenkasse kein individuell zurechenbares Spieler-Guthaben war) - bewusst als reiner Einmal-Bootstrap (`on conflict do nothing`, beim finalen Nochmal-Durchlesen selbst korrigiert, damit ein künftiges erneutes Ausführen der Datei nie echten Spielerfortschritt überschreiben kann. Beim eigenen Verdrahten mehrfach selbst gefunden+gefixt, bevor es auffiel: ein `disabled`-Attribut auf gesperrten Knoten hätte den vorgesehenen "hier fehlt noch X"-Hinweistoast unerreichbar gemacht (disabled-Buttons feuern keine Klick-Events) - entfernt, Sperr-Optik jetzt rein per CSS. Alte, jetzt überholte Test-Dateien (`guild-tech.spec.js`, große Teile von `guild-tech-ext.spec.js`) bewusst übersprungen statt gelöscht (dokumentieren die Herleitung des alten Systems); `guild-tech-ext-readside.spec.js`s Read-Side-Tests blieben größtenteils gültig, nur der Nachtwache-Block wurde auf die neuen Tabellen umgestellt. 27+9 (Backend) + 5 (UI, 50x stabil) neue Tests grün. **Finaler Gesamt-Regressionslauf (alle 3 Projekte): 1100 bestanden, 337 korrekt übersprungen, 0 Fehlschläge, Exit-Code 0, 52,2 Minuten** - `npm run qa:static` (eslint/npm audit/statische Analyse) ebenfalls sauber. — neu: `sql/20260731-guild-tech-tree-v2-foundation.sql` (**noch nicht ausgeführt**), `sql/20260731-changelog-guild-tech-tree.sql` (**noch nicht ausgeführt**), `js/systems/bkmp-guild-tech.js`, `tests/e2e/guild-tech-tree.spec.js`, `tests/e2e/guild-tech-tree-migration.spec.js`, `tests/e2e/guild-tech-tree-ui.spec.js`; geändert: `js/systems/bkmp-guild.js` (alter Katalog/Panel entfernt), `js/ui/bkmp-ui-components.js` (`bkmpUiGuildTechTreeHtml`), `supabase.js`, `api/claim-idle-offline-progress.js`, `style.css`, `index.html`, `idle-stream-mini.html`, `scripts/static-checks.js`, `package.json`, `tests/mock/rpc-engine.js`, `tests/FEATURE_MATRIX.md`, `tests/e2e/guild-tech.spec.js`/`guild-tech-ext.spec.js`/`guild-tech-ext-readside.spec.js` (übersprungen/angepasst) — 🟡 lokal

---

## 2026-07-30

- **[Fix]** Arena zeigte fälschlich noch übrige Angriffe an, obwohl der Server das Tageslimit bereits korrekt blockierte (Spieler-Report BagonTr01: "Ich habe noch 10 Angriffe übrig und bekomme die Meldung dass ich keine mehr habe"). Ursache: die Anzeige schätzte "Angriffe heute" bisher aus den geladenen letzten 15 Kämpfen - seit Kriegsrat (Gilden-Technologie v2) das Tageslimit über 15 anheben kann, blieb diese Schätzung bei 15 hängen, sobald ein Spieler an einem Tag öfter angriff. Fix: echte, ungedeckelte Serverabfrage statt einer Schätzung aus der Anzeige-Liste. Dabei einen zweiten, unabhängigen Bug in der eigenen Test-Mock-Infrastruktur gefunden+gefixt (`gte`/`lt`-Zeitstempel-Filter waren dort nie korrekt implementiert - reine Zahlenumwandlung, die bei Datums-Strings immer fehlschlug). Neuer Regressionstest (10x + 3 Projekte grün). — `supabase.js`, `js/systems/bkmp-arena.js`, `tests/mock/rest-engine.js`, `tests/e2e/guild-tech-ext.spec.js` — 🟡 lokal
- **[SQL]** Rein lesende Diagnose für einen erneuten Runenverlust-Report (BagonTr01: "Mal wieder eine Rune nach dem Prestige weggebauggt") vorbereitet - Prestige-Reset selbst fasst Runen im Code nachweislich nicht an, einzig plausible bekannte Erklärung sind die neuen Automatiken "Auto-Aufstieg"/"Auto-Verschmelzung" (verbrauchen bewusst Runen, aber nur nach explizitem Einschalten durch den Spieler) - lässt sich per SQL allein nicht bestätigen, da diese Schalter rein lokal (Browser) gespeichert sind. — `sql/20260729-diagnose-bagontr01-rune-loss-post-prestige.sql` (neu, rein lesend) — 🟡 lokal
- **[Fix]** Der Runen-"Einsetzen"-Button wurde auf üblichen Desktop-Fensterbreiten vom geöffneten Kraftrune-Lager verdeckt - Spieler mussten das Lager jedes Mal erst schließen (Spieler-Idee OPShadowWolf: "Einsetzbutton weiter nach links verschieben"). Statt den Button nur um feste Pixel zu verschieben, reserviert die Hauptzeile jetzt per live berechneter CSS-Variable exakt so viel Rand-Abstand, wie der Balken tatsächlich überlappen würde - bleibt bei jeder Fensterbreite erreichbar. Bewiesen: 258px Überlappung ohne Fix, sauber getrennt mit Fix (1280×720). — `js/systems/bkmp-runes.js`, `style.css`, `tests/e2e/runes.spec.js`, `tests/e2e/save-load.spec.js` (alter Workaround entfernt) — 🟡 lokal
- **[Neu]** Auto-Kauf kann jetzt gezielt Ressourcentypen ausschließen (z.B. "alles außer Gold", während man gerade darauf spart) - Spieler-Idee Fightmaria. Checkboxen im Upgrades-Tab, rein lokal gespeichert. — `idledorf.js`, `style.css`, `tests/e2e/prestige-automation-remaining-skills.spec.js` — 🟡 lokal
- **[Neu]** "Automatische Verteilung" im Prestige-Baum respektiert jetzt eine einstellbare Zweig-Priorität (Auf/Ab-Knöpfe) statt nur nach reinem Preis zu kaufen - Spieler-Idee Kaledoss, da der auf 52 Knoten gewachsene Baum sonst zu repetitiv wird. — `js/systems/bkmp-prestige.js`, `style.css`, `tests/e2e/prestige-automation-remaining-skills.spec.js` — 🟡 lokal
- **[Neu]** Neuer Tab "🏆 Clan": Clan-Bestenliste (alle Gilden nach Level/XP sortiert, braucht keine neue Tabelle) + Clan-Arena (asynchroner Gilde-gegen-Gilde-Kampf, direkter Port der bewährten Spieler-Arena - Macht = Summe aller Mitglieder-Kampfwerte, Sieg füllt die Kasse, Niederlage kostet nur Rating, nur Anführer/Stellvertreter dürfen angreifen, 3 Angriffe/Tag) - Spieler-Idee Kaledoss. Dabei einen dritten Mock-Fidelity-Bug gefunden+gefixt: `.or()`-Filter wurden im Test-Mock nie wirklich ausgewertet. 33 neue Tests grün. — `supabase.js`, `js/systems/bkmp-guild.js`, `idledorf.js`, `index.html`, `idle-stream-mini.html`, `style.css`, `tests/mock/rpc-engine.js`, `tests/mock/rest-engine.js`, `sql/20260730-guild-arena.sql` (neu, **noch nicht ausgeführt**), neu: `tests/e2e/guild-clan-arena.spec.js` — 🟡 lokal
- **[SQL]** Vorbereitete, fertig ausgefüllte Insert-Zeilen für das öffentliche Changelog-Panel (4 Einträge für die obige Feedback-Board-Runde) — `sql/20260730-changelog-entries.sql` (neu, idempotent, **noch nicht ausgeführt**) — 🟡 lokal
- **[Fix]** `sw.js` (reiner Passthrough-Service-Worker für "Als App installieren") wandelte jeden vorübergehenden Netzwerk-Haenger/Abbruch bisher in einen unbehandelten Fehler um ("Uncaught (in promise) TypeError: Failed to fetch", live auf bkinvestment.de beobachtet - gleiches Fehlermuster wie beim Runenverlust-Fund vom 25.07.). `.catch()` liefert jetzt stattdessen eine normale Netzwerkfehler-Response, für den Browser ununterscheidbar davon, als hätte gar kein Service Worker dazwischengehangen. — `sw.js` — 🟡 lokal
- **[Änderung]** Prestige-Panel umgebaut (Spieler-Beschwerde: "30 Meter scrollen für die Punktevergabe") - der Baum zum Punkte-Ausgeben steht jetzt direkt unter dem Fortschrittsbalken statt hinter acht Info-Blöcken; die reinen Erklär-Blöcke (Du erhältst/Reset-Keep/Nächster Durchlauf/Meilensteine) leben jetzt standardmäßig eingeklappt in einem Akkordeon darunter. Der Bestätigungsdialog vor dem eigentlichen Aufstieg zeigt die Reset/Bleibt-erhalten-Liste unverändert ein zweites Mal. 37 bestehende Tests grün. — `js/systems/bkmp-prestige.js`, `style.css` — 🟡 lokal
- **[Neu]** Neuer Sammel-Button "⚡ Auf +15 maximieren" im Runen-Lager - wertet alle unausgerüsteten Legendären des aktuellen Rüstungsplatzes automatisch bis +15 auf (gleiche Kosten/Fehlschlagchance wie manuelles Klicken, nur ohne wiederholtes Klicken) - Spieler-Idee BagonTr01 ("nach AFK teilweise ne Stunde zum Durchklicken"). Ergänzt die bestehende Prestige-Automatik, ersetzt sie nicht. 5 neue Tests grün, Regressionslauf sauber. — `js/systems/bkmp-runes.js`, `style.css`, neu: `tests/e2e/rune-maximize-legendary.spec.js` — 🟡 lokal
- **[Neu]** Clan-Arena hat jetzt eine Kampfanimation (Gilden-Tag-Abzeichen statt Dorf-Skin, da eine Gilde kein einzelnes eigenes Dorf hat) - Nachbesserung, nachdem ein Spieler nach dem ersten echten Angriff fragte, ob etwas kaputt war. Gleiches Timing/HP-Balken/Schadenszahlen-Muster wie die bestehende Spieler-Arena-Animation. 2 neue Tests grün (Sieg + Niederlage), Regressionslauf sauber. — `js/systems/bkmp-guild.js`, `index.html`, `style.css`, `tests/e2e/guild-clan-arena.spec.js` — 🟡 lokal
- **[Änderung]** "Clan"-Feature auf Nutzerwunsch in "Gilden-Arena" umbenannt (Konsistenz mit dem Rest des Spiels, das durchgehend "Gilde" nutzt) - nur sichtbare Texte geändert (Tab-Button, Überschriften, Toast/Log-Meldungen, Kampfanimations-Titel), interne Funktions-/CSS-/ID-Namen bewusst unangetastet. Regressionslauf sauber (35/35). — `js/systems/bkmp-guild.js`, `index.html`, `idle-stream-mini.html`, `tests/e2e/guild-clan-arena.spec.js`, `sql/20260730-changelog-entries.sql` — 🟡 lokal
- **[SQL]** Server-seitige Tempo-Grenze gegen Timer-Speedhacks/Zustands-Manipulation (Spieler-Meldung, Video-Beweis für "HTML5 Universal Speed Hack" + Cheat Engine) - neuer Postgres-Trigger auf `idle_player_state` kürzt implausible Kills-/Level-/Ressourcen-Sprünge anteilig auf das per echter Wanduhrzeit maximal mögliche Maß, statt sie ungeprüft zu speichern; loggt jeden Fall in eine neue Tabelle. Keine Client-Datei geändert (rein serverseitig), kein Cache-Busting nötig. Retroaktive Kontenprüfung ergab keine belastbare Verdächtigen-Liste (Kennzahl als unzuverlässig erkannt+verworfen, ausführlich in CLAUDE.md dokumentiert) - nur die vorwärtsgerichtete Sperre wurde umgesetzt. 4 neue Tests grün, Regressionslauf sauber. — neu: `sql/20260730-idle-player-state-anticheat-guard.sql` (**noch nicht ausgeführt**), `tests/mock/anticheat-guard.js`, `tests/e2e/anticheat-progress-rate-guard.spec.js`, geändert: `tests/mock/rest-engine.js`, `package.json` — 🟡 lokal
- **[Fix]** Gilden-Technologie: Paragon-Ränge trugen bereits korrekt zum echten Spieleffekt bei, aber die "+X%"-Kartenanzeige zeigte den Paragon-Beitrag nie an ("Level Paragon hoch aber am Wert ändert sich nichts") - alle betroffenen Karten (9 ursprüngliche + 8 der 10 neuen Zweige) lesen jetzt denselben bereits Paragon-inklusiven Bonus-Cache statt selbst neu zu rechnen. Neuer UI-Regressionstest bewiesen (kein bestehender RPC-Test hätte den Bug je gefunden). — `js/systems/bkmp-guild.js`, `tests/e2e/guild-tech.spec.js` — 🟡 lokal
- **[Fix]** Info-Tooltip (Paragon-Erklärung im Prestige-Baum) hatte eine hässliche, sehr schmale Form (ein Wort pro Zeile) statt eines normalen Rechtecks ("Das einfach in einen Rechteck Fenster bitte. Sieht dumm aus."). Ursache: `position:absolute` mit nur `max-width` (ohne eigene `width`) schrumpft in dieser Kombination auf die Breite des längsten EINZELNEN Worts statt natürlich bis zur Kappungsgrenze zu wachsen. Fix: `width: max-content` ergänzt - kurze Tooltips bleiben kompakt einzeilig, lange wachsen jetzt sauber zu einem echten, bis 240px breiten Rechteck. Per Browser-Screenshot verifiziert (kein echter Login nötig, reine CSS-Struktur). — `style.css` — 🟡 lokal
- **[Fix]** Dungeon-Schlüssel-Anzeige zeigte trotz Schlüsselbund-Prestige-Bonus weiterhin die alte, feste Obergrenze "5" (Spieler-Screenshot: "Ei-Dungeon Schlüssel: 7/5") UND der Sekunden-Countdown blieb für den betroffenen Dungeon-Typ dauerhaft eingefroren, sobald der Bestand über 5 stieg - der SERVER akkumulierte längst korrekt bis zum echten, höheren Deckel (bereits per bestehendem Test bewiesen), nur die Client-Anzeige (`bkmpDungeonKeyLineHtml()`) und der Countdown-Ticker-Gate (`bkmpDungeonStartCountdownTicker()`) nutzten noch die alte feste Konstante statt des tatsächlichen, Schlüsselbund-inklusiven Maximalwerts. Fix: neue `bkmpDungeonEffectiveKeyMax()` (identische Formel wie die Server-RPC, `bkmpPrestigeBonus('dungeon_key_cap_bonus')`-basiert) an allen 4 betroffenen Stellen eingesetzt. Neuer Regressionstest beweist sowohl den Anzeigetext als auch den nicht mehr einfrierenden Ticker (45/45 stabil in Wiederholungen). — `js/systems/bkmp-dungeon.js`, `tests/e2e/dungeon-key-prestige-bonus.spec.js` — 🟡 lokal
- **[Änderung]** Schlüsselbund-Prestige-Knoten: Maximalrang von 50 auf 3 gesenkt (Nutzerwunsch nach Rückfrage: "Nur Maximalrang auf 3 senken", reiner Grind-Fix) - `effectPerRank`/Kosten-Basis unverändert, betrifft nur diesen einen Knoten (nicht die geteilte WEAK-Kostenkurve). Bereits investierte höhere Ränge (Screenshot zeigte "Rang 10/50") bleiben unangetastet voll wirksam, nur weitere normale Käufe über Rang 3 hinaus sind nicht mehr möglich - danach greift automatisch der bestehende Paragon-Pfad. Neuer Test beweist Kauf-Stopp bei Rang 3 + unveränderten Bestandsbonus, 88/88 grün im Regressionslauf. — `js/systems/bkmp-prestige.js`, `tests/e2e/prestige-tree-v2.spec.js` — 🟡 lokal
- **[Fix/Änderung]** Fairness-Nachbesserung, gleicher Tag: der neue niedrige Schlüsselbund-Maximalrang (siehe oben) machte den Paragon-Pfad für Spieler mit bereits höherem Bestandsrang sofort erreichbar, was der Nutzer live in seinem eigenen Account bemerkte ("Rang 10 (Max)" + schon "Paragon-Rang 3/1K"). Auf Nutzerwunsch "Paragon dort entfernen... für alle die dort bereits investiert haben zurückgeben deren prestige Punkte": Paragon für diesen Knoten strukturell entfernt (`paragonEligible:false`), neue `bkmpPrestigeMigrateSchluesselbundDowngrade()` kappt bei jedem Login automatisch auf Rang 3 und erstattet die überschüssigen Punkte (normale Ränge 4+ UND alle Paragon-Ränge) exakt zurück - Rückerstattung nutzt dieselbe Kostenformel wie der ursprüngliche Kauf, kein Schätzwert. Idempotent, läuft automatisch, kein Spieler-Zutun nötig. 4 neue/aktualisierte Tests (inkl. Integrationstest über den echten Login-Ablauf), 101/101 grün im breiten Regressionslauf. — `js/systems/bkmp-prestige.js`, `idledorf.js`, `tests/e2e/prestige-tree-v2.spec.js` — 🟡 lokal

---

## 2026-07-28

- **[Neu]** Investor-Auszahlungen jetzt dokumentierbar: neue "Ausgezahlt"-Checkbox im Admin-Panel + Upload eines Auszahlungsbeweis-Fotos. Auf der Website erscheint das Foto als kleines anklickbares Vorschaubild direkt neben dem Investorennamen, vergrößerbar per Klick. Sobald ein Investor als ausgezahlt markiert ist, zeigt seine Karte zusätzlich statt der laufenden "Aktueller Anteil"-Kachel einen kurzen Zusammenfassungssatz in Vergangenheitsform ("War Investor von … bis … und hat … verdient …"). Anlass: erste reale Investor-Auszahlung (CrazyMcNugget). — `admin.html`, `js/core/bkmp-site.js`, `supabase.js`, `index.html`, `style.css`, `sql/20260728-investor-payout-proof.sql` (neu, **noch nicht ausgeführt**) — 🟡 lokal
- **[Neu]** Drei neue Runen-Hintergrund-Automatiken (Nutzerwunsch nach Klärung eines vermeintlichen Bugs bei "Automatische Runenaufwertung" — die lief korrekt, hatte bei komplett auf +15 ausgerüsteten Runen nur nichts mehr zu tun): Auto-Legi-Aufwertung (wertet auch unausgerüstete legendäre Runen automatisch auf +15 auf), Auto-Aufstieg (lässt passende +15-Legendäre-Paare automatisch auf +16+ aufsteigen, verbraucht dabei eine zweite Legendäre) und Auto-Verschmelzung (verschmilzt automatisch vollständige +0-Dreiergruppen). Alle drei als eigene Ein/Aus-Schalter direkt im Runen-Tab, gekoppelt an den bestehenden Prestige-Knoten "Automatische Runenaufwertung", mit einer einmaligen Bestätigung beim Einschalten (nicht bei jedem Lauf). Nutzen ausschließlich bereits bestehende, manuell getestete Aktionen — keine neue Spiellogik. 6 neue Tests, bestehende Runen-Tests unverändert grün. — `js/systems/bkmp-runes.js`, `idledorf.js`, `style.css` — 🟡 lokal
- **[Änderung]** Paragon-System besser erklärt (mehrfache Spieler-Nachfragen "was ist Paragon, wo bekomme ich das her"): kleiner "?"-Info-Button direkt an jeder Paragon-Zeile im Prestige-Baum erklärt jetzt per Tooltip, dass Paragon dieselben Prestige-Punkte kostet wie der normale Baum (keine eigene Währung) und nur 4% Bonus pro Rang gibt, dafür ohne Maximum. Zusätzlich ein einmaliger Hinweis-Toast, sobald der Meilenstein "Jenseits der Grenze" (350 investierte Punkte) das Paragon-System tatsächlich freischaltet, statt dass es kommentarlos erscheint. Milestone-Beschreibungstext ebenfalls ausführlicher. Nutzt die bereits fertigen, bisher ungenutzten UI-Bausteine `bkmpUiTooltipHtml`/`bkmpUiShowToast` (Phase 3) — erster echter Einsatz. — `js/systems/bkmp-prestige.js`, `style.css` — 🟡 lokal

---

## 2026-07-27

- **[Fix]** Beim systematischen Testen aller Prestige-Automations-Skills (Nutzerwunsch) gefunden: die Knoten "Schlüsselmeister"/"Schlüsselbund" (Zweig Runen & Dungeons) zeigten einen rohen internen Entwickler-Hinweis als Spieler-Text und waren trotz echter Kosten wirkungslos. Beim Fertig-Verdrahten zusätzlich gefunden: eine bereits live gelaufene SQL-Migration hatte versehentlich die feste, für alle Spieler gleiche Schlüssel-Regenerationszeit (Spielerwunsch vom 16.07.) stillschweigend wieder auf "rollend seit dem individuellen letzten Zeitpunkt" zurückgestellt — seit dem 26.07. unbemerkt live. Zusätzlich eine harte Datenbank-Obergrenze (max. 5 Schlüssel) gefunden, die Schlüsselbunds höheren Deckel sonst beim Speichern abgelehnt hätte. Alle drei in einer Migration behoben, 5 neue Tests. — `js/systems/bkmp-prestige.js`, `sql/20260727-fix-dungeon-regen-fixed-slots-and-wire-prestige.sql` (neu, **noch nicht ausgeführt**), `tests/mock/rpc-engine.js` — 🟡 lokal
- **[Fix]** **DRINGEND:** Automatische Ei-Ausbrütung (Prestige-Automation) konnte ein legendäres Ei über zwei freie Nester hinweg praktisch duplizieren (Discord-Meldung "Kaiser [Dustin]": "Auto brütet funktioniert aber die Eier werden trotzdem angezeigt das man sie nochmal brüten kann") — der Automations-Zweig wählte das "beste" Ei blind aus dem rohen Ei-Bestand statt (wie die manuelle Ansicht) bereits genestete Eier auszuschließen; ein zweites freies Nest bekam dadurch später dasselbe, noch nicht geschlüpfte Ei zugewiesen. Fix: Guard direkt in `bkmpDragonAssignEggToNest()` (schließt auch künftige Aufrufer mit ein) + Busy-Sperre über die gesamte Bestätigungsdauer + derselbe Ausschluss-Filter im Automations-Dispatcher. Per rot-vor/grün-nach-Test bewiesen (Fehler exakt reproduziert, dann behoben). — `js/systems/bkmp-breeding.js`, `idledorf.js` — 🟡 lokal

---

## 2026-07-26

- **[Änderung]** Direkter Folge-Wunsch ("Auch die dürfen erhöht werden etc."): die 9 URSPRÜNGLICHEN Gilden-Tech-Zweige (Angriff/Verteidigung/Gold/Kritchance/Kritischer Schaden/Bossschaden/Runenglück/Erfahrungsbonus/Prestigebonus) bekommen jetzt denselben Rebalance wie die 10 neuen — Maximalstufe 20→35, Kosten 200.000·1,4^Stufe→250.000·1,18^Stufe (identisch zu Brutbeschleuniger/Gildenschmiede), danach dieselbe Paragon-Fortführung. — `js/systems/bkmp-guild.js`, `tests/e2e/guild-tech.spec.js` — 🟡 lokal
- **[Änderung]** Gilden-Technologie: Maximalstufe + Kosten der 10 neuen Zweige deutlich angehoben, neue Paragon-Fortführung danach (Spieler-Feedback: bei 2,36 Mrd. Gold Kassenstand waren alle 10 mit nur ~40M komplett durchkaufbar). 8 der 10 Zweige (nicht die 2 Ein/Aus-Schalter) bekommen 3x-2,5x mehr Stufen + neu kalibrierte Kosten (zusammen jetzt ~4,36 Mrd. Gold zum Maxen statt ~40M), danach eine Paragon-Fortführung mit stark steigenden Kosten (max. Rang 1000, wie beim Spieler-Prestige-Paragon). 3 nachgelagerte Prozent-Deckel proportional mitangehoben, damit die neuen Stufen nicht wirkungslos verpuffen. — `js/systems/bkmp-guild.js`, `js/systems/bkmp-runes.js`, `js/systems/bkmp-prestige.js`, `style.css`, `tests/e2e/guild-tech-ext.spec.js` — 🟡 lokal
- **[SQL]** Für die Gilden-Tech-Rebalance (jetzt alle 19 Zweige, siehe Eintrag oben): `guild_tech_upgrade()` auf die neuen, höheren Maximalstufen/Kosten + 17 neue Paragon-tech_id-Einträge umgestellt. — `sql/20260726-guild-tech-rebalance-paragon.sql` (neu, **noch nicht ausgeführt**) — 🟡 lokal
- **[SQL]** **DRINGEND, live bestätigt behoben:** "Dungeon-Status konnte nicht geladen werden" bei mehreren Spielern gemeldet — verursacht durch die bereits vorbereitete `sql/20260726-dungeon-key-prestige-bonus.sql` (fügte `dungeon_regen_calc()` zwei neue Default-Parameter hinzu, legte dabei aber eine ZWEITE, überladene Fassung neben die alte statt sie zu ersetzen). Jeder bestehende 2-Parameter-Aufruf (u.a. aus `dungeon_get_all_status()`, betrifft jeden Spieler beim Öffnen des Dungeon-Tabs) wurde dadurch mehrdeutig und schlug fehl. Per direktem Live-Check gegen die echte Supabase-REST-API bestätigt (`PGRST203 - Could not choose the best candidate function`). Fix: alte, jetzt überflüssige 2-Parameter-Fassung entfernen — die neue deckt dank identischer Default-Werte jeden bestehenden Aufruf weiterhin gleich ab. — `sql/20260726-fix-dungeon-regen-calc-ambiguity.sql` — 🟢 live (vom Nutzer ausgeführt + bestätigt)
- **[Neu]** 10 neue Gilden-Technologie-Zweige (Kriegsrat/Turm-Vorreiter/Brutbeschleuniger/Gildenschmiede/Gilden-Autokauf/Nachtwache/Streak-Schutz/Stadtmauer/Aufstiegsvorbereitung/Willkommenspaket) — Ausgangsproblem: alle 9 bestehenden Zweige waren Stufe 20/20, die Gildenkasse hatte nichts mehr zu tun. Neues Kosten-/Maximalstufen-Tier-System (analog zum bestehenden Prestige-Tier-Muster) ersetzt die bisher für ALLE Zweige gleiche 20-Stufen-Kurve; die alten 9 Zweige bleiben dabei unverändert. Kriegsrat (+Arena-Tageslimit) und Stadtmauer (+Raid-Stadt-HP-Beitrag) ändern echte Server-RPCs, Nachtwache (+Offline-Deckel) ändert die Offline-Berechnung serverseitig, die übrigen 7 landen im bereits bestehenden Gilden-Bonus-Cache. Drei echte Anzeige/Realität-Mismatch-Bugs beim Verdrahten gefunden+gefixt (Arena-Panel/Fehlermeldung zeigte immer "10" statt des echten, ggf. erhöhten Limits; Aufstiegs-Hinweistext zeigte die undiscountierten Basiswerte). 16 neue Testfälle, 3 Projekte grün. — `js/systems/bkmp-guild.js`, `js/systems/bkmp-arena.js`, `js/systems/bkmp-breeding.js`, `js/systems/bkmp-runes.js`, `js/systems/bkmp-events.js`, `js/systems/bkmp-prestige.js`, `supabase.js`, `idledorf.js`, `api/claim-idle-offline-progress.js`, `style.css`, 2 neue `tests/e2e/*.spec.js` — 🟢 live (per Spieler-Screenshot bestätigt)
- **[SQL]** Für die 10 neuen Gilden-Technologie-Zweige: `guild_tech_upgrade()` auf ein Kosten-/Maximalstufen-Tier-System umgestellt (alte 9 Zweige unverändert), `arena_attack()`/`raid_join()` um Kriegsrat-/Stadtmauer-Bonus erweitert. — `sql/20260726-guild-tech-branches-v2.sql` — 🟢 live (per Spieler-Screenshot bestätigt)
- **[Neu]** Vollständige Erweiterung/Neubalancierung des langfristigen Progressionssystems (siehe `PROGRESSION_REBALANCE_PHASE1.md` für die volle Analyse+Simulation) — Ausgangsproblem per echten Produktionsdaten bewiesen: ein Level-2000-Spieler erreichte das alte Upgrade-Cap in 3 Minuten. Kernstücke: 1) alle 9 normalen Upgrade-Caps deutlich angehoben (500→2.500 / 5.000→20.000 / 100→150 bzw. 500) + zentrales Softcap-System (voller Bonus bis softCap1, abgeschwächter Grenznutzen + steilere Kosten danach — Feintuning per eigener Simulation korrigiert, damit "Softcap" nicht versehentlich zum harten Jahre-Stopp wird); 2) deterministische Upgrade-Meilensteine (25/50/100/500/1.000 Ränge, kein eigener Speicherzustand); 3) Prestige-Baum von 6 Knoten auf 5 Zweige × 10 Knoten + "Vermächtnis"-Zweig erweitert (alle 6 alten Knoten-IDs bleiben unverändert bestehen, bereits investierte Ränge behalten ihren vollen Bonus — additives JSONB, keine SQL-Migration nötig); 4) exponentielle Prestige-Kosten (ersetzt die alte lineare `cost=rang`-Formel); 5) Prestige-Meilensteine nach insgesamt investierten Punkten (schalten u.a. 2 Zweige + Paragon frei); 6) Paragon-System für volle Knoten (deutlich schwächerer Bonus, extrem steigende aber endliche Kosten, ausgeschlossen für Chance-basierte Knoten); 7) zweite Prestige-Ebene "Aufstieg" (Drachenseelen, eigene Währung, hohe Freischalt-Hürde, setzt den gesamten Prestige-Baum zusätzlich zurück) — ebenfalls ohne neue SQL-Spalte (reservierter JSONB-Schlüssel); 8) neue Gold-Senke "Gebäude-Überladung" (temporärer Produktionsboost, steigende Kosten bei Mehrfachnutzung); 9) Auto-Kauf skaliert jetzt mit Prestige-Investition und bevorzugt Upgrades außerhalb der Softcap-Zone. 732 neue/erweiterte Testfälle über mehrere neue Spec-Dateien, volle Regression grün. — `idledorf.js`, `js/systems/bkmp-prestige.js`, `js/systems/bkmp-dungeon.js`, `js/systems/bkmp-breeding.js`, `js/systems/bkmp-runes.js`, `js/core/bkmp-combat-math.js`, `js/ui/bkmp-hud.js`, `style.css` + mehrere neue `tests/e2e/*.spec.js` — 🟡 lokal
- **[SQL]** Vorbereitet für die Prestige-Knoten "Schlüsselmeister"/"Schlüsselbund" (schnellere Dungeon-Schlüssel-Regeneration/höheres Limit) — rückwärtskompatible neue optionale Parameter für `dungeon_regen_calc()`, alte Aufrufer unverändert. Die eigentliche Rang→Bonus-Verdrahtung folgt separat (Konsistenzrisiko, siehe Kommentar in der Datei) — Knoten sind bereits kaufbar, wirken aber noch nicht. — `sql/20260726-dungeon-key-prestige-bonus.sql` (neu, **noch nicht ausgeführt**) — 🟡 lokal
- **[Fix]** Stufenleisten-Buttons ("Automatisch"/"Beste Stufe"/"Stufe wählen") verschwanden nach einem Dungeon- oder Turm-Lauf dauerhaft (Nutzerscreenshot, fälschlich zunächst für die Boss-Belohnungsanzeige gehalten) — ein Fix vom 20.07. hatte das Wiedereinblenden nach Laufende ersatzlos entfernt, mit einer Begründung, die nur für den kompakten Mobil-Modus zutraf; auf normaler Desktop-Breite ist die betroffene Leiste aber die einzige sichtbare und blieb seitdem nach dem ersten Dungeon-/Turm-Lauf für den Rest der Sitzung unsichtbar. Fix: neue `bkmpIdleStageBarWantedVisible()` prüft dynamisch den echten aktuellen Zustand (identisches Muster wie der bereits dokumentierte Phase-7.3-Fund) — blendet auf Desktop korrekt wieder ein, bleibt im kompakten Modus weiterhin absichtlich versteckt. Per neuem Regressionstest (4 Fälle: Dungeon-Sieg, Turm-Niederlage, Turm-Aufgeben, kompakter Modus bleibt versteckt) bestätigt. — `idledorf.js`, `js/systems/bkmp-dungeon.js`, `js/systems/bkmp-tower.js` — 🟡 lokal
- **[Fix]** OBS-Stream-Overlay zeigte keinen Fortschritt mehr, wenn das Hauptfenster im Hintergrund lief (nur die Deko-Animationen bewegten sich) — Browser drosseln `setInterval` in einer wirklich verdeckten Registerkarte unabhängig vom Code ("Intensive Throttling"), der normale Kampf-Tick fiel bei längeren Streaming-Sessions dadurch faktisch aus. Fix: solange die Karte versteckt ist UND tatsächlich ein Overlay-Zuschauer verbunden ist (`bkmpCombatBroadcastHasListener`, Presence-gestützt), läuft ein eigenes, selteneres Intervall, das dieselbe bestehende Server-Nachhol-Berechnung wie beim normalen Offline-Claim nutzt und bei einem Stufenwechsel sofort neu broadcastet — kein Eingriff in den echten Kampf-Loop, keine zusätzliche Serverlast für nicht-streamende Spieler. — `idledorf.js`, `js/core/bkmp-idle-state.js` — 🟡 lokal
- **[Fix]** Buttons/Karten innerhalb offener Tabs wie Upgrades/Runen/Prestige/Dorf-Skins/Drachenzucht fühlten sich während aktivem Kampf träge an, während die Hauptnavigation flüssig blieb (Video-Beweis + Nutzer-Klarstellung) — diese 5 Panels bauten sich bei JEDEM Drachen-Kill komplett per `innerHTML` neu auf, solange sie offen waren; stand die Maus dabei gerade über einem Button (z.B. "Kaufen"), wurde der DOM-Knoten unter dem Cursor mitten in der Aktion ersetzt. Fix: Neu-Rendern wird jetzt verschoben, solange die Maus über dem Panel steht (`:hover` deckt per CSS-Spec automatisch alle Kind-Elemente ab, gleiches Prinzip wie das bestehende Pausieren des Streamer-Tickers bei Hover) — holt beim nächsten Kill oder sobald die Maus das Panel verlässt zuverlässig auf, keine Werte/Formeln verändert. — `idledorf.js` — 🟡 lokal
- **[SQL]** Speichern schlug ab Level 2001 dauerhaft fehl (Meldungen Danw_90 + Kaledoss) — eine seit der ursprünglichen Schema-Datei bestehende Datenbank-Regel begrenzte `level` hart auf maximal 2000, vermutlich als früher Sicherheitswert aus einer Zeit, in der niemand annähernd dort ankam. Kein Datenverlust, der zuletzt erfolgreich gespeicherte Stand blieb erhalten. Deckel auf 1.000.000 angehoben, dieselbe Änderung vorsorglich auch für `dragon_kills` (bisher `<=5.000.000`, noch nicht ausgelöst, aber exakt dasselbe Muster). — `sql/20260726-fix-level-cap-constraint.sql` (neu, **noch nicht ausgeführt**) — 🟡 lokal
- **[Fix]** "Auto-Schmelzen" im Runen-Lager erkannte vorhandene Runen nicht (zeigte "0"), obwohl "Lager aufräumen" dieselben Runen problemlos fand — Auto-Schmelzen filterte bisher ausschließlich aus dem lokalen, seit dem 25.07.-Ladefix auf 300 wertvollste Zeilen gekappten Bestand; genau die für Auto-Schmelzen benötigten unbearbeiteten +0-Runen werden bei sortiert-absteigender Kappung als Erstes verdrängt. Fix: neue gemeinsame `bkmpGetStoredMeltableRunes()` (immer frischer Serverabruf) wird jetzt von Auto-Schmelzen UND Lager-aufräumen genutzt — beide sehen ab sofort garantiert denselben Bestand. Keine automatische Verarbeitung neuer Runen eingebaut (bleibt bewusst eine reine Klick-Aktion), keine Spielregeln geändert. — `js/systems/bkmp-runes.js`, `supabase.js` — 🟡 lokal
- **[Fix]** Hover-Effekte auf Buttons/Karten/Tabs im gesamten Idle-Dorf reagierten spürbar verzögert/ruckelig (Video-Beweis) — die Hauptnavigation (alle 15 Tabs) animierte bei Hover/aktivem Tab bisher `top`, eine Layout-Eigenschaft, die bei jedem Bildschirmbild einen echten Reflow erzwingt statt nur zu compositen; zwei weitere Stellen (`.achievements-subtab`, `.btn`) nutzten pauschal `transition: all`. Auf `transform: translateY()` (GPU-komponiert) bzw. die tatsächlich genutzten Einzeleigenschaften umgestellt, identisches visuelles Ergebnis. Ehrlich gemessen: der Kampf-/Tick-Loop selbst war NICHT die Ursache (mit/ohne aktivem Loop praktisch identische Latenz) — der Fix bleibt rein CSS-seitig, keine Änderung an Render-/Tick-Funktionen. — `style.css` — 🟡 lokal
- **[Neu]** Öffentliches Changelog auf der Website (📜-Knopf neben "Status") — eigenständig, getrennt vom bestehenden Feedback-/Status-Board. Zeigt kurze, chronologische Einträge (Datum + Kategorie Fix/Neu/Änderung/Balance + Titel + Beschreibung) mit Filter-Chips. Neues Admin-Formular unter admin.html → "Changelog" zum Hinzufügen/Bearbeiten/Löschen. Genau diese Datei hier (`CHANGELOG.md`) ist die interne, technische Fassung für Entwicklung/Nachvollziehbarkeit — das neue Panel auf der Website ist die kurze, öffentliche Fassung für Spieler, beide unabhängig voneinander gepflegt. — `sql/20260726-changelog.sql` (neu, **noch nicht ausgeführt**), `supabase.js`, `js/ui/bkmp-changelog.js` (neu), `index.html`, `admin.html`, `style.css` — 🟡 lokal
- **[Fix]** Runen verschwanden nach normalem Neustart bei sehr großem Runen-Lager (bestätigt: 7.500–15.600 Zeilen bei zwei Spielern) — die Ladeanfrage war unbegrenzt und scheiterte bei jedem Netzwerkfehler, wobei sie das komplette sichtbare Inventar still auf leer zurücksetzte. Kein Datenverlust, keine SQL nötig. Fix: Laden in "ausgerüstet" (immer verlässlich) + "Lager, auf 300 wertvollste gekappt" aufgeteilt, kein stilles Leeren mehr (sichtbarer Hinweis + Retry-Knopf), neuer slot-übergreifender "Alle [Seltenheit] verkaufen"-Sammelverkauf als Präventionswerkzeug. — `supabase.js`, `idledorf.js`, `js/systems/bkmp-runes.js`, `js/core/bkmp-idle-state.js`, `style.css` — 🟡 lokal
- **[Änderung]** Minimieren-Button am Raidboss-Hinweisbanner entfernt (kaum Nutzen, wirkte optisch störend) — Timer/Anmeldestatus bleiben unverändert sichtbar. — `js/systems/bkmp-raid.js`, `style.css` — 🟡 lokal
- **[Fix]** Cache-Busting für `js/core/bkmp-idle-state.js` war bei einem früheren Fix nicht mitgezogen worden — Browser mit noch altem Cache-Stand warfen `ReferenceError: bkmpIdleFlushInFlight is not defined`. Version nachgezogen. — 🟡 lokal
- **[SQL]** `rename_player_account()` aktualisierte `idle_player_runes`/`idle_prestige_state`/`idle_player_village_skins` bisher nicht bei einer Namensänderung — nach einem Umbenennen wurden Runen/Prestige/Dorf-Skins unsichtbar (Daten blieben unter dem alten Namen unangetastet erhalten, kein echter Verlust). Migration + einmaliger Backfill für bereits betroffene Spieler. — `sql/20260725-fix-rename-name-key-propagation.sql` — 🟢 live (per Nachprüfung bestätigt)
- **[Fix]** 4 gemeldete Spieler-Bugs behoben: Runen-Aufstieg ohne Sofort-Speichern-Schutz, Edelstein-Dungeon-Belohnung rundete bei niedrigem Angriff auf 0, Erfolgs-Titel-Bonus wurde nirgends angezeigt, "Effekte An/Aus"-Schalter verschwand ~1s nach dem Öffnen (DOM-Knoten wurde beim zweiten HUD-Render zerstört). — `idledorf.js`, `js/core/bkmp-site.js`, `js/systems/bkmp-dungeon.js`, `js/systems/bkmp-runes.js`, `js/ui/bkmp-hud.js`, `style.css` — 🔵 gepusht

---

*Ältere Änderungen (vor dem 26.07.2026) sind nicht rückwirkend in dieses Format übertragen worden — die vollständige, ausführliche Phasen-/Bugfix-Historie ab dem 17.07.2026 steht weiterhin in `CLAUDE.md`.*
